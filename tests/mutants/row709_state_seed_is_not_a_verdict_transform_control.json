{
  "schema": "bd-mutate-spec/1",
  "_comment": "Transform control for row 709. This spec must report exactly one valid ESCAPED mutant: it applies M1's byte-identical seed-check-deleting transform while its only band node is the unmeasurable-population test, which measures a repository with no shell population at all and therefore never reaches the check. The escape proves the CAUGHT results in row709_state_seed_is_not_a_verdict.json are assertion failures rather than a SyntaxError, an import failure, a collection failure, or a transform that never applied.",
  "subject": "transform control for the row 709 seed-equals-outcome check",
  "band": [
    "tests/test_row709_state_seed_is_not_a_verdict.py::test_unmeasurable_population_is_unknown_not_clean"
  ],
  "mutants": [
    {
      "label": "TC1 the seed check is deleted under a node that never reaches it",
      "file": "tests/test_row709_state_seed_is_not_a_verdict.py",
      "old_regex": "(?m)^            if seed not in outcomes:$",
      "new": "            if True:  # mutant: skip every seed",
      "direction": "regression",
      "catcher": "tests/test_row709_state_seed_is_not_a_verdict.py::test_unmeasurable_population_is_unknown_not_clean"
    }
  ]
}
