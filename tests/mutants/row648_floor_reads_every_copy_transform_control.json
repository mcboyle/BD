{
  "schema": "bd-mutate-spec/1",
  "_comment": "Row 648c transform control. This deliberately must ESCAPE: it applies the identical M1 transform (hoisted-only lock read) while running only the prerelease test, which never calls _lock_copies. The escape proves the main battery's CAUGHT verdicts are assertion failures rather than invalid Python, an import failure, or a collection error.",
  "subject": "transform-only control for the hoisted-only lock read",
  "band": [
    "tests/test_frontend_dependency_security_floor.py::test_numeric_semver_rejects_prerelease_versions"
  ],
  "mutants": [
    {
      "label": "TC1 the identical M1 transform against a test that never reads the lock",
      "file": "tests/test_frontend_dependency_security_floor.py",
      "old": "        if _is_copy_of(key, suffix):\n",
      "new": "        if key == suffix:  # M1: hoisted copy only, nested copies unread (the 648b escape)\n",
      "direction": "regression",
      "catcher": "tests/test_frontend_dependency_security_floor.py::test_numeric_semver_rejects_prerelease_versions"
    }
  ]
}
