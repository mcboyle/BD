{
  "schema": "bd-mutate-spec/1",
  "_comment": "Row 293 transform control. This spec must report one valid ESCAPED mutant: it applies the byte-identical M1 signature regression while the only named test imports vpn_leak_tests without entering or asserting the IPv6 probe. An escape proves the primary CAUGHT verdict is an assertion failure rather than a parse, collection, or import break.",
  "subject": "the expected-address swallowing regression remains importable without a behavioral assertion",
  "band": [
    "tests/test_row_293_ipv6_measured_pass_is_reachable.py::test_transform_control_imports_probe_without_asserting_ipv6_behavior"
  ],
  "mutants": [
    {
      "label": "TC1 swallowed expected-address transform remains importable",
      "file": "bulk_downloader/vpn_leak_tests.py",
      "old": "def _probe_ipv6(\n    socks_port: int,\n    expected_exit_ip: Optional[str] = None,\n    **_,\n) -> ProbeResult:",
      "new": "def _probe_ipv6(socks_port: int, **_) -> ProbeResult:\n    expected_exit_ip = None  # mutant drops provider-supplied exit evidence",
      "direction": "regression",
      "catcher": "tests/test_row_293_ipv6_measured_pass_is_reachable.py::test_transform_control_imports_probe_without_asserting_ipv6_behavior"
    }
  ]
}
