{
  "schema": "bd-mutate-spec/1",
  "_comment": "Transform control for row 670: the M1 injection-guard inversion is banded on a test that imports the verifier and drives only load_session, never a render, so it MUST ESCAPE. Its escape proves the CAUGHT rows in row670_verifier_session.json are assertion failures, not compile breaks.",
  "subject": "transform control for the session injection seam",
  "band": [
    "tests/test_row670_verifier_carries_a_session.py::test_load_session_normalises_shapes_without_rendering"
  ],
  "mutants": [
    {
      "label": "CONTROL injection guard inverted outside the asserted path",
      "file": "bulk_downloader/template_selector_verifier.py",
      "old_regex": "(?m)(?<=^        if )(?=storage_state is not None:$)",
      "new": "not ",
      "direction": "regression",
      "catcher": "tests/test_row670_verifier_carries_a_session.py::test_load_session_normalises_shapes_without_rendering"
    }
  ]
}
