{
  "schema": "bd-mutate-spec/1",
  "_comment": "Row 297 transform control. This spec must report one valid ESCAPED mutant: it applies the byte-identical M1 census-loop regression while the only named test imports the module and resolves the census helper without executing it or asserting its behavior.",
  "subject": "the empty synthesized-parameter transform remains importable without a behavioral assertion",
  "band": [
    "tests/test_ct1_corpus_validation.py::TestCorpusPresent::test_transform_control_imports_census_without_asserting_behavior"
  ],
  "mutants": [
    {
      "label": "TC1 empty credential-census transform remains importable",
      "file": "tests/test_ct1_corpus_validation.py",
      "old": "        metrics[\"requests\"] += len(syn[\"requests\"])\n        for request in syn[\"requests\"]:",
      "new": "        metrics[\"requests\"] += len(syn[\"requests\"])\n        for request in ():",
      "direction": "regression",
      "catcher": "tests/test_ct1_corpus_validation.py::TestCorpusPresent::test_transform_control_imports_census_without_asserting_behavior"
    }
  ]
}
