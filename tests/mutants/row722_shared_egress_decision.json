{
  "schema": "bd-mutate-spec/1",
  "_comment": "Row 722 shared-egress decision record. The value-free regex matches only the structural field name after the required question key. Re-run with toolchain/bin/bd-mutate --spec tests/mutants/row722_shared_egress_decision.json --work DETACHED_SCRATCH.",
  "subject": "the shared-egress question carries a nonempty decision",
  "band": [
    "tests/test_row722_operator_decision.py::test_row722_records_both_operator_decisions_with_reasons"
  ],
  "mutants": [
    {
      "label": "M1 remove the shared-egress decision field",
      "file": "project-knowledge/FLEET_EGRESS_DECISIONS.md",
      "new": "Disposition",
      "direction": "regression",
      "catcher": "tests/test_row722_operator_decision.py::test_row722_records_both_operator_decisions_with_reasons",
      "old_regex": "(?s)(?<=### Distinct per-host egress\\n\\n)Decision(?=:)"
    }
  ]
}
