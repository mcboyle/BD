{
  "schema": "bd-mutate-spec/1",
  "_comment": "Standalone transform control: a behaviour-preserving parenthesisation of the adapter's own argument. Nothing observable changes, and the band is the SINGLE import-only test, so it MUST ESCAPE. A CAUGHT control would mean the row 673 battery is anchored on source identifiers rather than behaviour -- repair the battery, never delete the control.",
  "subject": "CONTROL template_assist: parenthesise the login group argument",
  "band": [
    "tests/test_row673_reviewed_probe_adapter_ships_once.py::test_row673_transform_control_import_only"
  ],
  "mutants": [
    {
      "label": "CONTROL template_assist: parenthesise the login group argument",
      "file": "bulk_downloader/template_assist.py",
      "old_regex": "(?m)(?<=^    login = selector_group\\()template(?=, \"login\"\\)$)",
      "new": "(template)",
      "direction": "regression",
      "catcher": "tests/test_row673_reviewed_probe_adapter_ships_once.py::test_row673_transform_control_import_only"
    }
  ]
}
