{
  "schema": "bd-mutate-spec/1",
  "_comment": "Row 664 transform control. These byte-identical mutations run against an import-only test. Both must ESCAPE, proving the main CAUGHT verdicts come from behavioral assertions rather than invalid transforms or import failures.",
  "subject": "transform controls for the dedup history-write and worker-dispatch mutations",
  "band": [
    "tests/test_row664_dedup_refusal_reaches_history.py::test_runner_import_transform_control"
  ],
  "mutants": [
    {
      "label": "M1 remove the dedup refusal history write under import-only coverage",
      "file": "bulk_downloader/runner.py",
      "old_regex": "(?m)(?<=^            )db_log(?=\\(self\\.site_id, self\\.config\\.get\\(\"name\", \"\\?\"\\), url,\\n                   \"skipped_duplicate\", \"\", 0, _dup, bytes_fetched=0\\)$)",
      "new": "(lambda *args, **kwargs: None)",
      "direction": "regression",
      "catcher": "tests/test_row664_dedup_refusal_reaches_history.py::test_runner_import_transform_control"
    },
    {
      "label": "M2 bypass worker dispatch under import-only coverage",
      "file": "bulk_downloader/runner.py",
      "old_regex": "(?m)(?<=^            )self\\._process_one(?=\\(browser, url, persistent_ctx=persistent_ctx\\)$)",
      "new": "(lambda *args, **kwargs: None)",
      "direction": "regression",
      "catcher": "tests/test_row664_dedup_refusal_reaches_history.py::test_runner_import_transform_control"
    }
  ]
}
