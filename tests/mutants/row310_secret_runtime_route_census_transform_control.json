{
  "schema": "bd-mutate-spec/1",
  "_comment": "Transform control for row 310. TC1 is byte-identical to primary M1, but its sole test imports this gate without calling the route concretizer or asserting route coverage. It must report exactly one valid ESCAPED mutant; CAUGHT, INVALID, ERROR, or UNKNOWN would not prove the primary assertion failures are behavioral.",
  "subject": "transform control -- importing the secret-display gate does not constrain its runtime-route census",
  "band": [
    "tests/test_secret_display_never.py::test_transform_control_imports_secret_gate_without_building_spa"
  ],
  "mutants": [
    {
      "label": "TC1 multi-argument route omission escapes an import-only test",
      "file": "tests/test_secret_display_never.py",
      "old": "    for rule in rules:\n        values = dict(rule.defaults or {})",
      "new": "    for rule in (rule for rule in rules if len(rule.arguments) <= 1):\n        values = dict(rule.defaults or {})",
      "direction": "regression",
      "catcher": "tests/test_secret_display_never.py::test_transform_control_imports_secret_gate_without_building_spa"
    }
  ]
}
