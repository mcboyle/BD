{
  "schema": "bd-mutate-spec/1",
  "_comment": "Standalone transform control: a behaviour-preserving parenthesisation of the page-state parser's input. Nothing observable changes, and the band is the SINGLE import-only test, so it MUST ESCAPE. A CAUGHT control would mean the row 674 battery is anchored on source identifiers rather than behaviour -- repair the battery, never delete the control.",
  "subject": "CONTROL build_recorded_dom_fixture: parenthesise the page-state parser input",
  "band": [
    "tests/test_row674_live_state_round_trips.py::test_row674_transform_control_import_only"
  ],
  "mutants": [
    {
      "label": "CONTROL build_recorded_dom_fixture: parenthesise the page-state parser input",
      "file": "tools/build_recorded_dom_fixture.py",
      "old_regex": "(?m)(?<=^    parser\\.feed\\()html(?=\\)$)",
      "new": "(html)",
      "direction": "regression",
      "catcher": "tests/test_row674_live_state_round_trips.py::test_row674_transform_control_import_only"
    }
  ]
}
