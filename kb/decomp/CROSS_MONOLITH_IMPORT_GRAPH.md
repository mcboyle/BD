# CROSS-MONOLITH IMPORT GRAPH (generated)

Generated by `tools/cross_monolith_graph.py` from `/home/claude/work`. Do not hand-edit -- regenerate. Classifies every inter-module import as **module-level** (cycle-risk) or **lazy** (in-function -> safe).

## The four-monolith subgraph

| src | dst | module-level | lazy | edge is |
|---|---|---:|---:|---|
| app | dev_suite | 0 | 124 | lazy (safe) |
| dev_suite | app | 0 | 16 | lazy (safe) |
| runner | app | 0 | 10 | lazy (safe) |
| app | runner | 1 | 3 | **MODULE-LEVEL (hazard if cyclic)** |
| app | deep_detect | 0 | 2 | lazy (safe) |
| dev_suite | runner | 0 | 1 | lazy (safe) |
| runner | deep_detect | 0 | 1 | lazy (safe) |

## Cycles

- Strongly-connected components (ALL edges) touching a monolith: [['app', 'dev_suite', 'runner']]
- SCCs in the **module-level-only** graph touching a monolith: NONE

**INVARIANT HOLDS:** every monolith import cycle is broken by lazy (in-function) imports -- the module-level-only graph among the monoliths is **acyclic**. No inter-monolith cycle would fail at load today.

## The decomposition rule this enforces

Every plan's "keep inter-module imports lazy / absolutize but stay in-function" instruction is load-bearing **because of these cycles**. When a split moves a function carrying a `from . import <sibling>` import:

1. transform it to absolute (`from bulk_downloader import <sibling>`), and
2. **keep it inside the function** (lazy). Never hoist it to module scope.

A hoist that creates a module-level edge closing one of the cycles above fails at import time. Re-run `--check` from the extracted zip after any split that touches a monolith; it exits 1 on a module-level cycle.

## Module-level inter-monolith edges (the watch-list)

- `app` imports `runner` at module level (1x) -- safe only while `runner` does not module-level-import back into `app`'s cycle.
