# Config reference

Generated by `tools/gen_reference_docs.py`. Validators: `validate_sites_config.py`, `validate_global_config.py`, `validate_templates.py`.

# Generated schema reference

Auto-generated by `tools/document_schemas.py` from source — do not hand-edit.

## 1. SQLite schema (from `bulk_downloader/db.py`)

### `history`

- `id INTEGER PRIMARY KEY AUTOINCREMENT`
- `site_id TEXT, site_name TEXT, url TEXT, status TEXT`
- `filename TEXT, file_size INTEGER, message TEXT, screenshot TEXT`
- `honeypot_score REAL DEFAULT NULL`
- `ts TEXT DEFAULT(strftime('%Y-%m-%dT%H:%M:%S','now'))`

### `queue`

- `site_id TEXT NOT NULL`
- `url TEXT NOT NULL`
- `status TEXT NOT NULL DEFAULT 'pending'`
- `message TEXT DEFAULT ''`
- `retries INTEGER DEFAULT 0`
- `retry_after REAL DEFAULT 0`
- `screenshot TEXT DEFAULT ''`
- `force_download INTEGER DEFAULT 0`
- `priority TEXT DEFAULT ''`
- `ord INTEGER DEFAULT 0`
- `filename TEXT DEFAULT ''`
- `file_size INTEGER DEFAULT 0`
- `ts_added TEXT DEFAULT(strftime('%Y-%m-%dT%H:%M:%S','now'))`
- `ts_updated TEXT DEFAULT(strftime('%Y-%m-%dT%H:%M:%S','now'))`
- `PRIMARY KEY(site_id, url)`

### `push_subscriptions`

- `endpoint TEXT PRIMARY KEY`
- `p256dh TEXT NOT NULL`
- `auth TEXT NOT NULL`
- `user_agent TEXT DEFAULT ''`
- `created_at TEXT DEFAULT(strftime('%Y-%m-%dT%H:%M:%S','now'))`
- `last_sent_at REAL DEFAULT 0`

### `session_history`

- `id INTEGER PRIMARY KEY AUTOINCREMENT`
- `ts REAL NOT NULL`
- `site_id TEXT NOT NULL`
- `account_idx INTEGER`
- `event_type TEXT NOT NULL`
- `detail TEXT DEFAULT ''`

### Indexes

- `idx_hist_site_ts` on `history(site_id, ts DESC)`
- `idx_hist_status` on `history(status)`
- `idx_hist_url` on `history(url)`
- `idx_q_site_status` on `queue(site_id, status)`
- `idx_q_site_ord` on `queue(site_id, ord)`
- `idx_q_status` on `queue(status)`
- `idx_sh_site_ts` on `session_history(site_id, ts DESC)`
- `idx_sh_event` on `session_history(event_type)`

