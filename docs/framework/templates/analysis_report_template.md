# Analysis report template

Structural template for an analyst's per-capture(-pair) report. Descriptive only; no
validation claim is asserted here — claims are proposed via the corpus-entry template and
decided by the reviewer.

## Subject
- site / title:
- capture(s) analyzed (paths, query-stripped):
- DOM log present: yes/no (gates selector analysis)

## Template validation (goal-URL/rendition/signing)
- verdict: matched / partial / drift (which axes)
- notes (signing by marker NAME only; no values):

## Selector recognition (if DOM present)
- top candidate selectors by confidence:
- selectors stable across the pair vs. drifting:

## Rendition intelligence
- descriptors observed (path identifiers only):
- highest available observed (operator-confirmed):

## Drift
- axes drifted (identity/rendition/signing-marker/structural):
- severity (cosmetic/moderate/breaking):
- one-off vs. repeated (cite history):

## Confidence & calibration
- recognition confidence by axis:
- calibration note (predicted vs. observed, if outcomes available):

## Proposed corpus entry (DRAFT — for reviewer)
- attach the draft corpus entry (no id, outcome untested).

## Posture check
- confirm: no signing values in this report; no replay/executable content; nothing acted on.
