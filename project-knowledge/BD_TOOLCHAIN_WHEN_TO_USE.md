<!-- verified-against: v3.66.805 -->
# BD toolchain -- WHEN to use what

Generated by `bd-tools --emit-doc` (derived from the live bin/, never hand-kept).
A category's TRIGGER is the 'when'; each tool's line is the 'what'.

## Bootstrap / provisioning
**WHEN:** SESSION START -- opening the sandbox, before any work.

- `bd` -- 
- `bd-boot` -- run the per-session bootstrap chain, BUDGETED and CHECKPOINTED.
- `bd-prestage` -- extract the pack-embedded kit zips into $STAGING AHEAD of
- `bd-install` -- run the BulkDL kit installer.
- `bd-venv` -- provision the SERVICE venv (work/venv) + the cloakbrowser stealth
- `bd-preflight` -- assert the work tree faithfully matches the uploaded source
- `bd-state` -- cross-check STATE.json against the actual source zip in ~1s.
- `bd-status` -- one-shot health check of the sandbox env + services.
- `bd-doctor` -- one-shot READ-ONLY sandbox triage. Answers "is anything obviously
- `bd-intake` -- the uploads dir (/mnt/user-data/uploads) is READ-ONLY and EVICTS
- `bd-optpack` -- detect / report / install the OPTIONAL expansion packs (E-H).
- `bd-fetch` -- when a pack/dependency is missing from uploads, fetch it LIVE
- `bd-packs` -- presence + integrity of every offline pack in one glance.

## Cut readiness / gates
**WHEN:** BEFORE EVERY CUT -- bd-precut --gate is the one entry point; these are what it delegates to.

- `bd-ready` -- one-shot cut-readiness preflight (aggregates the check tools).
- `bd-guardcheck` -- the 7 release-guard files must stay byte-identical unless a new
- `bd-guard-declare` -- declare an intentional change to a release-guard file by
- `bd-versync` -- a version bump is a 3-part edit that must land together:
- `bd-changelog` -- the CHANGELOG-emoji gate has bitten 3x: a non-ASCII char in the
- `bd-ascii` -- flag non-ASCII / emoji (the CHANGELOG emoji gate, hit x3).
- `bd-pinscan` -- `== N` magnitude pins on parity/count invariants re-break on every
- `bd-bandcheck` -- validate a proposed band target list BEFORE running it, so you
- `bd-imports` -- undeclared new import edges vs the frozen baseline.
- `bd-regen` -- check (or regenerate) every derived in-sync doc in one shot.
- `bd-factcheck` -- immunize `stale-copy-of-derived-fact` (the most-cited shape:
- `bd-treecheck` -- is the source tree structurally sound? (the generalized
- `bd-smoke` -- fast import smoke of the core modules (broken-import gate).
- `bd-selfcheck` -- verify the TOOLCHAIN itself, statically and safely.
- `bd-fullsuite` -- run the ENTIRE tests/ suite in-sandbox, correctly.
- `bd-regen-order` -- regenerate every derived artifact in THE ONE CORRECT ORDER.
- `bd-lost-symbol` -- what PUBLIC symbols did a cut silently delete?

## Inspect / search
**WHEN:** ANY TIME -- answering 'where is X / what reads Y' before editing.

- `bd-route` -- route ownership by TEXT-GREP is wrong (the `string-grep-not-decorator`
- `bd-deps` -- blueprint / module dependency inspector (DEPENDENCY_GRAPH.json).
- `bd-pin` -- when you tighten a dependency pin, EVERY occurrence must move together
- `bd-envscan` -- 
- `bd-sym` -- every site of a symbol/flag (grep by SYMBOL, not symptom).
- `bd-kb` -- search the PK/KB docs for a term (grouped by doc).
- `bd-capsweep` -- before you call a catalog item OPEN, prove it isn't built ANYWHERE.
- `bd-freshest` -- point every scoping/validation pass at the NEWEST authoritative
- `bd-lsp` -- one-command offline code intelligence for the BulkDownloader tree.
- `bd-curl` -- CSRF-aware request helper for BulkDownloader operator endpoints.

## Impact / change
**WHEN:** BEFORE BANDING -- given a diff, derive WHAT MUST BE TESTED (bd-band-derive is the engine; blast/suites/touched are shims).

- `bd-since` -- what has changed in the work tree vs the pinned source zip.
- `bd-band-derive` -- one diff-derived band engine (Plan D / INV-2).
- `bd-snapshot` -- mechanize the "snapshot a source file before editing" discipline
- `bd-checkpoint` -- wholesale source-tree checkpoint + restore (rollback safety).
- `bd-timeit` -- wall-clock a command (find the slow gate).
- `bd-reindex` -- regenerate ALL generated indices under the SERVICE VENV.
- `bd-consumer-graph` -- reverse index: what NAMES this thing?

## Quality / security / docs
**WHEN:** BEFORE A CUT that touches the relevant surface; or on a periodic sweep.

- `bd-ssrf` -- outbound-fetch sites and whether an SSRF guard is near (SSRF-REM).
- `bd-secrets` -- lightweight committed-secret scan of the source tree.
- `bd-coverage` -- test-coverage gaps from COVERAGE_GAPS.json.
- `bd-flakes` -- known flaky/hazard tests you must NOT blindly band or full-run.
- `bd-doclint` -- PK docs missing a verified-against marker (complements bd-docstale).
- `bd-docstale` -- which PK docs are behind the current version.
- `bd-render` -- one-command visual gate: start spa_serve, wait for it, run the
- `bd-parity-scan` -- derive the CLI-only surface LIVE; no baseline to go stale.
- `bd-surface-census` -- enumerate EVERY operator-facing surface and score reachability.

## Egress / SSRF primitives (Wave 0)
**WHEN:** WHEN TOUCHING outbound requests / URL handling (SSRF surface).

- `bd-url-classify` -- network-risk verdict for URLs (the Wave-0 egress primitive).
- `bd-path-scan` -- static finder for unguarded filesystem sinks (path traversal).
- `bd-secret-floor` -- prove the canonical redactor is the ONLY redaction path.
- `bd-fetch-policy` -- the ONE canonical outbound-fetch policy + conformance check.
- `bd-host-guard` -- the SSRF-REM triage matrix: sinks x guard x destination risk.

## Security static audits (Wave 1)
**WHEN:** WHEN TOUCHING request/response, redaction, CSRF, or path handling.

- `bd-redaction-scan` -- find ad-hoc redaction logic across output surfaces.
- `bd-csrf-audit` -- audit CSRF coverage of mutating endpoints.
- `bd-path-guard` -- confinement-helper coverage for filesystem writes.
- `bd-devsurf-audit` -- audit dev/cockpit exposed surfaces.
- `bd-defaults-audit` -- audit dangerous default settings.
- `bd-html-taint` -- track unsafe HTML into UI rendering sinks (XSS surface).
- `bd-report-sanitizer` -- canonical HTML sanitizer for rendered reports + a check.
- `bd-fetch-migration` -- plan migrating raw outbound fetches to safe_fetch.

## Interop readiness (Wave 2)
**WHEN:** WHEN TOUCHING an external integration (yt-dlp / JDownloader / CRX).

- `bd-yt-dlp-check` -- validate yt-dlp availability + safe invocation (Batch B).
- `bd-jd-check` -- validate JDownloader integration readiness (Batch B JD-3).
- `bd-crx-check` -- validate the Chromium extension pack (Batch B EXT-4).
- `bd-node-plugin-check` -- validate the Node plugin runtime (Batch B GH-2).

## Guard fuzz + fixtures (Wave 3)
**WHEN:** WHEN CHANGING A GUARD or a parser boundary -- fuzz before trusting it.

- `bd-secret-fixture` -- generate a fake-secret test corpus (deterministic).
- `bd-secret-canary` -- plant fake secrets and verify the REAL scrubber catches them.
- `bd-fuzz-pathguard` -- fuzz the REAL path-confinement guard (backup._is_safe_path).
- `bd-fuzz-redaction` -- fuzz the REAL scrubber with secret-shape mutations.
- `bd-fuzz-urlguard` -- fuzz URL/public-host classification against known truth.
- `bd-fuzz-import` -- fuzz import/parse endpoints with malformed + hostile input.
- `bd-property-tests` -- property-based tests for the critical guards.
- `bd-mutation-test` -- would this gate still pass if the feature were DELETED?
- `bd-redaction-compiler` -- one rule source -> runtime scrubber + scanner + tests.

## Taint dataflow (Wave 4)
**WHEN:** WHEN A SECRET / PATH / URL / TEMPLATE could reach a sink you just added.

- `bd-url-taint` -- track untrusted URLs into outbound fetches (SSRF root-cause).
- `bd-egress-taint` -- track network egress decisions (public-host guard / proxy / VPN / fail-closed coverage). Flows config/site egress settings + URL values into fetch/launch sinks. Advisory, read-only. Usage: bd-egress-taint [--work DIR] [--json] [--selftest]"""
- `bd-secret-taint` -- track secret-like data flow (capture/log/API/template/plugin -> exports, UI, clipboard). Complements bd-secret-floor. Advisory, read-only. Usage: bd-secret-taint [--work DIR] [--json] [--selftest]"""
- `bd-path-taint` -- track untrusted paths/filenames into filesystem writes. Complements bd-path-scan. Advisory, read-only. Usage: bd-path-taint [--work DIR] [--json] [--selftest]"""
- `bd-template-taint` -- track secrets through template flows (capture/log/API -> template export/import). The last mile of bd-secret-taint. Advisory, read-only. Usage: bd-template-taint [--work DIR] [--json] [--selftest]"""

## netns / VPN egress proof (Wave 5)
**WHEN:** WHEN TOUCHING capture isolation, netns, or VPN egress (F5).

- `bd-netns-proof` -- evidence that a process is (or isn't) in a distinct netns.
- `bd-netns-launch-proof` -- prove a launched process runs inside a netns.
- `bd-vpn-proof` -- proof that vpn_required was honored for a site (request path, proxy, tunnel status, no direct egress). Loads vpn_runtime and records the resolved egress decision; a vpn_required site resolving to a direct path (no proxy, no VPNRequiredError) is a leak. Read-only. Usage: bd-vpn-proof [--work DIR] [--site SITE] [--json] [--selftest]"""
- `bd-egress-proof` -- prove the egress path for a site/request (direct/proxy/VPN, tunnel status, no direct-leak evidence). Same resolver as bd-vpn-proof, framed as a per-request egress attestation. Read-only. Usage: bd-egress-proof [--work DIR] [--site SITE] [--json] [--selftest]"""
- `bd-vpn-required` -- validate vpn_required behavior (route via proxy or fail-closed).
- `bd-vpn-egress-scan` -- find egress paths that ignore vpn_required (fail-open risk).

## Release evidence + invariants (Wave 6)
**WHEN:** AT CUT/CLOSE -- assemble the proof that this release is what it claims.

- `bd-evidence` -- one entry point for release evidence (Plan C-C2).
- `bd-proof-ledger` -- machine-readable release proof record.
- `bd-evidence-chain` -- tamper-evident manifest linking every release artifact.
- `bd-invariant-engine` -- define + enforce project invariants as one gate.
- `bd-rollback-plan` -- generate a version-specific rollback checklist.
- `bd-release-note` -- generate release notes from STATE + tree diff + changelog.
- `bd-release-attestation` -- signed-style attestation bundle for a release.
- `bd-trust-score` -- score release confidence (0-100) from evidence.
- `bd-rollback-oracle` -- decide rollback vs patch-forward.
- `bd-deploy-proof` -- validate a deploy landed (version, assets, health, smoke).
- `bd-review-pack` -- assemble a review bundle for a cut.
- `bd-evidence-pack` -- package validation evidence for a release.
- `bd-policy-compiler` -- compile markdown policy rules into executable checks.

## Config / schema / data safety (Wave 7)
**WHEN:** WHEN ADDING/CHANGING A CONFIG KEY or a stored schema (see FG-COUNT-PIN-EDITABLE-SET).

- `bd-config-risk` -- score config key / env-var risk by capability class.
- `bd-config-orphan` -- find incomplete config surfaces.
- `bd-json-schema-check` -- validate JSON Schema definitions (structure + defaults).
- `bd-json-roundtrip` -- test JSON load/save stability of stores.
- `bd-store-check` -- validate app data store shapes (sessions/presets/templates/state).
- `bd-schema-oracle` -- validate all JSON/embedded schemas + cross-schema defaults.
- `bd-config-lineage` -- trace a config key end-to-end.
- `bd-api-contract` -- snapshot API route contract to detect drift.

## Plugin / capture / template hardening (Wave 8)
**WHEN:** WHEN TOUCHING plugins, capture, or templates.

- `bd-plugin-manifest` -- validate plugin manifests (schema, caps, api, paths).
- `bd-plugin-audit` -- audit plugin source surfaces.
- `bd-template-safety` -- check template export/import safety.
- `bd-log-sanitize` -- scrub a log/text file for safe handoff sharing.
- `bd-scrub-proof` -- prove an artifact is safe to share.
- `bd-share-safe` -- build a sanitized share bundle (scrub, verify, manifest).
- `bd-plugin-threatmodel` -- threat-model a plugin before enabling.
- `bd-plugin-containment` -- validate plugin isolation boundaries.
- `bd-plugin-sandbox-check` -- verify plugin isolation settings.
- `bd-capture-risk` -- flag risky capture artifacts.
- `bd-wacz-scrub` -- scrub secrets from a WACZ / capture export zip.
- `bd-artifact-quarantine` -- quarantine risky generated artifacts pending scrub.
- `bd-plugin-permission-diff` -- detect plugin permission creep across manifest versions. Diffs capabilities/permissions between an old and new plugin manifest; a NEW full-access cap (lifecycle/page_access) or added permission is creep to review. Usage: bd-plugin-permission-diff OLD.json NEW.json [--json] [--selftest]"""
- `bd-plugin-diff` -- compare plugin pack changes (hooks/permissions/processors). Diffs two plugin manifests for new hooks, capabilities, and processors. Usage: bd-plugin-diff OLD.json NEW.json [--json] [--selftest]"""
- `bd-template-diff` -- compare template versions (selectors/apis/policies/guards). Diffs two template.json files for changed selectors, api, network_patterns, status, confidence, and recognizer metadata. Usage: bd-template-diff OLD.json NEW.json [--json] [--selftest]"""
- `bd-template-promote-check` -- validate template promotion readiness. Compares a template against its promotion contract fields (status must be gold/reviewed, safety_notes present, confidence high, operator evidence) -- used as OLD=contract-baseline NEW=candidate, reports which promotion fields changed/are unmet. Usage: bd-template-promote-check BASELINE.json CANDIDATE.json [--json] [--selftest]"""
- `bd-plugin-fuzz` -- fuzz plugin manifests + hook returns against the validator.
- `bd-plugin-chaos` -- test plugin failure isolation.
- `bd-plugin-lifecycle-trace` -- static trace of plugin hook lifecycle.
- `bd-capture-trace` -- summarize the capture pipeline events (static).

## Supply chain + toolchain assurance (Wave 9)
**WHEN:** WHEN PINS / DEPS / PACKAGED TOOLS change.

- `bd-supply-chain-scan` -- inspect vendored/offline dependencies.
- `bd-binary-audit` -- inventory executables in bundles and bin dirs.
- `bd-offline-proof` -- prove offline install completeness.
- `bd-dependency-license` -- inventory dependency licenses.
- `bd-env-parity` -- compare sandbox/stash/prod capability parity.
- `bd-archive-normalize` -- normalize pack/archive names + flag non-canonical.
- `bd-time-travel` -- reconstruct available project state for a version.
- `bd-version-genealogy` -- build a version ancestry graph from the changelog.

## Architecture intel / decomposition (Wave 10)
**WHEN:** BEFORE AN ARCH/DECOMP WAVE -- find the seam, the coupling, the blast, the ordered plan.

- `bd-decomp` -- the decomposition co-pilot (Plan E4).
- `bd-graph-build` -- 
- `bd-l0-graph` -- 
- `bd-defect-scan` -- 
- `bd-seam-finder` -- find low-risk extraction seams in a large module.
- `bd-coupling-meter` -- measure cross-subsystem coupling.
- `bd-domain-slicer` -- group code by product domain.
- `bd-monolith-xray` -- deep decomposition x-ray of a large module.
- `bd-decomp-plan` -- plan a safe file decomposition (ordered, invariant-checked).
- `bd-monolith-slice` -- suggest concrete extraction slices.
- `bd-risk-graph` -- build a code risk relationship graph.
- `bd-interface-contracts` -- generate subsystem interface contracts.
- `bd-contract-testgen` -- generate tests from interface contracts.
- `bd-state-machine-extract` -- infer state machines from code + tests.
- `bd-state-machine-test` -- test allowed/forbidden state transitions.
- `bd-workflow-model` -- model operator workflows.
- `bd-workflow-replay` -- safely dry-run-validate an operator workflow.

## UI / FE / operator quality (Wave 11)
**WHEN:** WHEN TOUCHING the SPA / operator surface.

- `bd-fe-route-diff` -- compare frontend route/nav inventory.
- `bd-spa-boundary` -- verify SPA (operations) vs Cockpit (governance) separation. Flags operational-SPA components that import governance-only surfaces or vice versa. Advisory, read-only. Usage: bd-spa-boundary [--work DIR] [--json] [--selftest]"""
- `bd-cockpit-governance` -- verify Cockpit stays governance/review-focused. Flags risky RUNTIME controls (start/stop/delete/exec/kill actions) creeping into Cockpit/governance components. Advisory, read-only. Usage: bd-cockpit-governance [--work DIR] [--json] [--selftest]"""
- `bd-dashboard-readonly` -- prove the Dashboard is read-only. Flags write/mutation actions (POST/PUT/DELETE fetches, mutate/save/delete handlers) inside Dashboard components -- a dashboard should display, not mutate. Advisory, read-only. Usage: bd-dashboard-readonly [--work DIR] [--json] [--selftest]"""
- `bd-ui-contract` -- define + check expected UI behavior per route component.
- `bd-body-contract` -- controls that call the RIGHT endpoint with a body it REJECTS.

## Doc/KB truth + agent governance (Wave 13)
**WHEN:** WHEN DOCS/STATE MAY HAVE DRIFTED from source -- and at session close.

- `bd-pending` -- reconcile STATE.json's pending_pk_update against reality.
- `bd-doc-truth` -- compare docs to source behavior.
- `bd-kb-conflict` -- find contradictory KB instructions across docs.
- `bd-runtime-gate` -- identify runtime-only validations.
- `bd-agent-watchdog` -- check whether an AI patch violated scope.
- `bd-agent-scorecard` -- score an AI session's quality.
- `bd-agent-redteam` -- adversarially review a proposed patch.
- `bd-agent-debate` -- structured pro/con review of a proposed cut.
- `bd-decision-memory` -- bind decisions to code.
- `bd-archaeology` -- explain why code exists.
- `bd-lineage` -- track feature lineage across versions.
- `bd-frontier-lab` -- sandbox harness for experimental analyses.
- `bd-innovation-radar` -- track future ideas separately from the backlog.
- `bd-tracker-recon` -- reconcile the task tracker against SHIPPED SOURCE.

## Runtime / scheduling / chaos (Wave 12)
**WHEN:** WHEN VALIDATING resilience of the queue / scheduler / DB / network paths.

- `bd-scheduler-check` -- validate background scheduler configuration.
- `bd-scheduler-sim` -- simulate scheduler load behavior.
- `bd-calendar-check` -- validate ICS/calendar output correctness.
- `bd-network-chaos` -- test network timeout/retry containment. Drives DNS-fail/proxy-fail/slow/cert-fail/redirect-loop through a reference containment wrapper; each must be caught + classified, never propagate (the contract a network client must implement). Sandbox proves the contract logic. Usage: bd-network-chaos [--json] [--selftest]"""
- `bd-queue-chaos` -- test job-queue failure containment. Cancellation/restart/stuck-job/duplicate/partial-failure driven through the containment wrapper; each contained. Usage: bd-queue-chaos [--json] [--selftest]"""
- `bd-db-chaos` -- test database failure containment. Locked-DB/WAL/missing-table/rollback/corrupt-row driven through the containment wrapper; each contained (the contract a DB layer must implement). Usage: bd-db-chaos [--json] [--selftest]"""
- `bd-capture-chaos` -- test capture failure isolation. Browser-crash/nav-timeout/missing-rrweb/large-DOM/bad-WACZ driven through the containment wrapper; each contained. Usage: bd-capture-chaos [--json] [--selftest]"""
- `bd-job-forensics` -- explain why a job failed from its log.
- `bd-run-timeline` -- build a timeline from a run log.
- `bd-log-causality` -- correlate logs across subsystems into causal chains.
- `bd-observability-map` -- map logging/metrics/events by subsystem.

## Toolchain self-governance
**WHEN:** WHEN CHANGING THE TOOLCHAIN -- and bd-coretest now runs inside bd-precut --gate on every cut.

- `bd-equiv` -- prove one tool can safely REPLACE another, before you retire it.
- `bd-tool-lint` -- lint the whole bd-* toolchain against the KB footgun catalog.
- `bd-tool-smoke` -- does the tool actually RUN? The check nothing else makes.
- `bd-sweep` -- the budgeted, journaled, allowlisted EXECUTOR for the toolchain.
- `bd-golden` -- approval testing for the TOOLS: prove a fix changed the guard,
- `bd-fixture-lint` -- validate the vuln/fixed corpus the detector floor rests on.

## Dev loop
**WHEN:** WHILE EDITING -- fast feedback between edits.

- `bd-band` -- run targeted test suites with the correct sandbox env + a per-suite
- `bd-parband` -- run targeted test suites CONCURRENTLY (the fast, isolated band).
- `bd-retest` -- re-run ONLY the failed suites from the last band (flake triage).
- `bd-cut` -- codify the proven GUI-parity wire-cut finalize so it can't drift.
- `bd-precut` -- predict + GATE a release cut before you bump/build.
- `bd-bump` -- the codified 3-part version bump (landed together, or nothing).
- `bd-job` -- detached job runner that survives the bash_tool exec boundary.

## Close / package
**WHEN:** SESSION CLOSE -- ONLY after the operator confirms stash GREEN. Order: build+verify+present -> STOP -> bd-handoff -> bd-pack.

- `bd-ship` -- one-command session close: the mirror of bd-boot (which chains the
- `bd-handoff` -- one-command session close.
- `bd-pack` -- assemble + LINT the next-session pack so a stale doc can never ship.
- `bd-session` -- the multi-cut session ledger. Multi-cut chats are regular (e.g.
- `bd-kb-sync` -- keep static project knowledge in sync via a sha256 manifest.
- `bd-pk-mirror` -- detect drift between static-PK .py files and their
- `bd-mkbdsuite` -- (re)build the bdsuite toolchain zip.
- `bd-mkauditstate` -- (re)build the audit_state zip for the session.
- `bd-zipcheck` -- is this release zip shippable? (one-shot, against the ZIP).
- `bd-repin-dist` -- pin the reproducible frontend dist and rebuild the release zip.

## Deploy / verify
**WHEN:** AT/AFTER DEPLOY -- operator-run; verifies the artifact and the live service.

- `bd-verify-live` -- confirm a deploy actually landed (run ON STASH, after deploy).
- `bd-rollback` -- thin wrapper over tools/rollback.py that auto-locates the tool +
- `bd-reconcile` -- mechanize the SAFE, un-skippable parts of the tracker reconcile.
- `bd-deploy-manifest` -- the files an OVERLAY deploy must DELETE, not just add.
- `bd-deploy-rehearse` -- run the disk-globbing gates against the POST-OVERLAY tree.

## Sandbox harness
**WHEN:** WHEN EXERCISING RUNTIME in-sandbox (capture, download, netns, browser).

- `bd-rev` -- provision (and verify) the audit-battery venv on demand.
- `bd-live` -- headless-browser smoke test of a URL, in-sandbox.
- `bd-dltest` -- exercise BD's REAL download pipeline end-to-end against public,
- `bd-runner-nav` -- exercise BD's REAL multi-step navigation path: a listing/detail
- `bd-corpus` -- pull recognizer corpus from SANCTIONED public test/demo sites,
- `bd-recognizer-drift` -- verify a recognizer still fires against a live/fixture page.
- `bd-fixture-serve` -- serve synthetic, content-free player-embed fixture pages.
- `bd-opv` -- run the OPV (operator-verification) checks that are now SANDBOXABLE.
- `bd-novnc` -- exercise BD's operator element-PICK and DOM-Analyzer WORKBENCH
- `bd-proxy` -- a NO-LEAK gate over BD's real redaction chain: prove that a capture
- `bd-vpnlab` -- a KILL-SWITCH gate. Two honest halves:
- `bd-sbcap` -- provision the ADVANCED sandbox capability layer on top of bd-boot.
- `bd-render` -- one-command visual gate: start spa_serve, wait for it, run the

## GUI render / capture
**WHEN:** WHEN making or verifying a FRONTEND/GUI change -- bd-render-env FIRST (it gates the browser), then bd-gui-surface for the denominator, bd-deep-capture for the pixels.

- `bd-render` -- one-command visual gate: start spa_serve, wait for it, run the
- `bd-render-env` -- gate (and repair) the RENDER environment before any capture.
- `bd-gui-surface` -- the COMPLETE operator-visible surface, derived from source.
- `bd-deep-capture` -- exhaustive GUI capture: every page, subtab, toggle, modal.
- `bd-fe-dead-control` -- frontend files whose CONTROLS reach no endpoint.

## Audit / coordination
**WHEN:** DURING AN AUDIT PROGRAM -- multi-session review work.

- `bd-audit` -- hydrate the audit_state ledger and mine it for promotable tools.
- `bd-parallel` -- coordinate parallel BD sessions + isolated workstreams.
- `bd-tools` -- the map of the toolchain: every bd-* tool, grouped, with a one-liner.

## Session-start gates (session)
**WHEN:** SESSION START -- immediately after bootstrap, before touching code.

- `bd-worktree-check` -- is the work tree derived from the NEWEST source present?
- `bd-verify` -- run the app capability suites (the verification kit) as one gate.

## Living context (session)
**WHEN:** SESSION START -- load derived truth + armed footguns + capability probe (bd-brief). Re-run after any environment change.

- `bd-footguns` -- an EXECUTABLE footgun registry.
- `bd-brief` -- the session-start context loader (run this FIRST, every session).
- `bd-ratchet` -- turn any metric into a monotonic gate (Plan A2 + F1).
- `bd-band-derive` -- one diff-derived band engine (Plan D / INV-2).
- `bd-coretest` -- fault-injection selftests for the release-gating core tools (Plan B1).
