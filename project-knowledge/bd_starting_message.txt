I'm continuing BulkDownloader work. Fresh sandbox -- please bootstrap it.

This message is VERSION-AGNOSTIC and lives in static project knowledge. The current
release/version, the canonical source-zip hash, guard SHAs, parity/ratchet counts,
in-progress work, and "what's next" all come from the per-session version pack
(STATE.json + the newest KB_HANDOFF_v3_66_<n>.md), NOT from this file. Source code is
always ground truth, re-derived each session.

UPLOADS (attach; the loader picks each by content/name, so order is not critical):
  REQUIRED:
  - bdsuite_v3_66_<n>.zip                 -- the bd-* toolchain (~247 tools). Unzipped
                                             FIRST; install_bdsuite.sh lands it.
  - pack_A.zip, pack_B.zip, pack_C.zip, pack_D.zip
                                          -- the four bd-install kit packs.
  - bd_cloak_pack_v3_66_<n>.zip           -- bd-venv consumable (pip wheels incl.
                                             authlib + joserfc). NOT globbed by
                                             bd-install (different naming scheme).
  - BulkDownloader_v3_66_<n>.zip          -- the source tree (newest full-tree zip;
                                             its version MUST match the KB_HANDOFF and
                                             its sha256 MUST match STATE.json's pin).
  - BulkDL_next_session_v3_66_<n>.zip     -- the per-session version pack (REQUIRED):
                                             STATE.json, newest KB_HANDOFF, Roadmap,
                                             Backlog, EXECUTION_PLAN, REMAINING_WORK,
                                             IMPROVEMENT_BACKLOG (one file).
  - BulkDownloader_project_files_3_66_<n>.zip
                                          -- the static-KB bundle (this file lives in
                                             it). If a PROJECT_KNOWLEDGE_UPDATE.md /
                                             RE-PASTE OWED flag shipped last session,
                                             re-paste this, then re-pin canon (step 6).
  SITUATIONAL (only if the task needs them):
  - audit_state_v3_66_<n>.zip             -- audit graph db + review state (restores to
                                             /home/claude/audit_carry/, NOT the work tree).
  - pack_E..pack_H                        -- expansion packs (E browsers, F pyext wheels,
                                             G netdeps debs, H audit pack); install on
                                             demand via `bd-optpack install <letter>`.

STEP 0 -- HALT GUARD (do this FIRST):
  Confirm a KB_HANDOFF_v3_66_*.md is present in uploads (i.e. the version pack was
  attached). If there is NONE, STOP and ask me for the version pack. Static project
  knowledge has no current-state file by design -- do NOT proceed from project
  knowledge alone, and do NOT guess the version from filenames.

BOOTSTRAP (setup.sh now FAILS by design -- do NOT run it):

1. Copy every uploaded zip to /home/claude/ IMMEDIATELY. The uploads dir
   (/mnt/user-data/uploads/) is read-only and can evict mid-session.

2. Unzip bdsuite_v3_66_<n>.zip and run install_bdsuite.sh -- it self-copies bin/ ->
   /home/claude/bin and makes /usr/local/bin symlinks. If the symlinks don't land,
   create them manually or `export PATH=/home/claude/bin:$PATH` (bash_tool is dash and
   loses env between calls, so re-export PATH each call).

3. Run the bootstrap chain, in order:
       bd-boot        # orchestrates; TIMES OUT on prestage -- it is RESUMABLE, do NOT pkill
       bd-prestage    # chunked; loop `bd-prestage --max N` until it says "all staged"
       bd-install     # lands the kit packs; extracts source to /home/claude/work/
       bd-venv        # builds the service venv (consumes cloak pack; shadows a working ffmpeg)
       bd-preflight
       bd-state       # STATE.json vs the source zip: expect PASS (version, file count, 7 guards)
       bd-status      # kits OK, services up
   NOTE: re-running bd-install mid-session destroys the venv -- re-run bd-venv if so.

4. Read the version and CONFIRM it matches the newest KB_HANDOFF:
       bd -c 'cd /home/claude/work && python3 -c "from bulk_downloader import __version__; print(__version__)"'
   If they disagree, STOP -- wrong source zip or wrong version pack.

5. Pick up context (ground-truth order: source > newest transcript > newest KB_HANDOFF >
   Backlog/Roadmap > static docs):
   a. tail /mnt/transcripts/journal.txt -- the catalog (names each transcript + a summary).
   b. Open the newest transcript in /mnt/transcripts/ for detail. LARGE -- read
      incrementally (grep/sed/view ranges), never whole.
   c. Read the newest KB_HANDOFF_v3_66_*.md (state), then EXECUTION_PLAN + REMAINING_WORK
      + Backlog + Roadmap (what's open + the plan). KB_ACTIVE_INDEX.md is the reading order
      for the static how/why docs (PROJECT_OPERATING_INSTRUCTIONS, CHARTER, GOALS, SANDBOX,
      SCHEMAS, KB_JUDGMENT).

6. If the last handoff flagged RE-PASTE OWED (static PK stale / boot kbsync CANON UNKNOWN):
   after re-pasting the project-files zip, re-pin canon:
       bd-kb-sync pin <static-kb-root> --state STATE.json --version 3.66.<n>

THEN, ONCE GREEN and context is loaded:
  - Work from the newest KB_HANDOFF's "what's next" + EXECUTION_PLAN. Honor the release
    discipline floor: RED-first TDD, band FROM THE EXTRACTED ZIP (never the work tree),
    single-feature app-source cuts, the 7 SHA-pinned guard files stay byte-identical unless
    I explicitly declare a new SHA. Build + verify + present each zip, then WAIT for my
    stash-green confirmation before bd-handoff -> bd-pack.
  - BAND THE FOOTGUN-MANDATED GATES for the change TYPE, not just the feature-named tests:
    FE mutating call-site add -> body_contract.py --regen + test_v3_66_729; route wiring ->
    dark ledger (endpoint_reachability.py --update) + test_v3_66_719 AND all phantom pins
    (test_parity_method_aware AND test_v3_66_336); import-edge change -> import_graph_gate
    --update + test_import_graph_no_new_edges; version bump -> settings_center_slice4 pin +
    build_pin_index + import_graph. A band whose denominator excludes the changed gate reports
    clean and useless -- the exact failure shape this program hunts.

If you're reconstructing context from thin air rather than from the transcript/handoff, STOP
and read them -- or ask me what's missing.
