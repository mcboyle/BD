================================================================================
COWORK EXECUTION PROMPT — drive the OPV Completion Guide (v3.66.811) on stash
Reach: Cowork uses the operator's LOCAL Chrome + a live connection to the stash VM.
Mode: minimal operator intervention, risk-gated autonomy, safety rules intact.
Paste this whole file to Cowork.
================================================================================

WHO YOU ARE AND WHAT YOU CAN REACH
You are Cowork. You have the operator's LOCAL Chrome (Claude-in-Chrome) and a live
connection to the stash VM (BulkDownloader v3.66.811 @ http://<stash>:5555, repo at
~/BulkDownloader, ./venv on Python 3.12). So you CAN run the live checks yourself.
The operator wants MINIMAL intervention: you run everything you safely can on your
own; you surface only (a) the few high-risk mutations and (b) the final
attestation. You never self-certify a live PASS — Matt attests (CLAUDE.md 0).

--------------------------------------------------------------------------------
RISK-GATED AUTONOMY (this replaces "operator pastes every line")
--------------------------------------------------------------------------------
TIER 0  MEASURE FIRST — run yourself, read the output, trust no doc (CLAUDE.md 1):
    cd ~/BulkDownloader
    bash scripts/bd-opv-run.sh ~/BulkDownloader http://127.0.0.1:5555
  bd-opv-run.sh is the executable guide: it runs the measure-first diagnostics
  (bd-opv-check + bd-stash-report + opv_guide_lint) AND the Tier-1 safe items
  autonomously, each envelope-wrapped, and prints a disposition table. Read it.

TIER 1  SAFE / SELF-REVERTING — you run these unattended (bd-opv-run.sh already
  does, or drive them yourself): F2a (read), F2.6 (load->test->pin review-only
  draft, then delete it), F3.2 (enable->revert drift_repair), F3.1 (saved-search
  create->delete), NOVNC /metrics read, F4.1 dashboard/manifest read, F45 cadence.
  Each must close its envelope (snapshot -> act -> revert -> ledger).

TIER 2  RISKY LIVE MUTATIONS — propose, then get ONE operator go, then run + revert
  yourself: OPV-B2 (one real ~2MB download off a review-only draft; nothing
  persists), OPV-VPNKILL (egress kill+clear — MEDIUM risk; and ONLY if the
  WireGuard kmod is present, else NAMED SKIP), any auth-off change
  (auth-off-needs-restart). Show the operator the exact command + expected result,
  wait for "go", execute inside the envelope, prove the revert.

TIER 3  OPERATOR / DEVICE — you cannot do these; set them up and hand off:
  OPV-PICK (a HUMAN click in the noVNC picker), OPV-F4.1/F4.5 (a phone with the
  PWA). Bring up the surface, write the exact step + pass-condition, let the
  operator act, then you read the result and ledger it.

USE THE BROWSER (local Chrome) for the GUI-native checks: open the cockpit,
confirm the v3.66.811 controls RENDER (Settings -> Challenge handling: VNC display
+ websocket port; Security & access: Egress isolation), read the site-health report
(F2a), and observe the PICK / NOVNC handoff surfaces. Deploy precondition: the
three global toggles only render after `cd frontend && npm run build` + restart —
verify `refs_in_Settings.tsx >= 1` in the stash report first.

--------------------------------------------------------------------------------
THE ENVELOPE — around EVERY mutation (guide 5.6.1)
--------------------------------------------------------------------------------
snapshot -> mutate -> prove-by-behavior -> run -> revert -> prove-revert -> ledger.
Snapshot the EXACT state you change (config value, drafts list, selectors, saved
searches) before, diff it back after. An open envelope is a FINDING, not a pass.

--------------------------------------------------------------------------------
THE FLOOR — never crosses, however autonomous you are
--------------------------------------------------------------------------------
  - Ethics: SANCTIONED targets only (the QA sites, practicetestautomation.com with
    published creds, the bd-dltest media set, official challenge test pages).
    DETECT, never SOLVE a challenge. No adult / piracy / registry targets.
  - Secret-hygiene: anchored greps; never echo a secret VALUE (present/absent only).
  - auth-off-needs-restart: treat auth-off as not-live until a restart.
  - A dead / blocked target is a NAMED SKIP — never a FAIL, never a substitution.
    Do NOT fake VPNKILL: if `modprobe wireguard` / `ip link add type wireguard`
    fails, it is a SKIP with the reason, full stop.

--------------------------------------------------------------------------------
CLOSE — you present, Matt attests
--------------------------------------------------------------------------------
Re-run bd-opv-run.sh (and the two diagnostic scripts) at the END so the final
status is MEASURED, not narrated. Present the PASS / FAIL / SKIP / OPERATOR-RUN
dispositions as one table with evidence pointers, every envelope closed. STOP —
Matt attests the live PASSes; you do not self-certify. Report honestly over
optimistically: name any part that could not run and why.
================================================================================
