================================================================================
COWORK EXECUTION PROMPT — drive the OPV Completion Guide (v3.66.811) on stash
Paste this whole file to Cowork as the task. It is self-contained.
================================================================================

ROLE
You are Cowork, the operator-automation driver. Your job is to execute the OPV
Completion Guide (project-knowledge/OPV_COMPLETION_GUIDE_v3_66_810.md, verified
against v3.66.811) against the live stash box, item by item, doing everything
EXCEPT the live security/automation mutation lines — those you hand to the
operator (Matt) to paste. You never self-certify a PASS; Matt attests.

Target: BulkDownloader v3.66.811 on stash (mboyle@10.0.70.20, service @
localhost:5555). Repo at ~/BulkDownloader with ./venv (Python 3.12).

--------------------------------------------------------------------------------
STEP 0 — MEASURE FIRST, TRUST NOTHING (CLAUDE.md 1)
--------------------------------------------------------------------------------
Before working ANY item, re-derive the ground truth from the running box. Do NOT
read status out of the guide or any register — half of a stale "remaining" set is
usually already done, blocked, or mis-scoped. Run, and read the output:

  cd ~/BulkDownloader
  ./venv/bin/python -m playwright --version >/dev/null 2>&1 || true
  bash scripts/bd-opv-check.sh    ~/BulkDownloader http://127.0.0.1:5555   # per-item READY/NEEDS-SETUP/BLOCKED
  bash scripts/bd-stash-report.sh ~/BulkDownloader http://127.0.0.1:5555   # GUI render truth + preconditions
  ./venv/bin/python tools/opv_guide_lint.py project-knowledge/OPV_COMPLETION_GUIDE_v3_66_810.md

Then upload the two /tmp/*.tar.gz to me if I am analyzing remotely. Use the READY/
NEEDS-SETUP/BLOCKED verdict per item to decide what is runnable THIS session.

DEPLOY PRECONDITION (v3.66.811): the three global GUI toggles
(captcha_vnc_display, captcha_vnc_websocket_port, netns_isolation) only render
after the SPA is rebuilt on the box:
  cd ~/BulkDownloader/frontend && npm run build   # then restart the service
Confirm from the stash report that `refs_in_Settings.tsx >= 1` for those keys
before claiming the GUI path for any item that uses them.

--------------------------------------------------------------------------------
THE SPLIT (non-negotiable)
--------------------------------------------------------------------------------
For each item the guide gives Prep / Paste / Observe / Pass / Teardown / Ledger.
  - PREP     : YOU run it (read-only setup, CSRF prelude, snapshots, staging).
  - PASTE    : the OPERATOR pastes it (the live mutation line). Hand it over
               verbatim; re-mint CSRF just before; if it 401s, re-run the prelude.
  - OBSERVE  : read the result off the operator's screen / the response.
  - PASS     : confirm the exact condition the guide names. If a target is dead,
               it is a NAMED SKIP — never a FAIL, never a substitution.
  - TEARDOWN : YOU run it (delete the draft/search you created, restore staged
               selectors, revert flags, confirm app_config.json back to baseline).
  - LEDGER   : YOU write the one-line ledger entry.

Where an item is fully read-only (F2a) or a safe round-trip you can revert
yourself, run it end-to-end with no operator paste.

--------------------------------------------------------------------------------
THE ENVELOPE — around EVERY mutation (guide 5.6.1)
--------------------------------------------------------------------------------
snapshot -> mutate -> prove-by-behavior -> run -> revert -> prove-revert -> ledger.
An unclosed envelope is a FINDING, not a pass. Snapshot the exact state you will
change (config value, drafts list, selectors, saved-searches) BEFORE, and diff it
back to baseline AFTER.

--------------------------------------------------------------------------------
THE FLOOR — never crosses, whoever is driving
--------------------------------------------------------------------------------
  - Ethics: SANCTIONED targets only (the QA sites, practicetestautomation.com with
    its published creds, the bd-dltest media set, official challenge test pages).
    DETECT, never SOLVE a challenge. No adult / piracy / registry targets.
  - Secret-hygiene: anchored greps, never echo a secret VALUE (present/absent only).
  - auth-off-needs-restart: turning auth off is not live until a restart; treat it
    as such.
  - One real download max where the guide says so (B2), against an allowlisted host.

--------------------------------------------------------------------------------
ORDER OF WORK (cheap + zero-risk first)
--------------------------------------------------------------------------------
Batch 1-4 in one session (no device): F2a, F2.6, F3.2-LIVE, F3.1/F3.1-WK.
Then 5-11 as devices/sessions allow: F1.4-EN, B2, PICK, NOVNC-PRECEDENCE,
VPNKILL, F4.1/F4.5, F45-METRIC.

Follow the guide's per-item Prep/Paste/Observe/Pass/Teardown/Ledger EXACTLY.
Known measured state to respect (re-confirm with Step 0, do not assume):
  - F2a  NEEDS-SETUP  : no failure history yet; drive a few sanctioned failures first.
  - F2.6 READY        : captures exist; the dry-run proved load->test->pin ->
                        draft_review_required. Delete the draft you create.
  - F3.2 forced-PASSED: only the SCHEDULED daily tick is owed (enable the flag,
                        wait for the real day boundary, then revert).
  - F3.1 week-bound   : the 7-day cap/dedup proof cannot be compressed.
  - F1.4 NEEDS-SETUP  : now a per-site GUI toggle (v3.66.810) that persists across
                        restart; seed >=3 login cycles on the practice site first.
  - B2   READY        : one real ~2MB fetch off a review-only draft; NOTHING persists
                        (the override suppresses both live-config and draft writes).
  - PICK / NOVNC      : the noVNC/KasmVNC display is up on stash; PICK needs a HUMAN
                        click; NOVNC is detect->handoff, ZERO solve.
  - VPNKILL BLOCKED   : WireGuard kmod is ABSENT on stash. The live-tunnel arm cannot
                        run until `modprobe wireguard` succeeds; the state machine is
                        already proven. Do NOT fake it — named SKIP if the kmod is out.
  - F4.1/F45 device   : the share flow needs a phone with the PWA; F45 is an optional
                        measurement (the mechanism is code-confirmed ~93%/50%).

--------------------------------------------------------------------------------
SESSION CLOSE
--------------------------------------------------------------------------------
  1. Every envelope closed (any open one = FINDING).
  2. For each PASS, write the ledger line and move it from "remaining" to
     "completed" in the live status doc with an evidence pointer.
  3. Assemble the evidence pack addendum.
  4. STOP. Present the PASS / FAIL / SKIP dispositions as a table. Matt attests
     (CLAUDE.md 0). You do not self-certify.

Report honestly over optimistically. If a step could not run, say which part and
why — an honest "unknown / operator-owned" beats a confident guess. Re-run the two
diagnostic scripts at the end so the final status is measured, not narrated.
================================================================================
