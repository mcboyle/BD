# GENERATED SPEC -- NOT A CONTRACT, NOT VERIFIED

Machine-generated by a multi-agent investigation on 2026-08-03 and kept only
because it could not be cheaply re-derived. Read this before reading anything
below it:

  * CLAUDE.md is the ONLY agent-facing contract. This file is not one. Where
    they disagree, CLAUDE.md wins and this file is the defect.
  * project-knowledge/SESSION_CARRY.md is the ONLY register. Every
    DECISION-CRITICAL fact from this spec -- premises, amendments, measured
    figures, file:line anchors -- is already recorded there in sections 15.20,
    15.21 and 15.23. Those sections, not this file, are authoritative.
  * This spec was ADVERSARIALLY REVIEWED and did NOT pass clean. Its surviving
    objections are listed in SESSION_CARRY 15.20. Applying it verbatim without
    those amendments ships a known defect.
  * Every figure here was measured against the tree at v3.66.842-846. Re-derive
    before acting; CLAUDE.md section 1 applies to this file too.
  * DELETE THIS FILE when its item ships. A stale spec that outlives its cut is
    exactly the second-document failure that retired TASK_TRACKER and
    CODEX_HANDOFF.

---

TRUE NUMBERS (re-measured at HEAD 7bf9f92, branch claude/bulkdownloader-handoff-j9o59v, working tree clean):
  Total: 391 files / 1529 occurrences. bulk_downloader/: 0 files / 0 occurrences.
  By directory (files/occ): project-knowledge 182/790, toolchain 133/487, tools 25/58,
    docs 17/76, tests 17/39, scripts 8/15, conf 2/13, kb 2/3,
    root singles (CHANGELOG.md, SANDBOX.md, VERSION.txt, slowest_tests.sh, .superpowers/sdd/wacz-processing-report.md) 5/48.
  By real content-type (via shebang, not suffix): python 320/951, shell 29/313, docs(md/txt/json/conf) 42/265.
  Mirror duplication (of 267 known pairs incl. the 5 gate-blind ones): 148 pairs currently agree,
    520 occurrences per side (34% of the 1529 total; 66% of project-knowledge's own 790). NOT "half".
  Corrected combined-executable (python+shell) = 1264, not the inherited ~1062.
  UNRECONCILED: "~212 genuine default-path VALUES" -- no predicate tried lands there (closest: 193 argparse defaults, 127 ALL-CAPS constants). Treat as UNKNOWN, not confirmed.

PK MIRROR GATE BLIND SPOT -- exactly 5 pairs, verified byte-identical today (no live drift, but undetectable if one occurs):
  1. project-knowledge/bd            <-> toolchain/bin/bd                          (name lacks "-", fails predicate)
  2. project-knowledge/bdenv.sh      <-> toolchain/bdenv.sh                        (fails predicate AND origin outside SOURCE_DIRS)
  3. project-knowledge/install_bdsuite.sh <-> toolchain/install_bdsuite.sh         (fails predicate AND origin outside SOURCE_DIRS)
  4. project-knowledge/cap01_witnesses.py <-> tools/audit/witnesses/cap01_witnesses.py (predicate passes, origin one dir too deep)
  5. project-knowledge/run01_witnesses.py <-> tools/audit/witnesses/run01_witnesses.py (predicate passes, origin one dir too deep)
Root cause (tests/test_pk_mirrors_do_not_drift.py lines 42-46, 59-76): non-recursive top-level scan of project-knowledge/,
predicate name.startswith("bd-") or suffix==".py", origin lookup restricted to exactly
{toolchain/bin, tools, tools/decomp} with no recursion.

RELEASE-FILE PIN: tests/test_generated_artifact_workflow.py:195 requires the literal substring
"/home/claude/release_148" to remain present in scripts/build_release.sh (which currently has 2 occurrences:
line 2 "cd /home/claude/work", line 5 "STAGE=/home/claude/release_148"). Any scope touching that file
must co-edit this test in the same cut or the suite goes red -- this is release discipline
(CLAUDE.md section 2/9), needs explicit per-task authorization.

BD_CUT_COPIES BAND: tests/test_generated_artifact_workflow.py:25 = ("toolchain/bin/bd-cut", "project-knowledge/bd-cut"),
both inside the 391-file set (9 occurrences each, currently mirror-identical). Parametrized tests over this tuple
assert on DIFFERENT substrings (presence of the repo-relative bd-regen-order path, absence of
"/home/claude/bin/bd-regen-order" and "shutil.which(\"bd-regen-order\")") -- editing bd-cut's own /home/claude
occurrences is compatible as long as the literal substring "/home/claude/bin/bd-regen-order" is not
(re)introduced; it does not exist today.

--- SCOPING OPTIONS ---

OPTION A -- toolchain/ + tools/ only, project-knowledge left untouched
  True file count: 158 (133 toolchain + 25 tools)
  True occurrence count: 545 (487 + 58)
  Band: the 16 tests referencing toolchain/bin (test_all_sources_parse, test_bd_doctor_probes_the_real_environment,
    test_bd_ready_preflight, test_code_intelligence_frontend_docs, test_deploy_manifest_stays_retired,
    test_differential_oracle_frontend, test_env_parity_sees_the_real_browser_pool,
    test_generated_artifact_workflow, test_pk_mirrors_do_not_drift, test_playwright_engines_single_source,
    test_provision_test_host, test_pytest_runner_boundary, test_queue_ts_since_cursor_pin,
    test_task_tracker_stays_retired, test_v3_66_820_share_tools_saw_no_session_keys, test_versync_gate)
    + test_desandbox_tool_verifiers.py (same defect class, named engines bd-mutation-test/bd-band-derive).
  Breaks: tests/test_pk_mirrors_do_not_drift.py::test_no_project_knowledge_mirror_has_drifted_from_its_origin
    -- GUARANTEED red (sha256 mismatch) for up to 148 pairs the moment any toolchain/tools origin is edited
    without its project-knowledge copy following. Not a risk, a certainty given the gate's own comparison.
  Verdict: not shippable alone; forces Option B or an explicit decision to accept a red gate.

OPTION B -- Option A + resync every project-knowledge mirror (including the 5 gate-blind ones)
  True file count: 306 (158 Option-A files + 148 project-knowledge files needing resync of the 267 known pairs)
  True occurrence count: 1065 edit-sites (545 + 520)
  Band: Option A's band, plus explicit manual byte-diff verification of the 5 gate-blind pairs
    (the automated gate structurally cannot check them -- "gate green" is not evidence for those 5).
  Breaks: nothing in the current suite if resync is verified by hand for the 5 blind pairs. Does not
    touch scripts/build_release.sh (line-195 pin untouched). Does not touch tests/, so none of the
    ~12 intentional retired-sandbox-fixture tests are at risk.
  Residual: 85 files still say /home/claude afterward -- 34 standalone project-knowledge docs/orphans
    (incl. bd-deploy-manifest/bd-reconcile/bd-tracker-recon, which have NO live origin anywhere and are
    dead weight, not a drift risk), plus docs/ (17), tests/ (17, mostly SHOULD stay untouched), scripts/ (8),
    conf/ (2), kb/ (2), 5 root singles.
  Verdict: fixes the class of defect this project has already been burned by once (bd-band-derive,
    bd-mutation-test) without touching the one hard-pinned release test or the deliberate test fixtures.

OPTION C -- everything, repo-wide (all 391 files / 1529 occurrences)
  True file count: 391. True occurrence count: 1529.
  Band: Option B's full band, PLUS tests/test_generated_artifact_workflow.py:195 (deterministic red
    unless co-edited -- release-file + pinned-test change, needs explicit authorization), PLUS a
    mandatory per-file read of the ~12 tests/*.py files using /home/claude as an intentional
    retired-sandbox fixture (listed in findings) to decide bug-vs-deliberate-fixture case by case --
    NOT mechanizable, a blind sed would produce a silent semantic failure (ast.parse would still
    succeed), not a syntax error. PLUS docs/ and project-knowledge's 34 standalone docs and dated
    historical records (CHANGELOG.md, AUDIT_2026_07_29.md-style files under project-knowledge) where
    editing rewrites what a dated record SAYS happened, not what should happen going forward.
  Breaks: tests/test_generated_artifact_workflow.py deterministically, unless co-edited; and
    non-deterministically any of the ~12 fixture tests if the per-file read step is skipped.
  Verdict: highest total remediation, by far the highest review cost, not mechanizable for tests/
    or historical docs. Recommend only if the operator explicitly wants the release-file pin and
    the retired-sandbox test fixtures re-litigated as part of the same cut.

RECOMMENDATION (for the operator to accept or override): Option B is the only one of the three that is both
complete for the "live behavior" surface (matches the documented prior-incident shape: bd-band-derive/
bd-mutation-test) and free of forced collateral edits to a pinned release test or to tests whose entire
purpose is to assert against this exact string. Option A is not independently viable. Option C is a much
larger, judgment-heavy undertaking that should be its own explicitly-scoped cut, not a default escalation
of B.