# GENERATED SPEC -- NOT A CONTRACT, NOT VERIFIED

Machine-generated by a multi-agent investigation on 2026-08-03 and kept only
because it could not be cheaply re-derived. Read this before reading anything
below it:

  * CLAUDE.md is the ONLY agent-facing contract. This file is not one. Where
    they disagree, CLAUDE.md wins and this file is the defect.
  * project-knowledge/SESSION_CARRY.md is the ONLY register. Every
    DECISION-CRITICAL fact from this spec -- premises, amendments, measured
    figures, file:line anchors -- is already recorded there in sections 15.20,
    15.21 and 15.23. Those sections, not this file, are authoritative.
  * This spec was ADVERSARIALLY REVIEWED and did NOT pass clean. Its surviving
    objections are listed in SESSION_CARRY 15.20. Applying it verbatim without
    those amendments ships a known defect.
  * Every figure here was measured against the tree at v3.66.842-846. Re-derive
    before acting; CLAUDE.md section 1 applies to this file too.
  * DELETE THIS FILE when its item ships. A stale spec that outlives its cut is
    exactly the second-document failure that retired TASK_TRACKER and
    CODEX_HANDOFF.

---

================================================================================
SPEC #7 (AMENDED) -- HTTP-design clear for the bdseed history residue
Target tree: branch claude/bulkdownloader-handoff-j9o59v @ 7bf9f92 (v3.66.844
+ the in-flight 15.8 WIP). Sequence AFTER 15.8 lands. One feature per cut.
================================================================================

0 | PREMISE VERIFICATION (do not re-litigate; it was measured)
--------------------------------------------------------------------------------
The operator's robustness argument is CORRECT. batch_ops.bulk_delete maintains
history_fts (batch_ops.py:190-200 -> db.db_fts_forget, db.py:854-933), the rows
it replays are the pre-delete values (_matching_rows SELECT *), the maintenance
runs once per batch AFTER the deletes, and membership is derived from fts5vocab
rather than inferred. tests/test_fts_external_content_delete.py: 21 passed,
exit=0. An independent end-to-end probe deleted 5 seeded rows and left the FTS
index holding exactly the one survivor. Build on POST /api/batch/delete.

Two consequences the decision did NOT include, both real and both handled below:
  * batch_ops DISCARDS db_fts_forget's report, so the clear cannot say whether
    the index was maintained. The operator's post-clear SQL is the only witness.
  * PRAGMA foreign_keys is never enabled (library.py:469-474), so
    `library.history_id -> history(id) ON DELETE SET NULL` does not fire. The
    clear leaves dangling back-refs and orphan library rows. Report, never hide.

--------------------------------------------------------------------------------
1 | tools/live_seed.py -- EDITS
--------------------------------------------------------------------------------

1.1  AFTER line 129 (`_HISTORY_PAGE = 500`), ADD:

# Rows per /api/batch/delete call. _build_query truncates id_in at 1000
# SILENTLY (batch_ops.py:76), and its `LIMIT ?` is appended AFTER the IN clause,
# so an under-sized limit silently drops ids too (measured: 6 ids with limit=2
# matched 2). Both are why the clear chunks and passes limit=len(chunk) rather
# than relying on any default.
_CLEAR_CHUNK = 200
# Each round reads at most _HISTORY_PAGE rows, so 20 rounds bound the clear at
# 10k rows. Deliberately bounded: capture.sh runs this unattended.
_CLEAR_MAX_ROUNDS = 20


1.2  REPLACE RESIDUE_NOTE (lines 978-986) ENTIRELY. The current text asserts the
     premise db.py:988-992 already retracted. Both the words "append-only" and
     "prune" are RETAINED on purpose: tests/test_live_seed_starts_and_settles.py
     :607 asserts one of them appears in str(residue).

RESIDUE_NOTE = (
    "a completed seeded download leaves a history row, a library_record row "
    "and a file under the seeded site's download_dir. The history row CAN be "
    "removed -- batch_ops.bulk_delete deletes by id over POST /api/batch/delete "
    "and maintains the external-content history_fts index while doing it "
    "(db.db_fts_forget) -- but teardown does not do it unless --clear-history "
    "is passed, because capture.sh runs teardown unattended. The older claim "
    "here, that history is append-only with db_prune (which deletes by AGE, "
    "not by marker) as its only deleter, was FALSE; db.py:988-992 records the "
    "retraction. The library row and the downloaded file are NOT removed by "
    "any path this tool has. Nothing is hidden; it is reported so a later "
    "reader does not mistake it for organic history."
)

# What survives even a successful --clear-history. Named separately so a
# "0 remain" line can never be read as "the box is as it was found".
CLEAR_LEFTOVER_NOTE = (
    "the matching library rows and the downloaded files under the seeded "
    "site's download_dir are NOT removed. SQLite foreign keys are off "
    "(library.py:469-474), so library.history_id keeps pointing at the "
    "deleted history id rather than being SET NULL. A history row whose "
    "FTS-indexed text was updated in place cannot leave the history_fts "
    "index either, and /api/batch/delete does not report that (it discards "
    "db_fts_forget's result). Check fts5vocab on the box to see it."
)


1.3  REPLACE _seeded_history (lines 989-1010) with the pair below. ONE read,
     TWO consumers -- the same shape 15.8 gave library_final.size_drift_scan --
     so the number reported and the rows acted on can never be about different
     passes. THE URL IS UNCHANGED. Do not add paginate/cursor: measured, it
     voids the three ('GET', '/api/history?q=bdseed&limit=500') stubs in
     tests/test_live_seed_starts_and_settles.py by exact-key miss, and turns
     test_teardown_says_unknown_when_it_cannot_read_history into a pass that
     never exercises its own fixture.

def _seeded_history_rows(client):
    """(marked history rows, readable). ([], False) when it cannot be read.

    Reads through the app, like every other question this tool asks.
    /api/history returns a BARE ARRAY by default and a {rows, next_cursor}
    envelope when paginating, so both shapes are accepted; anything else is
    UNKNOWN and says so rather than counting as zero.

    THE URL IS PART OF THE CONTRACT. tests/test_live_seed_starts_and_settles.py
    stubs it verbatim, and its FakeClient falls back to a default body on an
    exact-key miss rather than raising -- so changing the query string here
    does not fail those tests, it makes one of them pass without exercising
    its fixture. Unbounded residue is handled by the clear's ROUND LOOP
    (clear_seeded_history), not by paginating this read.

    The server-side filter is a LIKE over url/filename/message (db.db_search),
    so the marker is re-checked against the url field here: the subject is a
    seeded URL, not any row whose message happens to mention one.
    """
    body = client.get(f"/api/history?q={SEED_MARKER}&limit={_HISTORY_PAGE}")
    rows = None
    if isinstance(body, list):
        rows = body
    elif isinstance(body, dict) and isinstance(body.get("rows"), list):
        rows = body["rows"]
    if rows is None:
        return [], False
    return [r for r in rows
            if isinstance(r, dict)
            and SEED_MARKER in str(r.get("url", ""))], True


def _seeded_history(client):
    """(count of marked history rows, readable). (None, False) when unreadable.

    A projection of _seeded_history_rows, kept because teardown's residue report
    and every existing caller want the number, not the rows.
    """
    rows, readable = _seeded_history_rows(client)
    return (len(rows) if readable else None), readable


1.4  ADD, immediately after _seeded_history:

def _row_ids(rows):
    """Integer ids from history rows, skipping anything that is not one."""
    out = []
    for r in rows:
        try:
            out.append(int(r["id"]))
        except (KeyError, TypeError, ValueError):
            continue
    return out


def clear_seeded_history(client, *, dry_run: bool = False,
                         chunk: int = _CLEAR_CHUNK,
                         max_rounds: int = _CLEAR_MAX_ROUNDS) -> dict:
    """Delete the marked history rows over POST /api/batch/delete.

    WHY HTTP AND NOT A DIRECT sqlite DELETE. history_fts is an FTS5
    EXTERNAL-CONTENT table (content='history', db.py:113-118): SQLite maintains
    nothing for it, so a hand-rolled DELETE would leave every removed row's
    terms in the inverted index forever. batch_ops.bulk_delete calls
    db.db_fts_forget for the batch (batch_ops.py:190-200) and that is the whole
    reason this goes through the app.

    OWNERSHIP. Two predicates, both required, and the narrower one is the one
    that authorises a delete: the marker must be in the row's URL (what
    _seeded_history_rows already checks) AND in its site_name (which db_log
    stores as a literal, so it survives the site being deleted). A row matching
    only the first is counted as `unowned` and NEVER deleted -- under-deleting
    is the safe direction, and an unowned row simply keeps the remainder
    non-zero, which the report then says out loud.

    NO ARITHMETIC. The remainder is RE-MEASURED by re-reading /api/history,
    never computed as found-minus-deleted: db_log runs on every job-level
    transition (db.py:962-965), so a job settling during teardown appends a row
    after the first read, and /api/batch/delete answers a rejected filter and a
    genuine no-op with the SAME body (candidates_matched 0, processed 0,
    ok true -- measured), because _matching_rows swallows the TypeError from an
    unexpected filter key and returns [].

    THE CANARY. Before deleting anything, one chunk is re-sent with
    dry_run=True. Those ids were just read as present, so candidates_matched
    MUST equal len(chunk); a 0 means the filter shape is not the one
    _build_query accepts and the pass aborts having deleted nothing. This is
    the only cheap way to tell a malformed filter from an empty table.
    """
    out = {"action": "clear_seeded_history", "marker": SEED_MARKER,
           "dry_run": bool(dry_run), "readable": False,
           "found": None, "unowned": 0, "targeted": 0, "deleted": 0,
           "rounds": 0, "remaining": None, "remaining_readable": False,
           "stalled": False, "canary": None,
           "errors": [], "warnings": [], "ok": False,
           "note": CLEAR_LEFTOVER_NOTE}
    chunk = max(1, min(int(chunk), 1000))
    prev_remaining = None
    exhausted = True
    for round_no in range(1, max(1, int(max_rounds)) + 1):
        rows, readable = _seeded_history_rows(client)
        if not readable:
            out["errors"].append(
                f"round {round_no}: could not read /api/history, so the "
                f"{SEED_MARKER} row set is UNKNOWN")
            out["remaining"] = None
            out["remaining_readable"] = False
            return out
        out["readable"] = True
        out["rounds"] = round_no
        if out["found"] is None:
            out["found"] = len(rows)
        owned = [r for r in rows
                 if SEED_MARKER in str(r.get("site_name", ""))]
        out["unowned"] = len(rows) - len(owned)
        ids = _row_ids(owned)
        if not ids:
            out["remaining"] = len(rows)
            out["remaining_readable"] = True
            exhausted = False
            break
        if out["canary"] is None:
            probe = ids[:chunk]
            resp = client.post("/api/batch/delete",
                               {"filter": {"id_in": probe,
                                           "limit": len(probe)},
                                "dry_run": True, "delete_files": False})
            matched = resp.get("candidates_matched") \
                if isinstance(resp, dict) else None
            out["canary"] = {"sent": len(probe), "matched": matched}
            if matched != len(probe):
                out["errors"].append(
                    f"filter canary: /api/batch/delete matched {matched} of "
                    f"{len(probe)} ids that were just read as present. The "
                    f"filter shape is not the one batch_ops._build_query "
                    f"accepts; nothing was deleted. Response: {str(resp)[:200]}")
                out["remaining"] = len(rows)
                out["remaining_readable"] = True
                return out
        if dry_run:
            out["targeted"] = len(ids)
            out["remaining"] = len(rows)
            out["remaining_readable"] = True
            out["ok"] = True
            return out
        for i in range(0, len(ids), chunk):
            batch = ids[i:i + chunk]
            out["targeted"] += len(batch)
            resp = client.post("/api/batch/delete",
                               {"filter": {"id_in": batch,
                                           "limit": len(batch)},
                                "dry_run": False, "delete_files": False})
            if not isinstance(resp, dict) or resp.get("ok") is not True:
                out["errors"].append(
                    f"/api/batch/delete rejected a {len(batch)}-id chunk: "
                    f"{str(resp)[:200]}")
                continue
            processed = resp.get("processed")
            matched = resp.get("candidates_matched")
            if not isinstance(processed, int):
                out["errors"].append(
                    f"/api/batch/delete returned no processed count: "
                    f"{str(resp)[:200]}")
                continue
            out["deleted"] += processed
            if matched == 0 and processed == 0:
                # SOFT, not hard. Another writer removing these ids first gives
                # exactly this body, and so does a malformed filter -- the
                # canary above already ruled the second one out, and the
                # re-measured remainder settles the rest.
                out["warnings"].append(
                    f"a {len(batch)}-id chunk matched nothing (already gone?)")
            elif processed != matched:
                out["errors"].append(
                    f"/api/batch/delete processed {processed} of {matched} "
                    f"matched (errors={resp.get('errors')})")
        rows_after, readable_after = _seeded_history_rows(client)
        out["remaining"] = len(rows_after) if readable_after else None
        out["remaining_readable"] = bool(readable_after)
        if not readable_after:
            out["errors"].append(
                "could not re-read /api/history after the clear, so the "
                "remainder is UNKNOWN rather than zero")
            return out
        if not rows_after:
            exhausted = False
            break
        if prev_remaining is not None and len(rows_after) >= prev_remaining:
            out["stalled"] = True
            exhausted = False
            break
        prev_remaining = len(rows_after)
    if exhausted:
        out["stalled"] = True
    out["ok"] = bool(out["remaining_readable"]
                     and out["remaining"] == 0
                     and not out["errors"])
    return out


1.5  teardown (line 1147). CHANGE THE SIGNATURE and the residue block; leave
     the queue/site/tunnel body byte-identical.

     Line 1147, replace:
         def teardown(client, *, dry_run: bool = False) -> dict:
     with:
         def teardown(client, *, dry_run: bool = False,
                      clear_history: bool = False) -> dict:

     Lines 1170-1174, replace the plan literal with:

    # `history_rows` ALWAYS means rows that REMAIN; `history_rows_found` always
    # means rows the read SAW. Without a clear they are the same number, which
    # is exactly why one name was enough before and is not enough now: a clear
    # makes them differ, and a single key would silently change meaning between
    # two runs of the same tool. `readable` describes whichever measurement
    # produced `history_rows`.
    plan = {"action": "teardown", "marker": SEED_MARKER,
            "ids": ids, "sites": sites, "tunnels": tunnels,
            "residue": {"history_rows_found": history_rows,
                        "history_rows": history_rows,
                        "readable": history_readable,
                        "cleared": False,
                        "note": RESIDUE_NOTE}}
    if dry_run:
        plan["dry_run"] = True
        if clear_history:
            plan["clear"] = clear_seeded_history(client, dry_run=True)
        return plan

     Then, immediately BEFORE the final `return plan` (currently line 1203),
     INSERT:

    # LAST, and after the site deletes on purpose: a job that settles while
    # teardown is running appends a history row (db_log runs on every
    # job-level transition), so the set is re-read here rather than reusing
    # the one harvested at the top of this function.
    if clear_history:
        clear = clear_seeded_history(client, dry_run=False)
        plan["clear"] = clear
        residue = plan["residue"]
        residue["cleared"] = True
        residue["history_rows_found"] = clear.get("found")
        residue["history_rows"] = clear.get("remaining")
        residue["readable"] = bool(clear.get("remaining_readable"))


1.6  REPLACE _report_residue (lines 1206-1221) with:

def _report_residue(plan) -> None:
    """Say out loud what teardown could not remove.

    Silent only when the answer is a measured zero AND no clear was attempted.
    An unreadable history is NOT a clean one and says so; a non-zero count
    names itself rather than hiding inside JSON a reader may skim past.
    """
    plan = plan or {}
    residue = plan.get("residue") or {}
    clear = plan.get("clear") or {}
    rows = residue.get("history_rows")
    found = residue.get("history_rows_found")
    cleared = bool(residue.get("cleared"))
    if clear:
        for err in clear.get("errors") or []:
            print(f"live_seed: CLEAR ERROR - {err}", file=sys.stderr)
        for warn in clear.get("warnings") or []:
            print(f"live_seed: CLEAR WARNING - {warn}", file=sys.stderr)
        if clear.get("unowned"):
            print(f"live_seed: CLEAR SKIPPED - {clear['unowned']} row(s) carry "
                  f"{SEED_MARKER} in the URL but not in site_name, so they are "
                  f"not provably this tool's and were not deleted.",
                  file=sys.stderr)
        if clear.get("stalled"):
            print(f"live_seed: CLEAR STALLED - a round removed nothing while "
                  f"{SEED_MARKER} history rows remained.", file=sys.stderr)
    if rows is None:
        print(f"live_seed: RESIDUE UNKNOWN - could not read /api/history, so "
              f"whether this host still holds {SEED_MARKER} history rows is "
              f"undetermined. {RESIDUE_NOTE}", file=sys.stderr)
    elif rows:
        extra = ""
        if cleared:
            extra = (f" (found {found}, deleted {clear.get('deleted', 0)}, "
                     f"measured after the clear)")
        print(f"live_seed: RESIDUE - {rows} {SEED_MARKER} history row(s) "
              f"remain after teardown{extra}. {RESIDUE_NOTE}", file=sys.stderr)
    elif cleared:
        print(f"live_seed: CLEARED - {clear.get('deleted', 0)} of {found} "
              f"{SEED_MARKER} history row(s) removed; 0 remain, re-measured "
              f"not inferred. {CLEAR_LEFTOVER_NOTE}", file=sys.stderr)


1.7  ADD, immediately after _report_residue:

# Exit codes. 2 = REFUSED and 3 = TIMEOUT are already taken by main().
_EXIT_CLEAR_INCOMPLETE = 4
_EXIT_CLEAR_UNKNOWN = 5


def _teardown_exit_code(plan, *, clear_requested: bool) -> int:
    """0 only when the clear did what it was asked to do, MEASURED.

    Nothing read this before. main() returned 0 on every teardown outcome
    except a SeedRefused out of _queue_snapshot, so capture.sh:746-749's
    `|| echo WARNING` had never fired. Measured 2026-08-03 on four separate
    failures: an unreadable /api/history (prints RESIDUE UNKNOWN, exit 0), an
    unreadable /api/status so no marked site is found and none is deleted
    (prints NOTHING, exit 0), a site DELETE answering 500 (prints NOTHING,
    exit 0), and 64 rows left behind (prints RESIDUE, exit 0).

    SCOPED TO THE CLEAR, deliberately. teardown's queue-cancel step is
    independently broken -- the entries /api/queue/v2 returns carry no `id`
    key so `ids` is always empty, and the payload it would send is {"ids":...}
    where app_queue.py:400-406 reads "jobs" (measured: HTTP 400). Making the
    whole teardown fatal in this cut would warn on every capture for a defect
    this cut did not introduce. That one is filed separately.
    """
    if not clear_requested:
        return 0
    clear = (plan or {}).get("clear") or {}
    if not clear:
        return _EXIT_CLEAR_INCOMPLETE
    if not clear.get("remaining_readable"):
        return _EXIT_CLEAR_UNKNOWN
    return 0 if clear.get("ok") else _EXIT_CLEAR_INCOMPLETE


1.8  main(). Three edits.

  (a) AFTER line 1235 (`--teardown`), ADD -- as a MODIFIER of --teardown, the
      same relationship --start has to --seed, so it does NOT enter the
      seeding-mode set tests/test_capture_requests_every_seeding_mode.py
      derives by AST from main()'s `parser.error("choose ...")` guard:

    # OPT-IN, and the reason is that capture.sh is the unattended caller. The
    # predicate is the marker across ALL history, not this run's nonce, so the
    # first non-dry-run clear removes every accumulated row from every previous
    # capture at once (64 at v3.66.844, up from 62 at 842 and 58 at 840). That
    # is the operator's state, not this run's, and it must not be deleted by a
    # cron-shaped process nobody is watching. Run --teardown --clear-history
    # --dry-run first; it reads, canaries the filter, and deletes nothing.
    parser.add_argument("--clear-history", action="store_true",
                        help="after the teardown, delete the marked history "
                             "rows over POST /api/batch/delete (modifies "
                             "--teardown; OFF by default). Combine with "
                             "--dry-run to preview without deleting.")

  (b) At line 1265, replace `unsettled = None` with:

    unsettled = None
    teardown_rc = 0

      and replace the teardown branch (lines 1267-1270) with:

        if args.teardown:
            plan = teardown(client, dry_run=args.dry_run,
                            clear_history=args.clear_history)
            print(json.dumps(plan, indent=2))
            _report_residue(plan)
            teardown_rc = _teardown_exit_code(
                plan, clear_requested=args.clear_history)

  (c) Replace the final `return 0` (line 1385) with:

    if teardown_rc:
        # capture.sh's cleanup_live_seed tests this exit code. Returning 0 on a
        # failed clear is an unknown laundered into an OK; see
        # _teardown_exit_code.
        return teardown_rc
    return 0

--------------------------------------------------------------------------------
2 | capture.sh -- EDITS
--------------------------------------------------------------------------------

2.1  REPLACE cleanup_live_seed's body (lines 743-753) with:

cleanup_live_seed() {
  if [ "$SEEDED" = "1" ]; then
    SEEDED=0
    # BD_SEED_CLEAR_HISTORY is OPT-IN and defaults to 0, for the same reason
    # BD_SEED_FORCE is: this is the UNATTENDED caller. The seeder's clear
    # predicate is the marker across ALL history, not this run's nonce, so the
    # first armed run deletes every bdseed row accumulated by every previous
    # capture (64 at v3.66.844). Arming it is an explicit operator act, and the
    # first run should be:
    #   venv/bin/python tools/live_seed.py --teardown --clear-history --dry-run
    _clear_flag=""
    [ "${BD_SEED_CLEAR_HISTORY:-0}" = "1" ] && _clear_flag="--clear-history"
    venv/bin/python tools/live_seed.py --teardown $_clear_flag \
      >> "$OUT/05a_live_seed.log" 2>&1
    _teardown_exit=$?
    echo "live_seed: TEARDOWN-EXIT=$_teardown_exit" >> "$OUT/05a_live_seed.log"
    if [ "$_teardown_exit" -ne 0 ]; then
      echo "  WARNING: seed teardown exited $_teardown_exit -- synthetic rows" \
           "may remain; run: venv/bin/python tools/live_seed.py --teardown" \
           "$_clear_flag" >&2
      # Select on the diagnostic marker, never on position, and do it HERE.
      # The identical grep at step [5a] runs at SEED time against a log the
      # teardown has not written to yet (the seed truncates with `>`, the
      # teardown appends with `>>` 125 lines later), so it can never carry a
      # teardown reason. `tail`, not `head`: the teardown's lines are the last
      # ones in the file.
      grep 'live_seed: ' "$OUT/05a_live_seed.log" 2>/dev/null \
        | tail -12 | sed 's/^/    /' >&2
    fi
  fi
  if [ -n "$FIXTURE_PID" ]; then
    _stop_process_group "$FIXTURE_PID"
    FIXTURE_PID=""
  fi
}

     NOTE the unquoted `$_clear_flag`: intentional, so an empty value adds no
     argument. Same idiom as the existing `$_seed_force` at capture.sh:840.

2.2  DO NOT add a `--stage-exit "seed-teardown=..."` to the capture_verdict.py
     call at capture.sh:1041. Verified: tools/capture_verdict.py:130-132 turns
     ANY non-zero stage exit into a whole-capture FAIL with no warn tier, and
     capture.sh's step [5a] header states the seed path is "ENTIRELY NON-FATAL".
     Escalating a residue-clear failure to a capture FAIL is over-sensitivity,
     which CLAUDE.md 0 counts as a soundness bug. If the operator later WANTS it
     fatal, the one-line change is to add
         --stage-exit "seed-teardown=$_teardown_exit" \
     and to promote _teardown_exit to a file-scope variable. That is an operator
     decision, not a default.

--------------------------------------------------------------------------------
3 | reports/config_gui_manifest.json -- REQUIRED, easy to forget
--------------------------------------------------------------------------------
Add, in alphabetical position between "BD_SECRETS_FILE" and "BD_SEED_FORCE":

    "BD_SEED_CLEAR_HISTORY": "display-only",

This file is TRACKED and HAND-MAINTAINED (tools/config_surface_inventory.py:965
says "add the key to reports/config_gui_manifest.json, then re-pin"). Any new
BD_-prefixed name -- including a shell variable -- enters tests/test_gui_parity.py's
scan denominator and reads as "promoted but unledgered". BD_SEED_FORCE is
already there from capture.sh:807, which is the precedent and the tier to match.
Precedent for the failure: v3.66.836's BD_PROBE_PORT failed the parity gate on
the box.

--------------------------------------------------------------------------------
4 | RED TESTS -- put them in tests/test_live_seed_starts_and_settles.py
--------------------------------------------------------------------------------
Adding them to the EXISTING file rather than a new one keeps every axis-6 gate
out of the blast radius (a new tests/*.py file moves nine tree-enumerating gates
plus PIN_INDEX's test_files_scanned; editing one moves none of them). Do not add
a fixed-width src[pos:pos+N] window -- test_source_windows_do_not_shift.py
ratchets the COUNT of those and is one-directional.

Prove each RED on pristine source BEFORE implementing (stash the source, keep
the tests). Expected pristine failure mode is given for each.

R1  test_a_failed_clear_does_not_exit_zero
    FakeClient: queue readable, status marked, history returns a 2-row list
    whose site_name carries the marker, and POST /api/batch/delete answers
    {"ok": False, "error": "boom"}. Drive seed.main(["--teardown",
    "--clear-history"]) with seed.Client monkeypatched.
    ASSERT the return is non-zero.
    RED on pristine: argparse rejects the unknown --clear-history (SystemExit 2)
    -- which is a pass for the wrong reason, so ALSO assert
    callable(getattr(seed, "clear_seeded_history", None)) in a separate canary
    test that fails cleanly on pristine. Discriminate the exception: catch
    SystemExit and fail with its code named, do not let it read as the subject.

R2  test_the_remainder_is_measured_after_the_clear_not_computed
    Script the history GET as a SEQUENCE using the file's _Reply mechanism:
    read 1 -> 3 marked rows; read 2 (after the deletes) -> 2 marked rows,
    one of them an id that was NOT in read 1 (a job that settled mid-teardown).
    /api/batch/delete answers {"ok": True, "candidates_matched": 3,
    "processed": 3, "files_deleted": 0, "errors": 0, "dry_run": False}.
    ASSERT residue["history_rows_found"] == 3, residue["history_rows"] == 2,
    and that 2 is not 3-3. Arithmetic would say 0.
    RED on pristine: history_rows is 3 and there is no history_rows_found.

R3  test_the_two_residue_names_do_not_share_a_meaning
    Same client as R2. ASSERT residue["history_rows_found"] != residue
    ["history_rows"] after a clear, AND that with clear_history=False (default)
    the two are equal and equal to the pre-existing behaviour.
    RED on pristine: KeyError/None on history_rows_found.

R4  test_a_malformed_filter_is_caught_before_anything_is_deleted
    /api/batch/delete answers candidates_matched 0 / processed 0 / ok True for
    every call -- the exact body measured from a filter carrying an unexpected
    key. ASSERT the clear reports a hard error naming the canary, ASSERT
    clear["deleted"] == 0, ASSERT the tool made exactly ONE POST to
    /api/batch/delete (the dry-run canary) and no non-dry-run one, and ASSERT
    main() returns non-zero.
    RED on pristine: no canary exists.

R5  test_an_unreadable_history_after_the_clear_is_unknown_not_zero
    Sequence the history GET: read 1 -> 2 marked rows; read 2 -> {"error":"x"}.
    ASSERT residue["history_rows"] is None, residue["readable"] is False, and
    main() returns 5 (_EXIT_CLEAR_UNKNOWN), NOT 0 and NOT 4.
    RED on pristine: exit 0.

R6  test_a_row_this_tool_cannot_prove_it_owns_is_not_deleted
    History returns one row with the marker in the URL and site_name
    "My Real Site". ASSERT no id from that row reaches /api/batch/delete,
    ASSERT clear["unowned"] == 1, ASSERT the remainder is 1 and ok is False.
    RED on pristine: no ownership predicate.

R7  test_the_clear_is_off_unless_asked_for
    seed.teardown(client) with the file's existing stubs. ASSERT no POST path
    contains "/api/batch/delete", ASSERT residue["cleared"] is False, and
    ASSERT main(["--teardown"]) returns 0. This is the amendment-6 guard: it
    fails the moment anyone flips the default on.
    RED on pristine: passes trivially -- so this one is a GUARD, not a RED.
    Label it as such in the file, exactly as the existing guards are.

R8  test_capture_does_not_arm_the_clear_by_default   (reads capture.sh)
    Parse capture.sh with the backslash-continuation join the sibling gate uses
    (tests/test_capture_requests_every_seeding_mode.py:103). ASSERT the
    teardown invocation does not contain a literal "--clear-history", ASSERT
    it does contain "$_clear_flag", and ASSERT the default in the guard line is
    "${BD_SEED_CLEAR_HISTORY:-0}". Cut extracted shell on STRUCTURE (the
    function's closing brace), never a fixed width.

R9  test_the_teardown_exit_code_reaches_capture_sh
    ASSERT capture.sh's cleanup_live_seed captures `$?` into a variable after
    the seeder call and branches on it -- i.e. that the exit is READ, not just
    produced. A `||` on the command line is acceptable too; assert on whichever
    form ships, by structure.
    RED on pristine: the current form is a bare `|| echo`, which is a read --
    so this test must instead assert that the diagnostic grep runs in the
    FAILURE branch of the TEARDOWN, which pristine does not have anywhere.

--------------------------------------------------------------------------------
5 | BAND (derived, not guessed; measured on this tree 2026-08-03)
--------------------------------------------------------------------------------
Derivation used:
    grep -rln "live_seed" tests/*.py            -> 17 files
    grep -rln "capture\.sh" tests/*.py          -> 37 files
    grep -rln "teardown(" tests/*.py            -> 10, of which 3 are live_seed's
    grep -rln "SEED_MARKER\|RESIDUE_NOTE" tests/*.py
Axis-6 re-derived with CLAUDE.md's own recipe (56 candidates read): the set is
unchanged at NINE + tests/test_pin_index_in_sync.py. The two candidates added by
v3.66.843/844 were checked and both are NO --
tests/test_phase_script_collection.py --collect-only's a FIXED list of 8 wrapper
files plus tests/_phase_scripts, and tests/test_stale_locks_check_is_gone.py's
docstring (:32-42) records that its repo-wide gate was deliberately NOT written.
Because the new tests go into an EXISTING file, none of the ten is in scope.

RUN (named files only; never the whole tests/ directory):

  # subject
  tests/test_live_seed_starts_and_settles.py
  tests/test_live_seed.py
  tests/test_live_seed_payload_is_accepted.py
  tests/test_live_seed_urls_are_servable.py
  tests/test_l30_seeded_tunnel_integration.py     # real-app teardown()
  tests/test_l11_observes_a_download_not_a_skip.py # cites RESIDUE_NOTE
  tests/test_requeue_for_dedup_can_actually_dedup.py
  tests/test_seeded_runs_do_not_collide.py
  tests/test_seeded_runs_actually_fetch.py
  tests/test_seeded_site_can_actually_download.py
  tests/test_u34_pipeline_live_tests.py
  tests/test_queue_ts_since_cursor_pin.py

  # new HTTP caller of batch_ops.bulk_delete
  tests/test_empty_id_in_selects_nothing.py
  tests/test_fts_external_content_delete.py

  # capture.sh edited
  tests/test_capture_seeds_live_input.py
  tests/test_capture_requests_every_seeding_mode.py
  tests/test_capture_reports_the_seed_failure_reason.py
  tests/test_capture_vault_is_isolated.py
  tests/test_capture_shell_runtime.py
  tests/test_capture_execution_lanes.py
  tests/test_capture_fixture_roots.py
  tests/test_capture_provides_a_display.py
  tests/test_capture_step4_waits_for_serving.py
  tests/test_capture_runs_every_registered_live_check.py
  tests/test_capture_csrf_diag_redacts_cookies.py
  tests/test_u45_capture_sh_shipped.py
  tests/test_playwright_engines_single_source.py   # any .sh cut joins this

  # BD_-prefixed name added
  tests/test_gui_parity.py

Measured green baseline on the four core files before any edit:
  venv/bin/python -m pytest tests/test_live_seed_starts_and_settles.py \
    tests/test_live_seed.py tests/test_capture_requests_every_seeding_mode.py \
    tests/test_capture_seeds_live_input.py -q   -> exit=0, 70 passed in 2.57s

LEAK TRAP: do not co-band test_phases_195_199 with test_cut8_schedules (neither
is in this band; noted so a widened run does not introduce it).

--------------------------------------------------------------------------------
6 | RELEASE CHORES
--------------------------------------------------------------------------------
  * Version trio together: bulk_downloader/__init__.py __version__,
    tests/test_settings_center_slice4.py:200 pin, CHANGELOG.md (ASCII-only,
    prepended, anchored on the previous "## v" header). Then
    `venv/bin/python tools/build_pin_index.py` and read the form=="version"
    entries out of PIN_INDEX.json rather than assuming there is one.
  * `venv/bin/python toolchain/bin/bd-regen-order --work "$PWD"` AFTER the last
    source edit, not before.
  * No new import edge: live_seed already imports nothing new (json/sys only),
    so the import-graph baseline does not move. Confirm with
    tools/decomp/import_graph_gate.py before assuming it.
  * `git add` the new test additions before the FINAL band run; five of the
    axis-6 gates enumerate `git ls-files` and cannot see untracked files.

--------------------------------------------------------------------------------
7 | THE BOX SQL -- run BEFORE the first real clear
--------------------------------------------------------------------------------
Read-only, and read-only in a way that still sees WAL content and still allows
the fts5vocab temp table. Verified: `PRAGMA query_only=1` REFUSES to create the
temp virtual table ("attempt to write a readonly database"), so it cannot be
used here; `file:<db>?mode=ro` with uri=True works -- temp vocab OK, writes to
history refused, journal_mode reports wal. SQLite 3.45.1.

RUN THE SERVICE WHILE DOING THIS. mode=ro cannot initialise a missing -shm, so
a stopped service with no -shm present will error rather than read stale data.

# ---- as mboyle, on the box ----
cd ~/BulkDownloader
WD=$(systemctl show -p WorkingDirectory --value bulkdownloader); echo "WD=$WD"
DB="$WD/downloader_history.db"; ls -l "$DB" "$DB-wal" 2>&1

venv/bin/python - "$DB" <<'PY' > /tmp/bdseed_recon.txt 2>&1
import sys, sqlite3
db = sys.argv[1]
cx = sqlite3.connect(f"file:{db}?mode=ro", uri=True)
cx.row_factory = sqlite3.Row
q = lambda s, p=(): cx.execute(s, p).fetchall()
M = '%bdseed%'

print("db      :", db)
print("journal :", q("PRAGMA journal_mode")[0][0])

cols = {r[1] for r in q("PRAGMA table_info(history)")}
print("history columns:", sorted(cols))

print("\n[1] rows the clear would TARGET (marker in url):",
      q("SELECT COUNT(*) c FROM history WHERE url LIKE ?", (M,))[0]["c"])

print("[2] LIKE-matched but url-UNMARKED (never deleted; must be 0-ish):",
      q("SELECT COUNT(*) c FROM history WHERE (url LIKE ? OR filename LIKE ? "
        "OR message LIKE ?) AND url NOT LIKE ?", (M, M, M, M))[0]["c"])

print("[3] url-marked but site_name UNMARKED (reported as `unowned`, "
      "NOT deleted -- must be 0 or the clear cannot reach 0):",
      q("SELECT COUNT(*) c FROM history WHERE url LIKE ? "
        "AND COALESCE(site_name,'') NOT LIKE ?", (M, M))[0]["c"])

print("\n[4] breakdown by site_name / status:")
for r in q("SELECT COALESCE(site_name,'<null>') s, COALESCE(status,'<null>') t, "
           "COUNT(*) c FROM history WHERE url LIKE ? GROUP BY 1,2 "
           "ORDER BY c DESC", (M,)):
    print(f"      {r['c']:5d}  {r['s']!r:34s} {r['t']}")

r = q("SELECT MIN(id) a, MAX(id) b, MIN(ts) c, MAX(ts) d, "
      "COUNT(DISTINCT site_id) s FROM history WHERE url LIKE ?", (M,))[0]
print(f"\n[5] id range {r['a']}..{r['b']}   ts {r['c']} .. {r['d']}   "
      f"distinct site_id {r['s']}")

print("\n[6] files these rows point at (residue the clear does NOT remove):")
for r in q("SELECT filename, COUNT(*) c FROM history WHERE url LIKE ? "
           "GROUP BY 1 ORDER BY c DESC LIMIT 15", (M,)):
    print(f"      {r['c']:5d}  {r['filename']!r}")

# [7] library twins. ABSENT COLUMN IS UNKNOWN, NOT "no twins".
if "library_id" not in cols:
    print("\n[7] library twins: UNKNOWN -- history.library_id is ABSENT. It is "
          "added by migration 5 (migrations.py:446-460), which has not run on "
          "this database. This is NOT evidence that there are no twins.")
else:
    print("\n[7] url-marked rows carrying a library_id:",
          q("SELECT COUNT(*) c FROM history WHERE url LIKE ? "
            "AND library_id IS NOT NULL", (M,))[0]["c"])
have_lib = bool(q("SELECT name FROM sqlite_master WHERE type='table' "
                  "AND name='library'"))
if not have_lib:
    print("[8] library rows: UNKNOWN -- the `library` table does not exist "
          "(migration 4 has not run). Not evidence of zero.")
else:
    print("[8] library rows back-referencing a doomed history id "
          "(these SURVIVE the clear and their history_id will DANGLE, because "
          "PRAGMA foreign_keys is never enabled -- library.py:469-474):",
          q("SELECT COUNT(*) c FROM library WHERE history_id IN "
            "(SELECT id FROM history WHERE url LIKE ?)", (M,))[0]["c"])

# [9] FTS index membership. /api/batch/delete DISCARDS db_fts_forget's report
#     (batch_ops.py:199-200), so this is the only way to see the outcome.
if not q("SELECT name FROM sqlite_master WHERE type='table' "
         "AND name='history_fts'"):
    print("\n[9] history_fts: ABSENT -- UNKNOWN, not clean. FTS5 may not be "
          "compiled in; db_init fails open (db.py:132-136).")
else:
    cx.execute("DROP TABLE IF EXISTS temp._bdseed_vocab")
    cx.execute("CREATE VIRTUAL TABLE temp._bdseed_vocab USING "
               "fts5vocab('main','history_fts','instance')")
    docs = {r[0] for r in q("SELECT DISTINCT doc FROM temp._bdseed_vocab")}
    hist = {r[0] for r in q("SELECT id FROM history")}
    doomed = {r[0] for r in q("SELECT id FROM history WHERE url LIKE ?", (M,))}
    print(f"\n[9] fts docs {len(docs)} | history rows {len(hist)} | "
          f"orphan docs already present {len(docs - hist)} | "
          f"doomed rows currently indexed {len(doomed & docs)} | "
          f"doomed rows NOT indexed (nothing to forget) "
          f"{len(doomed - docs)}")
    print("    RECORD THESE FOUR NUMBERS. After the clear, re-run this block: "
          "orphan docs must not have grown by more than the number of doomed "
          "rows whose FTS-indexed text was changed in place (expected 0).")
PY
cat /tmp/bdseed_recon.txt

DECISION RULE before arming anything:
  [2] must be 0 or near it, and every hit inspected by hand. A non-zero [2] is
      a row the LIKE reaches whose URL is not seeded -- the tool will not delete
      it (it re-checks the url) but you should know it exists.
  [3] MUST be 0. A non-zero [3] means some seeded rows are not provably owned,
      the clear will skip them, and the remainder will never reach 0 -- which
      the tool will correctly report as ok=false. Decide before, not after.
  [8] is the size of the residue the clear does NOT remove. If it is large, say
      so in the register rather than letting "0 history rows remain" read as
      "the box is as it was found".

THEN, in this order:
  1) venv/bin/python tools/live_seed.py --teardown --clear-history --dry-run
     Expect: clear.canary.matched == clear.canary.sent, deleted 0, exit 0.
     A canary mismatch here means STOP -- the filter shape is wrong and nothing
     has been deleted.
  2) venv/bin/python tools/live_seed.py --teardown --clear-history
     echo "exit=$?"          # UNPIPED
     Expect exit 0, "live_seed: CLEARED - 64 of 64 ... 0 remain".
  3) Re-run the recon block above; [1] must be 0 and [9]'s orphan count must be
     unchanged.
  4) Only then set BD_SEED_CLEAR_HISTORY=1 for capture runs, if wanted at all.

================================================================================