# GENERATED SPEC -- NOT A CONTRACT, NOT VERIFIED

Machine-generated by a multi-agent investigation on 2026-08-03 and kept only
because it could not be cheaply re-derived. Read this before reading anything
below it:

  * CLAUDE.md is the ONLY agent-facing contract. This file is not one. Where
    they disagree, CLAUDE.md wins and this file is the defect.
  * project-knowledge/SESSION_CARRY.md is the ONLY register. Every
    DECISION-CRITICAL fact from this spec -- premises, amendments, measured
    figures, file:line anchors -- is already recorded there in sections 15.20,
    15.21 and 15.23. Those sections, not this file, are authoritative.
  * This spec was ADVERSARIALLY REVIEWED and did NOT pass clean. Its surviving
    objections are listed in SESSION_CARRY 15.20. Applying it verbatim without
    those amendments ships a known defect.
  * Every figure here was measured against the tree at v3.66.842-846. Re-derive
    before acting; CLAUDE.md section 1 applies to this file too.
  * DELETE THIS FILE when its item ships. A stale spec that outlives its cut is
    exactly the second-document failure that retired TASK_TRACKER and
    CODEX_HANDOFF.

---

REDESIGN: close the repo-root .db/-wal class at its two seams, as TWO CUTS (one feature per cut; both sequenced AFTER 15.8 lands). No global db.DB_PATH rebinding anywhere (kills fatal 2); the collection gate closes all 30 module-scope importers through one seam (kills fatal 1).

The -wal/-shm siblings are the git-visible tripwire and STAY unignored: once no writer ever opens a repo-root DB, no WAL can be stranded there, and any future reappearance in git status is real signal.

====================================================================
CUT 1 -- collection-time class (dominant writer: 30 test files)
====================================================================
Mechanism: tests/conftest.py sets a sys attribute for the DURATION OF COLLECTION ONLY; app.py's boot-persistence block is skipped while it is set. Every run-time consumer (fresh_app's explicit db_init(), bd_module_wipe re-imports, subprocesses) executes after pytest_collection_finish pops it. Production never sets it. sys attribute, NOT a BD_* env var: children spawned by tests must not inherit the skip, and a new BD_ name enters the P2 env-ledger denominator (config_surface_inventory.py:34-40; the v3.66.836 box failure).

--- EDIT 1: tests/conftest.py ---
Insert immediately after line 30 (`sys.path.insert(0, str(PKG_ROOT))`):

# --- Collection-time boot suppression -----------------------------------------
# Importing bulk_downloader.app runs its boot-persistence block (db_init +
# run_history.init + integrity/FTS/queue-recovery), which resolves the DB
# against cwd when BD_INSTALL_DIR is unset. Collection imports test modules
# from the LAUNCH cwd -- the repo root -- so every band deposited
# downloader_history.db there before any fixture ran (352,256 bytes measured
# at v3.66.844; .gitignore covers *.db but NOT the -shm/-wal siblings, which
# is why a worker killed mid-WAL shows in git status). The sentinel below is
# visible ONLY between conftest import and pytest_collection_finish: module-
# scope imports (collection) see it; every test body, fixture, bd_module_wipe
# re-import and child process runs after the pop and boots normally. A sys
# attribute rather than a BD_* env var, for two measured reasons: children
# spawned by tests must NOT inherit the skip, and a BD_-prefixed name enters
# the config-parity env ledger denominator (tools/config_surface_inventory.py
# matches the BD_ prefix; see the v3.66.836 box failure in CLAUDE.md 2a).
sys._bd_collection_no_boot = True


def pytest_collection_finish(session):
    """Collection is over: re-arm app.py's boot-persistence for run time."""
    if getattr(sys, "_bd_collection_no_boot", False):
        try:
            del sys._bd_collection_no_boot
        except AttributeError:
            pass

(Verified by experiment: sentinel True at module import, absent at run time, under plain pytest AND -n 2 --dist loadfile. Each xdist worker sets and pops its own.)

--- EDIT 2: bulk_downloader/app.py ---
Replace lines 79-140 (from the `# --- FLASK ---...` header line through the final `    pass` of the run_integrity_check try at :140; the region currently begins `db_init()` at :80 and everything below :140 -- `app = Flask(...)` at :146 onward -- is untouched). New text (existing comments preserved verbatim, reindented +4; the run_history import is hoisted so the module attribute exists unconditionally):

# --- FLASK --------------------------------------------------------------------
# v3.66.xxx: the boot-persistence block below runs at IMPORT. Under pytest, 30
# test files import this module at COLLECTION time (module-scope imports, cwd
# still the launch directory), so every band/collect deposited
# downloader_history.db + .premigration.bak + three dot-sentinels -- and, on a
# kill mid-WAL, the un-gitignored -wal/-shm siblings -- into the repo root.
# tests/conftest.py sets sys._bd_collection_no_boot for the DURATION OF
# COLLECTION ONLY (popped in pytest_collection_finish), so every RUN-time
# consumer of these tables (fresh_app's explicit db_init(), bd_module_wipe
# re-imports) executes with the sentinel absent and is behaviour-identical.
# Production never sets it. ALL SIX blocks below touch the DB and must stay
# inside this gate together -- any one left outside re-creates the DB at cwd.
from . import run_history as _run_history
if not getattr(sys, "_bd_collection_no_boot", False):
    db_init()
    # B1 (post-365): run-history substrate tables (job_runs + run_events). Advisory
    # store -- init failing is non-fatal (every writer no-ops if the store is absent).
    _run_history.init()
    # v3.47.8 (#42): one-shot integrity check on boot (rate-limited to 24h
    # via sentinel file). Catches SQLite corruption from power loss or disk
    # errors before the user wonders why a download "completed" but vanished.
    try:
        _ok, _problems = db_integrity_check()
        if not _ok:
            from . import log as _boot_log
            _boot_log.get_logger(__name__).error(
                "DB INTEGRITY CHECK FAILED -- %d problem(s) reported by SQLite. "
                "Consider running `bdctl db-vacuum` or restoring from backup. "
                "First 3 problems: %s",
                len(_problems), _problems[:3]
            )
    except Exception as _e:
        from . import log as _boot_log
        _boot_log.get_logger(__name__).warning(
            "db_integrity_check raised: %s -- skipping", _e
        )

    # v3.48 (#75): weekly FTS5 index optimization. No-op if FTS isn't
    # present or if it ran recently (7-day sentinel rate limit).
    try:
        from .db import db_fts_optimize as _fts_opt
        _fts_ran, _fts_msg = _fts_opt()
        if _fts_ran:
            from . import log as _boot_log
            _boot_log.get_logger(__name__).info("FTS optimize: %s", _fts_msg)
    except Exception as _e:
        pass  # FTS optimize is best-effort

    # v3.48 (#127): log queue-recovery summary so the operator can confirm
    # no jobs were silently dropped across the restart.
    try:
        from .db import db_queue_recovery_summary
        _recover = db_queue_recovery_summary()
        from . import log as _boot_log
        if _recover.get("total", 0) > 0:
            _boot_log.get_logger(__name__).info(
                "queue recovery: %d job(s) restored from persistence -- %s",
                _recover["total"], _recover.get("by_status", {})
            )
    except Exception as _e:
        pass

    # v3.47.8 (#42): fire a deep PRAGMA integrity_check on a background thread
    # once per 24 hours. Boot-time selftest already does PRAGMA quick_check
    # synchronously; this runs the slower, more thorough variant async so the
    # app comes up at full speed even on a multi-GB DB. Result goes to the
    # standard log; corruption is reported at ERROR.
    try:
        from .db import run_integrity_check as _run_db_integrity
        _run_db_integrity()  # debounced -- does nothing if last run was <24h ago
    except Exception:
        # Integrity scheduling failures must NOT break startup. The selftest
        # quick_check already provided minimum coverage.
        pass

NOTE ON COMMENT BYTES: the original comments at :81-82, :85-87, :92-96, :101, :104-105, :115-116, :123, :129-133, :136, :138-139 contain em-dash characters; when applying, preserve the ORIGINAL bytes of those pre-existing comment lines exactly (only indentation added). The `--` shown above is a transport artifact of this spec, not an instruction to change them. `sys` is already imported at app.py:5.

--- EDIT 3: tests/test_v3_43_24_reliability.py:357 ---
old:    db_init_pos = src.find("\ndb_init()")
new:    # v3.66.xxx: db_init() is nested one level under the collection gate;
        # a bare find("db_init()") would anchor the COMMENT at app.py:24.
        db_init_pos = src.find("\n    db_init()")
(keep the surrounding asserts and message unchanged)

--- EDIT 4: NEW FILE tests/test_collection_leaves_no_repo_db.py (the RED) ---
Exact content parse-checked; RED on pristine proven by reproduction (deposit measured), GREEN requires Edits 1+2:

"""Pytest COLLECTION must not deposit downloader_history.db into the launch cwd.

Importing bulk_downloader.app at module scope executes app.py's boot-persistence
block (db_init + run_history.init + integrity/FTS/queue-recovery) from whatever
directory pytest was launched in. Measured at v3.66.844: 29 test files import
the app at module scope directly, 1 more via downloader_ui, and one
--collect-only of a single such file from a bare cwd deposits 352,256 bytes of
DB-class artifacts (downloader_history.db 217088 + .premigration.bak 135168)
plus three dot-sentinels, before any fixture exists. .gitignore covers *.db but
NOT the -shm/-wal siblings, so a worker killed while a WAL connection is open
leaves them visible in git status.

The fix is the collection sentinel in tests/conftest.py + the gate in app.py.
This test proves the class stays closed. app_config.json / logs/ /
live_recordings/ are DIFFERENT subjects (cwd-relative, all gitignored) and are
deliberately not asserted here.
"""
import ast
import os
import subprocess
import sys
from pathlib import Path

REPO = Path(__file__).resolve().parent.parent
SUBJECT = REPO / "tests" / "test_csrf_origin_guard.py"


def _module_scope_imports_app(path):
    """AST, not grep: a module-scope Import/ImportFrom targeting exactly
    bulk_downloader.app (boundary '.', never '_')."""
    tree = ast.parse(path.read_text(encoding="utf-8"))
    FUNC = (ast.FunctionDef, ast.AsyncFunctionDef, ast.Lambda)

    def walk(node, in_func):
        for child in ast.iter_child_nodes(node):
            yield child, in_func
            yield from walk(child, in_func or isinstance(child, FUNC))

    for node, in_func in walk(tree, False):
        if in_func:
            continue
        if isinstance(node, ast.Import) and any(
                a.name == "bulk_downloader.app"
                or a.name.startswith("bulk_downloader.app.")
                for a in node.names):
            return True
        if isinstance(node, ast.ImportFrom):
            m = node.module or ""
            if m == "bulk_downloader.app" or m.startswith("bulk_downloader.app."):
                return True
            if m == "bulk_downloader" and any(a.name == "app" for a in node.names):
                return True
    return False


def test_collect_only_deposits_no_db_in_cwd(tmp_path):
    # Precondition: the subject still imports the app at module scope. If this
    # fires, the test is aiming at a dead subject -- re-derive the importer set
    # and point SUBJECT at a live member; do not delete the assertion.
    assert _module_scope_imports_app(SUBJECT), (
        f"{SUBJECT.name} no longer imports bulk_downloader.app at module "
        "scope; re-derive the importer set and repoint SUBJECT")
    env = {k: v for k, v in os.environ.items()
           if k not in ("BD_INSTALL_DIR", "PYTEST_CURRENT_TEST")}
    r = subprocess.run(
        [sys.executable, "-m", "pytest", "--collect-only", "-q",
         "-p", "no:cacheprovider", str(SUBJECT)],
        cwd=tmp_path, env=env, capture_output=True, text=True, timeout=180)
    # The gate must SEE its subject: collection succeeded and collected tests.
    # An import error would exit non-zero and prove nothing about residue.
    assert r.returncode == 0, r.stdout + r.stderr
    assert "collected" in (r.stdout + r.stderr), r.stdout
    residue = sorted(
        p.name for p in tmp_path.iterdir()
        if p.name.startswith("downloader_history.db")
        or p.name in (".integrity_check_last", ".integrity_last_run",
                      ".fts_optimize_last"))
    assert residue == [], (
        "collection deposited DB artifacts into the launch cwd: %s" % residue)

--- CUT 1 protocol ---
1. RED first: add EDIT 4 alone; run
   venv/bin/python -m pytest tests/test_collection_leaves_no_repo_db.py -x > /tmp/red1 2>&1; echo "exit=$?"   (must FAIL on residue)
2. Apply Edits 1-3. Re-run: must PASS.
3. Version trio (three edits together per CLAUDE.md 3) + ASCII changelog.
4. git add ALL files (five axis-6 gates enumerate git ls-files; a new test file is invisible to them until staged), THEN regen:
   venv/bin/python toolchain/bin/bd-regen-order --work "$PWD"   (PIN_INDEX must be regenerated -- tools/build_pin_index.py globs tests/*.py at :151/:211, so the new test file moves test_files_scanned)
5. Band (re-derive at cut time; measured starting set):
   - tests/test_collection_leaves_no_repo_db.py, tests/test_v3_43_24_reliability.py
   - fixed-window app.py readers (from the escape-aware anchor scan; re-derive with:
     grep -rlE "\\[\\s*\\w*pos\\w*\\s*:\\s*\\w*pos\\w*\\s*\\+" tests/*.py | xargs grep -l "app" ):
     test_phase1_root_flip test_v3_43_26_qb_bridge test_v3_43_28_stash_deep test_v3_43_29_plex_deep test_v3_43_30_watch_folder test_v3_43_31_rate_limit test_v3_43_33_ai_login test_v3_43_34_sse_broker test_v3_43_35_account_pool test_v3_43_37_jellyfin_deep test_v3_43_37_retry_policy test_v3_43_38_dashboard_widgets test_v3_43_39_captcha_resolver test_v3_43_40_extension_routing test_v3_43_41_download_window test_v3_43_43_multi_provider_ai test_v3_43_44_heuristic_scoring test_v3_43_45_template_extractor test_v3_43_46_audit test_v3_43_47_audit test_v3_43_54_resolution test_v3_43_56_stealth
   - boot-symbol readers: test_healthcheck_disk_captures test_selftest_captures_root_disk test_stale_locks_check_is_gone test_t2_fixture_controller test_t3_db_inspectors test_v3_66_657_rob3_selftest_battery test_t56_systemd_missing_unit test_v3_66_271_gcw2_pick_overlay
   - run-time boot consumers (prove behaviour-identical): test_b1_run_history test_cut4_runs_filter test_api plus one bd_module_wipe-marked file (derive: grep -l bd_module_wipe tests/*.py | head -1)
   - axis-6 (new test file): test_pin_index_in_sync test_source_windows_do_not_shift test_pytest_runner_boundary test_capture_execution_lanes test_deploy_manifest_stays_retired test_task_tracker_stays_retired test_codex_handoff_stays_retired test_all_sources_parse test_generated_artifacts_are_not_tracked test_history_columns_go_through_migrations
   Leak trap: never co-band test_phases_195_199 with test_cut8_schedules. Named FILES, never the whole tests/ dir.

====================================================================
CUT 2 -- tool-process class (subprocess writers)
====================================================================
Mechanism: divert THE CLI PROCESS ONLY via env BD_INSTALL_DIR (db._resolve_db_path branch 2, resolved at call time) to a mkdtemp scratch, removed in a finally. In-process callers (the 9 executor test files import build()) are untouched -- no module-global rebinding exists anywhere in this design. Verified: url_map byte-identical under the divert (996 route items, route_source 'live url_map' both ways).

--- EDIT 5: tools/gui_parity_inventory.py, main() (currently :911-931) ---
After `a = ap.parse_args(argv)`, wrap the remainder:

    # v3.66.xxx: build() imports bulk_downloader.app, whose boot-persistence
    # resolves the DB against BD_INSTALL_DIR (db._resolve_db_path branch 2)
    # or, unset, against CWD -- so every CLI run from the repo root
    # (capture.sh [2a], install_linux.sh, provision_test_host.sh step 07,
    # bd-regen-order, tests spawning this tool with cwd=repo) deposited
    # downloader_history.db + .premigration.bak (352,256 bytes measured) and
    # three dot-sentinels there. Divert THE CLI PROCESS ONLY: in-process
    # callers (tests import build()) are governed by their own fixtures, and
    # rebinding db.DB_PATH globally is exactly the cross-file contamination
    # the 15.x bisect killed. Verified: the url_map is INVARIANT under this
    # divert (996 route items byte-equal, route_source 'live url_map').
    _scratch = None
    if not os.environ.get("BD_INSTALL_DIR"):
        import shutil as _shutil
        import tempfile as _tempfile
        _scratch = _tempfile.mkdtemp(prefix="bd-parity-scratch-")
        os.environ["BD_INSTALL_DIR"] = _scratch
    try:
        d = build(a.root)
        if a.json:
            print(json.dumps(d, indent=2))
            return 0
        os.makedirs(a.outdir, exist_ok=True)
        jp = os.path.join(a.outdir, "gui_parity_inventory.json")
        mp = os.path.join(a.outdir, "gui_parity_inventory.md")
        if _RC:
            _RC.write_json(jp, d)
            _RC.write_report(a.outdir, "gui_parity_inventory.md", _md(d))
        else:
            open(jp, "w").write(json.dumps(d, indent=2, default=str))
            open(mp, "w").write(_md(d))
        print(f"wrote {jp} and {mp}: {d['counts']['total']} items")
        return 0
    finally:
        if _scratch is not None:
            os.environ.pop("BD_INSTALL_DIR", None)
            _shutil.rmtree(_scratch, ignore_errors=True)

--- EDIT 6: tools/route_map_snapshot.py, __main__ block (:41-42) ---
old:
if __name__ == "__main__":
    sys.stdout.write(snapshot())
new:
if __name__ == "__main__":
    # v3.66.xxx: snapshot() imports bulk_downloader.app; from a bare cwd its
    # boot-persistence deposits downloader_history.db (+ WAL siblings on a
    # kill) into that cwd -- the repo root in the CLAUDE.md section-4 recipe
    # and in bd-regen-order --declare-surface. Divert the CLI process only;
    # stdout stays the ONLY output (the baseline redirect depends on that).
    _scratch = None
    if not os.environ.get("BD_INSTALL_DIR"):
        import shutil as _shutil
        import tempfile as _tempfile
        _scratch = _tempfile.mkdtemp(prefix="bd-snap-scratch-")
        os.environ["BD_INSTALL_DIR"] = _scratch
    try:
        sys.stdout.write(snapshot())
    finally:
        if _scratch is not None:
            os.environ.pop("BD_INSTALL_DIR", None)
            _shutil.rmtree(_scratch, ignore_errors=True)

--- EDIT 7: toolchain/bin/bd-regen-order, main() -- APPLIED BYTE-IDENTICALLY TO project-knowledge/bd-regen-order AS WELL (tests/test_generated_artifact_workflow.py:313 pins byte equality) ---
Insert immediately after the `if not os.path.isdir(work): ... return 1` block (:273-275), wrapping the remainder of main() (from `failed = []` through the final `return 0`) in try/finally:

    # v3.66.xxx: every child this tool runs (the CHAIN generators,
    # check_reach's -c probe, declare_surface's route_map_snapshot) inherits
    # this process env and runs with cwd=work -- so each app-importing child
    # deposited downloader_history.db into the work tree root. One scratch
    # dir, exported for the duration of main() and removed afterwards, covers
    # every child INCLUDING the -c probe that no per-tool __main__ divert can
    # reach. In-process callers (test_generated_artifact_workflow calls
    # main()) get their exact env back: the finally restores or removes it.
    _scratch = None
    if not os.environ.get("BD_INSTALL_DIR"):
        import shutil
        import tempfile
        _scratch = tempfile.mkdtemp(prefix="bd-regen-scratch-")
        os.environ["BD_INSTALL_DIR"] = _scratch
    try:
        <existing body from `failed = []` through `return 0`, reindented +4>
    finally:
        if _scratch is not None:
            os.environ.pop("BD_INSTALL_DIR", None)
            shutil.rmtree(_scratch, ignore_errors=True)

(--dry-run and --selftest return before this point and stay allocation-free.)

--- EDIT 8: NEW FILE tests/test_tool_runs_leave_no_repo_db.py (the REDs) ---
Three tests, parse-checked. RED on pristine: tests 1-2 fail on cwd residue (deposit measured); test 3 fails on seen install_dir None. The route_source assert is the POSITIVE can-see guard demanded by this unit: _routes_from_app catches ALL exceptions and returns None, so a divert that broke the app import would silently produce the catalog fallback AND no residue -- a residue-only test would go green on a broken fix.

"""The route-harvest CLIs must not deposit downloader_history.db into their cwd.

gui_parity_inventory and route_map_snapshot import bulk_downloader.app, whose
boot-persistence resolves the DB against BD_INSTALL_DIR or, unset, the child's
cwd -- which is the repo root for capture.sh [2a], install_linux.sh,
provision_test_host.sh step 07, the CLAUDE.md section-4 baseline recipe, and
test_v3_66_302_gui_parity_reconcile's fixture (cwd=_REPO). Measured deposit per
run from a bare cwd at v3.66.844: 352,256 bytes DB-class + three sentinels.

Each test runs the CLI from a throwaway cwd with BD_INSTALL_DIR stripped and a
private TMPDIR, then asserts THREE things: the output is the live-url_map one
(a divert that broke the app import would silently fall back to the catalog --
route_source is the can-see guard), no DB-class residue in the cwd, and no
leaked scratch dirs in TMPDIR (the divert must clean up after itself)."""
import importlib.machinery
import importlib.util
import json
import os
import subprocess
import sys
from pathlib import Path

REPO = Path(__file__).resolve().parent.parent
SENTINELS = {".integrity_check_last", ".integrity_last_run", ".fts_optimize_last"}


def _env_without_install_dir(tmp_tmpdir):
    env = {k: v for k, v in os.environ.items() if k != "BD_INSTALL_DIR"}
    env["TMPDIR"] = str(tmp_tmpdir)
    return env


def _db_residue(cwd):
    return sorted(p.name for p in Path(cwd).iterdir()
                  if p.name.startswith("downloader_history.db")
                  or p.name in SENTINELS)


def test_gui_parity_inventory_cli_no_residue_and_live_urlmap(tmp_path):
    cwd = tmp_path / "cwd"
    tdir = tmp_path / "tmp"
    cwd.mkdir()
    tdir.mkdir()
    r = subprocess.run(
        [sys.executable, str(REPO / "tools" / "gui_parity_inventory.py"),
         "--root", str(REPO), "--json"],
        cwd=cwd, env=_env_without_install_dir(tdir),
        capture_output=True, text=True, timeout=180)
    assert r.returncode == 0, r.stderr
    d = json.loads(r.stdout)
    # POSITIVE can-see guard: the catalog fallback would also be residue-free.
    assert d["route_source"] == "live url_map", d["route_source"]
    assert d["counts"]["total"] > 100
    assert _db_residue(cwd) == []
    leaked = sorted(p.name for p in tdir.iterdir())
    assert leaked == [], f"scratch dirs leaked in TMPDIR: {leaked}"


def test_route_map_snapshot_cli_no_residue_and_live_urlmap(tmp_path):
    cwd = tmp_path / "cwd"
    tdir = tmp_path / "tmp"
    cwd.mkdir()
    tdir.mkdir()
    r = subprocess.run(
        [sys.executable, str(REPO / "tools" / "route_map_snapshot.py")],
        cwd=cwd, env=_env_without_install_dir(tdir),
        capture_output=True, text=True, timeout=180)
    assert r.returncode == 0, r.stderr
    rows = [l for l in r.stdout.splitlines() if l.count("\t") == 2]
    # snapshot() has no catalog fallback, but empty/garbled stdout is the same
    # silent-failure shape; the url_map carries hundreds of rules.
    assert len(rows) > 200, f"snapshot shape wrong: {len(rows)} rows"
    assert _db_residue(cwd) == []
    assert sorted(p.name for p in tdir.iterdir()) == []


def _load_regen_tool():
    # Same loader as tests/test_generated_artifact_workflow.py:65.
    path = REPO / "toolchain" / "bin" / "bd-regen-order"
    loader = importlib.machinery.SourceFileLoader("bd_regen_order_s4", str(path))
    spec = importlib.util.spec_from_loader(loader.name, loader)
    assert spec is not None
    module = importlib.util.module_from_spec(spec)
    loader.exec_module(module)
    return module


def test_regen_order_exports_scratch_install_dir_to_children(tmp_path, monkeypatch):
    """Every child bd-regen-order runs (CHAIN, check_reach's -c probe,
    declare_surface) inherits its env with cwd=work; a scratch BD_INSTALL_DIR
    exported for main()'s duration is the only seam that reaches the -c probe.
    The spy stands where check_reach runs, i.e. where children observe env."""
    regen = _load_regen_tool()
    seen = {}
    monkeypatch.setattr(sys, "argv", ["bd-regen-order", "--work", str(tmp_path)])
    monkeypatch.setattr(regen, "CHAIN", [])
    monkeypatch.setattr(regen, "VERIFY", [])

    def _spy(_work):
        seen["install_dir"] = os.environ.get("BD_INSTALL_DIR")
        return True, "in sync"

    monkeypatch.setattr(regen, "check_reach", _spy)
    monkeypatch.delenv("BD_INSTALL_DIR", raising=False)
    assert regen.main() == 0
    assert seen.get("install_dir"), (
        "main() did not export a scratch BD_INSTALL_DIR to its children")
    assert os.environ.get("BD_INSTALL_DIR") is None, (
        "main() leaked BD_INSTALL_DIR after returning")
    assert not Path(seen["install_dir"]).exists(), "scratch dir not removed"

--- CUT 2 protocol ---
1. RED first: add EDIT 8 alone; run it (all three tests must FAIL: 1-2 on residue, 3 on missing export). Unpiped exit capture.
2. Apply Edits 5-7 (7 to BOTH copies). Re-run: PASS.
3. Version trio + ASCII changelog; git add all; bd-regen-order regen AFTER the last source edit (PIN_INDEX moves again -- new test file).
4. Band: tests/test_tool_runs_leave_no_repo_db.py; the 9 in-process executors (test_gui_parity test_parity_method_aware test_v3_66_328_spa_add_url test_v3_66_329_spa_transform_clear test_v3_66_330_spa_site_integration_actions test_v3_66_336_consolidated test_v3_66_696_jd_coverage_button test_v3_66_754_singleton_wiring test_v3_66_770_vpn_webrtc_killswitch); test_v3_66_302_gui_parity_reconcile; test_route_map_invariant; test_route_index_in_sync (the FILE, never the function name); test_bd_decomp; test_generated_artifact_workflow; test_bd_regen_check_is_read_only; test_provision_test_host; test_cloud_setup_truthfulness; test_u45_capture_sh_shipped; the axis-6 set as in cut 1. Also run tools/decomp/import_graph_gate.py in CHECK mode (new function-scope stdlib imports should not move the baseline; if it fires, the edge change must be declared in-cut).
5. No ROUTE_INDEX/route_map baseline change is expected (url_map invariance measured); if regen shows a diff there, STOP -- that is the fail-open registration dropping a route under the divert, and the divert must not ship until explained.

WHAT THIS CLOSES / WHAT REMAINS ARMED (say so): closed -- all 30 collection-time importers (one seam), --collect-only from any cwd, the 302-reconcile fixture's cwd=_REPO child, capture.sh [2a], install_linux.sh, provision_test_host.sh step 07, the CLAUDE.md section-4 snapshot recipe, bd-regen-order's entire child set including check_reach's -c probe. Still armed (documented residual, out of this unit's scope): direct ad-hoc `python -c "import bulk_downloader.app"` probes from the repo root (CLAUDE.md section 5's existing trap); direct manual runs of tools/build_endpoint_catalog.py, tools/dast_probe.py, tools/security_probe.py from the repo root (no script invokes them there today; same __main__ recipe applies if ever needed); non-DB gitignored litter at import (app_config.json, logs/, live_recordings/) -- different subjects, all git-invisible.