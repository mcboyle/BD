# Recon plans: queue items 1-9, with adversarial review

PROVENANCE. Produced 2026-08-04/05 by a nine-agent read-only recon over commit
3fded70, then reviewed by three adversarial lenses (red-really-red,
fix-reproduces-shape, band-completeness). Every agent was required to CONFIRM
its defect still existed and paste real command output as evidence, rather than
trust the queue entry. The lenses ran against a25e577 and say so -- the tree
moved mid-run, and they tracked it.

WHAT THIS IS AND IS NOT. A work-in-progress spec, NOT a contract. CLAUDE.md
section 8 is explicit that there is ONE agent-facing document, and a second one
an agent reads before acting is the defect rather than a resource. Do not read
this instead of CLAUDE.md. Read it when you pick up one of these items, and
RE-DERIVE before acting: SESSION_CARRY 15.22 records agents handed a spec
WITHOUT its amendments reproducing the spec's defects faithfully.

STALENESS. Every file:line anchor is against 3fded70/a25e577. This container
reverted its checkout three times on 2026-08-04, and one source read against a
stale tree produced a confidently wrong conclusion about a fix that was
present. Verify first: git -C /home/user/BD log --oneline -1

VERDICTS: 9 plans. 7 REAL, 2 PARTIALLY-REAL, 0 NOT-REAL. The two
PARTIALLY-REAL (items 7 and 8) are the ones to read before trusting the queue
entry -- both were filed as flat statements and came back qualified.

REVIEW COVERAGE: 2 of 3 lenses captured below. A missing lens is a gap in the
review, not a clean bill.


## Plans

```json
[
  {
    "item": "ITEM 1 (Batch A) -- bd-parband attributes a verdict to a suite it never ran; plus .bd_last_band.json has no gitignore rule",
    "defect_confirmed": "REAL",
    "evidence": "HEAD VERIFIED:\n$ git -C /home/user/BD log --oneline -1\n3fded70 Register 15.29: seven cuts, a contaminated corpus, and three corrected figures (#169)\n\n--- HALF 1, DIRECTION A: a bad path falls through to a broad run, verdict attributed to the missing suite ---\n$ BD_LAST_BAND=/tmp/bd_last_band_probe.json venv/bin/python toolchain/bin/bd-parband \\\n      tests/test_ITEM1_no_such_file.py tests/test_ITEM1_also_missing.py --jobs 2 --timeout 120\nexit=1\nparallel band 2 suites, 2 jobs, 120s each\n  FAIL  tests/test_ITEM1_no_such_file.py  32.6s\n  FAIL  tests/test_ITEM1_also_missing.py  32.6s\n\n2/2 suite(s) not clean (wall 32.6s vs ~65.2s sequential)\n  tests/test_ITEM1_no_such_file.py tail:\n      File \"/home/user/BD/tests/test_code_intelligence_adapters.py\", line 354, in __iter__\n        raise RuntimeError(\"hostile-list-secret\")\n    RuntimeError: hostile-list-secret\n  tests/test_ITEM1_also_missing.py tail:\n      File \"/home/user/BD/tests/test_code_intelligence_adapters.py\", line 354, in __iter__\n        raise RuntimeError(\"hostile-list-secret\")\n\nNeither requested path exists. The tail comes from tests/test_code_intelligence_adapters.py -- a file that was never requested. Matches the earlier measurement (50.2s there, 32.6s here; same shape). The written results file carries the same misattribution:\n  tests/test_ITEM1_no_such_file.py | fail | failed= -1 | passed= None | tail-mentions-adapters: True\n  tests/test_ITEM1_also_missing.py | fail | failed= -1 | passed= None | tail-mentions-adapters: True\n\n--- HALF 1, DIRECTION B: it MINTS A PASS for a nonexistent suite when the fallback run is green ---\n(the two quarantine env vars are run_tests_core's existing opt-in; they only make the\nfallback fast+green, they are NOT what causes the fall-through -- direction A above ran without them)\n$ BD_LAST_BAND=/tmp/bd_last_band_pass.json BD_QUARANTINE_SKIP=1 \\\n  BD_QUARANTINE_MANIFEST=/tmp/q_all_but_one.json \\\n  venv/bin/python toolchain/bin/bd-parband tests/test_ITEM1_no_such_file.py --jobs 1 --timeout 200\nexit=0\nparallel band 1 suites, 1 jobs, 200s each\n  PASS  tests/test_ITEM1_no_such_file.py  0.3s\nall 1 suites clean\nresults JSON:\n {\"suite\": \"tests/test_ITEM1_no_such_file.py\", \"status\": \"pass\", \"failed\": 0, \"passed\": 5,\n  \"tail\": \"...PASS test_action_timeline_effect_is_counts_only\\n PASS test_dom_log_event_shape\\n\n           PASS test_floor_load_bearing_keys_present\\n PASS test_network_log_entry_shape\\n\n           PASS test_toplevel_shape_pinned\\n Total: 5 | Passed: 5 | Failed: 0 | Skipped: 0\"}\nA green verdict with passed=5, for a file that does not exist. The 5 named tests are\ntests/test_capture_dict_shape_tripwire.py's.\n\n--- THE MECHANISM (callee read, not guessed) ---\n$ venv/bin/python run_tests.py tests/test_this_file_does_not_exist_xyz.py\n  WARN  no such file: tests/test_this_file_does_not_exist_xyz.py\n======================================================================\nRunning BulkDownloader test suite (minimal runner)      <-- the NO-FILTERS branch\n======================================================================\n--- test_2c_guard_zero_match_interlock.py ---   ... (834 lines and still going at 45s)\n\nrun_tests_core.py:1277-1284 prints WARN and `continue`s; with every requested path missing,\n`filters` is empty, so :1288 takes the else branch and :1303 sets\nfiles_to_run = sorted(TESTS_DIR.glob(\"test_*.py\")) -- THE WHOLE SUITE.\n\nAND THE EXISTING GUARD IS BLIND TO EXACTLY THIS CASE. run_tests_core.py:1605:\n    if total == 0 and requested:  ... print(\"BD-RUNNER UNEVALUABLE ...\") ; sys.exit(2)\nThat @860 fix catches \"requested file collected zero tests\". It cannot fire when the\nrequested file does not EXIST, because the substituted full-suite run makes total huge.\nSection 0 shape: the denominator excludes the subject.\n\nbd-parband's own door check (toolchain/bin/bd-parband:163-174) refuses only HANGERS,\na bare tests/ dir, and `not s.endswith(\".py\")`. A nonexistent *.py path passes it.\nbd-band, by contrast, already guards this at bd-band:187-191 (\"SKIP ... not present in\").\nbd-parband does not.\n\n--- HALF 2: .bd_last_band.json has no gitignore rule ---\n$ cd /home/user/BD && git check-ignore -v .bd_last_band.json\nexit=1        (no output; exit 1 = NOT ignored)\nWritten by toolchain/bin/bd-parband:192-193 to RESULTS, resolved at :58-59 as\nos.environ.get(\"BD_LAST_BAND\", os.path.join(sec.DEFAULT_WORK, \".bd_last_band.json\")).\nsec.DEFAULT_WORK resolves to the checkout root (bdtools_sec.py:25-52, walk to\nbulk_downloader/__init__.py). So an ordinary `bd-parband ...` run leaves an untracked,\nunignored file at the repo root. Only bd-parband writes it; bd-retest:46 reads the same path.\n\nAnchored-file-rule behaviour verified in a throwaway repo (matters because the file is\nabsent right now and the gate queries by name):\n$ git init -q /tmp/X; printf '/.bd_last_band.json\\n' > /tmp/X/.gitignore\n$ git -C /tmp/X check-ignore -v .bd_last_band.json   -> .gitignore:1 match, exit 0 (file ABSENT)\n$ touch /tmp/X/.bd_last_band.json; git -C /tmp/X check-ignore -q ... -> exit 0 (file present)\n\n--- FAST RED SIGNAL, measured, for the test the executor will write ---\n$ rm -f /tmp/red_probe.json\n$ BD_LAST_BAND=/tmp/red_probe.json venv/bin/python toolchain/bin/bd-parband \\\n      tests/test_ITEM1_missing.py --jobs 1 --timeout 1\nexit=1\n  TIMEOUT  tests/test_ITEM1_missing.py  1.2s\n$ cat /tmp/red_probe.json\n{\"work\": \"/home/user/BD\", \"timeout\": 1,\n \"results\": [{\"suite\": \"tests/test_ITEM1_missing.py\", \"status\": \"timeout\", \"failed\": -1, ...}]}\n1.2 seconds, exit 1 (not 2), and a verdict file minted for a suite that does not exist.\n\n--- OVER-SENSITIVITY BASELINES, measured on pristine ---\n(a) a REAL suite must still run:\n$ BD_LAST_BAND=/tmp/green_probe.json venv/bin/python toolchain/bin/bd-parband \\\n      tests/test_capture_dict_shape_tripwire.py --work /home/user/BD --jobs 1 --timeout 120\nexit=0 ; PASS tests/test_capture_dict_shape_tripwire.py 0.3s ; real 0m0.363s\n(b) the leak-pair co-band bd-parband EXISTS to make safe must still be accepted:\n$ BD_LAST_BAND=/tmp/leakpair.json venv/bin/python toolchain/bin/bd-parband \\\n      tests/test_phases_195_199.py tests/test_cut8_schedules.py --jobs 2 --timeout 1\nexit=1 ; grep -c \"refusing\" -> 0 ; TIMEOUT on both (dispatched, not refused)\n\n--- BAND FINDING THE DERIVER MISSES ---\n$ sha256sum toolchain/bin/bd-parband project-knowledge/bd-parband\n10b05d67be0dfc3c...  toolchain/bin/bd-parband\n10b05d67be0dfc3c...  project-knowledge/bd-parband      <-- TRACKED MIRROR, byte-identical today\ntests/test_pk_mirrors_do_not_drift.py::test_no_project_knowledge_mirror_has_drifted_from_its_origin\nfails if you edit one and not the other. bd-band-derive did NOT return it.",
    "files": [
      "/home/user/BD/toolchain/bin/bd-parband:163-174 (the door check: add path validation before dispatch, refuse with exit 2)",
      "/home/user/BD/toolchain/bin/bd-parband:152-160 (main(); a.work is the resolution root for relative suite paths)",
      "/home/user/BD/toolchain/bin/bd-parband:17 (docstring exit contract: currently 'Exit 0 ... 1 = any failure/timeout, or a refused hazard' -- must document exit 2)",
      "/home/user/BD/project-knowledge/bd-parband (TRACKED MIRROR -- re-sync byte-for-byte in the SAME cut or test_pk_mirrors_do_not_drift.py fails)",
      "/home/user/BD/.gitignore:~40-50 (add the anchored rule in the runtime-state block beside .fts_optimize_last / .integrity_check_last; comment on its OWN line)",
      "/home/user/BD/tests/test_toolchain_534.py:776 (_RUNNERS block -- home for the new bd-parband refusal tests)",
      "/home/user/BD/tests/test_gitignore_rules_actually_match.py:165-168 (_GENERATED list -- add '.bd_last_band.json')",
      "/home/user/BD/run_tests_core.py:1277-1284,1288,1303,1605 (the callee mechanism and the blind @860 guard -- READ, do not edit in this cut)",
      "/home/user/BD/toolchain/bin/bd-band:187-191 (the working precedent to mirror)",
      "/home/user/BD/toolchain/bin/bd-bandcheck:52-92 (check() -- has the right existence logic AND the LEAK_PAIRS trap; see risks)"
    ],
    "red_test": "THREE tests, all in EXISTING files so the cut adds no new test file (keeps the axis-6\nfile-count gates and PIN_INDEX's test_files_scanned unmoved).\n\n(1) tests/test_toolchain_534.py -- test_parband_refuses_a_suite_path_that_does_not_exist\n    Beside the existing _RUNNERS block (:776), which already runs bd-* tools by subprocess\n    with _REPO_ROOT.\n    Arrange: results = tmp_path/\"band.json\"; env = {**os.environ, \"BD_LAST_BAND\": str(results)}.\n    Act:  subprocess.run([sys.executable-or-venv-python, str(_REPO_ROOT/\"toolchain/bin/bd-parband\"),\n                          \"tests/test_DOES_NOT_EXIST_parband_gate.py\",\n                          \"--jobs\",\"1\",\"--timeout\",\"1\"], env=env, capture_output=True, text=True,\n                          timeout=60)\n    Assert ALL THREE (each is independently load-bearing):\n      a) r.returncode == 2                       -- CANNOT-EVALUATE, not a verdict\n      b) a DISCRIMINATING marker in r.stdout+r.stderr, e.g. \"BD-PARBAND UNEVALUABLE\"\n         AND the offending path \"test_DOES_NOT_EXIST_parband_gate.py\" by name.\n         REQUIRED, not decoration: argparse also exits 2 on a bad argv, so rc==2 alone\n         does not discriminate the condition being hunted (CLAUDE.md 2a).\n      c) not results.exists()                    -- NO VERDICT WAS MINTED for a suite that\n         does not exist. This is the item's actual claim; (a) and (b) are the mechanism.\n    WHY IT FAILS ON PRISTINE (measured above, 1.2s): rc == 1, the output contains\n    \"TIMEOUT tests/test_DOES_NOT_EXIST...\" with no refusal marker, and the results file IS\n    written carrying status \"timeout\" for the nonexistent suite. All three assertions fail.\n\n(2) tests/test_toolchain_534.py -- test_parband_still_runs_a_suite_that_exists\n    (the over-sensitivity direction; see over_sensitivity_test)\n\n(3) tests/test_gitignore_rules_actually_match.py -- add \".bd_last_band.json\" to _GENERATED\n    (:165-168) and extend the block comment above it to state the new entry's provenance\n    (written by toolchain/bin/bd-parband:192 at sec.DEFAULT_WORK, i.e. the checkout root;\n    measured untracked-and-unignored in the container at 3fded70, not on the box).\n    Reuses the file's existing parametrized test_generated_artifacts_are_ignored.\n    WHY IT FAILS ON PRISTINE (measured above): `git check-ignore -q .bd_last_band.json`\n    returns 1, so the assertion fails immediately. Note the parametrize list is module-level,\n    so it is RED at collection-selection time with no filesystem setup and no need for the\n    file to exist.\n\nPROVE RED BEFORE IMPLEMENTING:\n  venv/bin/python -m pytest tests/test_toolchain_534.py -k parband \\\n      tests/test_gitignore_rules_actually_match.py -x -q\nExpect 3 failures. Then implement. Do not skip (1c) -- it is the only assertion that names\nthe defect rather than the symptom.",
    "fix": "HALF 1 -- validate every path BEFORE dispatch, in toolchain/bin/bd-parband:163-174.\n\nReplace the existing refusal block with a two-tier door that keeps the current hazard\nrefusal and ADDS existence, and returns sec.EXIT_CANNOT_EVALUATE (==2, bdtools_sec.py:59)\nfor both:\n\n    bad = []\n    for s in a.suites:\n        if any(h in s for h in HANGERS):\n            bad.append((s, \"known hanger\")); continue\n        if s.rstrip(\"/\") in (\"tests\", \"tests/\"):\n            bad.append((s, \"bare tests/ dir (hangs)\")); continue\n        if not s.endswith(\".py\"):\n            bad.append((s, \"not a .py file\")); continue\n        p = s if os.path.isabs(s) else os.path.join(a.work, s)\n        if not os.path.isfile(p):\n            bad.append((s, \"no such file under %s -- run_tests.py falls back to a \"\n                           \"BROAD RUN and the result would be attributed to this \"\n                           \"path\" % a.work)); continue\n    if bad:\n        print(\"%sBD-PARBAND UNEVALUABLE%s -- refusing to dispatch; no verdict minted:\"\n              % (R, RST), file=sys.stderr)\n        for s, why in bad:\n            print(\"  %sXX%s %s %s(%s)%s\" % (R, RST, s, DIM, why, RST), file=sys.stderr)\n        return sec.EXIT_CANNOT_EVALUATE\n\nFour properties that matter:\n * resolve against a.work, NOT cwd -- run_one already does cwd=a.work (:107), so any\n   other root would refuse valid bands (the over-sensitivity failure mode).\n * per-target reason. Today's `why` at :169-171 mislabels a non-.py path with no \"test\"\n   in its basename as \"bare tests/ dir\"; the loop above fixes that as a side effect.\n * exit 2, not 1. Nothing in the repo consumes bd-parband's exit code -- grep for\n   \"bd-parband\" across *.sh/*.py/*.yml/*.md returns only docs, CHANGELOG, run_tests_core\n   comments and tests/test_toolchain_534.py:776 (interpreter-hardcode gate only). No CI\n   job, no capture.sh stage. Safe.\n * do NOT write RESULTS on refusal -- a refusal is not a band, and bd-retest reads that\n   file. Leaving the previous file untouched is correct; overwriting it with a\n   CANNOT-EVALUATE row is the alternative but changes bd-retest's input shape, so prefer\n   leaving it alone. Assertion (1c) pins this.\n\nAlso update the module docstring exit contract at :17 to name exit 2.\n\nMIRROR (mandatory, same cut):\n    cp toolchain/bin/bd-parband project-knowledge/bd-parband\n    sha256sum toolchain/bin/bd-parband project-knowledge/bd-parband   # must match\n    venv/bin/python toolchain/bin/bd-pk-mirror                        # wider cross-check\n\nHALF 2 -- .gitignore. Add, in the runtime-state block beside .fts_optimize_last /\n.integrity_check_last (comment lines on their OWN lines -- test_no_pattern_carries_an_\ninline_comment forbids inline comments, and that gate exists because vapid_keys.json was\nignored by nothing for months):\n\n    # Band-results file written by toolchain/bin/bd-parband:192 at the checkout root\n    # (bdtools_sec.DEFAULT_WORK); bd-retest reads it back. Per-run state, not source.\n    # Override with BD_LAST_BAND.\n    /.bd_last_band.json\n\nAnchored with a leading slash: bd-parband only ever writes it at DEFAULT_WORK, and an\nunanchored rule would also swallow a same-named file anywhere in the tree. Verified in a\nthrowaway repo that the anchored file rule matches whether or not the file exists, so the\ngate is green with the file absent (this is NOT a directory rule, so the\ntest_a_directory_rule_matches_by_path_not_by_existence hazard does not apply).\n\nDo NOT touch run_tests_core.py:1605 in this cut. Making run_tests.py itself refuse an\nabsent path is the deeper fix and it is a separate blast radius (bd-band, bd-fullsuite,\ncapture.sh and the dev-tools API all drive it). Record it as a follow-up item; bd-band\nalready guards its own door at :187, and after this cut bd-parband does too.",
    "band": [
      "tests/test_toolchain_534.py",
      "tests/test_gitignore_rules_actually_match.py",
      "tests/test_pk_mirrors_do_not_drift.py  -- NOT returned by bd-band-derive; MANDATORY, the PK mirror of bd-parband is tracked and sha-gated",
      "tests/test_code_intelligence_foundation.py",
      "tests/test_contracts.py",
      "tests/test_deploy_script.py",
      "tests/test_generated_artifacts_are_not_tracked.py",
      "tests/test_git_deploy_gaps_are_documented.py",
      "tests/test_v3_66_748_release_forbids_hypothesis.py",
      "tests/test_all_sources_parse.py",
      "tests/test_pytest_runner_boundary.py",
      "tests/test_capture_execution_lanes.py",
      "tests/test_deploy_manifest_stays_retired.py",
      "tests/test_task_tracker_stays_retired.py",
      "tests/test_codex_handoff_stays_retired.py",
      "tests/test_source_windows_do_not_shift.py",
      "tests/test_history_columns_go_through_migrations.py",
      "tests/test_pin_index_in_sync.py"
    ],
    "risks": "1. THE BIG ONE -- DO NOT DELEGATE TO bd-bandcheck. It is the obvious \"single source of\n   truth\" move (bd-band:119-121 argues for it in exactly these words) and bd-bandcheck.check()\n   at :77-85 already has the right MISSING logic with the right reason string. But\n   check() also enforces LEAK_PAIRS (bd-bandcheck:34-37): test_phases_195_199.py +\n   test_cut8_schedules.py must not co-band. bd-parband's whole reason to exist is that\n   this pair IS safe under it -- its docstring (:5-8) says \"the per-process BD_HOME means\n   the state-leak co-band hazards (e.g. test_phases_195_199 leaking BD_INSTALL_DIR) can't\n   cross suites\". Delegating would make the tool refuse the exact case it was built for.\n   check() returns one rc, not per-reason, so selective delegation is not available\n   without changing its API. Implement the existence check locally (10 lines) and PIN the\n   divergence with the leak-pair test below, so a future \"improvement\" cannot destroy it.\n   Measured on pristine: that co-band is currently accepted (0 occurrences of \"refusing\").\n\n2. THE FIX REPRODUCING THE DEFECT'S SHAPE. The blindness being fixed is \"the check's\n   denominator excluded its subject\". A validator that iterates a.suites but resolves\n   against os.getcwd() instead of a.work has the same shape one level down: it would be\n   green from the repo root and refuse everything from anywhere else. Resolve against\n   a.work, and cover it explicitly (over-sensitivity test (b)).\n\n3. bd-parband refuses `not s.endswith(\".py\")`, so pytest node ids (tests/x.py::TestY) are\n   ALREADY rejected today. Keep that check ahead of the isfile() check -- do not \"improve\"\n   it by splitting on \"::\", that widens accepted input in a cut that is about narrowing it.\n\n4. Exit-code change 1 -> 2 for refusals. Verified no consumer: `grep -rn \"bd-parband\"`\n   over *.sh/*.py/*.yml/*.md hits only BD_TOOLCHAIN_REFERENCE.md:724-728,\n   BD_TOOLCHAIN_WHEN_TO_USE.md:287, SESSION_CARRY.md:3715, CHANGELOG.md:222/544,\n   docs/repo/*, run_tests_core.py comments, and tests/test_toolchain_534.py:776 (which\n   only greps for a hardcoded \"python3\"). Still band test_toolchain_534.py.\n\n5. STALE DOC, adjacent and cheap: BD_TOOLCHAIN_REFERENCE.md:728 says bd-parband writes\n   \"/home/claude/.bd_last_band.json\". It writes <checkout>/.bd_last_band.json (:58-59).\n   Correcting it in this cut is in-subject and low risk; bd-freshcheck may flag it anyway.\n\n6. Container-vs-box: every measurement above is from this container at 3fded70. The\n   .bd_last_band.json entry joins a _GENERATED list whose existing comment says it was\n   measured on test4 -- say in the comment that this entry was derived from the writer\n   (bd-parband:192) plus a container check-ignore, not from a box git status.\n\n7. Regen AFTER the last source edit: venv/bin/python toolchain/bin/bd-regen-order --work\n   \"$PWD\" (CLAUDE.md 2a). And the cut adds no new test file, so the axis-6 file-count\n   ratchets should not move -- but band them; that is what one-directional ratchets are for.",
    "over_sensitivity_test": "TWO assertions, both in tests/test_toolchain_534.py, both of which a bad fix passes today\nand would fail after an over-correction:\n\n(a) test_parband_still_runs_a_suite_that_exists -- the \"refuse everything\" over-correction.\n    subprocess bd-parband with tests/test_capture_dict_shape_tripwire.py, an explicit\n    --work str(_REPO_ROOT), --jobs 1 --timeout 120, BD_LAST_BAND -> tmp_path.\n    Assert r.returncode == 0, \"PASS\" in stdout, and json.loads(results.read_text())\n    [\"results\"][0][\"passed\"] == 5. Measured on pristine: exit 0, PASS, 0.363s wall, 5\n    passed. The explicit --work with a RELATIVE suite path is the point -- it is the exact\n    input a validator that resolves against cwd would wrongly refuse.\n    (If test_capture_dict_shape_tripwire.py ever changes count, assert [\"passed\"] > 0 and\n    [\"status\"] == \"pass\" instead of the literal 5 -- do not pin a number that can drift.)\n\n(b) test_parband_still_accepts_the_leak_pair_it_exists_to_make_safe -- the\n    delegate-to-bandcheck over-correction, which is the one a well-intentioned reviewer\n    will actually propose.\n    subprocess bd-parband with tests/test_phases_195_199.py tests/test_cut8_schedules.py,\n    --jobs 2 --timeout 1 (1s: we are testing the DOOR, not the suites).\n    Assert r.returncode != 2 and \"UNEVALUABLE\" not in (stdout+stderr) and \"refusing\" not\n    in (stdout+stderr). Docstring must state why: bd-parband gives each suite its own\n    BD_HOME (:104), so the BD_INSTALL_DIR leak that makes this pair unsafe under bd-band\n    structurally cannot cross suites here -- refusing it would delete the tool's reason to\n    exist. Measured on pristine: exit 1 (both TIMEOUT at 1s), zero \"refusing\" lines,\n    ~1.2s. So it is green today and goes RED the moment someone delegates.\n\nTogether these bound the fix from both sides: (1c) forbids a verdict for a suite that was\nnever run, (a)+(b) forbid a tool that refuses to run anything.",
    "size": "M",
    "blocked_on": ""
  },
  {
    "item": "ITEM 2 -- tests/test_pk_mirrors_do_not_drift.py does not fire",
    "defect_confirmed": "REAL",
    "evidence": "COMMIT VERIFIED. `git -C /home/user/BD log --oneline -1` -> `3fded70 Register 15.29: seven cuts, a contaminated corpus, and three corrected figures (#169)`, branch `main`, `git status --porcelain` empty before and after all work. Repo never mutated; all injection done in a copy at /tmp/bdrepro.\n\nHEADLINE: REAL -- the gate does not fire. BUT THE ITEM'S DIAGNOSIS IS WRONG ON (a) AND ONLY NARROWLY RIGHT ON (b). The actual cause of the demonstrated incident is a THIRD mechanism the item did not name.\n\n--- CURRENT STATE (the four named mirrors are already re-synced at this commit) ---\n$ venv/bin/python toolchain/bin/bd-pk-mirror --check ; echo exit=$?\n  ... -- MIRROR-IDENTICAL=276\nexit=0\n$ venv/bin/python -m pytest tests/test_pk_mirrors_do_not_drift.py -v\n  3 passed in 0.43s\nSo the incident's stale mirrors are gone; the BLINDNESS is what I re-proved, synthetically.\n\n--- MECHANISM 1 (THE REAL CAUSE, NOT IN THE ITEM): the gate is in no band ---\n$ venv/bin/python toolchain/bin/bd-band-derive --file toolchain/bin/bdtools_sec.py\n  band (13): test_bd_ready_preflight, test_contracts, test_desandbox_tool_verifiers,\n  test_toolchain_534, test_v3_66_305_config_danger, test_v3_66_319_env_tranche_4_3d,\n  test_v3_66_653_dep_freshness, test_v3_66_702_jd_supported_hosts_field,\n  test_v3_66_711_automation_gui, test_v3_66_713_unscanned_surfaces,\n  test_v3_66_716_decoys_and_shadow, test_v3_66_720_oidc_and_ffmpeg,\n  test_v3_66_820_share_tools_saw_no_session_keys\n$ venv/bin/python toolchain/bin/bd-band-derive --file toolchain/bin/bd-scrub-proof\n  changed source (0):\n  changed, unclassified (1): toolchain/bin/bd-scrub-proof\n  band (3): test_contracts, test_toolchain_534, test_v3_66_820_share_tools_saw_no_session_keys\nIdentical 3-suite band for bd-share-safe and bd-wacz-scrub, and for the PK side\n(`--file project-knowledge/bd-scrub-proof`). tests/test_pk_mirrors_do_not_drift.py\nis ABSENT from every one of them. The gate never ran. That is the 261-green band.\nRoot cause: none of bd-band-derive's signals can reach it. Signal 1 (stem glob) --\nstem \"bd-scrub-proof\"/\"bdtools_sec\" matches no test filename. Signal 2 (curated map)\n-- `grep -n \"pk_mirror|project-knowledge\" project-knowledge/TOUCHED_FILE_TO_TEST.md`\nreturns NOTHING. Signal 3 (COUNT_COUPLED, bd-band-derive:98-134) matches on\n`rel == w or basename(rel)==basename(w)` over two hardcoded rules, neither about\nmirrors. Signal 4 (module-consumer) -- the gate's source names no module; it\nimports only hashlib/pathlib/pytest.\n\n--- MECHANISM 2 (item's suspicion (a)): REAL DEFECT, BUT NOT BLINDNESS ---\n`_PytestStub` (run_tests_core.py:41-184) has no `fail`. Confirmed empirically -- in\n/tmp/bdrepro I appended a line to project-knowledge/bd-scrub-proof, then:\n  (pytest)      exit=1  \"Failed: 1 of 255 project-knowledge mirrors disagree ...\n                         project-knowledge/bd-scrub-proof f011ce84ef10d088 !=\n                         toolchain/bin/bd-scrub-proof 82f778849c96d5e6\"\n  (run_tests.py) exit=1  FAIL test_no_project_knowledge_mirror_has_drifted_from_its_origin\n                         AttributeError: '_PytestStub' object has no attribute 'fail'\n                         Total: 3 | Passed: 2 | Failed: 1\nIt FAILS under the runner -- it does NOT pass. So (a) cannot explain a green band.\nIt is still a real defect: the entire diagnostic (which mirrors drifted + the `cp`\nremediation lines) is destroyed and replaced by a harness AttributeError, which is\nexactly CLAUDE.md section 2a's \"harness defect masquerading as the subject\". Blast\nradius is wider than this file: `grep -rl \"pytest\\.fail\" tests/*.py | wc -l` -> 34.\n\n--- MECHANISM 3 (item's suspicion (b)): REAL BLINDNESS, BUT NARROW -- 2 files ---\nThe `break` at tests/test_pk_mirrors_do_not_drift.py:75 stops at the first matching\nSOURCE_DIR. Only 2 mirrored basenames exist in >1 SOURCE_DIR:\n  bd-audit-gate.py -> ['toolchain/bin', 'tools']\n  bd-triage.py     -> ['toolchain/bin', 'tools']\nNeither is one of the four stale mirrors, so this is not the incident's cause.\nProved live in /tmp/bdrepro by appending a line to tools/bd-audit-gate.py only:\n  $ pytest tests/test_pk_mirrors_do_not_drift.py -q   ->  3 passed in 0.05s\n  $ bd-pk-mirror --check  ->  AMBIGUOUS bd-audit-gate.py\n      pk=a8876ad7daff vs a8876ad7daff,a8876ad7daff,5f3588648e33\n      CHECK FAIL -- 1 drifted/ambiguous PK mirror(s)   exit=1\n\n--- MECHANISM 4 (NOT IN THE ITEM): denominator too narrow -- 3 real mirrors unseen ---\nDerived independently from `git ls-files` (tracked only, so untracked `audit-venv/`\nand `venv/` are structurally excluded), source = \".py OR dotless-with-shebang\":\n  PK source files: 276 | true mirrors (counterpart tracked OUTSIDE PK): 258 | PK-only: 18\n  disagreements on pristine: 0\n  counterpart dirs of the 258: toolchain/bin 239, tools 19, tools/audit/witnesses 2\nThe test pairs 255. The 3 it cannot see:\n  bd                   -> toolchain/bin/bd                        (name filter :69,\n                          `startswith(\"bd-\")` excludes the tool literally named `bd`)\n  cap01_witnesses.py   -> tools/audit/witnesses/cap01_witnesses.py (SOURCE_DIRS :42-46\n  run01_witnesses.py   -> tools/audit/witnesses/run01_witnesses.py  has no such entry)\nProved live -- appended a line to toolchain/bin/bd and tools/audit/witnesses/\ncap01_witnesses.py in /tmp/bdrepro:\n  $ pytest tests/test_pk_mirrors_do_not_drift.py -q  ->  3 passed in 0.05s\n  $ bd-pk-mirror --check -> AMBIGUOUS bd; AMBIGUOUS cap01_witnesses.py; CHECK FAIL\n\n--- CORRECTION TO A NUMBER YOU MIGHT OTHERWISE INHERIT ---\nbd-pk-mirror's own \"276\" is NOT 276 mirrors. Its `--work` default is the repo root,\nwhich CONTAINS project-knowledge/, so `_counterparts()` (bd-pk-mirror:145-154) finds\neach PK file as its own counterpart. 18 PK-only files (render_check.py, spa_shot.py,\ncapture_all.py, ...) are therefore mislabelled MIRROR-IDENTICAL instead of PK-ONLY.\nDo not cite 276 as a mirror count. That is a separate bd-pk-mirror defect, out of\nscope here, but it means the tool's PK-ONLY branch is currently unreachable on a\ngit checkout.\n\n--- OVER-SENSITIVITY EVIDENCE (why the obvious widening is a trap) ---\nNon-source PK files whose basename ALSO exists elsewhere in the tracked tree, with\nDIFFERENT content -- these become 5 instant false positives if the name filter is\nwidened to \"basename exists elsewhere\":\n  README.md            pk 61eaae8be14e/27930b vs README.md 600b0ecffd61/3105b (+7 more)\n  AUTOMATION_POLICY.md pk affba4b601fb/17277b vs AUTOMATION_POLICY.md 45dfa452396a/349b\n  BDSUITE_CHANGELOG.md pk e674f9f67309/19364b vs toolchain/... c07bcc7b9acb/334b\n  SANDBOX.md           pk dbf5ee326d98/34697b vs SANDBOX.md 6956fd5b8fc4/33960b\n  DECOMPOSITION_PROGRAM_ROADMAP.md pk 7ea9ddf55b34/502b vs kb/decomp/... 8b96e3d18904/398b\nKeeping the \".py OR dotless-shebang\" predicate excludes all 5 STRUCTURALLY.\n\n--- RESIDUAL, OUT OF SCOPE, FLAGGED ---\nproject-knowledge/bdenv.sh and project-knowledge/install_bdsuite.sh are byte-identical\nto toolchain/ copies -- genuine mirrors invisible to BOTH instruments (both exclude any\nextension other than .py). Do not widen to .sh in this cut; it needs its own measurement.\nAlso noted: `bd-bandcheck` still probes a hardcoded /home/claude/work and reported all 9\nreal band files as \"MISSING\" -- a separate live defect, not this item.",
    "files": [
      "toolchain/bin/bd-band-derive:98-134 (COUNT_COUPLED list + count_coupled(); exact-path matcher that cannot express 'any mirrored file')",
      "toolchain/bin/bd-band-derive:432-462 (tests_for_file(); where the new signal is unioned in, beside the SIGNAL 3 call at :449-451)",
      "project-knowledge/bd-band-derive (MANDATORY same-cut mirror re-sync: `cp toolchain/bin/bd-band-derive project-knowledge/bd-band-derive` -- editing the tool drifts its own mirror and the gate you just wired will fire on your own cut)",
      "tests/test_pk_mirrors_do_not_drift.py:42-46 (SOURCE_DIRS -- hardcoded 3 dirs, misses tools/audit/witnesses)",
      "tests/test_pk_mirrors_do_not_drift.py:59-76 (mirror_pairs() -- add a `root` parameter so a fixture tree is testable; :69 name filter; :71-75 the break)",
      "tests/test_pk_mirrors_do_not_drift.py:52 (_MIN_PLAUSIBLE_MIRRORS = 150 -- floor canary; add a COVERAGE canary beside it)",
      "tests/test_pk_mirrors_do_not_drift.py:124 (pytest.fail -> the call the stub cannot serve)",
      "run_tests_core.py:134-135 (_PytestStub.skip -- add `fail` immediately after, same @staticmethod shape)",
      "tests/test_toolchain_534.py:1180-1226 (test_band_derive_finds_the_curated_map_and_says_so_when_it_cannot -- copy its SourceFileLoader import pattern; home for RED test A)",
      "tests/test_pytest_runner_boundary.py:19-33 (_python()/_assert_ok() child-interpreter helpers -- home for RED test D)"
    ],
    "red_test": "FOUR RED tests, all in EXISTING files (deliberately: adding a new tests/*.py file moves nine axis-6 ratchets plus PIN_INDEX's test_files_scanned, per CLAUDE.md section 4 -- keeping the blast radius off that axis).\n\nRED-A -- the real defect. In tests/test_toolchain_534.py, beside the existing\ntest_band_derive_finds_the_curated_map_and_says_so_when_it_cannot (:1180). Load\ntoolchain/bin/bd-band-derive by SourceFileLoader exactly as that test does, then run it\nas a subprocess with --work REPO --file toolchain/bin/bdtools_sec.py and assert\n\"tests/test_pk_mirrors_do_not_drift.py\" is in stdout.\nWHY IT FAILS ON PRISTINE: measured above -- that band is 13 suites and the gate is not\none of them. Repeat for `--file toolchain/bin/bd-scrub-proof` (band=3) and\n`--file project-knowledge/bd-scrub-proof` (band=3). All three fail today.\n\nRED-B -- the break. In tests/test_pk_mirrors_do_not_drift.py. Build a synthetic tree in\ntmp_path: pk/x.py=\"A\", toolchain/bin/x.py=\"A\", tools/x.py=\"B\"; call\nmirror_pairs(root=tmp_path) and assert BOTH counterparts are returned (2 pairs for x.py),\nor that the drift check reports x.py.\nWHY IT FAILS ON PRISTINE: mirror_pairs() takes no arguments (TypeError), and even\nparameterised the `break` at :75 returns only the toolchain/bin pair. Verified live on the\nreal tree: drifting tools/bd-audit-gate.py leaves the suite 3-passed while bd-pk-mirror\nexits 1.\n\nRED-C -- the narrow denominator. In tests/test_pk_mirrors_do_not_drift.py, a COVERAGE\ncanary beside _MIN_PLAUSIBLE_MIRRORS: derive the true mirror population independently\n(`git ls-files -z`, drop project-knowledge/, index by basename; PK entry is a subject iff\nsuffix == \".py\" or (no suffix and first two bytes are b\"#!\")), then assert\n{p[0].name for p in mirror_pairs()} == that set.\nWHY IT FAILS ON PRISTINE: 255 != 258. It names bd, cap01_witnesses.py, run01_witnesses.py.\n\nRED-D -- the stub. In tests/test_pytest_runner_boundary.py, using its _python() child\nhelper: run `from run_tests_core import _PytestStub; assert hasattr(_PytestStub, \"fail\")`\nand, under activated_pytest_stub(), assert pytest.fail(\"boom\") raises with \"boom\" in str.\nWHY IT FAILS ON PRISTINE: reproduced verbatim --\n`AttributeError: '_PytestStub' object has no attribute 'fail'`.",
    "fix": "FIX-1 (the blindness -- do this one first and alone if you split).\nAdd a DERIVED signal to toolchain/bin/bd-band-derive, NOT a hardcoded path list -- a list\nwould reproduce the defect's shape (a frozen denominator that cannot see a new mirror).\nNew function beside count_coupled() at :127:\n\n    _PK_MIRROR_GATE = \"tests/test_pk_mirrors_do_not_drift.py\"\n\n    def pk_mirror_coupled(work, rel):\n        \"\"\"SIGNAL 8 -- PK-MIRROR. project-knowledge/ carries a second copy of much\n        of the toolchain; tests/test_pk_mirrors_do_not_drift.py compares them by\n        sha256. It imports nothing, its stem matches no source, and it is not in the\n        curated map -- so signals 1-4 are structurally blind to it and the gate ran\n        in NO band, for any mirrored file. DERIVED from the tree, not declared: a\n        new mirror is covered the day it lands.\"\"\"\n        pk = os.path.join(work, \"project-knowledge\")\n        if not os.path.isdir(pk):\n            return (\"MISSING\", [])          # UNKNOWN is a third state -- main() prints it\n        names = {f for f in os.listdir(pk)\n                 if os.path.isfile(os.path.join(pk, f))}\n        if rel.startswith(\"project-knowledge/\") or os.path.basename(rel) in names:\n            return (\"OK\", [_PK_MIRROR_GATE])\n        return (\"OK\", [])\n\nUnion it in tests_for_file() right after the SIGNAL 3 block at :449-451, guarded by\nos.path.isfile like the others. On the MISSING branch main() must PRINT a notice --\ncopy the exact @860 pattern find_map()/main() already use for the curated map\n(\"curated map not found\"), e.g. \"project-knowledge/ not found -- PK-mirror signal off\".\nA signal that can vanish silently is the section 0 shape and this tool already learned it.\nFAN-OUT, MEASURED: 278 tracked files outside PK have a PK top-level basename --\ntoolchain/bin 239, tools 20, toolchain 3, tools/audit/witnesses 2, root 3, and 11 doc\ncollisions. ZERO under bulk_downloader/. Cost when it fires: 3 tests, 0.43s.\nTHEN: `cp toolchain/bin/bd-band-derive project-knowledge/bd-band-derive`.\n\nFIX-2 (the gate's own denominator).\n(a) mirror_pairs(root: Path = REPO_ROOT) -- parameterise so a fixture tree is testable.\n(b) Replace the :69 name filter with the same predicate bd-pk-mirror:86-115 uses:\n    suffix == \".py\" OR (no suffix AND first two bytes are b\"#!\"). This admits `bd` and\n    every future dotless tool, and structurally excludes README.md / SANDBOX.md /\n    AUTOMATION_POLICY.md / BDSUITE_CHANGELOG.md / DECOMPOSITION_PROGRAM_ROADMAP.md --\n    the 5 measured false positives a naive widening would buy.\n(c) Replace SOURCE_DIRS (:42-46) + the break (:75) with a counterpart index built from\n    `git ls-files -z`, dropping project-knowledge/ and keying on basename. Tracked-only\n    is load-bearing: it excludes the untracked `audit-venv/` (which carries its own\n    README.md) and venv/ without a skip-list that could go stale.\n(d) Compare against ALL counterparts. Three verdicts, not two:\n      all == PK            -> ok\n      all agree, != PK     -> DRIFTED (existing message, existing `cp` remediation)\n      counterparts DISAGREE among themselves -> AMBIGUOUS, its own message, also fails.\n    Unknown is a third state and it fails -- and it is what the break was hiding.\n(e) Keep _MIN_PLAUSIBLE_MIRRORS (raise 150 -> 250, measured 258) AND add the RED-C\n    coverage canary. The floor alone cannot catch a narrowing that still clears it;\n    255 >= 150 is exactly how this survived.\n\nFIX-3 (the diagnostic). run_tests_core.py, immediately after :135:\n    @staticmethod\n    def fail(reason=\"\", pytrace=True): raise AssertionError(reason)\nAssertionError is what the runner already classifies as a failure, so no test flips\ndirection; 34 test files get a readable message instead of an AttributeError. Verified\nno test swallows it: every pytest.fail in tests/ sits INSIDE an except handler, never\ninside a try whose handler could catch AttributeError.",
    "band": [
      "DERIVED FLOOR -- venv/bin/python toolchain/bin/bd-band-derive --files toolchain/bin/bd-band-derive project-knowledge/bd-band-derive tests/test_pk_mirrors_do_not_drift.py tests/test_toolchain_534.py run_tests_core.py tests/test_pytest_runner_boundary.py  => 23 suites:",
      "tests/test_bd_ready_preflight.py",
      "tests/test_c4_timeout_budget.py",
      "tests/test_capture_execution_lanes.py",
      "tests/test_contracts.py",
      "tests/test_desandbox_tool_verifiers.py",
      "tests/test_generated_artifact_workflow.py",
      "tests/test_harness_retry_timeout.py",
      "tests/test_pk_mirrors_do_not_drift.py",
      "tests/test_pytest_runner_boundary.py",
      "tests/test_toolchain_534.py",
      "tests/test_v3_66_305_config_danger.py",
      "tests/test_v3_66_319_env_tranche_4_3d.py",
      "tests/test_v3_66_637_flake_registry.py",
      "tests/test_v3_66_660_mnt3_flake_expiry.py",
      "tests/test_v3_66_664_mnt3_quarantine.py",
      "tests/test_v3_66_702_jd_supported_hosts_field.py",
      "tests/test_v3_66_711_automation_gui.py",
      "tests/test_v3_66_713_unscanned_surfaces.py",
      "tests/test_v3_66_716_decoys_and_shadow.py",
      "tests/test_v3_66_720_oidc_and_ffmpeg.py",
      "tests/test_v3_66_730_touch_map_tool_bridge.py",
      "tests/test_v3_66_754_flake729_serial_pin.py",
      "tests/test_v3_66_772_flake_hardening.py",
      "CROSS-CHECK (grep -rl 'run_tests_core|_PytestStub|activated_pytest_stub' tests/*.py) returned 10 files, ALL of them already in the 23. No miss -- the derived band is a strict superset.",
      "ADD (axis-6 / regen, not derivable from the diff): tests/test_pin_index_in_sync.py -- its enumerator lives one import away in tools/build_pin_index.py:151,211 and no grep of tests/ can see it.",
      "ADD: tests/test_generated_artifacts_are_not_tracked.py -- enumerates `git ls-files -z` (everything); the cut touches tracked files.",
      "ADD: tests/test_all_sources_parse.py -- bare `git ls-files -- *.py` reaches tests/ and run_tests_core.py.",
      "ADD: tests/test_source_windows_do_not_shift.py -- run it, but do NOT add a fixed-width source window in this cut; if it ratchets, remove the window rather than raising the baseline.",
      "NOT NEEDED, and say why: test_deploy_manifest_stays_retired / test_task_tracker_stays_retired / test_codex_handoff_stays_retired (`*.py *.sh` ls-files) do move with an edited tracked .py -- include them if the cut adds or renames any tracked file; a content-only edit does not change their denominator. test_playwright_engines_single_source is `.sh`-only -- excluded. test_gitignore_rules_actually_match is `.gitignore`-only -- excluded. test_v3_66_820_share_tools_saw_no_session_keys is text-extensions-only and `.py` is not in its set -- excluded.",
      "AFTER THE LAST SOURCE EDIT (not before), per section 2a: venv/bin/python toolchain/bin/bd-regen-order --work \"$PWD\", then `git status --porcelain` over the six generated artifacts. run_tests_core.py is NOT currently in FUNCTION_INDEX.md (grep -c returns 0), so that artifact is unlikely to move -- verify, do not assume.",
      "SELF-CHECK THE FIX WORKED: after FIX-1 lands, `bd-band-derive --file toolchain/bin/bdtools_sec.py` must now list tests/test_pk_mirrors_do_not_drift.py, and `--file bulk_downloader/app.py` must NOT.",
      "BOX (operator, not the container): ./capture.sh --workers=$(nproc) > /tmp/capture.log 2>&1; echo \"exit=$?\" -- unpiped."
    ],
    "over_sensitivity_test": "FOUR assertions, one per direction the fix can swing wrong. Write them BEFORE the verdict logic, per section 0.\n\n(1) THE BAND MUST NOT BECOME \"BAND EVERYTHING\". In tests/test_toolchain_534.py, in the same test as RED-A: assert \"tests/test_pk_mirrors_do_not_drift.py\" is NOT in the band for `--file bulk_downloader/app.py`. MEASURED: zero of the 278 basename hits are under bulk_downloader/, so this assertion is meaningful today and will catch a future rule that degrades to a prefix match on the whole repo.\n\n(2) THE GATE MUST NOT CALL A NON-MIRROR A DRIFT. In tests/test_pk_mirrors_do_not_drift.py, assert on the REAL tree that \"README.md\", \"SANDBOX.md\", \"AUTOMATION_POLICY.md\", \"BDSUITE_CHANGELOG.md\" and \"DECOMPOSITION_PROGRAM_ROADMAP.md\" are NOT in {p[0].name for p in mirror_pairs()}. All five have same-named tracked files elsewhere with DIFFERENT content (sizes measured above); if the widened predicate ever admits them the suite goes instantly, permanently red on a defect that does not exist. This is the single assertion that stops FIX-2 swinging the other way.\n\n(3) PK-ONLY MUST STAY HEALTHY, NOT BECOME \"UNKNOWN THEREFORE FAIL\". 18 PK files have no counterpart anywhere (render_check.py, spa_shot.py, spa_tabs.py, capture_all.py, dark_audit.py, ...). Assert \"render_check.py\" is not in the pair set AND the drift list is empty on the pristine tree. The temptation when adding a third state is to make absence inconclusive; that reproduces the \"calls everything inconclusive\" failure the brief names.\n\n(4) THE FIX MUST NOT MAKE THE SUITE RED ON PRISTINE. Assert the drift list is empty and the pair count == 258 on this commit. Independently confirmed: comparing all 258 true mirrors against ALL their tracked counterparts yields 0 disagreements today, so FIX-2 is green on pristine and only RED-B/RED-C (which target the pairing logic, not the tree) go red before implementation.\n\nPlus the mechanical one for FIX-3: assert `pytest.fail(\"boom\")` under the stub raises with \"boom\" in the message -- not merely that the attribute exists. `hasattr` alone would pass against a no-op stub, which is the exact shape of a check that cannot see its subject.",
    "risks": "1. SELF-REFERENCE, and it will bite whoever executes this. Editing toolchain/bin/bd-band-derive DRIFTS project-knowledge/bd-band-derive, so the very gate this cut wires up will fail on this cut unless the mirror is re-synced in the same commit. Same for tests/test_pk_mirrors_do_not_drift.py? No -- that file has no PK mirror. Only the bd-band-derive edit needs the `cp`. Treat it as a feature: it is the cut's own proof the signal works.\n\n2. THE FOUR NAMED MIRRORS ARE ALREADY CLEAN AT 3fded70. Someone re-synced bd-scrub-proof / bd-share-safe / bd-wacz-scrub / bdtools_sec.py before this commit (git log shows e63b5b9, b7c3e51, 75e8014, 267dea9 touching them). So there is NO drift to fix -- this cut fixes the INSTRUMENT, not the tree. Do not expect the gate to go red on pristine after the fix; if it does, the tree drifted again since I measured.\n\n3. bd-pk-mirror's `--work` default includes project-knowledge/, so it counts each PK file as its own counterpart and reports 276 MIRROR-IDENTICAL when only 258 are mirrors; its PK-ONLY branch is unreachable on a git checkout. Anyone cross-checking FIX-2 against bd-pk-mirror will see 276 vs 258 and think the new test is narrow. It is not -- bd-pk-mirror is wide by 18. That is a separate defect; do not \"fix\" the test to match it.\n\n4. run_tests_core.py is a shared harness for the whole suite. FIX-3 is two lines and cannot flip a pass to a fail (AssertionError is already the runner's failure path, and pytest.fail is only reached on an already-failing branch), but it is still the widest-blast-radius edit here. If you want a minimal cut, drop FIX-3 -- it is a diagnostics improvement, not the blindness.\n\n5. `.sh` mirrors (project-knowledge/bdenv.sh, install_bdsuite.sh -- both currently byte-identical to toolchain/ copies) stay invisible after this cut. Deliberate: widening to .sh needs its own collision measurement and would pull in the docs/archive/ duplicate tree. State the exclusion in the fix's docstring rather than leaving it implied, or the next session re-derives it.\n\n6. bd-bandcheck is broken here (hardcoded /home/claude/work; reports every real band file as MISSING). Do not read its output as a band problem.\n\n7. CLAUDE.md section 2b: measure on THIS commit. Every number above is from 3fded70. A worktree comes up at the session base commit, not the branch tip.",
    "size": "M",
    "blocked_on": "Not blocked -- ready to cut, but RECOMMEND SPLITTING INTO TWO, per \"one feature per cut\". CUT 1 (S, the actual blindness, ship first): FIX-1 + mirror re-sync + RED-A in tests/test_toolchain_534.py. Band: test_toolchain_534, test_desandbox_tool_verifiers, test_v3_66_730_touch_map_tool_bridge, test_pk_mirrors_do_not_drift, test_contracts. This alone closes the demonstrated incident -- the gate starts running. CUT 2 (M, the gate's own denominator): FIX-2 + FIX-3 + RED-B/C/D, full 23-suite band. Reason to split: cut 1 is the only part that addresses the 261-green band, and cut 2 changes a shared harness. One caveat if you split -- cut 2's gate widening (255 -> 258 pairs) will then be banded automatically BY cut 1's new signal, which is a clean end-to-end proof cut 1 worked."
  },
  {
    "item": "ITEM 3 (Batch B remainder) -- bd-opv, bd-env-report-check, bd-equiv, bd-fullsuite: can each report a verdict it did not earn?",
    "defect_confirmed": "REAL",
    "evidence": "TREE VERIFIED. `git -C /home/user/BD log --oneline -1`\n  3fded70 Register 15.29: seven cuts, a contaminated corpus, and three corrected figures (#169)\nAll findings below are about commit 3fded70. All four tools are REAL; none is clean. Read-only throughout (probes live in /tmp/erc and /tmp/fw).\n\n=== 1. bd-fullsuite -- REAL, and the worst of the four ===\n`classify(out)` (toolchain/bin/bd-fullsuite:127-131) regex-matches ENV_PATTERNS against the ENTIRE combined stdout+stderr of a test FILE, first-match-wins. `status` (:436) then excuses the WHOLE file as \"env\", and `:728` computes the exit code from `real` only. So ONE env-looking line anywhere in a file's output suppresses every genuine failure in that file.\n\nSynthetic work tree /tmp/fw with a run_tests.py emitting a real AssertionError plus one Gtk line. Single variable changed between the two cases: presence of the string \"Namespace Gtk not available\".\n\n  $ venv/bin/python toolchain/bin/bd-fullsuite --work /tmp/fw --only mixed --jobs 1\n    running 1 file(s) (per-file isolation, 1 jobs, 300s each)\n      ENV  tests/test_mixed.py (ENV_GTK)\n    === 1 files | passed 0 | REAL failed 0 | skipped 0 | env 1 | timeout 0 | 0.2s\n      ENV  tests/test_mixed.py (ENV_GTK -- environment, not a code defect)\n    GREEN (1 env, 0 load-flake, 0 real; results -> /root/.bd_fullsuite/results.json)\n    exit=0\n  (the file's actual output contained: \"E   AssertionError: prune should report 1 removed, got 7\", \"Failed: 2\")\n\n  CONTROL, same failure with the Gtk line removed:\n  $ venv/bin/python toolchain/bin/bd-fullsuite --work /tmp/fw --only realonly --jobs 1\n      FAIL tests/test_realonly.py (REAL)\n    === 1 files | passed 0 | REAL failed 1 | skipped 0 | env 0 | timeout 0 | 0.2s\n      FAIL tests/test_realonly.py\n        E   AssertionError: prune should report 1 removed, got 7\n    exit=1\n\nSecond, independent sub-finding visible in the same output: `passed 0` and it still prints GREEN. bd-fullsuite has NO \"nothing earned a pass\" guard -- the exact guard bd-opv does have at :1400. A run in which every collected file classifies env exits 0 GREEN with zero tests verified.\n\nThird: the logic is DUPLICATED. `run_one` (:428/:436, spawn path, the default) and `_parse_worker_line` (:225/:226, `--fork` path) each derive kind+status independently. Fixing one leaves the other defective -- that is itself the \"fix reproduces the shape of the defect\" trap. The fork path additionally classifies over a TRUNCATED buffer (`out[-3000:]` at :208), so its denominator is smaller again.\n\nFeasibility of the sound fix CONFIRMED, not assumed: run_tests.py emits per-test markers on stdout, so failures can be attributed individually.\n  $ venv/bin/python run_tests.py tests/test_env_report_freshness.py | tail -5\n      PASS  test_report_without_provenance_is_unknown_not_fresh\n      Total: 7 | Passed: 7 | Failed: 0 | Skipped: 0\n  run_tests_core.py:1389-1391 and :1420-1422 print \"  FAIL  <name>\" followed by an indented 3-line error excerpt; :1385/:1416 print \"  PASS  <name>\"; :1387/:1418 \"  SKIP  <name> (<err>)\". A whole-file IMPORT ERROR arrives as ONE FAIL row (run_tests_core.py:654), so the legitimate GTK-import case still segments to a single env-classified block and stays excused.\n\n=== 2. bd-opv -- REAL (narrow; the aggregate guard is sound, the unit grading is not) ===\nThe aggregate contract is genuinely well built and I verified it holds:\n  $ venv/bin/python toolchain/bin/bd-opv --only OPV-QR\n      SKIP  OPV-QR       qr/pyzbar/PIL unavailable (No module named 'qrcode')\n      0 PASS  0 FAIL  1 SKIP  0 GATED\n    CANNOT-EVALUATE bd-opv: reason=EMPTY (1/1 check(s) SKIPped on absent preconditions and none PASSed -- no OPV item was actually verified; this is not a clean run)\n    exit=2\n  Full run: 17 PASS 0 FAIL 7 SKIP 1 GATED, exit=0.\n\nThe defect is at :1368-1370 -- a check that RAISES is graded SKIP, i.e. folded into the benign \"precondition missing\" bucket, and SKIP feeds nothing but `n_skip`:\n    try:\n        status, detail = fn()\n    except Exception as e:\n        status, detail = SKIP, f\"check raised: {type(e).__name__}: {str(e)[:80]}\"\nProven by loading bd-opv as a module and substituting REGISTRY (repo untouched; /tmp/erc/opvprobe.py):\n  $ venv/bin/python /tmp/erc/opvprobe.py\n      SKIP  OPV-BOOM     check raised: ZeroDivisionError: the check itself is broken\n      PASS  OPV-GOOD     a real pass\n      1 PASS  0 FAIL  1 SKIP  0 GATED\n    exit=0\nA broken check is indistinguishable in the summary line from a genuinely absent precondition (the OPV-QR row above), and one unrelated PASS clears the CANNOT-EVALUATE guard. The tool's own docstring defines SKIP as \"precondition missing (e.g. a setting/route absent in this build)\" -- a crashed check is not that.\n\n=== 3. bd-equiv -- REAL ===\n`run_tool` (:62-68) discards `cp.returncode` entirely. ERROR_SENTINEL (:46, commented \"@852: a crash is not agreement\") only fires on a HARNESS-level exception (timeout/OSError), never on the compared subprocess exiting non-zero. A tool that dies emits a traceback containing no extractable tokens, so it reads as \"produced nothing\", and `new >= nothing` is vacuously true.\n  /tmp/erc/old_t.py raises RuntimeError on input \"B\" and prints two test paths on \"A\"; /tmp/erc/new_t.py prints three on both.\n  $ venv/bin/python toolchain/bin/bd-equiv --old /tmp/erc/old_t.py --new /tmp/erc/new_t.py --inputs A B --work /tmp/erc --json\n    { \"verdict\": \"SUPERSET\", \"inputs\": 2,\n      \"rows\": [ {\"input\":\"A\",\"old_count\":2,\"new_count\":3,\"missing\":[],\"added\":[\"test_gamma\"],...},\n                {\"input\":\"B\",\"old_count\":0,\"new_count\":3,\"missing\":[],\"added\":[\"test_alpha\",\"test_beta\",\"test_gamma\"],...} ],\n      \"errored\": false, \"tokens_seen\": 8, \"regressions\": 0, \"additions\": 4 }\n    exit=0\n`errored: false` while the OLD tool crashed, and the printed verdict is \"Safe to retire IF 'more' is safe.\" This is the tool that authorizes deletion.\nThe @852/@855 guards ARE present and correct (`grade`, :92-125) and main() refuses an empty input list (`if not inputs: ... return 2`, :313-315). One LATENT hole worth noting but not the headline: `grade([], False)` returns \"EQUAL\" because the @855 guard is written `if rows and sum(...) == 0` -- unreachable through main() today, but it is a library-level footgun for any future caller.\n\n=== 4. bd-env-report-check -- REAL ===\nAgainst the live report it behaves correctly:\n  $ venv/bin/python toolchain/bin/bd-env-report-check\n    STALE: version 3.66.818 != tree 3.66.867 (commit differs: cee4be70f8e7 vs 3fded70208ed)\n    exit=1\nBut VERSION is the decisive field per the docstring, and the UNKNOWN guard at :98 requires BOTH fields to be unknown. With version UNKNOWN/absent and an advisory commit present, :110's guard (`if got_version and got_version != \"UNKNOWN\" and ...`) skips, `problems` stays empty, and it returns FRESH:\n  $ venv/bin/python toolchain/bin/bd-env-report-check --tree /home/user/BD --report /tmp/erc/r1.md   # generated_against_version=UNKNOWN + a commit\n    FRESH: version 3.66.867 (commit differs: 3fded70208ed vs 3fded70208ed)\n    exit=0\n  $ venv/bin/python toolchain/bin/bd-env-report-check --tree /home/user/BD --report /tmp/erc/r2.md   # NO version key at all + a commit\n    FRESH: version 3.66.867 (commit differs: deadbeefdead vs 3fded70208ed)\n    exit=0\nNote \"FRESH: version 3.66.867\" -- it prints the TREE's version as though the report had asserted it. The existing test only covers both-UNKNOWN (tests/test_env_report_freshness.py:119 test_explicit_unknown_provenance_does_not_pass), which is why this survived.\nREACHABLE IN PRODUCTION, not hypothetical. scripts/cloud-setup.sh:95-101:\n    if [ \"$HAVE_REPO\" = 1 ]; then\n      GEN_VERSION=\"$(grep -oE '__version__ *= *\"[^\"]+\"' bulk_downloader/__init__.py 2>/dev/null | grep -oE '[0-9]+\\.[0-9]+\\.[0-9]+' || true)\"\n      GEN_COMMIT=\"$(git rev-parse HEAD 2>/dev/null || true)\"\n    ...\n    echo \"generated_against_version=${GEN_VERSION:-UNKNOWN}\"\n    echo \"generated_against_commit=${GEN_COMMIT:-UNKNOWN}\"\nAny tree where `git rev-parse HEAD` succeeds but the version grep does not (partial checkout, mid-rebase, a reformatted __version__ line) produces exactly r1.md -- and the checker calls it FRESH.\nCosmetic but confusing, worth fixing in the same touch: the note truncates both SHAs to [:12], so r1.md printed \"commit differs: 3fded70208ed vs 3fded70208ed\".\n\n=== BAND, DERIVED NOT GUESSED ===\n  $ venv/bin/python toolchain/bin/bd-band-derive --files toolchain/bin/bd-opv toolchain/bin/bd-equiv toolchain/bin/bd-fullsuite toolchain/bin/bd-env-report-check tests/test_toolchain_534.py tests/test_env_report_freshness.py\n    == bd-band-derive :: FILES (6)\n      changed source (2): tests/test_toolchain_534.py, tests/test_env_report_freshness.py\n      changed, unclassified (4): toolchain/bin/bd-opv, toolchain/bin/bd-equiv, toolchain/bin/bd-fullsuite, toolchain/bin/bd-env-report-check\n      band (11): [see band field]\n    floor, not ceiling -- on-stash full suite + verify_release are binding.\nNOTE FOR THE IMPLEMENTER: bd-band-derive classified all four SUBJECT files as \"changed, unclassified\" -- the extensionless bd-* population problem from CLAUDE.md section 1, in the band tool itself. The band it returned is therefore driven by the two TEST files, not by the tools. Cross-check with grep confirmed nothing was dropped:\n  $ grep -rl \"bd-opv\\|bd-fullsuite\\|bd-equiv\\|bd-env-report-check\" tests/*.py\n    tests/test_env_report_freshness.py\n    tests/test_toolchain_534.py\n    tests/test_v3_66_653_dep_freshness.py\n  -- all three are inside the derived 11.\nAxis-6 re-derived (not quoted from CLAUDE.md) with `grep -rln \"ls-files\\|rglob\\|--collect-only\" tests/*.py` -> 60 hits; the ones whose denominator a TEST-FILE edit can move are the known set. Because the recommendation below adds NO new test file, the file-COUNT gates (test_pin_index_in_sync's test_files_scanned, test_source_windows_do_not_shift, test_pytest_runner_boundary, the three *_stays_retired gates, test_capture_execution_lanes, test_generated_artifacts_are_not_tracked) do not move. Verified there is no fixed-width source window in either target test file: `grep -n \"src\\[\" tests/test_toolchain_534.py tests/test_env_report_freshness.py` returns only `:800  line = src[:m.start()].count(\"\\n\") + 1`, which is not a fixed-width slice. _MAX_WINDOWS is 115 (tests/test_source_windows_do_not_shift.py:65) -- band it anyway to PROVE the new tests did not add one.",
    "files": [
      "toolchain/bin/bd-fullsuite:127-131 -- classify(): regex over the WHOLE file output, first-match-wins. Segment by per-test marker instead.",
      "toolchain/bin/bd-fullsuite:428,436 -- run_one(): spawn path (DEFAULT), kind=classify(out) then status='env' for the whole file",
      "toolchain/bin/bd-fullsuite:225,226 -- _parse_worker_line(): DUPLICATE of the same logic for --fork. Must be fixed with the same helper or it drifts.",
      "toolchain/bin/bd-fullsuite:208 -- out[-3000:] truncation feeding the fork-path classifier (smaller denominator again)",
      "toolchain/bin/bd-fullsuite:728 -- return 1 if (real or tmo or aborted) else 0; add the zero-pass CANNOT-EVALUATE guard here",
      "toolchain/bin/bd-fullsuite:39-42 -- exit-contract docstring, update to name the new third state",
      "toolchain/bin/bd-opv:1368-1370 -- except Exception -> SKIP; a crashed check must become its own state, not the benign bucket",
      "toolchain/bin/bd-opv:1392-1406 -- exit computation; the new state must reach EXIT_CANNOT_EVALUATE",
      "toolchain/bin/bd-opv:11-16 -- docstring defining PASS/FAIL/SKIP/GATED; add the fifth outcome",
      "toolchain/bin/bd-equiv:62-68 -- run_tool(): cp.returncode discarded; return ERROR_SENTINEL on non-zero exit",
      "toolchain/bin/bd-equiv:46 -- ERROR_SENTINEL definition/comment ('a crash is not agreement') -- the intent is already stated, only the implementation is short",
      "toolchain/bin/bd-equiv:117 -- grade(): 'if rows and sum(...)==0'; the leading `rows and` makes grade([]) return EQUAL (latent, library-only)",
      "toolchain/bin/bd-env-report-check:98-112 -- evaluate(): decisive-field UNKNOWN/absent must be UNKNOWN(2), not FRESH(0)",
      "toolchain/bin/bd-env-report-check:20-27 -- docstring 'VERSION IS DECISIVE'; it is currently not enforced when version is UNKNOWN",
      "tests/test_toolchain_534.py -- established home for this family (see test_equiv_refuses_to_certify_over_an_empty_token_set :695, test_a_file_that_collects_nothing_is_not_a_pass :1108, test_doc_truth_mints_a_verdict_instead_of_always_zero :844). Add the parametrized invariant + the bd-opv/bd-equiv/bd-fullsuite cases here.",
      "tests/test_env_report_freshness.py:119 -- test_explicit_unknown_provenance_does_not_pass covers only BOTH-unknown; add the version-only-unknown and version-absent cases beside it",
      "scripts/cloud-setup.sh:95-101 -- the writer that can emit version=UNKNOWN with a real commit (read-only context for the fix; no change required)"
    ],
    "red_test": "RECOMMENDED SHAPE: one parametrized invariant + four per-tool cases, all in tests/test_toolchain_534.py except the bd-env-report-check pair, which belongs beside its existing siblings in tests/test_env_report_freshness.py. Add NO new test file -- that keeps every file-count axis-6 gate still (see band notes) and matches where the family already lives.\n\nTHE PARAMETRIZED INVARIANT (the finding itself)\n  tests/test_toolchain_534.py :: test_a_unit_that_could_not_be_evaluated_never_lands_in_the_success_bucket\n  Table-driven over the four tools. Each row supplies (tool, an argv/fixture that makes exactly ONE unit unevaluable while at least one other unit succeeds, the exit code that must NOT be 0). Asserts rc != 0 AND that stderr/stdout names the unevaluable unit. Fail-closed: a row whose fixture cannot be constructed FAILS rather than skips -- otherwise the invariant reacquires the very shape it is testing.\n  WHY IT FAILS ON PRISTINE: all four rows currently exit 0. Proven above, one command each.\n\nPER-TOOL RED CASES (each must be shown failing on 3fded70 before any implementation)\n\n1. bd-fullsuite :: test_an_env_signal_does_not_excuse_a_real_failure_in_the_same_file\n   Build the /tmp/fw fixture already proven above (tempfile.TemporaryDirectory, a run_tests.py stub, tests/test_mixed.py). Assert rc != 0 and \"AssertionError\" reaches the operator.\n   WHY RED ON PRISTINE: measured exit=0, \"REAL failed 0\", \"GREEN\". Pasted in evidence.\n   Run the SAME fixture through --fork as a second row. _parse_worker_line:225 is a separate code path and a fix landing only in run_one leaves it green -- that row is what stops the fix reproducing the defect's shape.\n\n2. bd-fullsuite :: test_a_run_in_which_nothing_passed_is_not_green\n   Same harness, every file env-classified, zero passes. Assert rc != 0 / CANNOT-EVALUATE.\n   WHY RED ON PRISTINE: the measured run printed \"passed 0 ... GREEN ... exit=0\". :728 has no n_pass guard; bd-opv:1400 has exactly this guard, which is the proof it is the intended semantics.\n\n3. bd-opv :: test_a_check_that_crashes_is_not_graded_as_a_missing_precondition\n   Load bd-opv via importlib.machinery.SourceFileLoader (the /tmp/erc/opvprobe.py recipe works and needs no subprocess), substitute REGISTRY with one raising check + one passing check, call main([]).\n   WHY RED ON PRISTINE: measured \"1 PASS 0 FAIL 1 SKIP\", exit=0.\n   Set _BD_OPV_REEXEC=1 before exec_module or the module re-execs itself under the venv python at import time (bd-opv:65-70).\n\n4. bd-equiv :: test_a_compared_tool_that_exits_non_zero_is_not_silence_it_is_a_crash\n   Two stub scripts in tmp_path; old raises on one of two inputs. Assert verdict is CANNOT-EVALUATE / rc == 2, not SUPERSET/0.\n   WHY RED ON PRISTINE: measured verdict SUPERSET, \"errored\": false, exit=0.\n\n5. bd-env-report-check :: test_an_unknown_version_is_unknown_even_when_the_commit_is_present\n   Two fixtures beside tests/test_env_report_freshness.py:119, reusing its existing _tree/_report/_run helpers (READ THEM before calling -- do not guess their signatures): (a) generated_against_version=UNKNOWN with a real commit; (b) the version key absent entirely with a commit present. Assert rc == 2 both times.\n   WHY RED ON PRISTINE: both measured FRESH, exit=0.",
    "fix": "RECOMMENDATION: ONE parametrized invariant, and I recommend it over four separate fixes -- but paired with four per-tool unit fixes in the SAME cut, not instead of them.\n\nWhy one invariant: the four defects are one defect wearing four hats. Every one of these tools already HAS a correct aggregate CANNOT-EVALUATE concept (sec.EXIT_CANNOT_EVALUATE, and bd-opv/bd-fullsuite/bd-equiv each mint it correctly in at least one path). In all four the failure is at the UNIT level: a per-unit \"I could not evaluate this\" outcome is folded into the SUCCESS bucket instead of the unknown bucket, and the aggregate then honestly reports over a denominator the unevaluable unit has left. bd-opv's SKIP bucket, bd-fullsuite's ENV bucket, bd-equiv's silent-crash row, bd-env-report-check's UNKNOWN version -- same shape, four names. Stating it once as a table-driven invariant is what stops the NEXT toolchain addition acquiring it, which four bespoke tests would not.\n\nWhy not ONLY the invariant: it is an assertion, not a repair. Each tool needs its own unit-level change.\n\nWhy one cut and not two: CLAUDE.md section 4 records test_source_windows_do_not_shift sitting RED on main for five releases because a gate landed ahead of what it constrains. Splitting bd-fullsuite out would leave one invariant row red on main between cuts. Land it together.\n\nTHE FOUR UNIT FIXES\n\nA. bd-fullsuite (the substantial one)\n   Extract ONE helper used by both run_one:428-437 and _parse_worker_line:222-226 -- the duplication is the standing hazard.\n   Segment `out` on the per-test markers run_tests_core.py emits (\"  PASS  \", \"  FAIL  \", \"  SKIP  \" + indented continuation lines), classify each FAILING block on its own text, then:\n     - every failing block classifies ENV  -> status \"env\"  (unchanged, still excused)\n     - some ENV, some REAL                 -> status \"fail\", report the REAL ones\n     - no failing block found but rc != 0  -> status \"unknown\", NOT \"env\" and NOT \"pass\"\n   Add to :728: if no file produced a real verdict (zero passes across the run), exit sec.EXIT_CANNOT_EVALUATE with a REASON_EMPTY line, mirroring bd-opv:1400.\n   Raise or drop the fork path's out[-3000:] truncation (:208), or the fork classifier segments a buffer the spawn classifier does not.\n\nB. bd-opv (:1368-1370)\n   Give a raised check its own outcome (e.g. BROKEN), printed distinctly from SKIP, and make it reach EXIT_CANNOT_EVALUATE at :1392-1406. A check that died is not a precondition that was absent.\n\nC. bd-equiv (:62-68)\n   In run_tool, return ERROR_SENTINEL when cp.returncode != 0. The intent is already written at :46 (\"a crash is not agreement\"); only the implementation is short. Optionally drop the `rows and` in :117 so grade([]) is CANNOT-EVALUATE for library callers.\n\nD. bd-env-report-check (:98-112)\n   Treat the DECISIVE field as decisive: if got_version is None/\"\"/\"UNKNOWN\", return UNKNOWN(2) regardless of the commit. Keep commit strictly advisory. Also widen the [:12] SHA truncation in the note so \"commit differs: X vs Y\" cannot print two identical strings.",
    "band": [
      "tests/test_contracts.py",
      "tests/test_env_report_freshness.py",
      "tests/test_toolchain_534.py",
      "tests/test_v3_66_305_config_danger.py",
      "tests/test_v3_66_319_env_tranche_4_3d.py",
      "tests/test_v3_66_653_dep_freshness.py",
      "tests/test_v3_66_702_jd_supported_hosts_field.py",
      "tests/test_v3_66_711_automation_gui.py",
      "tests/test_v3_66_713_unscanned_surfaces.py",
      "tests/test_v3_66_716_decoys_and_shadow.py",
      "tests/test_v3_66_720_oidc_and_ffmpeg.py",
      "tests/test_desandbox_tool_verifiers.py -- ADDED on top of the floor: the sibling suite for toolchain exit contracts (imports bdtools_sec, asserts the CANNOT-EVALUATE discipline these fixes extend). bd-band-derive did not return it because the four subject files classified 'unclassified'.",
      "tests/test_source_windows_do_not_shift.py -- ADDED: axis-6, proves the new tests introduced no fixed-width window (_MAX_WINDOWS=115)",
      "tests/test_all_sources_parse.py -- ADDED: axis-6, bare '*.py' reaches tests/; the edited test files must parse",
      "tests/test_pin_index_in_sync.py -- ADDED as insurance ONLY IF the implementer adds a new test file instead of extending the two existing ones; build_pin_index.py globs (root/'tests').glob('*.py') at :151/:211, so a new file changes test_files_scanned and PIN_INDEX.json must be regenerated. The recommended plan adds no file, so it does not move."
    ],
    "over_sensitivity_test": "Each fix has a specific way to swing too far, and each needs its own assertion IN THE SAME TEST. These are the ones that matter:\n\nbd-fullsuite -- a file whose failures are ALL genuinely environmental must STILL be excused and STILL exit 0. Without this the tool becomes unusable in any container lacking GTK typelibs or a browser cache, which is most of them. Use a synthetic fixture, NOT a real repo file: I checked the obvious candidate and it does not reproduce here --\n  $ venv/bin/python toolchain/bin/bd-fullsuite --only v3_43_80_modules --jobs 1\n    === 1 files | passed 49 | REAL failed 0 | skipped 0 | env 0 | timeout 0 | 5.4s\n    GREEN   exit=0\nCLAUDE.md section 5 says that file false-fails without typelibs; in THIS container it passes 49/49, so anchoring the control on it would produce a test that proves nothing here and something else on the box. Build the all-env fixture in tmp_path instead.\nSecond control: an all-SKIPPED or genuinely passing file must not become \"unknown\" -- the new unknown state must be reachable only when rc != 0 with no attributable failing block.\n\nbd-opv -- a genuine precondition SKIP must stay benign and keep the run at exit 0. The OPV-QR row (\"qr/pyzbar/PIL unavailable (No module named 'qrcode')\") is the live anchor and it is real at this commit: the full run is 17 PASS / 0 FAIL / 7 SKIP / exit 0. If the fix turns any SKIP into a failure, bd-opv becomes a tool that can only refuse in every container. Only a check that RAISED may take the new state -- assert both directions in one test.\n\nbd-equiv -- a tool that legitimately emits zero tokens on ONE input while another input yields real tokens must stay evaluable. @852 already encodes this (\"PARTIAL emptiness stays evaluable ... a tool that can only refuse is not an instrument\") and tests/test_toolchain_534.py:695 guards it. The new signal must be the non-zero EXIT CODE, never an empty token set -- assert that a clean tool printing nothing on one input still grades EQUAL/SUPERSET at exit 0.\n\nbd-env-report-check -- (a) a report whose version MATCHES the tree must still be FRESH/0, and (b) a version match with a DIFFERING commit must still be 0 with the advisory note. The docstring at :20-27 is explicit that commit is advisory precisely because \"a gate that fires on every commit would be switched off, taking the version check with it\". tests/test_env_report_freshness.py:53 test_matching_version_is_fresh already holds direction (a); direction (b) needs an explicit assertion or the obvious over-correction (make commit decisive too) passes the new test and kills the tool.\n\nINVARIANT-LEVEL control: the parametrized test must also assert the POSITIVE row for each tool -- given a fixture where every unit IS evaluable, the tool exits 0. Four rows proving refusal and none proving acceptance would certify a suite of tools that can only say no.",
    "risks": "1. HIGHEST RISK -- the fix reproducing the defect's shape. bd-fullsuite's segmentation is itself a denominator: if the marker regex does not match the real run_tests.py output, EVERY failing block becomes unattributable. Choose the failure direction deliberately -- unattributable must mean \"unknown\", never \"env\". The correctness of the marker format is verified above (run_tests_core.py:1385-1391, :1416-1422, and a live run), but the implementer must re-check rather than trust this paragraph.\n\n2. bd-fullsuite runs the whole 1210-file suite; a segmentation bug that flips previously-excused files to REAL turns a green box run red for reasons unrelated to the code under test. Before merging, run bd-fullsuite over a few hundred real files and DIFF the per-file statuses against the pre-fix results.json (it is written to $BD_FULLSUITE_STATE/results.json, so a before/after diff is cheap). Any file whose status changes must be explained, not accepted. Its selftest is strong (24 controls, all PASS at this commit) and must stay green.\n\n3. The two duplicated bd-fullsuite sites are the classic partial fix. --fork is opt-in (:508, action=\"store_true\"), so a contributor testing only the default path will see the spawn fix work and never exercise _parse_worker_line. The invariant needs a --fork row.\n\n4. bd-opv's new state changes an exit contract other things may read. scripts/bd-opv-run.sh and scripts/bd-opv-check.sh exist but are a DIFFERENT subject (the on-stash shell OPV orchestrators, not this tool) -- I confirmed neither invokes toolchain/bin/bd-opv. Still, re-derive consumers before changing the contract rather than trusting that grep.\n\n5. bd-env-report-check's exit 2 is consumed by documentation flows: CLAUDE.md, project-knowledge/NEXT_SESSION_BOOTSTRAP.md, project-knowledge/SESSION_CARRY.md and docs/repo/CLAUDE_CODE_PROMPT.md all reference it, and tests/test_env_report_freshness.py:134 asserts the bootstrap doc gates its grep behind this check. Making more reports UNKNOWN is the intended direction, but it will make the checker non-zero more often in containers provisioned from a tree where the version grep failed -- which is the point, and should be stated in the CHANGELOG so it is not read as a regression.\n\n6. gitleaks: the fixtures involve no credentials, so the CLAUDE.md section 7 trap does not apply here. Do not \"improve\" any fixture into realistic-looking values.\n\n7. Release chores this cut still owes: version bump is three edits together (bulk_downloader/__init__.py, tests/test_settings_center_slice4.py's pin, CHANGELOG.md ASCII-only), then bd-regen-order AFTER the last source edit, then bd-guardcheck. None of the seven SHA-pinned guard files is touched by this plan. Container green is evidence toward the cut, never evidence the box is green.\n\n8. Scope: this is four tools in one cut, which strains \"one feature per cut\". I judged the shared invariant to be the single feature and the four unit fixes its minimum viable support -- but if the operator prefers a narrower blast radius, the clean split is bd-fullsuite alone (it is ~60% of the work and carries all of risk 2) with the invariant landing in the same cut as the other three, its bd-fullsuite row added afterward. Do not land the invariant with a red row.",
    "size": "L",
    "blocked_on": ""
  },
  {
    "item": "ITEM 4 -- bd-claim is inert when invoked from a shell (keyed on os.getpid(), 9 occurrences), so the .githooks/pre-commit concurrent-writer guard cannot be exercised by an operator.",
    "defect_confirmed": "REAL",
    "evidence": "HEAD VERIFIED: `git -C /home/user/BD log --oneline -1` -> `3fded70 Register 15.29: seven cuts, a contaminated corpus, and three corrected figures (#169)`. Tree clean before and after (`git status --porcelain` -> 0 lines). All mutating work was done in a faithful copy under /tmp; /home/user/BD/.git/bd-claims was and remains empty.\n\n`grep -c \"os.getpid()\" toolchain/bin/bd-claim` -> 9  (matches the item exactly; lines 81,83,85,91,132,133,139,147,156)\n\n--- (1) THE CLAIM IS STILLBORN. In a /tmp repo holding byte-copies of toolchain/bin/bd-claim and .githooks/pre-commit, core.hooksPath=.githooks:\n  $ venv/bin/python toolchain/bin/bd-claim add victim.py --label agentA\n  claimed 1 path(s) as pid 16401 (agentA)          <- reports SUCCESS\n  $ cat .git/bd-claims/*.json\n  {\"pid\": 16401, \"ppid\": 15935, \"label\": \"agentA\", \"paths\": [\"victim.py\"]}\n  $ venv/bin/python toolchain/bin/bd-claim list\n  no live claims                                    <- exit 0\n  $ ls .git/bd-claims/ | wc -l\n  0                                                 <- live_claims() reaped it\n\n--- (2) THE CONSUMER FAILS OPEN. The v3.66.848 failure verbatim:\n  $ bd-claim add victim.py --label agentA ; echo \"half-written work\" >> victim.py\n  $ git add -A && git commit -m \"agent B regen sweep\"\n  exit=0\n  [master 88ab5cb] agent B regen sweep\n   victim.py | 1 +\nAgent A's uncommitted work was swept onto the branch and the hook said nothing. Textbook CLAUDE.md section 0: the gate cannot see its subject, so it reports OK.\n\n--- (3) THE MECHANISM ITSELF IS SOUND -- only the key is wrong. Same repo, claim keyed to a genuinely live `sleep 300`:\n  pre-commit: REFUSING -- these staged paths are claimed by another live process:\n      victim.py   held by pid 18510 (agentA)\n  exit=1\n\n--- (4) SECOND, COUPLED DEFECT: THE HOOK'S SELF-EXCLUSION IS DEAD CODE. .githooks/pre-commit:80 calls `mod.conflicts(Path(repo), staged, pid=os.getppid())`. Instrumented (a /tmp copy of the hook, applied-check by length arithmetic):\n  HOOK-DEBUG hookpy.pid=22179 os.getppid()=22173 grandparent=bash\n  --- claimant pid=22170   invoking shell pid=21811\nos.getppid() is the PRE-COMMIT BASH process, spawned fresh per commit. It can never equal any stored claim pid, so conflicts()'s self-exclusion branch (bd-claim:114-115) is unreachable from the hook. Demonstrated live -- a shell holding a claim on its own file:\n  $ printf '{\"pid\": %d, ...}' \"$$\" > .git/bd-claims/$$.json ; git add victim.py ; git commit\n  exit=1\n  pre-commit: REFUSING ... victim.py   held by pid 18997 (me)\ni.e. pid 18997 IS the committing shell. THIS IS WHY THE TWO MUST SHIP IN ONE CUT: fixing only the key converts a silently-inert guard into one that locks the operator out of committing their own claimed file.\n\n--- (5) THIRD SYMPTOM, SAME ROOT CAUSE: `release` is also inert from a shell (bd-claim:91 unlinks f\"{os.getpid()}.json\", the CLI's own fresh pid):\n  $ bd-claim add victim.py --label A\n  claimed 1 path(s) as pid 308 (A)\n  $ bd-claim release\n  nothing to release for this pid\n\n--- (6) WHY THE SELFTEST MISSED IT (its denominator excludes the subject): `bd-claim --selftest` is GREEN on pristine (SELFTEST PASS, exit 0). Its \"live foreign claim\" case writes a file NAMED 999999.json whose pid FIELD is os.getpid() of the still-running test process, and compares against pid=os.getpid()+1. It never spawns bd-claim as a subprocess, so no case in it has a claimant that exits. Same shape as the bug.\n\n--- (7) DESIGN-CRITICAL MEASUREMENT -- THE OBVIOUS FIX REPRODUCES THE SHAPE. Keying on os.getppid() (the invoking shell) is NOT a fix in an agent harness. Measured across two consecutive Bash tool calls in this container:\n  call 1: this shell pid=24713 ppid=508 ; /proc/508/comm = claude\n  call 2: previous shell pid=24713 alive? NO   |  its parent 508 alive? YES  |  new shell pid=25788 ppid=508\nEach tool call gets a FRESH shell that dies immediately; only the harness process (508) is durable. So a ppid key works for an interactive operator and is inert for exactly the concurrent-agent case the tool exists for.\n\n--- (8) THE TWO INPUTS A CORRECT FIX NEEDS ARE BOTH AVAILABLE (measured, not assumed):\n  env inheritance into the hook:  HOOK sees BD_CLAIM_OWNER=session-XYZ\n  /proc ancestry from the hook:   anc pid=304 (bash) -> 302 (git) -> 32411 (bash) -> 508 (claude) -> 495 -> 491\nSo the committing session is derivable both ways.\n\n--- (9) CALLERS OF conflicts() IN THE TRACKED TREE (grep over the repo excluding .git/venv/audit-venv/node_modules): exactly 5 -- bd-claim:133,139,147,156 (all in _selftest) and .githooks/pre-commit:80. live_claims() at bd-claim:99 (show) and :113 (conflicts). A signature change must update all five.\n\n--- (10) Current version: bulk_downloader/__init__.py:33 -> __version__ = \"3.66.867\". /proc/sys/kernel/pid_max = 32768 here, so the selftest's 2**22 \"dead pid\" holds, but pid recycling is fast at this pid_max -- see risks.",
    "files": [
      "/home/user/BD/toolchain/bin/bd-claim:81-87 (add(): rec keyed on os.getpid(), filename f\"{os.getpid()}.json\", success message printed for an already-dead claim)",
      "/home/user/BD/toolchain/bin/bd-claim:90-95 (release(): unlinks f\"{os.getpid()}.json\" -- can never match a claim made by an earlier process)",
      "/home/user/BD/toolchain/bin/bd-claim:47-55 (_alive(): the only liveness predicate; needs a start-time check and a TTL sibling)",
      "/home/user/BD/toolchain/bin/bd-claim:58-73 (live_claims(): reap rule; silently unlinks on read -- must report what it reaped and why)",
      "/home/user/BD/toolchain/bin/bd-claim:110-120 (conflicts(): signature `pid: int` must become an owner-set; self-exclusion at :114-115 is currently unreachable from the hook)",
      "/home/user/BD/toolchain/bin/bd-claim:169-176 (main(): argparse -- add --owner/--ttl/--pid; BD_CLAIM_OWNER env like the existing BD_CLAIM_LABEL at :173)",
      "/home/user/BD/toolchain/bin/bd-claim:123-166 (_selftest(): 4 conflicts() call sites at :133,:139,:147,:156 must be updated; battery must gain a real subprocess-lifetime case)",
      "/home/user/BD/toolchain/bin/bd-claim:24-26 (docstring: 'A claim is keyed on PID and dies with the process' -- the sentence that describes the defect as a feature; also :21 'release # (also on exit)' is false, grep for atexit/signal returns nothing)",
      "/home/user/BD/.githooks/pre-commit:80 (hits = mod.conflicts(Path(repo), staged, pid=os.getppid()) -- os.getppid() is the per-commit bash pid; replace with the committing session's owner set)",
      "/home/user/BD/.githooks/pre-commit:64-98 (the embedded hook_py heredoc: where BD_CLAIM_OWNER / /proc-ancestry derivation goes; note the comment at :56-58 about ARGV-vs-stdin -- do not reintroduce stdin)",
      "/home/user/BD/tests/test_toolchain_534.py:485-501 (test_the_tools_this_cut_added_are_wired_and_selftest_clean -- runs `bd-claim --selftest` and requires literal 'SELFTEST PASS'; the extended selftest must keep both)",
      "/home/user/BD/tests/ (NEW FILE: tests/test_v3_66_868_claim_survives_the_shell.py -- proven-RED source at /tmp/claude-0/-home-user-BD/1edd5632-bd1e-5ab5-99ea-485358c1f4d6/scratchpad/test_red_proof.py)",
      "/home/user/BD/bulk_downloader/__init__.py:33 (__version__, currently 3.66.867)",
      "/home/user/BD/tests/test_settings_center_slice4.py:200 (the one real version pin -- per CLAUDE.md section 3, confirm via PIN_INDEX.json form==version, do not grep)",
      "/home/user/BD/CHANGELOG.md (ASCII-only entry, prepended, anchored on the previous ## v header)"
    ],
    "red_test": "NEW FILE tests/test_v3_66_868_claim_survives_the_shell.py. A complete, already-executed proof-of-RED is at /tmp/claude-0/-home-user-BD/1edd5632-bd1e-5ab5-99ea-485358c1f4d6/scratchpad/test_red_proof.py -- it builds a tmp git repo containing byte-copies of toolchain/bin/bd-claim and .githooks/pre-commit, symlinks venv/bin/python, sets core.hooksPath, and drives real `git commit`. Measured on pristine 3fded70:\n\n  $ /home/user/BD/venv/bin/python -m pytest test_red_proof.py -p no:cacheprovider -q\n  FAILED test_a_shell_claim_survives_the_process_that_made_it\n  FAILED test_the_hook_refuses_a_sweep_of_a_shell_made_claim\n  FAILED test_the_claimant_may_commit_its_own_claimed_path\n  3 failed, 1 passed in 1.32s\n\nTHREE GENUINELY RED CASES, and WHY each fails on pristine:\n1. test_a_shell_claim_survives_the_process_that_made_it -- runs `bd-claim add` as a SUBPROCESS (so the claimant truly exits), then a SECOND subprocess runs `bd-claim list`. Fails on pristine because the claim is keyed on the first subprocess's now-dead pid and live_claims() reaps it -> stdout is \"no live claims\". This is the item's literal claim. The subprocess boundary is load-bearing: an in-process test would keep the pid alive and pass for the wrong reason -- which is exactly how the existing _selftest missed this.\n2. test_the_hook_refuses_a_sweep_of_a_shell_made_claim -- claim, then `git add -A && git commit`. Fails on pristine with returncode 0 (asserts != 0 and \"REFUSING\" in stderr). Consumer-level proof.\n3. test_the_claimant_may_commit_its_own_claimed_path -- the OVER-SENSITIVE direction. Fabricates a claim owned by the test process itself (pristine bd-claim cannot produce a durable one) and commits it. Fails on pristine because the hook refuses its own claimant: \"REFUSING ... held by pid 30805 (me)\".\n\nFOURTH CASE IS GREEN ON PRISTINE AND MUST BE DECLARED AS SUCH: test_a_dead_claimant_never_wedges_the_repo (claim owned by pid 2**22, commit must succeed). It proves nothing on its own -- it is a regression guard against the fix swinging the other way, and it MUST be constrained by bd-mutate rather than by its pristine result.\n\nADD AFTER IMPLEMENTATION (RED only against the fixed design, so write them alongside the fix and prove them by mutant, not by pristine): test_an_expired_ttl_claim_is_reaped; test_bd_claim_release_actually_releases_a_shell_made_claim; test_add_refuses_when_no_durable_owner_can_be_derived (the UNKNOWN third state).\n\nALSO EXTEND bd-claim --selftest with a real subprocess-lifetime case, because test_toolchain_534.py:485 already runs it and its current battery structurally cannot represent a claimant that exits. Keep the literal string \"SELFTEST PASS\" -- that test asserts on it.",
    "fix": "Ship BOTH halves in one cut. Half A alone converts a silently-inert guard into one that wedges the operator (evidence 4).\n\nHALF A -- give a claim a DURABLE OWNER, bounded in two independent ways (toolchain/bin/bd-claim).\nClaim record gains: `owner` (string token), `owner_pid` (int|null, for liveness), `owner_start` (field 22 of /proc/<pid>/stat, defeats pid reuse), `expires_at` (epoch seconds). Keep `pid`/`ppid` for diagnostics.\nOwner resolution order in add(), most explicit first:\n  1. `--owner TOKEN` flag;\n  2. `$BD_CLAIM_OWNER` (mirror the existing BD_CLAIM_LABEL pattern at :173);\n  3. `--pid PID` -- claim on behalf of a named live process;\n  4. DERIVED: walk /proc/<pid>/stat ancestry upward from os.getppid(), skipping comm in {bash,sh,dash,zsh,fish,git}, and take the first survivor as owner_pid. Measured reachable: hook-bash -> git -> bash -> claude(508).\n  5. If none of the above yields a durable owner (no /proc, walk terminates in shells only, PID 1) -> REFUSE with exit 2 and say so. UNKNOWN IS A THIRD STATE AND IT FAILS. Never fall back to os.getpid(): that is the defect.\nFilename becomes a sanitized owner token, not f\"{os.getpid()}.json\".\nREAP RULE in live_claims(), dead if EITHER holds -- the two bounds are independent so neither alone can wedge the repo:\n  expired  = expires_at is not None and time.time() >= expires_at\n  vanished = owner_pid is not None and (not _alive(owner_pid) or start_time(owner_pid) != owner_start)\n`--ttl SECONDS` on add (bounded default, suggest 4h) is what stops a claim keyed on a long-lived harness pid outliving the work -- the over-sensitive direction. add() must PRINT the expiry and the exact release command.\nrelease() keys on the SAME resolved owner, not os.getpid(); it must say \"released <owner>\" or name what it looked for and did not find.\nshow() must report what it reaped and why (\"reaped N stale claim(s): 2 expired, 1 vanished\") -- a read that silently destroys evidence is its own trap.\nconflicts() signature changes from `pid: int` to `owners: set[str]` (or an owner-predicate). Update all five call sites: bd-claim:133,139,147,156 and .githooks/pre-commit:80.\n\nHALF B -- make the hook's self-exclusion actually identify the committer (.githooks/pre-commit:80, inside the heredoc at :64-98).\nReplace `pid=os.getppid()` with a derived owner set:\n  1. `$BD_CLAIM_OWNER` if present in os.environ -- MEASURED to be inherited by git hooks, so this is exact, not a guess. PREFER THIS.\n  2. Fallback only if unset: the hook process's full /proc ancestor PID set; a claim whose owner_pid is an ancestor of this commit is mine.\nKeep the existing UNKNOWN-fails discipline verbatim (:74-77 exec_module failure, :81-84 read failure both exit 1) -- do not weaken it while touching the block. Keep ARGV passing (:56-58 explains why stdin is wrong).\n\nDOCS in the same cut: bd-claim:24-26 currently states the defect as a feature (\"keyed on PID and dies with the process\"); :21 claims release happens \"also on exit\" and grep finds no atexit/signal handler. Rewrite both, and document that CONCURRENT AGENTS MUST SET BD_CLAIM_OWNER (see risks).",
    "band": [
      "FLOOR -- venv/bin/python toolchain/bin/bd-band-derive --files toolchain/bin/bd-claim .githooks/pre-commit  (verbatim output: 'changed source (0)', 'changed, unclassified (2)', band 4):",
      "tests/test_contracts.py",
      "tests/test_negative.py",
      "tests/test_toolchain_534.py",
      "tests/test_task_tracker_stays_retired.py -- NOTE bd-band-derive returned test_v3_66_742_lint_walk_pruned.py as its 4th; keep that one too:",
      "tests/test_v3_66_742_lint_walk_pruned.py",
      "AXIS-6 (a NEW test file is added, so these ten move -- re-derived at 3fded70 with grep -rln 'ls-files|rglob|--collect-only' tests/*.py and each hit's enumerated path checked; all ten confirmed PRESENT):",
      "tests/test_source_windows_do_not_shift.py",
      "tests/test_pytest_runner_boundary.py",
      "tests/test_capture_execution_lanes.py",
      "tests/test_deploy_manifest_stays_retired.py",
      "tests/test_codex_handoff_stays_retired.py",
      "tests/test_all_sources_parse.py",
      "tests/test_generated_artifacts_are_not_tracked.py",
      "tests/test_history_columns_go_through_migrations.py",
      "tests/test_pin_index_in_sync.py -- the tenth; NO grep of tests/ can find it (it imports tools.build_pin_index, which globs (root/'tests').glob('*.py') at :211). Regenerate PIN_INDEX regardless.",
      "THE NEW FILE ITSELF: tests/test_v3_66_868_claim_survives_the_shell.py",
      "NOT IN BAND, checked and excluded with reasons: test_gitignore_rules_actually_match.py (.gitignore paths only); test_v3_66_820_share_tools_saw_no_session_keys.py (text extensions, .py not in the set); test_playwright_engines_single_source.py (git ls-files -z '*.sh' -- bd-claim and pre-commit are BOTH EXTENSIONLESS, so it does not move); test_cut40_dashboard_today_iso.py and test_fts_external_content_delete.py (rglob scoped to bulk_downloader/, verified by reading :375-392 and :380-395).",
      "CROSS-CHECK grep, per CLAUDE.md section 4 (used to find what the tool missed, not instead of it): grep -rln 'bd-claim|bd_claim|pre-commit|hooksPath' tests/ -> test_toolchain_534.py, test_negative.py, test_v3_66_742_lint_walk_pruned.py. All already in the floor; no addition.",
      "RELEASE CHORES (section 3): bump bulk_downloader/__init__.py:33 (3.66.867 -> next), the version pin (read form=='version' out of PIN_INDEX.json -- do NOT grep, section 3 measures 8 hits of which 1 is real), ASCII-only CHANGELOG entry.",
      "REGEN, AFTER THE LAST SOURCE EDIT (section 2a): venv/bin/python toolchain/bin/bd-regen-order --work \"$PWD\", then venv/bin/python tools/build_pin_index.py, then git status --porcelain over the generated artifacts.",
      "git add the new test file BEFORE the final band run, or the five ls-files-based gates cannot see it and their pass proves nothing about it (section 2a)."
    ],
    "over_sensitivity_test": "FOUR assertions, because this fix has four distinct ways to swing the other way. The first two are the ones that matter.\n\n1. THE CLAIMANT MUST NOT BE LOCKED OUT OF ITS OWN FILE. test_the_claimant_may_commit_its_own_claimed_path (RED case 3 above): a live, durable claim owned by the committing session must let that session commit. This is currently RED and is the assertion that forces Half B into the same cut -- without it, Half A ships a guard that refuses every commit by the agent that made the claim.\n\n2. A CRASHED CLAIMANT MUST NEVER WEDGE THE REPO, and it must be reaped by BOTH bounds independently:\n   a. owner process vanished -> reaped (test_a_dead_claimant_never_wedges_the_repo; green on pristine, so it MUST be constrained by mutant M2 below, not by its pristine result);\n   b. TTL expired while the owner pid is STILL ALIVE -> reaped anyway. This second one is the real test of the design: it is what stops a claim keyed on a session-long harness pid from outliving the edit that justified it.\n\n3. NO CLAIMS MEANS NO OPINION. A commit in a repo with an empty (or absent) .git/bd-claims must exit 0 with no output. The single-operator session must not notice this exists -- the hook's :40-44 short-circuit and _selftest's fourth case both encode this today; keep both and assert the hook's stderr is empty.\n\n4. THE REFUSAL TO CLAIM MUST BE NARROW. `bd-claim add` failing closed (exit 2, \"no durable owner\") must fire ONLY when the derivation genuinely finds nothing -- an explicit --owner or BD_CLAIM_OWNER must always succeed, including when /proc is unreadable. Otherwise the \"unknown is a third state\" fix makes the tool unusable, which is the section-0 inverse applied to my own proposal.\n\nPROVE ALL FOUR WITH bd-mutate (venv/bin/python toolchain/bin/bd-mutate --spec mutants.json --band \"<band>\"), not by reading:\n  M1 reap-rule always-true (dead = True)      -> RED cases 1+2 must fail\n  M2 reap-rule always-false (dead = False)    -> assertion 2a must fail  [the one that constrains the green guard]\n  M3 delete the TTL disjunct                  -> assertion 2b must fail\n  M4 delete the self-exclusion in conflicts() -> assertion 1 must fail\n  M5 hook: revert :80 to pid=os.getppid()     -> assertion 1 must fail\n  M6 add(): fall back to os.getpid()          -> RED cases 1+2 must fail\nRun with PYTHONDONTWRITEBYTECODE=1 and purge __pycache__ around each mutant (section 6: an equal-length substitution restored inside the same wall-clock second changes neither mtime nor size, and the next run imports the mutant off a file whose sha256 matches the original). M5 mutates SHELL -- bd-mutate validates it with `bash -n`; a non-parsing mutant is INVALID, not escaped. Exit 2 from bd-mutate is \"no verdict\", not a softer 1.",
    "risks": "1. HIGHEST RISK -- MY OWN PROPOSAL REPRODUCES THE SHAPE OF THE DEFECT IF THE OWNER TOKEN IS SHARED. If two concurrent agents descend from the SAME durable ancestor (measured here: both Bash calls had ppid 508 `claude`), the derived owner_pid is identical for both, the hook's ancestry self-exclusion matches agent B against agent A's claim, and the guard goes silently inert for exactly the multi-agent case it exists for -- a fresh instance of \"a gate that cannot see its subject reports OK\". MITIGATION, and it must be enforced not documented: prefer the explicit BD_CLAIM_OWNER token over the ancestry walk; have `bd-claim add` DETECT that the derived owner is a shared ancestor (another live claim already holds the same owner_pid with a different label) and refuse or warn loudly; and have the hook prefer the env token, using ancestry only when no token is set. An implementer who takes the ancestry walk as the primary key will ship a guard that is green and blind.\n\n2. THE OBVIOUS FIX (key on os.getppid()) IS WRONG AND LOOKS RIGHT. Measured: the invoking shell is dead by the next tool call (24713 alive? NO), so a ppid key works for an interactive operator and is inert for an agent. Anyone who \"fixes\" this without running the two-call measurement will believe it works.\n\n3. PID REUSE with pid_max=32768 (measured on this container). _alive() alone can be fooled into reporting a recycled pid as the original claimant -> a stale claim blocks commits forever, which is the wedge the design exists to avoid. This is why owner_start (field 22 of /proc/<pid>/stat) is in the record and not optional. Note the field is space-delimited but comm can contain spaces/parens -- parse after the LAST ')' in the stat line, or the index is wrong.\n\n4. /proc IS LINUX-ONLY. Both the ancestry walk and the start-time check assume it. The target box and CI are Linux (uname verified as the deployment target), but the derivation must treat an unreadable /proc as UNKNOWN -> refuse to derive an owner, never silently degrade to os.getpid().\n\n5. TTL USES WALL CLOCK. A system clock jump expires claims early. Acceptable, but `show()` must distinguish \"expired\" from \"vanished\" so a surprised operator can tell which bound fired -- otherwise the diagnosis is invisible.\n\n6. SIGNATURE CHANGE HAS FIVE CALL SITES, and four of them are inside bd-claim's own _selftest (:133,:139,:147,:156). Read them before editing: three pass `pid=os.getpid()` and one `pid=os.getpid()+1`; a partial update leaves a selftest that passes while asserting nothing. tests/test_toolchain_534.py:485-501 runs `--selftest` and requires exit 0 AND the literal string \"SELFTEST PASS\".\n\n7. .githooks/pre-commit IS TRACKED (confirmed: `git ls-files .githooks` -> .githooks/pre-commit) but EXTENSIONLESS. It is invisible to `git ls-files -- '*.py' '*.sh'` gates, and bd-band-derive classified BOTH changed files as \"changed, unclassified\" with \"changed source (0)\" -- so the band tool cannot see either file as source. test_toolchain_534.py:116-119 already records exactly this trap about this exact file. Do not read the small band as evidence of small blast radius.\n\n8. core.hooksPath IS ARMED IN THIS CONTAINER (`git config --get core.hooksPath` -> .githooks) and /home/user/BD/.git/bd-claims already exists. A broken hook edit will block commits for every concurrent agent on this tree. BD_SKIP_CLAIM_CHECK=1 is the documented override (.githooks/pre-commit:25,28-30); make sure it still works after the edit, and test hook changes in a /tmp repo first as this recon did.\n\n9. THE ONE-FEATURE-PER-CUT RULE IS UNDER STRAIN. Half A and Half B are two changes, but shipping A alone is strictly worse than the status quo (evidence 4 proves it locks the operator out). Ship both; say so in the CHANGELOG.\n\n10. CANNOT BE VERIFIED HERE, STATED RATHER THAN GUESSED: the box's pid_max, whether the operator's real workflow sets BD_CLAIM_OWNER, and whether capture.sh exercises the hook at all. All container measurements; the box is still the gate.",
    "size": "M",
    "blocked_on": ""
  },
  {
    "item": "ITEM 5 \u2014 bulk_downloader/ai_boot_readiness.json has no in-flight marker; a reader sampling the file during the ~108s readiness run cannot distinguish \"a run is in flight\" from \"the run finished and failed.\"",
    "defect_confirmed": "REAL",
    "evidence": "HEAD VERIFIED:\n  $ git -C /home/user/BD log --oneline -1\n  3fded70 Register 15.29: seven cuts, a contaminated corpus, and three corrected figures (#169)\n  $ git -C /home/user/BD status --porcelain   -> (empty)   branch: main\n\nTHE DEFECT, MEASURED. Driver: /tmp/claude-0/-home-user-BD/1edd5632-bd1e-5ab5-99ea-485358c1f4d6/scratchpad/probe_item5.py\n(read-only w.r.t. the repo; all state in a tmpdir). Run with /home/user/BD/venv/bin/python, exit=0.\n\nSCENARIO A -- first-ever run. Is anything written before attempt 1 completes?\n  -- sampled 0.6s into attempt 1, run() still executing --\n  [mid attempt 1] FILE ABSENT\n  run() returned {'code': 0}\n  [after run] on-disk state='ready' attempt=2 ...\n=> There is NO write between process start and the END of attempt 1. run() (ai_boot_readiness.py:127-170)\n   persists only at :132 / :137 / :147 / :166 -- every one of those is at or after an attempt's outcome.\n   Attempt 1 issues up to 5 sequential HTTP calls each bounded by REQUEST_TIMEOUT = 120.0 (:17).\n\nSCENARIO B -- the box's actual behaviour (systemd Restart=on-failure / RestartSec=60,\ninstall_service.sh:266-276, Type=simple, StartLimitIntervalSec=0). A prior run left a TERMINAL\ndegraded doc under the SAME boot_id; a NEW run is now in flight:\n  prior run rc = 1\n  [prior run terminal] on-disk state='degraded' attempt=2 error_code='ollama_unreachable' gpu.available=False models.text='pending'\n  -- NEW run is executing attempt 1 right now --\n  [mid new attempt 1] on-disk state='degraded' attempt=2 error_code='ollama_unreachable' gpu.available=False models.text='pending'\n        read_status()-> state='degraded'\n  new run rc = {'code': 0}          <-- the run CONVERGES; the reader saw 'degraded'\n=> Byte-identical to terminal failure, and read_status() cannot rescue it: boot_id matches (same boot)\n   and 108s << STALE_AFTER_SECONDS = 600 (ai_boot_status.py:12). This is exactly the wrong\n   \"Audit #3 reproduced\" verdict recorded at project-knowledge/SESSION_CARRY.md:3657-3666.\n\nSCENARIO C -- the INVERSE, which already exists today and is the trap any naive fix falls into:\n  [orphaned retrying (no process alive)] on-disk state='retrying' attempt=1 error_code='ollama_unreachable' gpu.available=False models.text='pending'\n        read_status()-> state='retrying'\n=> A process SIGKILLed / OOMed / `systemctl stop`ped after a :166 write leaves state='retrying'\n   readable for a full 600s. So 'retrying' is ALREADY a lying in-flight marker.\n\nVOCABULARY the file can carry (measured from source, not assumed):\n  run() emits state in {not_applicable, degraded, retrying, ready}; read_status() adds {unknown, stale}.\n  There is NO field anywhere that answers \"is a process working on this right now?\"\n  $ grep -n '\"final\"\\|in_progress\\|in_flight' bulk_downloader/ai_boot_*.py   -> no output\n\nNUANCE, stated so the implementer is not surprised: the writes at :166 for a non-final attempt DO\ncarry state='retrying', so the item's literal \"indistinguishable\" is only strictly true for\n(a) the pre-attempt-1 window -- which on the box is the previous run's terminal degraded doc, Scenario B --\nand (b) an orphaned marker, Scenario C. In window (a) the defect is total. That window is the whole of\nattempt 1, which is EXPECTED to fail on the box (it races Ollama's socket and the GPU driver), so it is\nthe window a reader is most likely to sample. The defect is real; its mechanism is \"no write happens\nbefore the first probe call\", not \"the retrying token is missing\".\n\nPRISTINE BAND BASELINE (so a later red is attributable):\n  $ BD_HOME=/tmp/bd_home BD_INSTALL_DIR=/tmp/bd_inst venv/bin/python -m pytest -q -p no:cacheprovider \\\n      tests/test_ai_boot_readiness.py tests/test_ai_boot_service_install.py tests/test_ai_boot_status.py \\\n      tests/test_ai_boot_status_api.py tests/test_ollama_boot_probe.py\n  29 passed in 2.82s   exit=0\n  $ ... tests/test_contracts.py tests/test_dependency_graph_in_sync.py tests/test_function_index_in_sync.py\n  28 passed in 19.64s  exit=0\n  $ ... tests/test_import_graph_no_new_edges.py tests/test_pin_index_in_sync.py\n  10 passed in 15.48s  exit=0\nALL GREEN on pristine. (test_contracts is green under plain pytest here; SESSION_CARRY open Item 6\nrecords it as pre-existing red under `bd-band` specifically -- use pytest, or expect that noise.)\n\nCONSUMER CENSUS (the item required this before any schema change).\nInstrument: AST over 2566 tracked files = 2108 *.py + 458 tracked extensionless python-shebang\nfiles (CLAUDE.md section 1 -- a *.py glob would have missed toolchain/ and project-knowledge/).\n0 files failed to parse. Script: .../scratchpad/consumers2.py\n  MY FIRST PREDICATE WAS WRONG AND I AM REPORTING IT: it tested `node.module` truthy, which is\n  None for `from . import ai_boot_status`, so it returned ONE consumer and silently excluded\n  app_ai.py -- the section-0 shape, in my own instrument. Corrected predicate covers both\n  ImportFrom forms. Corrected output:\n    bulk_downloader/ai_boot_readiness.py:7  from .ai_boot_status import STATE_PATH, load_effective_config, sanitize_endpoint, write_status\n    bulk_downloader/app_ai.py:19            from . import ai_boot_status\n    tests/test_ai_boot_readiness.py:3       from bulk_downloader import ai_boot_readiness\n    tests/test_ai_boot_status.py:4          from bulk_downloader import ai_boot_status\n    tests/test_ai_boot_status_api.py:2      from bulk_downloader import ai_boot_status\nString reach over ALL tracked files (catches .sh/.ts/.tsx an AST walk cannot read):\n    install_service.sh:271                       PRODUCER (systemd ExecStart), not a reader\n    bulk_downloader/app_ai.py:21                 payload[\"boot_readiness\"] = ai_boot_status.read_status()  -> GET /api/ai/status\n    frontend/src/hooks/useIntegrations.ts:168-180  interface AiBootReadiness {state?, error_code?, models?}\n    frontend/src/routes/Integrations.tsx:371     <AiBootReadinessStatus value={aiStatus.data?.boot_readiness} />\n    frontend/src/components/ui/AiBootReadinessStatus.tsx:3-18  if/else-if chain on value?.state\n    tests/capture_parallel_files.txt:4           lane assignment for test_ai_boot_status_api.py\n  NO bd-* tool reads it:  $ grep -rln \"ai_boot\" toolchain/bin/  -> no output\n  NO shell/capture consumer: $ grep -rn \"ai_boot\" capture.sh scripts/ -> no output\n\nREADER-BREAK ANALYSIS (what a new field can and cannot do):\n  - Frontend has NO runtime schema validation ($ grep -rln \"zod\\|z.object\" frontend/src/ -> none).\n    apiGet<T> (frontend/src/lib/api-client.ts:66) is an unchecked cast; every AiBootReadiness field\n    is optional. An EXTRA JSON key is inert at runtime and compile-time. SAFE.\n  - AiBootReadinessStatus.tsx is an if/else-if on `state`. A NEW state token (e.g. \"starting\")\n    falls through to the default label \"AI readiness unknown\" -- a soft break. Adding a SEPARATE\n    field and REUSING the existing \"retrying\" token requires zero frontend change and already\n    renders \"AI warming\" (AiBootReadinessStatus.tsx:7, pinned by its vitest case at\n    AiBootReadinessStatus.test.tsx:9).\n  - No test anywhere asserts an exact key-set of the readiness document\n    ($ grep -rn \"sorted(body)\\|set(body)\\|body.keys()\" tests/test_ai_boot*.py -> none).\n  - THE ONE EXACT-EQUALITY CONSTRAINT THAT BINDS: tests/test_ai_boot_status.py:40\n    `assert loaded == written`. read_status() must NOT invent a field that write_status() did not\n    write. This is why the marker must be stamped in write_status(), not derived in read_status().\n  - tests/test_ai_boot_status_api.py:14 is exact-equality but over a monkeypatched read_status\n    return value, so it is insulated. SAFE.\n  - write_status() has exactly ONE production caller: ai_boot_readiness.py:54 (_persist).\n    _persist has exactly FOUR call sites: :132, :137, :147, :166.\n  - build_pin_index.py counts FILES (`scanned += 1` per path at :151); appending tests to EXISTING\n    files leaves test_files_scanned unchanged -- see \"risks\" for why that matters.",
    "files": [
      "bulk_downloader/ai_boot_status.py:12 \u2014 add IN_FLIGHT_TTL_SECONDS beside STALE_AFTER_SECONDS = 600 (do NOT name it BD_*; CLAUDE.md section 4 bands test_gui_parity on any BD_-prefixed name)",
      "bulk_downloader/ai_boot_status.py:70-92 \u2014 write_status(): add keyword-only `final: bool = True`; stamp \"final\" into `payload` in the same .update() block that stamps schema_version/boot_id/updated_at (:74-78), so the RETURNED payload carries it and tests/test_ai_boot_status.py:40 `loaded == written` still holds",
      "bulk_downloader/ai_boot_status.py:95-113 \u2014 read_status(): three new branches, ordered AFTER the boot_id check (:107) and BEFORE the expired check (:111). See `fix`.",
      "bulk_downloader/ai_boot_readiness.py:53-54 \u2014 _persist(): add a REQUIRED keyword-only `final: bool` (no default) and forward it to write_status; the missing default is what forces all five call sites to name it",
      "bulk_downloader/ai_boot_readiness.py:132 \u2014 not_applicable persist -> final=True",
      "bulk_downloader/ai_boot_readiness.py:137-138 \u2014 invalid_config degraded persist -> final=True",
      "bulk_downloader/ai_boot_readiness.py:140 \u2014 INSERT the in-flight marker write immediately BEFORE `probe = probe_factory(...)`: _persist({**_base(cfg, 0), \"state\": \"retrying\", \"error_code\": \"\", \"error\": \"\"}, state_path, now, boot_id, final=False). Before the factory, not after, so a slow factory is also covered.",
      "bulk_downloader/ai_boot_readiness.py:143-146 \u2014 INSERT a per-attempt marker refresh at the top of the `for index in range(...)` loop body: _persist({**_base(cfg, attempt), \"state\": \"retrying\"}, state_path, now, boot_id, final=False). This is the heartbeat that keeps the TTL honest.",
      "bulk_downloader/ai_boot_readiness.py:147 \u2014 ready persist -> final=True",
      "bulk_downloader/ai_boot_readiness.py:166 \u2014 failed persist -> final=final (the local `final` bound at :150 already means exactly this)",
      "tests/test_ai_boot_readiness.py \u2014 APPEND two tests (do not create a new file; see risks)",
      "tests/test_ai_boot_status.py \u2014 APPEND three tests (do not create a new file)",
      "OPTIONAL, only if the label should change: frontend/src/hooks/useIntegrations.ts:168-175 (add `final?: boolean;`), frontend/src/components/ui/AiBootReadinessStatus.tsx:4-16 (prefer `value?.final === false` over the state token), frontend/src/components/ui/AiBootReadinessStatus.test.tsx (add cases). NOT REQUIRED for correctness \u2014 reusing the existing \"retrying\" token already renders \"AI warming\"."
    ],
    "red_test": "FIVE assertions. Four are proven RED on pristine 3fded70 with REAL AssertionErrors (not\nNameError/AttributeError \u2014 CLAUDE.md section 2a: discriminate the exception you are hunting).\nProof driver: /tmp/claude-0/-home-user-BD/1edd5632-bd1e-5ab5-99ea-485358c1f4d6/scratchpad/red_proof.py, exit=0:\n\n  RED-1: RED on pristine (AssertionError: no document existed while attempt 1 was in flight)\n  RED-2: RED on pristine (AssertionError: reader mid-run saw terminal 'degraded'/'ollama_unreachable')\n  RED-3: GREEN on pristine  <-- BAD, would prove nothing        [labelled deliberately, see below]\n  RED-4: RED on pristine (AssertionError: orphaned in-flight marker still reads 'retrying' at +181s)\n  RED-5: RED on pristine (AssertionError: finality-less doc reported as terminal 'degraded')\n\n--- tests/test_ai_boot_readiness.py (APPEND; reuse the module's existing CFG / ScriptedProbe / _factory) ---\n\nRED-1  test_in_flight_marker_precedes_the_first_probe_call\n  Wrap probe.list_models with a spy that records path.exists() and the parsed doc AT CALL TIME,\n  then run() with retry_delays=(), now=lambda: 1000.0, boot_id=\"boot-a\".\n    assert seen[\"exists\"] is True, \"no document existed while attempt 1 was in flight\"\n    assert seen[\"doc\"][\"final\"] is False\n  WHY IT FAILS ON PRISTINE: run() makes no _persist call before :147/:166, both of which are at or\n  after attempt 1's outcome. The file does not exist yet.\n\nRED-2  test_new_run_does_not_leave_the_previous_terminal_failure_readable   [the box's real shape]\n  1. Run a failing probe to exhaustion -> assert on-disk state == \"degraded\" (precondition, must be\n     asserted or the test can pass for the wrong reason).\n  2. Start a SUCCEEDING run against the SAME path and SAME boot_id=\"boot-a\", with a spy on\n     list_models that captures status.read_status(path, now=..., boot_id=\"boot-a\").\n    assert captured[\"doc\"][\"state\"] != \"degraded\"\n    assert captured[\"doc\"][\"final\"] is False\n  WHY IT FAILS ON PRISTINE: nothing is written before the new attempt 1, so the reader gets the\n  previous process's terminal record verbatim. Measured above as Scenario B.\n\n--- tests/test_ai_boot_status.py (APPEND) ---\n\nRED-4  test_abandoned_in_flight_marker_expires_well_before_the_stale_window\n    status.write_status({\"state\": \"retrying\"}, path, now=1000.0, boot_id=\"boot-a\", final=False)\n    fresh = status.read_status(path, now=1060.0, boot_id=\"boot-a\")\n    dead  = status.read_status(path, now=1000.0 + 181, boot_id=\"boot-a\")\n    assert fresh[\"state\"] == \"retrying\" and fresh[\"final\"] is False\n    assert dead[\"state\"] == \"stale\" and dead[\"stale_reason\"] == \"abandoned\"\n  WHY IT FAILS ON PRISTINE: STALE_AFTER_SECONDS is 600, so at +181s read_status returns the payload\n  verbatim with state \"retrying\". Note the literal 181 rather than IN_FLIGHT_TTL_SECONDS+1 \u2014\n  referencing a constant that does not exist yet would raise AttributeError, which an incomplete\n  stub also raises, and the RED would prove nothing.\n\nRED-5  test_document_that_cannot_answer_finality_is_unknown_not_terminal\n  Hand-write a pre-fix v1 doc (no \"final\" key) with a matching boot_id and a fresh updated_at:\n    {\"schema_version\": 1, \"state\": \"degraded\", \"error_code\": \"ollama_unreachable\",\n     \"boot_id\": \"boot-a\", \"updated_at\": \"1970-01-01T00:16:40Z\"}\n    out = status.read_status(path, now=1050.0, boot_id=\"boot-a\")\n    assert out[\"state\"] == \"unknown\" and out[\"reason\"] == \"no_finality_marker\"\n  WHY IT FAILS ON PRISTINE: read_status returns state \"degraded\". This is the rolling-deploy case\n  (a doc written by the pre-fix binary) and it is the ONLY place \"unknown is a third state\" can be\n  enforced. Without it, an unanswerable doc silently reads as terminal \u2014 the exact defect, restored.\n\nRED-3 is the over-sensitivity clamp; see over_sensitivity_test. It is GREEN on pristine and I am\nsaying so rather than dressing it up as a RED \u2014 its job is to constrain the fix, not to prove the bug.",
    "fix": "SHAPE: the marker must be SELF-EXPIRING, never a boolean a dead process leaves behind. That is\nthe whole design, and it is what stops the fix reproducing the defect (Scenario C is today's\nproof that a bare state token rots into a lie).\n\n1. bulk_downloader/ai_boot_status.py:12 \u2014 add beside STALE_AFTER_SECONDS = 600:\n     IN_FLIGHT_TTL_SECONDS = 300   # a run writes at every attempt boundary (~18s apart in the\n                                   # 108s measured run); 300s tolerates one slow attempt without\n                                   # calling a live run abandoned. NOT BD_-prefixed on purpose.\n\n2. write_status(..., *, final: bool = True) \u2014 stamp it in the SAME payload.update() block at :74-78\n   as schema_version/boot_id/updated_at:  \"final\": bool(final)\n   Defaulting to True is safe ONLY because (a) write_status has exactly one production caller and\n   (b) _persist below makes the argument REQUIRED, so no run() path can inherit the default by\n   accident. The default exists solely to keep the three existing test callers\n   (test_ai_boot_status.py:33, :47, :75) and `loaded == written` (:40) working unedited.\n\n3. read_status() \u2014 insert AFTER the boot_id check (:107) and BEFORE the expired check (:111):\n     a) \"final\" not in payload            -> return {**payload, \"state\": \"unknown\",\n                                                     \"reason\": \"no_finality_marker\"}\n        THE THIRD STATE. A document that cannot answer the question must say so and must not be\n        graded as a terminal verdict. Self-heals on the next readiness write.\n     b) payload[\"final\"] is False and (epoch - updated) > IN_FLIGHT_TTL_SECONDS\n                                          -> return {**payload, \"state\": \"stale\",\n                                                     \"stale_reason\": \"abandoned\"}\n        The writer vanished. Reusing the existing \"stale\" token means AiBootReadinessStatus.tsx:15\n        already renders \"AI readiness stale\" with zero frontend change.\n     c) payload[\"final\"] is False and within TTL -> fall through, payload returned verbatim with\n        state \"retrying\" and final False. This is the in-flight answer the item asked for.\n   `updated` may be None (unparseable updated_at) \u2014 treat that as case (b), fail-safe.\n\n4. bulk_downloader/ai_boot_readiness.py:53-54 \u2014 _persist gains a REQUIRED keyword-only `final: bool`\n   (deliberately no default) and forwards it. Every one of the four existing call sites plus the two\n   new ones must then name it; omitting one is an authoring-time TypeError, not a silent True.\n\n5. Two new writes (see `files` for exact anchors):\n     - one before probe construction, state \"retrying\", attempt 0, final=False \u2014 closes the\n       pre-attempt-1 window that Scenario A/B proved open;\n     - one at the top of each loop iteration, state \"retrying\", attempt=N, final=False \u2014 the\n       heartbeat that keeps IN_FLIGHT_TTL_SECONDS honest without inventing an expires_at field.\n\nWHY NOT a new `state` token like \"starting\": AiBootReadinessStatus.tsx:4-16 is an if/else-if chain\nand an unknown token falls through to \"AI readiness unknown\". Reusing \"retrying\" + adding a separate\n`final` field keeps the frontend correct with no change at all.\n\nWHY NOT bump SCHEMA_VERSION to 2: it is defensible but it costs three deliberate assertion edits\n(test_ai_boot_status.py:41, :61, :62) and buys nothing that branch 3(a) does not \u2014 an absent\n`final` key already identifies a pre-fix document. If the implementer bumps it anyway, those three\nlines are the complete list of what breaks, and test_ai_boot_status_api.py:9/:14 does NOT break\n(monkeypatched, self-consistent).\n\nWHY NOT derive the marker in read_status instead of stamping it in write_status:\ntests/test_ai_boot_status.py:40 asserts `loaded == written`. A field invented at read time breaks it.",
    "band": [
      "DERIVED (not hand-rolled): venv/bin/python toolchain/bin/bd-band-derive --files bulk_downloader/ai_boot_readiness.py bulk_downloader/ai_boot_status.py  -> band (8):",
      "tests/test_ai_boot_readiness.py",
      "tests/test_ai_boot_service_install.py",
      "tests/test_ai_boot_status.py",
      "tests/test_ai_boot_status_api.py",
      "tests/test_contracts.py",
      "tests/test_dependency_graph_in_sync.py",
      "tests/test_function_index_in_sync.py",
      "tests/test_import_graph_no_new_edges.py",
      "--- ADDED ON TOP (the tool says floor, not ceiling) ---",
      "tests/test_ollama_boot_probe.py \u2014 the probe whose ProbeFailure/timeout semantics the marker's TTL is reasoned from; not in the derived band, cheap, green at baseline",
      "tests/test_pin_index_in_sync.py \u2014 CLAUDE.md section 4's tenth axis-6 gate, whose enumerator lives one import away in tools/build_pin_index.py:151/:211 and which NO grep of tests/ can find. Band it and regenerate PIN_INDEX regardless of what any grep returned.",
      "frontend vitest AiBootReadinessStatus.test.tsx \u2014 ONLY if the optional frontend change is taken",
      "--- CROSS-CHECK, per the brief ---",
      "grep -rl \"ai_boot\" tests/*.py -> test_ai_boot_readiness.py, test_ai_boot_service_install.py, test_ai_boot_status.py, test_ai_boot_status_api.py. STRICT SUBSET of the derived band. The tool missed nothing.",
      "AXIS-6 NOT REQUIRED, and here is why rather than an assertion: appending to EXISTING test files adds no tracked file, so the five ls-files-enumerating gates and test_source_windows_do_not_shift do not move. build_pin_index counts FILES (scanned += 1 per path at :151), so test_files_scanned is unchanged too. Creating a new test file would pull all of them in \u2014 do not.",
      "NO fixed-width source-window test reads write_status / read_status / run: grep -rn \"src[pos:pos+\" tests/*.py | grep -i boot -> no output. Verified, not assumed.",
      "test_gui_parity.py NOT required \u2014 no BD_-prefixed name is introduced. Keep it that way when naming IN_FLIGHT_TTL_SECONDS.",
      "test_route_index_in_sync.py / test_route_map_invariant.py NOT required \u2014 no route added; app_ai.py:21 changes zero routing surface.",
      "REGEN, after the LAST source edit (CLAUDE.md section 2a): venv/bin/python toolchain/bin/bd-regen-order --work \"$PWD\"  \u2014 FUNCTION_INDEX and DEPENDENCY_GRAPH will shift on line numbers in both edited modules. No NEW import edge is created (ai_boot_status gains no import; it must NOT import ai_boot_readiness for REQUEST_TIMEOUT \u2014 that would be a cycle and a new edge), so tools/decomp/import_graph_gate.py --update should be unnecessary; verify rather than assume."
    ],
    "over_sensitivity_test": "RED-3 (append to tests/test_ai_boot_status.py). Proven GREEN on pristine and I am reporting that\nhonestly \u2014 it is the clamp, not the proof of the bug. Its job is to stop the fix swinging into\n\"call everything inconclusive\", which CLAUDE.md section 6 names as the exact way this class of\nfix destroys its own tool:\n\n  def test_terminal_documents_are_never_downgraded_to_unknown_or_in_flight(tmp_path):\n      path = tmp_path / \"terminal.json\"\n      for state in (\"ready\", \"degraded\", \"not_applicable\"):\n          status.write_status({\"state\": state}, path, now=1_000.0, boot_id=\"boot-a\", final=True)\n          out = status.read_status(path, now=1_050.0, boot_id=\"boot-a\")\n          assert out[\"state\"] == state          # NOT \"unknown\", NOT \"stale\"\n          assert out[\"final\"] is True\n      # and the terminal verdict must survive the whole existing stale window unchanged\n      status.write_status({\"state\": \"degraded\", \"error_code\": \"gpu_unavailable\"}, path,\n                          now=1_000.0, boot_id=\"boot-a\", final=True)\n      near = status.read_status(path, now=1_000.0 + status.STALE_AFTER_SECONDS - 1, boot_id=\"boot-a\")\n      assert near[\"state\"] == \"degraded\" and near[\"error_code\"] == \"gpu_unavailable\"\n\nThis is the assertion that makes IN_FLIGHT_TTL_SECONDS apply to final=False documents ONLY. Without\nit the obvious \"improvement\" \u2014 expiring every document at 300s \u2014 silently halves STALE_AFTER_SECONDS\nfor terminal verdicts and turns a real, actionable `degraded: gpu_unavailable` into `stale`, which is\nthe same information loss as the original defect pointed the other way.\n\nSecond clamp, same file, guarding the other over-sensitive direction:\n  a LIVE run must not be graded abandoned. RED-4's `fresh` half (now=1060.0, i.e. 60s in \u2014 longer\n  than RestartSec=60 and well inside the measured 108s run) asserts state stays \"retrying\" with\n  final False. Keep both halves in one test so a future edit cannot satisfy one by breaking the other.\n\nAUDIT OF MY OWN PROPOSAL FOR THE DEFECT'S SHAPE (CLAUDE.md section 0, rule 3):\n  Q: does the new gate have a denominator that excludes its subject?\n  A: it would, on one path, and branch 3(a) is the fix for it. If read_status defaulted a MISSING\n     `final` key to True, then any document written by a path that forgot the field \u2014 including\n     every pre-fix document during a rolling deploy \u2014 would read as a terminal verdict. That is\n     this defect, reintroduced by the fix, invisibly. Hence: absent `final` => \"unknown\" /\n     \"no_finality_marker\". Unknown is a third state and it fails.\n  Q: and if it defaulted to False instead?\n  A: every legacy doc reads in-flight, then expires to \"abandoned\" \u2014 over-sensitive, but it fails\n     LOUDLY and safely. Still worse than saying \"I cannot tell\", which is what 3(a) does.",
    "risks": "1. THE FIX'S OWN SHAPE \u2014 the highest risk and the reason for branch 3(a). A boolean `in_progress`\n   written before attempt 1 and cleared on the terminal write is the obvious fix AND IT REPRODUCES\n   THE DEFECT EXACTLY: Scenario C measured today's `retrying` token doing precisely that \u2014 a process\n   killed mid-run leaves the marker readable for 600s, so the reader is lied to in the opposite\n   direction. The marker MUST expire on a timestamp the writer keeps refreshing. If the implementer\n   drops the per-attempt heartbeat write (files[7]) but keeps IN_FLIGHT_TTL_SECONDS, a slow-but-live\n   run gets graded abandoned; if they keep the heartbeat but drop the TTL, a dead run reads live\n   forever. Both halves ship together or neither does.\n\n2. TTL VALUE IS A JUDGEMENT CALL AND I AM NOT HIDING IT. Measured run: ~108s, six attempts\n   (RETRY_DELAYS = (1,2,4,8,16) at ai_boot_readiness.py:18, 31s of sleeps), so ~18s between writes.\n   But ONE attempt can issue five sequential calls each bounded by REQUEST_TIMEOUT = 120.0, so a\n   pathological all-timeouts attempt is silent for up to 600s and WOULD be graded \"abandoned\" at\n   IN_FLIGHT_TTL_SECONDS = 300. That is fail-safe (unknown/stale, never a false terminal verdict)\n   and is the correct direction, but it is a real behaviour and belongs in the CHANGELOG line rather\n   than being discovered on the box. Do NOT import REQUEST_TIMEOUT into ai_boot_status to \"derive\"\n   the constant \u2014 that is a reverse import edge, a cycle, and a re-freeze of the import-graph baseline.\n\n3. STATE_PATH IS RELATIVE \u2014 Path(\"state\")/\"ai_boot_readiness.json\" at ai_boot_status.py:13 \u2014 so it\n   resolves against the CWD of whichever process touches it. The unit sets WorkingDirectory=${APP_DIR}\n   (install_service.sh:268) and the Flask app must be running from the same place for\n   app_ai.py:21 to read what the readiness unit wrote. This is NOT this item, but any hand-rolled\n   probe of the fix must set BD_INSTALL_DIR + chdir per CLAUDE.md section 5, or it will write into\n   the repo and the next probe will read the previous probe's rows.\n\n4. SCENARIO B ONLY BITES BECAUSE OF THE RESTART LOOP. install_service.sh:266-276 is\n   Type=simple / Restart=on-failure / RestartSec=60 / StartLimitIntervalSec=0 \u2014 the unbounded loop\n   already filed as project-knowledge/AUDIT_2026_07_29.md item 24. This fix makes the loop OBSERVABLE\n   (a reader can finally see that a run is in flight); it does not stop it. Do not let a reviewer read\n   the fix as closing audit item 24 \u2014 different defect, adjacent code.\n\n5. NO ONE CAN VERIFY THIS ON THIS BOX'S REAL HARDWARE FROM A CONTAINER. Everything above is measured\n   against ScriptedProbe/tmpdir fakes and the real source; the ~108s convergence with both models\n   resident on the Tesla T4 is SESSION_CARRY's measurement, not mine. Container green is necessary,\n   not sufficient (CLAUDE.md section 7) \u2014 the binding evidence is ./capture.sh on the box.\n\n6. IF A NEW TEST FILE IS CREATED INSTEAD OF APPENDING, the blast radius jumps to all nine-plus axis-6\n   gates plus PIN_INDEX regeneration, and per CLAUDE.md section 2a those gates enumerate `git ls-files`\n   so their pre-merge pass proves NOTHING about an unstaged new file \u2014 the ratchet fires one cut late.\n   Append to the two existing files.\n\n7. NOT VERIFIED BY ME: whether the frontend vitest suite currently passes. I did not run npm/vitest \u2014\n   frontend/node_modules is not committed and I was read-only. If the optional frontend change is\n   taken, establish that baseline first.",
    "size": "M",
    "blocked_on": ""
  },
  {
    "item": "ITEM 6 -- CLAUDE.md section 6 owes a line: an interrupted bd-mutate does NOT restore the tree. Confirm the mechanism in toolchain/bin/bd-mutate, then propose (a) the prose and (b) whether bd-mutate should trap signals.",
    "defect_confirmed": "REAL",
    "evidence": "HEAD verified: `git -C /home/user/BD log --oneline -1` -> `3fded70 Register 15.29: seven cuts, a contaminated corpus, and three corrected figures (#169)`. Matches. Tree clean (`git status --porcelain` empty), so today's two incidents were hand-recovered; no residue live right now.\n\nMECHANISM. `grep -n \"write_text(original|finally|signal|atexit|SIGTERM|KeyboardInterrupt\" toolchain/bin/bd-mutate` ->\n  112:        finally:            <- unrelated (tempfile unlink in _validate)\n  211:        finally:\n  212:            path.write_text(original, encoding=\"utf-8\")\nExactly ONE restore site, inside a per-mutant `finally:`. No `signal`, no `atexit`, no try/except around the loop. A `finally` unwinds on the normal path and on any Python-level exception; a signal whose default disposition is \"terminate\" never becomes one.\n\nPROOF BY EXPERIMENT (not by reading). Driver spawns bd-mutate on a scratch tree, waits until the band's own heartbeat log proves the MUTANT is on disk, then signals. Same instrument for all three:\n  SIGINT  -> bd-mutate exit=-2   post-kill sha 5b76d0962c09ab4e  RESTORED=True   orphaned pytest alive=False\n  SIGTERM -> bd-mutate exit=-15  post-kill sha b7fdeefd2ff2fd36  RESTORED=False  orphaned pytest alive=True\n  SIGKILL -> bd-mutate exit=-9   post-kill sha b7fdeefd2ff2fd36  RESTORED=False  orphaned pytest alive=True\n(pristine sha 5b76d0962c09ab4e; `def f(): return 1` -> left as `return 2`.)\n\nTHE ITEM'S LITERAL TRIGGER REPRODUCES:\n  $ timeout 30 venv/bin/python toolchain/bin/bd-mutate --spec spec.json --band tests/test_m.py --work .\n  timeout exit=124\n  pristine: 5b76d0962c09ab4e   after: b7fdeefd2ff2fd36   ->  def f():\\n    return 2\n\nA FALSE PROBE I HAD TO DISCARD (recorded because it is the section-0 shape). My first SIGINT run reported exit=0/RESTORED=True. It was not the handler working -- the signal never arrived. Backgrounding with `&` from a non-interactive shell sets SIG_IGN for SIGINT: `/proc/<pid>/status` -> `SigIgn: 0000000001001006` (bit 0x2 set), and `kill -INT` left the process alive. Re-run with the signal actually deliverable (`SigIgn: 0000000000000000`) gave exit=-2 and a genuine restore.\n\nCOMPOUNDING DEFECT, ALSO MEASURED. Line 184 `original = path.read_text(...)` reads from the WORKING TREE, never from git, and the restore check (215) compares before/after WITHIN the run -- so it structurally cannot see that \"before\" was already wrong. Scratch tree left at the item's own residue `len(hits) > 99` where `> 1` belonged:\n  ### tree BEFORE the 2nd battery:  2:    if len(hits) > 99:\n  [ESCAPED] threshold 99 -> 1000 ... 0 caught, 1 escaped, 0 invalid, 0 error\n  ### tree AFTER the 2nd battery:  2:    if len(hits) > 99:\nThe second battery adopted the mutant as pristine, ran to a clean sha256-verified restore, and put `> 99` back. Its ESCAPE verdict is precise, well-evidenced, and about code that is not in git.\n\nBASELINE FOR ANY CODE CHANGE: `venv/bin/python toolchain/bin/bd-mutate --selftest` -> `SELFTEST PASS`, exit=0 at 3fded70. Note its case 6 (\"the subject file is restored byte-identically\", :298-300) only ever exercises the NORMAL exit path -- the selftest's own denominator excludes the interrupted case.\n\nCLAUDE.md is NOT ascii-restricted (contains '\u2014', '\u2026', '\u2192'); the ASCII rule is CHANGELOG-only.",
    "files": [
      "/home/user/BD/CLAUDE.md:752 -- insert the new prose AFTER this line (end of the bd-mutate contract paragraph, which is where 'restores by sha256 ... 2 = the battery has no verdict' over-promises) and BEFORE '**Stale bytecode defeats...**' at :754",
      "/home/user/BD/toolchain/bin/bd-mutate:211-218 -- the `finally:` that is the ONLY restore site; skipped on SIGTERM/SIGKILL",
      "/home/user/BD/toolchain/bin/bd-mutate:184 -- `original = path.read_text(...)`: the residue-laundering line; the restore target comes from the tree, not from git",
      "/home/user/BD/toolchain/bin/bd-mutate:148-172 -- `run_battery` preflight; the journal check belongs here, beside the existing empty-spec / red-baseline exit-2 guards",
      "/home/user/BD/toolchain/bin/bd-mutate:244-303 -- `_selftest`; add the RED case here (case 7/8), NOT as a new tests/*.py file",
      "/home/user/BD/toolchain/bin/bd-mutate:306-328 -- `main()`; add `--recover` here as a CLI flag (NOT a BD_ env var, see risks)",
      "/home/user/BD/tests/test_toolchain_534.py:484-504 -- test_the_tools_this_cut_added_are_wired_and_selftest_clean already runs `bd-mutate --selftest` and asserts exit 0 AND 'SELFTEST PASS' (timeout=180); this is what makes a _selftest case a real gate",
      "/home/user/BD/toolchain/bin/bd-claim:41-43 -- precedent: `repo/.git/bd-claims/<pid>.json`, the sidecar location to copy"
    ],
    "red_test": "PLACEMENT: extend `_selftest` in /home/user/BD/toolchain/bin/bd-mutate (:244-303) with case 7. Do NOT add a tests/test_*.py file -- `tests/test_toolchain_534.py:484` already executes `--selftest` and asserts exit 0 plus the literal 'SELFTEST PASS', so a new case is wired into the suite for free, AND adding no test file keeps the nine axis-6 gates and PIN_INDEX's `test_files_scanned` from moving (CLAUDE.md section 4). That is a real reduction in blast radius, not a convenience.\n\nWHAT IT ASSERTS (case 7, \"an interrupted battery is detected, not laundered\"), in a tmpdir like the existing cases:\n  1. Spawn `subprocess.Popen([sys.executable, __file__, \"--spec\", ..., \"--band\", ..., \"--work\", td])`.\n  2. Wait on a HEARTBEAT the band test writes containing the subject file's current text -- poll until it contains the mutant. Do NOT sleep a fixed number of seconds and hope; that is a race, and it is CLAUDE.md 2a's \"cut on structure, never on a fixed width\" in another costume.\n  3. `os.kill(p.pid, signal.SIGTERM)`; `p.wait()`.\n  4. Assert the subject file now holds the MUTANT (this documents the trigger and will keep passing).\n  5. THE RED ASSERTION: a following `run_battery(...)` on that dirty tree must return 2 and name the residued file. Today it returns 0 or 1 with a verdict.\n\nWHY IT FAILS ON PRISTINE 3fded70: there is no journal and no preflight, so step 5 gets a verdict instead of 2 -> `ok` goes False -> `_selftest` prints 'SELFTEST FAIL' and returns 1 -> `tests/test_toolchain_534.py::test_the_tools_this_cut_added_are_wired_and_selftest_clean` goes RED on both its assertions (exit != 0, and 'SELFTEST PASS' absent). Proven by measurement above: on this tree the dirty-tree run returned exit 1 with `0 caught, 1 escaped` and said nothing about the residue.\n\nHARNESS DISCIPLINE (CLAUDE.md 2a): discriminate the failure. Assert the exit-2 message names the residued PATH, not merely that rc==2 -- an empty spec and a red baseline also return 2, so a bare `rc == 2` would pass on a tree where the fix does not exist. Keep the band test's sleep to ~3-5s so the whole selftest stays far under test_toolchain_534's `timeout=180`.",
    "fix": "TWO PARTS. Part (a) ships regardless; part (b) is the argued answer to the item's question.\n\n=== (a) THE CLAUDE.md PROSE (insert after :752) ===\n\n**An interrupted battery does NOT restore the tree.** The restore is a\n`finally:` in the per-mutant loop, so it runs on the normal path and on any\nPython-level exception -- and a signal whose default disposition is to\nterminate the process never becomes one. Measured at 3fded70 by killing a\nbattery mid-band: SIGINT unwinds through `KeyboardInterrupt` and the file comes\nback byte-identical, but **SIGTERM (shell exit 143) and SIGKILL both leave the\nmutant on disk**, with the band's `pytest` orphaned and still running against\nit. `timeout 30 bd-mutate ...` reproduces it exactly: exit 124, mutant still\nthere. So every unattended way a battery dies -- an agent's command timeout,\n`timeout(1)`, an orchestrator reaping a slow job -- is a way it dies dirty, and\nCtrl-C, the one case the `finally` covers, is the case that does not happen\nwhen nobody is watching. Nor is Ctrl-C a reliable way to test this: a bd-mutate\nbackgrounded with `&` from a non-interactive shell inherits SIGINT as SIG_IGN\n(`SigIgn` bit `0x2` in `/proc/<pid>/status`), so `kill -INT` does nothing at\nall and the battery runs to completion looking like a clean restore.\n\n**A second run then launders the residue into the baseline.** `bd-mutate` reads\nthe \"original\" it will restore from the WORKING TREE, never from git, and its\nrestore check compares that same text back -- so a battery started on a dirty\ntree adopts the mutant as pristine and puts it back, reporting a clean\nsha256-verified restore. Measured: a tree left at `len(hits) > 99` where `> 1`\nbelonged ran a full battery, scored an ESCAPE, and restored `> 99`. The verdict\nwas precise, well-evidenced, and about code that is not in git. A before/after\ncheck cannot see that \"before\" was already wrong -- section 0, inside the tool\nthis section tells you to trust.\n\n**So: if a battery did not print its summary, `git status` before you believe\nanything, and before you re-run.** `git diff -- <the files in your spec>` is\nthe check and `git checkout --` is the repair; the second run is what makes the\nresidue permanent. And read \"restores by sha256\" above for what it is -- a\nwithin-run consistency check, not a promise about a tree you did not watch it\nleave.\n\n=== (b) SHOULD bd-mutate TRAP SIGNALS? THE TRADEOFF ===\n\nShort answer: the dichotomy in the item is false. Trapping IS unreliable, but\n\"therefore prose\" is not the honest conclusion -- an ON-DISK JOURNAL is reliable\nin exactly the way a handler is not. Ship prose now, journal next, trap last.\n\nFOR a SIGTERM trap: it is cheap (`signal.signal(SIGTERM, ...)` restoring and\nre-raising), and it covers the empirically dominant trigger -- measured today\nas `timeout(1)`/agent command timeouts, both SIGTERM. It could also reap the\norphaned pytest, which currently survives the parent (measured alive=True) and\nkeeps executing against the mutated tree.\n\nAGAINST relying on it, decisively: it cannot cover SIGKILL, the OOM killer,\ncontainer teardown, or a hard crash -- measured, SIGKILL leaves the mutant and\nno code can change that. There is also an irreducible window between\n`write_text(mutated)` and the handler completing. And the killer argument is\nCLAUDE.md's own: A PARTIAL TRAP REPRODUCES THE SHAPE OF THE DEFECT. It converts\na reliable failure (every kill leaves residue) into a rare one (only\nSIGKILL/OOM does), and this residue is SILENT -- the next run adopts it as\npristine. The tool would then look crash-safe while retaining a class it can\nneither see nor report. That is precisely \"a gate that cannot see the thing it\nis asked about reports OK\", installed as the fix for it.\n\nTHE HONEST FIX -- make the unsafe state observable, and make the next run say so:\n  1. Before the first `write_text`, write a journal sidecar recording path,\n     original sha256, the original BYTES, pid and timestamp. Delete it only\n     after the restore verifies. Put it at `<git-dir>/bd-mutate-inflight/<pid>.json`,\n     copying bd-claim (`toolchain/bin/bd-claim:41-43` uses `repo/.git/bd-claims/<pid>.json`):\n     inside `.git/` it is invisible to `git status`, to `git clean -fdx`, and to\n     every axis-6 enumerator, so it needs no .gitignore edit and contaminates\n     no other gate. Resolve the dir with `git rev-parse --git-dir`, not\n     `repo/\".git\"` -- in a worktree `.git` is a FILE and the literal path fails\n     (CLAUDE.md 2b puts agents in worktrees routinely).\n  2. Preflight in `run_battery` (:148-172, beside the existing exit-2 guards) --\n     RESOLVE the journal, never assume:\n       - file sha == recorded ORIGINAL  -> tree already clean (hand-repaired or\n         git-reset). REAP the journal and proceed. This is the anti-wedge branch.\n       - file sha == recorded MUTATED   -> residue confirmed. Exit 2\n         (BD-MUTATE-UNRUNNABLE), name the file, point at `--recover`.\n       - neither                        -> UNKNOWN, and unknown FAILS: exit 2,\n         refuse to guess, tell the operator to diff.\n  3. `--recover` writes the stored original back and re-verifies by sha.\nThis survives SIGKILL and OOM because it is on disk rather than in a handler --\nthe property no trap can have. Exit 2 already means \"the battery has no\nverdict\" (:49-52), so this reuses the tool's existing vocabulary rather than\ninventing one.\n\nRANKING. (1) Prose -- mandatory, ships regardless; it is the only mitigation\nthat needs no code, covers SIGKILL, helps a tree an operator already has, and\nit corrects a standing over-promise (\"restores by sha256\"). (2) Journal +\npreflight + `--recover` -- the real fix, and the only version that can SAY SO\nwhen it cannot verify. (3) SIGTERM trap plus child reap -- worth having, but\nonly AFTER (2) and never INSTEAD of it. If only one thing lands this cut, land\nthe prose: doc-only, tiny blast radius, and honest while (2) is designed. Do\nnot let it become the excuse to skip (2).",
    "band": [
      "DERIVED -- `venv/bin/python toolchain/bin/bd-band-derive --file toolchain/bin/bd-mutate` -> changed source (0), changed/unclassified (1), band (3):",
      "tests/test_contracts.py",
      "tests/test_gui_parity.py",
      "tests/test_toolchain_534.py",
      "DERIVED -- `bd-band-derive --file CLAUDE.md` -> band (67). Treat as a floor with a very low signal ratio: most of the 67 only cite CLAUDE.md in a docstring. The four that actually READ it from disk are the real members:",
      "tests/test_codex_handoff_stays_retired.py  (:98 reads CLAUDE.md; also an axis-6 `git ls-files -- *.py *.sh` gate)",
      "tests/test_generated_artifact_workflow.py  (:304 reads CLAUDE.md as `policy`)",
      "tests/test_provision_test_host.py  (:86 CLAUDE_MD = REPO_ROOT / 'CLAUDE.md')",
      "tests/test_cloud_setup_truthfulness.py  (compares CLAUDE.md's env table against cloud-setup.sh)",
      "CROSS-CHECK ADDITIONS (grep -rln, not instead of the tool):",
      "toolchain/bin/bd-freshcheck -- run it, do not just band it. CLAUDE.md is in its `_DOCS` (:50) and it validates every `file.ext:NNN` anchor in the doc.",
      "MEASURED CAVEAT: its anchor regex requires an extension, so `toolchain/bin/bd-mutate:212` is NOT anchor-checked (verified: regex match False) while `capture.sh:55` is. Prefer citing the MECHANISM over the line number in the prose -- an unvalidated anchor goes stale silently.",
      "tests/test_toolchain_534.py::test_the_tools_this_cut_added_are_wired_and_selftest_clean -- the gate that executes `--selftest`; MUST be run if bd-mutate changes.",
      "tests/test_toolchain_534.py::test_the_toolchain_does_not_grow_unbudgeted -- only moves if a NEW bd-* tool is added. Keep the fix inside bd-mutate and it does not.",
      "AXIS-6 / PIN_INDEX: if the RED case goes in `_selftest` (recommended), NO test file is added, so the nine axis-6 gates and PIN_INDEX's `test_files_scanned` do NOT move. If the executor adds a tests/test_*.py instead, they inherit all nine plus a PIN_INDEX regen (CLAUDE.md section 4).",
      "GUARDS: neither CLAUDE.md nor toolchain/bin/bd-mutate is SHA-pinned. Run `bd-guardcheck` anyway; do NOT touch tools/build_release.py, which IS pinned and is the parity-inventory generator.",
      "REGEN: `venv/bin/python toolchain/bin/bd-regen-order --work \"$PWD\"` AFTER the last source edit, then `git status --porcelain` (CI's check)."
    ],
    "over_sensitivity_test": "The fix's failure mode in the other direction is a tool that refuses to run, or calls every battery unrunnable -- which destroys bd-mutate exactly as CLAUDE.md section 6 warns (\"a fix for 'reports clean when blind' that simply calls every scan inconclusive passes the escape's test and destroys the tool\"). Assert all three in the SAME cut, as `_selftest` case 8:\n\n1. CLEAN-RUN CONTROL: after a battery that completes normally, NO journal remains and an immediately following battery returns a real verdict (0 or 1), NOT 2. Without this, the journal wedges every subsequent run and the tool is dead.\n2. REAP CONTROL (the important one): plant a journal whose recorded file currently matches the recorded ORIGINAL sha -- i.e. the operator already ran `git checkout --`, or a `git reset --hard` landed. The preflight must REAP it and proceed to a verdict, not refuse. Precedent and rationale: CLAUDE.md 2b, bd-claim \"is inert unless a claim is in flight ... and a dead claimant is reaped rather than wedging the repo\". A residue check that refuses on the mere PRESENCE of a journal is the cry-wolf gate that gets switched off.\n3. UNKNOWN IS NOT \"DIRTY\": plant a journal whose file matches NEITHER recorded sha and assert the exit-2 message says it cannot tell and asks for a diff -- rather than silently `--recover`ing and overwriting an edit the operator made deliberately. Unknown fails, but it fails by SAYING SO, not by acting.\n\nAlso assert `--recover` is not a blind overwrite: it must verify by sha after writing, and refuse if the file matches neither recorded state.",
    "risks": "1. SELF-AUDIT, per CLAUDE.md's \"THE FIX REPRODUCES THE SHAPE OF THE DEFECT\". The journal is itself a check that can go blind: if writing it fails (read-only .git, missing dir, worktree where `.git` is a FILE), the battery must NOT proceed unjournalled and silently. Failing to create the journal is exit 2, not a warning. Otherwise the fix ships the exact defect it closes -- a safety net that reports OK while absent.\n2. DO NOT introduce a `BD_`-prefixed env var (e.g. BD_MUTATE_ALLOW_RESIDUE). CLAUDE.md section 4: any BD_ name -- \"including a shell local\" -- enters test_gui_parity's ledger denominator and reads as \"promoted but unledgered\"; v3.66.836 failed the parity gate on the box for exactly this. Use the `--recover` CLI flag. UNVERIFIED: I did not fully pin the parity scan's denominator (its generator, tools/build_release.py, is a SHA-pinned guard file I did not modify or trace end-to-end), so this is a conservative avoidance, not a measured exemption. If a BD_ name proves unavoidable, band tests/test_gui_parity.py and ledger it.\n3. Journal location is the one place to get right. Anywhere in the worktree makes it an untracked file, and CLAUDE.md 2a records untracked files contaminating a regen (three RED files inflated `test_files_scanned` by 3); test_generated_artifacts_are_not_tracked.py enumerates `git ls-files -z`. Inside `<git-dir>/` it is invisible to all of them. Resolve via `git rev-parse --git-dir`.\n4. The orphaned pytest (measured alive=True after SIGTERM/SIGKILL) is a live second writer. If a SIGTERM trap is ever added, it must reap the child BEFORE restoring, or the restore races a process still importing the file. This is also why `--recover` should re-verify by sha after writing.\n5. Prose-only leaves the compounding laundering defect open. That is acceptable for one cut but should be recorded as an open item, not treated as closed by the prose.\n6. bd-freshcheck must be run after the CLAUDE.md edit; it gates on anchors in that file. My measurement says an extensionless `bd-mutate:NNN` citation is NOT validated by it -- so do not rely on the gate to keep such a citation honest.\n7. Scope/authorization: the prose is a doc change. Changing toolchain/bin/bd-mutate is toolchain behaviour and CLAUDE.md section 9 reserves runtime/build/guard/release changes for explicit per-task authorization. Confirm with Matt before landing part (b).\n8. All measurements above are on commit 3fded70 in this container. The container reportedly reverted three times today; re-verify HEAD before executing.",
    "size": "M",
    "blocked_on": "Not blocked for part (a), the CLAUDE.md prose -- it is ready to cut as written, with the caveat that a doc-only change has no natural RED test and its evidence is the measurement pasted above. Part (b), the journal + preflight + --recover change to toolchain/bin/bd-mutate, needs one operator decision before implementation: whether a toolchain behaviour change is authorized this cut (CLAUDE.md section 9). If both land together the RED case belongs in _selftest and the cut stays S/M; if only the prose lands, record the laundering defect (bd-mutate:184) as an open item rather than letting the prose read as a closure."
  },
  {
    "item": "ITEM 7 \u2014 \"the 12-tool retirement, ~20 references across 7 surviving tools\" (SESSION_CARRY.md:3727, filed as \"Item 3\" in register 15.29)",
    "defect_confirmed": "PARTIALLY-REAL",
    "evidence": "HEAD VERIFIED \u2014 matches the prompt.\n  $ git log --oneline -1\n  3fded70 Register 15.29: seven cuts, a contaminated corpus, and three corrected figures (#169)\n  $ git branch --show-current -> main ; git status --porcelain -> (empty, and still empty at end of session)\n\n=== REAL: the budget is at the ceiling with zero headroom ===\n  $ ls toolchain/bin/ | grep -c '^bd-'          -> 239\n  $ git ls-files toolchain/bin/ | grep -c 'bd-' -> 239\n  tests/test_toolchain_534.py:1036  _TOOL_BUDGET = 239   ->  239 <= 239, zero headroom. Adding any tool fails the gate.\n  $ venv/bin/python -m pytest tests/test_toolchain_534.py -k \"toolchain_does_not_grow or unwired_bd_tools\" -q\n  2 passed, 21 deselected in 19.04s      (exit=0)\n\n=== NOT-REAL AS WRITTEN #1: the twelve are enumerated NOWHERE in the tree ===\n\"12-tool\" appears exactly once in the whole repo, in the item itself:\n  $ git grep -n \"12-tool\" $(git rev-list --all --max-count=200) -- '*.md'\n  3fded70...:project-knowledge/SESSION_CARRY.md:3727:  Item 3    the 12-tool retirement, ...\n  (the only other hits are an unrelated \"112-tool population\" in BDSUITE_CHANGELOG.md:208)\nThe nearest thing to a list is the tombstone, and it names the REFERRERS, not the retirees:\n  project-knowledge/BD_TOOLCHAIN_REFERENCE.md:50-57 / CHANGELOG.md:295-301 (v3.66.858)\n  \"Twenty were proposed... The other twelve each require an edit in a tool that lives on --\n   bd-sweep (8 refs), bd-coretest (4), bd-tool-lint (3), and single references in bd-freshcheck,\n   bd-guardcheck, bd-cut and bd-tool-smoke\"\nThe 20-tool retirement plan is an UNTRACKED session artifact. It is not in project-knowledge/,\ndocs/, pending-specs/, or any commit reachable from main. There is no `gh` binary in this\ncontainer to read PR #159's body. The item CANNOT be executed as written.\n\n=== NOT-REAL AS WRITTEN #2: \"~20 references\" is 19 TOOL-SLOTS, not 20 references ===\n8+4+3+1+1+1+1 = 19, and it is a DISTINCT-TOOL count per referrer, not an occurrence count.\nMeasured raw name-exact occurrences (using the repo's own _name_re from test_toolchain_534.py)\ninside those 7 files: bd-sweep alone names 45 distinct surviving tools, bd-tool-lint 33.\nSaying \"20 references\" over-states precision and under-states the edit surface.\n\n=== NOT-REAL AS WRITTEN #3: the prose-only figure in the prompt is the BASELINE, not the live count ===\n  $ venv/bin/python /tmp/it7_measure.py\n  TOOLS: 239\n  PROSE_ONLY: 177\n  BASELINE: 184 BUDGET: 239\n  WIRED: 62\n_PROSE_ONLY_BASELINE = 184 was pinned at v3.66.856 when the tree held 246 tools\n(git log -S: 166 -> 180 @849 -> 184 @856). v3.66.858 retired 7 tools, all prose-only, so live\nfell 184 -> 177 and the constant was never re-pinned. THE RATCHET ALREADY CARRIES 7 SLACK.\nThe assertion is `len(prose) <= _PROSE_ONLY_BASELINE` (:330), a ceiling \u2014 so a retirement does\nNOT force this number to change. Register 15.29's \"184 of 239, measured\" is wrong; it quotes\nthe constant, not a measurement. That line was itself filed as a CORRECTION of an earlier \"166\".\n\n=== THE REAL DEFECT, FOUND WHILE VERIFYING, AND IT IS BIGGER THAN THE ITEM ===\nBoth blocking referrers go BLIND when their subject is deleted \u2014 CLAUDE.md section 0, exactly.\n\n(a) bd-coretest CERTIFIES ABSENT TOOLS AS PASSING FAULT-INJECTION.\n  $ cd /tmp && venv/bin/python toolchain/bin/bd-coretest --bin /tmp/it7_emptybin --only pack\n  exit=0\n  bd-pack  PASS\n    [ok][FAULT   ] FAULT->refuse          refused an incomplete pack (rc=2)\n  SELFTEST PASS\nMechanism: run() at bd-coretest:22-30 does subprocess.run([sys.executable, exe, ...]); a missing\nexe makes python exit rc=2, and test_pack's predicate is `(rc != 0 or \"FAIL\" in out or ...)`\n(:151) \u2014 so \"the file does not exist\" satisfies \"the tool correctly refused\".\nFull sweep with EVERY tool absent:\n  $ cd /tmp && venv/bin/python toolchain/bin/bd-coretest --bin /tmp/it7_emptybin --no-cache\n  CORE TOOLS PASSING: 6/13   (assertions 10 pass / 10 miss)\n  PASS over an empty directory: bd-checkpoint, bd-cut, bd-factcheck, bd-handoff, bd-pack, bd-zipcheck\nFour of those six (bd-checkpoint, bd-handoff, bd-pack, bd-zipcheck) are precisely the tools\nbd-coretest's 4 refs point at. Over-sensitivity control on the real bin:\n  $ venv/bin/python toolchain/bin/bd-coretest --no-cache\n  exit=0 ; CORE TOOLS PASSING: 13/13 (assertions 22 pass / 0 miss) ; SELFTEST PASS\n\n(b) bd-tool-lint's CRITICAL_CORE SILENTLY SHRINKS ITS DENOMINATOR.\n  toolchain/bin/bd-tool-lint:46-49 lists 10 tools; :403 `cc_present = [t for t in CRITICAL_CORE if t in tset]`\n  $ venv/bin/python toolchain/bin/bd-tool-lint --bin toolchain/bin --json  -> exit=0\n  \"critical_core\": {\"baseline\": 0, \"present\": 7, \"regressed\": false, \"untested\": [], \"untested_count\": 0}\n7 of 10, because bd-blast, bd-suites and bd-touched were retired at rev-702 and NOTHING SAYS SO.\n  $ for t in bd-touched bd-suites bd-blast; do [ -e toolchain/bin/$t ] || echo \"ABSENT $t\"; done\n  ABSENT bd-touched / ABSENT bd-suites / ABSENT bd-blast\nThe ratchet is CRITICAL_CORE_UNTESTED_BASELINE = 0 (:52), \"floors at zero\". Retiring bd-pack,\nbd-handoff and bd-checkpoint would take present 7 -> 4 with regressed:false and exit 0.\n\n(c) THIS HAS ALREADY HAPPENED ONCE, and the repo documents it at tests/test_toolchain_534.py:505-519:\n  \"bd-pack:169  undefined name 'TRACKER_FILES' ... deleted at v3.66.841 with the TASK_TRACKER\n   retirement and that use site was left behind ... the dangling name sat outside its denominator.\n   bd-pack --selftest exits 0 because it drives another path.\"\nA prior retirement left a live NameError in a surviving tool and its own gate could not see it.\n\n=== WHAT EACH REFERRER ACTUALLY DOES ON DISAPPEARANCE (the question the item asks) ===\n  bd-sweep (8, the HIGHEST count) -> HARMLESS. :69-95 NEVER_EXEC is a frozenset safety\n      denylist; discover() only yields tools that exist, so a dead entry is inert.\n      The tombstone's ranking by reference count is backwards.\n  bd-coretest (4)   -> WORST. Silent false-PASS, proven above. Cases at :141,:147,:156,:237,:289\n      plus CORE_TOOLS :323-327 (feeds the sha-keyed cache).\n  bd-tool-lint (3)  -> SILENT DENOMINATOR LOSS, proven above.\n  bd-cut (1)        -> :239 comment, :274 an operator MESSAGE string (\"bd-mkauditstate ship it.\n      UNKNOWN, not OK.\"). No invocation. Text-only, but it instructs the operator to run a tool\n      that would no longer exist.\n  bd-tool-smoke (1) -> :5,:34,:399 docstring/help text only. Inert.\n  bd-freshcheck (1) -> :22 docstring only. Inert.\n  bd-guardcheck (1) -> :47-49 historical-narrative docstring only. Inert.\nSo the true blocking coupling is TWO tools (bd-coretest, bd-tool-lint), not seven.\n\n=== RECONSTRUCTED CANDIDATE SET (high confidence, but NOT a tracked list) ===\nDerived by fitting the tombstone's per-referrer distinct-tool counts against measured references\nand the stated subject-death criterion. bd-tool-lint's \"3\" fits CRITICAL_CORE exactly:\n  bd-tool-lint 3 = bd-pack, bd-handoff, bd-checkpoint      (the only CRITICAL_CORE zip-era members)\n  bd-coretest  4 = those three + bd-zipcheck               (the only other zip-era case, :237)\n  bd-cut       1 = bd-mkauditstate ; bd-tool-smoke 1 = bd-mkbdsuite\n  bd-sweep     8 = 8 of the 12 that sit in NEVER_EXEC :70-73\nCorroboration: v3.66.857 (\"salvage before the retirement\") names bd-repin-dist, bd-pack and\nbd-mkbdsuite as members of the 20. Remaining NEVER_EXEC zip-era names: bd-optpack, bd-prestage,\nbd-ship, bd-snapshot. bd-freshcheck's 1 is most likely bd-freshest (\"flags ... stray zips\").\nbd-state stays HELD (tools/build_session_pack.py:128 genuinely invokes it \u2014 confirmed: _invokes\nreturns tests/test_toolchain_534.py and tools/build_session_pack.py).\nI could not close bd-guardcheck's single ref (bd-since / bd-evidence / bd-agent-scorecard).\n=> 11 of 12 identified; the set is a RECONSTRUCTION and MUST be re-derived, not inherited.\n\nFootprint per tool (from the v3.66.858 precedent, git show --stat cb27691): 4 surfaces \u2014\ntoolchain/bin/<t>, project-knowledge/<t> mirror, project-knowledge/bd-tools registry row,\nproject-knowledge/STATIC_KB_MANIFEST.json entry. Verified present for all 13 candidates.\n\n=== ONE MORE MEASURED TRAP ===\nbd-pack and bd-handoff read as WIRED, but only off an echo banner:\n  $ venv/bin/python /tmp/it7_who.py\n  executable-context denominator: 2192\n  bd-pack     invoked by: ['toolchain/install_bdsuite.sh']\n  bd-handoff  invoked by: ['toolchain/install_bdsuite.sh']\n  bd-checkpoint / bd-zipcheck  invoked by: NOTHING (prose-only)\n  toolchain/install_bdsuite.sh:17 is `... && echo \"  ready: bd-boot, bd-cut, bd-handoff, bd-pack, ...\"`\nAlready documented at tests/test_toolchain_534.py:144-149 as KNOWN REMAINING IMPRECISION.\nNote _prose_only EXCLUDES toolchain/bin/ from its denominator (:302), so tool->tool references\nnever count as wiring \u2014 deliberate, but it means the prose-only metric is silent about exactly\nthe coupling this item is about.",
    "files": [
      "tests/test_toolchain_534.py:1036 \u2014 _TOOL_BUDGET = 239, lower to 239 minus N retired",
      "tests/test_toolchain_534.py:150 \u2014 _PROSE_ONLY_BASELINE = 184, re-pin to the live value (177 today) to close the 7 slack v3.66.858 left",
      "tests/test_toolchain_534.py:144-149 \u2014 the install_bdsuite.sh echo-banner imprecision note; update if bd-pack/bd-handoff go",
      "toolchain/bin/bd-coretest:22-30 \u2014 run(); a missing exe returns rc=2 and reads as a correct refusal",
      "toolchain/bin/bd-coretest:141-144 \u2014 test_checkpoint (PASSes when absent)",
      "toolchain/bin/bd-coretest:147-153 \u2014 test_pack (PASSes when absent)",
      "toolchain/bin/bd-coretest:156-161 \u2014 test_handoff (PASSes when absent)",
      "toolchain/bin/bd-coretest:237-253 \u2014 test_zipcheck (PASSes when absent)",
      "toolchain/bin/bd-coretest:323-327 \u2014 CORE_TOOLS list, feeds the sha-keyed cache at :360-372",
      "toolchain/bin/bd-tool-lint:46-49 \u2014 CRITICAL_CORE, 3 of 10 entries already dead",
      "toolchain/bin/bd-tool-lint:52 \u2014 CRITICAL_CORE_UNTESTED_BASELINE = 0",
      "toolchain/bin/bd-tool-lint:403-406 \u2014 cc_present filters CRITICAL_CORE to what exists; the blindness site",
      "toolchain/bin/bd-tool-lint:435-436 \u2014 the critical_core JSON block; add a `missing` field here",
      "toolchain/bin/bd-sweep:69-95 \u2014 NEVER_EXEC frozenset (inert dead entries; cosmetic only)",
      "toolchain/bin/bd-cut:239,274 \u2014 bd-mkauditstate comment and operator message",
      "project-knowledge/BD_TOOLCHAIN_REFERENCE.md:36-63 \u2014 the v3.66.858 tombstone; extend, and NAME the twelve this time",
      "project-knowledge/SESSION_CARRY.md:3727 \u2014 the item text to correct",
      "project-knowledge/bd-tools \u2014 registry rows (1-3 lines per tool)",
      "project-knowledge/STATIC_KB_MANIFEST.json \u2014 one entry per tool",
      "project-knowledge/<tool> \u2014 the PK mirror, deleted alongside toolchain/bin/<tool>"
    ],
    "red_test": "Both RED tests below were WRITTEN AND RUN against pristine HEAD 3fded70 (from /tmp, repo untouched) and BOTH FAILED. Real output:\n\n  $ cd /tmp && /home/user/BD/venv/bin/python -m pytest /tmp/it7_red_demo.py -q\n  FAILED it7_red_demo.py::test_coretest_does_not_certify_an_absent_tool\n  FAILED it7_red_demo.py::test_tool_lint_reports_critical_core_entries_it_cannot_see\n  2 failed in 6.56s\n\nTEST 1 \u2014 goes in tests/test_toolchain_534.py (EDIT an existing file, do not add a new one; a new\ntest file moves ten axis-6 gates plus PIN_INDEX for no benefit).\n  def test_coretest_does_not_certify_a_tool_that_is_not_there():\n      run bd-coretest --bin <tempfile.TemporaryDirectory()> --only pack\n      assert \"bd-pack  PASS\" not in out\n      assert returncode != 0\n  WHY IT FAILS ON PRISTINE: proven above \u2014 the run prints \"bd-pack  PASS\",\n  \"[ok][FAULT] FAULT->refuse ... (rc=2)\" and \"SELFTEST PASS\" at exit 0, because rc=2 from\n  \"python: can't open file\" satisfies test_pack's `rc != 0` predicate at bd-coretest:151.\n  Pristine failure message captured verbatim:\n      AssertionError: bd-coretest certified an ABSENT tool as PASS: ... bd-pack  PASS\n\nTEST 2 \u2014 same file.\n  def test_tool_lint_says_which_critical_core_entries_it_cannot_see():\n      cc = json.loads(bd-tool-lint --bin toolchain/bin --json)[\"critical_core\"]\n      assert \"missing\" in cc\n      assert sorted(cc[\"missing\"]) == [\"bd-blast\", \"bd-suites\", \"bd-touched\"]\n  WHY IT FAILS ON PRISTINE: the key does not exist. Verbatim:\n      AssertionError: critical_core has no `missing` key -- a retired entry vanishes from the\n      denominator silently: {'baseline': 0, 'present': 7, 'regressed': False, 'untested': [],\n      'untested_count': 0}\n  This test is RED for a defect ALREADY IN THE TREE (three dead entries), so it is honest RED\n  and not RED-by-construction against a hypothetical.\n\nTEST 3 (for the retirement cut itself, once the gates are fixed) \u2014 assert every name in\nbd-tool-lint's CRITICAL_CORE and bd-coretest's CORE_TOOLS resolves to a file in toolchain/bin,\nor is listed in an explicit RETIRED tombstone constant. RED today: 3 CRITICAL_CORE members and\n0 CORE_TOOLS members dangle. This is the gate that makes cut 2 unable to repeat the\nTRACKER_FILES failure.",
    "fix": "DO NOT START WITH THE RETIREMENT. The item as filed would detonate two blind gates and repeat\nthe v3.66.841 TRACKER_FILES failure at four times the scale. Three cuts, in this order.\n\nCUT 1 \u2014 \"a retired tool's gate must not certify its absence\" (the prerequisite; no tool retired).\n (a) bd-coretest: add at the top of run(), or in each case, an explicit existence probe.\n     `if not os.path.isfile(exe): rec(tool, case, False, \"tool ABSENT from --bin\", kind=\"UNKNOWN\")`\n     and return a sentinel rc that no predicate can read as success (e.g. 127 with a distinguishing\n     marker). Per CLAUDE.md section 0, ABSENT is a THIRD STATE and it FAILS \u2014 it must not be\n     silently folded into either PASS or a normal FAIL.\n     Discriminate the way CLAUDE.md section 2a demands: match on the missing-file condition\n     specifically (os.path.isfile), never on rc==2, because a real tool can legitimately exit 2.\n (b) bd-coretest: make CORE_TOOLS (:323-327) self-checking \u2014 every name must resolve, or the\n     harness reports which do not. Today :360-362 already filters with `if os.path.isfile(...)`\n     when building the cache key, so a deleted tool silently changes the cache key's input set.\n (c) bd-tool-lint: at :403 keep cc_present but ALSO compute\n     `cc_missing = [t for t in CRITICAL_CORE if t not in tset]`, emit it in the :435 JSON block as\n     \"missing\", print it on the always-visible ratchet line (:606), and require it to be empty\n     unless the name appears in an explicit RETIRED_CRITICAL_CORE constant carrying a version.\n     Retiring a tool then costs one deliberate edit that is visible in review, which is the point.\n (d) Seed RETIRED_CRITICAL_CORE with the three that are already dead (bd-blast, bd-suites,\n     bd-touched, all rev-702) so the tree goes green honestly rather than by widening a filter.\n\nCUT 2 \u2014 \"retirement tier 2\", and ONLY after cut 1 is on main.\n (a) RE-DERIVE the candidate set. Do not inherit my reconstruction and do not inherit the\n     untracked plan. Method, all three signals, and say which:\n       venv/bin/python toolchain/bin/bd-consumer-graph --dead-tools\n       the _prose_only() set from tests/test_toolchain_534.py (177 today \u2014 measure, do not quote)\n       the subject-death argument stated per tool (zip retired / uploads mount gone / snapshots are\n       branches / scripts/deploy.sh replaced the overlay), each with a NAMED replacement\n     bd-equiv CANNOT license these (@852/@855 grade them CANNOT-EVALUATE because their corpora are\n     dead) \u2014 state that plainly, as v3.66.858 did, rather than implying a mechanical proof.\n (b) Per tool deleted, four surfaces: toolchain/bin/<t>, project-knowledge/<t>,\n     the project-knowledge/bd-tools registry row, the STATIC_KB_MANIFEST.json entry.\n (c) Delete the corresponding bd-coretest case AND its CORE_TOOLS entry in the same commit; move\n     the CRITICAL_CORE entry into RETIRED_CRITICAL_CORE. Cut 1's gate now FAILS if you forget.\n (d) Run bd-tool-smoke over the whole bin afterwards \u2014 it is the only static name-resolution check\n     that can see the 239 extensionless scripts (tests/test_toolchain_534.py:506-512) and it is\n     what would have caught TRACKER_FILES.\n (e) Rewrite bd-cut:274's operator message so it does not instruct running a retired tool.\n (f) NEVER_EXEC entries in bd-sweep: leave or prune, either is safe. Prefer pruning for hygiene,\n     but do not treat it as blocking \u2014 it is a denylist, and a dead entry on a denylist is inert.\n\nBASELINES \u2014 exactly which move, and to what (the prompt's premise is half right):\n  _TOOL_BUDGET (tests/test_toolchain_534.py:1036): 239 -> 239 - N. REQUIRED. The `<=` would still\n     pass if left, but the constant's own comment (:1031-1035) says leaving it \"would silently\n     re-permit the growth the retirement just paid for\", and v3.66.858 set the precedent (246->239).\n  _PROSE_ONLY_BASELINE (:150): 184 -> the LIVE value after the cut. NOT forced by the assertion\n     (it is a `<=` ceiling and live is already 7 under), but it should be re-pinned because\n     v3.66.858 left 7 slack, and a ratchet with slack will not fire. If the reconstructed 12 all go,\n     10 of them are prose-only, so live falls 177 -> 167 and _TOOL_BUDGET falls 239 -> 227.\n     MEASURE both at cut time; do not use my numbers.\n  If the version bumps: bulk_downloader/__init__.py:33 (3.66.867), the pin at\n  tests/test_settings_center_slice4.py:200, CHANGELOG.md (ASCII-only), then\n  venv/bin/python tools/build_pin_index.py and bd-regen-order AFTER the last source edit.\n\nCUT 3 \u2014 register hygiene. Correct SESSION_CARRY.md:3727 to name the twelve explicitly and to say\n19 tool-slots across 7 files of which only 2 block; correct 15.29's \"184 of 239\" to distinguish\nthe constant from the live 177. Extend the BD_TOOLCHAIN_REFERENCE.md tombstone to NAME the\nretirees, which is the fix for the root cause here: the reason this item cost a full recon pass\nis that v3.66.858 recorded \"twelve stay queued\" without writing down which twelve.",
    "band": [
      "tests/test_bd_ready_preflight.py",
      "tests/test_bd_regen_check_is_read_only.py",
      "tests/test_contracts.py",
      "tests/test_toolchain_534.py",
      "tests/test_v3_66_305_config_danger.py",
      "tests/test_v3_66_319_env_tranche_4_3d.py",
      "tests/test_v3_66_702_jd_supported_hosts_field.py",
      "tests/test_v3_66_711_automation_gui.py",
      "tests/test_v3_66_713_unscanned_surfaces.py",
      "tests/test_v3_66_716_decoys_and_shadow.py",
      "tests/test_v3_66_720_oidc_and_ffmpeg.py",
      "tests/test_v3_66_820_share_tools_saw_no_session_keys.py",
      "tests/test_pk_mirrors_do_not_drift.py",
      "tests/test_task_tracker_stays_retired.py",
      "tests/test_pin_index_in_sync.py",
      "tests/test_settings_center_slice4.py",
      "tests/test_versync_gate.py",
      "tests/test_release_hygiene_gates.py",
      "tests/test_bd_doctor_probes_the_real_environment.py",
      "tests/test_generated_artifact_workflow.py"
    ],
    "risks": "BAND PROVENANCE. The first 12 entries are verbatim bd-band-derive output at 3fded70:\n  $ venv/bin/python toolchain/bin/bd-band-derive --files toolchain/bin/bd-coretest toolchain/bin/bd-tool-lint tests/test_toolchain_534.py\n  changed source (1): tests/test_toolchain_534.py\n  changed, unclassified (2): toolchain/bin/bd-coretest, toolchain/bin/bd-tool-lint\n  band (12): [the 12 above]\n  floor, not ceiling\nNOTE THE WARNING IN ITS OWN OUTPUT: it classified BOTH bd-* tools as \"unclassified\". Per CLAUDE.md\nsection 1, 469 tracked extensionless bd-* scripts are invisible to a *.py glob, so the derived band\nfor a toolchain/bin edit is weaker than for a .py edit. Entries 13-20 are my cross-check additions\n(grep -rln over the candidate names + the PK-mirror, version-pin and registry surfaces the\nv3.66.858 precedent actually touched). This is a FLOOR.\n\nAXIS-6, and why cut 1 should EDIT tests/test_toolchain_534.py rather than add a file: adding any\ntest file moves ten gates (test_source_windows_do_not_shift, test_pytest_runner_boundary,\ntest_capture_execution_lanes, test_deploy_manifest_stays_retired, test_task_tracker_stays_retired,\ntest_codex_handoff_stays_retired, test_all_sources_parse, test_generated_artifacts_are_not_tracked,\ntest_history_columns_go_through_migrations, plus test_pin_index_in_sync whose enumerator lives one\nimport away in tools/build_pin_index.py:151,211). I ran section 4's recipe\n(grep -rln \"ls-files\\|rglob\\|--collect-only\" tests/*.py) and it returned 40+ hits; I did NOT\nclassify each one's subject, so RE-DERIVE that membership rather than trusting the count. CLAUDE.md\nrecords three consecutive revisions of that table being wrong.\nCut 2 DELETES tracked files, which moves test_deploy_manifest_stays_retired,\ntest_task_tracker_stays_retired and test_codex_handoff_stays_retired (all enumerate\ngit ls-files -- '*.py' '*.sh'; the bd-* scripts are extensionless so they may NOT move \u2014 verify,\ndo not assume) and definitely moves test_pk_mirrors_do_not_drift (245 bd-* mirrors at :51).\n\nTWO INDEPENDENT RISKS TO THE PLAN ITSELF:\n1. My twelve is a RECONSTRUCTION with 11 of 12 identified and one (bd-guardcheck's single ref)\n   unresolved. Acting on it without re-deriving repeats the exact failure this repo keeps having.\n2. test_pk_mirrors_do_not_drift IS ITSELF A KNOWN-BROKEN GATE \u2014 register 15.29's own \"Item 7\"\n   says it does not fire (it calls pytest.fail() which the custom runner stubs without .fail, and\n   its SOURCE_DIRS loop breaks on first match). So deleting a toolchain/bin tool and forgetting\n   its project-knowledge mirror WILL NOT BE CAUGHT by that test. Run\n   `venv/bin/python toolchain/bin/bd-pk-mirror --check` by hand in cut 2.\n3. The container cannot prove the box safe. bd-cut, bd-tool-lint --gate and bd-coretest gate CI\n   and the release; v3.66.858 declined to touch them for exactly this reason. Cut 1 edits two of\n   them, so it needs a real ./capture.sh on test4 before cut 2 starts.\n4. bd-coretest has a sha-keyed cache (:360-372) that can serve a stale PASS. Use --no-cache for\n   every verification run in these cuts; I did, and said so.\n5. bd-pack currently carries a live undefined name (TRACKER_FILES at :169) and its --selftest\n   exits 0 anyway. If bd-pack is retired, that bug leaves with it \u2014 do not spend a cut fixing it\n   first; but if bd-pack SURVIVES re-derivation, that defect is a separate item.",
    "over_sensitivity_test": "Three assertions, one per direction the fix could swing wrong. All have measured baselines at 3fded70.\n\n1. bd-coretest must stay GREEN on the real tree, with the SAME numbers. The absence probe must not\n   turn present-and-correct tools inconclusive:\n     $ venv/bin/python toolchain/bin/bd-coretest --no-cache\n     exit=0 ; \"CORE TOOLS PASSING: 13/13   (assertions 22 pass / 0 miss)\" ; \"SELFTEST PASS\"\n   Pin 13/13 and 22-pass/0-miss in the test, not just exit 0. A fix that renders every case UNKNOWN\n   would satisfy \"does not certify an absent tool\" and destroy the harness \u2014 this is the\n   CLAUDE.md section 6 trap (\"a fix for 'reports clean when blind' that simply calls every scan\n   inconclusive passes the escape's test and destroys the tool\"), and this item is a live instance\n   of exactly that shape.\n\n2. bd-tool-lint must stay exit 0 and must NOT start reporting spurious missing entries:\n     $ venv/bin/python toolchain/bin/bd-tool-lint --bin toolchain/bin --json  -> exit=0\n     critical_core.present must remain 7 and untested_count must remain 0 AFTER the fix;\n     only the new \"missing\" key changes, to exactly [\"bd-blast\",\"bd-suites\",\"bd-touched\"].\n   And the ratchet must still fail on a real regression: with --ratchet, a CRITICAL_CORE tool that\n   is PRESENT but has no --selftest must still set regressed:true. Assert that positively, or the\n   fix could satisfy the missing-name check by moving every name into the RETIRED constant.\n\n3. The retirement's own gate must be BIDIRECTIONAL. Assert both:\n   (a) a CORE_TOOLS/CRITICAL_CORE name with no file and no tombstone entry FAILS; and\n   (b) a name with a file present PASSES and is not flagged.\n   Without (b), \"make every dangling name fail\" degenerates into \"flag everything\", and the first\n   maintainer who hits it will widen the filter \u2014 which is how bd-tool-lint's cc_present became\n   blind in the first place.\n\nAdditionally, the budget/prose ratchets must be checked in the tightening direction after any\nre-pin: with _TOOL_BUDGET lowered to the new live count, adding one tool must fail\ntest_the_toolchain_does_not_grow_unbudgeted. Verify by temporarily pointing _bd_tools at a fixture\ndir, never by touching toolchain/bin.",
    "size": "L",
    "blocked_on": "Not ready to cut as filed. Two blockers, one of which needs the operator.\n\nBLOCKER 1 (operator decision, cannot be resolved in a container): the twelve are not written down\nanywhere. The v3.66.858 plan was an untracked session artifact and I verified it is absent from\nproject-knowledge/, docs/, pending-specs/ and every commit reachable from main. I reconstructed 11\nof 12 by arithmetic fit against the tombstone's own per-referrer counts, and one slot\n(bd-guardcheck's single reference \u2014 bd-since, bd-evidence or bd-agent-scorecard) I could not\nclose. Matt must either confirm the set or authorize re-deriving it from scratch. Retiring the\nwrong tool is not recoverable by a follow-up cut in the way a code defect is.\n\nBLOCKER 2 (sequencing, resolvable now): cut 1 is a prerequisite and is NOT what the item asks for.\nRetiring anything today makes bd-coretest report PASS for tools that no longer exist and shrinks\nbd-tool-lint's CRITICAL_CORE denominator silently \u2014 both proven above with real output, and the\nsame class already cost this repo the TRACKER_FILES dangling name at v3.66.841.\n\nCut 1 is fully specified, its two RED tests are written and PROVEN FAILING on pristine, and its\nover-sensitivity control is measured (13/13, 22 pass / 0 miss). It can start immediately and does\nnot depend on blocker 1. Recommend cutting it first and treating the retirement as the cut after.\n\nScope note for whoever picks this up: CLAUDE.md section 9 puts runtime, build, guard and release\nchanges behind explicit per-task authorization. bd-cut, bd-tool-lint --gate and bd-coretest all\ngate CI or the release, so cut 1 needs that authorization plus a real ./capture.sh on test4 before\ncut 2 begins. No guard SHA is touched by any of this."
  },
  {
    "item": "ITEM 8 -- bd-band carries zip-era /home/claude paths (3 occurrences); root-cause the \"Passed: 4 | Failed: 10\" on tests/test_contracts.py under bd-band",
    "defect_confirmed": "PARTIALLY-REAL",
    "evidence": "HEAD NOTE: verified `3fded70` at start. It MOVED mid-session to `a25e577` (\"Save the nine-item recon plans before compaction\") -- a docs-only commit adding project-knowledge/pending-specs/recon-queue-items-1-9.md (314 lines, 1 file). Nothing in my subject changed. All line numbers below are at a25e577. `git status --porcelain` empty throughout; I mutated nothing.\n\n=== (1) THE REPORTED 4/10 IS CLOSED. IT DOES NOT REPRODUCE AT HEAD. ===\n$ venv/bin/python toolchain/bin/bd-band tests/test_contracts.py ; echo exit=$?\n  interpreter: /home/user/BD/venv/bin/python\n  PASS  tests/test_contracts.py: Total: 14 | Passed: 14 | Failed: 0 | Skipped: 0\n  ALL PASS -- 1/1 suites green\nexit=0\n\n=== (2) THE ACTUAL CAUSE OF THE 4/10 WAS THE INTERPRETER, NOT THE ENV. ===\nThe 4/10 split is exactly \"tests that import bulk_downloader.app\" vs \"tests that do not\":\n$ AST scan of tests/test_contracts.py -> 10 tests reference bulk_downloader.app, 4 do not.\nReproduced verbatim by driving the WRONG interpreter (what pre-851 bd-band did):\n$ env BD_HOME=$(mktemp -d) BD_DISABLE_KEEPALIVE=1 PYTHONPATH=/tmp/prestaged_site_packages \\\n      PLAYWRIGHT_BROWSERS_PATH=/home/claude/.cache/ms-playwright \\\n      /usr/local/bin/python3 run_tests.py tests/test_contracts.py ; echo exit=$?\n  FAIL test_contract_ok_field_is_boolean\n    File \"/home/user/BD/bulk_downloader/app.py\", line 9, in <module>\n      from flask import Flask, ...\n  ModuleNotFoundError: No module named 'flask'          (x10, identical cause)\n  Total: 14 | Passed: 4 | Failed: 10 | Skipped: 0\nexit=1\n$ /usr/local/bin/python3 -V  -> Python 3.11.15   (symlink to /usr/bin/python3.11)\n\n=== (3) THE SUSPECT ENV VARS ARE CAUSALLY INERT FOR THIS SYMPTOM (A/B). ===\nA) venv python, WITH the dead env vars  -> Total: 14 | Passed: 14 | Failed: 0\nB) venv python, WITHOUT them            -> Total: 14 | Passed: 14 | Failed: 0\nThe note's attribution to band_env() was wrong. The cause was, in the SAME FILE:\n$ git show 267dea9^:toolchain/bin/bd-band | grep -n 'run(\\['\n  179:  r = run([\"python3\", \"run_tests.py\", s], cwd=rundir, env=env, timeout=a.timeout)\n\n=== (4) TIMELINE PROVES THE NOTE PREDATES THE FIX. ===\nnote's own evidence file  /tmp/b4.txt, /tmp/b7.txt  (\"Passed: 4 | Failed: 10\")\n  $ stat -c %y /tmp/b4.txt   -> 2026-08-03 23:43:02 +0000\nfix 267dea9 \"v3.66.851: the band tools drove an interpreter without pytest (#152)\"\n  $ git log -1 --format=%ci 267dea9 -> 2026-08-04 00:48:45 -0400 (= 04:48 UTC), ~5h LATER\nSESSION_CARRY.md:3535-3540 cites band_env at \":52-53\"; at HEAD it is :55-56 -- shifted by\nexactly the 3 lines 267dea9 inserted (the sys.path/bdtools_sec import). Same file, same\nfunction, later tree. The register entry is stale, not wrong-when-written.\n\n=== (5) A REAL, LIVE, DIFFERENT DEFECT SITS ON THOSE EXACT LINES. ===\ntoolchain/bin/bd-band:52-57\n    def band_env():\n        env = dict(os.environ)\n        env.update(BD_HOME=tempfile.mkdtemp(), BD_DISABLE_KEEPALIVE=\"1\",\n                   PYTHONPATH=\"/tmp/prestaged_site_packages\",\n                   PLAYWRIGHT_BROWSERS_PATH=\"/home/claude/.cache/ms-playwright\")\n        return env\nIt OVERWRITES the caller's ambient values. Real pool measured on this container:\n$ ls /opt/pw-browsers -> chromium-1228, chromium_headless_shell-1228, firefox-1532, webkit-2311\n$ env PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers venv/bin/python -c \"<launch chromium headless>\"\n  LAUNCH OK ; exit=0                                        [unpiped]\n$ env PLAYWRIGHT_BROWSERS_PATH=/home/claude/.cache/ms-playwright venv/bin/python -c \"<same>\"\n  playwright._impl._errors.Error: BrowserType.launch: Executable doesn't exist at\n  /home/claude/.cache/ms-playwright/chromium_headless_shell-1228/.../chrome-headless-shell\n  exit=1                                                     [unpiped]\nSame with the repo's own instrument:\n$ PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers            bd-env-parity -> chromium/firefox/webkit = True\n$ PLAYWRIGHT_BROWSERS_PATH=/home/claude/.cache/...     bd-env-parity -> all three = False\nPYTHONPATH is REPLACED, not appended, and it prepends a world-writable /tmp path AHEAD of\nvenv site-packages:\n$ venv/bin/python -c \"import flask\" -> /home/user/BD/venv/lib/python3.12/site-packages/flask\n$ PYTHONPATH=<scratchdir with a flask.py> venv/bin/python -c \"import flask\"\n  RuntimeError: SHADOWED by PYTHONPATH\n(demonstrated in a private scratch dir, NOT at /tmp/prestaged_site_packages, so as not to\npoison the eight concurrent agents; that literal path does not currently exist.)\n\n=== (6) THIS IS THE LAST SURVIVOR OF A FIX ALREADY APPLIED THREE TIMES. ===\n$ grep -n \"prestaged_site_packages|/home/claude/.cache/ms-playwright\" toolchain/bin/bd-{band,parband,fullsuite,retest}\n  bd-band:7,8,22 (docstring)  bd-band:55,56 (LIVE)      <- only offender\n  bd-parband, bd-fullsuite, bd-retest: no hits\nbd-fullsuite:398-406 is the canonical pattern and its comment describes bd-band's bug:\n  \"PLAYWRIGHT_BROWSERS_PATH is deliberately NOT set: dict(os.environ) already carries the\n   ambient one, and overwriting it with a hardcoded path aimed every browser test at a\n   cache that does not exist here\"\n  env.update(BD_HOME=home, BD_DISABLE_KEEPALIVE=\"1\",\n             PYTHONPATH=os.pathsep.join([work] + [x for x in [os.environ.get(\"PYTHONPATH\")] if x]))\nbd-retest:48-49 and bd-parband:104 set ONLY BD_HOME + BD_DISABLE_KEEPALIVE.\nbd-doctor:209-225 carries the measurement verbatim: \"The previous version FORCED\nPLAYWRIGHT_BROWSERS_PATH=/home/claude/... and PYTHONPATH=/tmp/prestaged_site_packages ...\nMeasured: with that env the probe exits 1 ... on a host where it exits 0 ... PYTHONPATH in\nparticular is REPLACED, not appended.\"\ntests/test_env_parity_sees_the_real_browser_pool.py is the sister gate -- it fixed exactly\nthis for bd-env-parity and its denominator is bd-env-parity ONLY, so bd-band was invisible\nto it. Section 0 shape.\n\n=== (7) THE MIRROR. bd-band-derive MISSED IT. ===\n$ sha256sum toolchain/bin/bd-band project-knowledge/bd-band\n  5b7bc7a3b7611423... toolchain/bin/bd-band\n  5b7bc7a3b7611423... project-knowledge/bd-band     (byte-identical)\n$ venv/bin/python -m pytest tests/test_pk_mirrors_do_not_drift.py -q -> 3 passed\nThat gate compares CONTENT. Editing one copy turns it red.\n\n=== (8) \"DOES BD-BAND HAVE OTHER ASSUMPTIONS?\" -- YES, ONE, AND IT IS BOUNDED. ===\n$ bd-band <the 4 derived suites> ; exit=1\n  PASS test_contracts (14/14) | FAIL test_desandbox_tool_verifiers (Total: 1, Failed: 1)\n  PASS test_toolchain_534 (23/23) | PASS test_v3_66_730_touch_map_tool_bridge (3/3)\n$ venv/bin/python run_tests.py tests/test_desandbox_tool_verifiers.py\n  FAIL  IMPORT ERROR: type object 'mark' has no attribute 'slow'\n$ venv/bin/python -m pytest tests/test_desandbox_tool_verifiers.py -q -> 10 passed, exit=0\nrun_tests.py always activates run_tests_core._PytestStub (activated_pytest_stub, :188-209 --\nit binds whenever \"pytest\" is not already in sys.modules, which it never is under\nrun_tests.py). Stub mark attributes measured: {bd_module_wipe, parametrize, skip, skipif}.\nMarker census over all 1210 tests/test*.py files:\n  bd_module_wipe 83 | parametrize 58 | skipif 11 | slow 1 (MISSING)\nSo the \"other assumption\" costs exactly 1 of 1210 files -- but that file is IN bd-band's own\nderived band. PRE-EXISTING at HEAD, not caused by this cut. Separate item.",
    "files": [
      "/home/user/BD/toolchain/bin/bd-band:52-57 -- band_env(); replace the two hardcoded literals with the bd-fullsuite pattern (inherit PLAYWRIGHT_BROWSERS_PATH, prepend rundir to the AMBIENT PYTHONPATH)",
      "/home/user/BD/toolchain/bin/bd-band:172 -- the sole call site, `env = band_env()`; becomes `env = band_env(rundir)` so --from-zip prepends the EXTRACTED tree, never a.work",
      "/home/user/BD/toolchain/bin/bd-band:6-9 and :22 -- module docstring still teaches the retired incantation (PYTHONPATH=/tmp/prestaged_site_packages, PLAYWRIGHT_BROWSERS_PATH=/home/claude/..., --from-zip /home/claude/out/...zip); update in the same cut, prose only",
      "/home/user/BD/project-knowledge/bd-band -- byte-identical mirror (sha256 5b7bc7a3b7611423...); apply the IDENTICAL edit or tests/test_pk_mirrors_do_not_drift.py goes red",
      "/home/user/BD/tests/test_toolchain_534.py:776 -- `_RUNNERS = (\"bd-band\", \"bd-parband\", \"bd-fullsuite\", \"bd-retest\")` already exists; reuse it",
      "/home/user/BD/tests/test_toolchain_534.py:779-807 -- test_no_runner_hardcodes_the_wrong_interpreter, the v3.66.851 sibling; the new tests go immediately after it",
      "/home/user/BD/toolchain/bin/bd-fullsuite:398-406 -- READ FIRST: the canonical env construction to copy",
      "/home/user/BD/toolchain/bin/bd-doctor:209-225 -- READ FIRST: the same defect, already diagnosed with its measurement"
    ],
    "red_test": "Two new tests in tests/test_toolchain_534.py, immediately after test_no_runner_hardcodes_the_wrong_interpreter (:779). Adding to an EXISTING file rather than a new one is deliberate: it keeps every axis-6 file-count gate (test_source_windows_do_not_shift, test_pin_index_in_sync's test_files_scanned, test_pytest_runner_boundary) out of the blast radius.\n\nTEST A -- structural, AST-scoped, both mirror copies:\n  def test_no_runner_forces_a_retired_sandbox_path_into_the_band_env()\n  For each name in _RUNNERS, for each of (\"toolchain/bin\", \"project-knowledge\"): ast.parse\n  the file, collect the ids of module/class/function docstring Constants, then walk for\n  ast.Constant str nodes NOT in that set containing \"/home/claude\" or\n  \"/tmp/prestaged_site_packages\". Assert the offender list is empty.\n\n  PROVEN RED ON PRISTINE -- I ran exactly this predicate at a25e577:\n    LIVE-CODE offenders (docstrings+comments excluded): 4\n      toolchain/bin/bd-band:55      '/tmp/prestaged_site_packages'\n      toolchain/bin/bd-band:56      '/home/claude/.cache/ms-playwright'\n      project-knowledge/bd-band:55  '/tmp/prestaged_site_packages'\n      project-knowledge/bd-band:56  '/home/claude/.cache/ms-playwright'\n\n  WHY AST AND NOT grep -- I tried the naive text ban first and it returned 16 offenders,\n  including bd-parband:61,65 and bd-fullsuite:102, which NAME the retired path in comments\n  explaining the fix. A text ban would fire on already-correct code, get switched off, and\n  reproduce the shape of the defect it is closing. Comments are absent from the AST for\n  free; docstrings must be excluded explicitly.\n\nTEST B -- behavioural, the real proof:\n  def test_band_env_inherits_the_ambient_browser_pool_and_pythonpath(monkeypatch)\n  Load toolchain/bin/bd-band by path (extensionless: importlib.machinery.SourceFileLoader,\n  the pattern bd-band's own _load_bandcheck:74-87 uses). Module import is safe -- main() is\n  guarded by __name__.\n  monkeypatch.setenv PLAYWRIGHT_BROWSERS_PATH=\"/opt/pw-browsers\" and\n  PYTHONPATH=\"/sentinel/caller/path\"; call band_env(<tmp_path>) and assert:\n    env[\"PLAYWRIGHT_BROWSERS_PATH\"] == \"/opt/pw-browsers\"\n    \"/sentinel/caller/path\" in env[\"PYTHONPATH\"].split(os.pathsep)\n    str(tmp_path) == env[\"PYTHONPATH\"].split(os.pathsep)[0]\n\n  PROVEN RED ON PRISTINE -- I loaded band_env at a25e577 and called it:\n    ambient PLAYWRIGHT survives?  False -> /home/claude/.cache/ms-playwright\n    ambient PYTHONPATH preserved? False -> /tmp/prestaged_site_packages\n  (and the pristine signature takes no argument, so the tmp_path assertion is red twice\n  over -- TypeError before the assert.)\n\nHARNESS TRAP the implementer must not trip: tests/test_desandbox_tool_verifiers.py is in\nthe derived band but CANNOT run under bd-band -- run_tests.py's pytest stub has no\nmark.slow, so it reports \"Total: 1 | Failed: 1  IMPORT ERROR\". Under plain pytest it is\n10 passed. That failure is PRE-EXISTING at a25e577 and must not be read as caused by this\ncut. Run that one suite with venv/bin/python -m pytest.",
    "fix": "Replace toolchain/bin/bd-band:52-57 with the pattern bd-fullsuite:398-406 already proves, and update the call site at :172.\n\n    def band_env(rundir):\n        # The ambient PLAYWRIGHT_BROWSERS_PATH is INHERITED, never overwritten.\n        # Forcing a literal aimed every banded browser test at a pool that does\n        # not exist and reported the band red for an environment artifact --\n        # the same class @851 fixed for the interpreter one function below.\n        # PYTHONPATH is PREPENDED, not replaced: overwriting it also removed\n        # whatever the caller had arranged.\n        env = dict(os.environ)\n        env.update(BD_HOME=tempfile.mkdtemp(), BD_DISABLE_KEEPALIVE=\"1\",\n                   PYTHONPATH=os.pathsep.join(\n                       [rundir] + [x for x in [os.environ.get(\"PYTHONPATH\")] if x]))\n        return env\n\nand at :172   env = band_env(rundir)\n\nFOUR THINGS THAT MATTER:\n1. rundir, NOT a.work. band_env() is called at :172, after rundir is resolved at :162\n   (--from-zip extraction) or :167 (work tree). Prepending a.work in --from-zip mode would\n   import the work tree's modules into a band whose entire purpose is a pristine tree.\n   Passing the argument -- rather than reading a.work inside -- is what makes that\n   impossible to get wrong later.\n2. Do NOT substitute a newer literal. \"/opt/pw-browsers\" hardcoded is the identical defect\n   with a fresher path. If the var is unset, leave it unset and let Playwright use its own\n   default; bd-doctor:221-225 probes candidates instead, which is also acceptable, but it\n   must never override a value the caller set.\n3. Mirror the edit into project-knowledge/bd-band byte-for-byte.\n4. Clean the docstring at :6-9 and :22 in the same cut -- it is a copy-paste incantation\n   that teaches the bug. Not gate-enforced (a docstring text ban is the over-sensitive\n   trap from TEST A); just prose, in both copies.\n\nWHAT THIS DOES NOT FIX, deliberately -- file separately, do not batch (section 2 rule 3):\n  (a) run_tests_core._PytestStub.mark has no `slow`, so bd-band cannot collect\n      tests/test_desandbox_tool_verifiers.py (1 of 1210 files, measured). Its band is\n      enormous (run_tests_core is the runner for everything) and it is a different subject.\n  (b) bd-band --selftest prints \"SKIP ... SELFTEST PASS\" and exits 0 -- the exact SKIP->PASS\n      shape tests/test_desandbox_tool_verifiers.py:297 was written to kill for\n      bd-band-derive. Once band_env() takes an argument it becomes cheaply selftestable\n      without the full test tree, but that is its own cut.\n  (c) bd-band-derive classified the extensionless bd-band as \"changed source (0) / changed,\n      unclassified (1)\" -- the *.py-glob blindness CLAUDE.md section 1 measures at 469\n      files. It still produced a band, so this is a note, not a blocker.",
    "band": [
      "venv/bin/python toolchain/bin/bd-band-derive --file toolchain/bin/bd-band  -> FLOOR, 4 suites:",
      "tests/test_contracts.py",
      "tests/test_desandbox_tool_verifiers.py  [RUN UNDER PLAIN PYTEST -- bd-band cannot collect it, mark.slow gap; 10 passed / exit 0 is the expected result]",
      "tests/test_toolchain_534.py  [where the new tests live]",
      "tests/test_v3_66_730_touch_map_tool_bridge.py",
      "--- ADDED BY CROSS-CHECK, the derive tool missed all three ---",
      "tests/test_pk_mirrors_do_not_drift.py  [MANDATORY: compares project-knowledge/bd-band CONTENT against toolchain/bin/bd-band; currently 3 passed and byte-identical, so editing one copy turns it red]",
      "tests/test_env_parity_sees_the_real_browser_pool.py  [the sister gate for this exact defect in bd-env-parity; same subject, cheap]",
      "tests/test_v3_66_799_audit_tool_selftests.py  [parametrizes --selftest over shipped toolchain tools incl. bd-band; the recommended fix does not change selftest output, so this should stay green -- run it to prove that]",
      "--- AXIS-6: NONE MOVE, and this is a design property, not luck ---",
      "The cut adds NO new test FILE (new tests go into the existing tests/test_toolchain_534.py) and adds no fixed-width source window. So test_source_windows_do_not_shift.py, test_pytest_runner_boundary.py, test_capture_execution_lanes.py, test_all_sources_parse.py, test_pin_index_in_sync.py (test_files_scanned via tools/build_pin_index.py:151,211), test_generated_artifacts_are_not_tracked.py and the three *_stays_retired.py gates all keep their denominator. RE-DERIVE THIS RATHER THAN TRUSTING IT if a new test file is added after all.",
      "test_playwright_engines_single_source.py does NOT join: its enumerator is git ls-files -z '*.sh' and bd-band is not a shell script.",
      "grep -rl cross-check over tests/ for 'bd-band\\b|band_env' returned exactly {test_desandbox_tool_verifiers, test_toolchain_534, test_v3_66_730_touch_map_tool_bridge} -- a SUBSET of the derive output, so the derive was not narrow here; the misses were the mirror and browser-pool gates, which reference neither string.",
      "--- RELEASE CHORES ---",
      "No new import edge (the fix adds no import), so tools/decomp/import_graph_gate.py --update is NOT required -- verify rather than assume.",
      "venv/bin/python toolchain/bin/bd-regen-order --work \"$PWD\"  AFTER the last source edit, then git status --porcelain over the generated artifacts (CI's gates job checks exactly this and runs no pytest at all).",
      "venv/bin/python toolchain/bin/bd-guardcheck  -- bd-band is not one of the seven pinned guard files; expect 7 ok, 0 drifted. A '0 ok, 0 drifted, 7 missing' summary is a FAILURE signal.",
      "Version bump: only if this ships as a numbered cut -- then all three edits together (bulk_downloader/__init__.py, tests/test_settings_center_slice4.py:200, CHANGELOG.md ASCII-only) plus tools/build_pin_index.py."
    ],
    "risks": "1. THE HEADLINE RISK IS MISREPORTING, NOT CODE. The queue item asks to root-cause a 4/10 that no longer exists. If this ships described as \"fixes the 4/10\", the register inherits a second false claim on top of the first. The cut must say: the 4/10 was closed by v3.66.851; the register entry at SESSION_CARRY.md:3535-3540 is stale AND its causal attribution was wrong; a DIFFERENT live defect occupies the same two lines. Someone should also correct that register entry, or the next session re-opens this.\n\n2. OVER-SENSITIVITY, DEMONSTRATED NOT HYPOTHESISED. My first draft of TEST A was a text ban and it flagged 16 sites, 12 of them legitimate: bd-parband:61,65 and bd-fullsuite:102 name \"/home/claude\" in comments explaining the fix, and both mirror copies double the count. Shipping that would fail already-correct code, which is how a gate gets switched off. The AST version returns exactly 4. If the implementer \"simplifies\" TEST A back to a grep, the cut has reproduced the shape of the defect it closes.\n\n3. THE --from-zip TRAP. band_env() must prepend rundir, not a.work. Getting this backwards silently makes a --from-zip band import the work tree -- destroying the one property that mode exists for, while every test still passes. This is precisely CLAUDE.md's \"succeeds quietly on the wrong thing\".\n\n4. THE MIRROR. project-knowledge/bd-band is byte-identical today and test_pk_mirrors_do_not_drift.py compares content. bd-band-derive did not return that gate. A cut that edits one copy fails a gate it never banded.\n\n5. NO CURRENT TEST SUITE FLIPS ON THE BROWSER FIX -- SAY SO. I A/B'd four browser-touching suites (test_v3_66_171_launch_parity 3/3, test_template_sandbox 11/11, test_cloak_persistent_context_fallback 14/14, test_v3_43_52_keeper_collision 9/9) under both PLAYWRIGHT_BROWSERS_PATH values and every one was identical -- they mock rather than launch. The defect is proven at the mechanism level (chromium launch exit 0 vs exit 1, unpiped) and is ARMED, not currently firing through the test suite. Do not claim it \"fixes failing tests\"; it removes a trap that fires the moment a band includes a launching suite, and it already fires for anything shelling out to a browser.\n\n6. MY OWN MEASUREMENT'S DENOMINATOR. This subagent shell has NO PLAYWRIGHT_BROWSERS_PATH and HOME=/root; I supplied /opt/pw-browsers to represent the standard container env documented at tests/test_env_parity_sees_the_real_browser_pool.py:14. It is not in /etc/environment or /proc/1/environ, so it is set per-session (panel env box / cloud-setup.sh:337-342). In a session where it is genuinely unset, bd-band's override is not a regression -- both are wrong. The fix is still correct, but the severity depends on the caller's env, and that is a fact about my measurement, not about the code.\n\n7. HEAD MOVED UNDER ME (3fded70 -> a25e577) while eight agents read the same tree. Every line number and sha here is at a25e577. Re-verify the anchors before editing; per CLAUDE.md 2b a finding is about a commit, and this one names its commit.",
    "over_sensitivity_test": "Three assertions, in the same tests, that stop the fix swinging the other way:\n\n(a) band_env() MUST STILL DO ITS JOB. Assert env[\"BD_HOME\"] is a fresh existing directory\n    that is NOT os.environ's BD_HOME, and env[\"BD_DISABLE_KEEPALIVE\"] == \"1\". A \"fix\" that\n    reduces the function to `return dict(os.environ)` passes both halves of TEST B and\n    destroys the per-suite isolation the tool exists for. Verified present on pristine:\n    BD_HOME set? True   KEEPALIVE set? 1\n\n(b) THE UNSET CASE MUST NOT INVENT A PATH. With PLAYWRIGHT_BROWSERS_PATH deleted from the\n    environment, assert that band_env() either omits the key entirely or sets it to a\n    directory that os.path.isdir() confirms. This is what forbids swapping one hardcoded\n    literal for a newer one -- \"/opt/pw-browsers\" baked in is the identical defect with a\n    fresher path, and it would pass a naive \"the string /home/claude is gone\" check.\n    On pristine this assertion is RED: value '/home/claude/.cache/ms-playwright', isdir False.\n\n(c) TEST A MUST NOT FIRE ON PROSE. Assert explicitly that the offender list is empty for\n    bd-parband and bd-fullsuite, which DO contain \"/home/claude\" in comments at\n    bd-parband:61,65 and bd-fullsuite:102. This pins the AST scoping in place: if someone\n    later replaces the walk with a text scan, this assertion goes red and names why.\n    Measured: text ban -> 16 offenders (12 false); AST ban -> 4 offenders (0 false).",
    "size": "S",
    "blocked_on": ""
  },
  {
    "item": "ITEM 9 -- capture.sh does not record the commit it captured (plus: should capture.sh gain a selftest stage?)",
    "defect_confirmed": "REAL",
    "evidence": "### 0. COMMIT MISMATCH -- read this first, then note why the findings still hold\n\n    $ git -C /home/user/BD log --oneline -1\n    a25e577 Save the nine-item recon plans before compaction (items 1-6, partial)\n\nHEAD is a25e577, NOT the 3fded70 I was told to expect. This is NOT the reverted-checkout\nfailure the brief warns about -- the tree is one commit AHEAD, and 3fded70 is its direct parent:\n\n    $ git merge-base --is-ancestor 3fded70 HEAD && echo \"YES ancestor of HEAD\"\n    YES ancestor of HEAD\n    $ git diff --stat 3fded70 a25e577\n     .../pending-specs/recon-queue-items-1-9.md  | 314 +++++++++++++++++++++\n     1 file changed, 314 insertions(+)\n    $ git diff --name-only 3fded70 a25e577 | grep -E 'capture|test|\\.sh$|\\.py$'\n    (none -- diff touches no .sh, no .py, no test, no capture file)\n\nThe one added file is a markdown recon plan from a sibling agent. capture.sh is byte-identical\nat both commits and in the worktree:\n\n    $ git rev-parse 3fded70:capture.sh ; git rev-parse a25e577:capture.sh\n    db97190e6a3441243dcd53825eb899598da79312\n    db97190e6a3441243dcd53825eb899598da79312\n    $ sha256sum capture.sh ; git show HEAD:capture.sh | sha256sum\n    b9511853b88963af7a28b582e4d5f81afd6009e139e6e52f72c91b91b202ccdb  capture.sh\n    b9511853b88963af7a28b582e4d5f81afd6009e139e6e52f72c91b91b202ccdb  -\n\nI proceeded on that basis. Every finding below is a property of capture.sh blob db97190e,\nwhich is the same blob at 3fded70 and at a25e577. `git status --porcelain` is empty.\n\n### 1. THE DEFECT: no git invocation exists in capture.sh at all\n\n    $ grep -n \"rev-parse HEAD\" capture.sh ; echo \"exit=$?\"\n    exit=1\n\n    $ grep -n \"git\" capture.sh\n    23:#     pytest lanes. That file is gitignored yet SHIPS in the zip, so a stale\n    24:#     copy from an earlier overlay deploy outlives `git clean -fd` (ignored\n    365:# and GITIGNORED (.gitignore `reports/*`), yet it ships inside the release zip.\n    367:# deletes -- and `git clean -fd` cannot remove an ignored file (that needs -x).\n    920:# NON-FATAL BY DESIGN: a headless box with no Xvfb is a legitimate\n    972:# the stale-or-half-built frontend/dist/ class that a git deploy silently does\n\nAll six hits are COMMENTS. capture.sh executes no git command anywhere in its 1106 lines.\n\n### 2. RAN THE REAL SCRIPT -- the actual 01_sysinfo.log it produces today\n\nI drove capture.sh through its own existing executable harness\n(tests/test_capture_shell_runtime.py::_run_probe, which cuts the script at the structural\nsentinel `# -- [2b/9]` and so INCLUDES step [1] sysinfo), redirecting all output to /tmp:\n\n    $ /home/user/BD/venv/bin/python /tmp/mkprobe.py\n    returncode= 0\n    === capture-out dir listing ===\n        01_sysinfo.log 1709\n        02_pytest_parallel.log 0\n        ...\n\n    $ cat /tmp/probe_run/capture-out/01_sysinfo.log\n    --- date ---\n    2026-08-05T00:13:52+00:00\n    --- uname ---\n    Linux vm 6.18.5-fc-v18 #1 SMP PREEMPT_DYNAMIC @0 x86_64 x86_64 x86_64 GNU/Linux\n    --- os-release ---\n    PRETTY_NAME=\"Ubuntu 24.04.4 LTS\"\n    ... (os-release, python, pip freeze, disk, memory, ports) ...\n    --- version we installed ---\n    __version__ = \"capture-probe\"\n    # capture probe\n\n    $ grep -cE '\\b[0-9a-f]{40}\\b' 01_sysinfo.log      -> 0\n    $ grep -ciwE 'commit|branch'   01_sysinfo.log      -> 0\n\nZero commit identity of any kind. I also swept EVERY file the probe wrote:\n\n    01_sysinfo.log: sha-like hits=0        02_pytest_parallel.xml: 0\n    02_pytest_parallel.log: 0              02_pytest_serial.xml: 0\n    02_pytest_serial.log: 0                02_suite_run.log: 0\n    02a_gui_parity_inventory.log: 0\n\n(PREDICATE NOTE, in the spirit of CLAUDE.md 1: my first grep used\n`-iE \"commit|sha|rev-parse|branch|[0-9a-f]{40}\"` and returned 1 hit. The hit was the `free -h`\nheader line, because \"shared\" contains \"sha\". I re-derived with `-cE '\\b[0-9a-f]{40}\\b'` and\n`-ciwE 'commit|branch'`, both 0. Reporting the bad predicate because it is the exact trap\nthis file warns about.)\n\n### 3. The banner is NOT a workaround -- it never reaches the bundle\n\ncapture.sh:325-332 echoes `version : $VERSION` to stdout. That stdout is not inside $OUT, and\ncapture.sh:1086 tars only $OUT:\n\n    tar czf \"$ARCHIVE\" -C /tmp \"$(basename $OUT)\"\n\nThe operator's `./capture.sh > /tmp/capture.log` lands OUTSIDE the archive. So a fix that only\ntouches the banner does not make a bundle self-identifying. It must go in 01_sysinfo.log.\n\n### 4. Independent corroboration: the register has filed this FOUR times and it is still open\n\n    project-knowledge/SESSION_CARRY.md:1710  \"the capture bundle cannot identify its own commit\"\n    :1997  \"A CAPTURE BUNDLE STILL CANNOT IDENTIFY ITS OWN COMMIT, and this session paid for it\n            twice: two uploads arrived together and were separable only by reading\n            02_SUMMARY.txt and 09_http_smoke.log.\"\n    :2625  \"Small, unclaimed: add `git rev-parse HEAD` to 01_sysinfo.log\"\n    :3557  \"`git rev-parse HEAD` into 01_sysinfo.log (checked this bundle; still absent...)\"\n\nNote :1997's caveat, which sharpens the item: 09_http_smoke.log DOES carry a sha via\n/api/health -- but ONLY when the service stage ran. A capture that dies at step [2] carries no\nidentity at all.\n\n### 5. There is already a correct implementation of this in the repo, in Python\n\nbulk_downloader/app_health.py:49-111 `build_identity()` solves exactly this problem and its\ndocstring is the design rationale, pre-written:\n\n    \"WHY GIT FIRST. build_info.json ... holds whatever the last zip build left. The value found\n     on the live box was a8881d9d471c from 2026-07-19, which git cat-file cannot resolve at all\n     ... A checkout knows its own commit, so derive rather than re-stamp ... `source` is not\n     decoration. A fallback to a recorded file is indistinguishable from a live read unless it\n     says so, and that indistinguishability is precisely how the stale value went unnoticed.\"\n\nIt returns `{\"sha\", \"built_at\", \"source\"}` where source is `git` | `build_info.json` | `unknown`.\nThat is the CLAUDE.md-0 three-state pattern already shipped. The shell fix should mirror it.\n\n### 6. I tested the proposed shell snippet in FOUR directions (all real output)\n\n    ###### A: real repo (the box case) ######\n    commit    : a25e57767a83503b3ebdce85190f91280fffb590\n    branch    : claude/bulkdownloader-handoff-j9o59v\n    toplevel  : /home/user/BD\n    committed : 2026-08-05T00:03:57+00:00\n    tree      : clean\n      -> exit=0\n\n    ###### B: BD_HOME is not a work tree (the probe-harness case) ######\n    commit    : UNKNOWN (not a git work tree; capture cannot identify its source)\n      -> exit=1\n\n    ###### C: fresh `git init` repo (proves the harness can go GREEN post-fix) ######\n    commit    : ec2fb6c28d48b92798223d2db59fb45bb8893a00\n    branch    : master\n    toplevel  : /tmp/isrepo.UX8L\n      -> exit=0\n\n    ###### D: BD_HOME is a SUBDIR of a repo -- THE TRAP I ALMOST SHIPPED ######\n    $ cd /home/user/BD/tools && emit_commit_identity\n    commit    : a25e57767a83503b3ebdce85190f91280fffb590\n    branch    : claude/bulkdownloader-handoff-j9o59v\n    toplevel  : /home/user/BD          <-- but BD_HOME would be /home/user/BD/tools\n      -> exit=0\n\nDirection D is the audit of my own fix (CLAUDE.md rule 3). `git rev-parse HEAD` walks UP the\ntree, so if BD_HOME is not the repo root the stage emits a valid-looking sha about a DIFFERENT\ntree -- a confident wrong answer, which is worse than the absent one it replaces. The fix must\ncompare `--show-toplevel` against BD_HOME and declare MISMATCH.\n\nBare `git rev-parse HEAD` in a non-repo:\n    fatal: not a git repository (or any of the parent directories): .git   exit=128\nSo the UNKNOWN branch is reachable and safe-failing (this also covers git's \"dubious ownership\"\nrefusal, which exits non-zero the same way).\n\n### 7. Adjacent, verified, pre-existing (AUDIT_2026_07_29.md:224)\n\ncapture.sh:344 logs `head -3 bulk_downloader/__init__.py` under the label \"version we\ninstalled\". On the real tree that prints the module docstring, not the version:\n\n    $ head -3 bulk_downloader/__init__.py\n    \"\"\"Bulk Downloader - package layout\n    <blank>\n    Module map:\n    $ grep -n \"^__version__\" bulk_downloader/__init__.py\n    33:__version__ = \"3.66.867\"\n\n$VERSION is already computed at capture.sh:325 and is not written to the log. The probe run\nabove only showed a version because the FAKE __init__.py puts it on line 1.\n\n### 8. Nothing mechanically constrains adding lines here\n\n    $ grep -rn \"CAPTURE_SH|capture\\.sh\" tests/*.py | grep -E \"\\[[a-z_]*pos|\\+ *[0-9]{3,}|\\[0:\"\n    exit=1        (no fixed-width source window slices capture.sh)\n\nEvery `capture.sh:<line>` reference in tests/ is inside a docstring, and two are ALREADY stale\n(test_capture_csrf_diag_redacts_cookies.py:10 says \"capture.sh:1005 tars\"; the tar is at 1086).\nProse, not assertions.\n\ntests/test_capture_execution_lanes.py:154-167 reads capture.sh source, but every assertion is\n`X in source` or `count(...) >= 2` -- floors. Adding text cannot break them.\ntests/test_capture_vault_is_isolated.py:748-760 enumerates `--stage-exit \"...\"` pairs but\nasserts `assigned & referenced` is non-empty -- a property, not a fixed set. Adding a stage is safe.\n\n### 9. RED PROVEN ON PRISTINE, using the repo's own harness\n\n    $ /home/user/BD/venv/bin/python /tmp/red_proof.py\n    probe returncode: 0\n    40-hex shas found in 01_sysinfo.log: []\n    lines mentioning commit/branch: []\n    ASSERTION UNDER TEST: '01_sysinfo.log records the captured commit'\n    RESULT: FAIL  <-- RED on pristine, as required\n\n    $ BD_DISABLE_KEEPALIVE=1 venv/bin/python -m pytest tests/test_capture_shell_runtime.py -q\n    exit=0\n    7 passed in 2.17s        (the harness itself is green at this commit)\n\n### 10. SECOND HALF -- the selftest stage. Confirmed gap, and the verdict semantics that decide the design\n\n    $ grep -n \"selftest\" capture.sh ; echo \"exit=$?\"\n    exit=1\n\nCorroborated by SESSION_CARRY 15.19:2221 -- \"capture.sh NEVER CALLS /api/selftest ... So no\ncapture bundle can confirm or refute ANY selftest change, and every selftest check on the box\nsits permanently outside the capture's denominator.\" It then records the exact bite: a session\ngrepped the 844 bundle for \"stale lock\", got silence, and reported the WARN as gone. \"The claim\nwas true, but the evidence was worthless -- a denominator that structurally cannot contain the\nsubject, reporting clean.\"\n\nMechanics I read rather than assumed:\n  - bulk_downloader/app_selftest.py:46-69 -- GET /api/selftest, no `startup` arg = fresh run.\n  - bulk_downloader/selftest.py:632-640 -- returns {\"ok\": summary[FAIL]==0, \"checks\":[...],\n    \"summary\": {ok,warn,fail}, \"elapsed_ms\"}. WARNs do NOT clear `ok`. 15 checks appended.\n  - tools/capture_verdict.py:130-132 -- ANY nonzero --stage-exit becomes a FAIL reason.\n    There is no warn tier for stage exits.\n  - tools/capture_verdict.py:138-148 -- the precedent that decides this: live WARNs were\n    DELIBERATELY ungated because gating them \"reported FAIL on a healthy box that no code change\n    could ever turn green ... a gate that cries wolf gets switched off.\"\n  - capture.sh:1016-1020 -- step [7]'s loop is hardcoded `\"http://localhost:5555/api/dev/$route\"`.\n    /api/selftest has a different prefix, so it CANNOT ride that loop; it needs its own stage.\n  - capture.sh's own header (lines 12-14) records the precise failure a naive stage would repeat:\n    step [7] probed `sse_smoke`, which is not a registered route, so it \"returned\n    {\"error\":\"endpoint not found\"} on every run\" and nothing noticed. `curl -fsS` exits 0 on a\n    200 carrying an error body.",
    "files": [
      "/home/user/BD/capture.sh:336-348 -- the sysinfo brace block; `} > \"$OUT/01_sysinfo.log\" 2>&1` is line 347. Insert the commit-identity emitter INSIDE these braces, immediately after the `--- date ---` pair (line 337) so it is the second thing a reader sees.",
      "/home/user/BD/capture.sh:344 -- `echo \"--- version we installed ---\"; head -3 bulk_downloader/__init__.py`. Pre-existing mislabel (see evidence 7). Optional rider; see risks.",
      "/home/user/BD/capture.sh:285 -- `cd \"$BD_HOME\"`. This is why plain `git rev-parse` resolves at all; the sysinfo block runs after it. Do not move it.",
      "/home/user/BD/capture.sh:325-332 -- the stdout-only banner. Echo the short sha here TOO (operator convenience), but this is not the fix.",
      "/home/user/BD/capture.sh:1012-1033 -- step [7] `/api/dev/$route` loop. NOT extensible to /api/selftest (different prefix). New stage goes after it, before step [8] at :1035.",
      "/home/user/BD/capture.sh:1060-1078 -- the capture_verdict.py call. A selftest stage adds `--stage-exit \"selftest=$SELFTEST_EXIT\"` here.",
      "/home/user/BD/bulk_downloader/app_health.py:49-111 -- build_identity(); COPY ITS THREE-STATE SHAPE. Do not import it (capture.sh is shell and runs before the service is up).",
      "/home/user/BD/tools/capture_verdict.py:130-132 -- any nonzero stage exit = FAIL. No warn tier.",
      "/home/user/BD/tools/capture_verdict.py:138-148 -- the over-sensitivity precedent that governs the selftest stage's WARN handling.",
      "/home/user/BD/bulk_downloader/selftest.py:632-640 -- run_all() return shape; `ok` is zero-FAILs, WARNs excluded.",
      "/home/user/BD/bulk_downloader/app_selftest.py:46-69 -- the /api/selftest view.",
      "/home/user/BD/tests/test_capture_shell_runtime.py:26-46 -- _build_probe(); cuts at the structural sentinel `# -- [2b/9]`, so step [1] IS in range. Reuse, do not rebuild.",
      "/home/user/BD/tests/test_capture_shell_runtime.py:49-172 -- _run_probe(); builds a fake BD_HOME that is NOT a git repo. That is the ready-made UNKNOWN-direction fixture.",
      "/home/user/BD/tests/test_capture_runs_every_registered_live_check.py:1-50 -- the model for a registry-denominator gate; its docstring is the argument template.",
      "/home/user/BD/tests/test_capture_vault_is_isolated.py:748-760 -- enumerates --stage-exit pairs but asserts a non-empty intersection, so a new stage is safe.",
      "/home/user/BD/tests/test_capture_execution_lanes.py:154-167 -- capture.sh substring assertions, all floors; safe.",
      "NEW /home/user/BD/tests/test_capture_records_its_commit.py -- cut 1's RED test.",
      "NEW /home/user/BD/tests/test_capture_probes_the_selftest_battery.py -- cut 2's RED test (only if cut 2 is authorized)."
    ],
    "red_test": "TWO CUTS, TWO RED TESTS. CLAUDE.md 3 (\"one feature per cut\") -- do not batch these.\n\n=== CUT 1 RED TEST: tests/test_capture_records_its_commit.py (new file) ===\n\nReuse the existing executable harness rather than rebuilding it (CLAUDE.md 8). Sibling-test\nimport is an established pattern here -- tests/test_wave2_backlog.py:26-27 does exactly this.\nPrefer importlib.util.spec_from_file_location on the absolute path (what I used in\n/tmp/red_proof.py, proven working) over a bare `import test_capture_shell_runtime`, so the test\ndoes not depend on tests/ being on sys.path.\n\n  from test_capture_shell_runtime import _build_probe   # or importlib on the abs path\n\nThree tests. Direction 1 and 3 fail on pristine; direction 2 must pass on pristine AND after.\n\n  (1) UNDER-SENSITIVITY / the actual item -- RED on pristine.\n      Build the probe. Build the fake BD_HOME exactly as _run_probe does, then\n      `git init` it and make one `--allow-empty` commit (use -c user.email/-c user.name so it\n      works with no global git config -- verified working, evidence 6 direction C). Run the\n      probe. Read $CAPTURE_TEST_OUT/01_sysinfo.log and assert:\n          - a 40-hex sha is present AND equals the fake repo's `git rev-parse HEAD`\n          - the branch name is present\n      WHY IT FAILS ON PRISTINE: PROVEN, not predicted. /tmp/red_proof.py drove the real\n      pristine capture.sh through this exact harness and got\n      `40-hex shas found in 01_sysinfo.log: []` -> \"RESULT: FAIL\". Pristine capture.sh runs no\n      git command at all (evidence 1), so no sha can appear by accident.\n      Assert the sha EQUALS the fake repo's HEAD, not merely that some 40-hex string exists --\n      otherwise the test would also pass if the stage reported the OUTER /home/user/BD repo,\n      which is precisely the direction-D trap.\n\n  (2) OVER-SENSITIVITY -- must be GREEN on pristine and stay green. See over_sensitivity_test.\n\n  (3) THE MISMATCH GUARD -- RED on pristine (the line does not exist).\n      git init the fake home's PARENT, not the fake home. Then BD_HOME is a subdirectory of a\n      repo and `git rev-parse HEAD` answers about the parent. Assert 01_sysinfo.log contains an\n      explicit MISMATCH marker and does NOT present the parent's sha as the captured commit.\n      Direction D in evidence 6 is the measured proof this case yields a valid-looking wrong\n      answer today.\n\n  DO NOT write a pure `assert \"git rev-parse HEAD\" in capture_sh_source` test. It would go\n  green against a snippet that is present but broken, unreachable, or writing to stdout instead\n  of the log -- a gate whose denominator is the text rather than the behaviour. Source-substring\n  assertions are fine as a CHEAP ADDITION, never as the proof.\n\n=== CUT 2 RED TEST: tests/test_capture_probes_the_selftest_battery.py (new file) ===\n\nThe probe harness cuts at `# -- [2b/9]`, so it cannot reach a stage placed near line 1035. Two\noptions; take (a).\n\n  (a) Extract the selftest stage on STRUCTURE (CLAUDE.md 2a: never a fixed width). Split the\n      source on the two adjacent stage banners -- `# -- [7/9]` ... `# -- [8/9]` -- assert the\n      sentinel count is exactly 1 each, then run the extracted fragment under `bash` with a\n      fake `curl` on PATH and a fake python. Confirm `bash -n` on the fragment first so a\n      mis-cut presents as a harness error, not a subject failure.\n  (b) Cheaper fallback: unit-test a new tools/ helper that parses the selftest JSON, plus one\n      source assertion that capture.sh pipes /api/selftest into it. Weaker.\n\n  THE FAKE CURL MUST BE ABLE TO DISAGREE WITH ITSELF. CLAUDE.md 6 records two shipped harness\n  defects of exactly this kind -- a fake curl that answered every URL identically, and a fake\n  python returning 0 for every -c. Parameterize the fake curl's response body:\n      - {\"ok\":true,\"summary\":{\"ok\":15,\"warn\":0,\"fail\":0},\"checks\":[...]}  -> stage exit 0\n      - {\"ok\":true,\"summary\":{\"ok\":12,\"warn\":3,\"fail\":0},\"checks\":[...]}  -> stage exit 0  (!)\n      - {\"ok\":false,\"summary\":{\"ok\":13,\"warn\":0,\"fail\":2},\"checks\":[...]} -> stage exit NONZERO\n      - {\"error\":\"endpoint not found\"}                                    -> stage exit NONZERO\n      - {\"ok\":true,\"summary\":{\"ok\":0,\"warn\":0,\"fail\":0},\"checks\":[]}      -> stage exit NONZERO\n      - malformed / truncated JSON                                        -> stage exit NONZERO\n      - curl itself exits 7 (connection refused)                          -> stage exit NONZERO\n  Rows 4-7 are the whole point. Row 4 is capture.sh's own recorded sse_smoke bug (header lines\n  12-14) reproduced as a test. Rows 5-6 are \"unknown is a third state and it fails\". Row 3\n  minus row 2 is the FAIL/WARN discrimination.\n  WHY RED ON PRISTINE: `grep -n \"selftest\" capture.sh` returns nothing (exit=1, evidence 10) --\n  the extraction finds no stage and every row fails.",
    "fix": "=== CUT 1 (recommended, small, high value): record the captured commit ===\n\nInsert INSIDE the existing brace block at capture.sh:336-348, right after the `--- date ---`\npair, so it lands in 01_sysinfo.log (the bundled file) rather than the un-tarred banner. Exact\nsnippet, already exercised in all four directions (evidence 6):\n\n    echo \"--- source identity ---\"\n    _bd_top=$(git rev-parse --show-toplevel 2>/dev/null)\n    _bd_sha=$(git rev-parse HEAD 2>/dev/null)\n    if [ -z \"$_bd_sha\" ] || [ -z \"$_bd_top\" ]; then\n      echo \"commit    : UNKNOWN -- $PWD is not a git work tree;\"\n      echo \"            this bundle cannot identify the source it was taken from\"\n    elif [ \"$(cd \"$_bd_top\" 2>/dev/null && pwd -P)\" != \"$(pwd -P)\" ]; then\n      echo \"commit    : MISMATCH -- git answered for $_bd_top, but the capture ran in $PWD.\"\n      echo \"            The sha below describes a DIFFERENT tree; do not read it as this one.\"\n      echo \"            (reported anyway, labelled) $_bd_sha\"\n    else\n      echo \"commit    : $_bd_sha\"\n      echo \"branch    : $(git rev-parse --abbrev-ref HEAD 2>/dev/null)\"\n      echo \"toplevel  : $_bd_top\"\n      echo \"committed : $(git log -1 --format=%cI 2>/dev/null)\"\n      _bd_dirty=$(git status --porcelain 2>/dev/null | wc -l)\n      if [ \"$_bd_dirty\" -ne 0 ]; then\n        echo \"tree      : DIRTY ($_bd_dirty uncommitted path(s)) --\"\n        echo \"            the commit above is NOT what was captured\"\n      else\n        echo \"tree      : clean\"\n      fi\n    fi\n\nFive deliberate choices, each with a reason:\n\n  1. FULL 40-hex sha, not build_identity()'s [:12]. The bundle is read offline, often months\n     later; the operator pastes it into `git show`. Costs nothing.\n  2. UNKNOWN is printed EXPLICITLY, not left as git's bare `fatal:` on stderr. The block\n     redirects 2>&1 so the fatal would land in the log, but as an unlabelled error a reader\n     skims past -- exactly the indistinguishability app_health.py:70-72 names as \"precisely how\n     the stale value went unnoticed\".\n  3. MISMATCH branch. Without it the stage emits a valid-looking sha about the wrong tree\n     (evidence 6, direction D). This is the defect's own shape and it is the single most\n     important line in the snippet.\n  4. DIRTY count. `git reset --hard` is the deploy, but CLAUDE.md 7 records that it discards\n     operator live-edits -- i.e. live-edits exist. A capture of a dirty tree is not a capture of\n     that commit, and saying only the sha would be a new lie replacing the old silence.\n  5. NOT wired to --stage-exit. See over_sensitivity_test.\n\nAlso add `commit  : ${_bd_sha:0:12}` to the stdout banner at capture.sh:325-332 for the\noperator watching live. That is convenience, not the fix -- the banner is not tarred\n(capture.sh:1086 archives only $OUT).\n\n=== CUT 2: the selftest stage -- ARGUING FOR IT, WITH ONE HARD CONSTRAINT ===\n\nVERDICT: WORTH DOING, as a separate cut, and ONLY in the form below. The brief's own warning is\nthe right one, and it is the reason most of the design is about what the stage must refuse to do.\n\nWHY IT IS WORTH DOING. Fifteen selftest checks (selftest.py, 15 `checks.append/extend` sites)\ncurrently sit permanently outside the capture's denominator. That is not a theoretical gap --\nSESSION_CARRY 15.19 records it producing a WRONG CLOSURE: a session grepped a bundle for \"stale\nlock\", got silence, and certified the check gone. The claim happened to be true; the evidence\nwas structurally incapable of being false. That is CLAUDE.md section 0 exactly, and it is the\nclass of error the contract rates highest. The stage also has an unusually clean design: unlike\nthe live-check registry (see test_capture_runs_every_registered_live_check.py, where the gate\nwas computed from the request and could never see an unrequested ID), /api/selftest returns\nALL checks in one response, so there is no requested-subset denominator to get wrong.\n\nTHE HARD CONSTRAINT -- what makes it a stage rather than a decoration:\n\n    SELFTEST_EXIT=0\n    curl -fsS --max-time 30 http://localhost:5555/api/selftest > \"$OUT/07b_selftest.json\" 2>&1\n    if [ $? -ne 0 ]; then SELFTEST_EXIT=1; fi\n    venv/bin/python tools/selftest_verdict.py --report \"$OUT/07b_selftest.json\" \\\n        >> \"$OUT/07b_selftest.log\" 2>&1 || SELFTEST_EXIT=1\n\n  and add `--stage-exit \"selftest=$SELFTEST_EXIT\"` to the verdict call at capture.sh:1060-1078.\n\n  tools/selftest_verdict.py (new, ~40 lines) exits:\n    0  -- parsed, summary present, (ok+warn+fail) >= 1, and fail == 0\n    1  -- fail > 0                                     [the real signal]\n    1  -- unparseable, missing `summary`, or zero total checks   [unknown fails]\n  and PRINTS the per-check name/status/message table into 07b_selftest.log so the bundle carries\n  greppable evidence.\n\n  THE NON-EMPTY-DENOMINATOR ASSERTION IS THE LOAD-BEARING LINE. `(ok+warn+fail) >= 1` is what\n  stops this cut reproducing the defect it fixes. Without it, a response of `{\"checks\":[]}`, or\n  `{\"error\":\"endpoint not found\"}` behind an HTTP 200, yields \"zero failures\" -- truthfully, and\n  uselessly -- while putting a reassuring 07b_selftest.log in the bundle that makes the blind\n  spot INVISIBLE to the next session's grep. That would be strictly worse than today's honest\n  silence, and it is not hypothetical: capture.sh's own header (lines 12-14) records step [7]\n  probing `sse_smoke`, a non-route, getting `{\"error\":\"endpoint not found\"}` on every run, for\n  an unknown number of releases, with nothing noticing.\n\nARGUMENT AGAINST, and why it loses: a fresh capture runs on a box whose vault is locked and\nwhose download dirs may be empty, so some checks legitimately WARN (15.19 records three live\nWARNs on a healthy box). Gating on those would be the cries-wolf failure. It loses because the\nFAIL/WARN split already exists in the data (selftest.py:632-640, `ok` = zero FAILs) and\ncapture_verdict.py:138-148 already made and documented this exact call for live warns. Reuse\nthe decision; do not relitigate it.\n\nSEQUENCING: cut 1 first. It is ~20 lines of shell with no runtime dependency and no new stage\nexit. Cut 2 adds a new tool, a new gate and a new failure mode to the release gate, and it wants\nits own band and its own box run.",
    "band": [
      "=== FLOOR, derived not hand-rolled ===",
      "venv/bin/python toolchain/bin/bd-band-derive --file capture.sh   -> 100 files. Full list in the tool's --emit output; do not retype it.",
      "CROSS-CHECK PERFORMED (CLAUDE.md 5): grep -rln 'capture\\.sh|CAPTURE_SH' tests/*.py -> 39 files. comm -23 against the derived band -> EMPTY. The derived band is a strict superset; grep found nothing it missed.",
      "=== ADD ON TOP -- axis-6, because BOTH cuts add a NEW TEST FILE ===",
      "tests/test_source_windows_do_not_shift.py  (git ls-files tests/*.py)",
      "tests/test_pytest_runner_boundary.py       ((REPO/'tests').glob('test*.py'))",
      "tests/test_all_sources_parse.py            (git ls-files -- *.py, bare glob reaches tests/)",
      "tests/test_generated_artifacts_are_not_tracked.py  (git ls-files -z, everything)",
      "tests/test_history_columns_go_through_migrations.py (_tracked('*.py') at :49, call :59)",
      "tests/test_deploy_manifest_stays_retired.py     (git ls-files -- *.py *.sh)",
      "tests/test_task_tracker_stays_retired.py        (git ls-files -- *.py *.sh)",
      "tests/test_codex_handoff_stays_retired.py       (git ls-files -- *.py *.sh)",
      "tests/test_pin_index_in_sync.py -- THE INVISIBLE TENTH. Its enumerator is one import away (tools/build_pin_index.py globs (root/'tests').glob('*.py') at :151 and :211), so no grep of tests/ finds it. Regenerate PIN_INDEX after adding the test file: venv/bin/python tools/build_pin_index.py",
      "=== ALREADY IN THE DERIVED BAND (verified by membership check, listed so nobody re-adds them) ===",
      "tests/test_capture_execution_lanes.py, tests/test_playwright_engines_single_source.py (the *.sh gate -- capture.sh IS a shell edit), tests/test_capture_verdict.py, tests/test_u45_capture_sh_shipped.py, tests/test_provision_test_host.py, tests/test_capture_runs_every_registered_live_check.py, tests/test_capture_vault_is_isolated.py, tests/test_capture_shell_runtime.py",
      "=== READ-THESE-FIRST subset if you want a fast pre-band before the full 100 ===",
      "tests/test_capture_shell_runtime.py tests/test_capture_execution_lanes.py tests/test_u45_capture_sh_shipped.py tests/test_capture_vault_is_isolated.py tests/test_capture_verdict.py tests/test_provision_test_host.py tests/test_capture_runs_every_registered_live_check.py",
      "=== FOR CUT 2 ONLY, additionally ===",
      "tests/test_v3_66_642_selftest_battery.py, tests/test_stale_locks_check_is_gone.py, tests/test_capture_verdict.py (the new stage-exit)",
      "=== ARTIFACTS + THE ORDER THAT MATTERS ===",
      "Run venv/bin/python toolchain/bin/bd-regen-order --work \"$PWD\" AFTER the last source edit, not before (CLAUDE.md 2a). git add the new test file BEFORE the final band run, or five of the axis-6 gates cannot see it and their pass proves nothing about it.",
      "Move any out-of-scope RED files OUT of tests/ before regenerating -- build_pin_index counts (root/'tests').glob('*.py') regardless of tracking. Eight other agents are working this tree; check git status first.",
      "=== NOT NEEDED ===",
      "No route change, no new import edge, no version bump (capture.sh-only maintenance edits ship without one -- see capture.sh:8, 'capture.sh-only; NOT a release cut -- no version bump'). If the operator wants a version bump anyway, that is three edits, section 3.",
      "=== THE ONLY BINDING EVIDENCE ===",
      "cd ~/BulkDownloader && ./capture.sh --workers=$(nproc) > /tmp/capture.log 2>&1; echo \"exit=$?\"   -- and re-pin the graph hash BEFORE running it, or step [2b] reports drift and capture_verdict turns that into a whole-capture FAIL. Container green is necessary, not sufficient."
    ],
    "risks": "1. THE COMMIT MISMATCH ABOVE. HEAD is a25e577, brief said 3fded70. I verified 3fded70 is the\n   direct parent, the only diff is one markdown file, and capture.sh is the identical blob\n   (db97190e) at both. All findings hold at either commit. Flagging rather than burying it.\n\n2. MY FIX NEARLY REPRODUCED THE DEFECT'S SHAPE, and the audit is in the evidence. Direction D\n   (evidence 6): `git rev-parse HEAD` walks UP, so if BD_HOME is not the repo root the stage\n   emits a valid-looking sha about a different tree -- replacing honest silence with a\n   confident wrong answer. The `--show-toplevel` comparison is the mitigation and it must not\n   be \"simplified away\" by a later reviewer who reads it as belt-and-braces. It is the guard.\n\n3. THE PRIMARY OVER-SENSITIVITY RISK IS AN EXECUTOR \"IMPROVING\" CUT 1 by wiring the commit line\n   to --stage-exit. capture_verdict.py:130-132 makes any nonzero stage exit a whole-capture\n   FAIL. A non-git tree would then fail the release gate for a reason no code change can fix --\n   the exact failure capture_verdict.py:138-148 was written to undo for live warns. Leave cut 1\n   informational. The dedicated regression test (over_sensitivity_test) exists to enforce this.\n\n4. CUT 2 IS WHERE A DECORATIVE FIX IS TEMPTING. A stage that only checks curl's exit code\n   passes on `{\"error\":\"endpoint not found\"}` behind HTTP 200 -- capture.sh's own documented\n   sse_smoke bug, header lines 12-14. It would put a reassuring 07b_selftest.log in the bundle\n   and make the 15.19 blind spot invisible to the next grep: strictly worse than today. The\n   `(ok+warn+fail) >= 1` non-empty-denominator assertion is not optional.\n\n5. HARNESS-DEFECT RISK IN CUT 2's TEST. CLAUDE.md 6 records two shipped instances -- a fake curl\n   answering every URL identically, a fake python returning 0 for every -c. If the fake curl\n   cannot serve different bodies per case, rows 3-7 of the parameterization are unobservable and\n   the battery proves nothing while looking thorough. Write the fake to read its body from an\n   env var and assert in the harness that two cases actually produced different stage exits.\n\n6. EXTRACTION RISK IN CUT 2's TEST. Cut on the `# -- [7/9]` / `# -- [8/9]` banners, never a\n   fixed width; run `bash -n` on the fragment before running it. CLAUDE.md 2a records an\n   extractor that swallowed a closing `fi` and produced bash syntax errors that presented as\n   subject failures.\n\n7. NINE AGENTS ARE WRITING THIS TREE. `git add <explicit paths>` only, never `-A`. Consider\n   `bd-claim add capture.sh tests/test_capture_records_its_commit.py`. A regen commit has\n   previously swept another workflow's uncommitted RED battery into itself.\n\n8. TWO PRE-EXISTING ISSUES I FOUND AND AM DELIBERATELY NOT FOLDING IN (CLAUDE.md 3, one feature\n   per cut): (a) capture.sh:344's `head -3 bulk_downloader/__init__.py` misses `__version__` at\n   line 33 -- verified, one-word fix ($VERSION is already computed at :325), documented at\n   AUDIT_2026_07_29.md:224; (b) several `capture.sh:<line>` references in test docstrings are\n   already stale and adding ~20 lines at step [1] shifts more. They are prose, assert nothing,\n   and I confirmed no test slices capture.sh by fixed width or asserts on a line number. Do not\n   chase them; note (a) to the operator as a candidate rider if he wants it in the same cut.\n\n9. UNVERIFIED, stated as unknown: I could not run capture.sh end-to-end (no systemd, no service,\n   no box). Everything above steps [1]-[2] was exercised through the repo's own probe harness;\n   steps [7]-[9] were read, not run. The selftest stage's real-box behaviour -- in particular\n   how many of the 15 checks WARN on a fresh capture -- is UNMEASURED here and only the box can\n   answer it. That number decides nothing about the design (WARNs are ungated either way) but\n   the operator should see it before cut 2 is called done.",
    "over_sensitivity_test": "TWO ASSERTIONS, one per cut. Both must be GREEN on pristine and stay green after the fix --\nthat is what makes them anti-regression rather than anti-nothing.\n\n=== CUT 1: the UNKNOWN path must not fail the capture ===\n\ntest_capture_without_a_git_tree_still_completes_and_says_so\n\n  Run the probe with the stock _run_probe fake BD_HOME -- which is a plain tmpdir, NOT a git\n  work tree (verified: `git rev-parse HEAD` there exits 128, \"fatal: not a git repository\").\n  Assert ALL THREE:\n    a) completed.returncode == 0      -- the capture is not failed by an unidentifiable tree\n    b) 01_sysinfo.log contains the literal token \"UNKNOWN\"\n    c) 01_sysinfo.log contains NO 40-hex string (it must not fabricate or inherit one)\n\n  (a) is the anti-over-sensitivity clause: it fires if someone wires the commit line to\n      --stage-exit, which capture_verdict.py:130-132 would turn into a whole-capture FAIL on\n      any non-git tree -- the cries-wolf soundness bug, and the precise mistake\n      capture_verdict.py:138-148 was written to undo for live warns.\n  (b) is the anti-under-sensitivity clause: it fires if someone \"simplifies\" the fix to a bare\n      `git rev-parse HEAD` whose failure prints only git's fatal to stderr. Silence and UNKNOWN\n      must be distinguishable, which is app_health.py:70-72's whole argument.\n  (c) is the anti-direction-D clause: it fires if the guard is dropped and the stage starts\n      reporting an enclosing repository's HEAD.\n\n  This test is GREEN on pristine for (a) and (c) and RED for (b) -- so state which clause is\n  the RED one when reporting, and do not let (a)'s pass be read as the test having proven\n  something. Its job starts after the fix lands.\n\n  Pair it with the direction-D test (red_test #3): parent-of-fake-home is the repo. Together\n  they pin both failure modes of the same guard: too quiet (b) and too loud/wrong (c).\n\n=== CUT 2: WARNs must not fail the capture ===\n\ntest_selftest_warnings_do_not_fail_the_capture\n\n  Feed the fake curl {\"ok\":true,\"summary\":{\"ok\":12,\"warn\":3,\"fail\":0},\"checks\":[...12+3 entries]}\n  -- modelled on the three live WARNs SESSION_CARRY 15.19 records on a HEALTHY box -- and assert\n  SELFTEST_EXIT == 0. Then feed {\"ok\":false,\"summary\":{\"ok\":13,\"warn\":0,\"fail\":2},...} and assert\n  SELFTEST_EXIT != 0.\n\n  Both assertions in ONE test, so the pair cannot drift apart. A stage that fails on WARNs would\n  report FAIL on a box no code change could turn green; a stage that ignores FAILs is\n  decoration. The single failing predicate that catches BOTH is: the stage's exit must track\n  summary[\"fail\"] and nothing else.\n\n  And the third clause that stops the fix swinging into vacuous-safety -- the mirror of \"a fix\n  for 'reports clean when blind' that simply calls every scan inconclusive\":\n      {\"ok\":true,\"summary\":{\"ok\":0,\"warn\":0,\"fail\":0},\"checks\":[]}  MUST exit NONZERO.\n  Zero failures over zero checks is not a pass. Without this clause a stage could satisfy every\n  other row by always returning 0 on a well-formed response, and the 15 real checks would still\n  be uncaptured behind a log file that makes the gap invisible.",
    "size": "M",
    "blocked_on": "Not blocked for CUT 1 -- ready to cut. Two things the executor should know rather than rediscover: (1) HEAD is a25e577, not the 3fded70 in the brief; it is one markdown-only commit ahead and capture.sh is the identical blob at both, so this is not the reverted-checkout case and the plan holds -- but say which commit you cut from. (2) CLAUDE.md 9 puts runtime/build/release changes behind explicit per-task authorization, and capture.sh IS the release gate; the cut needs Matt's go, and only his box run (./capture.sh --workers=$(nproc), graph hash re-pinned first) is binding evidence. CUT 2 (the selftest stage) is a separate authorization: it adds a new tool, a new stage exit and a new way for the release gate to fail. I also could not measure how many of the 15 selftest checks WARN on a real fresh capture -- no service in this container -- so that number is UNKNOWN here and should be seen before cut 2 is called done."
  }
]
```

## Adversarial review

### Lens 1

Verified every RED claim against the working tree. HEAD is **a25e577** (one markdown-only commit past the 3fded70 in the brief; `git diff --stat 3fded70 a25e577` = 1 file, `project-knowledge/pending-specs/recon-queue-items-1-9.md`). No subject file differs between the two commits. All measurements below are mine, at a25e577.

**Verdict: 6 of 9 sound as written. 3 carry a RED that fails for the wrong reason (signature/TypeError, not wrong behaviour). No plan has a RED that is already true.**

---

## ITEM 1 -- SOUND

Independently confirmed both halves:
- `git check-ignore -v .bd_last_band.json` -> exit 1, no output. Adding it to `_GENERATED` in `/home/user/BD/tests/test_gitignore_rules_actually_match.py:166-169` is red at the parametrized assertion (`_git("check-ignore","-q",rel).returncode == 0`). Checked the rest of that file for a "every pattern must match an existing path" gate -- there is none (`test_a_directory_rule_matches_by_path_not_by_existence` is the only existence-adjacent one), so the anchored rule is safe with the file absent.
- `/home/user/BD/toolchain/bin/bd-parband:163-174`: the refusal list is `HANGERS | bare tests/ | not s.endswith(".py")`. A nonexistent `.py` path passes the door; `json.dump(...)` at :191-192 runs unconditionally after the pool; exit is 1, never 2. All three assertions (rc==2, marker, `not results.exists()`) are red.

One executor trap worth writing into the test: `RESULTS` is resolved at **module import** (`bd-parband:58-59`), not per-call. The plan's subprocess+`BD_LAST_BAND` form is correct; anyone who "simplifies" it to an in-process import will write `.bd_last_band.json` into the repo root and the `not results.exists()` assertion becomes meaningless.

## ITEM 2 -- RED-A/C/D sound; **RED-B is signature-red, and a better assertion exists**

Confirmed red:
- RED-A: `bd-band-derive --file toolchain/bin/bdtools_sec.py` -> band (13), and `tests/test_pk_mirrors_do_not_drift.py` is not among them. ok
- RED-C: `mirror_pairs()` returns **255**; `bd`, `cap01_witnesses.py`, `run01_witnesses.py` are all absent from the pair set. ok
- RED-D: `_PytestStub` (`/home/user/BD/run_tests_core.py:42`) defines `skip`/`skipif` only, no `fail`. ok

RED-B is the weak one. `mirror_pairs()` at `/home/user/BD/tests/test_pk_mirrors_do_not_drift.py:59` takes **no parameters**, so `mirror_pairs(root=tmp_path)` raises TypeError -- the test is red because the kwarg does not exist, not because the `break` at `:75` is wrong. That is the shape CLAUDE.md 2a warns about: an incomplete implementation raises the same exception.

**Assert this instead (or in addition), and it needs no signature change:**
```python
names = [pk.name for pk, origin in mirror_pairs()]
assert names.count("bd-audit-gate.py") == 2   # toolchain/bin AND tools
```
I measured `names.count("bd-audit-gate.py") == 1` on the real tree today, and both `toolchain/bin/bd-audit-gate.py` and `tools/bd-audit-gate.py` exist. That is a behaviour red against the `break`, on the shipping tree, with a real AssertionError. Keep the fixture-tree version as the secondary case.

## ITEM 3 -- SOUND (all five verified in source)

- `bd-fullsuite:127-131` `classify()` regex-searches the **whole** `out`, first-match-wins; `:436` and `:226` both set `status = "env" if kind.startswith("ENV")` for the file. RED 1 red, and the `--fork` row is genuinely a separate code path (`_parse_worker_line:212-230`, classifying over `out[-3000:]`). ok
- `bd-fullsuite:728` is `return 1 if (real or tmo or aborted) else 0` -- no `n_pass` guard. RED 2 red. ok
- `bd-opv:1367-1370` grades a raised check `SKIP`; the aggregate guard at `:1400` is `if n_ran and n_pass == 0`, so one unrelated PASS clears it. RED 3 red. ok
- `bd-equiv:62-67` `run_tool` returns `(cp.stdout or "") + (cp.stderr or "")` and never reads `cp.returncode`. RED 4 red. ok
- `bd-env-report-check:98` requires **both** fields UNKNOWN; `:110` is `if got_version and got_version != "UNKNOWN" and ...`. Version=UNKNOWN (or absent) + a real commit -> `problems` empty -> FRESH/0. Both fixtures red. ok
- The `_BD_OPV_REEXEC=1` note is correct -- `bd-opv:65` gates a venv re-exec at import time.

One shape caveat on the parametrized invariant: the bd-opv row **cannot** be an argv row (it needs `SourceFileLoader` + `REGISTRY` substitution), so the "table over four tools" is not uniform. Say so in the table, or an executor will force bd-opv into the argv column, fail to construct the fixture, and the row will skip -- the invariant reacquiring the exact shape it exists to test.

## ITEM 4 -- SOUND

`/home/user/BD/toolchain/bin/bd-claim`: `add()` writes `{"pid": os.getpid()}` to `f"{os.getpid()}.json"` (:79-84); `release()` unlinks the same (:89); `live_claims()` reaps on `_alive(c["pid"])` (:69-71); `.githooks/pre-commit:80` passes `pid=os.getppid()`, which is the per-commit bash. All three REDs are behaviour reds against real shipped code.

One framing point: RED case 3 (`test_the_claimant_may_commit_its_own_claimed_path`) fabricates a durable claim that pristine `bd-claim` **cannot produce**. It is a legitimate and necessary constraint -- it is what forces Half B into the same cut -- but it is red against a hypothetical state, so do not report it alongside cases 1-2 as "three defects proven". Two defects proven, one design constraint pinned.

## ITEM 5 -- RED-1/2/5 sound; **RED-4 fails for the wrong reason**

- RED-1: `run()` (`/home/user/BD/bulk_downloader/ai_boot_readiness.py:127-170`) persists only at `:132/:137/:147/:166`, every one at or after an attempt outcome. Nothing is written before `probe_factory(...)`. Red. ok
- RED-2: red for the same reason. ok
- RED-5: hand-written v1 doc, matching `boot_id`, `updated_at` inside 600s -> `read_status` (`ai_boot_status.py:95-113`) returns the payload verbatim with `state == "degraded"`. Clean AssertionError red, and it is the only assertion that enforces the third state. ok

RED-4 is broken. It calls `status.write_status({...}, path, now=1000.0, boot_id="boot-a", final=False)`, and `write_status` (`ai_boot_status.py:70-71`) is `(document, path=STATE_PATH, *, now=None, boot_id=None)` -- **no `final`**. On pristine that is a `TypeError` at the write, never reaching the `STALE_AFTER_SECONDS is 600` assertion the plan cites as the reason it fails. The plan dodged this identical trap one line earlier (choosing the literal `181` over `IN_FLIGHT_TTL_SECONDS+1` precisely to avoid an AttributeError an incomplete stub also raises) and then walked into it through the kwarg.

**Assert this instead:** build the document the way RED-5 already does -- write the JSON by hand with `"final": false`, matching `boot_id`, `updated_at` at epoch 1000 -- then
```python
assert read_status(p, now=1060.0, boot_id="boot-a")["state"] == "retrying"      # live
assert read_status(p, now=1181.0, boot_id="boot-a")["state"] == "stale"          # abandoned
assert read_status(p, now=1181.0, boot_id="boot-a")["stale_reason"] == "abandoned"
```
Pure behaviour red, no signature dependency, and it survives an implementation that spells the writer's kwarg differently.

## ITEM 6 -- SOUND, with the doc half honestly RED-less

`/home/user/BD/toolchain/bin/bd-mutate` has exactly one restore site (`:211-212`, inside the per-mutant `finally`), zero `signal`/`atexit`/`KeyboardInterrupt` handling, and `original = path.read_text(...)` at `:184` reads the working tree. The proposed `_selftest` case 7 -- a following `run_battery` on a residued tree must return 2 and **name the residued path** -- is red on pristine, because no journal and no preflight exist. The "name the path" clause is load-bearing exactly as stated: bare `rc == 2` also matches empty-spec and red-baseline.

Two harness risks the executor must not paper over: (1) the RED depends on the SIGTERM landing while the mutant is on disk -- poll the heartbeat, never sleep a fixed interval, or the test goes flaky-green; (2) the plan's own evidence measured the child pytest **surviving** the kill, so case 7 must reap it or `tests/test_toolchain_534.py:485` (`--selftest`, `timeout=180`) inherits an orphan.

Part (a), the CLAUDE.md prose, has no RED and the plan says so. Report it as measurement-backed, not test-backed.

## ITEM 7 -- SOUND (strongest REDs in the batch; I reproduced both)

```
$ cd /tmp && venv/bin/python toolchain/bin/bd-coretest --bin /tmp/it7empty --only pack --no-cache
bd-pack  PASS
  [ok][FAULT   ] FAULT->refuse          refused an incomplete pack (rc=2)
CORE TOOLS PASSING: 1/1   (assertions 1 pass / 0 miss)
SELFTEST PASS
exit=0
```
```
$ venv/bin/python toolchain/bin/bd-tool-lint --bin toolchain/bin --json | jq .critical_core
{"baseline": 0, "present": 7, "regressed": false, "untested": [], "untested_count": 0}
```
No `missing` key. `CRITICAL_CORE` (`toolchain/bin/bd-tool-lint:46-49`) lists 10; `bd-blast`, `bd-suites`, `bd-touched` are all absent from `toolchain/bin/`, so the expected list in test 2 is exactly right. Budget confirmed at the ceiling: 239 tools, `tests/test_toolchain_534.py:1036` `_TOOL_BUDGET = 239`, `:150` `_PROSE_ONLY_BASELINE = 184`.

Note for the reader of the register, not a defect in the plan: these two REDs belong to **cut 1**, which is not what the item asks for. The item's headline (the 12-tool retirement) has no RED and is blocked on the operator. The plan states this.

## ITEM 8 -- TEST A sound; **TEST B is signature-red and should be split**

TEST A is solid. `toolchain/bin/bd-band:52-56` hardcodes both literals, and `sha256sum` confirms `project-knowledge/bd-band` is byte-identical (`5b7bc7a3b7611423...`), so the AST scan's 4 offenders are real. The plan's own measurement that a text ban returns 16 (12 false, from explanatory comments in `bd-parband`/`bd-fullsuite`) is the right reason to keep it AST-scoped.

TEST B calls `band_env(<tmp_path>)`; pristine `band_env()` takes no argument, so it is a TypeError before any assertion. The plan admits this ("red twice over") but the diagnostic is weak: a stub `def band_env(rundir): return dict(os.environ)` satisfies both stated assertions while deleting the per-suite `BD_HOME` isolation the tool exists for. Split it:
- `test_band_env_does_not_clobber_the_ambient_browser_pool` -- call the **zero-arg** `band_env()` with `PLAYWRIGHT_BROWSERS_PATH`/`PYTHONPATH` monkeypatched, assert the ambient values survive. Clean AssertionError red today, no signature dependency.
- `test_band_env_prepends_the_run_directory` -- the `rundir` argument test, red by signature, honestly labelled as such.

The plan's over-sensitivity (a) (`BD_HOME` fresh + `BD_DISABLE_KEEPALIVE`) already catches the degenerate stub, so the pair is safe either way -- but the split makes the RED prove the defect rather than the refactor.

## ITEM 9 -- SOUND

Confirmed: `capture.sh` executes **no** git command. `grep -n "git" capture.sh` returns 3 hits, at `:24`, `:367`, `:972`, all inside comments; `grep -n "rev-parse"` returns nothing. The sysinfo brace block (`capture.sh:335-347`) emits date/uname/os-release/python/pip/disk/memory/ports/`head -3 __init__.py`/`head -10 CHANGELOG.md` and closes with `} > "$OUT/01_sysinfo.log"`. The probe harness cuts at `# ?? [2b/9]` (`tests/test_capture_shell_runtime.py:28`), so step [1] is in range. RED test 1 (sha must equal the fake repo's HEAD, not merely "some 40-hex exists") and RED test 3 (MISMATCH marker) are both red, and the equality form is the right one -- it is what forbids the direction-D wrong-tree answer.

One scoping caveat on the over-sensitivity clause (c), "01_sysinfo.log contains NO 40-hex string": that holds in the probe's synthetic fixture but is fragile against real content (`head -10 CHANGELOG.md`, `pip freeze`) if the assertion is ever reused outside it. Pin it to the fixture explicitly.

Cut 2's RED is red trivially (`grep -n selftest capture.sh` -> nothing), so its entire worth is the fake curl's ability to serve **different bodies per row**. If that is not built, rows 3-7 are unobservable and the battery looks thorough while proving nothing -- the plan names this; do not let it be trimmed.

---

## Cross-cutting

Three plans (2 RED-B, 5 RED-4, 8 TEST B) reach for a RED whose pristine failure is a `TypeError` from a not-yet-existing parameter, while stating a behavioural reason for the red. Each has a strictly better assertion available against the shipping signature -- measured above for items 2 and 5. This is the mirror of the failure the repo keeps hitting: not "a test that passes the moment you write it", but "a test that fails the moment you write it, for a reason unrelated to the defect". Both leave you without evidence.

### Lens 2

All measurements below are on **HEAD a25e577** (the tree moved from 3fded70 mid-session; items 8 and 9 already noted this). Read-only throughout; `git status --porcelain` empty.

---

# ITEM 1 -- bd-parband door check

**Verdict: fix is sound in shape; its denominator stops one door short.**

**F1.1 (high) -- the same fall-through is reachable through `bd-retest`, which the plan reads but does not guard.** The plan cites `bd-retest:46` as a reader of the results file and then closes only bd-parband's argv. Verified at `toolchain/bin/bd-retest:83-90`:

```
d=json.load(open(RESULTS)); work=a.work or d.get("work",sec.DEFAULT_WORK)
failed=[r["suite"] for r in d["results"] if r["status"]!="pass"]
...
outcomes=[run_one(work,s,a.timeout) for _ in range(a.retries)]
```

`run_one` (`bd-retest:47-56`) calls `run_tests.py <suite>` with **no existence check** -- the identical fallback. And bd-retest's grading is worse than bd-parband's: if the substituted whole-suite run comes back green, `all(o=="pass")` prints `FLAKE` and the tool **removes a real failure from the ledger**. This is reachable without any hand-editing: band fails -> the test file is renamed/deleted in the fix -> `bd-retest` re-runs the old results file. bd-retest's own header states the contract (`:43-44`): *"both tools resolve this identically ... Edit them together or the pair breaks."* Put the existence check in one helper both import, or say in the CHANGELOG that the consumer half is still open.

**F1.2 (low, method) -- the "no consumer of the exit code" claim rests on a denominator that excludes the likeliest consumers.** The grep was over `*.sh/*.py/*.yml/*.md`; the 239 extensionless `bd-*` tools are invisible to it -- exactly the population that would invoke another bd-tool. I re-derived: `grep -rn "bd-parband" toolchain/bin/` returns only `bd-retest:4,5,43,80` (prose) and `bd-tools:98` (a menu list). **The conclusion holds**, but the instrument in the plan cannot support it (section 1: say the denominator in the same sentence as the count).

Sound: resolving against `a.work`, the refusal-before-`RESULTS`-write ordering (verified: `json.dump(... open(RESULTS,"w"))` at `bd-parband:190-191` is after dispatch), and assertion (1c). The bd-bandcheck non-delegation analysis (risk 1) is the best reasoning in the nine plans -- the leak-pair pin should ship exactly as written.

---

# ITEM 2 -- bd-band-derive PK-mirror signal + gate widening

**Verdict: FIX-1 is the right shape but copies a precedent that is silent in the two modes that matter; FIX-2(d) creates an undeclared invariant.**

**F2.1 (high) -- the @860 pattern the plan tells you to copy suppresses its own notice in `--json` and `--emit`.** Verified `bd-band-derive:1234-1241`:

```
if not a.json and not a.emit:
    if not _mp:
        print("  NOTE curated map not found ... this band is narrower than designed.", file=sys.stderr)
```

CLAUDE.md section 4 advertises `bd-band-derive --emit` as the way to get a band. In `--emit` (and `--json`), a missing `project-knowledge/` would drop signal 8 **without a word** and hand back a narrower band that still looks authoritative -- the precise failure the @860 comment says it exists to prevent, reinstated by the mode gate. The notice already goes to **stderr**, so it cannot corrupt the emitted stdout line: print it unconditionally for the new signal, or make the degraded case exit non-zero. Do not copy the `not a.json and not a.emit` condition.

**F2.2 (medium) -- FIX-2(d)'s AMBIGUOUS state promotes a coincidence into a gate.** Measured: 11 duplicate tracked `.py` basenames outside PK; two are PK mirrors, and all three copies of each are byte-identical **today**:

```
a8876ad7daff6eb9  project-knowledge/bd-audit-gate.py | toolchain/bin/... | tools/...
34c772972b89bbdd  project-knowledge/bd-triage.py     | toolchain/bin/... | tools/...
```

After FIX-2(d), `tools/bd-audit-gate.py` must stay byte-equal to `toolchain/bin/bd-audit-gate.py` forever -- a **new invariant nothing in the tree declares**, created as a side effect of fixing the `break`. When it fires the message will say AMBIGUOUS and the remediation on offer (`cp` PK -> origin) is the wrong repair for a toolchain/tools divergence. Either declare that pair explicitly (an allowlist row with a reason) or make AMBIGUOUS name both counterparts and their shas and say the repair is "reconcile the two origins", not "re-sync the mirror".

**F2.3 (low) --** FIX-3's stub: I confirmed 49 `pytest.fail(` sites in `tests/*.py` and **none** use `msg=`/`reason=`/`pytrace=` keywords, so the proposed `fail(reason="", pytrace=True)` signature is safe. Worth stating in the cut, since a `msg=` caller would turn the diagnostic fix into a TypeError.

---

# ITEM 3 -- bd-opv / bd-equiv / bd-fullsuite / bd-env-report-check

**Verdict: the invariant is the right abstraction, but the bd-fullsuite half contains a direct self-contradiction that would be "resolved" by weakening whichever test is discovered second.**

**F3.1 (high) -- the zero-pass CANNOT-EVALUATE guard and the all-env excuse collide on the plan's own fixture.** Fix A adds *"if no file produced a real verdict (zero passes across the run), exit EXIT_CANNOT_EVALUATE, mirroring bd-opv:1400"*. The over-sensitivity control demands *"a file whose failures are ALL genuinely environmental must STILL be excused and STILL exit 0"*, built as a synthetic all-env fixture in tmp_path. That fixture has **`passed 0`** -- it is the same run the plan quotes as evidence for the guard (`=== 1 files | passed 0 | ... env 1 ... GREEN`). RED test 2 requires it to be non-zero; the over-sensitivity test requires it to be zero. Verified the exit is currently `return 1 if (real or tmo or aborted) else 0` (`bd-fullsuite:728`) and bd-opv's analogue is `if n_ran and n_pass == 0` (`bd-opv:1400`), i.e. bd-opv makes the same collision -- it just never runs a bd-opv where every check is env-excused. Decide the semantics explicitly before writing either test: my read is "all-env with zero passes **is** unevaluable (exit 2), and the excuse controls the *reporting*, not the exit code" -- but the plan must say so, or the first implementer to hit the red will delete an assertion.

**F3.2 (medium) -- the ENV_BYTECODE purge-and-retry is keyed off the classifier being replaced.** `bd-fullsuite:428-435`: `kind = classify(out)` then `if kind == "ENV_BYTECODE" and retry_bytecode: ... return run_one(...)`. That is a **whole-file** remedy derived from the whole-file first-match-wins classify. Under per-block classification, which block triggers the purge is undefined -- and the path of least resistance is to keep the old whole-output `classify()` alive for the retry decision, which restores the two-classifiers-drifting hazard the plan names as its own standing risk. Specify: retry if **any** block classifies ENV_BYTECODE, and delete the old call.

**F3.3 (medium) -- the fork path's truncation is load-bearing, not an aside.** Verified `bd-fullsuite:208` emits `out[-3000:]` and `_parse_worker_line:212-231` parses that. A segmenter over a truncated buffer routinely sees an **orphaned continuation with no marker line** -- which under the plan's own rule ("no failing block found but rc != 0 -> unknown") flips every long-output file in `--fork` to `unknown`. The plan says "raise or drop the truncation" in a subordinate clause; it is a precondition of the fix, and the `--fork` row of the invariant should assert a file whose output exceeds 3000 chars.

Sound: the bd-equiv (`cp.returncode`), bd-opv (crashed ? absent precondition) and bd-env-report-check (decisive field decisive) fixes are each one-line-shaped and correctly bounded, and the four over-sensitivity anchors (OPV-QR live SKIP, @852 partial emptiness, version-match-with-differing-commit) are real, measured, and the right ones.

---

# ITEM 4 -- bd-claim durable owner

**Verdict: the design is right about liveness and wrong about identity in the default case; and it introduces a fresh instance of the defect's shape in `add`.**

**F4.1 (high, not in the plan) -- a stable owner filename makes repeated `add` silently drop earlier claims.** Verified `toolchain/bin/bd-claim:80-86`:

```
rec = {"pid": os.getpid(), ..., "paths": sorted(set(paths))}
(claims_dir(repo) / f"{os.getpid()}.json").write_text(json.dumps(rec, indent=1), ...)
print(f"claimed {len(rec['paths'])} path(s) as pid {os.getpid()}...")
```

`add` **replaces** the record; it does not merge. Today that is masked because every CLI invocation has a fresh pid (each claim gets its own file, and all of them are stillborn). The moment the filename becomes a stable owner token -- the whole point of the fix -- `bd-claim add a.py` followed by `bd-claim add b.py` leaves a claim over **b.py only**, prints "claimed 1 path(s)", and `a.py` is silently unprotected. A guard that loses its subject while reporting success is the exact shape this cut is closing. Make `add` read-modify-write (union with the existing record) or refuse a second `add` and require `--replace`; assert it with a two-add test.

**F4.2 (high) -- the ancestry walk is listed as resolution step 4 and refusal as step 5, but in this environment step 4 always succeeds and always returns the shared harness pid.** The plan's own measurement (both Bash shells `ppid 508`, `/proc/508/comm = claude`) says the derived owner is **identical for concurrent agents**, so the hook's self-exclusion matches agent B against agent A's claim and the guard goes inert for the only case it exists for. The plan puts this in risk 1 with a mitigation ("detect a shared ancestor, refuse or warn") but leaves the walk as the default derivation. Under the plan's own rule -- *unknown is a third state and it fails* -- a derivation that lands on a shared ancestor **is** unknown. Demote the walk: `--owner`/`$BD_CLAIM_OWNER` or refuse (exit 2). The proposed shared-ancestor detector is also weak as specified: it fires only when a second live claim carries the same `owner_pid` *with a different label*, and two agents that both omit `--label` collide silently.

**F4.3 (low) --** the mitigation must also cover the hook side: `$BD_CLAIM_OWNER` is inherited into hooks (measured, good), but nothing in `scripts/cloud-setup.sh` or the panel env box sets it per-agent, so the *documented* mode is the fallback mode. If the cut ships without a mechanism that gives concurrent agents distinct tokens, it ships a guard that is green and blind -- say that in the CHANGELOG rather than leaving it as a docs line.

---

# ITEM 5 -- ai_boot in-flight marker

**Verdict: the three-state read (`final` absent -> unknown) is correct and is the best single decision in the nine plans. Two problems: the TTL's denominator, and whether the marker is observable by its only consumer.**

**F5.1 (high) -- `IN_FLIGHT_TTL_SECONDS = 300` is derived from the happy-path cadence and excludes the worst case the module itself admits.** Verified `ai_boot_status.py`/`ai_boot_readiness.py`: `REQUEST_TIMEOUT = 120.0`, and one attempt issues up to five sequential calls, so a live attempt can be silent for **600s** with the heartbeat only at the top of the loop (`for index in range(len(delays) + 1)` at `:143`). A genuinely live run then reads `stale`/`abandoned`. The plan calls this "fail-safe"; under this lens it is not -- it replaces *"cannot distinguish in-flight from terminal"* with *"cannot distinguish in-flight from abandoned"*, same class, opposite sign, and it fires precisely in the slow-Ollama case the readiness unit exists for. The fix is not a bigger constant (that re-opens the abandoned window): **heartbeat per probe call**, inside `_attempt`, or pass a `touch()` callback into the probe. If that is out of scope, duplicate the arithmetic bound as a literal with a comment (`5 * REQUEST_TIMEOUT + margin`) -- not an import, correctly ruled out for the cycle.

**F5.2 (medium) -- the fix may be unobservable to its only production reader, and the plan files that as out of scope.** `STATE_PATH = Path("state")/"ai_boot_readiness.json"` (`ai_boot_status.py:13`) is **relative**, resolved against each process's CWD. The writer is the systemd readiness unit (`WorkingDirectory=${APP_DIR}`); the reader is the Flask app via `app_ai.py:21` -> `/api/ai/status` -> the SPA. If those CWDs ever differ, every write in this cut lands where the reader never looks and the item delivers nothing, while every unit test passes (they pass explicit tmp paths). That is a one-command check on the box and it should gate "done", not sit in risks as "not this item".

**F5.3 (low) --** placing branch (a) *before* the expired check means a legacy doc that is also 10 days old reports `unknown/no_finality_marker` rather than `stale/expired`, losing the more actionable of the two reasons. Cheap fix: check expiry first for docs lacking `final`, or carry both keys.

---

# ITEM 6 -- bd-mutate journal

**Verdict: prose (a) is correct and ships. Journal (b) as specified inherits the tainted baseline it is meant to protect, and its "unjournalled = exit 2" rule kills the tool in its own selftest.**

**F6.1 (high) -- the journal records the working tree's text as "original", which is the same laundering baseline.** Verified `bd-mutate:184`: `original = path.read_text(encoding="utf-8")`. If a battery starts on a residued tree, the journal's "original" **is the residue**. Consequences: `--recover` faithfully restores residue and reports a verified recovery; and the preflight's first branch -- *"file sha == recorded ORIGINAL -> tree already clean, reap and proceed"* -- is the laundering, now automated and blessed by a gate that says it checked. The prose in part (a) documents this defect and then part (b) builds the recovery mechanism on top of it. Ask git for the baseline: record `git rev-parse HEAD:<path>` (or `git diff --quiet -- <path>`, **with `cwd=work`**) alongside the bytes, and have the preflight distinguish "matches the working tree I recorded" from "matches HEAD".

**F6.2 (high) -- "failure to create the journal is exit 2" collides with bd-mutate's own usage.** `--work` defaults to `BD_REPO` but is routinely a scratch tree -- `_selftest(work)` at `:244` builds tmpdirs, and every recon agent in this batch drove batteries in `/tmp`. A tmpdir is not a git repo, so `git rev-parse --git-dir` fails, the journal cannot be written, and under the proposed rule the battery exits 2 -- including `--selftest`, which `tests/test_toolchain_534.py:485` asserts exits 0 with the literal `SELFTEST PASS`. The rule also needs `git rev-parse --git-dir` executed with **`cwd=work`**, not the process CWD, or a battery launched from the repo root against a `/tmp` tree writes its journal into *this* repo's `.git` and the residue in `/tmp` is invisible to the next preflight. Specify: git-dir resolved from `work`; non-git `work` -> journal in a tmp dir keyed by the resolved work path, not a refusal.

Sound: the SIGTERM-vs-SIGINT measurement, the `SigIgn` false-probe disclosure, and the argument that a partial trap converts a reliable failure into a rare silent one. Ship (a) as written.

---

# ITEM 7 -- the 12-tool retirement / bd-coretest / bd-tool-lint

**Verdict: the sequencing call (gates before retirement) is right. The bd-coretest fix closes one of several ways the harness is indistinguishable from a refusal, and RED test 2 contradicts fix (d).**

**F7.1 (high) -- an existence probe does not repair the predicate.** Verified `bd-coretest:22-30` (`subprocess.run([sys.executable, exe, ...])`, harness exception -> `return 99`) and `:151`:

```
rec("bd-pack", "FAULT->refuse", (rc != 0 or "FAIL" in out or "missing" in out.lower()), ...)
```

`rc != 0` is satisfied by **any** harness failure, not just a missing file: a SyntaxError, an ImportError, a missing dependency, a `NameError`. After the proposed fix, *deleting* a tool is caught and *breaking* one is still graded a correct refusal -- and that is not hypothetical, because the plan's own evidence records `bd-pack:169` carrying a live undefined name (`TRACKER_FILES`) today. So the very tool used as the worked example can currently PASS its fault case by crashing. The repair the lens demands is a **positive marker** from the tool's own output (or a discriminated harness signature: python's "can't open file", a traceback with no tool banner), not a wider set of pre-flight file checks. The existence probe is worth having; do not let it be mistaken for closing the class.

**F7.2 (medium) -- RED test 2 and fix (d) cannot both be satisfied.** RED test 2 asserts `sorted(cc["missing"]) == ["bd-blast","bd-suites","bd-touched"]`; fix (d) seeds exactly those three into `RETIRED_CRITICAL_CORE` so the tree "goes green honestly". If retired names are excluded from `missing`, the RED test can never go green after the fix. Define it explicitly: `missing = absent ? not tombstoned` (the failing set) and `retired_absent` reported separately (non-failing, so the tombstone is visible in the JSON). Then RED test 2 asserts on `retired_absent` today and on `missing` after a synthetic deletion.

**F7.3 (low) --** the blocker is correctly identified. Note the plan's own reconstruction is 11-of-12 and it says so; do not let cut 2 start from the arithmetic fit.

---

# ITEM 8 -- bd-band `band_env()`

**Verdict: the code fix is correct and well-argued. The gate that is supposed to keep it fixed reproduces the shape at a larger constant.**

**F8.1 (high) -- `_RUNNERS` is four hand-named tools; the population is 239.** The plan's own diagnosis is that the sister gate failed because *"its denominator is bd-env-parity ONLY, so bd-band was invisible to it. Section 0 shape."* Widening 1 -> 4 by hand is the same instrument with a bigger constant. Measured now across `toolchain/bin/`:

- 66 files contain `/home/claude` or `/tmp/prestaged_site_packages`;
- and at least one is **live code doing exactly the banned thing** -- `toolchain/bin/bd-sbcap:46` `PRESTAGE=/tmp/prestaged_site_packages` and `:143` `_pwcache=/home/claude/.cache/ms-playwright` (used at `:144` to build a `PLAYWRIGHT_BROWSERS_PATH=`), plus `toolchain/bin/bd-repin-dist:195-196` defaulting `--work`/`--out` to `/home/claude/*`.

Some of those are zip-era tools queued for retirement (item 7) and some may legitimately name the retired sandbox -- which is *why* the gate needs an enumerated denominator (all tracked `toolchain/bin` + `project-knowledge` python-shebang files) plus a **dated allowlist with a reason per entry**, rather than a four-name include list that quietly excludes 235 files. As written, the cut leaves a live `PLAYWRIGHT_BROWSERS_PATH=/home/claude/...` in the tree and ships a gate that certifies the class is closed.

**F8.2 (medium) -- the model test being copied silently skips a missing subject.** `tests/test_toolchain_534.py:794-796`:

```
p = os.path.join(root, "toolchain", "bin", name)
if not os.path.isfile(p):
    continue
```

If a renamed or retired runner drops out, the gate reports OK over a smaller denominator -- and item 7 is a live proposal to retire tools. Copying the pattern copies that. Assert the denominator: every enumerated name resolved, and the offender-scan set is non-empty.

Sound: the AST-vs-grep measurement (16 offenders -> 12 false vs 4 true) is exactly the right discipline and should survive review intact; the `rundir`-not-`a.work` argument is correct and load-bearing; the honest "no test suite currently flips on this" statement is the right call.

---

# ITEM 9 -- capture.sh commit identity

**Verdict: the identity snippet's three-state design is right, including the MISMATCH branch (the best self-audit in the batch). One rider is fatal as written.**

**F9.1 (high) -- the banner rider aborts the capture at step [1].** `capture.sh:53` is `set -u  # don't set -e: we want all 9 steps to run even if one errors`. The plan says *"Also add `commit : ${_bd_sha:0:12}` to the stdout banner at capture.sh:325-332"* -- and I read both regions: the banner is at `:325-332`, `_bd_sha` is not assigned until the sysinfo block at `:336+`. Under `set -u`, expanding an unset variable in a non-interactive shell is a **fatal** error: the whole capture dies at step [1], in the cut whose purpose is making the bundle identifiable. Either compute the identity vars before the banner (and have the sysinfo block echo the already-set values) or write `${_bd_sha:-UNKNOWN}`. The main snippet is safe under `set -u` -- failing command substitutions leave the variables set-but-empty, and every use is guarded -- so this is the rider alone, not the fix.

**F9.2 (medium) -- the UNKNOWN branch asserts a reason it did not earn.** `commit : UNKNOWN -- $PWD is not a git work tree` fires for every non-zero `git rev-parse`: no git binary on PATH, `safe.directory` / dubious-ownership refusal (realistic -- capture.sh runs `sudo systemctl` around it), a corrupted index. In a cut whose whole thesis is that a bundle must not assert what it cannot verify, the failure branch hard-codes one diagnosis for a set of causes. Capture `rev-parse`'s stderr into the log (the block already redirects `2>&1`) and label it "git could not answer: <reason>".

**F9.3 (medium, cut 2) -- the proposed curl line destroys the evidence its own error branch needs.** `curl -fsS ... > "$OUT/07b_selftest.json" 2>&1` sends curl's diagnostics **into the JSON file**, so a connection failure produces a file that fails the parse check with the actual reason overwritten. Send stderr to `07b_selftest.log`, keep the JSON file JSON-only. Everything else about cut 2 -- particularly the `(ok+warn+fail) >= 1` non-empty-denominator assertion and the parameterized fake curl that must be able to disagree with itself -- is correctly specified and is the right shape.

---

## Cross-cutting

Two patterns recur and are worth naming once:

1. **Four plans propose a gate whose denominator is a hand-written list of names** (item 8's `_RUNNERS`, item 7's `CRITICAL_CORE`/`CORE_TOOLS`, item 2's SOURCE_DIRS replacement, item 4's owner resolution order). Only item 2 derives it from the tree. A hand-list is how every defect in this batch survived; three of the four fixes re-adopt it at a larger constant.
2. **Three plans propose to copy an existing "good precedent" without auditing the precedent** -- item 2 copies the @860 notice (mode-gated, silent in `--json`/`--emit`), item 8 copies `test_no_runner_hardcodes_the_wrong_interpreter` (silent skip on a missing subject), item 3 copies `bd-opv:1400` (whose zero-pass guard contradicts the env excuse it is being grafted onto). Read the precedent's failure branch before reusing it.

Nothing in items 1, 5, 6 (part a), or 9's core snippet needs redesign -- those four are the closest to shippable, in that order, once F1.1, F5.1, F6.1 and F9.1 are addressed.
