# CONTRACT_RULES.baseline -- paragraph conservation for the contract corpus.
#
# Generated by `bd-contract-rules --extract` from CLAUDE.md. Each row is a
# whitespace-normalised, case-sensitive sha256 prefix of ONE paragraph,
# plus an excerpt for a human. The excerpt is never a success oracle.
# The gate asserts every ACTIVE row is still a paragraph somewhere in
# the corpus (CLAUDE.md, project-knowledge/CONTRACT_CASEBOOK.md), so MOVING a paragraph passes and
# deleting or rewriting one FAILS until it is declared below.
#
# IT MAY ONLY SHRINK BY DECLARATION. Move a row under DECLARED REMOVALS and
# put the reason on the same line -- for a dedupe, name the paragraph that
# survives. DO NOT regenerate this file to clear a failure: regenerating is
# how a lost rule becomes the new baseline, which is the shape bd-mutate hits
# when a battery starts on a dirty tree and adopts the mutant as pristine.
#
# paragraphs: 308
# Recomputed for v3.66.1163 after the comparison became case-sensitive: all
# 308 current CLAUDE.md paragraphs remain ACTIVE. At v3.66.1164 five reviewed
# task-authority pointer rewrites were added to the four historical declarations
# below rather than being erased or silently adopted.

954cda71a521  # CLAUDE.md — operating contract for BulkDownloader
7b7d54555a40  You are working on **BulkDownloader (BD)**: a self-hosted Flask + Playwright + React/TypeScript
1b4313a22f3b  **This sentence read "single deployment target: headless host `test4`" for the whole life of thi
19cfc8837bc0  Older prose here, in `project-knowledge/`, and in the SDD reports calls that box `stash`. That i
1469c959369a  **A SECOND BOX NOW EXISTS, AND IT IS A DIFFERENT CLAIM FROM THE ONE ABOVE.** The operator is sta
5f8851f8c889  **THE BROADER CLAIM THAT USED TO SIT HERE WAS FALSE, AND ITS DENOMINATOR IS THE LESSON.** It rea
e78b5db9e84e  The glob is why. `bd-jobs` is one of the tracked, extensionless, python-shebang `toolchain/bin`
dce8adc43323  It cost a real session: `bd-jobs run --host test6` was refused with "test6 has no bd-jobs — depl
928804300818  **So a reading is now about a HOST as well as a commit.** Section 2b already says a finding is a
87815ea4ff8c  Read this file fully before your first edit. It encodes rules that were learned by breaking thin
cf5e67283345  ## 0 | The one rule that generates most of the others
6979f9a296f9  **A gate that cannot see the thing it is asked about reports OK — and that is worse than no gate
0fe1dd523993  A check asserts over a denominator that structurally excludes its subject, so it reports clean:
b7f86f7f6d56  - A band tool didn't count `.tsx`/`.ts` as source, so it reported "changed source (0)" on a real
89d5000fcad8  **Fix pattern, every time:** make the denominator contain the subject; derive reachability rathe
6c8302eb7d46  **The inverse is equally damaging: a gate that fires on identity.** A manifest pin once hashed b
b7b802f6b038  **WHEN THIS FILE RECORDS A MISTAKE CLASS, CHECK THE CHANGE IN FRONT OF YOU AGAINST IT — BEFORE C
dfbcd274ea52  **THE FIX REPRODUCES THE SHAPE OF THE DEFECT. Audit it before you ship it.** This is the highest
6e31f1f07b78  - `tools/check_requirements.py` exists to replace `pip check`, whose denominator is the set of I
890163aa2c4d  The last two were caught by the fix's OWN non-empty-denominator assertion, on first run, before
78d650b73c6e  **CREATING A PATH IS A PROMISE TO REMOVE IT, AND NOTHING GATES THAT PROMISE.** Measured at v3.66
1e9525765ac2  The shape generalises past this instance: an artifact created unconditionally and consumed condi
bfc06daf3c23  **A TOOL BUILT TO PREVENT A FAILURE CAN MANUFACTURE IT — SO VERIFY YOU CAN RECORD BEFORE YOU ACT
3e4eb71e8338  The ordering is the rule and it generalises well past ssh. **When an action has a side effect yo
373c458d2d44  **A COMMENT IS INSIDE THE DENOMINATOR OF EVERY GATE THAT READS SOURCE TEXT.** Four times in the
76f27a03ea5d  - a test forbade a literal that its own explanatory comment contained; - a comment quoted the de
cee265cdf961  **And a line-scoped assertion about a shell LOOP is wrong in both directions.** Three times acro
ae3e7be9c04a  Explaining a removal by naming the removed thing recreates it. Assert over **comment-stripped**
4e40867e1579  **A SUBPROCESS HARNESS THAT COPIES `os.environ` CANNOT TEST THE ABSENCE OF A FLAG THE BAND SETS.
e397888936f8  Generalise it: **a test that varies an environment variable must POP that variable, not merely r
193c0e678387  **TO ASK WHETHER IMPORTING SOMETHING TOUCHES A RESOURCE, MONKEYPATCH THE RESOURCE -- DO NOT READ
a14962d593f7  ```python _real = sqlite3.connect def traced(*a, **k): if str(a[0]) != ":memory:": hits.append(t
07fd575714fd  Run it with the gating flag both set and unset. Three of the four sites here were invisible in o
e2baa3235be6  **THIS FILE IS IN THE GATE'S DENOMINATOR AND ITS CLAIMS STILL GO STALE — AND THE PARAGRAPH THAT
baf298a2ac2c  ``` bd-freshcheck : _DOCS names CLAUDE.md and project-knowledge/IMPROVEMENT_BACKLOG.md bd-doc-tr
a3e64a783877  **But the conclusion survives the correction, because the denominator is not the predicate.** Bo
9b850bb95721  That was caught only because a register re-derivation **ran the tool instead of quoting the note
9e4f3f23c492  **Reading this section does not inoculate you against it.** The same session that wrote the five
bc188048debc  ## 1 | The method rule
94a7db2418e9  **Documents go stale silently and are then read as authority.**
901854ae89ce  Every figure taken from a register, tracker, changelog, or grep has at some point been wrong. Ev
b1c115a61219  - **Verify-then-act.** Before working any queued item, re-derive its status from source. Histori
a89754c8bddc  ```bash git ls-files -- '*.py' | wc -l git ls-files | while read f; do head -1 "$f" | grep -q '^
be489b52d5e5  **THE NUMBERS THAT USED TO SIT HERE ARE GONE ON PURPOSE, AND THE REASON IS THIS BULLET'S OWN HIS
f4b13f2e1e1a  This is not a small distinction. At v3.66.1072 a false claim in this file's own front matter — "
085bf151064a  **The word "executable" was WRONG here for weeks, and the way it was wrong is the point.** This
7358970624e8  *(Unverified: an earlier revision claimed "seventeen tracked shell scripts also embed Python her
1323b9e07769  **HIT AGAIN at v3.66.1077, hunting leaked tmpdirs.** A census asked "does this file clean up aft
1d48f9b84bc7  The reason is section 0's, applied to introspection: recollection's denominator is "what I remem
f61831294db9  ## 2 | Release discipline (non-negotiable)
c6d32c02f127  1. **RED-first TDD.** Tests proven failing on pristine source *before* implementation. A test th
f6f9557a6957  | File | sha256 (16) | | --- | --- | | `bulk_downloader/extraction_core.py` | `5b6248a5c9e664ab`
d4e5798a6547  *(Pinned at v3.66.805. Re-derive with `bd-guardcheck`, which reads `guards.json` — the single so
f3093718fa14  Until v3.66.818 `bd-guardcheck` reported `0 ok, 0 drifted, 7 missing` and **exited 0** on a clea
72c120215ad1  6. **MEASURE THE COST OF ANYTHING YOU ADD TO EVERY TEST RUN.** An autouse fixture, a conftest ho
c6c0473a0425  Before packaging a change for review, regenerate all tracked artifacts from the repository root
cb63a865028d  ```bash venv/bin/python toolchain/bin/bd-regen-order --work "$PWD" ```
20bdb3dab72f  This command does not re-freeze intent baselines; declaration flags remain explicit operator dec
1cc05fd43550  ### 2a | The cut-to-merge loop: failures that recur
b5aaec83e8b2  Every rule below cost a red CI run, a red capture, or a wasted round trip during 2026-08-01/02 (
2252b0e0dacf  **Regenerating artifacts.**
8ef5662b6dd2  - **Regen AFTER the last source edit, not before.** `bd-regen-order` was run, then one more line
3b13c56cbe52  **Gates that cannot see untracked files.** Most axis-6 gates enumerate `git ls-files` (section 4
156349fbe6af  **Git hygiene after a merge.**
20c7e99fd76d  - **`git fetch --prune` must accompany every post-merge reset.** GitHub's auto-delete removes th
f4f331055845  **Test harnesses that misreport themselves.** Both of these make a harness defect look like the
e9caabe4fe84  - **Discriminate the exception you are hunting.** An end-to-end probe caught `AttributeError` an
47c62edc260a  **Subagent output is data, not evidence.** A verifier returned `{"status":"PASS","summary":"test
803467054397  ### 2b | Running work across several agents
2d36e25d7875  Earned at v3.66.847/848, where nine workflows ran against one branch. Every rule here cost real
706a322c86b6  **A subagent worktree comes up at the SESSION'S BASE COMMIT, not at your branch tip.** Measured:
6927866dc508  - Two mutation agents found the feature absent, **reimplemented it themselves**, and mutated the
b769d1a93537  A preflight of "the tests pass" does **not** catch this: on the old tree the OLD tests pass, so
361d5eb438c1  ```bash git fetch origin <branch> && git checkout --detach FETCH_HEAD git log --oneline -1 # say
4364cec89c25  Assert the symbol is PRESENT before measuring anything. And note `venv/` is gitignored, so it do
73a90a095a51  **Never `git add -A` while another agent is writing the tree.** This is now enforced rather than
273ca3266c39  The failure it models: a regen commit swept a concurrent workflow's uncommitted RED battery into
3ffe3983c641  **A NEAR-MISS AT v3.66.926 SHOWS WHAT THAT RULE ACTUALLY BUYS.** A review agent running a mutati
a2e83dad0eab  ```python # --- MUTANT (latch): boot at module scope unless the flag is set --- if not _os.envir
64a53a340391  That is a verbatim re-introduction of the defect the cut had just removed. It landed between a `
0a4cf011612d  **`git fetch --prune` does NOT delete the LOCAL branch.** After a squash merge the remote branch
21cf32bcc542  ```bash git fetch --prune origin && git checkout -B main origin/main git branch -D <the merged t
22a575dc119b  Nothing in the repo catches this -- the stop hook's "unpushed commit on main" did. And the repai
0beb0c212eb6  **A finding is about a commit. Say which one.** Four bands, three mutation batteries and five re
466b5755c9d7  **Give an agent the deviations, not just the plan.** The generated spec for that cut had failed
cf11f875fa5d  **A green band is not the absence of a regression.** v3.66.834's band was green while the cut ha
b1b90bcf4da0  **Say which question you measured.** v3.66.837 claimed six call sites were "live, MEASURED not a
b5fddbc30242  ## 3 | Version bump = three edits, together
efc9c87ea8d9  Never bump one without the others:
4aba90765c41  1. `bulk_downloader/__init__.py` → `__version__` 2. `tests/test_settings_center_slice4.py` → the
60c1ba032e8c  Then regenerate `PIN_INDEX` (`venv/bin/python tools/build_pin_index.py`) and read the `form == "
db29ee6a7cef  Do **not** reach for `grep -rnE '__version__ *== *"3\.66\.' tests/` here. Measured at v3.66.838:
7ba509553e57  CHANGELOG entries must be **ASCII-only**; an emoji trips a gate on the box.
b3b2f61959c4  ## 4 | Band rules (which tests to run)
b3224583afb1  **A denominator change has the blast radius of the denominator, not the diff.** Band every test
22b547f51ca3  **Derive it with `bd-band-derive`, NOT by hand.** This paragraph used to say "derive it with `gr
1dda22ebcfa9  ```bash venv/bin/python toolchain/bin/bd-band-derive --file <changed file> # one file venv/bin/p
f61b0fd63025  **RUN THE BAND WITH REAL PYTEST. Deriving is one question; running is another, and the running h
8b9859e4529f  ```bash BD_DISABLE_KEEPALIVE=1 venv/bin/python -m pytest <the derived files> -q ```
bbb8b5b54b0c  `bd-band` and `bd-parband` now do exactly that and are fine to use. Before @950 they shelled out
4e476409c234  Measured at v3.66.949 across the whole suite, per-file isolation on both sides:
acf123e570bc  | | files | | --- | --- | | the shim reports non-PASS | 28 | | of those, PASS under real pytest
c508f5a78a26  **86% of what the mandated band tool reported was manufactured.** One file, both runners, same t
901b7755f2e8  The tell was available the whole time and nobody pulled it: `bd-band` already **refused to run**
8a7d32f6db55  *(A claim made while finding this and then retracted: the shim does NOT also hide a real failure
858322d9e555  | method | suites | missed | | --- | --- | --- | | `grep -rl live_seed tests/*.py` | 17 | — | |
6e3478bd4ad3  A strict superset: 8 files the grep could not see, none dropped. It unions four signals, and `gr
e958521bdaba  **A FLOOR MEANS YOU MAY ADD TO IT. IT DOES NOT MEAN YOU MAY DROP FROM IT.** Measured at v3.66.10
0b6f9daac460  Its output is a **floor, not a ceiling**: it says so itself, and everything else in this section
c7a6873e53d8  - Route change → band **both** `test_route_index_in_sync` **and** `test_route_map_invariant`; re
870fd4b7f221  ```bash PYTHONPATH="$PWD" venv/bin/python tools/route_map_snapshot.py \ > tests/route_map_baseli
45d0e46c3d84  The redirect is required — `route_map_snapshot.py` writes to **stdout only**, it does not write
b28ec10f9a0f  **INCLUDING AN EDGE FROM A TEST FILE, since v3.66.889.** The gate used to walk `bulk_downloader/
7b20e7dcdb73  **So `test_import_graph_no_new_edges.py` is now an axis-6 gate too** -- a cut that touches no so
9c71915f69d7  ```bash grep -rln "def <function_you_touched>\|<function_you_touched>" tests/*.py ```
335397e15c57  Measured 2026-08-02: v3.66.835 added an 8-line comment to `start_manual_login`; `pause_site_keep
09016b4ad838  **Editing a DOC or a REGISTER bands the freshness gates, and no module-derived band reaches them
12e28307b075  Two failure shapes, both hit in one cut:
f40de9c814d3  - **An anchor to an untracked path can never resolve.** Citing the launcher's `stop-hook-git-che
d3ec302f70ce  **Writing the bad form as an EXAMPLE is itself the bug.** The first draft of this bullet quoted
457185418df7  **BUT NEVER NAME YOUR OWN UNMERGED BRANCH TIP: the squash DESTROYS it, and the gate goes red on
737b4a6e5b26  15.58 survived the identical treatment because it named `b24e675`, the squash commit of the PREV
e084be4b729a  Nothing catches this before the merge, by construction: every pre-merge check runs on a tree whe
2e880e6a5e52  So when a cut touches `CLAUDE.md`, `project-knowledge/**`, or any tracked doc:
9a37986ce6e9  ```bash venv/bin/python toolchain/bin/bd-freshcheck --repo-only # exit 0, or fix the claim venv/
7cf3d0361d41  **Naming trap:** `test_spa_wired_join_is_faithful` is a *function inside* `tests/test_route_inde
a4fbfbabb8da  **Leak trap:** `test_phases_195_199` leaks `BD_INSTALL_DIR` — never co-band it with `test_cut8_s
aebda98bb8b3  **The tests are a denominator too.** Some tests take *every tracked test file* as their subject
fb709ab6e0a3  This is not hypothetical: `test_source_windows_do_not_shift.py` ratchets the count of fixed-widt
fd9b4c51fb3d  **No automatic predicate here — read.** Three attempts at an AST rule to find these returned 76,
80a2d585baf8  ```bash grep -rln "ls-files\|rglob\|--collect-only" tests/*.py # 82 hits; read them all ```
276bc2737110  Then keep only the ones whose enumerated path reaches `tests/` or the whole repo. **The membersh
35674721c6ca  **COUNT THE `yes` ROWS OF THE TABLE BELOW; DO NOT TRUST A NUMBER IN THIS SENTENCE.** Four succes
52ab8084e257  | file | enumerates | moved by a new test file? | | --- | --- | --- | | `test_source_windows_do_
de1058585885  The NO rows are the distinction worth keeping. Each runs a genuine repo-wide enumerator and then
577e1947a6ce  **A tenth gate is axis-6 and the grep above cannot find it, because its enumerator is not in the
827b30c165e5  **A GREEN BAND AT ONE `-n` DOES NOT RETIRE A CROSS-FILE FAILURE.** Measured over v3.66.1042-1045
2c44790cefc1  So the failure set is a property of the **schedule**, not of the tree, and three reflexes are wr
3d757d847ebb  ## 5 | Environment traps
ba23083970a2  - **`test_v3_43_80_modules::test_all_modules_import` is environmental, not a regression.** It fa
c9d0d30659d8  **The method trap hit while proving it is worth more than the finding.** `git stash push --keep-
7f4d57d42637  - Three Python resolution paths exist (system / prestaged / service venv) and they carry **diffe
d1076420bb34  - **Never run the whole `tests/` directory locally — but BOTH of the reasons this rule used to g
f117002d85d2  | claim this rule rested on | measured | | --- | --- | | `test_perf_lab.py` is *the* recorded ha
afdd06b1e8af  A targeted sweep found no hang either: **79** files carrying hang-prone shapes (`while True`; `.
cbf4b56a3a7c  **THE CIRCULARITY WAS BROKEN BY AN OPERATOR EXEMPTION, AND THE SUITE DOES NOT HANG.** The untest
9468fad5dd86  ``` 14 failed, 14943 passed, 91 skipped in 635.42s (10m35s) # 4 workers tests exceeding the 240s
9ffa06001c7a  The 14 are the documented container-only set (`test_e2e_smoke` ×7, the `no_backend` body-contrac
5c62851e2194  **SO THE SWEEP IS PERMITTED, IN EXACTLY ONE FORM. Never bare `pytest tests/`.**
1dc9a9987287  ```bash BD_DISABLE_KEEPALIVE=1 PYTHONUNBUFFERED=1 venv/bin/python -m pytest tests/ \ -n 24 --dis
1eb5b52964c7  Every flag is load-bearing and a different one is a different experiment: `--timeout` is what tu
d1f4e2810279  **`-q` WAS REMOVED FROM THIS FORM AT v3.66.1126, AND `-u` ADDED. BOTH ARE OBSERVABILITY FIXES FO
5aa2d92ad1e3  `-q` sets `verbose == -1`, and xdist guards its entire crash-recovery narration behind `verbose
14b9c464695a  ```python # xdist.dsession.DSession.report_line, as of pytest-xdist 3.8.0 def report_line(self,
80ba32503e6d  (Symbol and pinned version, NOT `file:line`: these live in `venv/`, are not tracked here, and a
a79db49c798a  So under `-q` these NEVER appear: `replacing crashed worker gwN`, `maximum crashed workers reach
6b9f24eeb6df  `PYTHONUNBUFFERED=1` exists because a run that never exits never flushes. It is spelled as an EN
5cba5ece0511  **THE GENERAL RULE, because two independent blindfolds here produced the identical symptom and f
50af3aa1ad46  **`-n 24` IS THE FIXED CANONICAL COUNT; HOST CAPACITY DOES NOT REWRITE THE EXPERIMENT.** Post-Cu
bca1b4bbfb86  **AND MACHINE LOAD DOMINATES THE FAILURE COUNT, WHICH MAKES A SINGLE SAMPLE UNINTERPRETABLE.** M
adc17fee4cea  Three single samples were each read as signal in one session (19, 27, 51) and all three were noi
adebdc05ba12  **WHAT THIS DOES NOT LICENSE.** One ordering was measured — `-p no:randomly` with `--dist loadfi
f3514d6606c0  Note also what the sweep's own instrument got wrong: the candidate list was written without a tr
48510ea8de68  **Match on what the process IS, not on what its command line SAYS**: `ps -eo comm=,args=` and re
db2627c76db0  - **`BD_HOME` does not protect `~/.config/bulk-downloader`**, which resolves from `$HOME`, not `
682f7db21a8c  **`BD_HOME` does not govern the database either — `BD_INSTALL_DIR` does.** `db._resolve_db_path(
f9c59d4e3349  Rows then accumulate across probe runs, and the next probe reads them. That is not cosmetic: a p
d011a27730a8  `tests/conftest.py`'s `clean_workdir` gets this right and is the model: `monkeypatch.chdir(tmp_p
d19812bae276  **THE CONCRETE FORM, because the paragraph above did not stop it happening.** Item 36 was opened
f034b7f1b162  ```bash export BD_INSTALL_DIR="$(mktemp -d)" # NOT BD_HOME -- it governs nothing here venv/bin/p
fdf38e299422  **AND THAT LINE IS FOR A PROBE. NEVER EXPORT IT INTO A `pytest` RUN.** The two halves are opposi
3538dd67ad04  Measured again at v3.66.1024, same tree, same directory, the variable the only difference:
d958194ca8b7  | file | exported | popped | | --- | --- | --- | | `tests/test_provision_test_host.py` | 4 faile
2621624235ef  Both were reported to the operator as pre-existing order-dependent failures, "proven" by changin
07800fc5e17d  **The form for a band, then, is the variable POPPED:**
73eeba476d31  ```bash env -u BD_INSTALL_DIR bash -c 'BD_DISABLE_KEEPALIVE=1 venv/bin/python -m pytest ...' ```
ae04a0782ae4  `env -u` rather than "just don't set it": your shell may already carry it from an earlier probe,
87152794d1a2  **A `conftest.py` guard for this was built and DELETED at v3.66.1024, which is the more useful h
2e4de9eea372  **No gate can catch this, and know why before proposing one:** refusing a database path that res
e48d3ac3172b  | reading | rows | | --- | --- | | `file:db?immutable=1` (WAL ignored) | 114 | | plain `sqlite3.
bb277060f5ee  The first was a survey of quarantined databases, where `immutable=1` is exactly right — it takes
2cb7ea4311b8  **Rule: `immutable=1` for surveying a file you must not touch; a normal open for asserting what
d016ad034f05  **And name resolution is not version satisfaction — FIXED, and this paragraph was stale about it
74af2e7a446b  It now builds `Requirement(line)` and asserts `req.specifier.contains(have, prereleases=True)`,
01b18e4787de  **Kept rather than deleted, because the staleness is the lesson.** This file asserted "Open, and
bf58bd5b0395  - **IN A SHALLOW CLONE, ONLY `--is-ancestor` EXIT 0 IS TRUSTWORTHY.** A 0 means a connected path
82aa19ac2629  **Never repair a 128 with a by-sha fetch -- not because it fails, but because it SUCCEEDS INTO A
53708ec19847  Nothing runs on the happy path -- the first `is-ancestor` short-circuits, so a clean tree does n
5b114ebdc5da  - **`requirements-dev.txt` does not resolve in a cloud container, deliberately.** It carries the
b91b965a0ab5  **Provisioning a test host.** `scripts/provision_test_host.sh` is the one command that takes a f
9f2bfe9f62ba  The graph pin is the newest step and the least obvious. `capture.sh` step [2b] compares the rebu
22251135455e  Re-pin by hand after any source change, or step [2b] reports drift and `capture_verdict.py` turn
93fb52f129e5  ```bash GDB=$(mktemp -d)/KNOWLEDGE_GRAPH.db venv/bin/python tools/l0_extract.py --root "$PWD" --
a729b8160847  Only the second command takes `sudo`: `--write-hash` sets `projection_mode` false and returns be
0406cfd87355  `scripts/lib/system_deps.sh` is the **single source of truth** for system packages; `install_lin
bd27b1d2c613  ## 6 | Any tool that rewrites source must verify after writing
570e55fb78fa  Parse the result (`ast.parse`), confirm the expected content is present, and **restore the origi
84cd0b3e10b4  **`ast.parse` is not name resolution.** A file referencing an undefined name parses fine. Check
29e7f876389f  **The applied-check: use length arithmetic.** Every edit or mutation asserts `src.count(old) ==
d0d616c04b28  | check | fails silently when | | --- | --- | | `new in after` | `new` already occurs elsewhere
1af64c587efe  All three were hit, the last two inside a single mutation battery. Use instead:
c780ba08c722  ```python assert src.count(old) == 1 after = src.replace(old, new, 1) assert after != src and le
5e0fe94246d0  Length arithmetic is exact for one replacement of a unique anchor and cannot be fooled by substr
93b83ebda933  **AN EDIT SCRIPT MUST MUTATE IN MEMORY AND WRITE ONCE, AT THE END.** Measured at v3.66.1035: a t
dc2e50a48f4d  **AND NEVER RETYPE AN ANCHOR THAT CONTAINS PUNCTUATION -- `rg` THE LITERAL LINE AND PASTE IT.**
ce01741bd387  **`sed -i` IS NOT AN ACCEPTABLE APPLIED-CHECK.** It asserts nothing about uniqueness, so it will
ddd376da55e9  **A mutant that does not parse is INVALID, not caught, and not escaped.** Deleting a line can or
50d720935f2b  **Use `bd-mutate`; do not rebuild the harness.** Every rule in the rest of this section is mecha
14ac4456051d  ```bash venv/bin/python toolchain/bin/bd-mutate --spec mutants.json \ --band "tests/test_a.py te
d15054ea4be6  `mutants.json` is a list of `{label, file, old, new, catcher?}`. It enforces the unique anchor a
27ea49d532bb  **An interrupted battery does NOT restore the tree.** The restore is a `finally:` in the per-mut
4afae2c0d7a0  **A second run then launders the residue into the baseline.** `bd-mutate` reads the "original" i
dfa59abd22f3  **@875 added an on-disk journal, and you need to know what it does not cover.** Before the first
ffb6b9fde3ac  **If a battery did not print its summary, `git status` before you believe anything, and before y
8d5e368de7b1  **Stale bytecode defeats a same-length mutate-and-restore.** Python invalidates a `.pyc` on `(mt
b422414969ea  **Closing a mutation escape means proving RED in BOTH directions.** The test must FAIL with the
91b5139b201e  **A HARNESS MUST ASSERT THAT IT BUILT THE SHAPE, BEFORE IT ASSERTS THE VERDICT.** Three times in
218ce32614da  - a fake `ps` printed a PID column that `ps -eo comm=,args=` never emits, so it certified a dete
0d9c5adb8247  The fix is one line of discipline: **before asserting the outcome, assert the precondition** --
a86f5073b423  **An escape can be a HARNESS defect rather than a missing test, and those are the ones that surv
7255796cfa51  ## 7 | What git changes, and what it does not
1fa712bb083a  This repository is new. Most of BD's tooling was built in a world with **no version control**, w
eba5a684aeca  **Now genuinely easier:** diffing work against a baseline (`git diff` replaces zip comparison),
b740c827815c  **Changed — the deploy path is now git.** The box updates with `git fetch origin main` + `git re
6f2861e6a417  **Unchanged — do not assume git fixed these:**
3599dc353bc5  - **A deploy moves files. It does not make the running system match them.** Four gaps survive th
5d98adc51efc  **`scripts/deploy.sh` now does all of this (v3.66.848), and you should use it.** It fetches and
200272c8513a  **A FAILED DEPLOY IS NOT A NO-OP, AND IT CAN LEAVE THE SERVICE DOWN.** The script stops the unit
c37dbcdeaf53  **One property of it matters before you edit it: every step after `git reset --hard` executes th
829a3ed7c769  **In a *cloud container* none of this applies:** `scripts/cloud-setup.sh` runs `npm run build` a
b73728480685  **BUT THE GATE HAS A NAMED BLIND SPOT: `capture.sh` CANNOT SEE CROSS-FILE STATE LEAKS.** Measure
c1e932425958  **What it actually runs, re-measured 2026-08-08 at v3.66.958. THREE jobs, not two, and the gate
9586070327cd  - `postgres-integration` -- four mod3 files. - `gates` -- gitleaks, the generated-artifacts sync
c2240d7917a2  **THIS SAID "a 5-way matrix" AND NAMED FIVE UNTIL v3.66.1131, IN THE SAME SENTENCE THAT TELLS YO
06d933ea2c68  **THIS BULLET SAID `gates` RAN "exactly ONE pytest file: test_v3_66_939_ci_gate_shards_cover_eve
09d298c60d50  Note which instrument found it: not reading this paragraph, which had been read many times, but
84f821175bed  **THE PRIOR TEXT NAMED A FILE THAT DOES NOT EXIST.** It said `test_pk_mirrors_do_not_drift`; the
e861eb20b279  Sharding adds exactly one failure mode -- a suite that falls out of every shard still leaves a g
7502e6e78aa1  **A GATE CI DOES NOT RUN IS A GATE THAT DOES NOT EXIST. `_DECLARED` was HAND-PINNED and could no
85ec69bdf398  The mechanism now: every tracked `tests/test*.py` file either declares a module-level `BD_GATE_S
7b81c2b1c452  Do not try to replace the marker with a derived predicate; it was measured and it does not work.
f41f32dc1c24  **The prior text was written at v3.66.847 and was correct then**; @849 added the lane and nothin
242bda533ead  **What is still true, and is the point.** CI's denominator is file-INDEPENDENT: gates whose subj
3d0477c1d518  **READ CI STATUS FROM THE STATUS COLUMN, NEVER FROM A POSITIONAL FIELD.** `gh pr checks` prints
2b8084b6a9b2  **The one-minute budget and its remedy.** The step's comment set the rule on 2026-08-03: "81 tes
d50cc362af81  **The post-deploy checklist, and the step that keeps getting left off it.** A v3.66.824 handoff
07b20239bd82  ```bash cd ~/BulkDownloader && ./capture.sh --workers=$(nproc) > /tmp/capture.log 2>&1 echo "exi
b9a08b9244ea  `--workers=N` is parsed by `capture.sh` (grep the literal `--workers=*`; the anchor has moved on
1446941942e1  **Box runtime facts a fresh session will otherwise guess.** Neither is derivable from this file
c46ca7f92047  | fact | value | source | | --- | --- | --- | | service unit | `bulkdownloader` | `install_servi
5131b5828d24  The timezone is load-bearing for anything comparing a stored stamp to a local date, and it is th
f13fc286ef91  The corollary matters more than the value: a UTC box **cannot reproduce** timezone defects, so a
c4861bae1857  There is no `/api/version`. The only version route is `/api/dev/version_check` (`app_dev_maint.p
153c4f0cd88c  **The squash-merge branch trap — CLOSED at source, 2026-08-01.** The repo now has GitHub's **"Au
cfe9d8a10b0f  **Verified by experiment, not by reading the setting.** Enabling it acts only on FUTURE merges —
0c29ecf20e9f  **The mechanism it fixes, kept because the fallback still needs it.** Squash writes a *new* comm
89a338e3ee6b  **When the sequence below is still required:**
90ec109daff6  - a branch **merged before 2026-08-01**, which nothing will collect. (The original population --
bae8e35d00c5  **`git bundle verify` IS NOT PROOF A BUNDLE CAN BE RESTORED FROM.** Measured on the box 2026-08-
7823b1ad25eb  ```bash T=$(mktemp -d) && git init -q "$T" && \ git -C "$T" fetch "$BUNDLE" 'refs/*:refs/restore
d2f22c3c3391  Do not treat auto-delete as a licence to `--force`. The sequence is: prove the content is alread
635725d848e6  ```bash git fetch origin main git diff --stat origin/main origin/<branch> # MUST be empty git re
2709035fb578  The two-dot `git diff` is the load-bearing step: empty means the remote branch carries nothing `
65d85f0af682  **gitleaks CI has two traps, and they compound.** `.gitleaks-baseline.json` holds **PATH-scoped*
043ef23aa1f6  1. **The baseline cannot suppress a finding on a line your commit touches.** Earlier PRs passed
8a0bba7ef224  Corpus values in a test that asserts about credentials must therefore be zero-entropy repeats, n
c07e739ed865  **GitHub's own merge commits are not yours to re-author.** A squash lands as `GitHub <noreply@gi
587612ce7a59  ``` bulk_downloader/ the application (.py) tests/ test files (+ corpus/ and fixtures/ assets) to
b9d21a8f5099  Sizes are deliberately not written here. Every count in this block has been wrong at least once,
edabf3877314  ```bash find bulk_downloader tools -name '*.py' | wc -l # per directory as needed find tests -na
4caceada5772  **This is the only agent-facing contract. There is no second one.** `CODEX_HANDOFF.md` was retir
94caacd1d8e6  **LOOK IN `toolchain/bin` BEFORE YOU HAND-ROLL ANYTHING.** Count them (`ls toolchain/bin/bd-* |
9ccbcf4b16c2  | question this file makes you ask | tool | | --- | --- | | what is the band for this diff? | `b
4171f28176c0  Do not treat that as the list — it is a starting point, not a denominator. It is only the ones t
6fb86da9b374  **AND READ THE DOCSTRING OF THE TOOL NEAREST YOUR PROBLEM.** Hard-won findings live there and th
38cbb18e8a5c  **BUT A DOCSTRING IS A CLAIM, NOT A MEASUREMENT — CHECK WHAT THE TOOL RUNS.** Measured at v3.66.
84641a65a551  Two questions, and the second is the one that gets skipped: what does this tool CLAIM, and what
ec33bc5bb00a  **A related shape, same cut: `bd-parband`'s selftest asserted its delegation target was PRESENT.
a381fc5ad782  **Two populations share the word "tools":** `tools/**/*.py` and the `toolchain/bin` bd-* suite.
09accd7d94bf  ## 9 | Working with Matt
d5e1e1ed2faa  - Terse directives ("go", "cut", "1", or a bare file upload) mean **full authorization within th
cfde3dac40ab  It is written here rather than only in the register because a standing grant cannot be re-derive
0bfe957da0c9  What did NOT change: `scripts/deploy.sh` is the mechanism (section 7 -- it clears `__pycache__`,
cd2050ad4e18  Know which host you are deploying. On test5 the deployed tree and the agent's working tree are t
19bd9b62b0cc  Two rules the lifting immediately created, both learned the same night:
70f3e8c756f7  * **NEVER run a capture on a host whose tree you are editing, and never edit it mid-run.** test5
0eed309fe634  - **ULTRACODE AND FABLE-5 AGENTS ARE AT YOUR DISCRETION, 2026-08-12**, with the operator's condi
7c09d86cfe1b  The failure mode to avoid is not cost, it is a confident wrong answer arriving with the authorit
e08f05253ddf  - These grants are written here rather than only in the register for the reason section 9 alread
a0826e1b60e7  ## 10 | Before you claim anything works
f91db3debe95  Run the check and paste the real output. "Should work", "looks correct", and "the tests should p
65e0e4486fbd  **NO ESTIMATED PROGRESS IN A STATUS REPORT. REPORT WHAT COMPLETED.** "About halfway", "nearly do
9d3e74653487  **VERIFY THE SUMMARY AND VERDICT LINES SPECIFICALLY — THEY ARE THE LEAST-TESTED OUTPUT AND THE O
1c3dd097b8a0  - `deploy_fleet.sh --dry-run` fell straight through to the success line and printed `all 3 host(
d82d987fb8fe  The body of a tool is exercised by its tests; the last line it prints is exercised by nobody. Re
db215bea6bd7  **TEST THE SEAM, NOT ONLY THE COMPONENTS.** `bd-jobs` shipped at v3.66.1040 with eleven passing
a8aa83e5dd09  **AND A GREEN BATTERY IS NOT COVERAGE EVIDENCE — CHECK WHICH PATH IT TOOK.** Same tool, measured
7f3252560f84  **AND WHEN EVERY REFUSAL SHARES AN EXIT CODE, ASSERT THE REASON, NOT THE CODE.** This one has no
ffefbf2c1223  Two things close it, and both are needed. Assert the distinctive words of the refusal you mean.
5401f491497f  **WHEN A FUNCTION HAS N OUTCOMES, ASSERT THAT EACH ONE IS REACHABLE.** `reap` has three — kill a
9f296f55db15  ## 11 | Context economy
b0c769963204  **A session that runs out of window loses the work, so the window is a resource like any other —
f2b1c8fac17b  The census that produced these rules, over 3,158 transcript rows of one session, chars/token cal
652a442d720c  | bucket | ~tokens | n | share | | --- | --- | --- | --- | | tool_result | 143,374 | 476 | 39% |
33f019a9dff0  Four rules follow from it, and the second is the one nobody expects.
bb20d5f6313d  - **A SWEEP OVER HOSTS OR FILES GOES TO A SUBAGENT.** Its tool output never enters the caller's
60485e495558  **The counterweight, so this is not read as licence to skim.** None of the above permits reading
567d6a775d09  **And measure before optimising.** Every figure above came from running the census, and the larg

## DECLARED REMOVALS

b70cc3e3eb4a  rewritten v3.66.1141: the count was deleted, not the rule. The directive 'LOOK IN toolchain/bin BEFORE YOU HAND-ROLL ANYTHING' and the `ls toolchain/bin/bd-* | wc -l` recipe both survive in section 8; only the stale figure and its history changed.
c325c8fc8afb  rewritten v3.66.1162: the sanctioned command survives verbatim except for the independently qualified fixed worker count, now `-n 24`; all timeout, distribution, ordering, environment, and observability flags remain unchanged.
41f9a214c5c2  superseded v3.66.1162 by the fixed-count paragraph in section 5: repeated exact-population two-host qualification selected `-n 24`; explicit `-n 4` survives as the comparison and regression oracle, and results still record the resolved count.
dfe31469bbb6  rewritten v3.66.1162: the stale container-specific `-n 4` explanation was removed; the timeout, distribution, whole-run cap, and written-exit-marker requirements survive in section 5.
0b4dc44bfc2b  rewritten v3.66.1164: the six-host warning survives; only its pointer to the retired session ledger became an explicit historical description.
24e09355f56f  rewritten v3.66.1164: the freshness denominator now names the sole canonical task register, IMPROVEMENT_BACKLOG.md, instead of the retired session ledger.
506ceaeefcbf  rewritten v3.66.1164: the document/register freshness rule survives; its worked example now uses the canonical backlog.
40ede145538a  rewritten v3.66.1164: the pre-setting branch recovery rule survives; Git history now owns the historical reconciliation rather than a deleted session ledger citation.
7ffd05de5185  rewritten v3.66.1164: the one-agent-contract rule survives; surviving work now points to atomic canonical backlog rows rather than the retired second task authority.
