# IMPROVEMENT BACKLOG -- machine-visible since v3.66.1052

ASCII-only.

## Why this file exists

It was **item 85 of itself**. The backlog was produced by a review that called
it "the most valuable artifact of that session", and it then lived in an
untracked text file in the operator's home directory that no gate read. CLAUDE.md
section 1 states the consequence in general terms:

> "A DEFERRAL THAT LIVES ONLY IN PROSE HAS NOT BEEN DEFERRED -- IT HAS BEEN
> DROPPED. The ITEM LEDGER works for exactly one reason: a test reads it."

The proof arrived on 2026-08-12: a session with full authority to work the
backlog could not find it, and the operator had to say where it was. That is the
whole failure mode -- not that the list was wrong, but that nothing could see it.

`tests/test_v3_66_1052_the_backlog_is_machine_visible.py` now reads this file.

## Priority order, set 2026-08-12 at dc5943f (v3.66.1066)

Re-derive before working any of these -- section 1: roughly half of a stale
register's OPEN items turn out to be closed or mis-scoped, and this list is a
snapshot of a judgement, not a measurement.

1. ~~**22 + 48 + 91 + 94 as ONE campaign.**~~ -- ALL FOUR CLOSED. 22 and 94 at
   @1068/@1069, 91 at @1067+@1069. **91 sat OPEN through the two cuts that
   fixed it** and was still directing this list to "START WITH 91" when
   re-derived at @1072; the instrument had been reading comment-stripped source
   since @1067 and 1034 had carried a real restore since @1069. That is
   section 1's own statistic -- roughly half a stale register's OPEN items are
   already closed -- landing on the register that exists to prevent it. What
   survives the campaign: **92** and **93**, both still OPEN and both latent
   shapes rather than live failures.
2. ~~**46**~~ -- CLOSED @1072. It had failed EIGHT times by then, not four: the
   1062/1064/1067/1068 gates shipped on 2026-08-12 were the fifth through
   eighth, added by the session that wrote this very line. The successor is
   **99**, which is what the fix could not reach.
3. **89** -- the capture corpus. Silent, and recurs on every rebuild.
4. **95 + 98** -- both cheap, both residue-class.
5. **97** -- makes 96 true for a FRESH host rather than for these four.
6. **3, 35, 5-remainder, 25, 27** -- real, not urgent.
7. **13, 26.** 26 is the trap: seductive, because it is section 0 mechanized,
   and the likeliest to ship a confidently wrong number everything downstream
   inherits.

## THIS NUMBERING IS NOT THE ITEM LEDGER'S

These IDs are the BACKLOG's own and they are a **different namespace** from the
`ITEM LEDGER` in `SESSION_CARRY.md`. Backlog **21** is the row whose subject is
ledger item **48**. Three numbering schemes were once reconciled at 15.35/15.36
after exactly this confusion; do not merge them, and when citing a row say
"backlog N" rather than "item N".

## Format, which the gate depends on

One row per item: `| <id> | <status>[ @<evidence>] | <text> |`

`OPEN` carries no evidence. `CLOSED` and `MOOT` must carry it -- a version
(`@1049`) or a commit -- because a close nobody can check is a claim, not a
record. Status is one of `OPEN`, `CLOSED`, `MOOT`.

`MOOT` is deliberately distinct from `CLOSED`: it means the SUBJECT went away
rather than the work being done. Collapsing the two would let a disappeared
problem read as a solved one.

A `CLOSED` row MAY carry an unfinished remainder, but it must say so in the
status: `CLOSED @NNNN -- PARTIAL, and the remainder is named`. Rows 3, 89, 90,
95 and 97 are the worked examples. The status column is the only thing a machine
reads, so a remainder living in a row's TEXT under a bare `CLOSED` is invisible
to every count and every triage -- see row 143.

## FOUR IDS HAVE NEVER EXISTED, and the table is not contiguous

**15, 40, 41 and 42 are absent and their content is unrecoverable.** This is not
a deletion: the birth commit `7a8a5a2` (v3.66.1052) already lacks them, and the
untracked pre-tracking source it was built from called itself "77 items" while
listing 73. Recorded here because a table that runs 14, 16 and 39, 43 reads as
damaged, and the next session to notice would otherwise spend the same hour
proving nothing was lost. Nothing was lost here that any commit ever held.

| id | status | item |
| --- | --- | --- |
| 1 | CLOSED @1043 | bd-run: promote the scratchpad wrapper to a tracked tool |
| 2 | CLOSED @1053 | generalise "never filter at capture time" beyond `head` (partly done) |
| 3 | CLOSED @1087 -- PARTIAL, and the remainder is named | every long job writes a heartbeat file  CLOSED @1087 for jobs bd-jobs LAUNCHES, by deriving the heartbeat from the job's own log rather than asking the job to write one: `bd-jobs run` now gives the process a log (it used to send stdout and stderr to DEVNULL, so a REGISTERED job produced no output anywhere), records the path, and `progress()` returns PROGRESSING / STALLED / UNKNOWN from that log's mtime with the seconds since the last write -- the countable, never a percentage, per section 10. UNKNOWN is a real third state for an entry with no log or whose log has gone, and each of the three has a test that fails if the branch is never taken. THE MOTIVATING MEASUREMENT: on test6 2026-08-13 at f154aef a pytest master sat at [ 99%] for 462s after `[gw37] node down`, holding a zombie child, at a load average of 0.06, and `alive()` said LIVE for every second of it -- see row 102. WHAT IS NOT CLOSED: "EVERY long job" is wider than "every job bd-jobs launched". A suite started by hand, by capture.sh, or by a script that does not register gets nothing, and nothing forces registration. The remainder is a policy question (should an unregistered long job be refused, or merely invisible?) and it is NOT designed. Do not read this row as "long jobs are observable"; read it as "registered ones are" |
| 4 | CLOSED @1053 | ban estimated progress in status reports |
| 5 | CLOSED @1099 | per-run artifact directories keyed by run id. bd-run HALF DONE @1060 (<label>-<runid>.log + a symlink alias, prune counts real files only). STILL OPEN: capture.sh writes a fixed /tmp/bd_capture referenced by five test files, so consecutive captures overwrite each other -- a gate change and its own cut  CLOSED @1099. capture.sh now writes /tmp/bd_capture-<runid>/ with a matching tarball, pruned to the newest 5 at run START -- on the way IN rather than out, because a run that CRASHES leaves the most valuable directory on the box and end-of-run tidiness would delete it as part of the failure. Retention lives in scripts/lib/capture_run_dir.sh so a test can RUN it rather than grep it, and a keep of ZERO is refused. THE BLAST RADIUS WAS THE INTERESTING PART: two harnesses REWRITE capture.sh's OUT= line to sandbox output and needed new anchors (one reported `assert 0 == 1` when it broke and now names its subject), and test_capture_vault_is_isolated excluded the substring "/bd_capture/" which under the new scheme appears in NO path -- it would have passed VACUOUSLY, a section 0 gate reporting OK over a subject whose shape moved underneath it. The hyphen discriminates: a bare "/bd_capture" prefix over-corrects onto /tmp/bd_capture_vault, the correct arrangement, and did on first run. NOT COVERED: the vault directory is still fixed, which is fine because the run that creates it tears it down, but two SIMULTANEOUS captures on one host would collide there |
| 6 | CLOSED @1040 | bd-reap, shipped as `bd-jobs reap` |
| 7 | CLOSED @1040 | remote job registry, shipped as `bd-jobs` |
| 8 | CLOSED @1040 | ssh wrapper recording the remote PID, shipped as `bd-jobs run` |
| 9 | CLOSED @1037 | deploy preflight refuses if pytest is running on the target |
| 10 | CLOSED @1037 | deploy.sh restores the service on a post-stop failure |
| 11 | CLOSED @1043 | bd-fleet: version/tree/service/load/jobs for all hosts |
| 12 | CLOSED @1039 | fleet-wide deploy, scripts/deploy_fleet.sh |
| 13 | CLOSED @1110 | dedicated agent SSH key, SCOPED. STILL OPEN ON PURPOSE: the identity half is staged, the RESTRICTION half is not, and the row stays OPEN because the restriction is the whole value. Note: adding a second unrestricted key closes nothing -- the value is the restriction and retiring the broad key, so a partial version of this row is worse than leaving it open  STAGED 2026-08-13 on operator instruction, additive only, because the retiring half can lock an unattended session out of the fleet and block everything after it: an ed25519 key commented `bd-agent-2026-08-13-UNRESTRICTED-see-backlog-13`, fingerprint SHA256:WZs/boseZi0qIhTmKJzRfenEfLfpydU1PgY7zcYxoyY, APPENDED (never rewritten) to test4/test6/test7 authorized_keys, each backed up to authorized_keys.pre-bd-agent-2026-08-13 first. PROVEN BOTH DIRECTIONS, because "the new key works" and "I did not break the old one" are different claims: the new key authenticates alone under IdentitiesOnly=yes on all three, and the pre-existing key still authenticates on all three. test5 DELIBERATELY NOT TOUCHED -- it does not accept the existing agent key either, so adding one there would create new REACH rather than a new IDENTITY. THE ROW'S OWN WARNING STANDS and this closes nothing on its own; all it buys is that agent access is now DISTINGUISHABLE from the operator's in the logs. INCIDENTAL FINDING: the fleet's authorized_keys are NOT UNIFORM -- before this, test4 carried 5 keys, test7 3 and test6 2, with `administrator@bittorrent` and `administrator@BattleStation` present on test4 and test7 and absent on test6. Nothing in the tree records why, and docs/repo/FRESH_HOST_BRINGUP.md is the runbook that would have to  CLOSED @1110 -- THE RESTRICTION HALF SHIPPED AND THE BROAD KEY IS RETIRED, which is what the row said was the whole value. Applied to test4/test6/test7 on 2026-08-13 and verified in BOTH directions on each, because "the new arrangement works" and "the old access is gone" are different claims. FINAL STATE, identical on all three: exactly TWO keys, `mboyle-laptop` unrestricted for the operator and the agent key carrying `from="10.0.70.164",restrict,pty`. `pty` is added back deliberately -- `restrict` alone disables it and interactive persistent sessions are how the fleet is actually driven, so a bare `restrict` would have been a restriction that broke the thing it was protecting. PROVEN RATHER THAN ASSERTED FROM THE FILE: the agent key authenticates on a NEW connection with the control master bypassed; pointing `from=` at 192.0.2.1 makes sshd REFUSE that same key and restoring it makes it work again, so the clause is enforced and not merely present; a remote forward fails at setup and a local forward carries no traffic while commands and pty still succeed, so `restrict` is live; and the retired `mboyle@test4` key is refused on all three. A METHOD TRAP COST A WRONG READING FIRST AND IS WORTH THE LINE: a command-line `-i` ADDS to the config's `IdentityFile` rather than replacing it, so the first negative test reported "the broad key still works" when what answered was the agent key the config offers. Test a refusal with `-F /dev/null` or the result is about a different key. ORDERING WAS LOAD-BEARING: test5's `~/.ssh/config` was pointed at the agent key with `IdentitiesOnly yes` BEFORE any key was removed, while both still worked, so the wedge-hunt sweeps polling those hosts never lost a connection -- 29 samples in flight across the cutover, zero interrupted. `mboyle-laptop` was ADDED to test7 first, because it was absent there and removing the broad key without it would have left the operator's laptop with no non-administrator route to that box. `administrator@bittorrent` and `administrator@BattleStation` were REMOVED from test4 and test7 on explicit operator instruction, over a recommendation to leave them: their fingerprints are recorded in ~/bd-session-2026-08-13/row13_key_inventory_before.txt and in each host's ~/.ssh/authorized_keys.pre-row13 backup first, because nothing in the tree records who owns them and an unidentifiable key is not a safe thing to delete unrecorded. EVERY MUTATION WAS REVERSIBLE WHILE IT RAN: a timestamped backup, then an auto-rollback ARMED BEFORE the write that restores it after 900s unless a marker says the change was verified, cancelled only after a new connection proved the box still answers. The executor is ~/bd-session-2026-08-13/row13_scope_agent_key.py and asserts its invariants before writing anything, so a failed check changes nothing. WHAT IS NOT CLOSED BY THIS: test5 still has no key of its own on the fleet by design -- it is the origin, not a destination -- and `~/.ssh/config` on test5 is UNTRACKED and does not survive a rebuild, so a rebuilt master silently falls back to an identity the hosts no longer accept. That is now written into docs/repo/FRESH_HOST_BRINGUP.md rather than left as fleet folklore. And the operator's laptop key was verified PRESENT by fingerprint and never tested, because its private half is not on this machine -- presence is not reachability, and this row does not claim otherwise |
| 14 | MOOT @1051 | reimage .249 -- the clean-host role moved to .84/test7 instead, and the proof was retaken there |
| 16 | CLOSED @1044 | record machine load with every suite result |
| 17 | CLOSED @1043 | bd-ab, the A/B harness |
| 18 | CLOSED @1043 | bd-ladder, the parallel prefix ladder |
| 19 | CLOSED @1044 | auto-size -n from nproc and record the value |
| 20 | CLOSED @1053 -- was ALREADY in s1 before this cut; verified verbatim, not re-added | state the denominator in every reported count, mechanically |
| 21 | CLOSED @1049 | ledger item 48's SECOND MECHANISM: the leaker left sys.modules wiped, orphaning import-time bindings |
| 22 | CLOSED @1069 | fix the remaining sys.modules leakers. RE-DERIVED AT RUNTIME @1068 and the answer is THREE, not eleven or thirteen: of the 14 files the STATIC census listed, bd-modwatch in per-file mode found only 3 that actually orphan the module table, and 2 of those drop exactly `bulk_downloader.push`, which ZERO files bind at import time -- harmless. The one real leaker was `tests/test_v3_66_1034_guards_survive_a_module_wipe.py` (263 dropped, 5 swapped, including `bulk_downloader.app`). FIXED @1069 with a module-scoped save/restore so the wipe stops at its own file; the wipe itself stays, because the file exists to prove the guards survive one. Defining pairing 1034 -> 780 went 7 failed/12 passed -> 19 passed; the whole 8-file victim population passes at 154. Every earlier count was of a static heuristic that over-reports BY DESIGN, never of a leak |
| 23 | CLOSED @1044 | record the --dist loadfile worker assignment (recorded, NOT pinned) |
| 24 | CLOSED @1044 | per-worker chain logging |
| 25 | CLOSED @1093 | quarantine or annotate known-rotating tests  THE CENSUS THIS ROW NEEDED NOW EXISTS, and its headline is that "rotating test" NAMED THREE UNRELATED MECHANISMS AND ONE OF THEM WAS A SHIPPABLE PRODUCT DEFECT. Measured 2026-08-13 at f154aef: ELEVEN sequential samples on test4 at -n 16 and SIX on test6 at -n 48, same commit, same 48-core spec, whole suite in CLAUDE.md section 5's sanctioned form. n16: 7 clean, 4 with failures, 0 wedged. n48: 3 clean, 1 with failures, 2 WEDGED WITH NO VERDICT (row 102). THE THREE POPULATIONS: (a) `tests/test_perf_lab.py`, SEVEN tests failing together, on BOTH hosts -- NOT FLAKY. `bulk_downloader/perf_lab.py` walked `gc.get_objects()` and did `type(o).__name__`, which h11's sentinel metaclass refuses; it rotated only because the object is in the graph once something has exercised h11, which depends on --dist loadfile's assignment. FIXED at v3.66.1088, and the disappearance of this population is the confirmation. (b) `tests/test_t14_vpn_probe_egress::test_probe_no_tunnels`, 3 of 11 at n16 -- CLAUDE.md section 4 already records it as passing in isolation and failing co-batched. Genuinely schedule-dependent and the ONLY real quarantine candidate of the three. (c) `tests/test_v3_66_729_body_contract_fixtures`, 2 tests, 1 of 11 -- AN ARTEFACT OF MEASURING TWICE: that probe's rows accumulate across runs and the next probe reads them (CLAUDE.md section 5), so back-to-back suite runs are two different experiments. Independently reproduced on test5, where run 1 was clean and run 2 was not with only a COMMENT changed between them. SO THE ROW'S OWN FRAMING NEEDS CARE: quarantining by NAME would have hidden a real defect in (a) and mislabelled a harness artefact in (c). The census is what distinguishes them, and it is the prerequisite, not the deliverable. Data preserved at ~/bd-sweep-2026-08-13/ on test5  CLOSED @1093 BY FIXING IT, NOT BY QUARANTINING IT, and the reason is that the census's population (b) turned out not to be flaky either. test_t14_vpn_probe_egress::test_probe_no_tunnels failed co-batched because its `_vpn_state` fixture DID NOT BUILD THE SHAPE IT ASSERTS ON: the fixture swaps `vpn_config._state["tunnels"]`, while everything under test reads the module-level `vpn._tunnels` registry through `vpn.list_tunnels()`. Two different stores, so entering the context with an empty list set something no assertion consulted. The registry reset existed but only in the `finally` -- cleaning up for the NEXT test and never establishing the precondition for THIS one, which is CLAUDE.md section 6's harness rule exactly. It presented as rotation (3 of 11 at -n 16) only because --dist loadfile decides whether a tunnel-leaving file lands on the same worker first. REPRODUCED DETERMINISTICALLY 2026-08-13 at a0cdfff: register one tunnel, run the probe under this fixture, get registered_tunnels=1 where 0 is asserted. `test_egress_monitor_no_tunnels` carried the identical defect and is fixed by the same line. SO ALL THREE of the census's populations were real defects or artefacts and NONE was a test that needed quarantining -- (a) was a product crash fixed @1088, (c) is the measure-twice artefact, (b) is this. The row's original framing, quarantine or annotate, would have hidden every one of them. WHAT IS NOT CLOSED: the leaker itself was not identified -- a sweep of every tests/ file calling register_tunnel reproduced the failure with NONE of them alone, so the poisoning needs a combination and the entry reset makes the victim independent of it rather than removing it |
| 26 | OPEN | a vacuous-test detector (hard: "can this assertion fail?")  A DECIDABLE SLICE SHIPPED @1098 and the row stays OPEN above it. tests/test_v3_66_1098_no_assertion_can_be_trivially_true.py walks every tracked tests/test*.py and refuses an assert whose truth is decidable from the syntax tree alone -- `assert <anything> or True`, `assert True`. MEASURED: SIX live hits, and a grep for `or True` found only FOUR, so the AST earned its place (CLAUDE.md section 1). THE HEADLINE IS WHY THEY WERE THERE: the `or True` was SUPPRESSING FALSE ASSERTIONS, not saving typing. Two went RED when made real -- a repr legitimately containing 'token' as a FIELD NAME, and a package attribute that holds bare but is false under conftest's _canonicalize_package_children -- and one could neither pass nor fail, its second clause being `"sec" not in [s.lower() for s in ("SEC",)]` which is always False. WHAT THE SLICE CANNOT SEE, so the row is not closed: an assertion vacuous through a VARIABLE, an unreachable assertion, and one true of every possible implementation. The provenance matters too -- v3.66.1095 shipped a vacuous assert past review, a mutation battery, a 517-file band, twelve CI checks and four captures  SLICE 2 SHIPPED @1108 AND FOUND NOTHING, WHICH IS THE RESULT RATHER THAN A DISAPPOINTMENT. tests/test_v3_66_1108_no_assertion_can_be_trivially_false.py refuses an assertion FALSE for every input -- the mirror of @1098 -- plus statically unreachable asserts. MEASURED at v3.66.1107 over 1331 files: 32127 assert nodes, 37309 boolean-context expressions, ZERO always-false and ZERO unreachable. It closes no defect and is a FLOOR against a class that has occurred once. THE INSTRUMENT'S OWN DENOMINATOR IS THE THING TO KNOW BEFORE READING A GREEN RESULT: exactly 27 of those 37309 expressions can be decided by constant folding at all, and 25 of the 27 are the bare `assert False` idiom. That is not the defect it looks like -- an assertion built only from literals is inherently suspicious, so the decidable set and the suspicious set nearly coincide -- but it must not be read as coverage. THE FIRST VERSION WAS A BLIND GATE: folding only WHOLE assert tests decided 1 of 32127 (0.003%), because @1098's live instance was a CLAUSE inside a larger expression; widening to boolean-context sub-expressions is what made it able to see the row's own shape. ITS ONLY LIVE FINDING WAS A FALSE POSITIVE and is now an over-sensitivity control -- a null-coalescing `or` default in test_2c_guard_zero_match_interlock.py, missed by the exemption because an empty list literal is an ast.List and not an ast.Constant. RED-first was proven by INJECTION since the census is empty, and the mutation battery is 7/7 after two escapes that were both defects in the TESTS: a top-level `and` never exercises the nested-descent the walker does, and an oversized-integer case never reaches the result-length bound. WHAT KEEPS THE ROW OPEN is unchanged and now measured rather than asserted: vacuity through a VARIABLE, unreachability for any reason other than a preceding terminator, and an assertion true of every possible implementation |
| 27 | OPEN | cross-test over-sensitivity controls when the subject is a fixture  TWO WORKED EXAMPLES SHIPPED 2026-08-13, so the pattern this row asks for is now demonstrated rather than only described. v3.66.1088: the fix for the perf_lab crash could have been "label every type unnameable", which passes the crash test and DESTROYS the census the tool exists to produce -- so the cut carries an over-sensitivity test (`the census still counts the types it can name`) AND a mutant applying exactly that over-correction, making the pair machine-checked rather than argued. v3.66.1087: one log is asserted STALLED at a 300s threshold and PROGRESSING at 900s in a single test, so a fix that only ever tightened would fail. WHAT IS STILL OPEN is the mechanization: nothing detects a cut that OMITS the control, and both examples exist because the author happened to write them. A candidate is a bd-mutate convention -- every spec carries at least one over-correction mutant -- but that is a policy over a tool nobody is forced to run, which is the weaker half of the same problem  BOTH MECHANIZATIONS WERE MEASURED AT v3.66.1108 AND BOTH FAIL, SO THE ROW STAYS OPEN CARRYING THE EVIDENCE RATHER THAN SHIPPING A CHECK NOBODY WOULD ACT ON. DENOMINATOR for every count below: 1332 tracked tests/test*.py at 6d6db28, all 1332 parsed by ast with ZERO SyntaxErrors so nothing sits silently outside the predicates, reconciling exactly as 9 repo-wide + 9 module + 1314 frozen baseline entries. (1) THE DECLARATION ROUTE -- a required BD_OVERSENSITIVITY_CONTROL naming the test, keyed on BD_GATE_SCOPE = "repo-wide", on the BD_GATE_SCOPE precedent -- IS SECTION 0 IN ITS LITERAL FORM: ITS DENOMINATOR EXCLUDES THIS ROW'S OWN TWO WORKED EXAMPLES. v3.66.1087 (adbea3c) put its control in a file declaring BD_GATE_SCOPE = "module" at line 38. v3.66.1088 (fd713d7), the canonical example carrying BOTH the control and the matching over-correction mutant, added no test file at all -- its control went into tests/test_perf_lab.py, which is line 394 of the frozen tests/gate_scope_baseline.txt, and the baseline may only SHRINK, so that file can never carry a marker. 0 of 2, permanently, against the evidence the row is built from; the gate would ship GREEN over the exact population it was written for. (2) AND IT WOULD HAVE CAUGHT NOTHING ON THE POPULATION IT CAN SEE. All nine repo-wide files hand-labelled twice independently: EIGHT carry a genuine over-sensitivity control and the ninth (1085) has none, arguably correctly, its fix being one pre-import in tests/conftest.py with no benign-input failure mode a test could express. The observed defect rate inside the gate's denominator is ZERO, so its only in-scope firing would demand a sentence for the file whose right answer is "None" -- and it would accept that sentence unread. On the TWO measured occasions when an over-sensitivity control actually failed in this tree the control EXISTED, was NAMEABLE and was GREEN: v3.66.1054, where the control asserted "CAP" not in stdout.upper() and failed a CORRECT implementation because tmp_path is derived from the test's NAME and the substring matched the log path; and v3.66.1024, where a conftest guard shipped RED-first "with an over-sensitivity control that passed" and the full suite then failed it, the control having covered only a clean environment while the suite's own meta-tests sat outside its denominator. A gate asserting "a function of this name is defined in this file" is fully satisfied by both, so it is 0 of 2 there as well. That is the class CLAUDE.md's front matter already names as its own worst blind spot -- gates that ask whether a cited PATH still resolves, never whether the sentence around it is still true. (3) THE BD_GATE_SCOPE PRECEDENT IS NOT THIS SHAPE, AND THE DIFFERENCE IS THE ARGUMENT. That marker is an INPUT to a chain that is independently checked: "repo-wide" forces membership of _DECLARED, which the union assertion in test_v3_66_939 forces into a ci.yml shard, with four separate assertions over the wiring, so a true declaration COSTS two further edits and a false one buys nothing. BD_OVERSENSITIVITY_CONTROL terminates on itself -- nothing downstream would consume it -- so it degrades to a spellchecker for a habit, bought at the price of the appearance of coverage. (4) THE DERIVED-PREDICATE ROUTE IS CLOSED HARDER THAN "IMPRECISE": ITS FALSE-POSITIVE RATE IS NOT A NUMBER. A LEXICAL predicate over ast docstrings plus tokenize comments matching over-sensitiv / over-correct / cries wolf, whitespace-normalised per section 1's line-wrap rule, hits 5 of 9 repo-wide and 122 of the other 1323 (9.2%) -- reproducible to the file across three readings, and the wrap-tolerant form finds nothing a naive per-line grep misses. A STRUCTURAL predicate (a test asserting a NEGATIVE outcome about a function defined in the SAME file) hits 4 of 9 inside under EVERY reading, but its OUTSIDE count is an artifact of whoever implements the sentence: three independent implementations returned 28-64 (2.1%-4.8%), 42-110 (3.2%-8.3%), and a claimed 53, with no two bands agreeing. Either predicate: 6 of 9 inside. A gate whose false-positive rate moves by 2.3x on undocumented tokenizer choices is not a gate, and section 0 counts one that cries wolf as a soundness bug rather than a safe default. (5) AND IT IS WRONG IN BOTH DIRECTIONS AT ONCE. True recall against the hand labels is 6 of 8 -- it REFUSES two cuts that did write a proper control: 1068's test_a_batch_mode_still_exists_and_is_opt_in ("The co-batched question is legitimate ... It must remain askable, but only when asked for") and 1080's test_artifacts_survive_a_FAILING_run ("A debugging directory deleted on the one run that needed it is a worse defect than the leak this closes"), neither carrying the vocabulary nor the negative-outcome syntax. 1080 in fact carries a THIRD and stronger one the earlier labelling missed, test_the_run_context_is_anchored_outside_the_reclaimed_root, whose docstring records an over-correction that ACTUALLY HAPPENED ("resolving at call time put the run context inside the reclaimed root and deleted it with the rest, which broke two meta-tests"). Note also that 939's control states section 0's cries-wolf rule IN FULL in different words -- "the gate would fire on every clean tree and be switched off" -- and the lexical predicate misses it, so the vocabulary is half-adopted prose and not a standard: the literal phrase "over-sensitivity control" appears in 24 of the 1332 tracked test files. WHY IT RESISTS MECHANIZATION, in the sentence section 7 already uses for the same class: a gate is repo-wide by virtue of what it ASSERTS ABOUT, which its syntax does not record -- and a control is a control by virtue of what it asserts is STILL TRUE about benign input, which syntax does not record either. That is why a derived predicate cannot see it and a declared name cannot constrain it. WHAT THIS COSTS, stated because a refusal is not free: nothing detects a cut that omits the control, exactly as before; the only enforcement remains prose, and section 0's own record is that prose does not stop forgetting -- eight gates went undeclared in a row, 1031 and 1034 written by a session that had just read the note about 944 and 947. This row is accepting that cost knowingly rather than buying a green tick over the wrong denominator. THE ONE ROUTE NOT YET REFUTED IS EXECUTABLE RATHER THAN DECLARATIVE, and it is the next step if this row is worked again: teach bd-mutate a direction field so a mutant can APPLY the over-correction the fix could have made and require the named control to flip RED, which is the only construct in this tree that can tell a real control from a test renamed to look like one -- v3.66.1088 already did this by hand. It was NOT built and its cost is NOT measured, and two measurements must come first. ZERO mutation specs have ever been tracked -- git log --all --diff-filter=A over the whole history finds no mutants.json and none is on disk -- so every battery's evidence exists only as CHANGELOG prose, unverifiable and un-re-runnable, which is section 1's stale-prose-as-authority class sitting inside the strongest instrument the project has. And of the last 80 releases, 33 touch product or tool source (bulk_downloader/, tools/, toolchain/, scripts/, frontend/src/, excluding the version bump) while only 16 of those 33 report a battery at all, so any preflight check keyed on "this cut changes behaviour" would print UNKNOWN on 17 of 33 code cuts -- more than half -- which is the advisory-check-nobody-acts-on shape this row is refusing. |
| 28 | OPEN | a WRITE-recorder: the filesystem analogue of the socket recorder  REOPENED @1113 AS A FALSE CLOSURE, and it is the inverse of row 91. This row was marked `CLOSED @1055`; `git show` of that cut has the row flipping OPEN to CLOSED with its TEXT UNCHANGED, and what @1055 actually shipped was `bd-modwatch` -- a `sys.modules` TABLE watcher. A module-table watcher and a filesystem-write recorder share nothing but the word "watch". VERIFIED at f0c5667: no filesystem write recorder exists among the 254 tools in `toolchain/bin`. THE PAIR IS THE POINT: row 91 sat OPEN through the two cuts that fixed it, and this row read CLOSED for a cut that did not touch it, so the register has now been measured wrong in BOTH directions -- which is why row 105 (neither tracker gate runs in CI) is the structural fix rather than any individual correction. If the subject is genuinely unwanted, MOOT it with that reasoning written down; do not re-close it on @1055 |
| 29 | CLOSED @1038 | ast.parse -> import-and-resolve, recorded in CLAUDE.md s6 |
| 30 | CLOSED @1036 | record load-dominance |
| 31 | CLOSED @1036 | record "a failed deploy leaves the service down" |
| 32 | CLOSED @1036 | parameterise -n by host rather than the container's 4 |
| 33 | CLOSED @1058 | EXERCISE THE CANDIDATE WORKFLOW: run a tip on .85 before a merge. DONE ONCE, for real: v3.66.1058's tip ran on test4 in a detached worktree before merge -- symbol asserted present per section 2b, absolute venv interpreter, deployed tree untouched, 157 passed exit 0. The worktree (not the deployed checkout) is the shape that works |
| 34 | CLOSED @1059 | regenerate the socket recorder's blind-spot counts rather than hardcoding them. Derived at read time, denominator named beside each count, UNKNOWN when the tree cannot be read; ~0.26s once per process via PEP 562 __getattr__. The retired literals are deliberately not repeated here |
| 35 | CLOSED @1106 -- PARTIAL, and the remainder is refused with a reason | pre-commit self-check: tree clean, no orphans, services healthy, no scratch in tests/, ledger current  CLOSED @1106 PARTIAL. toolchain/bin/bd-cut-preflight (tracked @1104) is the composer this row wanted, with UNKNOWN as a third state that drives the verdict NONZERO and each check's denominator parsed out of that tool's own output -- so a tool exiting 0 having examined nothing reads UNKNOWN, not pass. FOUR OF FIVE PREDICATES COVERED: tree clean and ledger current came with the tool; scratch-in-tests and orphans land here. SCRATCH-IN-TESTS is deliberately ONE predicate for two section 2a failures -- stray scratch contaminating a regen, and a new file not staged so no axis-6 gate can see it -- because both are fixed by the same action; MEASURED, the second broke THREE cuts on 2026-08-13. Staged files are excluded: staging IS the remedy. ORPHANS is a local check because add() maps the exit code to the state and bd-jobs orphans exits 0 regardless, so routing it through add() would grade orphans-present as a pass. THE FIFTH, SERVICES HEALTHY, IS REFUSED WITH A MEASUREMENT rather than skipped: bd-doctor exits 1 CRITICAL on test5 for three missing operator wrappers with no bearing on commit safety, so a blocking check would refuse every cut on this box -- section 0's over-sensitivity failure. Service health is a pre-CAPTURE predicate and capture.sh manages the service itself. That absence is asserted by a TEST, so re-adding it silently goes red |
| 36 | CLOSED @1035 | socket recorder directory leak (744 dirs) |
| 37 | CLOSED @1035 | new gates wired into CI (isolation shard + _DECLARED) |
| 38 | CLOSED @1035 | the 11th axis-6 gate recorded in section 4's table |
| 39 | CLOSED @1035 | ledger currency: data plus a gate that catches staleness |
| 43 | CLOSED @1043 | bd-gc: reap /tmp/bd-* litter across hosts |
| 44 | CLOSED @1035 | "creating a path is a promise to remove it" (s0) |
| 45 | CLOSED @1035 | 1034/1031 added to a shard and to _DECLARED |
| 46 | CLOSED @1072 | make _DECLARED derivable, or gate an undeclared repo-wide gate | DERIVABLE IS THE HALF THAT DOES NOT WORK, and that is a measurement rather than a judgement: against the eight files that actually went undeclared (944, 947, 1031, 1034, 1062, 1064, 1067, 1068), an AST census over real call nodes catches 3 of 8 on a `git ls-files` argument and 4 of 8 if you also match code naming repo infrastructure -- the second widening the candidate pool from 34 files to 136, so it costs a 124-entry exemption list to buy one hit. 947, 1031, 1067 and 1068 carry NO structural signal separating them from a feature test. Shipped instead: every tracked `tests/test*.py` declares a module-level `BD_GATE_SCOPE` or sits in the frozen 1314-entry `tests/gate_scope_baseline.txt` (may only shrink, no regenerator by design); `"repo-wide"` requires `_DECLARED`, which the union assertion forces into a shard. 6/6 mutants caught. THE WORST FINDING WAS INCIDENTAL: the `gates` job runs ZERO pytest and `test_v3_66_939` was in no shard, so from v3.66.939 to v3.66.1071 the gate against a suite falling out of every shard had itself fallen out of every shard. It and the four 2026-08-12 gates are now wired in |
| 47 | CLOSED @1035 | "a gate CI does not run is a gate that does not exist" (s7) |
| 48 | CLOSED @1035 | closing an item must update the newest ledger -- gated |
| 49 | CLOSED @1035 | no ledger declares OPEN what the inventory closed -- gated |
| 50 | MOOT @1051 | re-role or reimage .249 -- superseded by the role moving to test7 |
| 51 | CLOSED @1055 | .85 carried /etc/sudoers.d/90-mboyle-codex AND 90-mboyle-nopasswd with byte-identical rules; the codex one is vestigial from the retired CODEX_HANDOFF era and the other three hosts have only the latter. Removed, visudo -c OK, sudo -n verified, backup at /root/sudoers-codex-backup-2026-08-12 |
| 52 | CLOSED @1048 | decide whether streamlink belongs in system_deps.sh -- it does; nothing had ever installed the preferred backend |
| 53 | CLOSED @1036 | capture.sh cannot see cross-file state leaks (s7) |
| 54 | CLOSED @1058 | add a pytest-tests/-shaped lane to capture, or state the blind spot there. Took the SECOND option: capture.sh prints a blind-spot block after the verdict and tee's it to 11_BLIND_SPOTS.txt, naming cross-file state leaks (with the v3.66.1034 measurement) and timezone defects. An actual pytest-shaped lane remains unbuilt and would be a gate change |
| 55 | CLOSED @1036 | audit beats recollection (s1) |
| 56 | CLOSED @1036 | post-cut residue audit, folded into 55 |
| 57 | CLOSED @1053 | check any instrument you build for its OWN blind spots |
| 58 | CLOSED @1053 | when the contract records a mistake class, check the change in front of you BEFORE committing |
| 59 | CLOSED @1053 | measure the cost of anything added to every test run |
| 60 | CLOSED @1053 -- was ALREADY in s0 before this cut; verified verbatim, not re-added | treat "I read the contract" as worth nothing |
| 61 | CLOSED @1038 | never retype an anchor containing punctuation (s6) |
| 62 | CLOSED @1038 | edit scripts mutate in memory, write once (s6) |
| 63 | CLOSED @1038 | sed -i is not an applied-check (s6) |
| 64 | CLOSED @1053 | verify CI status by the status column, not a positional field |
| 65 | CLOSED @1053 | prefer doc-only cuts as a shape (cheap band, no source risk) |
| 66 | CLOSED @1037 | validate a stub against the real command's output format (s6) |
| 67 | CLOSED @1037 | beware shell tail-call exec in fixtures (s6) |
| 68 | CLOSED @1039 | a floor means ADD to it, never drop from it (s4) |
| 69 | CLOSED @1037 | /proc/exe is useless for venv processes |
| 70 | CLOSED @1037 | state a detector's blind spot IN THE CODE |
| 71 | CLOSED @1042 | deferrals must land somewhere machine-visible, not in prose |
| 72 | CLOSED @1042 | verify SUMMARY/VERDICT lines specifically |
| 73 | CLOSED @1039 | a tracked example file uses documentation-range data |
| 74 | CLOSED @1042 | TEST THE SEAM, not just the component |
| 75 | CLOSED @1042 | a clean battery is not coverage evidence -- check the primary path |
| 76 | CLOSED @1042 | a tool acting on other hosts must verify it CAN record before acting |
| 77 | CLOSED @1042 | when a function has N outcomes, assert each is reachable |
| 78 | CLOSED @1047 | bd-jobs run --script FILE |
| 79 | CLOSED @1047 | bd-mutate line-buffered |
| 80 | CLOSED @1046 | GATE: no test may hand its own file to a subprocess pytest |
| 81 | CLOSED @1046 | GATE: conftest defines each pytest hook name at most once |
| 82 | CLOSED @1046 | GATE: no test writes real tool state (/tmp/bd-jobs) |
| 83 | CLOSED @1046 | ANSI audit -- bd-ab and bd-ladder were BLIND, both fixed |
| 84 | CLOSED @1046 | "a green band at one -n does not retire a cross-file failure" (s4) |
| 85 | CLOSED @1052 | make this backlog machine-visible -- this file, and the gate that reads it |
| 86 | MOOT @1051 | run bd-gc --apply -- the 2026-08-12 rebuild collected the litter; bd-fleet measures 2/0/0 against a pre-rebuild 1349/435/68 |
| 87 | CLOSED @1050 | the killswitch auto-cycle thread hung full-suite runs: an unguarded daemon thread performed a real tunnel stop/start 30s later and killed an xdist worker |
| 88 | CLOSED @1054 | bd-jobs reap kills the registered pid but not its children -- reaping a driver shell orphaned the hung pytest beneath it, and the orphan then blocked every deploy |
| 89 | CLOSED @1083 -- PARTIAL, and the remainder is an operator decision | the capture corpus is not restored by deploy.sh and does not survive a rebuild; two hosts silently had zero files in captures/ while analytics reported an empty store  CLOSED @1083 for the half a walk can fix: the SILENCE. `scan_captures` skips a root with `if not ddir.is_dir(): continue`, so an absent corpus and an empty one were both `total: 0` -- which is why two hosts read as an empty store rather than a missing one. `scan_captures_summary` now reports `roots`, `roots_missing` and a four-valued `corpus_state` (present / empty / absent / partial); `partial` exists because that is the state that actually occurred and collapsing it into either neighbour loses it. 4 mutants, 4 caught, including the over-sensitive direction (a fresh install must read `empty`, not raise an incident). NOT CLOSED: nothing RESTORES the corpus. It is gitignored data, deploy.sh moves code, and a backup policy is an operator decision rather than something a walk can invent |
| 90 | CLOSED @1054 -- PARTIAL, and the remainder is named | a dead xdist worker hangs the suite unboundedly -- pytest-timeout runs INSIDE the worker, so nothing bounds a run whose worker died. Needs a whole-run cap outside the process  QUALIFIED @1113. The cap @1054 shipped is `bd-run --max-seconds`, and it only bounds a run that WENT THROUGH bd-run. MEASURED at f0c5667: `grep -c bd-run capture.sh` = 0, so the one place this defect was actually observed -- the capture parallel lane -- was never covered by its own fix. That is precisely why @1111 had to add a SECOND independent cap (`CAPTURE_STAGE_CAP` in `scripts/lib/heartbeat.sh`) 57 cuts later, and why row 102's wedge could still hang a capture forever in the meantime. THE REMAINDER: any invocation that does not route through bd-run is still unbounded -- a suite started by hand, by a sweep driver, or by any harness that is not capture.sh. Same shape as row 3's remainder, and the same lesson: "a cap exists" and "the cap covers the caller that needs it" are different claims |
| 91 | CLOSED @1067+@1069 -- re-derived and corrected at @1072; the row had sat OPEN through two cuts that fixed it | THE ITEM-48 RATCHET EXEMPTS ITSELF. `_module_wipe_leakers()` in `tests/test_v3_66_1034_guards_survive_a_module_wipe.py` matches its `restores` regex against the WHOLE FILE, so 1034 scores as restoring on its own PROSE -- its regex source literal and an assertion message quoting the restore call. Measured @1062: budget 13, found 13, and 1034 absent from its own census while being the one leaker causing live failures. CLAUDE.md section 0's comments-are-in-the-denominator trap, inside the gate written for this class; two independent agents' classifiers were fooled by the same prose. Fix: assert over comment-stripped source (tests/shell_source.py is the mechanized equivalent for shell)  MEASURED at v3.66.1072, both halves: the INSTRUMENT was fixed @1067 -- `_module_wipe_leakers` now calls `python_code_only` and classifies over comment- and string-stripped source; and the SUBJECT was fixed @1069 -- 1034 carries a real `sys.modules.update(saved)` in code. So the census excluding 1034 is now CORRECT rather than fooled, and the assertion message that used to answer for it is invisible to the classifier. Census size 13. Recorded because this row is section 1's own statistic landing on the backlog: it read OPEN while the fix had shipped, and a session working the priority order would have re-fixed it |
| 92 | CLOSED @1096 | A CSRF MINT/CHECK BINDING ASYMMETRY is why the module-wipe class produces 403s specifically. `bulk_downloader/app.py:814` `_csrf_key` is module-level, so a fresh module EXECUTION mints a new key; `_check_csrf` reads it EARLY from the module dict frozen at the victim's collection time, while `bulk_downloader/app_csrf.py:16-19` and `app_pair.py:17-20` resolve `_csrf_token_for` LATE via importlib at call time. conftest's `_GUARD_REPATCH` covers three registered guards and `bulk_downloader.app` is outside that denominator. Recorded @1062; the FIX is NOT designed -- note CLAUDE.md's v3.66.1024 warning that a conftest guard in this exact area already fought a shipped position once  CLOSED @1096. `_check_csrf` now resolves `_csrf_token_for` through `importlib.import_module("bulk_downloader.app")` at call time, which is the idiom `app_csrf.py` and `app_pair.py` already used -- so mint and check always land on the same module's `_csrf_key`. REPRODUCED END TO END before the fix at 40937e6: a client held on the stale module gets a token from GET /api/csrf minted with the LIVE key, and the next POST is refused 403 "CSRF token missing or invalid" -- the app rejecting a token it had just issued. BINDING TIME ONLY: guarded routes, bootstrap exemptions, Bearer bypass, cross-origin Origin refusal and key rotation on restart are all unchanged. Cost 0.77 us per state-changing /api/ request. The cut carries TWO over-correction mutants, because the easy wrong way to make check agree with mint is to stop checking: one drops the comparison, one accepts a missing token, both caught |
| 93 | CLOSED @1096 | THE VICTIM POPULATION for the CSRF split, recorded so the next occurrence is recognised rather than re-investigated: 8 tracked test files hold a module-scope `bulk_downloader` import AND send an X-CSRF-Token -- test_auth_throttle, test_secret_display_never, test_t44_request_replay, test_t6_login_security, test_v3_66_38_pwmgr_hardening, test_v3_66_43_pwmgr_remainder, test_v3_66_709_automation_gui_contract, test_v3_66_780_config_key_write_parity. Two independent AST censuses agree. Measured @1062  CLOSED @1096 as a consequence of 92, not separately. The population RE-DERIVED at 40937e6 and still measures EXACTLY 8 files with a module-scope bulk_downloader import AND an X-CSRF-Token header -- same names as recorded, no drift -- and all 144 tests across them pass with the late-binding fix. The row's value was never the fix but the RECOGNITION: this population is what a 403 storm looks like when the cause is two module objects rather than an auth defect, and it is kept so the next occurrence is recognised rather than re-investigated |
| 94 | CLOSED @1068 | bd-modwatch answered a DIFFERENT QUESTION depending on how it was invoked, and said nothing about which. `--all` planned one group per FILE; explicitly-named files collapsed into ONE group, so a batch of N could only ever report 0 or 1, could never name the offender, and a wipe in one file offset by an import in another netted out CLEAN. The verdict then called groups "file(s)". THE ORIGINAL ROW SAID THE BATCH "reports 0" -- re-measured @1068 it reports 1, so that specific number did NOT reproduce; the defect is the silent change of question, which is worse and explains the same disagreement. Fixed: per-file by default, `--together` opt-in for the co-batched question a real --dist loadfile run answers, and the verdict names its mode. Backlog row 22's "bd-modwatch reports 0" evidence is therefore VOID as written |
| 95 | CLOSED @1080 -- PARTIAL, and the remainder is named | LEAKED TMPDIRS ARE A POPULATION, NOT ONE FILE -- re-measured on test5 2026-08-12 after a capture round: **2906 entries under /tmp totalling 357MB**, from at least three tracked tests that call `tempfile.mkdtemp()` and never remove the result: `tests/test_phases_195_199.py` (531 dirs, prefix `bd-phases-test-`), `tests/test_repos_F2_F9.py` (468, `bd-repos-test-`) and `tests/test_v3_45_8_macro_replay.py` (288, `bd-macro-replay-`). One directory per band run per file, on every host, growing forever. CLAUDE.md section 0 already records this exact shape at 744 directories and states the rule -- creating a path is a promise to remove it -- so the fix is `tmp_path` or an explicit cleanup, and the DENOMINATOR is every mkdtemp in tests/, not the three named here  CLOSED @1080 by fixing the ROOT rather than the call sites: 579 mkdtemp sites exist in tests/ and 366 pass NO PREFIX, so 6793 entries (38%) could never be attributed to a test by name. mkdtemp resolves through `tempfile.tempdir`, so tests/_tmproot.py points it at one per-process root and removes it at session end -- covering every call site, present and future. MEASURED both directions on a full suite: /tmp grew 40 with the mechanism on, 1917 with it off, 15777 passed either way. THE ROW'S OWN FIGURES WERE A 5.3x UNDERCOUNT (they came from bd-fleet's two-glob litter column, fixed @1078). WHAT IS NOT CLOSED: the accumulated backlog already on the four hosts (~13000-18000 entries each) is untouched and is the operator's to reclaim with bd-gc; the box-only gate fails while it stands, correctly, and says so  THAT REMAINDER IS NOW CLOSED, MEASURED 2026-08-13 at f154aef after the operator's clear-and-reboot: test4 64, test6 68, test7 65, test5 83 entries under /tmp, against the gate's 5000 threshold. `test_the_fleet_leaks_no_tmpdirs` ARMED with FLEET_TMP_CHECK=1 passes on all four -- run on EACH host, because its predicate is `len(list(Path("/tmp").iterdir()))` on whatever machine executes it, so one host's pass is a fact about that host and not about the fleet its NAME claims. The gate is correct and its name overstates its denominator; that is worth one line and not a cut. This retires the operator-bound half of 95 |
| 96 | CLOSED @1065 | PROVISIONING IS NOT UNIFORM AND NOTHING INSTALLS THE DIFFERENCE. test4 alone has PostgreSQL + `MOD3_PG_TEST_DSN` (18 tests: mod3 cutover/rehearsal/pg_isolation/shadow_read/dual_write) and `bd_dev_inspect` (3 tests: redactor seam), so it runs 21 tests the other three SKIP -- 15722 pass / 5 skip against 15701 / 26 in the 2026-08-12 captures. Measured @1062: ZERO mentions of postgres, MOD3_PG_TEST_DSN or bd_dev_inspect in scripts/provision_test_host.sh, scripts/lib/system_deps.sh or install_linux.sh, and no installer for either anywhere in the tree. Consequence: a clean-host bring-up proof goes green partly by SKIPPING what is missing, and ledger item 31's EXIT-3 can only ever be exercised on one box | CLOSED for THIS fleet at @1064 (shared scripts/lib/dev_capabilities.sh sourced by both provisioners) and @1065 (the DSN is persisted on the already-serving path too); postgres, bd_dev_inspect and ~/.config/bd/mod3.env now present on all four hosts, DSN answering. See row 97 for the part that is NOT closed |
| 97 | CLOSED @1094 -- PARTIAL, and the remainder is named | `bd_mod3_pg_provision` CONFIGURED postgres but did not INSTALL it -- it refuses with "postgresql-common absent (no pg_ctlcluster)" on a host where postgres is not already present. True in the cloud image, FALSE on bare Ubuntu, so on a fresh host the step WARNs forever and row 96 is closed for this fleet only. Measured @1065: test5/test6/test7 each needed `apt-get install postgresql` by hand first. Either the library installs it, or the WARN text must say "install postgresql first" instead of naming an internal binary  CLOSED @1094 PARTIAL. The absent-postgres branch now runs `apt-get install -y postgresql` and then VERIFIES by asking for `pg_ctlcluster` again, because this file's own @1064 lesson is that an exit code is not evidence the side effect happened -- an apt reporting success without delivering the binary refuses here instead of failing two gates later with an unrelated message. THE REMAINDER, which is why this is PARTIAL: the real apt path is NEVER EXERCISED. Every arm of tests/test_v3_66_1094_pg_provision_installs_postgres.py runs against a stub on a PATH containing only stubs, so what is proven is the DECISION -- when we install, with what arguments, and what we do when it fails or lies -- and nothing about whether the package actually installs on a bare host. That stays unverified until the next host is built from scratch, and this row should be re-read then rather than assumed closed. MUTATION NOTE worth keeping: two mutants escaped the first battery because every refusal in this function returns 1, so deleting a guard just falls through to the next one with the same exit code -- the tests now assert the distinctive words, per CLAUDE.md section 10 |
| 98 | CLOSED @1079 | EDITING test5 DURING ITS OWN CAPTURE DRIFTS THE GRAPH PIN. test5's working tree IS the deployed tree, and capture step [2b] compares a source-derived graph hash against a pin written at deploy time -- so four uncommitted files turned a healthy capture into CAPTURE VERDICT: FAIL (graph exit=1) @1063, with unit 15709/0/0/26 and live 34/2/0 underneath it. Not a defect in the gate; the gate was right. Candidate fix: capture.sh refuses, or warns loudly, when `git status --porcelain` is non-empty -- it already knows it is running against a repo  CLOSED @1079: scripts/lib/tree_state.sh reports three states and capture.sh refuses a DIRTY tree at preflight, naming the paths, saying why it matters here, and warning off the stash-mid-run repair that makes it worse. A LIBRARY because backlog 35 asks the same question before a commit. UNKNOWN is NOT refused: making it fatal broke 13 tests that build a synthetic capture directory, which was the honest signal that a non-repo run cannot produce this hazard. Override CAPTURE_ALLOW_DIRTY (unprefixed on purpose), which REPORTS before honouring itself |
| 99 | CLOSED @1082 | TWENTY-SIX PRE-POLICY TEST FILES MAKE A REAL `git ls-files` CALL AND ARE IN NO CI SHARD. Measured at v3.66.1071 by AST over call nodes (not grep: `subprocess.run(["git","ls-files",...])` puts the literal inside a STRING, so a comment-stripped scan sees 3 and a docstring-inclusive grep over-reports). Several are named by CLAUDE.md section 4's own axis-6 table as repo-wide gates: `test_deploy_manifest_stays_retired`, `test_task_tracker_stays_retired`, `test_codex_handoff_stays_retired`, `test_gitignore_rules_actually_match`, `test_history_columns_go_through_migrations`, `test_playwright_engines_single_source`, `test_v3_66_820_share_tools_saw_no_session_keys`, `test_v3_66_944_static_kb_manifest_describes_the_tree`. The @1072 BD_GATE_SCOPE policy does NOT reach them -- they sit in the frozen baseline, exempt by construction, which is the price of adopting the policy without classifying 1314 files in one cut. Deciding each is a CI BUDGET question (the gates-job comment's one-minute rule is already breached at ~140s), so it is the operator's, not a mechanical sweep. Next step is to time the 26 and propose a shard split, not to bulk-add them  CLOSED @1082 on operator authorization (a CI budget change is a build change). RE-DERIVED at HEAD the population was 24, not 26 -- two had been declared by @1072 and @1080 in the interim, which is section 1's verify-then-act paying for itself. All 24 TIMED individually (196s total, all passing) and split across five shards drawn from measured time rather than count, per the @939 precedent: four balanced at ~33s each and `test_desandbox_tool_verifiers` alone at 65s, because no split puts every lane under the one-minute budget while that file stays whole and trimming a shard's list is forbidden. Classified rather than bulk-added: every one asserts an invariant over the TREE (tombstones, anti-duplication ratchets, denominator gates), which is exactly what CI's file-independent lane is for. NOT DONE, deliberately: they keep their `gate_scope_baseline.txt` entries rather than gaining `BD_GATE_SCOPE` markers -- _DECLARED is what CI reads, and marking 24 files is a mechanical cut of its own |
| 100 | CLOSED @1092 | THE @1079 TREE GUARD CHECKED PREFLIGHT ONLY, so it cannot see the failure it was written for. `capture.sh` refuses a tree that is DIRTY WHEN THE RUN STARTS (scripts/lib/tree_state.sh, backlog 98) and that is the whole check -- a tree that goes dirty DURING the run passes it. That is exactly what invalidated test5 at the 1082 capture round: nine files edited mid-run, step [2b]'s graph pin drifted against a source-derived hash, and the capture reported `graph exit=1`. The guard shipped HOURS BEFORE that happened and could not have caught it. THE SHAPE GENERALISES: a gate whose denominator is one INSTANT cannot answer a question about an INTERVAL -- section 0's denominator rule in the time dimension rather than the set dimension. Candidate fix: record the tree sha at preflight and re-check it immediately before the graph gate, naming the paths that moved; tree_state.sh already computes both halves, so this is a second call site rather than a new predicate. NOT a candidate: checking only at the end, which spends a forty-minute run to report something knowable at the start  CLOSED @1092. The postflight half now exists: `bd_tree_state_snapshot` records HEAD plus the porcelain listing after the preflight check passes, `bd_tree_state_drift` compares at the end, and a non-empty drift file grades the run INVALID (exit 3) rather than FAIL -- a THIRD verdict state, because spending the word FAIL on a void measurement is exactly how the 1082 round was read as a code defect over a green suite. HEAD is in the snapshot because a mid-run COMMIT leaves `git status` empty on both sides while moving the source the graph pin is derived from. UNKNOWN stays a third state at both ends: an unreadable tree returns nonzero rather than an empty snapshot, since two empty snapshots compare EQUAL and would certify no-drift over a subject nothing could see. CAPTURE_ALLOW_DIRTY does NOT suppress it -- the override consents to a known state at t=0, not to a tree that moves mid-run. MEASURED before shipping: all four hosts read 0 dirty paths after the 2026-08-13 capture round and tools/deployed_version.txt is gitignored, so the check does not fire on a normal run; 6/6 mutants caught, including one that drops the trailing-newline normalisation, which the over-sensitivity control caught live during this cut when it would have made EVERY capture report INVALID |
| 101 | CLOSED @1095 | `unittest.mock.patch.dict(sys.modules, ...)` RESTORES THE DICT TO ITS ENTRY SNAPSHOT, so any module FIRST IMPORTED INSIDE THE BLOCK IS DELETED ON EXIT. Nobody writes that deletion and no gate sees it. It poisons any identity-keyed lazy cache whose OWNER survives while its SUBJECT does not. MEASURED at v3.66.1085: httpx's module-global HTTPCORE_EXC_MAP survived while httpcore was evicted, the next import produced a second httpcore module object, and every isinstance() in that map failed for the rest of the worker process -- so httpx re-raised raw httpcore errors through the branch it marks `# pragma: no cover`. That reached production as test6's only capture failure at v3.66.1083. THE ASYMMETRY IS REQUIRED: evicting owner AND subject together is self-healing, because re-importing the owner resets the cache and it rebuilds from the live subject. DENOMINATOR, measured at f154aef: 28 call sites across 6 tracked test files use the idiom. v3.66.1085 closed the httpcore INSTANCE by pre-importing it in tests/conftest.py so it is in every later snapshot; THE CLASS IS OPEN, and the next victim is whichever lazily-imported module some future patch.dict block touches first. A general fix is NOT designed. Candidates: a lint forbidding the bare idiom (fights 28 shipped call sites); a helper that snapshots and restores by NAME rather than by dict identity; or a conftest hook that diffs sys.modules across each test and fails on a net DELETION. The third is closest to a real gate, but its cost is paid once per test, ~15600 times per capture, and section 2 rule 6 requires that cost be MEASURED before it is added  CLOSED @1095 WITH A RUNTIME GUARD RATHER THAN A CENSUS, and the audit is what forced that choice. tests/_sys_modules_guard.py wraps mock's dict restore and fails any test whose patch.dict(sys.modules) deletes a module it did not insert. THE AUDIT: all 28 sites across 6 files measured at e8bb3fd observed ZERO evictions, and the whole suite at -n 16 also observed zero -- so the allowlist is EMPTY as a measurement, not as an omission. But a zero from a census proves nothing here, because the hazard is SCHEDULE-DEPENDENT: whether a module is first-imported inside the block depends on what an earlier test imported, and --dist loadfile decides that. A static gate would have reported that zero as a pass. BOTH DIRECTIONS ASSERTED: fires on a real module imported inside the block, silent on the fake swap 27 of 28 sites use, silent on a real module the patch itself inserted, and it records rather than raises when a failure is already in flight. COST measured at 4 ms per whole capture (median of 5): the wrapper runs at 28 sites, not 15840 tests. WHAT IT STILL CANNOT SEE: it fires on the eviction, not on the harm -- harm needs another module holding an identity-keyed reference -- and it watches sys.modules only, while the same restore-to-snapshot semantics apply to every patch.dict target |
| 102 | CLOSED @1124 | A FULL SUITE AT `-n 48` WEDGES AT 99% WITH A DEAD XDIST WORKER, AND THE RUN THEN PRODUCES NO VERDICT AT ALL. MEASURED on test6, 2026-08-13 at f154aef, 48 cores, --dist loadfile, sequential samples: TWO OF SIX ended with `[gwNN] node down: Not properly terminated` as the final log line at `[ 99%]`, after which the master wrote nothing further for 462s and 255s before being killed, with a 1-minute load average of 0.06. DIFFERENT WORKERS each time (gw40, gw37), so it is not one bad worker. At `-n 16` on the same 48-core spec, ELEVEN OF ELEVEN samples completed. PROCESS EVIDENCE at wedge time: the master `Sl` holding a `Z` child it never reaped, plus TWO further zombies under a subprocess some test spawned and never waited on -- two leaks in one snapshot, and only the first explains the wedge. WHY IT MATTERS BEYOND THE SWEEP: `capture.sh --workers=$(nproc)` is exactly -n 48 on these boxes, so the capture parallel lane runs the configuration that wedges. Captures HAVE been completing and that lane runs a DESELECTED SUBSET rather than the whole suite, so its rate is unmeasured and is NOT assumed equal. DO NOT lower --workers until the rate is measured on the capture lane itself -- that would change the gate's shape on a guess. A NO-VERDICT RUN IS NOT A GREEN ONE: a log with no pytest summary line contains no occurrence of the word `failed` and reads as clean to anything scanning for it, which is why the sweep runner records `summary=ABSENT` and says the sample must not be counted  THE CAPTURE LANE'S RATE IS NOW MEASURED, AND THE ROW'S CENTRAL WORRY IS ANSWERED. MEASURED 2026-08-13 over EIGHT preserved rounds: 31 host-runs of capture.sh, ZERO with an absent verdict. The worker count was READ FROM EACH RUN'S OWN run-context line rather than assumed -- 31 of 31 at 48 workers, none unreadable -- because capture.sh defaults to 4 without the flag and an assumed denominator would have made the whole comparison meaningless. STATISTICS, STATED HONESTLY: one-sided Fisher exact on 0/31 versus the full suite's 2/6 gives p ~= 0.023, so the lane IS lower at the 5% level and no further. The lane's 95% CI is [0.000, 0.112] and the full suite's is [0.043, 0.777] -- SIX SAMPLES IS A VERY WIDE INTERVAL, and an earlier working estimate of ~1e-6 was WRONG because it treated the 2/6 point estimate as the true rate. THE OBVIOUS MECHANISM IS DEAD: the lane is not a small deselected subset. `--collect-only -m capture_parallel` collects 15075 of 15869 tests (94.999%), so 'fewer files, less scheduling surface' does not explain the difference and CLAUDE.md's own '176 files / 1458 tests' figure was stale by an order of magnitude (corrected @1103). SO WHAT REMAINS OPEN is the MECHANISM, not the rate: something other than lane size distinguishes a run that wedges from one that does not, and 794 deselected tests is where to look first. NO CHANGE TO --workers IS WARRANTED on this evidence: 0/31 is the lane behaving, and lowering it would trade a measured-good configuration for an unmeasured one  THE HANG'S MECHANISM IS NOW MEASURED AND THE ROW NARROWS TO ONE QUESTION. REPRODUCED AT HEAD (17ed291, v3.66.1107) on 2026-08-13, run test6-full-005-190725: a full suite at -n 48 printed `[gw28] node down: Not properly terminated` at 99% and then wrote NOTHING for 726 seconds at a 1-minute load average of 0.27. THAT KILLS THE HOPE THAT @1095's EVICTION GUARD OR @1100's httpx PRE-IMPORT HAD FIXED IT: the defect is live at HEAD, not merely historical at f154aef. WHERE THE MASTER IS STUCK, from py-spy under sudo (ptrace_scope is 1 on this fleet) taken BEFORE anything was killed: MainThread in xdist/dsession.py loop_once at `queue.get`, 48 receiver threads idle in execnet gateway_base `read`, and ONE unreaped ZOMBIE child still parented to the master. IT IS A LIVELOCK, NOT A DEADLOCK, and the distinction is the whole diagnosis: `loop_once` is `while 1:` around `self.queue.get(timeout=2.0)`, and it leaves that loop ONLY when `self._active_nodes` empties. The dead node's work is gone, no further event is ever queued, `tests_finished` never becomes true, so the master wakes every two seconds forever at zero load. Left alone it would still be running. WHAT IS STILL UNKNOWN, and is now the whole of this row: WHY THE WORKER DIES. There is no traceback, no INTERNALERROR, no FAILED line, and -- checked on all three hosts since 2026-08-12 -- ZERO OOM kills and nothing in dmesg. `Not properly terminated` is xdist's workermanage.py text for a channel that closed with no error attached, so the evidence that would name the cause dies with the worker. RATES AT HEAD, one commit, 57 samples, three arms ROTATED ACROSS THREE HOSTS so host is not confounded with arm: full 1 of 19, capture lane 0 of 19, the 800 deselected tests alone 0 of 19. The lane is now 0 of 50 counting the 31 preserved host-runs, so capture.sh's configuration remains the measured-good one and NO CHANGE TO --workers IS WARRANTED. Note the deselected arm does NOT wedge on its own, so 'a leaky test among the 800' is not the explanation either; those 800 do take as long as the other 15075, which says they are the heavyweight half rather than a small tail. THE FD-INHERITANCE HYPOTHESIS IS DEAD, and the way it died is worth keeping: a probe reported seven processes holding a worker pipe, which looked decisive and was an artifact of the probe's own denominator -- it collected EVERY pipe on a worker's fds, including the ones the worker itself created for its children, so a multiprocessing resource_tracker holding its own parent pipe scored as a hazard. An execnet channel is a pipe held by BOTH a worker and its master; counted that way there are 96 channels per host and THIRD-PARTY HOLDERS ARE ZERO, sampled every 20 seconds for the length of the sweep. WHAT SHIPPED @1111 IS THE OPERATIONAL HALF, not a fix for the livelock: capture.sh's run_with_heartbeat polled `while kill -0` with NO time bound, so a wedged lane would have hung a capture forever and written no verdict -- this row's own warning that a NO-VERDICT RUN IS NOT A GREEN ONE, live in the gate. It now lives in scripts/lib/heartbeat.sh with a wall-clock cap (CAPTURE_STAGE_CAP, default 5400s against a 219-315s measured lane), stops the process GROUP, returns 124, says so in the stage's own log, and capture_verdict.py names 124 as UNFINISHED rather than printing a bare number. The bound is in the PARENT deliberately: `--timeout=240 --timeout-method=thread` runs inside the worker and cannot fire when the worker is the thing that died, which is exactly why the wedge survives it  THE LIVE HYPOTHESIS AND THE EXPERIMENT THAT SETTLES IT, filed @1113 because it existed only in a session-close section that the next close supersedes wholesale -- prose that would have been DROPPED rather than deferred. EVERY WEDGE HAS BEEN ON test6. Phase 1 at 17ed291 across three rotated arms and 82 samples, then phase 2 at 283588d full-suite-only: on the full arm test6 is 2 of 6 against test4 0 of 10 and test7 0 of 10, one-sided Fisher p = 0.046, and the TWO HISTORICAL wedges behind the original row were also test6. A HOST AUDIT FOUND EXACTLY ONE STRUCTURAL DIFFERENCE: kernel, OS, Python, pytest, xdist, execnet and the commit are byte-identical across the three, all are 48-core VMware guests, and test6's root filesystem is EXT4 where test4 and test7 are XFS -- with /tmp on / everywhere, so the entire suite's tmpdir churn runs on ext4 there and XFS elsewhere. CPU model differs three ways and therefore isolates nothing. THE DISCRIMINATOR IS A WITHIN-HOST A/B and it is unfinished: `full-tmpfs` on test6 alternating against plain `full` -- same box, same commit, same command, TMPDIR on a 64G tmpfs -- so the filesystem is the only variable. It had 2 samples when the session paused; roughly 4 samples/hour, about 10 per arm for a usable comparison. If wedges follow ext4 and stop on tmpfs the filesystem is implicated; if they continue, ext4 is exonerated and test6 differs for some other reason. DO NOT REFORMAT test6 TO XFS TO "FIX" THIS before that arm reports -- it would destroy the only host that reproduces the defect, and a livelock survives any filesystem change anyway, so it would simply bite the next ext4 box instead. ZOMBIE ACCUMULATION IS ALSO DEAD as a cause: peak counts are comparable on all three (29/33/28), so the unreaped zombie under a wedged master is a CONSEQUENCE of the worker dying rather than a cause. ONE DESIGNED-BUT-UNAPPLIED FIX is worth carrying: a wedged master's stdout is never flushed, and `set -m` on the launching shell is what makes SIGINT reach a backgrounded job at all -- bash sets SIG_IGN for `&` jobs when job control is off and `trap - INT` cannot reset a signal ignored on entry, so the first SIGINT-before-SIGKILL attempt at this was inert. The instrument, the 94 recorded samples and the per-wedge forensics live in `~/bd-wedge-2026-08-14/` and are tracked nowhere -- that is row 117  *** THREE ANSWERS, 466 SAMPLES, 2026-08-13/14 AT df73ef7. *** (1) THE EXT4 HYPOTHESIS IS REFUTED. test2 -- a SECOND, independently built ext4 box at full fleet spec -- ran 0 wedges in 90 samples, while TWO XFS hosts each wedged once (test4 1/94, test7 1/92) -- AND test2 HAS SINCE WEDGED TWICE, 2/107, which STRENGTHENS rather than weakens the refutation: 1.9% sits with the XFS hosts' 0.8-0.9% and nowhere near test6's 28%, so every host now shows the same rare background wedge, exactly as 'a test crosses 240s and test6 is slower' predicts. The '0 in 90' figure was true when written at 12:00Z and is NOT current and both XFS wedges are the same phenomenon (99%, node down, line 224/229). test2 0/90 against XFS 2/290 gives one-sided Fisher p = 1.00: ext4 is NOT elevated. The pooled ext4-vs-XFS p = 0.017 is an ARTIFACT of test6 dragging its own filesystem's average up, and reading it as support would be the confound this whole fleet expansion was built to remove. DO NOT REFORMAT test6 -- the filesystem was never the variable, and the earlier p = 0.046 rested on the ext4 side being one machine. (2) TEST6 IS THE ANOMALY, NOT ITS FILESYSTEM: 7/30 against 2/380 for every other host, p = 1.7e-07, and 7/30 against its own ext4 twin's 0/90, p = 3.4e-05. Forty-fold. The software stack was already audited byte-identical, so the difference is BELOW what that audit could see -- firmware, NUMA layout, VMware host placement, disk backing, CPU stepping. That is the new question. (3) THERE IS A SIZE THRESHOLD AND IT IS REAL. On test6, same host and same night, full 7/30 against 0/54 for the reduced arms (full-50 and full-25 combined), p = 4.5e-04. FIFTY-FOUR consecutive reduced-size runs, zero wedges, on the box that wedges 23% of the time at full size. The threshold sits between 50% and 100% and gives a far cheaper reproducer than a full suite. Note what it does NOT separate: fewer files means both less accumulation AND a shorter run. *** AND THE MECHANISM, CONFIRMED ON TWO INDEPENDENT WEDGES: THE WORKER DOES NOT DIE, IT EXITS(1). *** For both dead workers, bpftrace recorded raw_exit_code=256 -- (1<<8)|0, EXIT STATUS 1, NO SIGNAL -- once per thread, with ZERO fatal signals beforehand, no core dump, no OOM, and no PYTHONFAULTHANDLER traceback. The only SIGKILL either ever received came from `sender=bash` FOURTEEN MINUTES LATER: the hunt's own cleanup on a process that was already a zombie. DO NOT MISREAD THAT SIGKILL AS THE CAUSE. This explains every previously-mysterious symptom: no traceback because faulthandler only fires on fatal signals; no core because there was no signal; `Not properly terminated` because that is xdist's text for a channel closed with NO ERROR ATTACHED, which is exactly what a clean exit produces; and the livelock because the master never sees a crash to report, so `_active_nodes` never empties. ALL FIVE WEDGES stopped at line 224 at [ 99%] on five DIFFERENT workers (gw42/gw28/gw40/gw32/gw23) across two commits, with the master at `loop_once (xdist/dsession.py:154)` holding exactly one unreaped zombie. SO THE ROW'S QUESTION IS NOW: WHAT CALLS exit(1) INSIDE A PYTEST WORKER DURING THE DRAIN PHASE? Exit status 1 is pytest's own 'tests failed' code, which is a lead rather than an answer. An exit tracer is installed fleet-wide via a .pth in each venv (a plain sitecustomize.py is SHADOWED by Ubuntu's own at /usr/lib/python3.12/) and dumps the caller's stack; it covers os._exit and sys.exit but NOT a bare `raise SystemExit(1)`, which CPython handles without consulting sys.excepthook. If the next wedge produces no trace, that absence is itself the finding  *** CLOSED @1124. THE MECHANISM IS pytest-timeout, AND IT IS NOT AN XDIST BUG. *** The exit tracer caught the dying worker's OWN STACK on wedge 6 -- a direct observation of the process as it left, not a correlation: `os._exit(1)` at `pytest_timeout.py:542`, called from its timer thread, in worker gw17. `--timeout-method=thread` CANNOT unwind another thread, so on a timeout it hard-exits the whole worker BY DESIGN. That single call produces every symptom this row spent its life describing: exit status 1 with no signal, no core, no traceback, all threads gone, and a channel the master sees close with NO ERROR ATTACHED -- which is exactly what `Not properly terminated` means -- after which `_active_nodes` never empties and `loop_once` spins on a 2s `queue.get` forever. VERIFIED BY CONSTRUCTION, not only by observation: a 20s test against a 5s timeout gives `worker 'gw7' crashed` and 9 failures in 50s under `thread`, against `Failed: Timeout (>5.0s) from pytest-timeout`, 1 failed 2 passed in 6.01s, under `signal` -- so the mechanism reproduces synthetically in under a minute. MY OWN LEADING CANDIDATE WAS WRONG AND IS WORTH RECORDING: execnet's `gateway_base.py:1253` `os._exit(1)` fitted every symptom -- exit 1, no signal, shutdown phase -- and was still not the path; the self-SIGINT it predicts never appeared in 480 samples. Matching symptoms is not measuring, which is the same lesson the ext4 hypothesis taught this row four hours earlier. EVERY EARLIER RESULT NOW AGREES: test6's 7/30 against 2/380 elsewhere is a SPEED difference, not a weird box -- it crosses 240s more often; the size threshold (0/54 reduced against 7/30 full) is less contention, so nothing crosses 240s; the two XFS wedges are the same mechanism, rarer; and 99% is the drain phase, where the last and longest files run with the least parallelism left to hide them. THE IRONY IS THE KEEPER: `--timeout-method=thread` was chosen to NAME a hanging test, and it is the reason the hanging test cannot be named. WHAT THIS ROW DOES NOT CLOSE goes to row 144: a test is exceeding 240s on test6, and xdist's recovery path -- which the synthetic probe showed working mid-run, replacing the killed worker and naming the test -- does NOT recover during the drain. Two defects, one symptom |
| 103 | CLOSED @1105 -- NOT REAL | THE PREMISE OF THIS ROW DID NOT REPRODUCE -- IT CLAIMED THE RUN-CONTEXT WORKER CHAINS ARE WRITTEN AT SESSION END, SO THE ONE RUN THAT MOST NEEDS THEM IS THE ONE WITHOUT THEM. `tests/_run_context.py` writes /tmp/bd-runctx/<pid>/gwNN.chain per worker, and every run's own footer tells you to replay a failure with `bd-ladder --chain ...`. MEASURED on test6 2026-08-13 during the row-102 wedge: the wedged run had written SIX chains out of FORTY-EIGHT workers, and the dead worker's (gw37) was not among them. CLAUDE.md's `never filter at capture time` rule in the time dimension -- the evidence is discarded by construction rather than by a `head`. Contrast with what DID survive and was sufficient: the pytest log, written incrementally, whose last line named the dead worker. Candidate fix: append each worker's chain entry as the file is ASSIGNED rather than at session end, since a partial chain is strictly more than none. Cost must be MEASURED first (section 2 rule 6): it is a per-file write on a path that runs 1258 times per suite  CORRECTED @1101, FIX WITHDRAWN, ROW STAYS OPEN ON THE UNEXPLAINED OBSERVATION ONLY. tests/_run_context.py's `note_file` has always appended PER TEST and reopens the file per write, and its docstring gives exactly this reason: "a worker killed mid-run must still leave a readable chain, and that is the run an investigation cares most about". MEASURED at 3.66.1099 under `-n 8 --dist loadfile`: EIGHT chains for EIGHT workers, complete and incremental. So the proposed fix -- write each entry as it is ASSIGNED rather than at session end -- describes work already done, and the ~2% cost budget it was gated on is moot. WHAT REMAINS UNEXPLAINED: the 6-chains-for-48-workers count taken during the row 102 wedge on 2026-08-13. The mechanism above says a wedged run should still have one chain per worker that ran anything. The likeliest reading is that the directory inspected was not that run's -- the path is keyed by the MASTER's pid -- but that was NOT established then and cannot be now. Closing this row would assert the diagnostic is fine; implementing it would ship a change with no defect under it. CLAUDE.md section 1 on a row written the same day: a plausible mechanism recorded at 2am reads as a measurement by morning  CLOSED @1105 AS NOT REAL, with the trigger to revisit named rather than left implicit. VERIFIED at the exact width the row worried about: run 534622 on test6 -- 48 cores, 48 workers, dist=loadfile, the Phase C capture at 16:10Z on 2026-08-13 -- wrote 48 CHAINS FOR 48 WORKERS. The @1101 correction already established that note_file appends per test and reopens the file per write; this is the positive measurement that it does so at scale. UNVERIFIABLE: the wedged run behind the original 6-of-48 count was pid 108402 (read from the preserved ps snapshot; the run-context directory is keyed by the MASTER's pid), and that directory no longer exists -- /tmp/bd-runctx holds exactly 20 entries, which is prune(keep=20) doing its job. THE TENSION IS WORTH KEEPING even though the row closes: the retention bound that prevents section 0's 744-leaked-directory failure is the SAME mechanism that destroyed this row's forensic evidence, and nothing currently distinguishes a run that FAILED from one that passed when deciding what to keep. WHAT WOULD REOPEN THIS: any future wedge whose run directory holds fewer chains than the run had workers -- a one-command check now that the expected value is known |
| 104 | OPEN | THE PER-RUN TEMP ROOT IS RECLAIMED BY A pytest SESSION HOOK, SO A KILLED RUN LEAKS ONE ROOT PER WORKER. tests/_tmproot.py installs a per-process root and removes it in `pytest_sessionfinish`; @1080 shipped that against a /tmp holding 15392 entries. A session hook cannot survive SIGKILL -- the same shape CLAUDE.md section 6 already records for bd-mutate's `finally`, where SIGTERM and SIGKILL both leave the mutant on disk while only Ctrl-C unwinds. Each xdist WORKER makes its own root (the module says so: keyed by pid because workers are separate processes), so a run killed at -n 48 leaks up to 49 roots at once. MEASURED 2026-08-13 at v3.66.1111, after a wedge hunt that killed roughly twelve runs across three hosts: 418 dirs / 5.1G on test4, 413 / 11G on test6, 342 / 3.0G on test7 -- about 19G total, with the oldest timestamped 05:02 the same day, so runs before this session contributed too. Disk was never at risk (3-5% used on 2.0T) and that is exactly why nothing noticed. A SECOND SHAPE IS VISIBLE IN THE SAME CENSUS and is not explained by kills: 72 of test4's dirs are EMPTY -- contents reclaimed, directory left -- which either means rmtree partially succeeded or a root was created by a process that wrote nothing, and those are different defects. NOT FIXED, and the obvious fix is wrong: an atexit or a signal handler still cannot catch SIGKILL, so it converts a reliable leak into a rare one, which is the partial fix that reproduces the shape (section 0). Candidates worth costing: a boot-time or capture-start sweep with a retention bound, the way capture.sh already prunes its own run dirs on the way IN rather than out (@1099); or having any harness that KILLS a run own the cleanup, since it is the only party that knows the run died. ANY TOOL THAT KILLS pytest INHERITS THIS -- bd-wedge-hunt does, and so does the new CAPTURE_STAGE_CAP path added at @1111, which stops the process group and therefore leaks whatever the lane had open  THE ROW UNDERSTATED ITS OWN LEAK POPULATION, corrected @1113 by reading the source rather than the row. `tests/_tmproot.py` `finish(exitstatus)` returns False WITHOUT REMOVING ANYTHING when `exitstatus != 0`, deliberately and with its reason in the docstring -- @1080's rule that a debugging directory deleted on the one run that needed it is worse than the leak. So the leaking population is not "killed runs"; it is EVERY RUN THAT DID NOT EXIT ZERO, which includes every ordinary red suite anyone has ever run. That materially changes the fix: a sweep cannot simply reclaim what it finds, because some of those roots are the artifacts of a failure someone may still want. The retention-versus-forensics policy this needs is not written anywhere and is now row 132. FIVE FLEET RESIDUES WITH NO REMOVAL OWNER, filed here because they were recorded only in a session-close section the next close supersedes: the ~19G of leaked `bd-testrun-*` roots above; `~/.bd-tools-venv` plus `~/.local/bin/py-spy` on test4/test6/test7 at 21M each (ptrace_scope is 1, so py-spy needs sudo); a 64G tmpfs at `/mnt/bdtmpfs` on test6 that consumes RAM only as used and does NOT survive a reboot; `~/.ssh/authorized_keys.pre-row13` on all three with every removed key's fingerprint preserved in `~/bd-session-2026-08-13/row13_key_inventory_before.txt`; and `~/cwatch.sh` plus `~/bd-channelwatch.log` on all three, whose loops are stopped and whose logs are the evidence for row 102's two dead hypotheses. NONE of these is in any tracked file, so a fleet rebuild silently discards them and nothing notices -- the same class as `test5`'s untracked `~/.ssh/config`, which `docs/repo/FRESH_HOST_BRINGUP.md` now covers. SEE ALSO ROW 146: the same reclaim gap for the leaked PROCESS rather than the leaked directory -- a wedged master survives SIGINT and stays resident, measured at five orphans and 11.6 hours at the end of the 2026-08-14 hunt |
| 105 | CLOSED @1116 | NEITHER TRACKER GATE RUNS IN CI, so the two checks that guard the registers are themselves ungated. MEASURED at f0c5667: `test_register_promises_resolve` and `test_v3_66_1052_the_backlog_is_machine_visible` appear ZERO times in `.github/workflows/ci.yml`, ZERO times in `_DECLARED`, and BOTH sit in the frozen `tests/gate_scope_baseline.txt` (1329 entries). CLAUDE.md section 7 states the rule this breaks -- a gate CI does not run is a gate that does not exist -- and the register is the one subject where that failure is invisible, because a stale register still READS fine. This is the CAUSE behind rows 106, 107 and 142; do it first. Cheapest row on this list: two shard entries  CLOSED @1116, and the estimate was right -- two shard entries, two markers, and two lines removed from the baseline. THE POLICY'S OWN GATE PROVIDED THE RED: adding `BD_GATE_SCOPE = "repo-wide"` to the two files made test_v3_66_939 fail twice, naming both files, from two independent directions -- declared-but-not-in-`_DECLARED`, and both-baselined-and-declaring-a-scope. That is the mechanism @1072 built working as designed: forgetting is RED rather than silent. Both now run in the `toolchain` shard beside test_toolchain_534, which is bd-freshcheck's own suite and the same family of subject. RE-MEASURED AFTER: ci.yml names them 2 times (was 0) and the frozen baseline names them 0 times (was 2, and 1329 -> 1327 entries -- it may only shrink, and it shrank). NOT COVERED, and it is the same gap row 106 records: these gates read the two registers, and a CI job that runs them still cannot see the 111 tracked `docs/**.md` and 5 `pending-specs/*.md` that no freshness gate reaches |
| 106 | OPEN | THE FRESHNESS GATES CANNOT SEE THE DOCUMENTS WHERE TASKS HIDE. `bd-freshcheck`'s `_DOCS` is a 2-tuple, `CLAUDE.md` and `project-knowledge/SESSION_CARRY.md` (toolchain/bin/bd-freshcheck:50); `bd-doc-truth` globs `project-knowledge/*.md` NON-RECURSIVELY plus the repo root. MEASURED at f0c5667: 111 tracked `docs/**.md` and 5 tracked `project-knowledge/pending-specs/*.md` sit outside BOTH gates -- 71 markdown files live under project-knowledge and only 66 are at the top level, so the pending-specs escape by exactly ONE directory level. That is why a directory literally named `pending-specs`, each of whose files says DELETE THIS FILE when its item ships, sat four-fifths shipped for 260 releases. The fix is a DENOMINATOR, not a new gate -- and per row 26's lesson, state the denominator in the gate's own output |
| 107 | OPEN | EIGHTEEN project-knowledge FILES STILL ROUTE "what is open" TO THE RETIRED TASK_TRACKER. MEASURED: `git grep -ln TASK_TRACKER -- project-knowledge/` = 18 against `git grep -ln IMPROVEMENT_BACKLOG -- project-knowledge/` = 2. `docs/PROCESS_CONVENTIONS.md` section 2 still calls `TASK_TRACKER_DATA.json` "the single source of task truth" -- a file that `tests/test_task_tracker_stays_retired.py` guarantees will never exist again. That gate cannot catch this: its own docstring says "Prose mentions are fine", so it bans the FILE and permits every POINTER to it. This is the single most direct cause of the operator's stated problem -- a new session reads the prose, goes looking for a register that was deleted, and files nowhere |
| 108 | OPEN | THE LEDGER GATE CHECKS ONLY ONE DIRECTION OF LEDGER-VERSUS-INVENTORY AGREEMENT. `test_no_ledger_declares_open_an_item_the_inventory_has_closed` catches a ledger saying OPEN over an inventory that says CLOSED. The MIRROR pair is unchecked: an entry whose inventory text still reads OPEN while some ledger declared it CLOSED passes direction B, because `test_every_inventory_entry_is_accounted_for` accepts membership in `_closed_ever()` as "accounted for". A survey of the 48 entries put 13 in that state. Read the existing test's docstring first -- it explains why the check is scoped to the NEWEST close only, and the mirror check needs the same restraint or it will demand the rewriting of history the ledger exists to preserve |
| 109 | OPEN | `bd-band-derive` REACHES BOTH ITS GATES ONLY VIA A FILENAME LITERAL IN THE TEST SOURCE (`tests/test_desandbox_tool_verifiers.py:66`, `BD = BIN / "bd-band-derive"`). A rename or a move silently narrows every future band, and the gate goes green because it is asserting over a path that no longer resolves -- CLAUDE.md section 0's shape, in the tool that DERIVES the bands. Low cost, and that same test file's own docstring records the last time this tool verified nothing |
| 110 | CLOSED @1115 | LIVE CHECK L13 CANNOT FAIL -- ITS ELIGIBILITY PREDICATE IS CONSTANT-TRUE. `live_tests/checks.py:1263` selects eligible rows with `m.get("installed") or m.get("library") or m.get("library_installed")`. VERIFIED at f0c5667 against the producer: `bulk_downloader/dev_suite/capture_diag.py:374-380` emits `site_id`, `library`, `host_pattern`, `adapter`, `library_installed` -- it NEVER emits `installed`, and `library` is the library NAME, a non-empty string on every well-formed row. So the middle operand is truthy regardless of whether anything is installed, `eligible` always equals `matrix`, the "no configured site maps to an installed extractor library" WARN is UNREACHABLE, and the PASS text reports a constant N of N. The one fact the check exists to establish -- that a library is actually present -- is the one it cannot observe. The `library_installed` key it should read is already right there  CLOSED @1115. The predicate is now `m.get("library_installed")` alone and the log line says INSTALLED so the reader can tell which question was asked. A row carrying no `library_installed` key is NOT counted eligible -- absent is not installed, and treating it as installed is how the original read. PROVEN RED FIRST in both directions: on pristine source the check reported `4 of 4 site(s) are library-extractor eligible` for a matrix in which ZERO were installed, and the over-sensitivity control (one installed of two) was written in the same file so a fix that simply always warns would fail. MUTATION: reverting the operand to `m.get("library")` is CAUGHT |
| 111 | CLOSED @1115 | LIVE CHECK L29 RETURNS PASS ON A GENUINELY KILLED VPN TUNNEL, AND ITS TEST FABRICATES THE KEY THAT WOULD HAVE CAUGHT IT. `live_tests/checks.py:2913` computes active kills as `[s for s in states if isinstance(s, dict) and s.get("killed")]`. VERIFIED: `bulk_downloader/vpn_kill_switch.py:63` is a dataclass whose fields are `tunnel_id`, `killed_at`, `reason`, `state` (`"killed"`/`"cycling"`/`"cleared"`), and four more; `to_dict()` is `asdict(self)`, so the emitted key is `state` and there is NO `killed` key -- `killed_at` is a float timestamp and is not what `.get("killed")` reads. `active` is therefore always empty and the FAIL branch is dead. WORSE, `tests/test_t46_l29_vpn_kill_switch.py:46,115,133` builds its fixtures as `{"tunnel_id": ..., "killed": True/False}` -- the test manufactures the very key the product does not emit, so it exercises a contract nothing produces and reports green. This is the highest-value row in the group: a safety check that always passes, held up by a test that always passes  CLOSED @1115, and the fallout was the confirmation. The predicate now reads `s.get("state") in ("killed", "cycling")`; `cycling` counts as ACTIVE deliberately, because it means the tunnel was killed and an auto-cycle is in flight so it is not carrying traffic, and vpn_kill_switch reverts it to `killed` when the cycle fails. A THIRD STATE WAS ADDED so this cannot recur silently: a record carrying no `state` field now returns WARN naming the record rather than PASS, because without it the next rename puts the check straight back to blind. THE OLD TESTS THEN WENT RED, WHICH IS THE PROOF: all three fabricated fixtures (`{"tunnel_id": ..., "killed": True}`) are themselves records with no `state` field, so the new WARN branch caught the test suite's own fabrication. All three were corrected to the shape `KillState.to_dict()` actually emits, and a repo-wide grep for a fabricated `"killed":` now returns zero. The new gate builds its input by CALLING the real to_dict() rather than writing a literal, and asserts the producer's shape as a precondition so it reports UNRUNNABLE if KillState ever grows a real `killed` key. MUTATION: 4 of 4 caught, including the revert |
| 112 | OPEN | `ARCHITECTURE_INVENTORY.md` HAS EXACTLY ONE COMMIT IN ITS ENTIRE HISTORY AND NOTHING REGENERATES IT. MEASURED: `git log --all -- '*ARCHITECTURE_INVENTORY.md'` = 1 commit; regenerating gives a 103-line diff (edges 1273 to 1634, modules 751 to 813, LOC up 15.7 percent). `tests/test_architecture_inventory.py` asserts against the FRESHLY BUILT dict and never against the committed file, and `grep -c ARCHITECTURE_INVENTORY tools/build_release.py` = 0, so no `--check` exists. The trap worth naming: `DOC_HYGIENE_AUDIT_v3_66_811.md:102-105` CLAIMS this file is gated and cites section 0 as the reason -- a document asserting the existence of the gate whose absence it is an instance of. Either regenerate it in the release chain or delete it; a stale architecture map is worse than none |
| 113 | CLOSED @1117 -- PARTIAL, and the remainder is named | THREE LIVE OPERATOR PAGES LINK TO `/cockpit/home`, RETIRED AT v3.66.344 -- 768 releases ago. VERIFIED at f0c5667: `bulk_downloader/app_settings_center.py:350`, `bulk_downloader/app_report_center.py:75`, `bulk_downloader/app_template_manager_ui.py:319`, each an anchor reading "Cockpit Home"; `grep -c 'cockpit/home' ENDPOINT_CATALOG.md` = 0, and `bulk_downloader/app_cockpit_home.py:121` carries the note recording the retirement. The gate built for exactly this class cannot see it: `nav_reachability.py` crawls INBOUND reachability, so an endpoint nobody links TO is caught and a dead link pointing OUT is not. Same file notes dead `_item_html`/`_group_html` awaiting "a later cleanup cut" since v3.66.344  CLOSED @1117 as PARTIAL. All three breadcrumbs now point at `/`, the SPA that replaced the hub, and `tests/test_v3_66_1117_cockpit_home_stays_retired.py` is a TOMBSTONE gate over the tracked tree so they cannot come back. THE GATE FOUND MORE THAN THE ROW DID, which is the argument for writing it rather than editing three lines: `docs/NAV_CONSOLIDATION.md` still described `/cockpit/home` as "the **hub** for everything cockpit-side" and stated that "every center shell page carries a left-arrow Cockpit Home breadcrumb" -- in the present tense, 772 releases after the page was retired. THAT DOCUMENT IS WHY THE DEAD LINK SURVIVED: a reader checking whether the breadcrumb was correct found a document saying it was intended. Corrected in place with the stale bullets struck through rather than deleted, because the v3.66.3xx arrangement is worth keeping as history. Note the two blind spots that let it live -- `docs/**.md` is outside both freshness gates (row 106), and `nav_reachability.py` crawls INBOUND reachability so a link pointing OUT at a dead route is structurally outside the denominator of the gate built for dead links. THE REMAINDER, NOT DONE: no general outbound-link checker exists. The obvious predicate -- every `href="/..."` in served HTML resolves to a registered route -- fires on correct code, because the SPA serves client-side paths that are not Flask routes, and section 0 is explicit that over-sensitivity is a soundness bug rather than a safe default. The dead `_item_html`/`_group_html` helpers are also still there; they are unreferenced and harmless, and removing them is a wider blast radius than this row justifies |
| 114 | OPEN | `project-knowledge/AUDIT_2026_07_29.md` HOLDS NINE LIVE FINDINGS IN A FLAT REPORT NO GATE READS, and rows 110 and 111 of this backlog are two of them -- found again, a year of releases later, by an audit that did not know the first one existed. That is the cost of a finding filed in prose. Two of the remainder (#19d, #19e) are regeneration gates in NO band and NO regen chain, so they cannot fire at all. File the remaining seven here with their evidence and then retire the report, or the next audit pays for them a third time |
| 115 | OPEN | RETIRE `scripts/build_release.sh`. Named in KICKOFF section 5 tier 2 as deferred; still tracked at f0c5667 alongside `tools/build_release.py`, and it appears ZERO times anywhere in this backlog. It is the clearest single instance of the class this consolidation exists to end -- deferred in prose, therefore dropped. Check `tests/test_build_release_f02.py` and `tests/test_v3_66_736_build_release_path_leak.py` for which of the two they actually exercise before removing either  SCOPED @1118, NOT DONE, and the measurement is why: this is a four-gate cut, not a deletion. MEASURED at aaea98e -- (1) `tests/test_generated_artifact_workflow.py:195` READS the file and asserts five separate facts about its contents, including the ORDER of two lines (`release.index("bd-regen-order") < release.index("Z=")`); (2) `tests/tracked_source.py` and `tests/test_v3_66_918_tracked_source_denominator.py` enumerate `git ls-files -z -- '*.py' '*.sh'`, so removing a tracked `.sh` moves their denominator; (3) THE FILE POSITIVELY PINS THE RETIRED SANDBOX HOME PATH -- 3 occurrences, and the literal is deliberately NOT spelled here because `tests/test_sandbox_home_stays_retired.py` bans it in tracked files and this row failed that gate on its first attempt, which is the same naming-the-removed-thing trap CLAUDE.md section 0 records. SESSION_CARRY notes in FOUR places that a blanket residue sweep turns red BECAUSE of those pins, and `pending-specs/s5-home-claude-residue.md` explicitly requires them to REMAIN. So a sweep and this retirement are in direct conflict and whichever lands second breaks the first; (4) `pending-specs/KICKOFF.md:222` already sized it -- 'adds a 4th axis-6 `*_stays_retired` gate, moves the `.sh` enumerator, needs section 4's table corrected'. THE ORDER THAT WORKS: retire this file FIRST, then run the retired-sandbox-home residue sweep, because that sweep's only remaining blocker is the pins in a file that is going away. Doing it the other way round means editing pins in a file about to be deleted. NOT attempted at @1118 rather than attempted badly: a deletion that moves three denominators and contradicts a filed spec is not a 40-minute cut, and the row said 'small' because nobody had measured it |
| 116 | MOOT @1113 -- investigated at f0c5667 and DID NOT REPRODUCE | A 2026-08-13 audit reported that `docs/eligibility_governance.md:26` and `docs/oracle_eligibility.md:16` state in bold present tense that no Class C apply path exists while `apply_path_exists()` in fact returns True for four subjects. EXECUTED at f0c5667 against the installed venv: `apply_path_exists()` returns **False** for `live_site_config`, `template`, `credential`, `schedule`, `oracle` and an unknown kind -- six of six. The function is `return agr.has_reverser(kind)` over a reverser registry that nothing has populated, which is precisely what the documentation says. The doc is ACCURATE and there is no defect. RECORDED RATHER THAN DELETED because the failure mode is reusable: an auditing agent that IMPORTS apply modules to inspect them registers their reversers as a side effect and then measures a registry its own probe filled. The subject moved under the instrument -- section 0, inverted |
| 117 | OPEN | `bd-wedge-hunt` IS TRACKED NOWHERE. It is the instrument built to answer row 102 -- the multi-host arm rotation, the wedge detector with its synthetic positive control, the per-wedge forensic capture (process forest, `/proc` status and wchan and per-thread kernel stacks, py-spy plain and blocking and `--locals`, dmesg, run-context chain counts) -- and it lives in `~/bd-wedge-2026-08-14/` on one machine, in one operator's home directory, which is the same condition backlog item 85 was raised to end. It produced 94 recorded samples and three reproductions. @1102 and @1104 are the precedent for rescuing a tool that earned its place; `toolchain/bin` is where it belongs. Rescue the synthetic positive control with it -- without that, a zero-wedge result is indistinguishable from a detector that matches nothing |
| 118 | OPEN | ZERO MUTATION SPECS HAVE EVER BEEN TRACKED, so no battery's evidence can be re-run. CLAUDE.md section 6 mandates `bd-mutate --spec mutants.json` and the tool exists at `toolchain/bin/bd-mutate`, but MEASURED at f0c5667 `git ls-files` matching `mutants*.json` = 0, and the six tracked files matching mutant/mutation are three frontend modules, a redaction gate and its test, and `bd-mutation-test` -- none of them a spec. Every battery this project has ever run therefore survives only as CHANGELOG prose asserting a CAUGHT count that nothing can reproduce. This session's two specs were rescued by hand to `~/bd-session-2026-08-13/mutants_1108.json` and `mutants_1111.json` and are the only ones that exist anywhere. Decide where a spec lives and make the tool write it there |
| 119 | OPEN | LEDGER ITEM 31 SUB-ROW **CAP-ROBUST arm C**, given a backlog id so it stops living as unnumbered prose. Held-open capture anti-bot resilience: arms A (page-death recovery) and B (operator re-nav) PASSED live; arm C -- challenge settle/resume acceptance -- is pending and is live-capture bound. `_inventory()` in the ledger gate matches only `N. **`, so this and rows 120-126 were structurally invisible to every check the ledger has |
| 120 | OPEN | LEDGER ITEM 31 SUB-ROW **JW-TMPL**. Auto-template for signed JWPlayer behind akamai/cloudflare; sandbox-verified on a real WACZ, blocked on a live capture with ultrafilms credentials. CARRY THE @965 CORRECTION WITH IT: 15.15 claimed the blocker was "site Ultra missing password" and the same condition the startup selftest prints on every restart, and that one credential fill might clear the row. Measured across all five 2026-08-08/09 captures, the message is `login: SKIPPED -- site 'wow' is missing password`, it lives in `04_service_boot.log` and NOT in the selftest (whose 12 checks are 11 ok / 1 warn and include no credential check at all), and the site is **wow**, not Ultra. Filling wow's password clears a boot warning and does nothing here. Two sites conflated into one shortcut -- do not re-conflate them |
| 121 | OPEN | LEDGER ITEM 31 SUB-ROW **LOGIN-NSTEP**. Cross-origin N-step login flow: the code is LIVE and unit-tested offline; what remains is the live cross-origin browser drive, which is operator-verify and cannot be closed from a unit run |
| 122 | OPEN | LEDGER ITEM 31 SUB-ROW **P3-T12-CALLSITE**. The held-open runner routes challenge settle through the `session_capture` seam. Detect, pause and handoff were observed live and a human completed a real CAPTCHA; what is missing is an explicit detector-cleared/resume event. THE RESTRAINT IS PART OF THE ROW: it was deliberately NOT closed by inferring resume from later authenticated activity, and that call should survive re-derivation -- inferring the event from a downstream consequence is asserting over a subject the instrument never saw |
| 123 | OPEN | LEDGER ITEM 31 SUB-ROW **RPTYL**. Reptyle authenticated capture. The capture COMPLETED -- redacted WACZ, two resources, zero residual secret findings -- but the row's legacy close criterion is `api_patterns >= 1` and the run produced `api_pattern_count = 0`. DO NOT CLOSE IT BY RELAXING THE CRITERION SILENTLY. Either meet it, or retire the criterion explicitly and in writing, which is a different and smaller decision |
| 124 | OPEN | LEDGER ITEM 31 SUB-ROW **FR-A6.2**. Builder enrichment slice 2, non-CF and non-video.js breadth. The recognizers are implemented and synthetically tested; what is missing is real guided-capture corpus evidence, so this is corpus-bound rather than code-bound |
| 125 | OPEN | LEDGER ITEM 31 SUB-ROW **FR-A6.3**. Builder enrichment slice 3, the remainder. Same shape and same blocker as row 124; if corpus evidence arrives it likely closes both, and they are separate rows only because the ledger carried them separately |
| 126 | OPEN | LEDGER ITEM 31 SUB-ROW **2c-DATA**. Reptyle selector re-capture. The re-capture is COMPLETE; five recaptured selector kinds still need review and a test against the live DOM |
| 127 | OPEN | LEDGER ITEM 31 DECISION **EXIT-3** -- OPERATOR-BOUND, and the answer to the obvious shortcut is already measured. PostgreSQL cutover exit-criterion: full suite green post-cutover plus an operator soak of at least two weeks. A GREEN CI `postgres-integration` JOB DOES NOT MOVE THIS ROW -- that job exercises the mod3 code paths and says nothing about production dual-write or a soak clock. Re-derived at v3.66.965 by running the preflight: `preflight_cutover()` returns `ok=False` with four reasons (dual-write not enabled, shadow-read not enabled, "shadow-read has compared 0 statement(s): zero comparisons is NOT evidence of agreement, it is an empty denominator", postgres not reachable) and `cutover_engaged()` is False. Correctly blocked fail-closed, in the product's own words. What is needed is an operator decision to start a soak, not a code change |
| 128 | OPEN | LEDGER ITEM 31 DECISION **OPV-F3.1** -- LAPSED, NOT OPEN, and the distinction is the whole row. Saved-search enqueue lane over seven uninterrupted days; the clock started 2026-07-23T01:37:15Z and the window ENDED 2026-07-30, which is now over a year of releases ago. It either closes on evidence already collected or the clock restarts from zero. An expired window is not an open task, and carrying it as one has let it sit un-decided through every re-derivation since. This is an operator decision and nobody else can make it |
| 129 | OPEN | CLAUDE.md SECTION 7 NAMES ITS OWN BLIND SPOT AND NOTHING TRACKS IT: line 1806, "the blind spot that remains: nothing checks that a `"module"` answer is honest". A test file may declare `BD_GATE_SCOPE = "module"` and thereby exempt itself from the repo-wide band while actually asserting over the whole tree, or the reverse. The declaration is taken on trust, which makes it the one input to the band machinery with no verification behind it. Note the shape before designing a fix: row 27 refused a marker-based mechanization after measuring that the declaration route was 0 for 2 against its own worked examples |
| 130 | OPEN | CLAUDE.md SECTION 4 NAMES ITS OWN RESIDUAL: line 889, the source-window ratchet counts windows and "does not catch a shift inside an existing one". THE COST IS ALREADY ON THE RECORD -- @835 shipped a shift that failed on the box after FOUR green container bands, which is the exact failure a window ratchet is built to prevent. Counting windows is a proxy for the thing that matters; the content inside a window can move freely under a constant count |
| 131 | OPEN | CLAUDE.md SECTION 5 NAMES AN UNTESTED DENOMINATOR: line 1182, a green result "was PARALLEL; a serial full run is a different denominator and is still untested". Everything this project knows about suite health comes from `-n 48` under `--dist loadfile`. A serial run is a different execution order, a different import graph and a different tmpdir population, and no one has ever measured it -- so "the suite is green" is a claim about one configuration being read as a claim about the suite. Cheap to answer once; expensive to keep assuming |
| 132 | OPEN | NOTHING DECIDES BETWEEN RETENTION AND FORENSICS FOR A RUN THAT FAILED VERSUS ONE THAT PASSED -- stated at @1105 and never resolved. It is now a live conflict rather than a theoretical one: `tests/_tmproot.py` `finish()` returns False without removing when `exitstatus != 0`, deliberately, under the @1080 rule that artifacts survive a failing run; row 104 wants leaked roots reclaimed. Both are right and they contradict, so the policy has to be written before either can be mechanized. See rows 104 and 133 |
| 133 | OPEN | TWENTY-FOUR PRE-POLICY GATE FILES KEEP AN ENTRY IN `tests/gate_scope_baseline.txt` INSTEAD OF A `BD_GATE_SCOPE` MARKER, called "a mechanical cut of its own" at @1082 and still uncut. Filed at LOW priority and with its own counter-argument attached, because the baseline is a legitimate exemption mechanism and a known, sized, deliberate debt is not the same as drift. The reason it belongs here at all: the baseline is now 1329 entries and it is where row 105's two tracker gates were found hiding, so its size is not free |
| 134 | OPEN | `project-knowledge/BD_IMPROVEMENT_PROGRAM_PLAN.md` -- UNBUILT PROGRAM, FILED AT THE OPERATOR'S DIRECTION rather than mooted, with the evidence for mooting attached so the decision costs one read. A2 metrics and Phase C consolidations C2/C3/C4/C6/C7 were never built, and the `PROGRAM_LEDGER.md` the plan itself mandates WAS NEVER CREATED (verified absent at f0c5667). Its successor is this backlog -- the file it would have been consolidated into. RECOMMENDATION ON RE-DERIVATION: MOOT with this text as the evidence |
| 135 | OPEN | `project-knowledge/AUDIT_PLAN_v3_66_539.md` -- UNBUILT PROGRAM, FILED AT THE OPERATOR'S DIRECTION. A 58-batch audit program that NEVER RAN: 2 of 58 deliverables exist and BOTH are dated before the plan was written, so neither is evidence of it. The plan is written in imperative present tense against an output directory that does not exist, which is why it reads as in-progress. 573 releases old. RECOMMENDATION ON RE-DERIVATION: MOOT with this text as the evidence |
| 136 | OPEN | `docs/superpowers/plans/2026-07-23-code-intelligence-governance-gates.md` -- UNBUILT PROGRAM, FILED AT THE OPERATOR'S DIRECTION. MEASURED at f0c5667: 87 checkboxes, ZERO ticked, describing 26 files of tooling that do not exist. It is also the blocker behind row 137. RECOMMENDATION ON RE-DERIVATION: MOOT, or cut ONE gate from it and MOOT the other 25 -- a plan with 87 unticked boxes has never once been executed as a plan |
| 137 | OPEN | `docs/superpowers/plans/2026-07-23-audit-knowledge-hygiene.md` -- UNBUILT PROGRAM, FILED AT THE OPERATOR'S DIRECTION. 21 of its 23 declared paths are absent; it is blocked behind row 136; and it is pinned to a v3.66.817 snapshot while HEAD is v3.66.1112, so its premises are 295 releases stale. RECOMMENDATION ON RE-DERIVATION: MOOT with this text as the evidence |
| 138 | OPEN | RESIDUAL `[PLANNED]` CODE-INTELLIGENCE TOOLS, and the row exists because the DOCUMENTATION IS WRONG IN BOTH DIRECTIONS. VERIFIED at f0c5667: `bd-review-next`, `bd-finding`, `bd-invariant` and `bd-dup` are ABSENT from toolchain/bin, while `bd-coverage-map` is PRESENT despite `CODE_INTELLIGENCE_PROGRAM.md:78-80` calling it `[PLANNED]` and "verified absent". Five shipped analysis frontends are wired into nothing. Correct the document before deciding anything else here -- half this row's subject is a documentation defect, not missing work |
| 139 | OPEN | `docs/superpowers/plans/2026-07-25-defect-scanner-precision.md` TASK 3 -- the `bd-defect-scan` suppression mechanism. GENUINELY OPEN AND SMALL, and the one item in the 134-139 group I would build rather than MOOT: a scanner with no suppression mechanism gets switched off the first time it cries wolf, which is the failure mode row 109's own gate documents. Its design doc is at `docs/superpowers/specs/2026-07-25-defect-scanner-precision-design.md` |
| 140 | OPEN | THE 100-PERCENT LINE-BY-LINE AUDIT'S COMPLETION LEDGER DOES NOT EXIST, AND THIS IS THE LARGEST GENUINELY-UNKNOWN QUANTITY THE 2026-08-13 SURVEY FOUND. 73 finding IDs across 41 batches are cited inline in the source, so the findings are real and traceable; what is unknown is WHICH BATCHES WERE COMPLETED. Unlike rows 134-137 this cannot be mooted by inspection -- nobody can say whether the work is done, and that is exactly the state a register exists to prevent. Answering it is a measurement, not a build: derive the completed set from the inline citations and write it down once |
| 141 | OPEN | THIRTEEN DOCUMENT LOCATIONS STATE SHIPPED WORK AS UNSTARTED, ALL STALE IN THE SAME DIRECTION -- filed as ONE row because they are one defect with thirteen instances, and because each one costs a future session a re-audit that finds nothing. Confirmed instances: `pending-specs/` is 4 of 5 fully shipped (item12's three cuts all landed, recon-queue 8 of 9) while every file says DELETE THIS FILE when its item ships; `docs/superpowers/specs/...code-intelligence-platform-design.md:5` says "implementation not started" and shipped IN THE SAME COMMIT as 13 modules and 14 tests; `UI_TOKENS.md` marks three shipped wedges Not started and names a premise file that does not exist; `KNOWN_FLAKES.md:23` carries OPEN P1 for a flake fixed in the very next band; `CODE_INTELLIGENCE_PROGRAM.md:78-80` calls `bd-coverage-map` "verified absent" when it is present (see row 138); `OPERATOR_POLICY_DECISIONS.md:7` says "not yet applied" and is contradicted 39 lines below; `docs/repo/CLAUDE_CODE_PROMPT.md:3` hands a new session a COMPLETED mission as its current one; `docs/template_health_cockpit.md:19-23` lists six shipped items as Planned; plus `DEFECT_PATTERN_CATALOG.md:5`, `kb/decomp/DECOMP_TOOLS_README.md:6`, `DECOMP_HAZARD_REGISTER.md:29` and `KB_JUDGMENT.md:1198`. Row 106 is why none of these can be caught: every one of them sits outside both freshness gates |
| 142 | OPEN | RETIRE LEDGER ITEM 31 NOW THAT ITS SUB-ROWS HAVE BACKLOG IDS (119-128), so that "one file tracks every task" becomes true rather than aspirational. THIS IS A RELOCATION, NOT A CLOSURE -- the work in 119-128 is untouched, and closing item 31 on the grounds that it was copied would be exactly the false closure row 28 is being corrected for. SEQUENCE MATTERS AND THE COST IS MEASURED: item 31 is the ONLY entry in 15.95's `OPEN:` declaration, so removing it leaves that set empty, and if the ledger block ever leaves SESSION_CARRY's newest close entirely then `_ledger()` returns None and ELEVEN of the FIFTEEN live assertions in `test_register_promises_resolve` fail or GO VACUOUS -- direction A degrades to vacuous because it `continue`s on a None ledger. So: verify the gate's behaviour on an EMPTY-but-present `OPEN:` set BEFORE touching it, and do not merge the ledger into this file. The ledger stops being a task list and stays a historical promise-resolution invariant, which is the shape that satisfies the goal without breaking a gate that has twice caught a real failure |
| 143 | OPEN | EIGHT CLOSED ROWS STATE UNCOMPLETED WORK IN THEIR OWN TEXT WITHOUT THE `PARTIAL` MARKER, so their remainders are invisible to the status column -- the only thing a machine reads. The sanctioned form already exists and rows 3, 89, 90, 95 and 97 use it; this row is the population that does not. TWO WERE CORRECTED @1113 by hand (28 reopened as a false closure, 90 qualified as PARTIAL) and the rest were identified by a survey that has not been re-run, so RE-DERIVE THE SET before acting -- CLAUDE.md section 1's rule that roughly half a stale register's entries turn out to be already closed or mis-scoped applies to this row as much as to any other. THE POLICY QUESTION UNDERNEATH IT, which is the operator's and is not yet answered: may a CLOSED row carry a remainder at all, or must every remainder become its own OPEN row? The PARTIAL form says yes; the "one file tracks every task" goal argues no, because a remainder inside a closed row is not a task anyone will ever be handed |
| 144 | CLOSED @1133 | WHY DOES `test_a_site_with_no_declared_wall_is_untouched` CROSS 240s IN THE FULL SUITE WHEN IT TAKES 28-36s STANDALONE? **ANSWERED @1133, AND THE PREMISE OF THE QUESTION WAS WRONG: IT DOES NOT NEED THE FULL SUITE.** This row and SESSION_CARRY 15.96 both concluded that only the full 48-way run could reach it. It reproduces STANDALONE, on one test, in ~10 minutes, with the variable this row named as speculation and nobody had run: CONCURRENT CHROMIUM STARTUP. MEASURED at d734dd5 over 26 samples at 48 concurrent browser-launching processes, on test6 AND test3: **4/26 = 15% reproduced**, fast branch median 28.5s (range 27.5-30.4s, n=22), slow branch 568.27 / 568.34 / 568.44 / 571.04s -- a 20x split with NOTHING in between. IT IS BIMODAL AND THE LOSING COST IS DETERMINISTIC: four reproductions on two hosts spanning 2.77 SECONDS across two different submit paths, so the path does not set the cost; the walk is a fixed sequence of selector attempts with fixed timeouts. The 240s ceiling is not a threshold the test creeps over -- it is a line the test is either 8x below or 2.4x above. THE MECHANISM, off the reproduction's own log: `do_login` fills the form and checks whether `page.url` has reached `success_url`; under browser-startup pressure the navigation has not COMMITTED when that check runs, so the early return does not fire, and because this test is the NO-DECLARED-WALL case it skips the interstitial branch and falls straight into `_submit_login`'s flail -- which `submit.py`'s own comment at that site predicted ("flails its whole selector list against a page that can never satisfy it... a stall, not a clean failure"). `filled=2 early=1` on EVERY reproduction: the test performs two logins and exactly one loses the race, never both and never neither. WHY IT IS A RACE AND NOT CONTENTION: 22 of the 26 samples ran with 114-190 browsers RESIDENT and still took the early return, and CPU spinners were 22/22 negative -- what does it is many processes LAUNCHING at once. DOWNSTREAM: 569s is past the 240s ceiling, so in a real suite pytest-timeout fires, calls `os._exit(1)`, and row 145's livelock follows -- the whole chain is now reproducible on demand rather than by waiting ~80 minutes for a wedge. THE REPRODUCER IS TRACKED at `toolchain/bin/bd-chromium-race` (prints five blind spots per run; `--selftest` resolves all three markers against `submit.py`), gated by `tests/test_v3_66_1133_the_chromium_race_reproducer.py`. FOUR OF MY OWN CLAIMS DIED BY MEASUREMENT GETTING HERE, all from small n on durations: "chromium refuted" (12 samples on ONE host while the other reproduced), "test6 differs from test3" (sample size; both reproduce), "the losing cost is a fixed 570s" (retracted on a sample I had KILLED, then restored by n=4), and "two paths with different costs" (the 568.34s sample took the supposedly-slow path). An observation destroyed before it completed is not a data point, and it shaped three of those four. WHAT REMAINS OPEN, and it is small: the reproducer exercises the FIRST link only -- it passes no `--timeout`, so pytest-timeout never fires and row 145's livelock is not exercised end to end in one command. This row previously owned that half; it became row 145, CLOSED @1126. THE TEST IS NAMED AND CONFIRMED THREE TIMES, on two hosts and by three independent instruments: 13:57Z test6 full-signal (signal method unwound and named it), 15:32Z test6 full under -v (the crashitem FAILED report named it), 16:26Z test2 full under -v -u (same, plus the recovery line). It is NOT test6-specific -- test2 stands at 3/129 and test6 at 11/45, same test, different rate. THE OBVIOUS *TRIGGER* IS REFUTED. THE MECHANISM IS NOT, AND THIS ROW READ "THE OBVIOUS EXPLANATION IS REFUTED" UNTIL 2026-08-14, WHICH POINTS THE NEXT SESSION AT THE WRONG EXPERIMENT. What was measured away is CPU CONTENTION as the trigger. The BRANCH is confirmed, by this row's own stack below: it sits in `page.locator(sel).first.press("Enter")`, which is inside `_submit_login` -- both that function and `do_login` live in `bulk_downloader/login_impl/submit.py`, with `_submit_login` defined well ABOVE `do_login` (grep `def _submit_login`, do not trust a line number). THE FULL CALL CHAIN WAS CAPTURED AND THEN LOST FROM EVERY TRACKED FILE; recovered @1131 from the 2026-08-14 session transcript and recorded here because it is the single most load-bearing piece of evidence this row owns: `_login` -> `do_login` (submit.py:895) -> `_submit_login` (submit.py:396) -> `page.locator(sel).first.press("Enter")` (submit.py:278) -> playwright sync -> asyncio run_forever -> epoll. THOSE ARE FRAME POSITIONS AT `df73ef7`, NOT ANCHORS TO FOLLOW -- they are where execution stood in a preserved stack, they will rot with the next edit to that file, and the durable form of the claim is the SYMBOL chain, not the numbers. What they establish is the thing the prose alone does not: `do_login` did not merely fail to return early, it was observed calling THROUGH into the walk. So under the full suite the test DOES enter the branch that 22/22 standalone runs never reached, and the open question is WHAT puts it there, not WHETHER it goes there. `do_login` has an early return when the page is already at success_url, and the theory was that under load the browser has not finished navigating when that check runs, so the test falls into `_submit_login`'s 154-selector walk. MEASURED 2026-08-14 and it does not hold: 22/22 standalone runs on test6 AND test3 took the early return (`login: page already at success URL after fill`), `_submit_login` never reached, all 28-36s, no bimodality -- INCLUDING an arm with 36 CPU spinners pinning test6 at load ~25. A whole-file arm (which is what `--dist loadfile` actually gives one worker) was 6/6 the same, slowest single test 27-32s against a 240s ceiling. So CPU contention alone does not reproduce it. NOTE the 578-SECOND FIGURE IN THAT FILE'S DOCSTRING IS HISTORICAL -- it measured PRISTINE source before @1020's fix and is not a current cost; an earlier reading of this row cited it as live evidence and was wrong. UNTESTED CANDIDATE, explicitly speculation: the full suite's contention is ~47 concurrent CHROMIUM launches, not CPU load, and the preserved stack has the test inside `page.locator(sel).first.press("Enter")` blocked in epoll with timeout=-1 -- a Playwright action, not compute. An arm that holds CPU flat while varying CONCURRENT BROWSER STARTS is the experiment this row wants and it has never been run |
| 145 | CLOSED @1126 | AN XDIST WORKER THAT HARD-EXITS DURING THE DRAIN LIVELOCKS THE MASTER, AND THE MECHANISM IS NOW READ, REPRODUCED AND CONFIRMED. Was the second half of row 144. `LoadScopeScheduling.tests_finished` counts a work unit with FEWER THAN TWO pending tests as finished (`if self._pending_of(assigned_unit) >= 2: return False`), so once every node is down to its LAST test the session declares itself done and `triggershutdown()` latches every worker via `WorkerController.shutdown()` -- WHILE THOSE TESTS ARE STILL RUNNING. The latch is `shutting_down = self._down or self._shutdown_sent`: set once, never cleared. A worker then dies on that last test (pytest-timeout's thread method calls `os._exit(1)`), `worker_errordown` re-queues its work and clones a replacement, but `_reschedule` refuses every latched node (`if node.shutting_down: return`), so the re-queued units can never be dispatched. `has_pending` stays True, `tests_finished` stays False, and `triggershutdown()` is reachable only AFTER `queue.get()` returns an event -- while the clone keeps `_active_nodes` non-empty, disarming the only other exit. Both exits closed, no further event possible, master spins on `queue.get(timeout=2.0)` forever AWAKE beside idle workers. EVIDENCE AT THREE LEVELS: (1) read live with gdb out of a master wedged 11.6 hours -- `_active_nodes=['gw48']`, workqueue 8 units UNDISPATCHED, tests_finished False, has_pending True; (2) a minimal reproducer at `upstream/xdist-drain-livelock/` -- hangs 5/5, control (same tree, `os._exit` replaced by `pass`) passes 5/5 in ~4s, does NOT reproduce at -n 4, `--max-worker-restart=0` or `--dist load`; (3) production confirmation in `logs/test2-full-005-160601.log`, the first wedge captured after @1126's observability fixes, carrying `replacing crashed worker gw4` at line 31828 -- inside the ~4KB tail that buffering had eaten on all 15 earlier wedges. NOT FILED UPSTREAM by operator decision; the bundle is filing-ready in-repo. Six earlier hypotheses died by measurement before this one, and it is the first that was MEASURED BEFORE IT WAS BELIEVED |
| 146 | CLOSED @1132 | A WEDGED pytest MASTER SURVIVES THE HUNT'S SIGINT AND STAYS RESIDENT, SILENTLY RAISING LOAD ON THE HOST BEING MEASURED. Same reclaim gap as row 104, different object: 104 owns the leaked TEMP ROOTS, this row owns the leaked PROCESS. **THE DIAGNOSIS BELOW WAS WRONG AND IS CORRECTED @1132 -- IT BLAMED THE ONE PATH THAT WAS ALREADY RIGHT.** This row said the hunt "captures forensics and sends SIGINT, but a master livelocked per row 145 is not in a state that SIGINT unwinds, so the run simply stays". Read the code: the WEDGE-CONFIRMED branch sends SIGINT, waits `sigint_grace`, re-runs forensics, and THEN sends `kill -9` to the process GROUP and the pid. It was correct throughout, and a fix aimed there would have changed nothing. THE ORPHANS CAME FROM THE BRANCHES THAT END A RUN WITHOUT KILLING IT, all four found by reading every terminal exit of the monitor loop at v3.66.1131: (1) INTERRUPT -- `main` caught KeyboardInterrupt, set STOP and RETURNED, and the host threads are `daemon=True`, so the interpreter killed them at exit; the remote run was never killed AND its row was never written. That is the whole of the five observed orphans, and it is why `rows.jsonl` holds ZERO abandoned rows: they were never mis-recorded, they were never recorded. (2) --hours said "letting in-flight samples finish" and did the opposite -- the loop is `while not STOP.is_set()`, so a sample drops out on the next tick and fell through to `setdefault("state", "COMPLETED")`, recording an abandoned run as a completed NON-WEDGE. A false negative in the denominator the whole hunt turns on. It never fired during the 2026-08-14 run -- MEASURED: 686 of 686 COMPLETED rows carry a real `pytest_exit` -- so no preserved row is contaminated and the 702 need no correction on this account. (3) CAPPED killed the master pid ONLY while the wedge branch four lines above killed the GROUP: same job, two spellings, up to 48 workers left behind. (4) UNKNOWN recorded "the run was NOT killed and may still be there" and did not try. THE FIX: one `reap_cmd(pid)` builder that resolves the process group and kills it, one `reap()` that runs it and RECORDS the outcome on the row (REAP-OK / REAP-SURVIVED / REAP-UNREACHABLE / REAP-NOPID), wired into all four paths; a new ABANDONED state guarded on the absence of `pytest_exit`; and a bounded `join` in the interrupt handler so each thread reaps and writes before exit. A DEFECT THE STRUCTURAL TESTS COULD NOT SEE, found by driving the real thing: the first `reap_cmd` used `kill -0` as its liveness probe and reported REAP-SURVIVED after correctly killing a five-process tree, because `kill -0` succeeds on a ZOMBIE -- a gate firing on correct work, which section 0 counts as a soundness bug. It now reads the process STATE and treats `Z` as gone, with an over-sensitivity twin asserting it can still report a genuine survivor. bd-mutate 5 caught / 0 escaped, and the first run ESCAPED one: `"join" in ast.unparse(handler)` was satisfied by the handler's own `", ".join(alive)`. MEASURED 2026-08-14 at the end of a ~19h hunt: FIVE orphaned masters across the fleet, the oldest 41614s (11.6 HOURS), with test4 carrying FIVE concurrent suites and test7 four before they were reaped. Each orphan is a master plus up to 48 idle workers. WHY THIS IS NOT MERELY UNTIDY: load is this bug's dominant covariate -- reduced-size arms are 0/73 against 15 of ~620 on full -- so unreaped masters progressively corrupt the very measurement the hunt exists to take, and they do it in the direction that INCREASES the apparent rate over time. Cross-host and cross-time contrasts drawn over a long hunt are suspect for exactly this reason and no correction has been applied to the 702 preserved rows. The hunt's own log says it plainly at shutdown: `interrupted -- setting STOP; in-flight samples are NOT killed`. THE FIX BELONGS WITH THE KILLER, the same conclusion row 104 reaches: whatever declares a run wedged is the only party that knows it died, so it should reap the process group after forensics, not hope a signal handler runs. A retention sweep at hunt START would also bound it, the way capture.sh prunes on the way IN (@1099) |
| 147 | OPEN | EVERY CAPTURE TAKEN BEFORE v3.66.1126 WAS BLIND TWICE, SO NO PAST CONCLUSION RESTING ON `THE LOG DID NOT SAY X` IS SOUND. Two independent blindfolds, either of which alone hides the evidence, both fixed at @1126 and neither retroactive. FIRST: `-q` sets `verbose == -1` and xdist guards `DSession.report_line` on `verbose >= 0`, so `replacing crashed worker gwN` and `maximum crashed workers reached` were never written, while `pytest_testnodedown` writes UNGUARDED and so `node down` always was -- the symptom shown, the response suppressed. Measured on a reproducer, same code path, only the flag differing: `-q` 0 replace lines, no flag 8, `-v` 8. SECOND: a run that never exits never flushes, so the tail is stranded in stdio's buffer. Measured across the corpus: 15 of 15 WEDGED logs end MID-LINE at a 4KB boundary, 642 of 642 COMPLETED logs end with a newline. The stranded ~4KB is exactly where the recovery narration lands. CONSEQUENCE: the 15 pre-@1126 wedge logs are missing their most diagnostic kilobytes and their silence is an ARTEFACT. During this investigation that silence was twice read as a finding -- `worker_errordown never ran` was stated, retracted, restated and retracted again -- before the process table (49 children from -n 48, proving a clone was spawned) settled it. WHAT THIS ROW WANTS: an audit of which prior rows and SESSION_CARRY sections cite an absent xdist diagnostic, so a later reader inherits the doubt rather than the claim. Not attempted here; the archive is preserved at `bd-wedge-FINAL-20260814-163016Z.tar.gz` on all SIX hosts (this row read "five" until v3.66.1129; re-measured byte-identical on all six, and gzip/tar-verified readable end to end rather than merely listed). THE GENERAL RULE, now in CLAUDE.md section 5: absence of a message is evidence of nothing until you have established BOTH that the tool emits it at the configured verbosity AND that the output survives to disk |
