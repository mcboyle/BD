# IMPROVEMENT BACKLOG -- machine-visible since v3.66.1052

ASCII-only.

<!-- canonical-task-register schema=1 rows=144 open=77 ids-sha256=832b9ab6486da39fb23b3af5beaa1cf0a59d669f47ca0aebd5f810fc16f319a0 -->

## Why this file exists

It was **item 85 of itself**. The backlog was produced by a review that called
it "the most valuable artifact of that session", and it then lived in an
untracked text file in the operator's home directory that no gate read. CLAUDE.md
section 1 states the consequence in general terms:

> "A DEFERRAL THAT LIVES ONLY IN PROSE HAS NOT BEEN DEFERRED -- IT HAS BEEN
> DROPPED. The ITEM LEDGER works for exactly one reason: a test reads it."

The proof arrived on 2026-08-12: a session with full authority to work the
backlog could not find it, and the operator had to say where it was. That is the
whole failure mode -- not that the list was wrong, but that nothing could see it.

`tests/test_v3_66_1052_the_backlog_is_machine_visible.py` now reads this file.

## Priority order, set 2026-08-12 at dc5943f (v3.66.1066)

Re-derive before working any of these -- section 1: roughly half of a stale
register's OPEN items turn out to be closed or mis-scoped, and this list is a
snapshot of a judgement, not a measurement.

1. ~~**22 + 48 + 91 + 94 as ONE campaign.**~~ -- ALL FOUR CLOSED. 22 and 94 at
   @1068/@1069, 91 at @1067+@1069. **91 sat OPEN through the two cuts that
   fixed it** and was still directing this list to "START WITH 91" when
   re-derived at @1072; the instrument had been reading comment-stripped source
   since @1067 and 1034 had carried a real restore since @1069. That is
   section 1's own statistic -- roughly half a stale register's OPEN items are
   already closed -- landing on the register that exists to prevent it. What
   survives the campaign: **92** and **93**, both still OPEN and both latent
   shapes rather than live failures.
2. ~~**46**~~ -- CLOSED @1072. It had failed EIGHT times by then, not four: the
   1062/1064/1067/1068 gates shipped on 2026-08-12 were the fifth through
   eighth, added by the session that wrote this very line. The successor is
   **99**, which is what the fix could not reach.
3. **89** -- the capture corpus. Silent, and recurs on every rebuild.
4. **95 + 98** -- both cheap, both residue-class.
5. **97** -- makes 96 true for a FRESH host rather than for these four.
6. **3, 35, 5-remainder, 25, 27** -- real, not urgent.
7. **13, 26.** 26 is the trap: seductive, because it is section 0 mechanized,
   and the likeliest to ship a confidently wrong number everything downstream
   inherits.

## THIS IS THE ONLY LIVE TASK NUMBERING

These IDs are the backlog's own canonical namespace. Older session-ledger,
tracker, plan, and spec numbering is historical evidence in Git, never a second
place to file or close work. When citing a task, say "backlog N".

**The namespace spans two files and the live surface is only one of them.**
Terminal rows that nothing pins were moved to
`project-knowledge/IMPROVEMENT_BACKLOG_ARCHIVE.md` at v3.66.1525, verbatim. THE
ARCHIVE IS NOT A SECOND TASK REGISTER: it holds zero OPEN rows, nothing may be
filed in it, and a gate asserts both --
`tests/test_register_archive_holds_the_moved_rows.py`. This file remains the only
place work is filed, closed or parked, and an id is never reused across the pair.

**A COUNT TAKEN OVER THIS FILE ALONE IS A COUNT OF THE LIVE ROWS.** The header
marker below is deliberately that count and not the union: its readers --
`bd-register-append`, `bd-register-close`, `project-knowledge/build_current_overlay.py`
-- write into this file, and a marker counting rows that are not in the file they
edit would be wrong for every one of them. The gates that need the WHOLE
population parse both files with the same row regex, which is exactly what
`tests/test_v3_66_1052_the_backlog_is_machine_visible.py`,
`tests/test_v3_66_1171_backlog_truth_is_current.py` and
`tests/test_v3_66_1255_backlog_references_resolve.py` now do. Say which one you
took; the two answer different questions and both are legitimate.

## Format, which the gate depends on

One row per item: `| <id> | <status>[ @<evidence>] | <text> |`

Status is one of `OPEN`, `CLOSED`, `PARKED`, `MOOT`.

Evidence is required for `CLOSED`, `MOOT` and `PARKED`, and forbidden for `OPEN`.
Evidence is a version (`@1049`) or a commit, because a close nobody can check is
a claim rather than a record -- and neither is a park.

`MOOT` is deliberately distinct from `CLOSED`: it means the SUBJECT went away
rather than the work being done. Collapsing the two would let a disappeared
problem read as a solved one.

`PARKED` is deliberately distinct from both: the work was SUSPENDED BY A
DECISION, rather than not yet started (`OPEN`) or overtaken by its subject
disappearing (`MOOT`). Its evidence names the version at which that decision was
taken -- rows 120, 122, 124 and 126 carry `@1253`, and row 127 carries `@1195`
and says so in its text. The evidence requirement is not a formality here: a
park is the class most likely to be forgotten, because by definition nobody is
working on it, and a parked row nobody can trace to a decision is
indistinguishable from an abandoned one.

`PARKED` was in use from v3.66.1195 and this section did not name it for the
326 versions that followed. It is not new, and it is not a typo the other gates would catch: the
status is already read by tests/test_v3_66_1052_the_backlog_is_machine_visible.py,
tests/test_v3_66_1171_backlog_truth_is_current.py and
tests/mutants/v3_66_1228_register_reconciliation.json. The rows were right and
this paragraph was stale, which is the worse direction -- a reader who trusted
the three-status enum wrote a triage that silently dropped five operator-parked
items.

A `CLOSED` row MAY carry an unfinished remainder, but it must say so in the
status: `CLOSED @NNNN -- PARTIAL, and the remainder is named`. Rows 3, 89, 90,
95 and 97 are the worked examples. The status column is the only thing a machine
reads, so a remainder living in a row's TEXT under a bare `CLOSED` is invisible
to every count and every triage -- see row 143.

## FIFTY-SEVEN IDS HAVE NO ROW, and the table is not contiguous

**No row has ever been removed from this register.** That is what this section
exists to establish, and it is now measured rather than asserted: over the 341
commits that touched this file in `d69ffcda`'s ancestry -- 341 distinct blobs,
back to the birth commit `7a8a5a2` (v3.66.1052) -- not one of the ids below ever
carried a row.

AMENDED AT v3.66.1525, AND THE SENTENCE ABOVE STILL HOLDS, WHICH IS WHY IT IS NOT
REWRITTEN. 608 terminal rows were MOVED to
`project-knowledge/IMPROVEMENT_BACKLOG_ARCHIVE.md` in that cut. A move is not a
removal: the bytes are unchanged, the id keeps its meaning, and the id population
this section is about is the UNION of the two files -- which is why the fifty-seven
below is still fifty-seven and not six hundred and sixty-five. **THE HOLE COUNT IS
THE TEST OF THAT CLAIM, NOT A CONSEQUENCE OF IT:** if the move had lost a row, a
fifty-eighth hole would appear in the union and both
`tests/test_v3_66_1052_the_backlog_is_machine_visible.py` and
`tests/test_register_archive_holds_the_moved_rows.py` would fire. A count of this
file's own rows is a count of the live surface and says nothing about absence. The counter-control ran in the same pass: ids that DO have rows
are found in the blobs where they belong (1, 14 and 16 in all 341 blobs, 799 in
the last 3), so "never present" here is a finding and not an empty parse.

THE FIFTY-SEVEN, as ranges:

    15, 40-42
    236, 270-279, 282, 288
    301-307
    352, 359, 362, 364-365, 369-370, 379, 382-383, 387
    392-396, 398-401, 403-407, 409-410
    457-462

They are also enumerated one id per entry, machine-readably, in
`project-knowledge/REGISTER_GAP_ALLOWLIST.json`, which is the authority a gate
reads; this section is the prose beside it and the two must not drift.

THE ORIGINAL FOUR.
**15, 40, 41 and 42 are absent and their content is unrecoverable.**
This is not a deletion: the birth commit `7a8a5a2` (v3.66.1052) already lacks
them, and the untracked pre-tracking source it was built from called itself
"77 items" while listing 73.

THE OTHER FIFTY-THREE ARE A DIFFERENT POPULATION, and the allowlist marks every
one of them UNADJUDICATED -- measured absent, never explained. They are ids
CONSUMED WITHOUT A ROW LANDING, and they cluster -- 270-279, 301-307, 392-410,
457-462 -- which is the shape of reserved blocks and abandoned batches rather
than 53 independent events. Two sub-populations are known and both are
recoverable, which is why "unrecoverable" must not be read across all 57:

  * NAMED BY LANDED WORK UNDER THE SAME NUMBER. Nine of the 53 are cited by
    tracked files in this tree: 236 (CHANGELOG, "filed as row 236"), 282
    (`tests/test_row_282_bd_opv_isolates_every_store.py`), 362 (`ci.yml`), 379,
    387, 395, 399, 407 and 409. For these the SUBJECT is recoverable from the
    citing artifact even though the register has no row.
  * A ROW EXISTED ON A BRANCH THAT NEVER LANDED. 282, 399 and 457-462 carried a
    real row in commits reachable from `git log --all` but from no ancestor of
    main (`4b4ce878`, `9392a4cb`, `f5fc0d63`, `a6c311d6`). Those rows are
    recoverable verbatim from git, and they were never in main, so nothing was
    removed from it.

RECONCILING EITHER SUB-POPULATION INTO ROWS IS OPEN WORK AND IS NOT A
DOCUMENTATION QUESTION. Only a human may change an UNADJUDICATED entry, so this
section records what was measured and changes no status.

Recorded here, as before, because a table that runs 14, 16 and 39, 43 reads as
damaged and the next session to notice would otherwise spend the same hour. The
earlier wording named four and was silent about the other 53, which cost that
hour anyway: a reader who checked the four and found fifty-three more concluded
the note was unreliable, which is the opposite of what it is for.

| id | status | item |
| --- | --- | --- |
| 5 | CLOSED @1099 | per-run capture output directories are keyed and retained correctly. Backlog 165 later serialized the singleton capture vault resources; true same-host parallel capture services are remainder -> backlog 175 and are not hidden in this closed row. |
| 13 | CLOSED @1110 | the scoped fleet agent identity, restriction, and broad-key retirement shipped and were verified. Test5 remains the origin rather than a fleet destination; fresh-host SSH configuration owns rebuild guidance, and laptop-key reachability was deliberately outside the available private-key boundary. |
| 14 | MOOT @1051 | reimage .249 -- the clean-host role moved to .84/test7 instead, and the proof was retaken there |
| 25 | CLOSED @1093 | all three measured rotating populations were defects or harness artifacts, not flakes: perf-lab sentinel handling, body-contract repeated-run state, and VPN victim-fixture initialization were repaired. The historical producer combination was not isolated, but current victims establish and restore their own exact state and no current failing subject remains. |
| 50 | MOOT @1051 | re-role or reimage .249 -- superseded by the role moving to test7 |
| 51 | CLOSED @1055 | .85 carried /etc/sudoers.d/90-mboyle-codex AND 90-mboyle-nopasswd with byte-identical rules; the codex one is vestigial from the retired CODEX_HANDOFF era and the other three hosts have only the latter. Removed, visudo -c OK, sudo -n verified, backup at /root/sudoers-codex-backup-2026-08-12 |
| 86 | MOOT @1051 | run bd-gc --apply -- the 2026-08-12 rebuild collected the litter; bd-fleet measures 2/0/0 against a pre-rebuild 1349/435/68 |
| 99 | CLOSED @1082 | repo-wide scope markers and direct CI membership shipped for the measured population. Remaining pre-policy baseline adjudication is transferred to backlog 133, and declaration honesty is owned by backlog 152. |
| 101 | CLOSED @1095 | module-identity restoration and its exact victim population shipped. Generic mutation of arbitrary import dictionaries is an accepted boundary, not an unnamed continuation of this row. |
| 102 | CLOSED @1124 | the xdist wedge population was measured, causal worker-loss and controller cleanup defects were repaired in later bounded rows, and the fixed canonical worker policy was promoted at v3.66.1162. Historical high-worker failures remain evidence, not current work. |
| 106 | CLOSED @1172 | The freshness corpus is derived from tracked root Markdown plus recursive project-knowledge and docs Markdown, with CHANGELOG and docs/archive named as historical exclusions. Both bd-freshcheck and bd-doc-truth consume that same bounded denominator and report its size; nested pending/task documents and nested stale anchors have direct regressions. |
| 107 | CLOSED @1164 | Current routing now points only to IMPROVEMENT_BACKLOG; retired TASK_TRACKER pointers were corrected as part of the one-authority cut.  NINETEEN project-knowledge FILES STILL ROUTE "what is open" TO THE RETIRED TASK_TRACKER. RE-MEASURED @1139 with the row's OWN commands: `git grep -ln TASK_TRACKER -- project-knowledge/` = **19** and `git grep -ln IMPROVEMENT_BACKLOG -- project-knowledge/` = **3**; the row read 18 and 2, so BOTH terms drifted and the gap widened by one while the row sat still. That is the row describing itself: a count in prose that nothing re-derives. `docs/PROCESS_CONVENTIONS.md` section 2 still calls `TASK_TRACKER_DATA.json` "the single source of task truth" -- a file that `tests/test_task_tracker_stays_retired.py` guarantees will never exist again. That gate cannot catch this: its own docstring says "Prose mentions are fine", so it bans the FILE and permits every POINTER to it. This is the single most direct cause of the operator's stated problem -- a new session reads the prose, goes looking for a register that was deleted, and files nowhere |
| 110 | CLOSED @1115 | LIVE CHECK L13 CANNOT FAIL -- ITS ELIGIBILITY PREDICATE IS CONSTANT-TRUE. `live_tests/checks.py:1263` selects eligible rows with `m.get("installed") or m.get("library") or m.get("library_installed")`. VERIFIED at f0c5667 against the producer: `bulk_downloader/dev_suite/capture_diag.py:374-380` emits `site_id`, `library`, `host_pattern`, `adapter`, `library_installed` -- it NEVER emits `installed`, and `library` is the library NAME, a non-empty string on every well-formed row. So the middle operand is truthy regardless of whether anything is installed, `eligible` always equals `matrix`, the "no configured site maps to an installed extractor library" WARN is UNREACHABLE, and the PASS text reports a constant N of N. The one fact the check exists to establish -- that a library is actually present -- is the one it cannot observe. The `library_installed` key it should read is already right there  CLOSED @1115. The predicate is now `m.get("library_installed")` alone and the log line says INSTALLED so the reader can tell which question was asked. A row carrying no `library_installed` key is NOT counted eligible -- absent is not installed, and treating it as installed is how the original read. PROVEN RED FIRST in both directions: on pristine source the check reported `4 of 4 site(s) are library-extractor eligible` for a matrix in which ZERO were installed, and the over-sensitivity control (one installed of two) was written in the same file so a fix that simply always warns would fail. MUTATION: reverting the operand to `m.get("library")` is CAUGHT |
| 111 | CLOSED @1115 | LIVE CHECK L29 RETURNS PASS ON A GENUINELY KILLED VPN TUNNEL, AND ITS TEST FABRICATES THE KEY THAT WOULD HAVE CAUGHT IT. `live_tests/checks.py:2913` computes active kills as `[s for s in states if isinstance(s, dict) and s.get("killed")]`. VERIFIED: `bulk_downloader/vpn_kill_switch.py:63` is a dataclass whose fields are `tunnel_id`, `killed_at`, `reason`, `state` (`"killed"`/`"cycling"`/`"cleared"`), and four more; `to_dict()` is `asdict(self)`, so the emitted key is `state` and there is NO `killed` key -- `killed_at` is a float timestamp and is not what `.get("killed")` reads. `active` is therefore always empty and the FAIL branch is dead. WORSE, `tests/test_t46_l29_vpn_kill_switch.py:46,115,133` builds its fixtures as `{"tunnel_id": ..., "killed": True/False}` -- the test manufactures the very key the product does not emit, so it exercises a contract nothing produces and reports green. This is the highest-value row in the group: a safety check that always passes, held up by a test that always passes  CLOSED @1115, and the fallout was the confirmation. The predicate now reads `s.get("state") in ("killed", "cycling")`; `cycling` counts as ACTIVE deliberately, because it means the tunnel was killed and an auto-cycle is in flight so it is not carrying traffic, and vpn_kill_switch reverts it to `killed` when the cycle fails. A THIRD STATE WAS ADDED so this cannot recur silently: a record carrying no `state` field now returns WARN naming the record rather than PASS, because without it the next rename puts the check straight back to blind. THE OLD TESTS THEN WENT RED, WHICH IS THE PROOF: all three fabricated fixtures (`{"tunnel_id": ..., "killed": True}`) are themselves records with no `state` field, so the new WARN branch caught the test suite's own fabrication. All three were corrected to the shape `KillState.to_dict()` actually emits, and a repo-wide grep for a fabricated `"killed":` now returns zero. The new gate builds its input by CALLING the real to_dict() rather than writing a literal, and asserts the producer's shape as a precondition so it reports UNRUNNABLE if KillState ever grows a real `killed` key. MUTATION: 4 of 4 caught, including the revert |
| 112 | CLOSED @1171 | retired the stale ungated root ARCHITECTURE_INVENTORY output, its default-writing generator, and its characterization-only test. DEPENDENCY_GRAPH.json/.md and project-knowledge/ARCHITECTURE_MAP.md remain the machine and human architecture surfaces; no tracked producer or current authority recreates the retired inventory. |
| 113 | CLOSED @1117 -- PARTIAL, and the remainder is named | THREE LIVE OPERATOR PAGES LINK TO `/cockpit/home`, RETIRED AT v3.66.344 -- 768 releases ago. VERIFIED at f0c5667: `bulk_downloader/app_settings_center.py:350`, `bulk_downloader/app_report_center.py:75`, `bulk_downloader/app_template_manager_ui.py:319`, each an anchor reading "Cockpit Home"; `grep -c 'cockpit/home' ENDPOINT_CATALOG.md` = 0, and `bulk_downloader/app_cockpit_home.py:121` carries the note recording the retirement. The gate built for exactly this class cannot see it: `nav_reachability.py` crawls INBOUND reachability, so an endpoint nobody links TO is caught and a dead link pointing OUT is not. Same file notes dead `_item_html`/`_group_html` awaiting "a later cleanup cut" since v3.66.344  CLOSED @1117 as PARTIAL. All three breadcrumbs now point at `/`, the SPA that replaced the hub, and `tests/test_v3_66_1117_cockpit_home_stays_retired.py` is a TOMBSTONE gate over the tracked tree so they cannot come back. THE GATE FOUND MORE THAN THE ROW DID, which is the argument for writing it rather than editing three lines: `docs/NAV_CONSOLIDATION.md` still described `/cockpit/home` as "the **hub** for everything cockpit-side" and stated that "every center shell page carries a left-arrow Cockpit Home breadcrumb" -- in the present tense, 772 releases after the page was retired. THAT DOCUMENT IS WHY THE DEAD LINK SURVIVED: a reader checking whether the breadcrumb was correct found a document saying it was intended. Corrected in place with the stale bullets struck through rather than deleted, because the v3.66.3xx arrangement is worth keeping as history. Note the two blind spots that let it live -- `docs/**.md` is outside both freshness gates (row 106), and `nav_reachability.py` crawls INBOUND reachability so a link pointing OUT at a dead route is structurally outside the denominator of the gate built for dead links. THE REMAINDER, NOT DONE: no general outbound-link checker exists. The obvious predicate -- every `href="/..."` in served HTML resolves to a registered route -- fires on correct code, because the SPA serves client-side paths that are not Flask routes, and section 0 is explicit that over-sensitivity is a soundness bug rather than a safe default. The dead `_item_html`/`_group_html` helpers are also still there; they are unreferenced and harmless, and removing them is a wider blast radius than this row justifies |
| 114 | CLOSED @1172 | The nine live audit findings were re-derived before the flat report was retired: #6 and #17 were already rows 110/111 CLOSED; #23 and #25 are fixed; #24 is OPEN row 164; #19d, #19e, #21, and the remaining #22 enforcement gap are now atomic OPEN rows 166-169. Historical prose remains in Git, while every live remainder has exactly one machine-visible owner. |
| 115 | CLOSED @1192 | Retired the uncalled `scripts/build_release.sh` zip-era wrapper and its frozen v3.66.137/v3.66.148 packaging path. Current source remeasurement found 10 tracked basename mentions, no execution caller, 4 assertions in its sole characterization, and an unfiltered `git ls-files -z` source denominator whose count moved without failing. The hard dependency was the bidirectional retired-home carrier ratchet: deletion alone produced the expected stale-entry RED, so the file and its allowlist entry moved atomically. `tools/build_release.py` remains the supported builder; current README instructions name it, the obsolete characterization is gone, and a repo-wide CI tombstone rejects restoration or executable references. Earlier claims of zero backlog mentions, five assertions, filtered source enumeration, a surviving pending spec/session handoff, and a fourth retirement gate were false or stale and are withdrawn. |
| 116 | MOOT @1113 -- investigated at f0c5667 and DID NOT REPRODUCE | A 2026-08-13 audit reported that `docs/eligibility_governance.md:26` and `docs/oracle_eligibility.md:16` state in bold present tense that no Class C apply path exists while `apply_path_exists()` in fact returns True for four subjects. EXECUTED at f0c5667 against the installed venv: `apply_path_exists()` returns **False** for `live_site_config`, `template`, `credential`, `schedule`, `oracle` and an unknown kind -- six of six. The function is `return agr.has_reverser(kind)` over a reverser registry that nothing has populated, which is precisely what the documentation says. The doc is ACCURATE and there is no defect. RECORDED RATHER THAN DELETED because the failure mode is reusable: an auditing agent that IMPORTS apply modules to inspect them registers their reversers as a side effect and then measures a registry its own probe filled. The subject moved under the instrument -- section 0, inverted |
| 119 | CLOSED @1228 -- MIS-SCOPED | LEDGER ITEM 31 SUB-ROW **CAP-ROBUST arm C**, given a backlog id so it stops living as unnumbered prose. Held-open capture anti-bot resilience: arms A (page-death recovery) and B (operator re-nav) PASSED live; arm C -- challenge settle/resume acceptance -- is pending and is live-capture bound. `_inventory()` in the ledger gate matches only `N. **`, so this and rows 120-126 were structurally invisible to every check the ledger has  RECONCILED @1228 AS A DUPLICATE OF ROW 122, which stays OPEN and owns the identical remaining subject. Arm C's settle step is now routed through row 122's own seam: `tools/capture_session.py:964` calls `_settle_challenge_handoff(page)` under the comment `C: detect/handoff an anti-bot interstitial via the canonical seam`, and that function (`tools/capture_session.py:436`) is titled `P3-T12-CALLSITE` -- row 122's own token. The live caller DISCARDS the returned `ChallengeHandler`, so the missing explicit detector-cleared/resume event is exactly row 122's stated gap and not a second one. The `_inventory()` complaint is historical: `tests/test_register_promises_resolve.py` was deleted by `856c2919` and its return is now refused by `tests/test_v3_66_1164_one_task_authority.py:23`. Both rows were filed together by `9d7f911a`. Verified at `c7b207f` |
| 120 | PARKED @1253 | LEDGER ITEM 31 SUB-ROW **JW-TMPL**. Auto-template for signed JWPlayer BEHIND AKAMAI/CLOUDFLARE. REOPENED at v3.66.1195 by operator ruling: the akamai/cloudflare CDN topology in the subject is ESSENTIAL to this row, not descriptive, so it is now the explicit acceptance criterion. MEASURED at v3.66.1195 over the correct denominator -- every capture whose recorded page host is `ultrafilms.com`, not a `*ultrafilm*` filename glob: 21 files carrying 14 DISTINCT SESSIONS by `captured_at`, dated 2026-05-30 to 2026-06-20 (4 files / 4 sessions post-date 06-12: 06-17T23:34, 06-17T23:37, 06-18T00:38, 06-20T00:58; the 06-12 session was the old glob's endpoint). All 21 of 21 are JWPlayer-bearing with a member-only entitlement call, signed renditions to 7680x4320, ranged reads of 724 MB / 1.58 GB / 2.80 GB, playback-style mid-stream seeks, and 44 download-flow fetches (`download`+`filename=`) across the files / 24 across the 14 sessions; signing tokens scrubbed in every file; ultra_draft.json's declared capture_sha256 equals ultra.wacz's digest. BUT NONE SATISFY THE CRITERION: 0 of 21 serve a player or media asset from an akamai/cloudflare/cloudfront host -- the JWPlayer entitlement and signed-rendition fetches come from `entitlements.jwplayer.com`, `prd.jwpltx.com` and `ultrafilms.com`, and the only CDN host seen anywhere is `cdn.jsdelivr.net` (a JS-library CDN). So the existing corpus proves signed JWPlayer end to end but does NOT exercise the akamai/cloudflare-fronted case this row now requires. BLOCKER: a live capture of a signed-JWPlayer site actually fronted by akamai or cloudflare; the UltraFilms captures are retained as adjacent evidence but do not close it. The earlier `17 captures` and `29 download-flow` figures were filename-glob counts and are superseded. PARKED: operator 2026-08-25: code proven end to end over the CORRECT denominator (21 files / 14 sessions, all JWPlayer-bearing) but 0 of 21 serve a player or media asset from an akamai/cloudflare/cloudfront host, and that topology is the acceptance criterion. Blocked on a live capture, not on code. UNPARKED BY OPERATOR RULING 2026-08-31: the work is carried by successor row 453. This row's terminal status is left exactly as it was rather than laundered, because bd-register-amend refuses status changes by design and that refusal is the anti-laundering property. |
| 121 |  CLOSED @1249 | LEDGER ITEM 31 SUB-ROW **LOGIN-NSTEP**. RE-SCOPED at v3.66.1195 to the defect that actually blocks it. The row asked for a live cross-origin browser drive and called the code LIVE and unit-tested. MEASURED at v3.66.1195, the browser-side behaviour is PROVEN twice -- auth.reptyle.com across 4 origins with IdP token exchange, 94 Authorization-bearing requests across two app origins and a persisted firebase authUser; auth.wowgirls.com across 2 origins with a clean 3-hop POST 302 -- but BOTH were driven by the operator Cloak capture tool and NOTHING in the corpus shows BD's own `replay_saved_login_flow` performing the drive. Worse and decisive: running `derive_login_flow` against those very captures emits a plan that CANNOT log in. So the live drive is not merely unobserved, it is blocked downstream of a broken derivation, and scheduling an operator-verify run would only re-demonstrate that. THE ROW IS THEREFORE RE-SCOPED: the work is to make `derive_login_flow` emit a plan that reproduces the two captures already known to succeed, using them as ground truth. The live cross-origin drive remains the acceptance criterion but sits downstream and cannot pass before that. State continuity is additionally NOT OBSERVED on the wowgirls capture (Cookie, Set-Cookie and Authorization all zero on every origin), and the two captures are different sites and sessions so they cannot be combined into one proof. |
| 122 | PARKED @1253 | LEDGER ITEM 31 SUB-ROW **P3-T12-CALLSITE**. The held-open runner routes challenge settle through the `session_capture` seam. Detect, pause and handoff were observed live and a human completed a real CAPTCHA; what is missing is an explicit detector-cleared/resume event. THE RESTRAINT IS PART OF THE ROW: it was deliberately NOT closed by inferring resume from later authenticated activity, and that call should survive re-derivation -- inferring the event from a downstream consequence is asserting over a subject the instrument never saw PARKED: operator 2026-08-25: detect, pause and handoff observed live and a human completed a real CAPTCHA; the missing artifact is an explicit detector-cleared/resume EVENT. The row's refusal to infer resume from downstream activity is preserved. UNPARKED BY OPERATOR RULING 2026-08-31: the work is carried by successor row 454. This row's terminal status is left exactly as it was rather than laundered, because bd-register-amend refuses status changes by design and that refusal is the anti-laundering property. |
| 123 | CLOSED @1195 -- the criterion is MET, and the cited zero was a mislabelled capture | LEDGER ITEM 31 SUB-ROW **RPTYL**. The legacy close criterion is `api_patterns >= 1` and the row recorded `api_pattern_count = 0`. MEASURED at v3.66.1195 over the CORRECT DENOMINATOR -- every capture whose recorded page host ends `reptyle.com` (62 `app.reptyle.com` plus 5 `auth.reptyle.com`), not the 26-to-28 files whose NAME contains `reptyle`, which is a different and smaller population that also omits genuine Reptyle captures named otherwise. Denominator 67 files / 26 distinct sessions; MEETS the criterion 53 files / 20 DISTINCT SESSIONS; zero 14 files / 6 sessions; unreadable 0 WITHIN this host-resolved population (the store as a whole carries 5 unreadable WACZ that can never enter any host denominator because their page host is unresolvable -- they are excluded, not passed). The 67 include 2 `template_onboarding/` byte-identical duplicates of top-level files; the session count 26 is immune to that duplication by construction. Observed resolutions across the conforming set are 720, 1080 and 2160. METHOD, stated exactly: no builder was executed. The criterion's own producer regex from `bulk_downloader/extraction_core.py` was applied to `urlparse(url).path` over each capture's `network_log` -- the producer's input, not its output -- so this measures what the builder WOULD record, and is producer-input evidence rather than an observed builder run. Earlier figures of 17 and 19 in this row's history were filename-glob counts and are superseded. Four verified on this host: Reptyle-1.redacted.wacz 468,369 B 62c278af, Reptyle-2.redacted.wacz 676,108 B a2212440, reptyle259-1.redacted.wacz 442,039 B 8145f643, and auth.reptyle.com_0b60f1ec_20260629_145050_52e5.redacted.wacz 735,590 B 7a308716. THE CITED ZERO CAME FROM TWO NON-CONFORMING SOURCES, both benign: reptyle.wacz (2,225,013 B, 13d3dd8a) IS NOT REPTYLE -- its draft records host auth.wowgirls.com, 266 network entries and captured_at 2026-06-11T23:50:19, identical to the tracked fixture tests/corpus/recognizer/reptyle.cap.json -- and re1231212.wacz (973,026 B, 5fd60bd7; page host `app.reptyle.com`, so it IS inside the host-based denominator even though a `reptyle` filename glob misses it) is genuine Reptyle but a thin landing/login session with 7 DOM events that never exercised the movie path. Zero was correct in both and uninformative in both. The criterion is NOT relaxed and NOT retired: it is met. WHAT IS NOT CLAIMED: no template draft has yet been re-derived from a conforming capture, and every existing draft is draft_requires_review at medium confidence. THE MISLABEL IS A SEPARATE DEFECT with its own owner in row 176: the name Reptyle runs from raw WACZ through draft into the repository's own tracked test fixture while the data is WowGirls. |
| 124 | PARKED @1253 | LEDGER ITEM 31 SUB-ROW **FR-A6.2**. Builder enrichment slice 2, non-CF and non-video.js breadth. The recognizers are implemented and synthetically tested; what is missing is real guided-capture corpus evidence, so this is corpus-bound rather than code-bound PARKED: operator 2026-08-25: the row states it itself -- recognizers implemented and synthetically tested, corpus-bound rather than code-bound. UNPARKED BY OPERATOR RULING 2026-08-31: five real guided captures arrived on 2026-08-29, after this park was set, so the corpus-bound condition it rested on no longer holds. The work is carried by successor row 452; this row's terminal status is left exactly as it was rather than laundered, because bd-register-amend refuses status changes by design. |
| 125 | CLOSED @1228 -- MIS-SCOPED | LEDGER ITEM 31 SUB-ROW **FR-A6.3**. Builder enrichment slice 3, the remainder. Same shape and same blocker as row 124; if corpus evidence arrives it likely closes both, and they are separate rows only because the ledger carried them separately  RECONCILED @1228 AS MIS-SCOPED: the tree carries no independent A6.3 subject. `tools/builder_gap_report.py::_targets` maps all four addressable A6 targets to A6-1/A6-2 only (`slice_of`, :234-238); `rg` finds ZERO `A6.3`/`A6-3`/`A6_3` references anywhere in `tools/`, `tests/` or `bulk_downloader/`; and `tests/test_a6_dom_derivations.py::test_gap_report_targets_closed_on_synthetic` asserts every gold's `targets` list is empty (5 passed at `c7b207f`). This row's own text already conceded 124 and 125 are separate rows only because the ledger carried them separately. The remaining real-corpus acceptance is ROW 124's, which STAYS OPEN -- closing this row drops no obligation |
| 126 | PARKED @1253 | LEDGER ITEM 31 SUB-ROW **2c-DATA**. Reptyle selector re-capture. The re-capture is COMPLETE; five recaptured selector kinds still need review and a test against the live DOM PARKED: operator 2026-08-25: re-capture COMPLETE; five recaptured selector kinds still need review plus a test against the LIVE DOM. UNPARKED BY OPERATOR RULING 2026-08-31: the work is carried by successor row 455. This row's terminal status is left exactly as it was rather than laundered, because bd-register-amend refuses status changes by design and that refusal is the anti-laundering property. |
| 127 | PARKED @1195 | LEDGER ITEM 31 DECISION **EXIT-3**. OPERATOR-PARKED at v3.66.1195; the measurement that parks it follows. PostgreSQL cutover exit-criterion: full suite green post-cutover plus an operator soak of at least two weeks. Re-derived at v3.66.1195 by RUNNING the functions, not by reading their source, on test5 at each freeze of this cut with Python 3.12.3 and exit status 0; the transcript `phase10/claude-row127-preflight-runtime.txt` is regenerated at each freeze and carries the exact host, HEAD, tree, dirty state and UTC of its latest run rather than a timestamp pinned here that would rot on the next regeneration. `preflight_cutover()` returns `ok=False` with `dual_write` False, `shadow_read` False, `shadow_compared` 0, `shadow_diverged` 0, `shadow_skipped` 0, `degraded_reason` None, and `cutover_engaged()` is False -- identical to the v3.66.965 derivation, PLUS a fourth reason the earlier derivation did not record: `postgres is not reachable` on this host. Zero comparisons is an empty denominator, not agreement, and the product is correctly blocked fail-closed. A GREEN CI `postgres-integration` JOB STILL DOES NOT MOVE THIS ROW: it exercises the mod3 code paths and says nothing about production dual-write or a soak clock. The operator was asked directly on 2026-08-19 and chose to PARK it rather than start a soak. Parked means terminal for re-derivation, not closed: nothing an agent can do moves it, and it reopens the day a soak is started. The two weeks are wall-clock and cannot be accelerated, simulated, or backfilled from retained evidence. WHAT THIS ROW'S STATUS CANNOT SAY: the register's status grammar is exactly OPEN, CLOSED and MOOT (`tests/test_v3_66_1052_the_backlog_is_machine_visible.py`), so a parked row is byte-identical in the status column to a row an agent could pick up tomorrow, and it is counted in `open=`. That is a real loss of information, accepted here rather than papered over by inventing a status the gate would reject; the distinction lives in this sentence and nowhere machine-readable. UNPARKED BY OPERATOR RULING 2026-08-31: the work is carried by successor row 456. This row's terminal status is left exactly as it was rather than laundered, because bd-register-amend refuses status changes by design and that refusal is the anti-laundering property. |
| 128 | CLOSED @1195 -- CRITERION RETIRED, not met | LEDGER ITEM 31 DECISION **OPV-F3.1**. The criterion was a saved-search enqueue lane observed over seven uninterrupted days; the clock started 2026-07-23T01:37:15Z and the window ended 2026-07-30. MEASURED at v3.66.1195, 20 days after it lapsed and 377 releases later (3.66.818 was current at the last commit before the lapse, 541b19b on 2026-07-29; the earlier figure of 325 in this row was measured against the wrong subtrahend): the retained corpus holds 48 artifacts dated inside that window, they cover exactly ONE day (2026-07-28), and ZERO of them mention the saved-search or enqueue lane -- they are `*.template-draft.json` and capture-runner files from template onboarding. Over the COMPLETE 1343-file corpus (a `-g '!*.wacz'` grep gave 42 and silently omitted 742 WACZ), 49 files mention saved-search: 42 text artifacts from the 2026-08-18 fleet capture, plus 7 WACZ whose internal content mentions it -- five `evil.*` dated 2026-08-01, `df_title1_cap2.scrubbed.wacz` 2026-08-04 and `df_title1_cap2.wacz` 2026-08-09. ALL 49 postdate the window (the WACZ by 2 to 10 days, the fleet text by three weeks); ZERO fall inside 2026-07-23..07-30, which is the load-bearing fact and is unaffected by the earlier undercount. So the row's own first option, closing on evidence already collected, was never available: that evidence does not exist. The operator was asked directly on 2026-08-19, with those numbers, and chose to RETIRE the criterion rather than restart the clock. WHY OPTION TWO WAS DECLINED, since the row itself offered only two: restarting the clock would spend seven more uninterrupted days to observe a July lane against a codebase 377 releases downstream of it, and would answer a question about the lane as it was, not as it is. RETIREMENT IS THEREFORE A THIRD OPTION the row did not offer, taken deliberately and recorded as such rather than dressed up as one of the two. Retiring is explicitly the smaller and different decision from meeting it, and this row is the writing it required. NOTHING REPLACES IT: no seven-day continuity observation of the enqueue lane is owed, and if that question is ever worth asking again it needs a new row with a criterion written for the current lane rather than the July one. What is NOT claimed: the lane was never observed for seven days, so this row closes on a retired criterion and not on evidence of the behaviour. |
| 133 | CLOSED @1173 | The exact 24 pre-policy repository gates were removed from `gate_scope_baseline.txt`, explicitly marked `repo-wide`, and retained in the exact CI shard union. A direct regression pins the 24 identities, zero overlap with the shrinking legacy baseline (1,265 after Cut G classified defect precision), and a negative module-scope control; the migration cannot pass by swapping a different unclassified path into the baseline. |
| 134 | MOOT @1171 | the umbrella improvement program never instantiated: PROGRAM_LEDGER, bdtools_metrics, and bd-metrics remain absent, while independently shipped capabilities have current owners and this backlog is the sole successor authority. The stale plan is retired rather than revived. |
| 135 | MOOT @1171 | zero of 58 v539 batches has the plan's required MD+JSON completion pair; the two v532 JSON pilots predate it. The imperative plan is retired, while backlog 140's exact evidence census owns the only surviving historical measurement. |
| 136 | MOOT @1171 | the retired 87-checkbox governance program has no surviving atomic subject outside current backlog 129, 133, and 152 and the exact CI/shard authority. No speculative gate was created merely to preserve the old program. |
| 137 | MOOT @1171 | the retired v817 knowledge-hygiene program named a mostly absent tree. Current freshness and stale-document work is owned by backlog 106 and the completed row 141 adjudication; restoring the plan would recreate a competing authority. |
| 138 | CLOSED @1171 | current documentation now distinguishes four absent historical command names, the built bd-coverage-map launcher, and four built standalone Python analysis frontends. Standalone is not unbuilt, and no analyzer was forced into a composite gate without a separate product decision. |
| 139 | CLOSED @1174 | DEFECT-SUPPRESS now has one reviewed 12-entry authority at `project-knowledge/DEFECT_PATTERN_SUPPRESSIONS.json`. Each DP-13 decision binds detector, normalized repository path, lexical owner, owning try-body AST, handler AST, and rationale without line identity. The two scanner entry points share the same suppression identity and filtering contract while retaining their existing raw detector behavior; both filter only after raw cache retrieval, expose raw/visible/suppressed denominators, and fail closed on malformed, stale, ambiguous, escaping, unreadable, or duplicate authority. Structural ratchet parsing consumes the visible `by_dp` map once and cannot skip an unavailable pinned defect metric. |
| 140 | CLOSED @1171 | project-knowledge/AUDIT_COMPLETION_LEDGER.json publishes the complete 58-batch historical universe, exact 72 current batch-scoped finding identities plus F0001, six explicit namespace aliases, two artifact-backed batches, and 19 with no surviving evidence. Citation-only batches remain partial/unknown; the census closes the measurement without claiming the old audit program completed. |
| 141 | CLOSED @1171 | adjudicated all nine named stale-current documents: UI tokens, known flakes, code-intelligence method, operator policy, template cockpit, hazard register, and KB judgment were corrected; the defect catalog was already current at v1167; the false decomp-tools README was retired. Nested freshness remains independently OPEN as backlog 106. |
| 143 | CLOSED @1171 | re-derived the bare-CLOSED remainder population. The capture-vault collision moved through backlog 165, whose singleton resources are serialized @1191; true parallel services moved explicitly to OPEN backlog 175. Scope/freshness debts already resolve to 133/106; historical limitations and superseded defects were rewritten as terminal boundaries. The sanctioned PARTIAL syntax remains available for explicit historical evidence, but new actionable work receives its own OPEN row. |
| 144 | CLOSED @1133 | the long browser-wall observation was resolved by measured process placement and bounded timing evidence; later full-suite browser timing failures are preserved per-run and require complete low-load confirmation. The stale pre-answer wording is historical, not a current remainder. |
| 160 | CLOSED @1172 | RECON-7 reconstructed the exact historical twelve as bd-pack, bd-handoff, bd-checkpoint, bd-zipcheck, bd-mkauditstate, bd-mkbdsuite, bd-freshest, bd-optpack, bd-prestage, bd-ship, bd-snapshot, and the missing guardcheck-linked bd-since. The ten survivors and their live consumers were retired; the two already-absent names remain tombstoned. bd-coretest now refuses a missing subject before fault injection and bd-tool-lint rejects any missing critical-core member. |
| 161 | CLOSED @1192 | S5-RESIDUE -- row 115 retired `scripts/build_release.sh` first; B0 then reconciled every `/home/claude` carrier. The live phase closed 46 removable code-shaped carriers @1192 while preserving six adversarial controls. The separately measured prose phase removed 33 obsolete occurrences from 11 current operator documents @1192. The fixture phase hand-adjudicated the actual 20 external test carriers / 50 intentional occurrences as runtime fixtures, retired-path detectors, historical regressions, or a security-boundary fixture; the earlier 21/51 count incorrectly included this gate's own newly spelled literal and is withdrawn. Reusing the assembled needle keeps the gate outside its denominator. Final current source is 71 durable carrier files / 260 occurrences with zero `UNSWEPT`; the 260th is this cut's own changelog entry, which names the retired path to describe the closure and is therefore counted rather than exempted. The distinct `/mnt/user-data` family is 32 files / 67 occurrences at @1192 (base was 33/70; row 115 removed two carrier files and three occurrences while this authority sentence remains a carrier) and stays outside this tombstone. All three phases are an exact required set and independently reject removal. The fixture denominator is machine-visible as exact per-file counts, 50 reconciled occurrences, and one semantic anchor per carrier. Installed `bd` and `bd-status` physically resolve the shared environment beside their install prefix; the installer validates the checkout and every destination relationship before its first filesystem mutation, then records the canonical root. Hermetic scratch-layout controls strip ambient overrides, assert exact pointer bytes, execute through the public symlink, prove invalid checkout leaves prior artifacts byte-identical, and prove missing/corrupt pointers fail closed. Installed/copy-fixture tools remain importable before their explicit `--work` is parsed; verdict consumers validate the actual subject. Review artifacts stay under the existing repository-root override rather than create a new runtime-tunable environment surface, and the live-path behavior test is explicitly classified by the gate-scope policy. |
| 162 | CLOSED @1178 | NESTED-PART -- `check_orphan_tempfiles` now recursively reports the same five temporary-artifact shapes under every configured download/capture hygiene root. The traversal remains read-only and bounded: directory symlinks are not followed, missing roots and per-file observation errors retain their prior best-effort behavior, recent siblings remain invisible, and exact-path samples prove the nested identity observed. Deterministic RED/GREEN coverage preserves the prior top-level behavior and explicitly keeps the subjectless stale-lock check retired. Current producers place partials beneath download, capture, resume, RAM-stage, queue, and library run directories; crash recovery and capture diagnostics already use recursive discovery, so the startup health denominator now matches that nested shape. |
| 163 | MOOT @1175 | AUTH-SCENE no longer requires a fresh authenticated-site run. The bounded target is `www.filthykings.com` with exact authenticated alias `members.filthykings.com`; `billing.filthykings.com` remains rejected. Retained non-secret provenance is `filthykings.com_026255e0_20260721_003033_62b2.redacted.wacz`, 406625 bytes, SHA-256 `764386de034f8daec2de29999efcc30b389d683b04e453df6e255251c521e969`: its 2026-07-21 capture records 2588 DOM entries, 905 network entries, members-host requests, the Gamma API/media hosts, and the play/manifest/segment/quality action sequence. The current tracked `templates/reviewed/filthykings.com.template.json` is SHA-256 `fa3fef2aa68cb141ad04fdaceb939930745061ff4d208a51abd80fbd1aa888d0`, status enabled, and carries the same provenance plus a ready/gap-free capped-probe verdict. Current registry tests prove canonical and authenticated-alias resolution and sibling rejection. Operator decision: preserve the redacted capture as historical provenance, treat source plus hermetic tests as the current acceptance authority, and do not contact the authenticated site merely to recreate evidence for a behavior already owned by deterministic contracts. |
| 164 | CLOSED @1177 | AI-BOOT-OBS -- capture records and gates an atomic bounded observation of the independent AI companion without adding any main-unit dependency. The exact evidence binds sanitized effective configuration, systemd invocation and restart identity, same-boot current-schema finality, ordered list/GPU/text/vision/residency phases, and positive text plus vision model VRAM ratios. Startup transients are polled within immutable time/sample ceilings; missing, stale, malformed, unsupported, degraded, over-contention, or acquisition-error evidence produces a structured HOLD artifact rather than an invisible pass. |
| 165 | CLOSED @1191 | Capture vaults are keyed as `/tmp/bd_capture_vault-${CAPTURE_RUN_ID}` and held by a directory descriptor from creation through shared object-bound teardown. A fixed global flock serializes every capture that mutates or probes the genuinely singleton systemd unit, drop-in, port 5555, and fixture port 8899: a concurrent vault or non-vault capture exits 73 with `CAPTURE-VAULT-CONCURRENCY-REFUSED`, sequential captures succeed, and the claim is held through all service-dependent stages. Detached pytest, live-test, and fixture descendants cannot inherit that claim, the holder is identified on refusal, non-destructive keyed ownership is directly gated, and credential writes are refused if chmod, descriptor, or flock acquisition fails. The real shared Xvfb launcher runs under a fake server and is proven to close the singleton descriptor too; setup-fault cleanup is descriptor/identity-bound and preserves substituted peers. The shared helper validates its inherited descriptor, safely ignores malicious or nondecimal values, and evaluates no environment text in the setsid or fallback launchers. Teardown passes the creation descriptor across the real shell/CLI boundary to `bd-gc`, refuses pathname replacement, and cannot name a peer vault. Crashed keyed vaults expose a per-vault lock: LIVE is never eligible; secrets-bearing released vaults and authenticated interrupted-removal residues are bounded classified populations with automatic collection. True parallel capture services and ports are row 175 rather than hidden in this serialization cut. |
| 166 | CLOSED @1179 | FRONTEND-SECRET-REGEN -- the generated frontend secret-key contract is now owned by the canonical regeneration chain. Its three server inputs are derived from the generator's import closure, every producer selects the sync gate in the affected band, CI checks the tracked TypeScript output, and mutations in each source domain prove stale output cannot escape. |
| 167 | CLOSED @1176 | TEMPLATE-SNAPSHOT-COVERAGE -- binds the exact ten producers (the compatibility shim, package assembler, accessor, and seven data modules) to `test_templates_list_identity.py`. Every producer bands the gate, the canonical regeneration workflow verifies the frozen 91-element identity without ever auto-freezing it, and a semantic template mutation is caught before the original identity is restored and rechecked. |
| 168 | CLOSED @1181 | PYTEST-CAPTURE-DIAGNOSTICS -- six-host capture evidence is exact: every host executed 16811 identities (16790 PASS, 21 SKIP, zero FAIL/ERROR); per-host >30s counts were 8/6/10/14/7/12, with intersection 6 and union 18. pytest_capture_results now reconciles two complete disjoint JUnit lanes before emitting schema-v2-compatible JSON/text: a measured 30.0s threshold, deterministic top-20 slowest and complete over-budget lists sorted by (-duration, identity), and exact skip identity/reason diagnostics. Parse ambiguity, duplicate identity, bad counts, missing lane, invalid duration, or vacuous evidence refuses instead of rendering a clean report; failure/error still makes ok=false without changing pytest's verdict. |
| 169 | CLOSED @1181 | SKIP-BASELINE-ENFORCEMENT -- the same six captures show all 39 permitted baseline identities execute on capable and constrained hosts: 18 PASS plus 21 exact permitted SKIP, not a false requirement for 39 simultaneous skips. capture.sh runs the repeatable two-lane reconciliation once after both complete JUnits; unexpected skips, missing baseline identities, duplicate/malformed/zero evidence, failure/error, or changed skip message refuse, and the checker exit reaches the final capture verdict without masking causal lane/converter exits. Baseline --update is disabled: shrinking the 39-identity authority to one host's observed 21 skips requires explicit hand adjudication. |
| 174 | CLOSED @1250 | FLEET-RESIDUE-OWNERS -- the six-host census and the operator's four retention owners are now recorded. (1) row 245 owns the 14 post-@1191 markerless UNKNOWN roots; the fresh census found no surviving pre-@1191 markerless roots and removed none. (2) The tracked bd-wedge-hunt owner retains `~/.bd-tools-venv` and `~/.local/bin/py-spy` on test4/test6/test7: each venv contains 898 files / 18,930,597 bytes, each link resolves inside that venv, and all three report py-spy 0.4.2 with binary sha256 `9b4d1f39b2a47ae44f4c6a46f615dcc0287d7755beba5065f32391951e07d594`. (3) The row-13 security-evidence owner retains test6's mode-0600 `~/.ssh/authorized_keys.pre-row13`, sha256 `b1b15e44a688018fc6027e66f7496d03548fa01080e01a02fa964c25f039bb21`; the separate live-key duplicate is row 244 and no authorized-keys file was edited. (4) The historical wedge-evidence owner retains byte-identical `~/cwatch.sh` on test4/test6/test7 plus the three distinct `~/bd-channelwatch.log` files, one per host. REMOVED test6 `/mnt/bdtmpfs` only after proving hostname `test6`, machine-id `f034e0bb5cdb4521ac09e9fe6429bc0a`, exact directory identity `dev=fc00 inode=7344125 uid=0 gid=0 mode=755`, zero children, exact-findmnt rc=1, mountpoint rc=32, zero fstab references, `mnt-bdtmpfs.mount` LoadState=not-found, zero references in 1,073 systemd files, and root-level lsof/fuser no-consumer rc=1. `rmdir` returned 0 at 2026-08-25T17:36:05Z; readback recorded `READBACK_EXISTS=no`, realpath/stat rc=1 and basename count 0. The duplicate key and process findings are report-only rows 244/243. Complete command/output evidence is `/home/mboyle/fleet-run-artifacts/2026-08-25/codex-cuts/row174.txt`. |
| 205 | CLOSED @1211 | GATE-HARDENING [LOW] -- `tests/test_codex_handoff_stays_retired.py` is a textual-proxy gate: it scans SOURCE TEXT to judge a runtime/behaviour property (The codex-handoff second agent-facing contract is retired and unreferenced executably (test_nothing_still_executes_against_the_handoff :112), and CLAUDE.md still owns the venv facts the handoff contra). Reported evadable: Verified: revive the document as lowercase codex_handoff.md and reference it with `Path("codex_handoff.md").read_text()` -- the RETIRED tuple names only uppercase CODEX_HANDOFF.md (:43) so the existenc. Fix per the CUT_TIERING gate rule -- behavioralize (exercise the property: render/click, drive the test client, structural-parse the CI) or, where a scan is an unavoidable floor, comment-strip+normalize, declare the evasion surface, and ship an evasion fixture. UNVERIFIED CANDIDATE: this LOW verdict was flagged by the audit; some LOW gates (notably test_v3_66_1098/1108, which the audit's own synthesis calls the SOUND MODEL) may not actually be evadable -- verify the evasion or close as NON-REPRODUCING. Evidence: `gate-antipattern-audit.md`. CLOSED @1211: the invocation scan folds assembled literals and casefolds, so a lowercase handoff path built in pieces is detected. Evasion shipped as `test_assembled_lowercase_python_reference_is_detected`; node count 4->5. |
| 208 | CLOSED @1210 | GATE-HARDENING [LOW] -- `tests/test_v3_66_1117_cockpit_home_stays_retired.py` is a textual-proxy gate: it scans SOURCE TEXT to judge a runtime/behaviour property (No served page links to the retired /cockpit/home route (docstring :1: 'No served page may link to /cockpit/home') -- an operator must never click into a 404. A runtime property about rendered HTML). Reported evadable: A relative href on a surviving cockpit page: '<a href="home">&larr; Cockpit Home</a>' rendered by the /cockpit/settings page resolves in the browser to /cockpit/home, yet contains no occurrence of the. Fix per the CUT_TIERING gate rule -- behavioralize (exercise the property: render/click, drive the test client, structural-parse the CI) or, where a scan is an unavoidable floor, comment-strip+normalize, declare the evasion surface, and ship an evasion fixture. UNVERIFIED CANDIDATE: this LOW verdict was flagged by the audit; some LOW gates (notably test_v3_66_1098/1108, which the audit's own synthesis calls the SOUND MODEL) may not actually be evadable -- verify the evasion or close as NON-REPRODUCING. Evidence: `gate-antipattern-audit.md`. CLOSED @1210: the gate drives the Flask test client, parses rendered anchors, resolves each with urljoin the way a browser does, and compares normalised paths. Reproduced first: <a href="home"> from /cockpit/settings resolves to the retired /cockpit/home while no source file contains that string. The text scan is retained as a floor with its evasion surface declared. |
| 243 | CLOSED @1465 | UNREGISTERED-PYTEST-PROCESSES -- CLOSING EVIDENCE (close-list.tsv, re-verified 2026-09-04 by an independent sweep that ran one command per row against this tree): OPERATOR_DECISIONS.md ~23:2xZ: "243: (not answered separately) closes as a recorded report alongside 244's resolution -- DEFAULT, revisit if wrong" (verified by reading the line myself). BLOCKER: tests/test_v3_66_1171_backlog_truth_is_current.py:105-107 asserts row 243 status=="OPEN" inside a repo-wide gate wired at .github/workflows/ci.yml:347 -- the expected_open dict must be updated in the SAME commit as the close or CI breaks. ORIGINAL ROW FOLLOWS -- report only; DO NOT KILL these processes because they may be another worker's live measurement. The operator-accepted 2026-08-25T17:30Z census recorded `accepted census=3` unregistered pytest masters. The complete six-host readback at 17:38-17:39Z returned test2/test3/test4/test6/test7=0 and test5=7 (PIDs 1587184, 1619109, 1655881, 1656718, 1656837, 1665966, 1667312), with `bd-jobs orphans` rc=1 and no malformed-registry UNKNOWN; the increase is expected volatile evidence from concurrent cuts, not permission to rewrite the accepted count. No process was signalled. Acceptance: for each still-live identity, record `/proc` starttime, ancestor launcher, checkout, command owner and whether a valid `bd-jobs` entry should exist; make each owned long-running launch register before work or document an explicit non-owning exemption; then prove the same exact identities ended naturally or were handled by their owner and a fresh complete fleet census reports zero unexplained unregistered pytest processes. Dependency: process owners/operator coordination; this row grants no kill authority. |
| 244 | CLOSED @1465 | DUPLICATE-TEST6-MBOYLE-LAPTOP-KEY -- CLOSING EVIDENCE, operator OPERATOR_DECISIONS.md 23:3xZ, verbatim: "test6 authorized_keys has 14 lines, ZERO duplicated key material (measured twice, nothing removed)". The row asked whether a duplicate key line existed; it does not, and the measurement was repeated rather than taken once. ORIGINAL ROW FOLLOWS -- report only; DO NOT EDIT ANY `authorized_keys` FILE in this row. On test6 (`f034e0bb5cdb4521ac09e9fe6429bc0a`) at 2026-08-25T17:37:01Z, live `~/.ssh/authorized_keys` was a mode-0600 regular file (`dev=fc00 inode=2276825 uid=1000 gid=1000 size=351 mtime=2026-08-21T22:09:44Z`, sha256 `11d313bad993a13385aa7064ea1046342f5bdbac31558189f481bf6776da1709`). Its complete `mboyle-laptop` population was 2 matching lines / 1 unique line; the two-line match-set sha256 was `f22f2dd14d344112c3492e34d750c09a793c8759b8dd8bb1816515227be0033d`, and no public-key bytes are copied here. Acceptance: the operator, not an agent, uses the sanctioned key-management path to retain exactly one intended laptop authorization, then records pre/post file identity and proves the complete matching population is exactly one without changing any unrelated authorization. Dependency: operator credential authority. PROVENANCE ADJUDICATED BY OPERATOR RULING 2026-08-31: the 2026-08-26T04:12:19Z agent dedupe is RATIFIED as the authoritative provenance record for this row. It is the only record that exists, and the identities it adjudicated were destroyed by the 2026-08-30 test5 reboot, so re-derivation is impossible -- ratifying a record that cannot be rechecked is a deliberate operator choice, recorded here rather than left implicit. The row may now close on that record plus its remaining named evidence. |
| 245 |   CLOSED @1465 | POST-1191-MARKERLESS-TEST-ROOTS -- CLOSING EVIDENCE (close-list.tsv, re-verified 2026-09-04 by an independent sweep that ran one command per row against this tree): Commit 5e2eeac3 (v3.66.1262) exists. tests/test_v3_66_1255_test_roots_publish_ownership_atomically.py imports BOTH toolchain/bin/bd-gc (consumer) and tests/_tmproot.py (producer) and tests the atomic-publish seam over 10 injected failure points -- I ran it myself: 12 passed, 0 tree mutation. This directly answers sonnet's doubt ("no check found that all producers publish both atomically"). RESIDUAL (why not HIGH): row 245's own ACCEPTANCE (the clause beginning `Acceptance:` in this row, below -- this cited a LINE NUMBER in this file until v3.66.1525 and the number was ALREADY WRONG before the archive move, resolving to row 176 rather than to this row; CLAUDE.md's rule is cite the heading, never the line, and a self-citation by line is the shape that guarantees it) names the 14 specific pre-existing markerless roots and says "Existing roots remain operator evidence until separately adjudicated" -- test5's 13 are confirmed gone (drafter, this host), test2's 1 root ("1rxmbimu") is unverified from this host. ALSO the same CI blocker as 243/244: tests/test_v3_66_1171_backlog_truth_is_current.py:105-107 pins row 245 status=="OPEN". ORIGINAL ROW FOLLOWS -- a six-host dry-run at exact deployed HEAD `3372dbeb15aeb54a7b4d2adc0b78df081710fc2e` / tree `9939056e3ed0dbffef6119c7bbaa60bfe3a792e7` classified 1,893 `/tmp/bd-testrun-*` roots and found exactly 14 UNKNOWN roots with neither `.bd-testrun` nor `.bd-testrun.lock`: test5=13 (`31d_bmbv`, `4lrqprvh`, `8uz3xay8`, `92yumgy6`, `939d7qki`, `dz85fe47`, `e7ronv62`, `lj6aknv0`, `mt7yx5at`, `rc223v_v`, `rp63wguz`, `tbcsnowc`, `u_jddq1a`), test2=1 (`1rxmbimu`), test3/test4/test6/test7=0. Every root has a nonzero filesystem birth time after ancestor commit `3e01ef0f48b03b925ff40c3db0a31a478111f9fa` (@1191, epoch 1787145933), so these are post-policy producer failures rather than the legacy pre-policy population row 174 expected. Every UNKNOWN root was NOT REMOVED. Acceptance: identify and deterministically force every producer path that can create the public root before publishing both ownership objects; preserve UNKNOWN/fail-closed cleanup for unavailable metadata; prove the fixture creates the markerless shape and the classifier fires exactly once; and add a negative control where successful publication is not reported. Existing roots remain operator evidence until separately adjudicated. Dependency: none. |
| 250 | CLOSED @1257 | SOCKET-RECORDER-LAUNDERED-LOST-ROWS-INTO-A-CLEAN-ZERO -- RED on 2026-08-26 at base f636bcb with the integrator command: the pairing test made `_lan_ip_guess()` connect a UDP socket to 8.8.8.8:53, yet the terminal evidence said 0 non-loopback attempts and 1 connect observed. `tests/_socket_record.py::sink_dir` resolved mutable `tempfile.tempdir` after `_tmproot.install()` changed it, so serial cleanup deleted the worker row before terminal summary and xdist gave the master and every worker different roots. CLOSED @1256: the recorder now captures real system temp at import and derives every sink from that shared, cleanup-surviving anchor. The exact reproduction is GREEN with 1 non-loopback attempt from 1 test and names 8.8.8.8:53 as SOCK_DGRAM. A real `-n 2 --dist loadfile` subprocess writes exactly 4 rows from 4 tests into exactly 2 per-pid worker files and the master reports all 4. A forced deleted-row arm reports UNKNOWN with 0 readable rows and 1 observed connect, while a genuinely connect-free run reports the distinct clean zero with 0 observed. The durable `bd-mutate` anchor reversion was CAUGHT by the named xdist test, and the identical valid transform ESCAPED its import-only control. CI runs the repo-wide gate in the isolation shard. |
| 280 | MOOT @1268 | FAILED-TOOLCHAIN-MEASUREMENT-IS-UNKNOWN -- This assigned row is an exact duplicate of row 264, which commit ede1181 (v3.66.1268) had already fixed on the starting main before row 280 was dispatched. The five sites are `tools/regenerate_goldens.py`, `toolchain/bin/bdtools_sec.py`, `tools/sast.sh`, `tools/operator_layer.py`, and `scripts/bd-opv-run.sh`; their shared gate is `tests/test_failed_measurements_have_distinct_states.py`, registered in the CI toolchain shard, with durable primary and transform-control specs under `tests/mutants/failed_measurements_distinct*.json`. Replaying the five named catchers against ede1181^ produced exactly five intended failures: golden apply returned 0 instead of refusal 2 and reported `applied: 1`; strict source scan did not raise SystemExit; SAST exited 0 and printed Clean after scanner failure; governance scanned 0 artifacts and returned `compliant=True`; malformed F2a JSON recorded PASS instead of FAIL. On the fixed tree the focused gate passes. The required bd-mutate battery reports 5 CAUGHT, 0 ESCAPED, 0 INVALID/UNKNOWN; the identical F43 transform control reports 1 ESCAPED. No second source fix, gate, CI entry, or mutant spec was invented for a defect already closed. |
| 455 | OPEN | ROW-126-SUCCESSOR-THE-REVIEWED-TEMPLATE-AGAINST-A-LIVE-DOM -- PARTIAL LANDED, ROW STAYS OPEN: v3.66.1463 carried patch row455, landed in commit 5d13e1b2, merged at 0a6cab9b and proven an ancestor of origin/main. It pins 19 selectors of the reviewed app.reptyle.com template, resolved by the shipped template_selector_verifier.verify_template_source over a live login-wall DOM captured session-free on 2026-09-03: the three login selectors DECIDED and identical in count to row 126's recorded 2026-06-15 DOM 80 days on, the four absent kinds reading UNKNOWN rather than reviewed, the fixture digest and inertness asserted as preconditions, and a negative control on the recorded modal page. WHAT REMAINS: four of the five selector kinds are still UNKNOWN because the AUTHENTICATED live DOM is unreachable -- the capture is session-free, and per the sibling finding filed as row 670 verify_template_source has no session option at all, so no template can yet be resolved against an authenticated page. This row closes when those kinds are adjudicated against an authenticated DOM, not before. ORIGINAL ROW FOLLOWS -- OPERATOR RULING 2026-08-31: unpark. bd-register-amend refuses to change a row status by design and raw editing the register is forbidden, so the work is carried by this successor OPEN row rather than by laundering row 126's terminal text; row 126 is amended only with a pointer here. Row 126 is PARKED @1253 with its remaining work named in its own text: the reviewed template kinds need review plus a test against the LIVE DOM, which a synthetic fixture cannot supply and which A6 keeps out of a repository test. THE OPERATOR GRANTED CAPTURE AUTHORITY on 2026-08-31. ACCEPTANCE: resolve the reviewed app.reptyle.com template against the live DOM through the operator harness, record the resolution with host, SHA and the exact selectors that matched, adjudicate each reviewed kind against what the live page actually contains, and pin the outcome in a test that can run without the live site afterwards; a kind the live DOM cannot decide reads UNKNOWN rather than reviewed, per CLAUDE.md A7. DEPENDENCY: none. |
| 470 | OPEN | FORTY-THREE-TOOLS-REPORT-GREEN-OVER-A-TREE-THAT-NEED-NOT-EXIST -- MEASURED 2026-08-31 by bd-tool-lint on main b2619507: of 150 tools that accept a corpus-shaped argument (--tree, --work, --home, --scan or --corpus), 43 never call require_corpus(), so a caller naming a path that does not exist, or that exists and is empty, gets a clean verdict about nothing. bd-tool-lint already classifies this as corpus-arg-unguarded and already reports the roster, and the ratchet baseline for it is 0, so the finding is not new information to the lint -- it is unenforced. This is the same fail-open that A7 names directly, multiplied across a third of the operational toolchain, and it is the reason a green tool result cannot currently be taken as evidence about a specific tree. The lint run also recorded 36 no-json, 6 non-ascii, 5 selftest-contract and 1 no-selftest findings across 61 distinct tools, and when-coverage is BLOCKED by 3 stale catalog entries (bd-boot, bd-install, bd-repin-dist) plus 27 uncategorised tools; those are separate dispositions, not part of this row. ACCEPTANCE: enumerate the 43 from the tree rather than from this text, since the number moves; for each, either add the guard or record an explicit exemption with the reason its argument is not corpus-shaped; prove RED by pointing a guarded tool at a nonexistent path and at an empty directory and requiring a nonzero UNKNOWN exit for both, and prove the guard did not merely make every invocation fail by keeping a positive control over a real tree green; and hold the corpus-guard count at zero with the existing ratchet so a new unguarded tool cannot be added silently. DEPENDENCY: none. |
| 471 | OPEN | THE-DEPLOY-PATH-NEVER-UPDATES-THE-MIRROR-IT-FETCHES-FROM -- MEASURED 2026-08-31 deploying v3.66.1378: bd and bd1 REFUSED with INTENDED-COMMIT-ABSENT, naming the merged commit 8b179d65 and reporting that a successful fetch from /home/mboyle/bd.git left it absent because that mirror's main was still b2619507. The refusal is correct and even prescribes the remedy, which is the point: deploy.sh already knows a fetch exiting 0 from a mirror is not delivery, and nothing in the deploy path ever pushes into that mirror, so the two hosts fail this way on EVERY release. The same log shape is preserved from 2026-08-30, so this is at least the second occurrence, and each time a human pushed by hand. The same root cause was silently worse on the capacity hosts: bd2, bd3 and bd4 each pointed at their own per-host bare mirror and sat at v3.66.1349, v3.66.1360 and v3.66.1371 while every fetch exited 0, which is a stale checkout reporting itself current -- the exact ORIGIN-NOT-AUTHORITATIVE hazard deploy.sh refuses on, undetected because nothing asks the capacity hosts anything. ACCEPTANCE: derive the set of hosts whose origin is not the official remote from the tree or the roles file rather than from this text, since it moves; make the release path either push the commit into every mirror it will later fetch from, or refuse before starting with the list of hosts that cannot receive it, so a partial fleet is never the silent outcome; RED-first, replay a mirror pinned one commit behind and require the refusal to name the host and the absent commit, and prove the fix delivers by asserting the mirror's main equals the intended commit BEFORE any host is deployed; negative control, a fleet whose mirrors are already current must perform zero pushes and still deploy. DEPENDENCY: none. NARROWED 2026-09-02 by measuring ALL TWELVE hosts (evidence: /home/mboyle/bd-persist/workers/1455/ROW471_MEASUREMENT.md): as a claim about the FLEET this is now FALSE -- 396 clones enumerated, ZERO with any remote pointing at a bare mirror; 12/12 origins authoritative under deploy.sh's own _origin_is_authoritative normalisation; 12/12 ls-remote resolved to the intended commit; bd and bd1, the two hosts this row names, are current and healthy. IT REMAINS TRUE OF THE BRING-UP PATH, which is why it stays open: /home/mboyle/bd-freshhost-50.sh lines 21-23 and /home/mboyle/bd-vm-bringup.sh lines 23-25 still `git init --bare ~/bd.git`, push main into it, then clone FROM it, leaving origin a local path -- while the sanctioned docs/repo/FRESH_HOST_BRINGUP.md:158 says `git clone <origin-url>`. THE NEXT FRESH HOST REPRODUCES THIS ON CONTACT. ACCEPTANCE IS NARROWED TO THAT: the two bring-up scripts clone from the authoritative origin, proven by provisioning a host and reading its remote, and no host-fetch claim is required because it is already measured false. NOTE FOR ANYONE TIDYING UP: ~/bd.git is NOT inert -- on six hosts it is the live band transport. |
| 633 | CLOSED @1465 | CUT-1414-RUNNER-RESUME-RECOVERY-IS-DEFERRED-BY-OPERATOR-RULING -- CLOSING EVIDENCE: the cut/1414 candidate authored content LANDED via c4102a6a "train 1: resume cannot leave the hold state it set", which is an ancestor of main, and tests/test_row434_resume_cannot_leave_the_hold_state_it_set.py is present on main as a superset of what the candidate carried. No worker was needed; the work shipped under a different commit than the row anticipated. ORIGINAL ROW FOLLOWS -- OPERATOR RULING 2026-09-01: candidate 56a8768ba0a7aaa7b5947c7ee132ae7e81f055c7 on local cut/1414-runner-three-findings-recovered implements row 434 and its lifecycle routing; its exact six changed files derive a 412-test band in that candidate worktree, the largest candidate band measured in this session and roughly 30 minutes of lane time for one row. Candidate ancestry is absent from origin/main. Current origin remote and recover/1414-* tag availability could not be confirmed by ls-remote and is UNKNOWN, not evidence of loss. ACCEPTANCE: keep this row OPEN until candidate 56a8768b lands, or every one of its six changed files is proven blob-identical to origin/main as superseded; SHA ancestry alone is insufficient. DEPENDENCY: operator lane-time ruling. |
| 648 | OPEN | REACT-ROUTER-6-TO-7-IS-A-MIGRATION-AND-TWO-ADVISORIES-WAIT-ON-IT -- MEASURED 2026-09-02 on origin/main 0b150ad1 (v3.66.1438). GitHub reports two OPEN medium advisories against react-router: an open redirect via a backslash in Link and useNavigate, and arbitrary constructor injection via deserializeErrors(). BOTH have first_patched_version 7.18.0 and NO 6.x line, so the vulnerable range >= 6.0.0 < 7.18.0 covers every version this product can currently run -- there is no patch-level escape. THE UPGRADE IS NOT A BUMP. React Router 7 merges react-router-dom INTO react-router, and `git grep -l react-router -- frontend/src` returns 82 files; frontend/package.json declares react-router-dom ^6.30.5 rather than react-router directly, so the dependency the advisory names is not even the one the manifest lists. Dependabot raised it as PR #707 (a lockfile bump across 2 files) and that PR was CLOSED on 2026-09-02 rather than merged, because merging an 82-file API migration through an auto-generated dependency PR is how a routing regression ships unreviewed. A scoped .github/dependabot.yml now ignores react-router MAJOR updates only -- every other update, including patch and minor for react-router itself, still flows -- so the same PR is not regenerated weekly and merged unexamined. THAT IGNORE ENTRY IS PART OF THIS ROW AND MUST BE DELETED WITH IT; leaving it in place after the migration would silently suppress the next genuine major. ACCEPTANCE: migrate to react-router 7 in a dedicated cut -- consolidate react-router-dom imports onto react-router, enumerate every routing API whose signature moved and state each one's disposition BEFORE changing it, and prove the result with the full frontend vitest suite, tsc --noEmit, a production build, and a route-by-route check that every path in the SPA still resolves; then confirm both advisories report resolved and remove the dependabot ignore entry in the same cut. DEPENDENCY: none, but it should not be batched with anything -- it touches every route in the product. |
| 654 | OPEN | TEST-FILES-EXIST-FOR-ROW-IDS-THAT-HAVE-NO-ROW -- MEASURED 2026-09-02 while tracing the register's fifty-seven absent ids. FOUR tracked test files are named for id 407, and one each for ids 362, 379, 387 and 399 -- and none of those five ids exists in project-knowledge/IMPROVEMENT_BACKLOG.md. Id 407 is the sharpest case: four files, two distinct subjects, and nothing to adjudicate which of them the id means. NOTE THE PHRASING DELIBERATELY SAYS "ID" AND NOT "ROW": tests/test_v3_66_1255_backlog_references_resolve.py resolves every `row N` reference against the row population and REFUSED an earlier draft of this row that called them rows. The gate was right and the draft was wrong -- these are ids precisely because they are not rows, and that refusal is recorded here rather than worked around. This is the mirror of the gap the contiguity gate catches. That gate compares the register against ITSELF, so a test file naming a nonexistent id is outside its denominator by construction, and 1255 runs the other direction only -- register prose to rows, never tree to rows. ACCEPTANCE: extend 1255's denominator to the tree side, so a tests/test_row<N>_* file whose id has no row is a named failure; then adjudicate the nine files -- rename to the id that actually owns the work, or file the missing entry. A negative control must prove the extended gate still passes on a correctly-numbered file, and the extension must not re-refuse a row that legitimately discusses an absent id, as this one does. DEPENDENCY: the contiguity gate landed at v3.66.1441; this is its other half. |
| 656 | OPEN | CUT-1205-PERSISTENT-QUALITY-LINE-NEVER-LANDED -- MEASURED 2026-09-02 on test5 by the worktree census (bd-persist/worktree-census-20260902T174434Z.shipped.tsv): seven registered worktrees under fleet-run-artifacts/2026-08-21/cut-1205-persistent-quality/* and .worktrees/cut-1205-persistent-quality sit on four heads dated 2026-08-21 (fc025991 "require contiguous validator trust transitions", cd6d920d "accept exact validator v3 environment pins", 3db2456c "bind validator execution to explicit policy", fed6f915 "bind cut evidence to observed execution"), tagged keep/test5-cut1205-persistent-quality-<sha8> on origin with their dirty diffs archived under bd-persist/archive/2026-09-02/cut-1205-persistent-quality/. Their files -- toolchain/bin/bd_cut_quality.py, tests/test_cut_quality_permits.py, the phrase "trust transition" -- do not exist on main: blob equality against main's own v3.66.1205 (9e45068c) and v3.66.1207 (18448251) reports every authored path differing. The design was either abandoned or never finished; nothing on main records which. ACCEPTANCE: the operator states whether a cut-quality receipt/validator-policy tool is wanted. If yes, a cut restores from the tags and lands it through the lane; if no, this row closes MOOT and the seven trees are removed after the tags are re-verified on origin. DEPENDS: operator decision. |
| 657 | OPEN | TWO-SAMPLES-OF-ONE-CANDIDATE-DISAGREED-BY-20-NODES -- STAYS OPEN, AND HERE IS WHY A LANDING DID NOT CLOSE IT: v3.66.1467 carried patch row657c, which hardened the supervisor teardown gate in tests/test_v3_43_76_phoenix_supervisor_thumbs.py to discover every configuring class and assert restored module state after each test. That is a real member of this row's FAMILY -- leaked module state across tests -- and it is landed and blob-verified. It is NOT this row's ACCEPTANCE, which asks for the named mechanism that makes tests/test_row386_the_download_chain_is_gated.py exceed 240 seconds inside a 772-file band while passing alone in 20. Integrator C refuses to credit a family member as the row's answer: crediting a patch for a row it did not close is how a release once credited a row for work it had nothing to do with. WHAT REMAINS is exactly the original acceptance below. ORIGINAL ROW FOLLOWS -- v3.66.1451 candidate b9106328 run twice with no edit between: sample 1 gave 20 failing nodes (19 of them pytest-timeout at 240s, not assertions) across tests/test_row386_the_download_chain_is_gated.py and tests/test_v3_66_1160_bridge_verifier_isolation.py; sample 2 gave band=0 over 772 files and ALL GREEN. Per-patch bisect found both patches clean in isolation and the base replay failed a different node. Evidence: bd-persist/evidence/row657-order-dependence/ holds both band logs and the base replay. FLAKE COURT ADJUDICATED THIS 2026-09-04 22:4xZ AND THE VERDICT WAS INHERITED, WHICH IS NOT PROGRESS TOWARD THIS ROW'S ACCEPTANCE AND MUST NOT BE READ AS ANY. Inherited means the failure was not CAUSED by the candidate under test -- it rules out one hypothesis, the strongest and cheapest one, and it names no mechanism. This row's acceptance asks for the mechanism, and says in its own words that a verdict of flaky is not acceptance; inherited is that verdict wearing a more precise name. The court left this row OPEN, correctly. WHAT THE ADJUDICATION IS WORTH: a fixing cut may now stop looking for a cause inside any one candidate's diff and look instead at the schedule, the neighbours and the shared resources -- which is where this row's acceptance already points it. ACCEPTANCE: name the mechanism that makes test_row386 exceed 240s inside a 772-file band while passing in 20s alone -- a leaked resource, a shared fixture, a port or lock, or a scheduling artefact of --dist loadfile. bd-shuffle-lane is the instrument. A verdict of flaky is NOT acceptance. |
| 661 | OPEN | A-CARRIER-IS-LEAKED-ON-THE-LOW-DISK-RETURN-PATH -- bulk_downloader/live_recorder.py around line 796: the early return taken when disk is low does not release the carrier _spawn_recording had prepared. Found INDEPENDENTLY BY BOTH lenses reviewing w2-egressc, neither of which refused on it: it is a bounded resource leak rather than a fail-open, it is pre-existing, and it is outside that patch's subject. Recorded so a reviewer's finding does not evaporate with the review. ACCEPTANCE: a test that fails on the current tree for the leak itself -- assert the carrier is released exactly once on the low-disk path, with a negative control on the normal path -- then the fix. Two further notes from the same review are deliberately NOT rows because both fail CLOSED: an untested close() path and an untested app.py wiring. |
| 665 | OPEN | api-queue-v2-CANNOT-SEE-TERMINAL-JOBS -- campaign finding D4. The v2 queue route cannot return jobs in a terminal state, so a caller polling it cannot distinguish finished from never-existed. Evidence bd-persist/campaign/. ACCEPTANCE: a test that asserts a terminal job is visible through /api/queue/v2 with its terminal state, failing on the current tree; UNKNOWN must not be reported as absent. |
| 667 | OPEN | SESSION-KEEPER-RE-LOGINS-SPEND-SITE-TOLERANCE-OUTSIDE-THE-LOGIN-CAP -- campaign finding D6. session_keeper re-logins roughly every 30 minutes and those attempts are not counted against the login cap, so a night of keeping a session alive can spend a site's tolerance invisibly. Separately the credential login logs cloakbrowser -- non-persistent while use_persistent_profile is True, so the log contradicts the configuration. Evidence bd-persist/campaign/. ACCEPTANCE: two assertions, each failing on the current tree -- every login attempt including a keeper re-login is counted against the cap, and the persistence log line matches the effective setting. EVIDENCE APPENDED 2026-09-06 (adjudication 20260906T160000Z): RELOGIN-CADENCE-UNEXPLAINED-BY-CONFIG: ~1 LOGIN PER 52 s AGAINST auto_relogin_interval_hours=12 / warmup_every=1800 -- Row 667 (OPEN) states the keeper 're-logins roughly every 30 minutes'; the measurement contradicts that premise in the same direction the row cares about and must be carried on it, not filed beside it. lane-test6.log:19 (2026-09-03T04:07:48Z, teenmegaworld): 15 'login: submitted via' events in the 13m17s window 03:51:10-04:04:27Z, four the campaign's own (matching login_budget.json to the second) and ELEVEN the service's own at 03:51:26 ... 04:03:45, mean ~52 s, with auto_relogin_enabled=True, auto_relogin_interval_hours=12, warmup_every=1800, auto_preemptive_relogin=False, predictive_relogin_enabled=False -- no configured interval explains it (register grep auto_relogin_interval / warmup_every: no row; app_kernel.py:511,540 defaults confirm 12 h / 1800 s). The eleven are UNATTRIBUTED between the session keeper and any other product login path (DONE.md lines 169-173 counted 135 login-submit lines that day; test6 evidence run_meta.json:keeper_logins_today_all_sites). This attribution is now decisive for closing 667: patch 667h landed at v3.66.1488 (landed.tsv:36, 'keeper re-logins count against the cap') but 667 is still OPEN -- if the 52 s logins ARE keeper re-logins, counting them against a five-per-night cap exhausts the cap within minutes and row 710's refusal counter becomes the operator's only view of it; if they are NOT keeper logins, 667h bounds nothing the lane measured. Either way the closure adjudication of 667 must replay this cadence against v3.66.1488+ before citing @1488. Also touches 722 (its structural point rests on the 30-minute figure) and 738 (single-process premise). |
| 675 | OPEN | scan-artifact-secrets-REPORTS-MINIFIED-JAVASCRIPT-AS-A-SECRET -- candidate row 6 from captures/455-run3/DONE.md. MEASURED: a captured page produced a kv_secret finding whose 8 matches were all minified JS assignments such as k=0 and k=null, caught by the generic key=value rule because _kv_key_is_secret(k) is true. A scanner that fires on ordinary page script makes a real finding harder to see. ACCEPTANCE: those 8 matches no longer fire, with a NEGATIVE CONTROL proving a genuine short-key secret still does. |
| 676 | OPEN | THE-CHALLENGE-DETECTOR-HALTS-A-CAPTURE-ON-A-WIDGET-FREE-PUBLIC-PAGE -- candidate row 7 from captures/455-run3/DONE.md. filthykings.com has zero anti-bot frames and types as login-wall, which yields operator_action_required and HALTS a capture where no human action exists. The login-wall regex firing on ordinary page text is already noted in the 454 survey; what is new is that it stops the run. CITE row 454 for the detector blindness rather than refiling it. ACCEPTANCE: a widget-free public page does not produce operator_action_required, with a negative control that a real login wall still does. |
| 687 | OPEN | installer-source-references-for-supported-linux-installation -- OPEN coverage residual, not implemented. The Windows application is retired (operator; O289/O292/O295). Restoring downloader.spec and repairing install_dev.bat are WITHDRAWN, not delivered and not landed, boarded or closed; preserve row687-night-20260909-cx-24 and its evidence. ACCEPTANCE: derive an explicitly documented, nonzero set of supported Linux application/fleet installation entry points from tracked scripts and current install/deploy references. Required repository-local source references must resolve to tracked files. Exclude retired Windows application installers, including install_dev.bat/install_windows.bat; include independent AI/tool bootstrap only when supported application setup invokes it. Optional, external and generated artifacts need not be tracked source. RED: a missing required local source fails; control: all required local sources present passes. Unresolved references or an empty entry-point set fail rather than pass. No retired Windows support restoration or script deletion follows. DEPENDENCY: none; residual implementation remains undispatched during drain. |
| 694 | CLOSED @1531 | A-PYTEST-TEST-LEAKS-AN-Xvfb-DISPLAY-SERVER-PAST-ITS-OWN-RUN-AND-BLOCKS-THE-NEXT-INSTALL -- MEASURED 2026-09-03 on two band hosts independently: test2 at 10.0.70.95 carries pid 582006 started 20:18Z the same day, and test3 at 10.0.70.80 carries pid 1193368 alive since 2026-09-02 21:00, each running 'Xvfb :99 -screen 0 1024x768x24'. Both have been REPARENTED TO INIT (ppid 1), and both hold a working directory that NO LONGER EXISTS -- a deleted pytest-of-mboyle/popen-gwNN/test_bdenv_does_not_clobber_a_0 temporary directory, which names the test that started them. Their environment carries BD_WORK_TREE, BD_CAPTURE_SIGNAL_REEXEC=1 and DISPLAY=:99, so the launch came through the capture re-exec path and not from a human. CONSEQUENCE, not theory: install_remote_teach.sh then fails with 'Server is already active for display 99' on 4 of 4 retries on BOTH hosts, so a leaked test fixture is refusing a real install on two machines, and on test3 it had been doing so for a day. SEE ALSO ROW 300, CLOSED @1297, the closest prior art and the INVERSE of this row: it closed PARALLEL-DISPLAY-TEST-OWNS-THE-DISPLAY-IT-KILLS by REMOVING a blanket 'never leave a server behind' kill that destroyed displays the test did not own. That is why negative control (3) below is mandatory rather than decorative -- the obvious fix for THIS row is the defect row 300 closed. This is the reclaim-gap shape rows 104 and 146 already record -- whatever starts a process is the only party that knows it must die -- on a new object: an X server rather than a pytest master or a temporary root. LAUNCHER NAMED BY MEASUREMENT 2026-09-03, so this row no longer asks for it: scripts/lib/system_deps.sh bd_start_display() DOES capture the pid -- xvfb_pid=$! -- and that is the correction a reviewer forced on this row before it shipped: the first draft said it never captures one, which is FALSE and would have sent a worker to add a capture that already exists. The true defect is narrower and worse. xvfb_pid is a LOCAL declared at the top of the function, and it is READ ON EXACTLY ONE PATH: the failure path, where a server that never became ready is killed after checking /proc/<pid>/comm looks like an X server. On the SUCCESS path the function prints the display and returns, the local goes out of scope, and THE PID IS DISCARDED -- so a caller that successfully started a server is handed a display string and no handle, and cannot reap what it started even if it wants to. capture.sh calls it at phase 5b and its EXIT trap has no display branch, which is the second half of the same gap. Under setsid there is a further trap for the fix: $! is the setsid child, not necessarily the Xvfb process, so a fix that publishes $! without resolving the actual server pid publishes the wrong number. The deleted pytest cwd was a LEAD and not the launcher: it names the test that happened to be running, which is why this row does not cite it as the cause. ACCEPTANCE: (1) bd_start_display() PUBLISHES the pid it already captures -- returning or exporting it on the SUCCESS path, where today the local simply goes out of scope -- and resolves it to the actual Xvfb process rather than to whatever setsid backgrounded; and capture.sh's EXIT trap gains the display branch that reaps it. (2) The teardown REAPS THE SERVER IT STARTED, asserted by exact count -- the test starts exactly one Xvfb and exactly one is gone afterwards, with the pid captured at spawn rather than discovered by pattern, since a pattern search for Xvfb :99 matches any other host user's server and the shell that wrote the search. (3) NEGATIVE CONTROL, mandatory: a PRE-EXISTING :99 server that the test did not start is still alive afterwards -- a teardown that kills every Xvfb on the box would pass a naive test and break every concurrent lane on a shared band host. (4) A band leaves no Xvfb behind: count the servers before and after a real band run and assert the delta is zero, with the count taken by a probe anchored on the invocation. Do not retire this by reaping the two current servers; the leak is the subject and the two processes are only its evidence. DEPENDENCY: none. TIER: T2. EVIDENCE APPENDED 2026-09-06 (adjudication 20260906T160000Z): provision_test_host.sh:569 and cloud-setup.sh:666 call bd_start_display in a subshell so BD_STARTED_DISPLAY_PID is lost (residual of 694, T1) -- VERIFIED on origin/main: scripts/provision_test_host.sh:569 'if DISPLAY_VALUE="$(bd_start_display :99)"; then', scripts/cloud-setup.sh:666 same shape, AND capture.sh:1622 '_cap_display="$(bd_start_display :99 2>/tmp/bd_display.err)"' -- ALL THREE callers on main invoke it in a command substitution, so it is not two extra callers but the whole population. That makes the 'or exporting it' branch of row 694's acceptance (1) (IMPROVEMENT_BACKLOG.main.md lines 721: 'returning or exporting it on the SUCCESS path') a dead branch: an exported variable set inside $( ) reaches no caller, including capture.sh, which the row does name. Fold into 694 as a sharpening: the pid must be RETURNED (stdout alongside the display, or a file handle), not exported; and tests/test_provision_test_host.py:1959-1966 pins the 'VALUE="$(bd_start_display :99)"' invocation shape, so the fix cannot change the call form without moving that gate. UNVERIFIED and deliberately not asserted: that the two installers SHOULD reap the :99 they start -- a provisioner starting a persistent display at step [6/9] (provision_test_host.sh:339) plausibly intends it to outlive the script, and cloud-setup.sh:655 says idempotency lives in bd_start_display; reaping there may be wrong. No other register row names bd_start_display, provision_test_host or cloud-setup (grep -> 694 only; :176/:278/:308/:405 are closed provisioning rows on other subjects). |
| 697 | OPEN | DEPLOY-HEALTH-GATE-REFUSES-A-RESTARTED-VAULT-HOST-BECAUSE-THE-STATE-READS-missing_credentials-RATHER-THAN-locked -- MEASURED 2026-09-04 while deploying v3.66.1466 to test2, test3 and test6: scripts/deploy.sh step 12 FAILED with GET /api/health returning HTTP 503 whose body did not prove the structured credential_vault_locked state, DEPLOY-test2 rc=1; bd-vault-unlock afterwards returned rc=0 and health then returned 200. Every service restart locks the vault because the key is memory-only, so this fires on EVERY deploy to a vault host. Raw evidence is bd-persist/deploy-1466-test2.log at lines 109 to 115 -- named in words rather than in the path-colon-line form on purpose, because that path is untracked and the doc anchor gate resolves tracked paths only. MECHANISM, CORRECTED FROM THE ONE FIRST REPORTED: deploy.sh ALREADY tolerates a locked vault -- its health step accepts a 503 whose body says degraded equals credential_vault_locked -- so the gate is not simply firing before the unlock; it HAS that branch and the body did not match it. bulk_downloader/app_health.py maps the credential state to that degraded string, and the operator observed credential_missing, i.e. state=missing_credentials, NOT locked. The open question is therefore why a restarted host with an initialized-but-locked vault reports missing_credentials, which is also a diagnostic collapsing two states that lead to opposite operator actions -- repair the credential, or unlock the vault -- the A7 shape. TIER: T1. HOOK-ABSENT CLAUSE, added 2026-09-05 on the deploy-health ruling of 14:49Z, which made the unlock hook OPTIONAL WITH A NAMED WARNING and put the seam and its opt-out in the deploy script, with the deploy owning the unlock and the fleet wrapper delegating. MEASURED ON origin/main 0700da23 BEFORE WRITING THIS: the script contains NO unlock hook, NO fleet-unlock environment seam and NO no-unlock flag -- three greps, zero hits. Its only unlock handling is the existing locked-vault diagnostic that names the Settings path. So the original acceptance below, which requires that THE SANCTIONED HOOK RUNS, cannot be satisfied on this tree by any correct cut: it demands a step that does not exist. THE CLAUSE THAT RESOLVES IT: when no hook is configured the gate does NOT fail for its absence and does NOT skip silently -- it emits a NAMED WARNING saying the hook is absent, and then decides on the vault state alone, so an initialized-but-locked vault still reads UNLOCK-PENDING and an uninitialized vault or a genuinely missing credential still FAILS. Absence of an optional hook is a stated condition, never a pass and never a failure. AND THE ABSENT CASE MUST STAY DISTINGUISHABLE FROM THE UNUSABLE ONE: a hook that is present but cannot be executed is NOT absent, and reporting it as absent sends the operator to install what is already there. Those two have opposite remedies and must not share a message -- the same collapse A7 records against bd-vault-unlock, and the reason the probed path is printed on every path rather than only on success. ORIGINAL ACCEPTANCE FOLLOWS -- an initialized-but-locked vault reads as UNLOCK-PENDING, the sanctioned unlock hook runs, the gate RE-PROBES within a stated bound, and only then decides; an UNINITIALIZED vault and a genuinely missing credential still FAIL; and if the fix touches the health mapping, locked, missing_credentials and uninitialized stay distinct. NEGATIVE CONTROLS: uninitialized still FAILs, an unrelated degraded string still FAILs, and the test asserts the DISTINCTIVE diagnostic rather than rc=1, which deploy.sh shares across many refusals. RED is taken against a synthesized health payload rather than by redeploying a live host, which A6 keeps out of a repository test. PROVENANCE: measured and briefed by integrator A as row-deployhealth and queued at bd-persist/rows-pending/deployhealth.md; FILED by integrator C, which owns register commits, so the row and the runtime patch that closes it land on different trains by design. DEPENDENCY: none. EVIDENCE APPENDED 2026-09-06 (adjudication 20260906T160000Z): Health reports vault_ready:true while is_unlocked:false / locked_no_references with 0 sites -- 'Misleading' is REFUTED by contract: bulk_downloader/app_health.py:344 sets vault_ready = credentials['ok'] is True, the field row 413 CLOSED @1379 introduced as the credential-serving half; and app_health.py:255-266 deliberately returns ok=True, state=locked_no_references when the vault is locked and the live configuration references zero credentials ('explicitly healthy instead of a misleading credential_vault_locked 503'). The same /api/health payload exposes credentials.is_unlocked=False and credentials.state, which is exactly what the agent read to answer the operator (transcript-part1.txt L3823-3836, 2026-09-05T17:48:22Z: test5/test2/test3 at v3.66.1498, ok=True vault_ready=True unlocked=False state=locked_no_references stored=21/594/34 sites=0). Not a new row. It IS material to OPEN row 697, whose acceptance says an initialized-but-locked vault reads UNLOCK-PENDING and whose text carries no zero-reference exemption (grep 'reference' in row 697 = 0 hits): a deploy gate reading vault_ready on a host with an empty sites_config passes green over a locked vault. BOUNDARY the 697 author must know: the locked_no_references arm is reached only when sites_config yields zero references (app_health.py:255), so on a real deploy with sites loaded it never fires -- the measured case is the post-'sites clear' cold start, not the deploy path 697 was filed on. Fold text: the three-host measurement, the arm's line numbers, and the note that the field name reads as vault state while its contract is credential-serving state (row 413's own wording 'vault readiness'). |
| 700 | OPEN | PAID-CAPTCHA-EGRESS-DISCLOSURE-IS-SHIPPED-BUT-UNGATED-AND-A-WORKER-ASSIGNED-ITS-TEST-A-ROW-ID-THAT-BELONGS-TO-ANOTHER-ROW -- MEASURED on origin/main 14522ebe: bulk_downloader/runner.py carries captcha_egress_disclosure_error, whose docstring states the contract -- a non-empty captcha_api_key turns solving on from the shipped default-off posture, moving an already-active key to a different provider changes its egress destination and is gated too, and the caller must remove the acknowledgement field before persistence. So the RUNTIME half is shipped. The GATE half is not: git ls-tree over origin/main finds no tests/test_row-N-captcha_egress_disclosure.py at all, so the behaviour is unpinned and a change to that function today breaks no test. WHY THIS ROW HAS AN ID AT ALL, which is the second defect: the gate exists in worker worktrees named for row 697, and its docstring opens Proposed row 697 -- a WORKER-ASSIGNED id, which A2 reserves to the integrator precisely because two workers filing concurrently choose the same number. This one collided with a real row: 697 is the deploy-health vault row filed at v3.66.1468 from an unrelated measurement, and the row654 filename gate RESOLVES test_row-N names against the register, so it would certify that false citation as correct. A gate that resolves a citation to the WRONG row is worse than one that cannot resolve it, because it reports OK. ACCEPTANCE: the gate lands under THIS row's id and pins the shipped contract through the canonical writer -- default-off to a configured third-party solver requires a one-shot acknowledgement, a rejected write leaves the solver off, and a provider change on an already-active key is gated the same way. RENAMING IT OUT OF THE test_row FAMILY IS FORBIDDEN: that launders it out of the filename gate's denominator, which is the population the row654 family exists to keep honest. The transform control that imports the module without judging the gate stays. ORDERING: this row must LAND BEFORE the rename lands, because the filename gate resolves against the register population on main -- a test named for an unfiled row is a dangling reference and refuses the tree. DEPENDENCY: none. |
| 701 | OPEN | THE-QUALITY-SCAN-SCORES-CANDIDATES-FROM-OTHER-SCENES-SO-THE-MIN-RESOLUTION-GATE-DECIDES-ON-A-FOREIGN-TILE -- HIGH, selection defect on shared code rather than a site template issue. MEASURED by campaign lane test3-a on africancasting 2026-09-04T08:01:54Z, dl=0/3; evidence under bd-persist/campaign/africancasting/ as record.json, page.png, page.scrubbed.html and verdict.md for both runs, plus SITE_REPORT.md, named in words rather than the path-colon-line form because those paths are untracked. Both masked screenshots show the CORRECT scene grounded at the top of the viewport with no Related-Videos content in it, yet the queue_terminal message for each download names OTHER scenes' titles with their own duration and quality tags. For n=2 the 240p item driving the refusal is titled for a DIFFERENT scene, so BD concluded the current scene's best quality was 240p and refused a scene it had rendered correctly. This is the 2026-08-29 Related-Videos shape reaching the QUALITY picker rather than the download picker, with the outcome inverted -- there it saved 5.1GB of the wrong scene under the right title, here it refuses the right scene. WHERE THE CODE STANDS, measured not assumed: detect.py sorts candidates by work, score and size, and row 388's own comment says a candidate that provably belongs to this page now OUTRANKS one that cannot be shown to and NOTHING IS DROPPED. So row 388 delivered a TIE-BREAK, not a SCOPE: foreign candidates are still admitted, still enter _all_candidates, and still reach the min-resolution gate in runner.py. _candidate_work_affinity returns 0 when it cannot tell, so a related-scene tile and a JS-driven control on the requested scene can both be 0 and the tie-break collapses to score and size. THAT LAST STEP IS A HYPOTHESIS about this page, marked as one deliberately, and the fixing cut must measure it rather than inherit it. ACCEPTANCE: the scored population is SCOPED to the requested scene's own container -- candidates that cannot be shown to belong are EXCLUDED, not merely outranked -- and a candidate whose affinity is UNKNOWN is refused rather than silently admitted; a RED fixture page carrying a related grid fails on the unmodified tree, with the fixture asserting its own preconditions that the grid exists, the foreign tiles carry quality text and the scene's own tier is present; the min-resolution refusal cannot be driven by an out-of-scope candidate and its Saw list names only in-scope ones; row 388's existing assertions stay GREEN as contracts; and exclusion and UNKNOWN-refusal each carry a bd-mutate spec that is CAUGHT with a transform control that ESCAPES. NOT PROVEN BY THE LANE and not to be claimed by the cut: that the defect is africancasting-specific, and that any challenge or Turnstile was involved -- none was observed and both scenes rendered for a logged-in session. DEPENDENCY: none. |
| 703 | CLOSED @1532 | THE-DNS-REBINDING-GUARD-IS-INSTALLED-AT-A-SMALL-MINORITY-OF-CLIENT-CONSTRUCTIONS-SO-THE-REST-RE-RESOLVE-AT-CONNECT -- T2, same subject as row 702 and a different failure: the guard exists, is correct, and is mostly not installed. _SSRFGuardedTransport in bulk_downloader/provider_resolve_impl/_common.py exists precisely to close the TOCTOU window -- its docstring says it reclassifies at connect time and pins the connection to a vetted IP literal, closing the DNS rebinding window between the pre-fetch resolution and httpcore's connect-time resolution. It is installed by the shared builder _make_default_http_get and _default_http_get; other sites construct httpx clients directly. THE POPULATION, RE-COUNTED BY THE INTEGRATOR ON THIS TREE AFTER THE AUTHOR'S OWN CORRECTION: 31 httpx.Client and AsyncClient constructions across 21 tracked files under bulk_downloader, and ZERO of those 31 lines carry transport= on the construction line itself, so a line grep cannot reconcile any install count and each site must be read. An earlier figure of twelve modules came from a survey truncated by its own head -12 and is void; that shape is filed separately as row 705. THOSE ARE OCCURRENCES, NOT ADJUDICATED CALL SITES: deciding which are in-scope production call sites is the fixing cut's own work. project-knowledge/MUTANT_INDEX.md claims the battery covers every production call site, so the documentation today asserts a coverage the tree does not have. ACCEPTANCE: every production call site is pinned through the guarded transport, directly or via the shared builder, with the in-scope population defined and asserted nonzero; a DRIFT GATE asserts spec-count equals call-site-count and DERIVES the call-site count FROM THE TREE rather than from the spec file it is checking, returning UNKNOWN rather than OK when it cannot measure the population; RED is adding an unpinned client construction and having the drift gate fail naming it; the MUTANT_INDEX claim is made true or corrected -- but that file is owned by the in-flight mutlib-ssrf work, so this cut READS it and PROPOSES the wording rather than editing a claim held elsewhere; and unpinning one call site and deleting the gate's tree-side denominator are each CAUGHT with a transform control that ESCAPES. DEPENDENCY: none, but its authored paths must not overlap the next mutlib-ssrf generation, which owns the ssrf delegation mutant specs, MUTANT_INDEX.md, tests/test_c1_ssrf_and_fileread.py and tests/test_v3_66_24_phase4_ssrf_hardening.py -- two patches sharing a path ride different trains. AMENDMENT (adjudicator-agreed 2026-09-04 22:0xZ, applied by the 20260906T160000Z adjudication): this row CLOSES ON THE LANDING OF THE rows687-703 LINEAGE, whose authored subject is the shared SSRF transport seam (ssrf_transport.py); until that lineage lands the row stays OPEN regardless of how many individual client constructions have been converted. EVIDENCE APPENDED 2026-09-06 (adjudication 20260906T160000Z): Row 703 amendment ('CLOSES ON THE LANDING OF THE rows687-703 LINEAGE ... ssrf_transport.py ... rowssrf-callers NOT-APPLICABLE') -- not applied, lineage not landed -- VERIFIED: row 703 (IMPROVEMENT_BACKLOG.main.md lines 730) contains none of 'rows687-703', 'ssrf_transport' or 'rowssrf-callers' (grep -> 0); `git ls-tree origin/main bulk_downloader/ssrf_transport.py tests/test_row703_ssrf_transport_is_installed_everywhere.py` is empty (the only ssrf_transport hits on main are the v3.66.303 CHANGELOG entry and tests/test_v3_66_303_ssrf_transport_guard.py, a different, older pin); landed.tsv through 1498 carries no 687-703 train; the proposal text lives only in bd-persist/questions-briefauthor-2.md lines 3-15 (Q1) and rows-to-file.md lines 972-980. So 703 stays OPEN (T2, SSRF subject; its ACCEPTANCE -- every production call site pinned through the guarded transport plus a tree-derived drift gate returning UNKNOWN when it cannot measure -- is unmet on main). The amendment is a pending text append to an existing row, not a new row: fold it into 703 at the next register cut, naming the lineage (gen c rows687-703c / rows687-703c-lu-cx-b; gen d rows687-703d; gen e per Q11) and the 21-file / 31-construction population so the closing cut is recognisable. The 'rowssrf-callers is NOT-APPLICABLE as a duplicate' ruling is a brief/dispatch matter (harness) and may be recorded in the same appended sentence or in HARNESS_BACKLOG.md, not as a row. Landing of any 687-703 generation remains UNVERIFIED (consistent with cross-reference finding 5). |
| 704 | OPEN | _looks_like_scene_url-ACCEPTS-PAGINATION-AND-SORT-LINKS-AS-SCENES-BECAUSE-slash-videos-slash-IS-A-SCENE-HINT -- MEDIUM: it has not yet produced a wrong download, and the only reason it has not is DOM ORDER, which is not a guarantee. MEASURED by the campaign lane on evilangel 2026-09-04: the post-fix run selected TWELVE candidates -- EIGHT real scene URLs of the form /en/video/studio/slug/N plus FOUR listing URLs, being /en/videos/sort/latest and the same with /page/2, /page/3 and /page/337. CAUSE, read in this tree: bulk_downloader/playlist_extractor.py _looks_like_scene_url tries _NON_SCENE_HINTS, then _LISTING_KEYWORDS, then -- absent a template url_patterns -- returns True for ANY _SCENE_URL_HINTS substring, and /videos/ is one of them. A pagination URL contains /videos/, and _LISTING_KEYWORDS has no /sort/ or /page/ member, so nothing rejects it. CONSEQUENCE TODAY: a lane taking the first three gets scenes only because the scenes lead in DOM order; on a page where pagination leads, a LISTING PAGE is submitted as a download -- the 2026-08-29 wrong-scene shape. The campaign test that records this deliberately does NOT hide it: it asserts the four pagination picks are still there, so a rule that silently dropped them from that test would conceal the defect rather than fix it. ACCEPTANCE: _looks_like_scene_url REFUSES the four measured listing URLs while the eight scene URLs from the same page still pass, RED first on the unmodified tree with the exact failure text recorded; a NEGATIVE CONTROL proves no URL the current rule accepts as a scene is newly rejected; and the listing rule is expressed as data the site templates can extend rather than as a hard-coded pair, since /sort/ and /page/ are two members of an open class. DEPENDENCY: none. EVIDENCE APPENDED 2026-09-06 (adjudication 20260906T160000Z): v2.1 fix 7 half-lands: classifier admits /videos/sites/<subsite> and /sort/.../page/N; discover picks in DOM order (no ranking) -- The product half is new EVIDENCE for row 704, not a new row. REPRODUCED HERE on d7176ce8 (== origin/main): _looks_like_scene_url('https://www.dogfartnetwork.com/en/videos/sites/blackmeatwhitefeet') -> True and '.../en/videos/sort/latest/page/2' -> True, while the real scene '.../en/video/blacksonblondes/In-Awe-Of-Its-Size/291195' -> True. CAUSE read in the tree: playlist_extractor.py:82-88 _LISTING_KEYWORDS carries '/site/' but not '/sites/', so the '/videos/' hint at :96 wins for the 10 network sub-site category pages -- the same mechanism 704 names ('/videos/ is one of them ... _LISTING_KEYWORDS has no /sort/ or /page/ member'). Row 704's text (IMPROVEMENT_BACKLOG.main.md lines 731) already anticipates this: 'the listing rule is expressed as data the site templates can extend rather than as a hard-coded pair, since /sort/ and /page/ are two members of an open class' -- /videos/sites/<x> is the third measured member and must be added to 704's RED population (dogfart, 2 listing pages, 13 of 21 selected not scenes, picks 1-2 both category pages, rows-to-file.md lines 306-327; evidence bd-persist/campaign/dogfart/v21-run/discovery_report.json) and to its negative control, so a fix that only rejects /sort/ and /page/ cannot close 704. The 'DOM order is the only reason no wrong download has happened' premise of 704 is also now stronger: on dogfart the nav precedes the grid on every page, so picks 1 and 2 ARE category pages (rows-to-file.md lines 317-322). The other half -- discover selects in DOM encounter order with no ranking (rows-to-file.md lines 314-316, 327-330) and the OPEN QUESTION whether the harness borrowed a follow-this-link rule as a this-is-a-scene rule (:331-334) -- is campaign-v2/bd-campaign.py (untracked) and goes to HARNESS_BACKLOG.md, where greps for 'DOM order', 'ranking' and 'title-bearing' are 0. Row 719 is cross-referenced only for the train rule (704 and 719 must not share a train). EVIDENCE APPENDED 2026-09-06 (adjudication 20260906T160000Z): _looks_like_scene_url wrong in BOTH directions on real member sites (nubilefilms /video/gallery True; ultrafilms /content/item/<id>-<slug> False) -- six-URL RED proposed -- Not a third row: the register deliberately holds ONE row per direction of this predicate (719's text: 'FILED SEPARATELY FROM ROW 704 DELIBERATELY ... Opposite directions of one classifier'), and the six URLs split cleanly into those two directions. Verified on origin/main d7176ce8 bulk_downloader/playlist_extractor.py: the nubilefilms half (/video/gallery, /video/gallery/website/71, /video/shorts -> True) is admitted because '/video/' is a member of _SCENE_URL_HINTS (:96) and _LISTING_KEYWORDS (:82-88) has no /gallery or /shorts member -- exactly 704's mechanism ('returns True for ANY _SCENE_URL_HINTS substring' with an incomplete listing list), and 704's acceptance already says the listing rule must be 'data the site templates can extend ... since /sort/ and /page/ are two members of an open class'; /gallery and /shorts are two more measured members and should be added to 704's measured population and its RED (assert the three nubilefilms listing URLs REFUSE while /video/watch/255524/<slug> still passes). The ultrafilms half (/members/content/item/82144c7c-<slug> -> False) folds into 719 (under-select on an authenticated members page; 754 @1497 added only '/film/','/films/' at :101 and no /content/item token exists), so 719's RED gains a second ground-truth shape and a second site; 719's fallback-to-nav mechanism is exactly what rows-to-file.md lines 455-459 measured (`/members/home/whatsnew`, `/members/content`). Register greps confirm no other home: refinementList 0, /shorts 0, content/item only closed rows 374/375, /gallery only closed 374/412. The remaining proposal at rows-to-file.md lines 478-482 (candidates emitted in DOM order, `--n N` takes the first N, no ranking, no UNKNOWN on zero accepts) is bd-campaign.py `discover` behaviour and goes to the harness backlog, not either row. Train rule stands: 704 and 719 still must not share a train. EVIDENCE APPENDED 2026-09-06 (adjudication 20260906T160000Z): A 4-segment listing route (/videos/page/2/foo) escapes the collection-route filter (residual of the scene-classifier gen-d/e work, T1) -- On origin/main d7176ce8 there is no _collection_route_segments (git show origin/main:bulk_downloader/playlist_extractor.py / grep -> 0; only the /page/N pagination regex at :429-430), so the 3-segment-only judgement exists solely in the UNLANDED gen-d/e candidates (landed.tsv through 1498 names no scene-classifier train). On main the same URL is admitted by exactly the defect row 704 already records: '/videos/ is one of them' and '/en/videos/sort/latest ... /page/2, /page/3 and /page/337' pass because _LISTING_KEYWORDS has no /sort/ or /page/ member (IMPROVEMENT_BACKLOG.main.md lines 731). Fold as an acceptance sharpening of 704: the listing rule must refuse a listing word in the second segment at ANY route depth (4+ segments included, no identity proof in later segments), with the 3-segment behaviour unchanged. Do not attach to 719 -- the register's train-conflict rule keeps 704 and 719 on separate trains over playlist_extractor.py (:746 DEPENDENCY). |
| 705 | OPEN | A-PUBLISHED-DENOMINATOR-TAKEN-FROM-A-PAGED-SURVEY-READS-AS-COMPLETE-AND-IS-NOT -- MEDIUM. It mints no false PASS in a gate; it puts wrong numbers into rows, briefs and reports, which is where the NEXT cut inherits its premise. MEASURED 2026-09-04 on its own instance: the per-module survey of httpx client constructions behind row 703 was run through a head -12, and twelve modules were published as THE POPULATION in a register row body and in a codex brief. The tree carries 31 constructions across 21 files. The nine dropped modules were runner.py, runner_extractors.py, runner_manual.py, runner_telemetry.py, session_keeper.py, tier_probe.py, vpn_leak_tests.py and the mullvad and pia vpn provider modules. THIS IS THE FIRST PAGER-TRUNCATION INSTANCE FILED, and that narrowness is deliberate -- an earlier draft called it the third, which would have been the error it describes. Its nearest relative is row 232, CLOSED @1225, where two tools scanned frontend/src with an rglob and judged a PRODUCT property over a population that also held Vitest specs; that row measured 22 such sites with 14 applying no population rule at all, and it failed in OPPOSITE directions from one defect. Same class, unexamined population; different truncating mechanism, wrong-glob versus pager. WHAT CONTAINED IT WAS NOT REVIEW LUCK: row 703's acceptance already required the count to be derived from the tree at check time and to return UNKNOWN when the population cannot be measured, so the fixing cut could not inherit the prose figure. A wrong number in a row is survivable; the same number inside a gate is not. A FOURTH INSTANCE, FOUND AFTER THIS ROW WAS DRAFTED, AND IT IS INSIDE A TOOL RATHER THAN IN PROSE: toolchain/bin/bd-freshcheck renders its broken-anchor detail as a join over the FIRST SIX entries. A bounce brief and BOTH lens verdicts then said six broken backlog anchors; the real count is TEN, established by an independent census and confirmed by subtracting the tool's own resolved-over-total figures. THE PRECISE MECHANISM MATTERS AND IS NOT WHAT IT FIRST LOOKS LIKE: the same detail line ALSO prints the true totals, so the tool publishes the honest number and truncates only the LIST beside it, with no ellipsis and no and-N-more. Three readers took the list length as the count. THE SEVERITY DISTINCTION, kept deliberately so this is not filed as more than it is: bd-freshcheck REFUSED CORRECTLY at rc=1 on all ten and minted no green. What it truncated was the SIZE OF A CORRECT REFUSAL -- which is exactly what a reader plans the fix around, so a worker who trusts the list fixes six of ten and believes it is done. ACCEPTANCE: a gate over the tracked documents that PUBLISH counts refuses a numeric claim stated as a population without a stated derivation, with its own publication population defined in the cut and asserted nonzero; RED names a real instance rather than a synthetic one; a NEGATIVE CONTROL proves a document that DOES carry its derivation passes; and the gate returns UNKNOWN when the document population cannot be measured. EXPLICITLY NOT CLAIMED, NARROWED AFTER THE FOURTH INSTANCE: that any shipped gate MINTS A FALSE PASS from a paged denominator. Three of the four instances are prose; the fourth is a display cap over a refusal that was CORRECT. A paged denominator actually DECIDING a pass or fail verdict would be categorically worse than everything described here and should be filed loudly on its own rather than folded into this row. ALSO NOT CLAIMED: that the display cap is the only one -- the population of tools that truncate a list beside a true total is itself unmeasured, and measuring it is part of this row's work rather than an assumption behind it. DEPENDENCY: none. |
| 707 | OPEN | A-CONCURRENT-CLEANUP-REPLACES-A-MUTATION-TREE-MID-BATTERY-SO-VERDICTS-DESCRIBE-A-SOURCE-NO-LONGER-BEING-MUTATED -- HIGH for EVIDENCE INTEGRITY rather than for product behaviour. REPORTED INDEPENDENTLY BY TWO IMPLEMENTERS WITHIN ONE HOUR on 2026-09-04, neither aware of the other: one watched a peer's loop remove and rsync the shared session scratchpad's mutation work directory from that peer's own worktree and saw its own mutation tree REPLACED MID-BATTERY, with foreign mutants and a 1072-line base deploy script appearing under its run; the other reported its detached scratch tree wiped twice, bounded between two verdicts, cause UNKNOWN. WHY THIS IS HIGH: a battery whose tree is swapped mid-run reports CAUGHT and ESCAPED about a source it is NO LONGER MUTATING, and those verdicts are the evidence a patch BOARDS on. It fails silently in BOTH directions -- an escape reported as caught boards a hole, a catch reported as escaped bounces a correct patch -- and nothing in the tooling reported the substitution. The first victim noticed only because a foreign FILENAME appeared. THE TRACKED HALF IS THIS ROW: toolchain/bin/bd-mutate takes --work defaulting to the repository root and neither derives a private work directory from the run's own identity nor re-verifies its subject between verdicts. The untracked half -- the agent cleanup loop that deleted a peer's directory -- is filed as harness item H41, because a register row cannot gate work outside the tracked tree. NEITHER HALF SUBSUMES THE OTHER: a private work directory stops the collision, an ownership check stops the deletion, and a run can still be corrupted by a tool that never notices its subject changed. THIRD SIGHTING, 2026-09-04 09:2xZ, AND THE FIRST REPRODUCIBLE ONE, added after this row was filed: row667g's implementer had the bulk_downloader/ DIRECTORY of its bd-mutate work copy DELETED mid-battery under the session scratchpad, TWICE, and moving the copy to a private temporary path fixed it. That is stronger than the two sightings above, which were each observed once: this one REPRODUCES, and the victim is a whole SOURCE DIRECTORY rather than a swapped tree, so the deleter removes a path it does not own rather than merely reusing a name someone else chose. TWO THINGS THE SIGHTING DOES NOT SETTLE AND WHICH ARE RECORDED AS OPEN RATHER THAN INFERRED: nobody chased the deleting process, so WHICH process does it is UNKNOWN; and all three sightings share ONE session scratchpad, so the evidence cannot discriminate between this row's tracked half and the harness half filed as H41. A whole-directory deletion POINTS AT a cleanup loop rather than at a name collision, but pointing is not measuring and the fixing cut must not inherit that as a premise. WHAT IT DOES CHANGE: the acceptance below already demands a RED reproducing the interference between two concurrent runs on unmodified tooling, and this is the first evidence that such a RED is achievable at all -- which moves the row from hard-to-reproduce to reproducible-once-observed. ACCEPTANCE: work directories are collision-proof, derived from the run's own identity; the subject's identity is RE-VERIFIED before each verdict and a substitution returns UNKNOWN rather than a verdict; RED reproduces the interference between two concurrent runs on the unmodified tooling; cleanup is restricted to paths whose ownership the run PROVED; and a single-run negative control shows the change did not simply make every battery refuse. EXPLICITLY NOT CLAIMED: that any patch boarded on 2026-09-04 carries a corrupted verdict. Both affected workers re-ran privately and their published numbers come from those runs. Whether an EARLIER battery was affected is UNMEASURED, which is UNKNOWN and not clean; a cut that can bound that window should say so. DEPENDENCY: none. |
| 709 | OPEN | A-STATE-VARIABLE-SEEDED-WITH-ONE-OF-THE-VERDICT-VALUES-IT-IS-LATER-COMPARED-AGAINST -- T2. A variable whose seed is drawn from the same vocabulary as its outcomes cannot distinguish never-ran from ran-and-said-this. MEASURED INSTANCE: CLOAK_STATE took the values OK, UNKNOWN and ABSENT from a requirements check's exit codes -- rc 0, rc 2 meaning could not evaluate, and rc 1 -- and was SEEDED with UNKNOWN. But UNKNOWN is a MEASURED OUTCOME there, so rc-2-was-measured and the-disposition-never-ran were THE SAME VALUE, and a per-state pin could not fail against a seed impersonating a verdict. NINE MUTANTS WERE WRITTEN BEFORE ANYONE NOTICED ONE OF THEM COULD NOT GO RED -- that is the cost signature of this shape: it does not break a gate, it makes one unable to fail, and the effort is spent before the impossibility is visible. Fixed at that site by seeding UNMEASURED, a value no measurement can produce. ACCEPTANCE: a TREE GATE that finds a state variable seeded with a literal equal to one of the verdict values it is later compared against, over a STATED population -- scripts/ and toolchain/bin to start, with any exclusion given its reason -- asserting a nonzero denominator and returning UNKNOWN rather than OK when the population cannot be measured. RED requires RESTORING THE DEFECT deliberately, because the one known instance is already fixed: with the UNKNOWN seed put back the gate names that seed with its file and line and FAILS, and GREEN with UNMEASURED. Mutation: deleting the seed check, and narrowing the population to a single file, are each CAUGHT with a transform control that ESCAPES. EXPLICITLY NOT CLAIMED: that any OTHER seed in the tree has this shape. ONE instance is measured; the POPULATION IS UNMEASURED, and measuring it is this row's first job rather than an assumption behind it -- filing it as a family would be the same error one size larger, and rows 705 and 706 on the preceding train exist because that error was made twice in one session. DEPENDENCY: the known instance's fix rides another candidate, so a fixing cut must confirm which tree carries the seed before restoring it for RED; a RED that depends on an unlanded cut is not RED on the correct base. |
| 710 | CLOSED @1529 | THE-LOGIN-CAP-REFUSING-ITSELF-IS-EXCLUDED-FROM-THE-FAILURE-RATE-AND-MUST-THEREFORE-BE-COUNTED-SOMEWHERE-ELSE -- T1, and FILED PRE-EMPTIVELY ON A RULING rather than on a defect, so the condition attached to that ruling is machine-visible BEFORE the patch it governs lands rather than after someone remembers to check. OPERATOR RULING 2026-09-04 13:0xZ, via the adjudicator: an in-flight patch adds auto_relogin_refused as a session event and DELIBERATELY EXCLUDES it from bulk_downloader/db.py _SESSION_FAILURE_EVENTS, so a login cap refusing its own re-login does not inflate a site's cockpit failure rate; the heartbeat_fail that precedes it is still counted. That exclusion is ACCEPTED and is pinned by a test in that patch, which makes it a CONTRACT either way -- which is exactly why it needed a ruling before shipping rather than after. THE RULING CARRIED A CONDITION AND THIS ROW IS THAT CONDITION: the refusal count must be VISIBLE AS ITS OWN COUNTER in the cockpit or status payload. A number that is excluded from one total and present in no other is not a decision to under-count, it is a decision to STOP COUNTING, and the two are indistinguishable to every later reader of the failure rate. STATE MEASURED ON THIS TREE, not assumed: _SESSION_FAILURE_EVENTS is currently heartbeat_fail, auto_relogin_fail and needs_takeover, and _SESSION_SUCCESS_EVENTS is login, heartbeat_ok and auto_relogin_ok -- so auto_relogin_refused belongs to NEITHER set today and the patch that introduces it decides where it lands. ACCEPTANCE: after that patch lands, the refusal appears as its own named counter in the payload an operator reads, with a test asserting a refusal increments THAT counter and does NOT increment the failure rate, and a negative control asserting a genuine auto_relogin_fail still increments the failure rate. If the patch already surfaces it, this row CLOSES on that evidence with no further work -- a row filed to hold a condition open is closed by the condition being met, not by a cut. If it does not, this row is the T1 follow-up and NOT a bounce of the patch, per the ruling. DEPENDENCY: the patch introducing auto_relogin_refused; this row cannot be worked before it lands, because the counter it asks for does not yet have a value to hold. |
| 716 | OPEN | MIRROR-CHAIN-ABANDONED-ON-RESOURCE-MISMATCH-SO-THE-REMAINING-MIRRORS-ARE-NEVER-TRIED -- T2. No wrong bytes are written; a download that could have succeeded from another mirror is refused to needs_review instead. MEASURED ON origin/main AND RE-READ BY THE INTEGRATOR: bulk_downloader/constants.py declares _HTTPDownloadFailed and _StagingUnavailable as SEPARATE direct subclasses of Exception -- the second is not a subclass of the first. The mirror loop in bulk_downloader/runner_transport.py catches _HTTPDownloadFailed and CONTINUES to the next mirror, while _StagingUnavailable is caught OUTSIDE that loop and returns straight to needs_review. So a resource mismatch abandons the whole chain. The routing is currently PINNED by two assertions in tests/test_part_staging_collision.py -- one asserting the error type and one asserting exactly one needs_review update -- so those pins are contracts a fix must deliberately revise rather than trip over. THE DISTINCTION THE FIX TURNS ON AND WHICH MUST NOT BE FLATTENED: the handler's own comment documents a DELIBERATE refusal for the PART-STAGING COLLISION case -- another live download owns the .part this transfer would stage into, and falling through to the Playwright path would write the browser's bytes into the very destination the refusal protects. THAT CASE MUST KEEP REFUSING. What must change is the RESOURCE-MISMATCH case, where the bytes are the same work and only the host differs. THE DISCRIMINATOR ALREADY EXISTS ONE LAYER UP AND IS DISCARDED AT THE BOUNDARY, which the integrator found by reading the RAISE sites rather than the handler: two of the three raise sites catch THREE distinct upstream exceptions from staging_claim -- StagingClaimedByAnotherJob, StagingResourceMismatch and StagingUnavailable -- in ONE except tuple and re-raise them all as _StagingUnavailable(str(e)), keeping only the message text. StagingResourceMismatch is exactly the mirror-retryable case and StagingClaimedByAnotherJob is exactly the case that must keep refusing: they arrive at that boundary as DIFFERENT TYPES and leave as the same one. This is the A7 collapsing-diagnostic shape at its most literal -- two diagnoses leading to OPPOSITE actions, flattened into one type with only detail text between them. ACCEPTANCE: a host-only mirror swap of THE SAME BYTES is attempted before needs_review, so a resource mismatch continues the loop rather than returning; the part-staging-collision refusal is UNCHANGED, same exception, same needs_review, no Playwright fallback; BOTH behaviours are proven in the SAME round, and a fix that makes the collision case retry is refused; and the two cases are DISTINGUISHABLE at the raise site, which per the finding above means STOPPING THE DISCARD rather than inventing a discriminator. The two existing pins are updated deliberately, with the new expectation stated, not deleted. DEPENDENCY: none. The w4-cdn403e round does NOT cover this -- that brief is a rebase plus a fixture arity fix. |
| 717 | OPEN | THE-CUT-QUALITY-POLICY-RESOLVES-TO-AN-OPERATOR-HARNESS-FILE-THAT-NO-TRACKED-DOCUMENT-NAMES -- T1 DOCS. Nothing in the code is wrong; the dependency is simply undocumented, so it survives exactly as long as the hosts that already carry the file. DEPENDENCY FIRST, BECAUSE THIS ROW READS AS FALSE WITHOUT IT: the policy file is NOT on origin/main today. Re-verified by the integrator -- git ls-files finds no toolchain cut-quality policy, git grep finds no active_checker anywhere in the tracked tree, and NEITHER BD_CUT_QUALITY_VALIDATOR nor BD_CUT_QUALITY_MATRIX appears on main at all. The policy ARRIVES WITH THE ROW 656 CUT, measured in that candidate's worktree. So a fixing cut that goes looking for the file on main will correctly find nothing, and this row is NOT WORKABLE until 656 lands. That is a deferral with a named condition rather than a stale row. MEASURED IN THE ROW 656 CANDIDATE: its active_checker block declares a schema, a sha256 and a resolver environment variable, and the file it resolves to is the operator-harness checker named here BY NAME ALONE -- deliberately without a path-colon-line citation, because the doc anchor gate resolves those against TRACKED paths only and a harness path in that form refuses the whole tree. The declared sha256 MATCHES the file, verified by hashing it. BD_CUT_QUALITY_MATRIX appears ZERO times even in the candidate, so if that variable is real its only home is the operator's environment; the row says which of the two it is rather than assuming. docs/repo/FRESH_HOST_BRINGUP.md matches cut_quality or cut-acceptance ZERO times. THE FLEET FACT IS ALREADY CLOSED MECHANICALLY -- the file was replicated to every fleet host by sha -- and that is precisely why the row exists: a completed ACTION is not a durable fact, and it protects only the hosts that were up when it ran. ACCEPTANCE: docs/repo/FRESH_HOST_BRINGUP.md names the checker file, its sha256, the environment variable or variables that resolve it, and WHERE A FRESH HOST OBTAINS IT -- the last being the part that makes the document useful rather than descriptive; and BD_CUT_QUALITY_MATRIX is either documented alongside if it is real or recorded as nonexistent, rather than documented into existence. DEPENDENCY: the row 656 cut must land first. |
| 719 | OPEN | THE-SCENE-URL-CLASSIFIER-SELECTS-ZERO-REAL-SCENES-ON-AN-AUTHENTICATED-MEMBERS-PAGE-AND-THE-FALLBACK-PICKS-ACCOUNT-NAV -- HIGH. FILED SEPARATELY FROM ROW 704 DELIBERATELY, and the reason is the whole point: 704 is _looks_like_scene_url OVER-ACCEPTING, where pagination and sort links pass as scenes. This is the SAME FUNCTION UNDER-SELECTING -- it accepts NOTHING on a page full of real scenes. Opposite directions of one classifier, and a fix aimed at either alone leaves the other. They are cross-referenced rather than merged because their REDs are different pages and their acceptance criteria do not overlap. WHY THIS INSTANCE IS DECISIVE AND EARLIER ONES WERE NOT: every previous zero-scene result could be dismissed as a logged-out render. This one cannot. wowgirls, fresh vault, cold login 2026-09-04 14:30Z: login SETTLED in 21.0 seconds and discovery landed on the REAL MEMBERS HOME, not a login page. MEASURED ON THAT AUTHENTICATED PAGE: 482 links, 211 distinct, 201 structurally eligible; discovery_report.json records selected_by_product_scene_rule ZERO and selected_by_title_fallback 159. The product classifier accepted nothing, so the generic title-bearing-anchor fallback ran and picked ACCOUNT NAV -- /my/activity, /my/favorites, /my/comments and a /goto/site/ link. THE PAGE DEMONSTRABLY CONTAINED SCENES: the same report holds 17 real scene URLs of the form /film/<id>/<slug>, and the link-shape census shows a films index. So the classifier rejected 17 valid scene URLs while the fallback accepted four pages that are not scenes at all. A WORKAROUND IS PROVEN AND IS NOT THE FIX: a per-site url_patterns of the film shape yields 36 hits and 0 junk. That is a per-site patch for a classifier defect, and it is why 0 of 32 sites having url_patterns produced junk picks all night -- with url_patterns empty the product classifier is the ONLY selector, and it selects nothing on a real members page. ACCEPTANCE: _looks_like_scene_url accepts the /film/<id>/<slug> ground truth from this capture, with a RED over the captured discovery_report population asserting a nonzero selected_by_product_scene_rule and naming the 17; the title-bearing fallback REFUSES account and interstitial paths outright, because a fallback that returns nav pages is WORSE than one returning nothing -- it reports state OK; and the fallback's own selections are counted separately from the classifier's so a future zero cannot hide behind a nonzero total. NEGATIVE CONTROL: row 704's four pagination URLs still REFUSE, proving the two directions were fixed together rather than traded. DEPENDENCY: shares bulk_downloader/playlist_extractor.py with row 704's cut, so the two must not ride the same train. EVIDENCE APPENDED 2026-09-06 (adjudication 20260906T160000Z): Row 719's fallback clauses target the campaign driver, not tracked product code -- VERIFIED: `git grep -i "'/my///goto//title-bearing/title_fallback/selected_by_title" origin/main -- bulk_downloader` returns 0 hits; the title-bearing fallback and its selected_by_title_fallback counter live in the HARNESS driver on test2 (~/campaign/bd-campaign.py md5 ca289c766f2ef161321d739096872173, lines 2947, 2966, 3019, 3044, 3057, 3072), and the local bd-persist copy (Sep 3) predates it. Row 719 (IMPROVEMENT_BACKLOG.main.md lines 746) acceptance clauses 2-3 ('the title-bearing fallback REFUSES account and interstitial paths outright ... counted separately') therefore cannot close via a landed cut under the register rule, and belong in HARNESS_BACKLOG.md alongside H43/H54. The row is NOT a duplicate of 754: 754 CLOSED @1497 (:781) added the /film/ and /films/ tokens with RED TestSceneUrlFilter::test_film_paths_are_scene_urls in tests/test_v3_43_75_bundle.py, which satisfies 719's ground-truth shape but not its stated RED ('over the captured discovery_report population asserting a nonzero selected_by_product_scene_rule and naming the 17' -- a harness counter) nor its NEGATIVE CONTROL (row 704's four pagination URLs still REFUSE -- 704 is still OPEN, :731). Amend 719: (a) move clauses 2-3 to HARNESS_BACKLOG.md with a pointer; (b) record that the classifier clause is PARTIALLY met by 754 @1497 and name the residual as the 704 negative control plus a product-side RED over the 17 /film/<id>/<slug> URLs from bd-persist/campaign/wowgirls/fresh-20260904T1430Z/discovery_report.json. Keep the 704/719 path-conflict rule (playlist_extractor.py). EVIDENCE APPENDED 2026-09-06 (adjudication 20260906T160000Z): FR-3: scene-URL classifier hint vocabulary + generic fallback must refuse listing/filter/account paths -- Row 719 (OPEN) is already the under-select half of this classifier and row 704 (OPEN) the over-accept half; the candidate is new evidence, not a new defect. The candidate's evidence is PARTLY WRONG: at d7176ce8 bulk_downloader/playlist_extractor.py:95-105 _SCENE_URL_HINTS already carries '/scenes/' (:97) and '/film/' '/films/' (:100), so the teenmegaworld '/scenes/<slug>_vids.html' claim is REFUTED by the tree; only ultrafilms '/members/content/item/<id>-<slug>' (transcript:4233, :4246) genuinely matches no hint and no _LISTING_KEYWORDS/_NON_SCENE_HINTS entry (:82-88, :108-116), so _looks_like_scene_url (:169-205) returns False for it -- fold that URL shape into 719's ground-truth list. The 'generic title fallback' that picked /my/activity, /goto/site/, ?sortBy and ?refinementList is NOT product code: it is bd-campaign.py lines 3037-3046 (harness), which calls the product classifier at :3022-3036 and falls back to title-bearing anchors when it accepts nothing; 719's own ACCEPTANCE already names that fallback, and the ?sortBy/?refinementList/sort-link over-match belongs to 704's 'listing rule expressed as data' clause (transcript:5002, :5036, :5060 measured the dfxtra sort link). Path rule stands: 704 and 719 must not share a train (playlist_extractor.py). EVIDENCE APPENDED 2026-09-06 (adjudication 20260906T160000Z): SCENE-URL-CLASSIFIER-EVIDENCE-EXTENDS-ROWS-704-AND-719-members-content-item-PHOTO-SET-DOMINATED-LISTINGS-AND-SORT-LINKS -- Not a new row, as the candidate says. Row 754 (CLOSED @1497) added only /film/ and /films/; playlist_extractor.py:95-106 _SCENE_URL_HINTS at d7176ce8 still has nothing that matches ultrafilms' /members/content/item/<8hex>-<slug>, so with url_patterns empty the classifier selects zero scenes there (SITE_RUNBOOK.md lines 113-119) -- exactly row 719's under-selection shape on a path 754 does not cover. The ultrafilms CONTENT listing mixing 1883 photo sets against 2 movies (61-69) belongs with 719's 'fallback picks non-scenes' half (closed 381 is res_score, a different defect). The over-acceptance evidence goes to row 704 in the same fold note: dfxtra a[href*='/video'] matched /en/videos?sortBy=all_scenes_latest_desc (286-292) and bang's /videos?by=trending&is4k=1 (1161) are QUERY-STRING sort links, i.e. members of 704's open class beyond /sort/ and /page/, which its data-driven listing rule must admit. url_patterns are re.search'ed over the full URL (playlist_extractor.py:193-200), so newsensations' gallery\.php\?id=[0-9]+&type=vids (1116) is usable today. Because 704 and 719 must not share a train (719's own DEPENDENCY), the /members/content/item/ corpus rides 719 and the query-sort corpus rides 704; rowId 719 is given as the primary fold target. EVIDENCE APPENDED 2026-09-06 (adjudication 20260906T160000Z): Scene URL classifier vocabulary gap: /members/content/item/ not recognised -- Verified in tree at d7176ce8: bulk_downloader/playlist_extractor.py:95-104 _SCENE_URL_HINTS is the full tuple video(s)/scene(s)/watch/episode(s)/movie(s)/film(s)/clip(s)/play/view_video.php; grep for '/content/' and '/item/' = 0 hits, so a /members/content/item/<slug> scene URL fails _looks_like_scene_url absent a template url_patterns. Row 754 CLOSED @1497 added only /film/ and /films/, so it does not cover this shape. This is the UNDER-SELECTION direction row 719 owns ('the SAME FUNCTION UNDER-SELECTING -- it accepts NOTHING on a page full of real scenes'), now on a second site: 719's acceptance names the wowgirls /film/<id>/<slug> ground truth (since satisfied by 754) but its remaining clauses -- the title fallback refusing account and interstitial paths, fallback selections counted separately, and the 704 negative control -- are still open, and a token-list fix that adds /content/item/ would be the third per-token patch. Provenance stated so it is not overread: transcript-part1.txt L4451 (2026-09-05T18:08:40Z) is a code reading plus a pre-emptive per-site url_patterns workaround (ultrafilms), NOT a measured zero-selection from a discovery_report; the agent's own words were 'worth a row if it shows up on a third site'. Rows 374/375 (CLOSED) already record the ultrafilms scene shape /members/content/item/<slug> as ground truth, and no tracked ultrafilms template with url_patterns exists (grep hits only tests/corpus and fixtures). Fold as: a second measured site shape for 719's cut, with the acceptance amended so the scene rule is expressed as data templates can extend (the same request row 704 makes for the listing rule). Train rule from 719 stands: shares playlist_extractor.py with 704, must not ride the same train. |
| 720 | OPEN | TEMPLATE-ONBOARDING-ALWAYS-CAPTURES-THE-LOGIN-PAGE-SO-NO-SITE-CAN-EVER-LEARN-A-DOWNLOAD-SELECTOR -- HIGH, and it is a PRECONDITION FAILURE sitting underneath several other symptoms rather than a cosmetic issue. MEASURED IN THE TREE: bulk_downloader/app.py _site_primary_url resolves in the order login_url, start_url, base_url, url, homepage, member_url, site_url -- login_url FIRST. api_template_onboard launches its capture at that url, so ANY site declaring a login_url -- all 32 on this fleet -- has its template captured against the LOGIN FORM instead of a content page. OBSERVED wowgirls 2026-09-04 14:49Z: the capture opened the site's auth host and its own HUD reported host auth.<site>, 20 requests, 1 media, dom ok, then no media trigger resolved yet -- click the player. THERE IS NO PLAYER ON A LOGIN PAGE, so the capture can never resolve a media trigger and the template can never learn download selectors. The draft and the cloak profile are keyed to the login host too, which compounds it: the artifacts that would be reused are named for the wrong host. IMPACT: this is why every site on this fleet shows No reviewed template and No manual-login profile, and therefore why manual_use_persistent_profile has nothing to point at. ACCEPTANCE: onboarding captures a CONTENT url -- prefer a listing or crawler-listing url, or a known scene from the url fingerprint's known path prefixes -- and falls back to login_url ONLY when no content url exists, saying in the run record which it used and why; OR the POST body accepts an explicit url, which the teach endpoint already does. RED: a site config carrying both a login_url and a content url onboards against the CONTENT url, failing on the unmodified tree. NEGATIVE CONTROL: a site with ONLY a login_url still onboards rather than refusing, so the fix does not strand login-only sites. NOT CLAIMED: that changing the order alone is sufficient -- _site_primary_url has other callers, and the cut must enumerate them before reordering a shared resolver rather than assuming onboarding is the only consumer. EVIDENCE APPENDED 2026-09-06 (adjudication 20260906T160000Z): FEAT-H: onboarding captures a CONTENT page and learns login+nav+download end-to-end, emitting the per-site JSON this campaign hand-wrote -- Row 720 (OPEN) already states the premise and the acceptance -- onboarding captures a CONTENT url (listing, crawler-listing or a known scene from the url fingerprint) and its title names the outcome FEAT-H wants, learning a download selector. FEAT-H adds the remainder after the content page is reached (drive login or an imported session, navigate to a scene, record login+nav+download and emit selectors.json/navigation.json as a reviewable template), which extends 720 rather than opening a new subject; the adapter-in-shipped-code and live-state-to-fixture halves are rows 673 and 674 (OPEN). Adjudication owed before scoping: row 755 CLOSED @1497 corrected _site_primary_url's content-URL selection across the five seams (app_sites_teach.py:141/:297/:410, app_sites_id_core.py:288, app_sites.py:135), which is the exact mechanism 720 names, so 720 must be re-derived against 1497 and may be PARTIAL with this fold as its named remainder. The campaign's 23 verified selectors/ files are the corpus a RED for that remainder can use (BACKLOG_CROSSREF_REPORT.md lines 18-19). EVIDENCE APPENDED 2026-09-06 (adjudication 20260906T160000Z): BD-DOWNLOAD-OUTCOME-DIFFERS-BETWEEN-TWO-BYTE-IDENTICAL-NF-MEDIA-SITES -- The premise is refuted at the product level by the run record. bd-persist/campaign/nubiles/nubiles-run.log:13-15 shows add_url at 08:13:37Z and needs_review at 08:13:38Z -- ONE second, before any 'Opening page' -- while nubilefilms (driver-nubilefilms.log:15-17) went pending -> running 'Opening page' -> done over four minutes. The logged message text ('Click Take over on this row, then complete the download in the popup browser') is the runner.py:1736 variant, i.e. the START-TIME first-run gate at runner.py:1708-1719: auto_teach_first_run (default True) and no learned.download trigger_selectors/row_selectors and no applied_template -> mint needs_review without opening the page. So the divergence is a per-site CONFIG difference, not byte-identical input producing different ranking. The ranker-fragility hypothesis (row 380 residual) is REFUTED: nubiles/1/verdict.md section (c) shows candidates/inspect ACCEPTED the content4.nubiles.net signed-href winner over 40 candidates on that very page. UNVERIFIED: that nubiles' config lacked applied_template -- inferred from the gate condition and timing; the campaign JSON carries no applied_template key and sites_config was not opened. Row 720 is the precondition that keeps learned.download empty on this fleet ('every site on this fleet shows No reviewed template'), so this is its downstream symptom and belongs there as evidence. If the fixing cut finds nubiles DID carry applied_template and the gate fired anyway, that is a NEW defect at runner.py:1718 and should be filed then. |
| 722 | OPEN | ALL-CAMPAIGN-TRAFFIC-EGRESSES-FROM-ONE-RESIDENTIAL-IP-AND-THE-LOGIN-CAP-DOES-NOT-BOUND-THE-VOLUME -- HIGH, FLEET and ENVIRONMENT rather than a product defect, so this row exists to carry an OPERATOR DECISION rather than to be patched. MEASURED on a runner host: no tunnel or wireguard interface, no proxy keys in the app config, the vpn config endpoint reporting zero configured and zero live tunnels, and a default route straight out its interface. Public egress measured as a single residential address. VOLUME AGAINST THAT ONE ADDRESS IN ONE NIGHT, counted by the campaign driver: 470 login submits and 847 keepalive context launches across all sites, with three lanes running concurrently. NOTE the egress address CHANGED mid-session to a different residential carrier, so any per-address claim is time-bounded and the row does not pin one. WHY IT MATTERS AND WHAT WAS FALSIFIED AGAINST IT: the competing browser-fingerprint explanation was TESTED AND DISPROVED -- installing real Google Chrome changed nothing -- which rules the browser out and leaves egress standing. One site's reCAPTCHA was an INVISIBLE badge on a read-only probe and had escalated to a manual image grid roughly forty minutes later, after the night's login volume; escalation under load is characteristic of IP reputation rather than browser identity. WITHDRAWN AS EVIDENCE FOR THIS ROW, and recorded so it is not re-used: the /login-abused landing is an AGE GATE, per row 721, not an abuse response. What still stands here is the captcha escalation under load and the raw volume. THE STRUCTURAL POINT: the session keeper re-logins roughly every thirty minutes per site per host across 32 sites, and those attempts do NOT count against the campaign's five-per-night cap -- so the cap does not bound the thing that actually causes lockouts, which row 667 already names from the other direction. ACCEPTANCE: an operator decision is recorded in OPERATOR_DECISIONS.md on distinct per-host egress and on whether the keeper is rate-limited or counted against the cap; if egress stays shared, the decision says so explicitly with its reason, because an unrecorded decision to do nothing is indistinguishable from having missed the finding. DEPENDENCY: row 667, which names the keeper's uncapped re-logins as a defect in the cap rather than in the keeper. AMENDMENT (20260906T160000Z adjudication): the campaign measured SPLIT EGRESS -- the CLI path, the browser path and the CF path did not share one address -- not the single residential IP this row's title asserts, so no per-address volume claim in this row is load-bearing. The row 474 signed-URL consequence folds here. EVIDENCE APPENDED 2026-09-06 (adjudication 20260906T160000Z): Operator action #2 (marked RESOLVED): consistent egress / proxy or VPN for the IP-signed CDN downloads -- No new row and no outstanding operator action beyond the record row 722 already demands, but the bundle's measurement belongs on 722 as evidence: (a) SITE_RUNBOOK.md lines 1796-1803 shows test2's egress was SPLIT (CLI 174.178.147.233 vs browser/CF-path 75.165.3.230), which is what produced the 474s on the IP-signed CDN URLs, and became uniform at 104.234.212.140 across CLI, cloak and real Chrome at ~2026-09-06 10:00Z -- this CORRECTS 722's 'one residential IP' history (722 itself already notes the address changed mid-session) rather than refuting the row; (b) the CAUSE of the change is UNVERIFIED in the bundle ('simply became uniform', SITE_RUNBOOK.md lines 1803); (c) the squid-proxy authorization at asks-and-answers.txt ASK 18 'Test squid proxy first' (03:47:19Z) was never exercised -- SITE_RUNBOOK.md lines 1584-1585 asked for it, 00-INDEX.md lines 34 then says 'no proxy needed'; (d) 00-INDEX.md lines 34 '(already resolved) ... no proxy needed' is precisely the unrecorded do-nothing that 722's ACCEPTANCE forbids ('an unrecorded decision to do nothing is indistinguishable from having missed the finding' -- IMPROVEMENT_BACKLOG.main.md lines 749), so the OPERATOR_DECISIONS.md record on per-host egress and keeper rate-limiting is still owed and 722 stays OPEN. Register grep 'squid/proxy': no other row. EVIDENCE APPENDED 2026-09-06 (adjudication 20260906T160000Z): Four guards in the row-722 shape gate (tests/test_row722_operator_decision.py) have no direct fixture (residual of 722, T1) -- tests/test_row722_operator_decision.py is not on origin/main (git ls-tree -r origin/main tests / grep row722 -> empty) and OPERATOR_DECISIONS.md is not tracked (git ls-tree / grep -i OPERATOR_DECISIONS -> empty), so the four unfixtured guards (_REQUIRED_QUESTIONS:72, missing-Decision :75, empty-Decision :78, tracked-file rc :103 per rows-to-file.md lines 1018-1022) belong to the unlanded row722-fixb/fixc cut, which rows-to-file.md itself says was amended 04:2xZ to move the sibling items into row722-fixc. Nothing on main to attach a new row to; the fixtures are the in-flight cut's own acceptance and ride with it. Row 722 (IMPROVEMENT_BACKLOG.main.md lines 749) is an operator-decision row whose dependency 667 has landed (@1488, landed.tsv:36) but whose OPERATOR_DECISIONS.md record is still owed (asks-and-answers.txt lines 159-166 has only 'Test squid proxy first'), so the row stays OPEN regardless. Severity low; register grep 'test_row722_operator_decision/_REQUIRED_QUESTIONS' -> 0. EVIDENCE APPENDED 2026-09-06 (adjudication 20260906T160000Z): CORRECTION: evilangel /login-abused IS a real IP block behind the age gate -- rows 721/722 (and H43) carry the superseded reading -- VERIFIED: the screenshot bd-persist/campaign/evilangel/ip-blocked-behind-agegate-20260904T152xZ.png (50371 bytes, mtime 2026-09-04 15:24:12Z) shows the evilangel page behind the age gate reading 'Your IP was blocked! Please try later or call the customer support ...' with the Gamma Billing footer; bd-persist/campaign/capability-survey-20260904.log line 4 records verdict=PARKED-IP-BLOCKED for evilangel 2e13b0df; transcript test2-agent-transcript.txt lines 2205-2210 (2026-09-04T15:24:12Z 'File IP-block correction and Turnstile detector row') appended the correction to bd-persist/rows-to-file.md ONLY -- it never reached the register (grep 'IP was blocked' IMPROVEMENT_BACKLOG.main.md -> 0). Row 722 (:749) explicitly 'WITHDRAWN AS EVIDENCE FOR THIS ROW ... the /login-abused landing is an AGE GATE, per row 721' -- that withdrawal is now refuted by measurement and must be reversed: the vendor states the IP block in its own words, which is direct evidence for shared-egress reputation. This is evidence for an OPERATOR-DECISION row (722 asks for an OPERATOR_DECISIONS.md record), not a code fix, so it folds rather than files. SECONDARY amendments owed: row 721 (:748) 'MEASURED: ... /login-abused is the vendor's own AGE GATE, not an abuse response' needs a correction note that on evilangel the gate is the outer shell of an abuse page (dfxtra may be gate-only, UNVERIFIED) -- 721's mechanism (ENTER affordance, recognition by CONTENT not URL shape) is unchanged and its 'a page that is genuinely an abuse response is NOT clicked through' negative control now has a live example; HARNESS_BACKLOG.md lines 269 H43's 'false positive' verdict is wrong for evilangel and right only on reasoning (URL shape cannot tell gate from block) -> harness amendment, not register. Note: bd-campaign.py (local :712-716) already clicks 'button:has-text("Enter")' as a harness generic fallback; the product half remains 721. EVIDENCE APPENDED 2026-09-06 (adjudication 20260906T160000Z): All campaign traffic egresses from ONE residential IP, no VPN; keeper re-logins are the uncapped volume driver (HIGH) -- Row 722 (IMPROVEMENT_BACKLOG.main.md lines 749) IS this finding, filed as the operator-decision carrier with DEPENDENCY 667, and its acceptance is still unmet: grep of /home/mboyle/bd-persist/OPERATOR_DECISIONS.md for '722' -> 0 hits, and its egress/proxy/vpn hits (:66, :269-275 row 439 HLS confinement, :940 bd-proxy) are unrelated, so no per-host-egress or keeper-cap decision is recorded. Two pieces of evidence postdate the row and should be folded in rather than re-filed: (a) rows-to-file.md lines 915-931 CORRECTION -- clicking the evilangel age gate's ENTER reveals 'Your IP was blocked! Please try later ...' behind it, so 722's clause 'WITHDRAWN AS EVIDENCE ... the /login-abused landing is an AGE GATE, per row 721, not an abuse response' is REVERSED for evilangel: the site states the block in its own words and evilangel's AWAITING-HUMAN at 15:11Z is a downstream symptom; (b) BACKLOG_CROSSREF_REPORT.md lines 22 extends it to SPLIT egress (browser vs CLI vs CF path differ), named there as the root cause of the bangbros/brazzers/naughtyamerica 474 downloads, and SITE_RUNBOOK.md lines 1262-1264 shows a vendor binding signed download URLs to the fetching host's egress (ip= param), so egress identity is now a download-correctness dependency as well as a reputation one. Per 722's own convention no address is repeated here; cite those lines. DEPENDENCY 667 has a landed fix (@1488 trainB32, landed.tsv:36) but 667 is still OPEN in the register, so 722's dependency line stands until 667 is adjudicated. Remains OPERATOR-DECISION in nature: the row exists precisely to hold that decision, hence FOLD rather than a new row or a bare DUPLICATE. EVIDENCE APPENDED 2026-09-06 (adjudication 20260906T160000Z): SIGNED-CDN-URL-HTTP-403 -- FOLD INTO ROW 722 AS THE MEASURED CONSEQUENCE OF SPLIT EGRESS -- No register row carries the 403 measurement (grep 403/cdn/ip=/signed in IMPROVEMENT_BACKLOG.main.md: only rows 92/93 CSRF 403s, 662 path-signed redaction, 120/453 signed JWPlayer -- none this shape). Measured at bd-persist/campaign/lane-test6.log:20 (DEFECT id=SIGNED-CDN-URL-HTTP-403, teenmegaworld, n=2,3,5,7 '[permanent] HTTP failed ... HTTP 403', URLs signed in the path with key=, end=, ip=<client IP>, transfer_mode=http) and :22 (SITE DONE, 4 PERMANENT HTTP 403). CAMPAIGN_V2_PLAN.md lines 10 and :33 record the cause as UNKNOWN (relogin invalidating signatures vs ip= binding vs expiry) and hand it to 'Codex w4-cdn403', which is absent from landed.tsv (only w4-loginsession/qualitypick/vaultcwd/loginapib/realchromeb/phpsessidb landed, landed.tsv:10-17) -- so the 09-03 instance was never root-caused. The 09-06 bundle attributes the sibling 474/403 class to split egress and reports it resolved once egress was consistent (PM-handoff-20260906T131616Z/00-INDEX.md lines 28-34; campaign/FABLE_TAKEOVER_PROMPT.md lines 60 'if egress ever splits again, downloads 474/403'). Row 722 is the egress OPERATOR-DECISION row and already records that the egress address CHANGED mid-session (IMPROVEMENT_BACKLOG.main.md lines 749), which is exactly what invalidates an ip=-bound signature; the 403 count is its measured consequence and belongs in its text as evidence for the decision, not as a new row. A product-side row (detect ip= in a path-signed URL and refuse/fetch in-session when egress differs) would require a measured split on the SAME host at the SAME time, which no cited log shows -- UNVERIFIED, so not filed. EVIDENCE APPENDED 2026-09-06 (adjudication 20260906T160000Z): OPERATOR-ACTION: egress IP reputation (Comcast 174.178.147.233) caused IP blocks; later Brightspeed 75.165.3.230 -- The register already carries this as row 722 (OPEN, 'FLEET and ENVIRONMENT ... exists to carry an OPERATOR DECISION'), which already records the mid-session carrier change and the 470 submits / 847 keepalive counts (transcript-part1.txt L1334-1381, 14:15:26Z public IP 174.178.147.233 AS7922 Comcast, no tun/wg interface). It is a FOLD rather than a plain duplicate because row 722's text explicitly WITHDRAWS the /login-abused evidence ('WITHDRAWN AS EVIDENCE FOR THIS ROW ... the /login-abused landing is an AGE GATE, per row 721'), while rows-to-file.md lines 915-936 ('CORRECTION (supersedes the age-gate row)') records a later measurement that reverses that withdrawal for evilangel: clicking ENTER on the age gate reveals 'Your IP was blocked! Please try later...' -- screenshot /home/mboyle/bd-persist/campaign/evilangel/ip-blocked-behind-agegate-20260904T152xZ.png (50371 B, Sep 4 15:24; exists). That is new evidence for 722 (the site states the block in its own words) and it also narrows row 721's premise (the age gate is the OUTER SHELL of the abuse page on evilangel; dfxtra's may still be only a gate). The decision row 722 asks for is still owed: asks-and-answers.txt records only 'Test squid proxy first'. No new row; no code. EVIDENCE APPENDED 2026-09-06 (adjudication 20260906T160000Z): SPLIT-EGRESS-CLI-VS-BROWSER-VS-CF-PATH-BROKE-EVERY-IP-SIGNED-CDN-URL-AND-THE-EXTRACT-THEN-REFETCH-PATH-CANNOT-SEE-IT -- Row 722 is the register's operator-decision carrier for egress ('FLEET and ENVIRONMENT rather than a product defect ... exists to carry an OPERATOR DECISION') and this is its newest measurement, so it folds there rather than standing as a row or being dropped. Evidence: three IP-signed downloads failed on the same root cause -- bangbros/brazzers Aylo download-private 474 and naughtyamerica ht-secure.naughtycdn.com ip= 474 -- because the signing path saw one test2 address and the direct CDN saw another (SITE_RUNBOOK.md lines 1396-1406, 1522-1536); squid 10.0.30.121:3128 was DOWN so the operator's 'Test squid proxy first' answer (asks-and-answers.txt lines 159-166) could not be executed (1509-1520); and it SELF-RESOLVED when test2's egress became uniform at a new address with no proxy or VPN applied (1796-1803). Cause of the on-host CLI-vs-browser split is UNVERIFIED. No product-side assertion is made: per-site proxy already applies to both the browser context (runner_browser.py:246) and the transport (runner_transport.py:297), and 'nothing verifies the two share an egress' has no failing case now that the environment is uniform -- filing it would be a hypothesis, not a defect. The decision 722 asks for (distinct per-host egress; keeper re-logins counted against the cap, dep 667 landed @1488) is still owed in OPERATOR_DECISIONS.md; this measurement should be recorded beside it. EVIDENCE APPENDED 2026-09-06 (adjudication 20260906T160000Z): BD has no in-session capture path for Aylo one-time/egress-bound download (474) -- Two halves. The CAPTURE-PATH half is the same defect as candidate 1 and lands in that single NEW row (no second row implied). The EGRESS half is new measured evidence for row 722, which BACKLOG_CROSSREF_REPORT.md lines 20 already extends ('it is actually SPLIT egress (browser vs CLI vs CF-path differ) -> the root cause of bangbros/brazzers/naughtyamerica 474 downloads'): transcript-part3.txt lines 2511 (07:27Z) the browser's OWN first fetch of an unconsumed window.open-captured URL returned 474 under split egress, 474 body 'Unauthorized -- the request requires user authentication' (transcript-part3.txt lines 2503-2515); transcript-part3.txt lines 3177 (12:50Z) the identical capture returned HTTP 206 then a full 6.67 GB 2160p file once browser and fetch shared one egress, and brazzers followed at 12:54Z. That is a measurement 722 should carry -- the row is explicitly an OPERATOR-DECISION row ('exists to carry an OPERATOR DECISION rather than to be patched') and the only recorded answer is 'Test squid proxy first' (asks-and-answers.txt lines 159-162), so the OPERATOR_DECISIONS.md record 722 asks for is still owed. The 'one-time' mechanism the candidate title names was RETRACTED by the transcript itself (part3:2503 'CORRECTION to the earlier one-time-request-bound theory') and must not enter the row. The CDP attempts at 01:42-02:00Z (transcript-part3.txt lines 1421-1545) are ad-hoc scratch scripts (bb_walk3-5.py, bb_auto.py, bb_final.py in PM-handoff/scratchpad-tools/) and are not product evidence beyond what candidate 1's row states. |
| 723 | OPEN | use_real_chrome-IS-DECLARED-ON-HOSTS-WITH-NO-REAL-CHROME-AND-THE-FALLBACK-IS-SILENT-TO-THE-RUN-RECORD -- ENVIRONMENT, and PARTLY RESOLVED ALREADY, filed so the remainder does not vanish with the fix. MEASURED: a persistent launch with channel chrome failed with the Chromium distribution chrome is not found, followed by retrying without system Chrome channel. The site declares real Chrome and stealth, and the host silently falls back to bundled Chromium. RESOLVED ON ONE HOST 2026-09-04 13:5xZ by installing Google Chrome through Playwright, verified on BOTH failing paths, launch and launch_persistent_context. The distinction that makes the fix reproducible: use_real_chrome sets Playwright channel chrome, which only GOOGLE CHROME satisfies -- chromium does not. STILL OPEN ON THE FLEET: one runner reports Chrome ABSENT with the same silent fallback, and two more were unreachable at the time of the check, so the population is at least one and at most three and is UNMEASURED beyond that. A HYPOTHESIS THIS ROW CARRIED AND WHICH WAS DISPROVED, kept because the disproof is the useful part: it originally speculated that the bundled-Chromium fingerprint was why one site's reCAPTCHA hardened to a manual challenge. Retested end to end under real Chrome with the fallback error absent from the log: the cold login still SETTLED, discovery still landed on the login page, and the run still returned a captcha challenge. The Chrome gap is real and worth closing; it is NOT the captcha cause, and row 722 is where that evidence went. ACCEPTANCE: every runner host either has Google Chrome or does not declare use_real_chrome, with the population derived from the fleet at check time rather than from this row; AND the silent fallback is surfaced in the SITE'S RUN RECORD rather than only in the service log, because a capability that degrades without telling the record is indistinguishable from one that worked. DEPENDENCY: none. EVIDENCE APPENDED 2026-09-06 (adjudication 20260906T160000Z): Silent fallback when Playwright channel='chrome' has no Google Chrome installed -- Row 723 OPEN 'use_real_chrome-IS-DECLARED-ON-HOSTS-WITH-NO-REAL-CHROME-AND-THE-FALLBACK-IS-SILENT-TO-THE-RUN-RECORD' is this exact defect, down to the same 2026-09-04 13:5xZ test2 install and the same channel-chrome-needs-Google-Chrome distinction (transcript-part1.txt L612-632: submit.py:665, manual.py:98, replay.py:257 set channel='chrome'; Chromium would not satisfy it). Cite 723, not the candidate's 736 (736 is the deploy-time cloak degradation summary, a different surface). FOLD rather than plain DUPLICATE because the transcript carries new measurement toward 723's first acceptance clause: 723 left the fleet population 'at least one and at most three ... UNMEASURED'; the transcript installs Google Chrome 152.0.7977.82 on test3 at 2026-09-05T17:31:56Z (L3296-3300) and records 'Chrome 152 installed everywhere' across test5/test2/test3 at L3805, so all three measured runner hosts now have it. That does NOT close 723: its acceptance derives the population 'from the fleet at check time rather than from this row', so three hosts prove nothing about a fourth, and the second clause -- the silent fallback surfaced in the SITE'S RUN RECORD rather than only in the service log -- is untouched by installing binaries. Fold text: the three-host install record with timestamps, and the note that the missing-binary condition is still invisible to the run record. Row 696 CLOSED @1465 covered the adjacent 'use_real_chrome never fires' plumbing and is not this. PARTIAL LANDING @1507, and the row STAYS OPEN. WHAT LANDED: cut/row723-chrome-fallback-run-record, PASS by bd-lens-L2 on re-verify. bulk_downloader/cloak.py gains a bounded thread-safe degradation ledger and its persistent_context channel_fallback arm records BOTH outcomes; bulk_downloader/runner_browser.py BrowserMixin gains _record_channel_fallback, so the silent Chrome channel fallback now reaches the SITE'S RUN RECORD rather than only the service log -- that is the SECOND clause of this row's acceptance, and it is now met. Gated by tests/test_row723_chrome_channel_fallback_reaches_run_record.py, which drives the channel_fallback arm itself for both recovered=True and recovered=False, with tests/mutants/row723_channel_fallback_run_record.json and a control in its own file, tests/mutants/row723_channel_fallback_run_record_transform_control.json. EXACTLY WHAT IS STILL OWED, and it is the ONLY thing owed: acceptance clause 1, the Chrome population derived FROM THE FLEET AT CHECK TIME rather than from this row. The worker's own DONE.md declares that clause UNMEASURED, so it is not claimed here. NEW IMPORT EDGES declared by the worker and re-frozen by the integrator at assembly per the 23:01:50Z ruling as amended 23:26:58Z: tests/test_row723_chrome_channel_fallback_reaches_run_record.py -> bulk_downloader/cloak.py and -> bulk_downloader/runner_browser.py. Both are TEST-to-module; no product module gained an edge, and the baseline diff equals the union of the declared edges exactly, with no removals. Row 720 is NOT in this patch and stays open. ALSO OWED, from the same DONE.md and not filed as rows here: submit.py and manual.py keep an inline stderr-only retry that cannot be wired without a site_id those functions do not carry, and _surface_pending_channel_fallbacks is the drain point for them. |
| 724 | OPEN | THE-CUT-PREFLIGHT-BANDCHECK-PARSER-HARD-CODES-ZERO-FAILURES-SO-UNSAFE-TARGETS-SUMMARISE-AS-NOTHING-WRONG -- T1, pre-existing. VERIFIED IN THE TREE BY THE INTEGRATOR: toolchain/bin/bd-cut-preflight's p_bandcheck counts ok lines with one regex and BAD lines -- UNSAFE, NOT A FILE, MISSING -- with another, refuses to report when both are zero, and then returns ok+bad as the total, ok+bad AGAIN in the PASSED position, and a LITERAL ZERO in the FAILED position. So a band whose every target is UNSAFE summarises as N band target(s) with nothing wrong. The parser counts the failures and then throws the count away at the moment it reports it. WHAT IS AND IS NOT AFFECTED, because the grade turns on it: the tool's VERDICT survives -- the exit-code mapping is registered separately and correctly, so the caller still reads FAIL from bd-bandcheck's own exit code. What is destroyed is the COUNT AN OPERATOR READS BESIDE THE VERDICT, which is what a reader plans the fix around. That is why this is T1 and not HIGH, and a row that overstated it would be scheduled against more urgent work. IT IS ALSO THE SHAPE THIS REGISTER ALREADY CARRIES: row 705's fourth instance is bd-freshcheck truncating a broken-anchor LIST beside a true total, so a reader took the list length as the count and fixed six of ten. Same class -- the number a reader plans around cannot represent the failure -- different mechanism: there a display cap, here a hard-coded zero. AND IT IS ONE LAYER INSIDE bd-cut-preflight'S OWN CONTRACT, which refuses a check that exits 0 having examined nothing; a parser that can NEVER report a FAIL is that same defect wearing the tool's own uniform. ACCEPTANCE: p_bandcheck reports the measured failure count in the FAILED position and counts only ok lines in the PASSED position, not ok+bad. RED on the unmodified tree: a bandcheck transcript containing at least one UNSAFE, one NOT A FILE and one MISSING line parses today to a shape carrying zero failures, and to three failures after the fix -- assert the tuple, not merely that something changed. NEGATIVE CONTROL: an all-ok transcript still reports zero failures, so the fix does not invert the defect. Mutation: restoring the literal zero, and folding bad back into the passed position, are each CAUGHT with a transform control that ESCAPES. DEPENDENCY: none. Related to row 677, which exists to make those exit codes trustworthy -- this is the reader that throws them away. PARTIAL LANDING @1507, and the row STAYS OPEN -- adjudicator law 23:06Z, integrator disposition (a) 23:1xZ. WHAT LANDED AND IS DONE: toolchain/bin/bd-cut-preflight p_bandcheck now reports the measured failure count in the FAILED position and counts only ok lines in the PASSED position, gated by tests/test_row724_preflight_bandcheck_counts_failures.py, which asserts the RED tuple EXACTLY as (3, 0, 3, 'band target(s)') on a transcript carrying one UNSAFE, one NOT A FILE and one MISSING, and the all-ok NEGATIVE CONTROL EXACTLY as (2, 2, 0, 'band target(s)'). Both mutants this row names are CAUGHT: tests/mutants/row724_unverifiable_slot_zero.json restores the literal zero, tests/mutants/row724_bandcheck_receipt.json folds bad back into the passed position, and a third, tests/mutants/row724_empty_target_denominator.json, keeps EMPTY TARGET in the BAD denominator. EXACTLY WHAT IS STILL OWED, and it is the ONLY thing owed: a transform control spec file of its own whose SUBJECT is toolchain/bin/bd-cut-preflight, banded on a test that IMPORTS bd-cut-preflight without driving a refusal, and which must ESCAPE. WHY: the cut that landed the above carried exactly one escaping control, tests/mutants/row747_transform_control.json, whose subject is toolchain/bin/bd-band -- row 747's file, not this row's -- so nothing yet proves the three CAUGHTs above are assertion failures at the named arms rather than an import or parse break in bd-cut-preflight. Model it on tests/mutants/row747_transform_control.json: zero-width anchor on the decision, insertion as `new`, so tests/test_row357_mutant_anchors_are_not_fragile.py does not refuse it as value-bearing. |
| 725 | OPEN | SSRF-A-MULTI-A-RECORD-HOST-IS-ACCEPTED-BECAUSE-THE-FIRST-UNSAFE-ANSWER-CLAIMS-THE-EXEMPTION -- MEASURED on rowssrf-loopback-cx-b: a host resolving to both a loopback and a link-local address is ACCEPTED on the base AND on the boarded patch -- the first unsafe answer wins, loopback takes the exemption, and the fetch then RE-RESOLVES UNPINNED. Row 702 closed the substring bypass; it did not close this. ACCEPTANCE: every address in a multi-answer resolution is classified and the connection is pinned to the vetted literal, with a RED over a two-answer fixture. TIER: T3 security. DEPENDENCY ADDED 2026-09-05: ROW 728's GENERATION c. This row's fixing cut authors bulk_downloader/provider_resolve_impl/_common.py and tests/test_rowssrf_loopback_reason_is_structured.py, and 728c authors the SAME TWO FILES -- it is the cut converting that test's bare census-size literals to named-set equality, which row 752 records. Two patches sharing a path ride different trains and are never merged by hand, so this cut is taken ON TOP OF 728c rather than beside it. Recorded in the row rather than left to assembly: a path conflict found at assembly costs whichever train re-cuts, and with three assemblers alternating that is not predictably the one that caused it. FOLLOW-UP OF rows 702 and 703. Filed by integrator C from the panel's NOTE-class shape findings under the 20:3xZ scope ruling; the panel measured each and deliberately did NOT charge it as an escape. |
| 726 | OPEN | SSRF-NO-DRIFT-GATE-DERIVES-THE-STRUCTURED-REASON-CONSUMER-POPULATION-FROM-THE-TREE -- Nothing derives the consumer population, so a later consumer can reintroduce an English text decision. Correct today, unprotected tomorrow. Row 702 does not demand such a gate and row 703 is where it belongs. ACCEPTANCE: a gate derives every consumer of the structured reason from the tree and fails when one decides on text, returning UNKNOWN when the population cannot be measured. TIER: T2. FOLLOW-UP OF rows 702 and 703. Filed by integrator C from the panel's NOTE-class shape findings under the 20:3xZ scope ruling; the panel measured each and deliberately did NOT charge it as an escape. |
| 727 | OPEN | _HostSafetyReason-RAISES-TypeError-ON-copy-deepcopy-AND-pickle -- A str.__getnewargs__ mismatch. LATENT: no consumer serializes it today and only SSRFBlocked holds the object, so nothing is broken now. ACCEPTANCE: the reason object survives copy, deepcopy and pickle, with a test asserting round-trip equality. TIER: T1 latent. FOLLOW-UP OF row 702. Filed by integrator C from the panel's NOTE-class shape findings under the 20:3xZ scope ruling; the panel measured each and deliberately did NOT charge it as an escape. AMENDMENT (three-way ruling 2026-09-04 23:05Z, applied by the 20260906T160000Z adjudication): this row STAYS OPEN until the 727+730 split lands; it is not closed by the handler change alone. STATUS AT v3.66.1511 (train F-03, measured by the integrator on the assembled tree, not inherited from either car's DONE.md): THE 727 HALF IS SHIPPED. bulk_downloader/provider_resolve_impl/_common.py gives _HostSafetyMessage a __getnewargs__ returning (str(self), self.code), and tests/test_v3_66_24_phase4_ssrf_hardening.py test_host_safety_message_round_trip_preserves_text_and_reason asserts copy, deepcopy and pickle all preserve the type, the text and the code, over two reasons, with two negative controls -- one proving the code is not merely the text and one proving the enum's own protocol is unchanged. THE ROW STAYS OPEN ANYWAY, because the split is not complete: bulk_downloader/app_scrape_listing.py api_scrape_listing still catches only httpx.HTTPError and that file contains ZERO occurrences of SSRFBlocked, while the SAME probe finds SSRFBlocked in five bulk_downloader modules, so this is a measured absence and not a narrow grep. What remains for closure is row 730's handler alone; do NOT re-implement the round-trip fix, it is on main from v3.66.1511. |
| 728 | OPEN | THE-THREE-urllib-SEAMS-RESOLVE-TWICE-AND-PIN-NEITHER-RESOLUTION -- check_url resolves and urlopen resolves AGAIN -- two getaddrinfo per request -- and the open reached a link-local address after the check passed on a public one. httpx pins; the three urllib guards do not. CONDITIONAL RESOLVED @v3.66.1503: the SSRF work landed at v3.66.1500 WITHOUT the pinning, and v3.66.1500's own changelog says so -- row 725's second clause, that the connection is pinned to the vetted IP literal rather than to a name that can resolve again, was explicitly excluded there and named as this row's subject. So the condition is met and this row is LIVE. It is also now SPLIT OUT of that train and NEEDS ITS OWN CUT, because the shared pinned-urllib-redirect implementation spans all three urllib seams and is not a delta on top of a patch that has already landed. ACCEPTANCE: each urllib seam pins to the vetted IP literal, proven by a rebinding fixture that changes its answer between the check and the open. TIER: T3 security. FOLLOW-UP OF row 703. Filed by integrator C from the panel's NOTE-class shape findings under the 20:3xZ scope ruling; the panel measured each and deliberately did NOT charge it as an escape. |
| 729 | OPEN | plex_deep._request-HAS-TWO-except-Exception-AND-ONE-HANDLER-IS-UNREACHABLE -- An SSRF refusal surfaces as a generic network error, which is the bd-vault-unlock collapse shape named in A7: two diagnoses with opposite remedies wearing one message. jellyfin and stash diverged from it rather than sharing it. ACCEPTANCE: an SSRF refusal is distinguishable from a transport failure at every deep-integration seam, and the unreachable handler is removed or made reachable. TIER: T2. FOLLOW-UP OF row 713. Filed by integrator C from the panel's NOTE-class shape findings under the 20:3xZ scope ruling; the panel measured each and deliberately did NOT charge it as an escape. AMENDMENT (three-way ruling 2026-09-04 23:05Z, applied by the 20260906T160000Z adjudication): DEPENDS-ON row713-fixb and RE-CUT under it -- this row inherits row 713's rule that its cut must not share authored paths with row 703's cut, so it rides a different train. EVIDENCE APPENDED 2026-09-06 (adjudication 20260906T160000Z): Register corrections from the rows727-729-730 adjudication (729 DEPENDS-ON 713b; 730 handler-without-reachability; 727 closes on split; residual T1) -- Three of the four items are text amendments to rows that are still OPEN and nothing has landed to change their status: IMPROVEMENT_BACKLOG.main.md lines 756 row 729 ends 'TIER: T2. FOLLOW-UP OF row 713. Filed by integrator C ...' with no 'DEPENDS-ON 713b / RE-CUT on top of 713b' clause; :757 row 730 has no 'handler landed with the 727+730 split ... never to be read as shipped SSRF protection' clause; :754 row 727 is OPEN and the split has NOT landed (git ls-tree origin/main tests / grep -iE 'row7(27/29/30)/hostsafety' -> empty; landed.tsv through 1498 names no 727/729/730 train and no row713-fixb). The 729 amendment adds a hard dependency on row713-fixb, which inherits 713's own rule (:740) that its cut must not share authored paths with row 703's cut. The fourth item -- the residual T1 'drive the guarded transport end-to-end into the three deep handlers with the mock-boundary injection removed' -- is ALREADY FILED as row 749 (:776, 'filed under condition 3 of the rows727-729-730 adjudication', DEPENDENCY: row713), so that part is DUPLICATE-OF-OPEN 749 and must not be filed again. Net: amend 729 and 730 text at the next register cut; 727 stays OPEN until the split lands with a version. |
| 730 | CLOSED @1530 | api_scrape_listing-CATCHES-ONLY-httpx-HTTPError-SO-AN-SSRF-REFUSAL-DEGRADES-TO-A-GENERIC-500 -- SSRFBlocked subclasses Exception, so a refused URL returns an unactionable 500 instead of the 502 the caller can act on. This is contrary to the repository's own precedent: orchestrate's sinks and selector_playground both catch it explicitly. ACCEPTANCE: the listing endpoint catches SSRFBlocked and returns the actionable status, with a test asserting the code and the distinctive diagnostic. TIER: T1. FOLLOW-UP OF row 713. Filed by integrator C from the panel's NOTE-class shape findings under the 20:3xZ scope ruling; the panel measured each and deliberately did NOT charge it as an escape. AMENDMENT (three-way ruling 2026-09-04 23:05Z, applied by the 20260906T160000Z adjudication): the handler landed with the 727+730 split and is NEVER TO BE READ AS SHIPPED SSRF PROTECTION -- it converts an unactionable 500 into an actionable status and asserts nothing about reachability of the refusal path. |
| 736 | CLOSED @1526 | A-DEPLOY-THAT-DEGRADES-THE-CLOAK-STILL-PRINTS-DEPLOY-OK-AND-REPORTS-HEALTH-GREEN -- The final step prints a plain success line and the health surface has no cloak match, so the fleet health column shows GREEN on a degraded host. NOT AN ESCAPE: row 686 mandates exit 0 on WARN, so exiting 0 is compliance -- what is missing is the qualifier beside it. ACCEPTANCE: a cloak degradation sets a flag that qualifies the end-of-run summary and appears in the health payload. TIER: T1. FOLLOW-UP OF row 686. Filed by integrator C from the panel's NOTE-class shape findings under the 20:3xZ scope ruling; the panel measured each and deliberately did NOT charge it as an escape. CLOSED @1526 IS PARTIAL AND THE REMAINDER IS NAMED: row 806, filed in the same commit, carries the half this closure did not meet. WHAT LANDED: scripts/deploy.sh sets CLOAK_STATE at six sites, builds SUMMARY_CLOAK once and appends it to BOTH terminal summaries, and its health STEP prints cloak=$CLOAK_STATE in every state including OK, gated by tests/test_deploy_script.py::test_cloak_state_is_reported_at_health_summary_and_record. WHAT DID NOT: that is deploy.sh's own stdout, read once by whoever watched the deploy; the /api/health PAYLOAD still does not carry cloak at all, so the fleet health column -- which is a curl of that payload and nothing else -- still reads GREEN on a degraded host, which is this row's own sentence. The acceptance clause 'appears in the health payload' is therefore NOT met by this closure and is row 806's subject. Amended by the integrator at assembly of train G-01-row753-clause3 per register-handoff/next-train/CLOSE-736.md; the status cell itself is unchanged because bd-register-amend refuses by design to alter a status cell ('amendment may not change the target row status'), which is filed as its own harness row. |
| 737 | OPEN | deploy.sh-REPORTS-A-PRESENT-BUT-UNREADABLE-HOOK-AS-ABSENT -- Two conditions with OPPOSITE remedies -- install it, or fix its mode -- collapsed into one message, and the shape is untested. This is A7's own bd-vault-unlock example recurring. SIBLING, closed by the same one-word change: the hook path is printed only on the SUCCESS path, so an operator cannot see WHICH path was probed. ACCEPTANCE: present-but-unreadable and absent are distinct diagnostics naming the remedy, and the probed path is printed on both paths. TIER: T1. FOLLOW-UP OF row 697. Filed by integrator C from the panel's NOTE-class shape findings under the 20:3xZ scope ruling; the panel measured each and deliberately did NOT charge it as an escape. |
| 738 | OPEN | THE-LOGIN-CAP-RESTS-ON-A-SINGLE-PROCESS-PREMISE-THAT-NO-TRACKED-TEXT-STATES -- MEASURED: two processes at four threads with a cap of three gave 3,3,3,4,3,4 -- the bound violated in two of six runs. Nothing configurable creates a second process today, since the server takes THREADS and the deploy starts one unit, so the cap is true and unbreakable by environment variable. It rests on nobody adding a login-capable CLI or a second instance against one home directory. ACCEPTANCE: the single-process premise is declared as an INVARIANTS.json entry so the next person adding a worker meets a declaration rather than a surprise. TIER: T2. FOLLOW-UP OF row 667. Filed by integrator C from the panel's NOTE-class shape findings under the 20:3xZ scope ruling; the panel measured each and deliberately did NOT charge it as an escape. |
| 739 | CLOSED @1533 | start_manual_login-IS-AN-UNCOUNTED-THIRD-CREDENTIAL-ROUTE -- Reachable from the login auto-teach branch under DEFAULT configuration, while the setup document says every credential login is counted. A cap with an uncounted route is only mostly a cap, and the documentation line is the misleading half. ACCEPTANCE: every credential route increments the cap, or the document states the exception with its reason. TIER: T2. FOLLOW-UP OF row 667. Filed by integrator C from the panel's NOTE-class shape findings under the 20:3xZ scope ruling; the panel measured each and deliberately did NOT charge it as an escape. |
| 740 | OPEN | record_login_attempt-IS-A-PUBLIC-SHIPPING-WRITER-WITH-NO-CAP-READ -- Zero production callers today and one import from the reserving function. The next caller to reach for it gets the ungoverned path by accident, which is the shape rather than a live defect. ACCEPTANCE: the public writer reads the cap or is made private, with a test asserting a direct call cannot bypass it. TIER: T1. FOLLOW-UP OF row 667. Filed by integrator C from the panel's NOTE-class shape findings under the 20:3xZ scope ruling; the panel measured each and deliberately did NOT charge it as an escape. EVIDENCE APPENDED 2026-09-06 (adjudication 20260906T160000Z): The login cap is check-then-write: a direct writer and a reservation race past it (count 4 at cap 3) (residual of 740, T1) -- The check-then-write does not exist in the tracked tree. On origin/main d7176ce8 (v3.66.1498) record_login_attempt at bulk_downloader/session_keeper.py:257-268 reads NO cap at all -- it json-dumps the source and calls db.session_event_record; the non-atomic read-then-insert at 'session_keeper.py:270-283' is a property of the UNLANDED row740-cx-b patch (git branch -a on the local clone shows no row740 branch; git ls-tree origin/main tests has no row740 test; landed.tsv has no row740 landing). Register rows are repo-tracked product defects, so a defect in an unlanded patch cannot be a row of its own; it is the acceptance bar the parent row must carry. The atomic seam already exists: reserve_login_attempt at session_keeper.py:177-241 does the count, the decision and the insert in ONE SQLite statement (its docstring at :179-185 names the exact unlocked read-then-write hazard). Row 738 (OPEN) already owns the multi-process form of the same count-4-at-cap-3 measurement. FOLD: extend row 740's ACCEPTANCE from 'the public writer reads the cap' to 'the public writer delegates to, or is replaced by, the single-statement reservation seam shared by every writer; a mixed-writer test (direct + reserve, same process, cap 3) asserts exact count 3 and exactly one refusal; a per-call-lock mutant and a deleted-UNKNOWN-branch mutant (:272-275 in the patch; expected RuntimeError with the fixture-unavailable reason, not TypeError) are each CAUGHT'. Cross-reference rows 667 and 738. Source: rows-to-file.md lines 1028-1029. |
| 741 | OPEN | relogin_event_type-COLLAPSES-CAP-UNAVAILABLE-INTO-auto_relogin_refused -- Repair-the-store and wait-for-the-day are opposite actions sharing one event type. Its substring test also misfiles a site reply that merely contains the phrase. ACCEPTANCE: the two states are distinct event types and the classifier does not decide on a substring. TIER: T1. FOLLOW-UP OF row 710. Filed by integrator C from the panel's NOTE-class shape findings under the 20:3xZ scope ruling; the panel measured each and deliberately did NOT charge it as an escape. |
| 749 | CLOSED @1535 | THE-GUARDED-TRANSPORT-IS-NOT-DRIVEN-END-TO-END-INTO-THE-THREE-DEEP-HANDLERS -- T1 RESIDUAL, filed under condition 3 of the rows727-729-730 adjudication. AFTER row713-fixb lands, the three deep integrations will have SSRF-refusal handlers -- but a handler is proven by the exception REACHING it, and the tests that prove these inject at the MOCK BOUNDARY rather than raising from the classifier. A test that mocks the boundary proves the handler's arms; it does not prove the client's real call path can produce the exception at all, which is the half that decides whether the guard is installed or merely present. THE DISTINCTION THIS ROW EXISTS FOR: an unreached handler and a working guard are indistinguishable from a green suite -- both show a passing test and no refusals -- and only one of them protects anything. Row 730 carries the same distinction for a different module and says a handler without reachability must never be read as shipped SSRF protection; this row is that requirement for the three deep clients. ACCEPTANCE: for EACH of the three deep clients, a test reaches the safe-destination check from that client's REAL call path with the mock-boundary injection REMOVED, and the refusal is observed arriving at the handler; the three clients are enumerated from the tree at check time rather than from this text, since row 713's own measurement found a FOURTH module the original finding did not name. NEGATIVE CONTROL: a legitimate destination still completes end to end through each client, so the proof is of routing rather than of refusing everything. Mutation: removing the guard from any one client's real path is CAUGHT, with a transform control that ESCAPES. DEPENDENCY: ROW 713 -- not workable before row713-fixb lands, because the handlers this row drives into do not exist yet. |
| 750 | OPEN | AN-IPv6-ENCODING-OF-THE-METADATA-ADDRESS-IS-NOT-UNWRAPPED-BEFORE-CLASSIFICATION-SO-A-SPELLING-BYPASSES-THE-GUARD -- T1 RESIDUAL of the 703d adjudication, which declined the NAT64 half on evidence -- the NAT64 prefix classifies as reserved on the shipped interpreter and is refused in both switch states -- and carried this out as the part that is NOT covered. MEASURED BY THE INTEGRATOR, on the shipped interpreter rather than from the note: the 6to4 spelling of the cloud metadata address reports sixtofour equal to that IPv4 address, while is_reserved is FALSE and is_link_local is FALSE. So a classifier that asks those predicates of the IPv6 object -- without unwrapping first -- sees an ordinary global address. The IPv4-mapped spelling behaves the same way: is_link_local is FALSE on the mapped form while the embedded address is the metadata one. NEITHER UNWRAP EXISTS ON MAIN: provider_resolve_impl/_common.py and hooks.py each contain ZERO references to the mapped, 6to4 or teredo accessors, so every never-allowed class is currently decided on the OUTER address only. WHY THIS IS THE DANGEROUS DIRECTION: it is not a refusal that fires wrongly, it is a refusal that does not fire. The guard is correct about every address it is asked about and is asked about the wrong object, so the suite stays green and the block simply does not happen -- and it happens for the one address the whole guard exists to stop. ACCEPTANCE: mapped, 6to4 and teredo forms are UNWRAPPED to their embedded IPv4 address and that address is classified, before any allowance is applied; every never-allowed class is evaluated BEFORE the private relaxation, so the switch relaxes private ranges only and never a metadata spelling; and the metadata test ITERATES the spellings -- plain, mapped, 6to4 and where the interpreter supports it teredo -- asserting the refused count equals the number of distinct spellings, so a spelling added later cannot pass by being absent from the corpus. NEGATIVE CONTROL: an ordinary global IPv6 address still passes, and a genuinely private address is still relaxed when the switch allows it. Mutation: removing any one unwrap, and moving the private relaxation ahead of the never-allowed evaluation, are each CAUGHT with a transform control that ESCAPES. NOT CLAIMED: that this has been exploited, or that any deployed host is reachable this way -- reachability depends on rows 687 and 703 routing a caller through the guard at all, which row 749 records is not yet proven end to end. DEPENDENCY: none for the unwrap itself; it is a change to the classifier and can land before or after the routing rows. |
| 752 | OPEN | THE-PARALLEL-DISPLAY-OWNERSHIP-TEST-FAILS-UNDER-THE-CANONICAL-SCHEDULE-AND-PASSES-ALONE -- T2. MEASURED 2026-09-05T04:2xZ on 10.0.70.53 (bd3, role=capacity; its hostname reads fresh-0, so the IP is the identity) against ref 9f0a68021b035f46fa92b63e5f9d62ba68883cbd (v3.66.1490), tree dd09c9e3c5d9d5fcdfd8b69e4baa204483d6b42d, checked equal to origin/main^{tree} rather than assumed. The canonical suite -- the sanctioned -n 24 --dist loadfile form over 1597 files, 24 workers -- returned 1 failed, 20852 passed, 55 skipped, 1 xpassed in 706.55s, and the one failure is tests/test_row300_parallel_display_cleanup_owns_process.py::test_cited_cleanup_leaves_a_foreign_display_alive. THE SAME FILE RUN ALONE ON THE SAME TREE PASSES 4 of 4 in 3.24s, in the suite's own checkout /tmp/bd-fullsuite. So the failure is schedule-dependent, in the family of rows 657, 659, 682 and 699. IT IS NOT A REGRESSION: the file last changed at v3.66.1297, whose commit message is the parallel display test owns the display it kills -- it was already a fix for a display-ownership race -- and nothing in 0700da23..9f0a6802 (v3.66.1488 and v3.66.1490) touches display, xvfb or row300 by path. Row 300 itself is CLOSED @1297; this row is the residue its fix did not reach, not a reopening of it. WHAT IS NOT KNOWN, stated because a localised failure is not a diagnosed one: the mechanism is UNNAMED. One green sample alone does not retire a schedule-sensitive failure (A5), and one green sample is all this row has. The contended resource, the neighbouring test, and whether the race is reliable or rare under -n 24 are all open. ACCEPTANCE: bd-flakecourt or a fixing cut NAMES the neighbouring test or the contended resource -- the display number, the X socket, the process group, or whichever it proves to be -- or a 3x reproduction under the SAME -n 24 --dist loadfile schedule and the SAME tree fails to reproduce and that negative result is recorded with its exact command, denominators and host; AND the test OWNS its display resource so that worker scheduling cannot decide the outcome. A verdict of flaky is NOT acceptance, and neither is a marker, a rerun, or a reorder. DEPLOY DISPOSITION, ruled by the operator 2026-09-05T04:4xZ: NOT a deploy gate; the deploy proceeds on this tree. Recorded here so that the canonical suite for v3.66.1490 is not later read as clean -- it was not, and this row is why. FILED by integrator B in a register-only cut at v3.66.1491. DEPENDENCY: none. FAMILY: rows 657, 659, 682, 699 -- test isolation with at least two distinct mechanisms already proven, so do not assume one fix retires them all. |
| 753 | OPEN | THE-HUNT-REAPING-TEST-FAILS-ONLY-UNDER-CANONICAL-LOAD-AND-PASSES-ALONE-AND-AT--n12 -- T2, and this row is a DIAGNOSIS rather than a report: the measurement that would normally be a fixing cut's first job is already done. MEASURED 2026-09-05T06:5xZ-07:0xZ on 10.0.70.53 (bd3, role=capacity; its hostname reads fresh-0, so the IP is the identity) against the EXACT final tree 43edc0e2a22b642db86f4c1bdd9a3fee7c6669d9 (sha 5a7a5ae1, v3.66.1492), verified equal to origin/main^{tree} on the host before the run rather than assumed. THE SUBJECT: tests/test_v3_66_1132_the_hunt_reaps_what_it_abandons.py::test_terminal_frame_without_eof_never_enters_an_unbounded_child_wait[ABORTED v1 reason=synthetic-0]. THE THREE MEASUREMENTS, all on that one tree: it FAILS under the sanctioned canonical form, -n 24 --dist loadfile over 1597 files at host load ~85, in a run that was otherwise 20863 passed / 55 skipped / 1 xpassed in 740.33s; it PASSES ALONE; and it PASSES under -n 12 --dist loadfile. Its whole file also passes alone, 157 passed in 228.89s, measured independently. So the discriminator is CONCURRENCY AND LOAD, not the tree: -n 24 at load 85 fails where -n 12 and serial both pass, and nothing about the code under test differs between those runs. WHY THIS IS NOT A REGRESSION: the subject is unchanged across 1490..1492 and exact-head CI is green on every one of those candidates, so no landing introduced it. WHAT IS STILL UNKNOWN, stated because a discriminator is not a mechanism: WHY load decides it. A test named for never entering an unbounded child wait, failing only when 24 workers contend, points at a timeout or a reap window that is generous at -n 12 and not at -n 24 -- but that is a hypothesis, not a measurement, and no one has instrumented the wait itself. ACCEPTANCE: the wait or reap bound the test depends on is NAMED and shown to be load-dependent -- by instrumenting the boundary, not by reading the source -- and either the production bound is corrected or the test asserts its own precondition so that contention cannot decide the outcome; AND a 3x run under the SAME -n 24 --dist loadfile schedule on a comparably loaded host either reproduces it, establishing the rate, or fails to and that negative result is recorded with its exact command, denominators, host and load. A verdict of flaky is NOT acceptance, and neither is raising a timeout until it stops failing. DEPLOY DISPOSITION, ruled by the operator 2026-09-05T07:0xZ: TOLERATED, not a deploy gate; the v3.66.1492 canonical suite was released as PASS-WITH-TOLERATED naming this row and row 752, and the deploy proceeds on that tree. Filed so that suite is never later read as clean. FILED by integrator B in a register-only cut at v3.66.1493. PARTIAL LANDING @3.66.1510 (train A2-04, car 4, cut/row753, base bc09e062): bd-wedge-hunt's collapsed reader window now yields S:ERROR-LATE / C:UNKNOWN in the owner record instead of the S:TIMEOUT / A:0 / C:TIMEOUT an honestly empty channel earns, so a measurement that did not happen is no longer indistinguishable from one that found nothing. bd-lens-L2 narrow verify BOARD (verdicts/row753-VERDICT-L2-verify.md): 7 mutants CAUGHT, 0 escaped, 0 invalid, transform control ESCAPES; 9 passed / 0 skipped under -k row753. ROW STAYS OPEN: acceptance clause 3, three runs under the canonical -n 24 --dist loadfile schedule on a comparably loaded host, is OWED and is outside worker authority. DEPENDENCY: none. FAMILY: rows 657, 659, 682, 699 and 752 -- schedule- and load-sensitive tests with at least two distinct mechanisms already proven, so do not assume one fix retires them all. This row differs from 752 in its discriminator: 752 varies with WHICH tests share a worker, 753 varies with HOW MANY workers contend. |
| 759 | OPEN | THE-GAMMA-DOWNLOAD-TRIGGER-SETTLES-UNCHANGED-AND-IS-TREATED-AS-CLICKED-SO-THE-QUALITY-LABEL-INSIDE-THE-MODAL-IS-NEVER-REACHED -- Gamma/Kosmos (dfxtra, evilangel, xempire) scene pages have no static media href: an hrefless Download div opens a modal whose LIVE control is the visible quality LABEL span (the 2160p cell is visible:False). BD ends needs_review "Clicked but no download started"; the same scene completes by hand via trigger -> modal -> label. MEASURED 2026-09-05 on test2 at v3.66.1498 (SITE_RUNBOOK.md lines 298-335, screenshots dfxtra-05-download-modal.png, evilangel-10-download-modal.png; FEATURE_REQUESTS.md lines 16-31 FR-1). Acceptance: BD saves a file from a dfxtra/evilangel scene honouring quality_preference on the highest VISIBLE label, never the hidden resolution cell. RED: Fixture page: hrefless Download div whose click appends a modal of quality rows (label span live, resolution cell hidden); unfixed runner ends needs_review with no download event. TIER: T2. FILED by the 20260906T160000Z adjudication (N15 N27 N29 N23(half) N35(half)). PART 759-4 -- a COMPUTED-HIDDEN resolution cell can never be the quality winner -- LANDED at v3.66.1506 (bulk_downloader/detect.py, the resolution score removed from a hidden cell); this row STAYS OPEN carrying parts 1/2/3, the hrefless Download trigger, the modal wait and the visible-label click in bulk_downloader/runner.py _process_one. |
| 760 | OPEN | A-SIGNED-ONE-TIME-DOWNLOAD-URL-DELIVERED-THROUGH-window.open-HAS-NO-CAPTURE-SEAM-SO-AN-AYLO-SCENE-ENDS-needs_review-AND-THE-URL-IS-CONSUMED -- Aylo/project1content members (bangbros, brazzers): Download button -> quality buttons (h264 2160p..320p) -> a POPUP/new page whose URL IS the signed mp4 on download-private-{ht,fl}.project1content.com; the runner waits on the clicking page only, never captures the popup, and the one-time URL is spent (474 on any re-fetch). MEASURED 2026-09-05/06 on test2 at v3.66.1498 (SITE_RUNBOOK.md lines 550-589, 1811-1831; transcript-part2 bangbros 20:29-20:36Z). RED: Fixture scene whose quality button calls window.open on a one-time signed URL: unfixed runner ends needs_review "modal-trigger button", no download event, URL consumed by the popup. TIER: T2. FILED by the 20260906T160000Z adjudication (N16 N30 N43 N23(half)). |
| 761 | OPEN | THE-DOWNLOAD-RANKER-NAMES-A-SAME-ORIGIN-LISTING-FACET-URL-AS-WINNER-BECAUSE-A-RESOLUTION-TOKEN-IN-ITS-QUERY-SCORES-AS-QUALITY -- On a logged-in Gamma scene page the production ranker named /en/videos/?refinementList[video_formats.format][0]=2160p (a same-origin LISTING FACET, label "4K (2160p) (2327)") as candidates.winner over the real modal trigger. MEASURED 2026-09-04T04:38Z test3 v3.66.1466 xempire run 20260904T043848Z and 2026-09-05 18:50Z test2 v3.66.1498 dfxtra run 20260905T184610Z (record.json). Sibling of 704/719 (scene classifier) but this is the DOWNLOAD ranker. RED: Fixture page carrying (a) <a href="/en/videos/?refinementList...=2160p">4K (2160p) (2327)</a> and (b) an hrefless Download div: assert the facet anchor is not the winner / is refused as a listing route. TIER: T2. FILED by the 20260906T160000Z adjudication (N2 N44 N35(half)). |
| 762 | OPEN | BOTH-GATE-PATHS-AUTO-CLICK-CONTINUE-TO-MEMBERS-AREA-AND-NOTHING-REFUSES-A-BILLING-CONSENT-INTERSTITIAL-SO-A-LOGIN-SETTLE-CAN-SPEND-REAL-MONEY -- T3 SAFETY (real-money side effect). Aylo login lands on /store, an upsell interstitial with a PRE-CHECKED checkbox ("you agree to add <upsell> ... charged $29.97 every 30 days until you cancel") whose advance control is CONTINUE TO MEMBERS AREA; dismiss_gates / the post-login dismisser clicks it. MEASURED 2026-09-05/06 on test2 at v3.66.1498 (SITE_RUNBOOK.md lines 520-541, 1350; FEATURE_REQUESTS.md lines 197-221 FR-13 operator screenshot). RED: Fixture with <input type=checkbox checked> + "$29.97 every 30 days" + button "CONTINUE TO MEMBERS AREA": dismiss_gates must NOT click; a second fixture without the charge text may proceed. TIER: T3. FILED by the 20260906T160000Z adjudication (N6 N17). |
| 767 | OPEN | tiny4k.com-IS-CLAIMED-BY-FOUR-WRONG-OWNERS-vip4k_family-wowgirls_network-THE-AYLO-HOST-LIST-AND-phoenix_catalog-SO-A-PORNPROS-SPA-RUNS-FOREIGN-SELECTORS -- _data_studios_a.py:13 lists tiny4k under vip4k_family ("same operator as WowGirls"), _data_players.py:113 under wowgirls_network, tests/test_v3_43_57_more_templates.py:192-201 pin both, and is_aylo_url claims it; tiny4k is PornPros/Fame Digital (Vue SPA, DOWNLOAD HD 2160/1080/720/480 with self-authorizing cdn.pornpros.com hrefs; verified 4K 3840x2160). MEASURED 2026-09-06 on test2 (SITE_RUNBOOK.md lines 801-849). RED: is_aylo_url("https://tiny4k.com/members/video/x") is False and suggest_for_url contains a pornpros template and neither vip4k_family nor wowgirls_network. TIER: T2. FILED by the 20260906T160000Z adjudication (N10 N37). PARTIALLY LANDED @v3.66.1501 AND STILL OPEN. Two of the four wrong owners were removed by the pm-template-gap cut: vip4k_family and wowgirls_network no longer carry tiny4k, a dedicated pornpros_tiny4k template now owns it, and a test asserts exactly one claimant with mutants restoring each removed claim. THE OTHER TWO REMAIN AND ARE WHAT THIS ROW NOW MEASURES: extractors_aylo.py still lists tiny4k.com in its Aylo host set, so is_aylo_url on a tiny4k members URL still returns True (measured on this tree at v3.66.1501), and phoenix_catalog.py still declares a tiny4k entry with host_patterns tiny4k.com. The row's RED is unchanged and is NOT satisfied. |
| 769 | OPEN | THE-login-LOG-LINES-CARRY-NO-SITE-ID-SO-CONCURRENT-LOGINS-INTERLEAVE-UNATTRIBUTABLY-WHILE-network-LINES-ALREADY-CARRY-ONE -- Every `login:` line in logs/bulk_downloader.log is written without a site id; three concurrent lanes interleave and a login is attributable only by correlating against the [<sid>][network] lines (cost one wrong attribution mid-analysis). MEASURED 2026-09-04 13:58Z on test2 (rows-to-file.md lines 751-754; transcript-part1:782). RED: Capture stderr while running the login submit path for a stub sid: every line starting "login:" carries the sid. TIER: T1. FILED by the 20260906T160000Z adjudication (N5 N14). |
| 770 | OPEN | BOTH-LOGIN-FILL-PATHS-RESOLVE-locator-first-WITH-NO-HONEYPOT-CHECK-SO-A-tabindex-MINUS-1-DECOY-BEFORE-THE-REAL-FIELD-IS-FILLED -- ultrafilms input#website_url tabindex=-1; wowgirls a 1px off-screen aria-hidden tabindex=-1 "Website (do not fill)" input[type=text] on a form whose real username field is also type=text; pegasproductions hidden decoys input[name=login...]. login_impl/submit.py:79-102 fallback chain ends in a bare input[type=text] and never consults the shipped honeypot rule. MEASURED 2026-09-04/05 on test2 by hand-drive (SITE_RUNBOOK.md lines 29-36, 163-173; FEATURE_REQUESTS.md lines 35-41; transcript:4093-4101, 4549). RED: Fixture form from the saved wowgirls DOM with the real field renamed so only the last-ditch selector matches: the honeypot must not be filled; second fixture with hidden decoys. TIER: T2. FILED by the 20260906T160000Z adjudication (N25 N31 N39). |
| 771 | OPEN | THE-INTERSTITIAL-DISMISSER-SPELLS-NO-THANKS-WITHOUT-THE-COMMA-SO-EVILANGEL-NOTIFICATION-PROMPT-STANDS-AND-HIDES-THE-MEMBERS-AREA-CONTROL -- A valid evilangel login lands on /en/interstitial where TWO stacked controls must be cleared IN ORDER: a notification prompt whose control text is exactly "NO, THANKS" (comma) over the anchor "No Thanks. Continue to Members Area"; the dismisser matches the comma-less spelling only. MEASURED 2026-09-05 19:0xZ on test2 by hand-drive (transcript:2076; SITE_RUNBOOK evilangel section). RED: Fixture with the two stacked controls driven through interstitial.dismiss_gates(page): both cleared in order. TIER: T2. FILED by the 20260906T160000Z adjudication (N32). EVIDENCE APPENDED 2026-09-06 (adjudication 20260906T160000Z): EVILANGEL-NEEDS-TWO-ORDERED-DISMISSALS-AND-clear_gates-BREAKS-AFTER-ONE-INTERSTITIAL-CLICK -- The candidate's stated mechanism is REFUTED for the seam that actually runs. The walls appear once, on /en/interstitial after login (SITE_RUNBOOK.md lines 377-391), which is the LOGIN path: login_impl/submit.py:979-980 calls interstitial.dismiss_gates, and its declared-selector pass _site_pass (interstitial.py:~690, docstring 'Try every declared selector once') loops over EVERY dismiss_selectors_login line in order, checks is_visible() immediately per selector (:733-757), clicks through _click_gate with settle_s between clicks (:809-815), and dedupes only by label -- so two ordered walls clear when both lines are declared. clear_gates' one-click-per-tier (interstitial.py:107-108, 300-333) is the CONTENT-URL path and only matters if the walls recur per scene, which the runbook does not show. What survives is one template-data line: the shipped gamma_kosmos default dismiss_selectors_login (_data_players.py:271-275) declares 'No Thanks. Continue' and 'Continue to Members Area' but NOT the browser-notification prompt 'NO, THANKS', whose comma defeats both the generic a:has-text('No Thanks') (:131-132) and the generic regex '^no thanks...' (:86). That line belongs in the template-corpus row (candidate 7, registerTitle SIX-TEMPLATE-GAPS-AND-THREE-DRIFTED-TEMPLATES...), which is not yet an open row, so no rowId can be cited; if the PM files that row this becomes its evilangel line, present-but-optional so dfxtra (no walls, same template, same day) is not stalled. The rows-to-file.md lines 1090-1112 claim that dismiss_selectors_login 'was empty for the site' contradicts the coverage table (evilangel matched gam_kosmos, SITE_RUNBOOK.md lines 1611) and is UNVERIFIED -- it is either row 720's onboarding precondition or a harness onboarding matter. Live reproduction of the product failing on the walls: UNVERIFIED (the runbook drove them by hand). Register has no open row on multi-step dismissal (367/372 CLOSED); not in 754-758. |
| 772 | OPEN | A-LOGIN-THE-SITE-REJECTED-IS-REPORTED-AS-SUCCESS-OR-AS-A-TAKEOVER-BECAUSE-NO-BRANCH-READS-THE-SITE-OWN-REJECTION-SO-A-WRONG-PASSWORD-IS-RETRIED-INTO-THE-CAP -- Gamma re-renders /en/login with "Wrong username or password provided"; Aylo redirects to /badlogin?ats=...; login settle reports the same shape as success/challenge/timeout, so a stale password from the password-manager export was retried into the login cap (evilangel, filthykings, bangbros). MEASURED 2026-09-05 on test2 by hand-drive, NOT via do_login -- the product path is UNMEASURED for this input and the row must say so (FEATURE_REQUESTS.md lines 61-71; FEATURE_IDEAS.md lines 50-61; transcript:5296-5307, 6052). Sibling of 708 (no-nav success) and 667 (cap accounting). RED: Fixture whose POST 302s to /badlogin?ats=x with success_url="": do_login must not return ok=True; second fixture re-rendering /login with the rejection text. TIER: T3. FILED by the 20260906T160000Z adjudication (N33 N24). |
| 773 | OPEN | NO-HISTORY-OR-LOGIN-RECORD-CARRIES-THE-EGRESS-IDENTITY-SO-A-BLOCK-CANNOT-BE-ATTRIBUTED-TO-IP-VERSUS-CREDENTIAL-VERSUS-BROWSER -- test2 egress was SPLIT (CLI 174.178.147.233 vs browser/CF path 75.165.3.230) so IP-signed CDN URLs returned 474; the cause was reached only after credential, browser and session hypotheses were spent because no record carries the egress identity. Product half of row 722 (the operator-decision carrier). MEASURED 2026-09-05/06 on test2 (SITE_RUNBOOK.md lines 1796-1803; BACKLOG_CROSSREF_REPORT.md lines 20). RED: history/login record for a fixture run carries an egress field (IP or UNKNOWN), never absent. TIER: T3. FILED by the 20260906T160000Z adjudication (N26). |
| 774 | OPEN | THE-SUBMIT-SWEEP-COUNTS-ANY-NAVIGATION-AS-SUCCESS-WITH-NO-ORIGIN-CHECK-SO-A-LOGIN-WITH-GOOGLE-CONTROL-RETURNS-OK-FROM-AN-OAUTH-ORIGIN -- bang.com: button:has-text("LOGIN") matches the LOGIN WITH GOOGLE control and navigates to accounts.google.com; the safe control is form[action*=login_check] button[type=submit]. Whether the PRODUCT sweep reached that button on bang is UNVERIFIED and the row says so. MEASURED 2026-09-06 on test2 by script (SITE_RUNBOOK.md lines 1145-1158, 1772-1773). RED: Fixture whose first Login-text button navigates to a second origin: do_login/submit sweep must not return True; a same-origin submit still does. TIER: T3. FILED by the 20260906T160000Z adjudication (N38). |
| 775 | OPEN | THE-CHALLENGE-PATH-HAS-NO-SINGLE-CHECKBOX-CLICK-AFFORDANCE-SO-THE-RULED-PERMITTED-TURNSTILE-TICK-IS-REACHABLE-ONLY-THROUGH-THE-PAID-SOLVER-PATH -- OPERATOR_DECISIONS.md 2026-09-03 permits, for the operator OWN accounts on the self-hosted tool, ONE real-browser click on the Turnstile checkbox (it submits to Cloudflare own evaluation, forges nothing) and forbids solver services; the product has no such affordance, so the permitted tick is only reachable through captcha_provider/captcha_api_key (row 751 family). MEASURED 2026-09-06 (SITE_RUNBOOK.md vip4k/vixenplus/adulttime sections; asks-and-answers ASK 12/14/16). RED: Fixture with a cf-turnstile container and an in-page checkbox (and a same-origin frame variant): with no captcha_api_key and the affordance enabled, exactly one click is issued and recorded; with it disabled, none. TIER: T2. FILED by the 20260906T160000Z adjudication (N40). |
| 776 | OPEN | THE-AUTO-TEACH-PRE-FLIGHT-HANDS-A-FIRST-RUN-SITE-TO-A-HUMAN-WITHOUT-RUNNING-THE-RANKER-SO-A-PAGE-WITH-A-DIRECT-MEDIA-HREF-STILL-DEMANDS-A-TAKEOVER -- nubiles: job went needs_review "Auto-teach: take over to teach download selectors" 12 s after start although the page carried a first-class direct media href the ranker already accepts. MEASURED 2026-09-04T08:13Z on test2 at v3.66.1466 (bd-persist/campaign/nubiles/run_meta.json; CAMPAIGN_RUNBOOK.md s8 :344-362 routes it as app-wide). RED: Site with no learned selectors, no applied_template, auto_teach_first_run default; fixture page whose inspect_candidates returns an accepted media_extension winner: start() must not mark needs_review. TIER: T2. FILED by the 20260906T160000Z adjudication (N28). |
| 778 | OPEN | THE-WEBSITE-TITLE-IS-CAPTURED-ONLY-AT-THE-TRANSPORT-BOUNDARY-SO-EVERY-EXTRACTOR-ARM-RECORDS-A-DONE-ROW-WITH-NO-TITLE -- Follow-up of row 375 (CLOSED @1339): the never-invent-a-title contract is met while the harvest itself is skipped -- a Vixen-family (blacked) completion records history status done with title "". MEASURED 2026-09-03 on test6 at v3.66.1450 (campaign/test6/blacked/1/record.json). RED: Fake page with document.title "BLACKED: X" run through an extractor completion via _process_one: history row carries the title. TIER: T2. FILED by the 20260906T160000Z adjudication (N48). |
| 780 | OPEN | THE-SITES-LOADER-REBUILDS-EACH-ENTRY-FROM-CFG_FIELDS-SO-listing_url-IS-SILENTLY-DROPPED-WHILE-TWO-READERS-STILL-CONSULT-IT -- UNVERIFIED BY THE REFUTER (its agent hit the usage limit) -- file only after one read of the loader. A wowgirls entry written with listing_url read back None after import_apply saved and reloaded; only crawler_listing_url persisted. MEASURED 2026-09-04T14:29Z on test2 at v3.66.1466 (transcript-part1:1579-1653). RED: Round-trip a sites_config entry carrying listing_url through the loader: the key survives or the two readers stop consulting it. TIER: T1. FILED by the 20260906T160000Z adjudication (N13 (UNVERIFIED)). |
| 784 | OPEN | THE-no-nav-LOGIN-VERDICT-KEEPS-NO-PAGE-WHEN-NEITHER-CHECK-IS-DECLARED-ALTHOUGH-ITS-DOCSTRING-SAYS-EITHER-WAY -- T1. Found by a lens on the 708b cut and deliberately NOT fixed there. bulk_downloader/login_impl/submit.py _no_nav_verdict documents that the page the run actually read is kept as evidence EITHER WAY, and that is the sentence that makes a settled-no-nav verdict auditable. member_state_check in bulk_downloader/login_impl/replay.py reaches write_login_evidence only AFTER two early returns, and BOTH of them return evidence_path None: the branch where the template declares neither a success_url nor a learned member_indicator, and the branch where page.url raises. The first is the COMMON case for an untaught template, so the state the 708b cut introduced to be auditable is, for exactly those sites, the state with no page kept. ACCEPTANCE: on a no-nav login where neither check is declared, the run record holds the HTML and the final URL of the page that was read, and the verdict is still not success. RED must assert the file EXISTS and that its bytes are the page read, not merely that evidence_path is non-None. NEGATIVE CONTROL: a capture failure still returns a falsy verdict, because keeping a page must never become permission. FOLLOW-UP OF row 708, which closed on its added acceptance only. |
| 785 | OPEN | THE-LOGIN-EVIDENCE-FILENAME-EMBEDS-SPACES-BECAUSE-THE-PHASE-STRING-IS-OPERATOR-PROSE -- T1. Found by a lens on the 708b cut and deliberately NOT fixed there. write_login_evidence in bulk_downloader/login_impl/replay.py builds its name as tag-stamp-pid.html, and _no_nav_verdict in bulk_downloader/login_impl/submit.py passes tag=login-PHASE where PHASE is one of three operator-readable strings measured in this tree: page closed mid-submit, no nav signal, page closed post-submit. Every one of them contains spaces, so the kept page is written to a path such as login-page closed mid-submit-STAMP-PID.html. Nothing refuses it -- the write succeeds -- which is why this is a row and not a crash: the cost lands later, on the shell quoting, the archive member set and the operator who has to name the file. ACCEPTANCE: the evidence filename is derived from the phase through an explicit slug whose output is asserted to match a restricted character class, with the human phase preserved INSIDE the file rather than in its name. The three phases in the tree at check time are the denominator, not these three, since a fourth seam may exist by then. NEGATIVE CONTROL: a phase of only punctuation still yields a usable unique name rather than an empty one. |
| 786 | OPEN | A-SIGNED-ONE-TIME-DOWNLOAD-URL-CAN-BE-SPENT-TWICE-BECAUSE-THE-HTTP-FALLBACK-RE-CLICKS-THE-ORIGINAL-CONTROL -- T2. bulk_downloader/runner_transport.py, in _do_download: the except _HTTPDownloadFailed arm falls back to Playwright by re-clicking best[locator] inside page.expect_download. For an ordinary static URL that is correct and is why the fallback exists. For a control that opens a SIGNED ONE-TIME URL -- the window.open shape -- the first click already spent the token to produce the URL the direct-URL httpx path then used, so the failure being recovered from may itself BE the spend, and the re-click spends a second one. The code says so in its own comment: some sites will not let us do this twice in quick succession, which records the symptom while treating it as flakiness. ACCEPTANCE: a retry policy that KNOWS a URL is one-time -- the fallback is not attempted, or is attempted only through a fresh grant, when the candidate was obtained from a single-use grant -- proven by a fixture whose second issue of the same token refuses. A retry counter is not the fix and must not be accepted as one, because the defect is that the second attempt is well-formed. NEGATIVE CONTROL: an ordinary static URL still gets the existing browser fallback unchanged. |
| 787 | OPEN | A-NOTHING-IN-SCOPE-SENTINEL-FOR-find_best_download-MUST-NOT-BE-TRUTHY-AND-KEYLESS -- T1 HAZARD SHAPE, filed although the cut that exposed it was REFUSED, because what is being recorded is the shape and not that cut. MEASURED in this tree: bulk_downloader/detect.py find_best_download returns either None or a dict carrying locator, and four of its five call sites decide on TRUTHINESS ALONE -- bulk_downloader/auto_detect.py and two seams in bulk_downloader/runner_manual.py all branch on if not best, and bulk_downloader/runner_extractors.py takes the result of the deep-detect retry the same way. A future sentinel that says nothing was in scope and is a truthy empty-ish object therefore passes every one of those guards and is treated as a FOUND candidate; the failure surfaces later and elsewhere, as best.get(locator) being None or best.get(_learned_sel) being the empty string, which is the expensive shape to diagnose. ACCEPTANCE: any nothing-in-scope signal this function grows is either falsy or is carried in a key every caller is proven to read, with the caller census derived FROM THE TREE at check time rather than from this row, and a gate that fails when a call site decides on truthiness alone. NEGATIVE CONTROL: a genuine candidate is still admitted by every call site. |
| 788 | OPEN | THE-708b-MUTANT-SPEC-DECLARES-ITS-TRANSFORM-CONTROL-INLINE-INSTEAD-OF-IN-ITS-OWN-SPEC-FILE -- T1 harness. Found by a lens AFTER the 708b cut landed at v3.66.1502, so this is a FOLLOW-UP and not a bounce. MEASURED in this tree: 136 of the 337 specs under tests/mutants are dedicated one-role-per-file transform controls named for that role, and NO other spec declares a control inline -- 708b's tests/mutants/row708_no_nav_login_is_not_success.json is the only one, carrying M10 labelled a transform control alongside nine regression mutants. A control is a mutation that by construction changes no behaviour under its band, so it MUST escape; declared as a regression with a named catcher, that escape reaches the branch of bd-mutate that reports a named catcher passing, which the battery scores as ESCAPED and therefore as a FAILURE. The dedicated files exist precisely because a control spec is expected to report exactly one valid escape, with a band containing only the control node. ACCEPTANCE: M10 moves to its own tests/mutants spec named for its role, its band is the control node alone, the regression spec is left with nine mutants, and BOTH batteries are run and reported -- the regression one all caught, the control one exactly one valid escape. The 136-versus-337 population is re-derived from the tree at check time rather than taken from this row. NEGATIVE CONTROL: the moved control still escapes for the intended reason, proven by its own diagnostic, not merely by a nonzero escape count. |
| 790 | OPEN | bd-precut-PRINTS-cut-ready-WHILE-A-BLOCKING-DETECTOR-IS-UNAVAILABLE -- T2, A2 and the M58 family: an unavailable measurement is never permission. ORDERED by the 23:15Z row660 ruling, OPERATOR_DECISIONS.md clause 4. WHAT IS ALREADY RIGHT, so a worker does not rebuild it: toolchain/bin/bd-precut keeps an `unknown` list and prints RESULT: cut-ready for what RAN (n) -- N check(s) NOT RUN, so UNKNOWN, not OK: when it is non-empty, and bd-coretest populates it correctly in BOTH skip branches (--no-coretest, and binary not found). The fix pattern exists in the same function. THE DEFECT, three arms, MEASURED by reading main(): (1) a MISSING bd-footguns prints [bd-footguns not found -- footgun registry SKIPPED] and appends NOTHING to `unknown`, so the blocking footgun registry is silently absent and the verdict still reads the plain cut-ready line; (2) a MISSING bd-ratchet has NO else branch at all -- no print, no unknown, no trace, the metric ratchet simply does not happen; (3) BOTH detectors test `if rc == 3` ONLY, so any other nonzero return -- a crash, a wrong interpreter, an uncaught exception -- is indistinguishable from a pass. CURRENT STATE: LATENT, not firing. bd-footguns, bd-ratchet and bd-coretest are all PRESENT in toolchain/bin, and the 1507 precut run printed the plain cut-ready line with zero UNKNOWN lines in its log, i.e. the list was genuinely empty. ACCEPTANCE: a missing bd-footguns or bd-ratchet appends to `unknown` so the verdict prints the cut-ready-for-what-RAN form and NAMES the absent detector, and a nonzero-but-not-3 return from either is UNKNOWN rather than silence. RED: stub each binary away, and separately make one exit 1, and assert the VERDICT LINE and the named detector -- assert the line, not the exit code, because an unrun check does not change the exit code. NEGATIVE CONTROL: with all three detectors present and passing, the plain cut-ready line is unchanged. EVIDENCE: bd-persist/verdicts/precut-unknown-absorption-MEASUREMENT-a2.md. Filed by bd-integrator-a2. |
| 791 | OPEN | A-WITNESS-RUN-LEAKS-BD_HOME-live_recorder-is_available-AND-sys.path-INTO-THE-IN-PROCESS-AUDIT -- T1 LATENT, same hazard class as row 659 and no observed failure yet. tools/audit/witnesses/cap01_witnesses.py in _w6 assigns os.environ BD_HOME to a tempfile.mkdtemp() that is never restored and rebinds live_recorder.is_available to a lambda returning True; tools/audit/witnesses/run01_witnesses.py inserts the repository root into sys.path. All three sites are PRE-EXISTING on origin/main, which is why this is a row and not a bounce. Acceptance: each witness scopes its environment, its module attribute and its import path for the duration of the witness only, proven by an assertion that the ambient value is unchanged after the run. RED: run the witness and assert BD_HOME, live_recorder.is_available and sys.path are what they were before it. TIER: T1. THE NEXT WORKER READS generation b's fix FIRST -- bd-review-wt/row659-cx-b patch 0b0ae84d (TemporaryDirectory plus patch.dict plus patch.object, mutants M1 and M2 CAUGHT) and its NOTE 1 that lr._reset_for_tests() is still unscoped -- that is the row-727 lesson. FILED by bd-adjudicator ruling 20260906T231002Z clause 4; this row does NOT close row 659. |
| 792 | OPEN | THE-AGE-GATE-ENTER-VOCABULARY-IS-PINNED-AGAINST-TOTAL-WIDENING-BUT-NOT-SUBSTRING-WIDENING-AND-THREE-NEW-CONSUMERS-CARRY-NO-TEST -- T1 DIAGNOSTIC residual of row 721, filed by the lens that boarded it (R1 and R2 in bd-persist/verdicts/row721-age-gate-enter-VERDICT-shape.md). R1: the ENTER affordance regex survives the mutant ^.*enter.*$ with 13 passed, because no label in the corpus contains the substring except ENTER itself, so a label such as ENTER YOUR CARD DETAILS would be admitted; one fixture label closes it. R2: three of the five new consumer sites carry no mutant and no test -- login_impl/manual.py in _gate_blocked and login_impl/submit.py in _fill_blocked and in _post_blocked -- and deleting each leaves the battery green. The shipped code refuses correctly at all three; this is coverage of surface the fix itself added. Acceptance: a substring-widening mutant on the ENTER vocabulary is CAUGHT, and each of those three consumers has a mutant with a named catcher. TIER: T1. |
| 793 | OPEN | THE-EVIDENCE-SERVER-KEYS-ON-A-DIRECTORY-PREFIX-RATHER-THAN-ON-SCREENSHOTS_DIR-PLUS-SUFFIX -- T2. This clause was folded into row 721's acceptance with the words no standalone row, and row 721 was CLOSED at v3.66.1509 by bd-adjudicator ruling 20260906T232900Z on the age-gate half alone, so the clause now needs its own row or it has no home. NOT BUILT by that cut: SCREENSHOTS_DIR occurs zero times in its patch and serve_ss is untouched. On main serve_ss hands any file under SCREENSHOTS_DIR to send_file with a guessed mimetype. Acceptance: the guard keys on under SCREENSHOTS_DIR and suffix is not .png rather than on a directory prefix, a real .png under a configured subdirectory still serves as an image, and a mutant restoring the prefix key is CAUGHT. UNVERIFIED and worth measuring first: whether anything writes a non-PNG under that directory today. TIER: T2. |
| 794 | OPEN | THE-bd-precut-VERDICT-PREAMBLE-ASSERTS-footguns-clean-metric-ratchets-clean-WITHOUT-CONSULTING-EITHER-RESULT -- T2 harness, the M58 shape inside the tool that gates every freeze. This is the same FAMILY as row 790 and not a duplicate of it: row 790 is about a detector whose absence never reaches the unknown list, this row is about the SENTENCE the verdict prints once it is there. MEASURED in toolchain/bin/bd-precut main(): the variable ran is assigned the literal string footguns clean, metric ratchets clean, unconditionally, after the blocking list is found empty and without reading either detector's result, and it is then interpolated into RESULT: cut-ready for what RAN (...). So a run whose bd-footguns was SKIPPED, or whose bd-ratchet never ran at all, still prints footguns clean, metric ratchets clean as the description of what ran -- immediately above the list that names those very checks as NOT RUN. The one line an integrator reads contradicts the lines beneath it. ACCEPTANCE: the RAN clause is derived from the detectors that actually reported, names only those, and says nothing about any detector in the unknown list. RED: make bd-footguns unavailable and assert the RAN clause does not claim footguns clean. NEGATIVE CONTROL: with every detector present and passing, the printed line is byte-for-byte unchanged. TIER: T2. FILED by the operator from bd-persist/verdicts/cx-hostsafety-VERDICT-correctness-5.md, and it is not a finding against that cut. |
| 795 | OPEN | THE-IMPORT-GRAPH-GATE-PRINTS-A-REMEDY-THE-CONTRACT-FORBIDS-AND-A-BARE-INTERPRETER -- T1 harness, and the SECOND occurrence of row 632's defect class in the same file (632 closed @1449 for BaselineShrinkError naming a remedy the wrapper rejects). MEASURED on main 142133f5 (v3.66.1517) by reading tools/decomp/import_graph_gate.py: main() prints "If intended, re-freeze in the SAME cut:" followed by `python3 tools/decomp/import_graph_gate.py --update`. Both halves are wrong. (1) Re-freezing in the SAME cut is exactly what COMMON.md forbids: a baseline frozen before the rebase is re-derived after it and paid twice. The drift figures 4331 -> 4352 -> 4364, twenty-one edges under a cut that never moved, are RECORDED IN PM-B-RULING-N7-gate-remedy-20260907T030913Z where they were measured twice independently; they are cited here, not re-derived here. The contract's remedy is rebase FIRST, then re-freeze ONCE. (2) `python3` is a bare interpreter and the tool pin forbids it on every host; the same file's module docstring and its missing-baseline message print the same bare form. This text is visible in a LANDED tree, so every worker who reads the gate's own remedy is told to do the wrong thing. ACCEPTANCE: the printed remedy names rebase-then-re-freeze-once and the pinned interpreter, and a test asserts the EXACT remedy string the gate emits and NAMES the contract heading it enforces, so the next drift between tool text and contract FAILS rather than being copied by a worker. RED: assert the emitted remedy string on this tree -- it reads "re-freeze in the SAME cut" and a bare python3. NEGATIVE CONTROL: the gate's verdict lines and exit codes are byte-for-byte unchanged. DENOMINATOR: rg -n -e "re-freeze" -e "python3 tools/" tools/decomp/import_graph_gate.py -- 10 matching lines on 142133f5, of which the remedy print is the one this row is about. TIER: T1. DEPENDENCY: none. FILED by bd-worker-b2 per ORDER-1513 O4, text from PM-B-RULING-N7-gate-remedy section 4. |
| 796 | OPEN | THE-ROW-675-LITERAL-VALUE-EXEMPTION-LANDED-IN-THE-DETECTOR-AND-NEVER-REACHES-THE-DOM-TEXT-PATH -- T2, the SCRUBBER half of row 675. ROW 675 STAYS OPEN and this row does not close it; closing 675 on the detector half alone is the 1499 false-closure shape. MEASURED on main 142133f5 (v3.66.1517) by reading both functions, not predicted: v3.66.1513 added _JS_LITERAL_VALUE to capture_artifact_redact._value_findings, which is the SCANNER predicate, so a minified literal assignment is no longer FLAGGED. The REWRITER is a different pass and was not touched -- capture_artifact_redact.redact_value step 4 substitutes PLACEHOLDER for every _KV_PAIR_RE match whose key satisfies _kv_key_is_secret and never consults _JS_LITERAL_VALUE, which occurs exactly twice in that module: its definition and the one use inside _value_findings. dom_analyzer.redacted_dom reaches that pass through redact_artifact as its layer 2, so the workbench still rewrites k=0 to the placeholder for exactly the two shapes the same release certified are not secrets. ACCEPTANCE: the two in-tree pins in tests/test_row675_minified_js_is_not_a_secret.py are the starting assertions. test_the_workbench_still_rewrites_the_literal_script_it_no_longer_flags today asserts the literal script is ABSENT from redacted_dom's html and that "scrubbed" is present; the follow-up INVERTS it, so a _LITERAL_VALUED script survives the emitted html verbatim. test_an_identifier_valued_minified_assignment_still_trips_the_detector stays green UNCHANGED, which is what proves the widening reached the rewriter WITHOUT reaching identifier-valued assignments. RED: redacted_dom on a script node whose text is for(var k=0;k<n;k++){} returns html carrying the placeholder in place of k=0. NEGATIVE CONTROL: a real kv secret in DOM text is still rewritten. DENOMINATOR: the six shapes already parametrised in that file -- _LITERAL_VALUED (2, must survive the rewriter) plus _IDENTIFIER_VALUED (4, must still be scrubbed and must still trip the detector). TIER: T2. DEPENDENCY: none. FILED by bd-worker-b2 per ORDER-1513, which names this row as mine and not b3's so it is neither filed twice nor not at all. |
| 797 | OPEN | THREE-LOGIN-CALL-SEAMS-ARE-CAUGHT-BUT-NOT-PINNED -- the keeper callback's `_login.do_login` in bulk_downloader/app.py, the runner's `do_login` in bulk_downloader/runner_auth.py, and `_login._looks_authenticated(cookies)` in bulk_downloader/dev_suite/capture_diag.py are each guarded only as a side effect of tests written for other subjects (test_row667_login_attempt_accounting.py::test_both_real_login_callers_spend_the_same_accounting_denominator and test_u17_cookies.py::test_auth_cookie_test_accepts_several_substantial_cookies). MEASURED 20260907 by bd-integrator-a2 on a detached clone at a8267e5d with bd-mutate: replacing each callable with a lambda is CAUGHT 3 of 3, 0 escaped, 0 invalid -- so this is NOT a live escape and NOT a defect. It is a missing durable pin: no tests/mutants spec names these three seams, so an edit to either catcher could stop defending them with nothing turning red. EVIDENCE: bd-persist/verdicts/rowtest2B-INTEGRATOR-a2-SUPERSEDED.md section 7.3, and the three ready-made specs in the superseded cut at bd-review-wt/rowtest2B-cx-cx-b (test2b_do_login_keeper_seam.json, test2b_do_login_runner_seam.json, test2b_cookie_delta_diagnostic_seam.json) whose anchors ALL RESOLVE EXACTLY ONCE against main's current text and can be lifted as they stand. ACCEPTANCE: the three specs are tracked in tests/mutants and each mutant is CAUGHT with its named catcher, with a transform control that ESCAPES. TIER: T1 -- test-surface only, no product behaviour changes. DEPENDENCY: none. TEXT by bd-integrator-a2, filed unchanged except for ONE correction the carrier measured before filing: a2 wrote the third seam's path as dev_suite/capture_diag.py and the tracked path is bulk_downloader/dev_suite/capture_diag.py, where _looks_authenticated is called in the auth_cookie_test D-29 diagnostic. The carrier re-confirmed all three seams, both named catchers and the three lift-ready specs still present at 142133f5 before filing. The id is the CARRIER's, per the 0544Z allocation order: a2 proposed 795 provisionally and flagged the race itself; 795 and 796 were already taken by this same car. |
| 799 | OPEN | tests-test_v3_66_726_body_contract-SPLITS-BY-RUNNER-SO-ITS-BLOCKING-FOOTGUN-CANNOT-BE-SHARDED -- T2. THE RESIDUAL of row 798, filed as its own row because 798's gate closes with 726 held out under a named exemption rather than fixed. MEASURED on clean a8267e5d by bd-w-a1 (FINDING-five-blocking-footguns): pytest reports 12 passed for that file while run_tests.py reports 10 of 12 -- the SAME file, two runners, two answers. Adopting a suite that splits by runner into a BLOCKING lane makes that lane flaky, and a flaky blocking gate gets disabled by the third person to hit it, so it was exempted deliberately and the exemption is pinned in code with four tests and dies with its cause in both directions. SUBJECT: why the two runners disagree on that file -- which two of the twelve differ, and whether the cause is collection, ordering, environment or a genuine behavioural split. ACCEPTANCE: the disagreement is explained with both runners' output side by side and named test ids, the cause is fixed or the file is split so that what is shardable is sharded, 726 is then ADOPTED into a CI shard, and the _EXEMPT entry in tests/test_footgun_detectors_are_executed_by_ci.py is DELETED -- its own spent_exemptions() check fails while a sharded file is still listed, so the row cannot be closed with the exemption left behind. DENOMINATOR: the twelve test ids in tests/test_v3_66_726_body_contract.py, enumerated with --collect-only under BOTH runners rather than from this row. DEPENDENCY: none -- 798's gate ships with the hole held to exactly one entry. |
| 800 | OPEN | THREE-TRACKED-MUTATION-SPECS-ANNOUNCE-A-TRANSFORM-CONTROL-IN-A-MUTANT-LABEL-WHILE-DECLARING-NONE-AND-TWO-CANNOT-DECLARE-ONE-WITHOUT-INVERTING-THEIR-REGRESSION-MUTANTS -- T2. H89 gives bd-mutate a declared-control concept at SPEC level: a spec is a control when its basename ends _transform_control.json or it carries top-level control_spec true, and inside a declared control the grading inverts -- ESCAPED becomes CONTROL-ESCAPED at exit 0, CAUGHT becomes CONTROL-CAUGHT at exit 1. THREE tracked specs carry a mutant whose LABEL announces a transform control while the SPEC is neither suffix-named nor field-declared, so for them the H89 condition SURVIVES the cut: the row still prints direction regression, a named catcher, verdict ESCAPED and exit 1, byte-identical to a genuine uncaught mutant, which is exactly what H89 exists to end. MEASURED at dd959706 and re-checked present at dbff6581: tests/mutants/row649_recovered_commits.json, tests/mutants/v3_66_1098_assertion_gate.json, and tests/mutants/w2_toolchain_row612_legacy_tests_control.json -- the last named *_control.json, ONE WORD short of the suffix the tool recognises, and a near-miss is worth naming because the next author will choose the same name for the same reason. THE FIRST TWO ARE MIXED, a control mutant sitting beside regression mutants in one spec, and because control-ness is declared at SPEC level, declaring them would invert the grading of their regression mutants too; THEY MUST BE SPLIT, which is a different problem with a different denominator and was correctly left out of the H89 cut rather than folded into it. ACCEPTANCE: each is SPLIT so its control mutants live in a declared control spec and its regression mutants stay in a regression spec, or (for the third) renamed to the recognised suffix; RED first in every case -- run the spec under the pre-split tool and record the verdict word and exit code, then after, asserting CONTROL-ESCAPED at exit 0 for the control half and unchanged CAUGHT grading for every regression mutant that rode beside it. NEGATIVE CONTROL, and it is the one that matters: prove the regression mutants in the SAME original spec did not silently invert, because a split that turns a real escape into a green control is this row's defect in the opposite direction. DENOMINATOR: all tracked tests/mutants/*.json enumerated with git ls-tree (NOT git ls-files, which reads the index and counts a worktree's own new specs -- that is how the H89 cut published 161 of 416 where the tree says 160 of 408 at dd959706, 168 of 428 at dbff6581), parsed rather than grepped, partitioned into suffix-declared, field-declared and label-only. RE-DERIVE IT; do not take these three on trust. SCOPE, AND IT IS NOT ROW 788's: row 788 owns tests/mutants/row708_no_nav_login_is_not_success.json, whose M10 is still inline at dbff6581 (10 mutants, verified), and that spec is EXCLUDED here. But 788's wording -- NO other spec declares a control inline, 708b's is the only one, measured against 337 specs -- IS FALSIFIED AT THE CURRENT TREE: these three are the same shape and 788's population has since grown to 408 at dd959706 and 428 at dbff6581. Neither row is a duplicate of the other; together they are four instances of one class, and 788's uniqueness sentence should not be relied on by whoever takes either. DEPENDENCY: H89 must land first -- there is no declaration mechanism to adopt before it. EVIDENCE: bd-persist/B3-F09-H89-PREP-20260907T1435Z.md section 6, and bd-persist/objects/row-h89-mixed-specs-draft.md. |
| 801 | OPEN | THE-ROW-744-GUARD-PINS-FOUR-SUBSTRINGS-RATHER-THAN-EITHER-CONDITION-OF-THE-RULING-IT-CERTIFIES -- T1. MEASURED 2026-09-08T20:43:39Z by bd-panel-b1 on origin/main d69ffcda+ while judging rowtoolchain-receipts-cx-toolchain (rows 744-747 CLOSED @1507), and the premise RE-VERIFIED by bd-integrator-c on origin/main 03748ab8 (v3.66.1525) by reading the test itself, not the verdict. WHAT THE GATE CLAIMS TO JUDGE: the 20:3xZ ruling gives bd-guard-declare two conditions -- the dry-run names the DECLARED-REPIN exception, and it never writes guards.json itself. WHAT IT ACTUALLY ASSERTS: tests/test_row744_guard_declare_wording_matches_repin_rule.py test_drift_dry_run_names_declared_repin_and_never_writes_manifest requires rc==0 plus the substrings 'brief', 'row 744' and 'does not write guards.json itself' in the combined stdout+stderr, and the absence of the string 'does NOT write guards.json'; and test_every_current_claim_mentions_declared_repin_exception requires >=3 lines of the TOOL SOURCE containing 'does not write' and each also containing 'CENSUS'/'census'/'row 744'. Every one of those is a substring match on WORDING. THE ESCAPE, MEASURED: mutating the tool's own message literal to 'may repin this guard at will; declaring it is optional' -- which INVERTS the exception the ruling exists to state -- left 2 of 2 tests PASSING; unmutated control 2 of 2; subject restored byte-identical. ESCAPE-CLASS decision. WHY THIS IS THE 722 SHAPE ONE LEVEL UP: the gate pins THAT some approved words appear, not WHICH rule is stated, so the text under it may drift to the opposite meaning while the guard stays green. ACCEPTANCE: the test asserts the ruling's two CONDITIONS -- (a) the dry-run output states the declared-repin exception as a condition and not merely as a token, and (b) guards.json is byte-unchanged after the run -- with a bd-mutate mutant on EACH condition CAUGHT, an unmutated control passing, and the subject restored byte-identical; wording-only edits that preserve both conditions must stay GREEN (negative control). NOT CLAIMED: the write-refusal leg is real and already asserted by sha256 comparison in the same test -- only the WORDING leg is unpinned, and the row does not ask for that assertion to be replaced. SOURCE: /home/mboyle/bd-review-wt/rowtoolchain-receipts-cx-toolchain/.review/VERDICT-panel.md ; bd-persist/panel/followup-rows.md item 44. |
| 802 | OPEN | THREE-bd-bandcheck-TESTS-ARE-SATISFIED-BY-A-MESSAGE-PRINTED-TWICE-BECAUSE-NONE-OF-THEM-ASSERTS-AN-EXACT-COUNT -- T1, pre-existing. MEASURED BY THE REGISTRAR on origin/main 03748ab8 (v3.66.1525) by reading the landed file, not the verdict. tests/test_row677_bandcheck_missing_is_unknown.py carries four tests and ZERO occurrences of `.count(`. Its three behavioural tests assert membership only: test_missing_target_is_unknown_and_names_unmeasurable_input asserts "MISSING '<target>'" in output and 'could not be measured' in output; test_unsafe_directory_remains_a_measured_finding asserts 'UNSAFE' in output and 'whole tests/ dir' in output; test_present_safe_file_remains_safe asserts 'all targets safe to band' in output. A substring assertion is satisfied by one occurrence, by two, or by twenty, so a bd-bandcheck that reported every finding TWICE would keep all three tests green. THIS IS ONE OF THE FOUR SHAPES CLAUDE.md A3 NAMES AS HAVING SHIPPED between v3.66.1223 and 1238 -- 'a mutation anchor resolving zero or two times, four times over' -- which is why an exact count and not a substring is the contract. PROVENANCE, and it is a RESIDUAL not a reopening: rows 744-747 are CLOSED @1507 and that closure is CORRECT. The stale candidate rowtoolchain-receipts-cx-toolchain also hardened these three tests with five exact-count assertions and that part never landed; bd-adjudicator-B's ruling ADJ-B-20260909T032149Z section 3 ruled the candidate's NEW test a duplicate of the landed test_delegated_unsafe_and_missing_targets_refuse_once and this hardening a residual owed against main. DO NOT REAPPLY THE CANDIDATE. ACCEPTANCE: each of the three tests asserts output.count(<its own needle>) == 1 for every needle it currently checks with `in` -- five assertions across the three tests -- and the assertion is on the count, not merely that the needle is present. RED on the unmodified tree: make bd-bandcheck emit each finding twice (or feed the parser a doubled transcript) and the three tests still PASS today; after the fix they FAIL, naming the count. NEGATIVE CONTROL: a single-occurrence run stays GREEN. POSITIVE CONTROL FOR THE MEASUREMENT ITSELF: `.count(` is present in 191 files under tests/, so the zero in this file is a real absence and not a dead grep. TIER: T1 -- one test file, no runtime subject, no mutation battery owed. DEPENDENCY: none. NOT CLAIMED: I did not run bd-bandcheck doubled; the RED above is the row's instruction to its worker, not a measurement I made. SOURCE: bd-persist/RULING-ESC-cx-trainer1-BISECT-IS-CLEARED-and-TWO-CLOSED-ROWS-OWE-RESIDUALS-THAT-LANDED-UNDER-OTHER-FILENAMES-ADJ-B-20260909T032149Z.md section 3 ; referred by bd-cx-trainer1 in bd-persist/cx-trainer1-residuals.md. |
| 803 | OPEN | bd-tool-lint-REPORTS-clean-true-AND-EXIT-0-IN-EVERY-MACHINE-READ-OUTPUT-MODE-OVER-A-CORPUS-MEMBER-ITS-OWN-PAYLOAD-RECORDS-AS-UNREADABLE -- T2, pre-existing, SHIPPED. PROPOSED BY bd-cx-review-correctness1 (cx-correctness1-toolchain-static/followup.md, subject blob 29e9330832f1e3ff5c50ef1ad387ccb95e7b52ab); PREMISE RE-VERIFIED BY THE REGISTRAR ON origin/main 03748ab8 (v3.66.1525) BY RUNNING THE TOOL, not taken from the referral. MEASUREMENT: a scratch --bin fixture of three executable stub tools with one at mode 000. `bd-tool-lint --bin <fix> --no-runtime --json` exits 0 and returns clean:true with read_tools:2, tools:3 and unreadable carrying "[Errno 13] Permission denied: .../bd-gamma" -- the tool KNOWS the member was never read and calls the corpus clean anyway. POSITIVE CONTROL, same fixture with the member restored to 755: exit 0, clean:true, read_tools:3, unreadable:[]. THE EXIT CODE AND THE clean FLAG ARE IDENTICAL IN BOTH RUNS, so a machine consumer reading either signal cannot distinguish a fully-read corpus from one third of it. `--corpus-debt` exits 0 on both runs likewise. The text mode DOES react (rc 2 unreadable vs rc 3 readable) -- CONFOUND NAMED: both text runs are nonzero because the fixture's stub tools are uncategorized, so the text evidence here is the rc 2-vs-3 SPLIT, not the nonzero itself; the json/corpus-debt evidence is confound-free because those modes return 0 in both. WHY IT MATTERS BEYOND ONE TOOL: bd-tool-lint is the instrument that detects fail-open in other tools, and it fail-opens in the mode a machine reads. CLAUDE.md A7: 'make unavailable measurement return UNKNOWN rather than OK'. NOT A DUPLICATE, checked by the registrar against both populations: row 470 (OPEN, FORTY-THREE-TOOLS-REPORT-GREEN-OVER-A-TREE-THAT-NEED-NOT-EXIST) is a require_corpus() guard ABSENT IN OTHER TOOLS; this row is bd-tool-lint's OWN output branches returning before its OWN unreadable refusal. Row 160 is CLOSED @1172. Row 737 (deploy.sh reports a present-but-unreadable hook as ABSENT) is the same shape class on a different subject. ACCEPTANCE: every output mode -- text, --json, --corpus-debt, and the --gate/--ratchet paths -- refuses an unreadable corpus member with the cannot-evaluate exit rather than 0, and --json sets clean:false while PRESERVING the discovered/read/unreadable counts and the structured payload, so the refusal is diagnosable and not merely louder. RED: the mode-000 fixture through all three modes, asserting the exact rc for each and asserting clean is false with unreadable length exactly 1 and read_tools exactly 2 of tools exactly 3. GREEN control: the same fixture at 755 exits 0 with clean:true, unreadable [] and read_tools exactly 3. MUTATION: remove the unreadable refusal independently in EACH output branch and require that branch's own test to go red -- one shared test passing for all three branches is the defect restated; carry a bd-mutate DECLARED transform control that loads the same subject. Assert the --gate/--ratchet interaction cannot let an unrelated policy refusal mask the unreadable classification. EVIDENCE: /home/mboyle/bd-persist/cx-correctness1-toolchain-static/ (runtime.json, main-runtime.json, named stdout/stderr) plus the registrar's independent re-run recorded in register-handoff/VERIFIED-803-bd-tool-lint-unreadable-20260909T0440Z.md. DEPENDENCY: none; independent of row 470. |
| 804 | OPEN | THE-TEMPLATE-SANDBOX-BROWSER-MODE-STILL-FOLLOWS-A-REDIRECT-TO-THE-METADATA-ADDRESS-BECAUSE-THE-PIN-MAPS-ONE-HOSTNAME-AND-NOTHING-CHECKS-THE-HOP -- T3 SECURITY. Sibling of 728 and the residual half of 779 (CLOSED @1521). PROPOSED by bd-panel-B2-B 2026-09-09T03:3xZ (panel/followup-rows.md section 779-R1; verdict bd-review-wt/row779-sandbox-rebinding-local/.review/VERDICT-panel.md), confirmed by bd-adjudicator-B 03:3xZ. Id assigned by bd-integrator-A (IDS-ASSIGNED-integrator-A-20260909T034531Z.md) and ADOPTED by the registrar; text is the proposer's, reflowed to one line with three literal pipe characters in a grep pattern replaced by slashes because a pipe ends a table cell -- no wording changed. MEASURED on origin/main 03748ab8 (v3.66.1525), exact counts, each grep hit read so comments are out of the denominator: bulk_downloader/app_template.py calls _is_safe_public_host exactly TWICE -- pre-fetch, and inside _GuardedRedirect which is the urllib branch only. Real page.goto sites: exactly 1; the two other grep hits are comments. page.route and .abort in app_template.py: 0. In bulk_downloader/cloak.py, page.route / request.abort / SSRF / _is_safe_public_host: 0 hits; cloaked_page forwards args= to launch and installs no interception. THE HOLE: row779's fix emits --host-resolver-rules=MAP <host> <literal>, which pins ONE hostname at launch. A 302 from that vetted host to http://169.254.169.254/latest/meta-data/ is an IP LITERAL: no DNS, not covered by the map, and page.goto follows it. page.content() and page.url are then returned to the caller, and no host check runs between the goto and the return. THE ASYMMETRY IS THE ARGUMENT, not a theory about Chromium: the httpx sibling closes this with an httpx request event hook that fires on EVERY outgoing request including redirect follow-ups (provider_resolve_impl/_common.py, the SSRF guard redirect hook); the urllib sibling closes it with _GuardedRedirect. Three sibling paths onto one trust boundary, two guarded on redirect, one not. NOT A DEFECT INTRODUCED BY 779: the gap pre-dates it, and 779 scoped itself honestly -- its test module docstring names _GuardedRedirect as what closes HTTP mode, says browser mode had neither, and then supplies only the pin. DOCSTRING CLAUSE, part of the row: app_template.py's pin comment says this is the same policy for the browser as _SSRFGuardedTransport; against a transport that re-validates every hop that is overstated, and the wording is corrected in the same cut. NOT A DUPLICATE, checked by the registrar across both register populations: row 779 (CLOSED @1521) is rebinding BETWEEN THE CHECK AND THE GOTO and contains the word redirect ZERO times; row 728 is the three urllib seams resolving twice; row 750 is an IPv6 ENCODING of the metadata address; row 703 is the httpx construction population. ACCEPTANCE: a redirect from a vetted public host to a private/link-local/metadata address in mode=browser is REFUSED, in the structured shape the other two paths use, with the LOOPBACK exemption preserved. RED-first on the defective parent, driven through the existing row779 fixture browser (tests/test_row779_template_sandbox_browser_mode_pins_the_vetted_address.py already ships a Chromium stand-in honouring the MAP subset) extended to simulate one 302 hop -- no network, no browser binary. RED: the route returns ok:true carrying the redirect target's body and final_url naming the metadata address. GREEN: refused, and the reason names the hop. NEGATIVE CONTROLS, all three: a redirect between two PUBLIC hosts still succeeds; a redirect to 127.0.0.1 still succeeds (the deliberate selector-authoring exemption survives); and the existing 13 row779 rebind tests stay green -- the hop check does not replace the pin, both are needed. DEPENDENCY: none; row 728 is the urllib seams and does not cover this. |
| 805 | OPEN | BD-SSRF-GUARD-DENOMINATOR-IS-httpx-ONLY-SO-ABOUT-TWENTY-urllib-AND-requests-EGRESS-SITES-ARE-OUTSIDE-THE-POPULATION-THE-GATE-CALLS-EVERYWHERE -- T2 SECURITY RESIDUAL. PROPOSED by bd-review-shape-B1 2026-09-09 from row703 (bd-persist/rows-to-file.md, the T2 security residual section). Id reserved by bd-integrator-A (IDS-ASSIGNED-integrator-A-20260909T034531Z.md) and ASSIGNED by the registrar; text is the proposer's. MEASURED on the row703 tree (base aa386213): bulk_downloader/ carries 12 requests imports and 167 urllib.request imports; about 20 egress call sites are invisible to tools/ssrf_client_census.py -- ai_provider.py 4, hooks.py 6, cli_dashboard.py 3, and aiassist.py, webhooks.py, tpdb.py, app_health.py, ollama_boot_probe.py, dev_suite/capture_diag.py, selector_playground.py 1 each. Of those ten files ONLY selector_playground.py carries any SSRF check (8 hits, _host_public re-validated at each redirect hop) -- and it is CHECK-THEN-CONNECT, so it still has the DNS-rebinding window that bulk_downloader/ssrf_transport.py exists to close. NOT A DEFECT IN row703, stated so it is not read as one: that cut only ADDS refusals and its gate docstring scopes itself honestly to every httpx client BD constructs. But the file is NAMED tests/test_row703_ssrf_transport_is_installed_everywhere.py and its DONE.md names the excluded population ZERO times (grep for urlopen / urllib / requests / aiohttp = 0 each), so the next reader will take everywhere literally. REACHABILITY, measured rather than assumed: the remaining nine take operator-configured or fixed URLs (127.0.0.1 ollama, a fixed tpdb API base, a configured webhook), so this is NOT attacker-reachable today on a single-operator install -- which is why it is a row and not a bounce. NOT A DUPLICATE, checked by the registrar across both register populations: row 703's population is explicitly the 31 httpx.Client and AsyncClient CONSTRUCTIONS across 21 files, so closing 703 in full leaves every site named here untouched; row 728 is three specific urllib seams resolving twice, not the census denominator; row 749 is driving the guarded transport into three deep handlers. ACCEPTANCE: the SSRF census defines its population as EVERY egress transport BD uses -- httpx, urllib.request and requests -- derived FROM THE TREE and asserted nonzero, returning UNKNOWN rather than OK when it cannot measure it; each site outside the guarded transport is either routed through it or carries a written, in-tree reason naming why it cannot be; and the gate's name and docstring state the population they actually cover, so everywhere is either true or not claimed. RED: add an unguarded urllib.request egress site and have the census fail naming it -- the census must fail on a NON-httpx site, since it already passes on this tree with about 20 of them present. NEGATIVE CONTROL: a site carrying a written exemption reason does not fail the gate, and deleting the tree-side denominator is CAUGHT with a transform control that ESCAPES. DEPENDENCY: none, but its authored paths must not overlap the rows687-703 lineage, which owns ssrf_transport.py -- two patches sharing a path ride different trains. |
| 806 | OPEN | THE-CLOAK-STATE-A-DEPLOY-MEASURES-NEVER-REACHES-api-health-SO-THE-FLEET-HEALTH-COLUMN-STILL-READS-GREEN-ON-A-DEGRADED-HOST -- T1. THE NAMED REMAINDER OF ROW 736, which closes PARTIAL in the same commit; this row is not a reopening of it. MEASURED BY THE REGISTRAR on origin/main 03748ab8 (v3.66.1525), 2026-09-09T05:4xZ, statically over the landed blobs. WHAT ROW 736 ACTUALLY GOT, and it is real: scripts/deploy.sh sets CLOAK_STATE at six sites, builds SUMMARY_CLOAK once and appends it to BOTH terminal summaries (ALREADY CURRENT and DEPLOY OK), and its health STEP prints `cloak browser capability (optional): cloak=$CLOAK_STATE` in every state including OK. tests/test_deploy_script.py::test_cloak_state_is_reported_at_health_summary_and_record exists at line 1204 and the worker ran it: 3 passed in 3.82s. WHAT IS STILL MISSING: that is deploy.sh's OWN STDOUT, read once by whoever watched the deploy. The /api/health PAYLOAD does not carry cloak at all -- bulk_downloader/app_health.py 0 case-insensitive occurrences, bulk_downloader/healthcheck.py 0. POSITIVE CONTROL on the same file so the zero is a real absence: app_health.py does carry version and credential_vault_locked (lines 349, 400, 472). WHY THAT IS THE HARM ROW 736 NAMED RATHER THAN A TIDINESS POINT: the fleet health column is a curl of that payload and nothing else -- bd-fleet-deploy.sh curls http://127.0.0.1:$HPORT/api/health at lines 438, 456, 477, 491 and 520, and bd-fleet-audit-cmd.sh at line 14. A host that deployed with cloak=ABSENT answers /api/health identically to a host with cloak=OK, so the column reads GREEN on a degraded host, which is row 736's sentence word for word. The operator-visible signal exists exactly once, in a terminal that has scrolled away. CLAUDE.md A7: a surface that cannot distinguish a degraded reach from a verified one is the fail-open shape, not a cosmetic gap. ACCEPTANCE: the deployed capability record deploy.sh already writes beside the graph pin (cloak=<state>, read back and verified at deploy.sh lines 1273-1281) is READ by the health endpoint and exposed as a named field in the /api/health payload, with ABSENT and UNKNOWN distinguishable from OK and from a missing record; a host that has never recorded one answers with the field explicitly UNKNOWN rather than omitting it, because an omitted field reads as healthy. RED on the unmodified tree: request /api/health on a fixture whose capability record says cloak=ABSENT and assert the payload names it -- fails today, the key does not exist. GREEN: the field is present and equals ABSENT. NEGATIVE CONTROLS: cloak=OK yields OK and does not turn the endpoint 503 -- row 686 mandates exit 0 on WARN and this row does not disturb that, a degraded cloak is a NAMED degradation and never a failing health verdict; and a host with NO capability record answers UNKNOWN, not OK, and not absent. EXACT-COUNT ASSERTION: the field appears exactly once in the payload. NOT A DUPLICATE, checked across both register populations: row 736 is the deploy-side flag and summary qualifier and is satisfied; row 686 is exit-0-on-WARN and is the reason this row must not fail health; row 737 is deploy.sh reporting a present-but-unreadable HOOK as absent, a different subject on the same script. DEPENDENCY: none. TIER: T1. EVIDENCE: register-handoff/VERIFIED-736-cloak-close-is-PARTIAL-20260909T0540Z.md ; worker return fleet-run-artifacts/codex-remote/rows736-737/DONE.md ; order bd-PM-A O16-1. |
| 807 | OPEN | outcome-collapses-on-a-zero-denominator-stats-mapping -- DIAGNOSTIC. The outcome function collapses on a zero-denominator stats mapping and returns something BYTE-IDENTICAL to the record of a clean run in which nothing failed, while the function's own docstring promises never a clean-looking empty record. ACCEPTANCE: empty stats, or stats whose only key is the empty string, is recorded False with counts and failed NULL -- exactly as the no-stats branch already does. CLASSED DIAGNOSTIC, and the classification is the load-bearing part: the shape lens could NOT reach the defect through the production seam, because the conftest writes only when chains are present and chains exist only once an item has begun. THE LENS MEASURED THAT UNREACHABILITY RATHER THAN ASSERTING IT AND THE ADJUDICATOR ACCEPTED THE CLASSIFICATION, so this row records a diagnostic-only finding and makes NO CLAIM that the seam is reachable in production. IT IS THE FIX WEARING THE DEFECT'S OWN SHAPE, which is A7's standing warning, and that is why it is filed rather than waved through. THIS ROW ALSO MAKES NO CLAIM THAT THE BEHAVIOUR REMAINS PRESENT TODAY AND NONE THAT IT IS IMPLEMENTED: the source finding still awaits its own current disposition. PROVENANCE: id assigned 2026-09-09T04:12:00Z by bd-integrator-A at the adjudicator's request off the row753-clause3 ruling section 3, TIER T1, wording proposed by the shape lens and carried forward unrewritten. The pre-existing silence noted alongside it rides with this row. DEPENDENCY: none. Implementation is NOT dispatched during the drain. |
| 808 | MOOT @1534 | downloader-spec-silently-omits-an-absent-frontend-dist -- MOOT BY PRODUCT RETIREMENT. The PyInstaller spec drops any absent data directory silently, testing only whether the source path exists, and frontend/dist IS ABSENT IN A FRESH CHECKOUT, so a build would ship WITHOUT the SPA. A6 is explicit that missing generated or frontend artifacts are FAILURES, not permission to omit them. THIS IS A DISTINCT SUBJECT FROM ROW 687: 687 is about a file the tree does not carry, and this is about what that file DOES once it exists. WHY IT IS MOOT AND NOT DELIVERED: the spec is the retired Windows developer installer's build recipe, and the operator has retired the Windows application, so this subject is withdrawn with its product. MOOT BY RETIREMENT IS NOT IMPLEMENTED, NOT LANDED, NOT BOARD AND NOT DELIVERED -- the behaviour was never corrected and no gate was added; the product that would have exhibited it is gone. CARRIED AS AN HONEST UNKNOWN AND NOT AS A FINDING: nobody has proven that spec builds at all, only that its inputs resolve, because PyInstaller is not installed and no lens was required to install it. THAT STAYS UNKNOWN AND UNKNOWN IS NOT PERMISSION. NOT COVERED BY THIS ROW, AND NOT MOOTED BY IT: the release packaging path's own required-present manifest check for frontend/dist is a SEPARATE subject on a supported path, and this retirement says nothing about it. PROVENANCE: id assigned per the adjudicator's ruling of 2026-09-09T11:37:06Z, its section 3 and item b, off cx-24's note 4; retirement per O289, O292, O295 and O296. Source provenance and the retired object row687-night-20260909-cx-24 are PRESERVED UNMODIFIED. DEPENDENCY: none. |
| 809 | OPEN | supported-SOCKS-runtime-paths-depend-on-an-undeclared-optional-httpx-dependency. The supported SOCKS proxy path constructs a guarded transport through httpx, and requirements.txt declares httpx WITHOUT the socks extra, so a clean environment installed from the shipped requirements raises an optional-dependency ImportError on that path. SCOPE: capability declaration for the EXISTING SOCKS proxy path. This row adds no proxy feature and changes no proxy policy. EVIDENCE: the adjudicated path from the VPN runtime through the transport and manual-runner layers to guarded SOCKS construction, measured on the base and on the row703 candidate in a clean environment with socksio absent; and the existing isolated clean-venv proof under bd-persist/cx-restart-20260909/row703/release/revision2/socksio-failure/source-correction/RESULT.json showing that declaring the socks extra enables actual guarded SOCKS construction WITHOUT NETWORK. THE FIXTURE-ONLY row703 CORRECTION DID NOT FIX THIS: it repaired a test's substitution frame, while the runtime declaration defect is PRE-EXISTING AND UNTOUCHED. ACCEPTANCE: a clean environment installed from the shipped requirements can construct the application's supported guarded SOCKS transport without an optional-dependency ImportError; proxy policy enforcement remains intact; and dependency version bounds and pins are UNCHANGED unless separately authorized. Choose the SMALLEST capability declaration consistent with supported SOCKS behaviour. NEGATIVE CONTROL: the missing-dependency reproducer must keep failing in an isolated environment, so the fix is proven to be the declaration and not the environment. THREE OUTCOMES: an environment whose dependency state cannot be determined is UNKNOWN, never OK. NO LIVE SITE, NO PROXY LOGIN AND NO RUNTIME NETWORK IS REQUIRED FOR THIS PROOF, and none is authorized by this row. RESTRAINT: do not install the optional dependency into shared CI or reference environments to make the failure disappear -- that destroys the only negative control the fleet has for this defect. DEPENDENCY: none for filing. The existing authored one-line dependency change is RETAINED EVIDENCE, NOT CLEARED RELEASE CONTENT, and no implementation is dispatched during the fresh-work pause. |
