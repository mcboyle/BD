# Bulk Downloader v3.0 — runtime dependencies
# Install: pip install -r requirements.txt
# Then:    playwright install chromium

flask>=3.0,<4.0
# Direct imports, not Flask implementation details.  These exact versions are
# the versions exercised by this repository's venv; no unmeasured range is
# implied by declaring them explicitly.
requests==2.34.2
Werkzeug==3.1.8
# Playwright pinned <2.0 because the .spec hard-codes paths under
# playwright._impl.* — a 2.x major bump may reorganize those.
# MOD-6 (v3.66.678): floor raised to 1.61 to unify the service venv (1.61.0) with
# the sandbox prestage (was 1.60.0) — both satisfied the old >=1.45 range, hiding
# the cross-environment drift. dep_freshness now flags anything below the floor.
playwright>=1.61,<2.0
# httpx is used for direct HTTP downloads (faster, with progress and resume).
# If unavailable, the worker falls back to Playwright's save_as.
httpx>=0.25,<1.0

# Template discovery and deep-detection paths parse captured HTML with
# BeautifulSoup. These paths fail open when bs4 is absent, but that silently
# disables selector fitness scoring on a clean install.
beautifulsoup4>=4.12,<5.0

# THE lxml / cssselect CENSUS BELOW IS AST-DERIVED, AND IT IS A CORRECTION.
# It comes from ast.parse over every tracked file the gate can read as Python
# -- 2577 at v3.66.848: 2108 ending in .py, PLUS 469 extensionless
# python-shebang bd-* scripts under toolchain/ and project-knowledge/ that the
# earlier `git ls-files -- '*.py'` denominator could not see at all (all
# parsed, zero SyntaxError), reading Import and
# ImportFrom nodes. The first version of these notes was derived BY GREP and
# was wrong in BOTH directions at once, which is precisely the trap CLAUDE.md
# section 1 names ("Grep is not a denominator"):
#   MISSED     bulk_downloader/synthetic_tests.py:96 -- a real fail-open lxml
#              importer, named in no prose on the branch that declared lxml.
#   FALSE HIT  bulk_downloader/diagnostics_bundle.py:130 -- NOT an importer.
#              "lxml" there is a string in a tuple of optional-dependency NAMES
#              that _capture_versions() feeds to __import__ for a version
#              report. Grep sees that line; AST does not, and AST is right.
#              (Reporting availability and declaring an install floor are
#              different jobs; that site is correct as written.)
# Do not re-derive this list with grep.
# tests/test_v3_66_653_dep_freshness.py::test_third_party_imports_are_declared
# re-runs the AST walk against this file on every band, so deleting a pin below
# is a test failure rather than a silent capability loss.

# v3.66.848: lxml. SAY WHICH DENOMINATOR THE COUNT IS OVER. The walk above
# covers all 2577 tracked Python files; the runtime census below covers
# bulk_downloader/ (565 of them), and the first version of this note mixed the
# two -- it said "All three" directly under a sentence claiming every tracked
# file, which is the same denominator slip the census itself was correcting.
# The denominator grew from 2108 to 2577 at the third repair (the 469
# extensionless scripts); the lxml count did NOT change, because none of them
# imports it -- re-measured, not assumed.
#   FOUR importers over the stated 2577-file denominator:
#     bulk_downloader/accessibility.py:185   ARIA audit
#     bulk_downloader/selector_playground.py:56  XPath selector evaluation
#     bulk_downloader/synthetic_tests.py:96  synthetic selector check
#     tests/test_v3_66_320_synthetic_json_path.py:110  <- a test, not runtime
#   THREE of those are runtime, and it is the three that fail open: the audit
# returns a single "Install lxml to run ARIA audit" finding, XPath selectors
# return an error string, and the synthetic check returns
# {"ok": false, "error": "lxml not installed"} -- so a missing lxml degrades
# silently, exactly as the beautifulsoup4 note above describes.
# It was never declared here, which is why `pip check` reported clean while
# tests/test_v3_43_80_modules.py's TestAccessibility and TestSynthetic failed
# on a fresh container: pip cannot see a requirement that was never written
# down.
# NOT pure Python: lxml is a C extension over libxml2. It ships manylinux,
# macOS and Windows wheels, so the common installs need no toolchain, but a
# platform without a wheel will need libxml2/libxslt headers to build.
lxml>=5.0,<7.0

# v3.66.848: cssselect translates CSS selectors to XPath. THREE importers over
# the same stated 2577-file denominator (re-measured after the denominator
# grew, and unchanged) -- the first version of this note said
# "Two", having silently counted a root-level file but not a tests/ one:
#   bulk_downloader/selector_playground.py:67 -- sets _HAS_CSSSELECT, which
#     is_available() reports to the operator verbatim. Without it the
#     playground falls back to _css_to_xpath_simple(), a hand-rolled
#     translator for "the common cases the operator actually types" (tag,
#     .class, #id and simple combinations); anything richer stops matching.
#   audit_templates.py:26 -- at the REPO ROOT, not in bulk_downloader/. It
#     sets _have_cssselect, and check_selector() then skips the
#     cssselect.parse() probe. What it does NOT do is report every invalid
#     selector as valid: that word was wrong. The four structural checks run
#     first and still run -- non-string type, empty/whitespace, [] and ()
#     balance, and quote balance inside each attribute selector -- so what is
#     lost is exactly the grammar probe. The selectors that stop being
#     rejected are the bracket- and quote-balanced but grammatically invalid
#     ones. MEASURED over a 13-selector probe: 7 flip from a cssselect error
#     to valid ("div >> p", "a[href=]", "::", "1abc", "div:", ">div", "*|"),
#     4 are still caught structurally, and 2 cssselect accepts anyway. Still
#     the CLAUDE.md section 0 shape -- a check whose denominator stopped
#     containing part of its subject -- but a proper subset of it, not all.
#   tests/test_v3_66_320_synthetic_json_path.py:111 -- a test, not runtime.
#   Both non-test importers fail open.
# It is also the reason declaring lxml alone is not enough. lxml's
# element.cssselect() is implemented by cssselect and raises ImportError
# without it -- MEASURED here before installing: "cssselect does not seem to
# be installed". That is the PRIMARY path at synthetic_tests.py:96, so a
# cssselect-less install silently routes every synthetic selector through the
# _css_to_xpath fallback. lxml exposes this as its `cssselect` extra rather
# than a hard dependency, so it is not pulled in by the lxml pin above and
# must be declared here.
# Floor 1.2 (2022-10-27, the first py3-only release); current is 1.5.0
# (2026-07-27) and installs into venv as a pure-Python wheel, ~20KB, no build
# step. Ceiling at the next major, matching the neighbours.
cssselect>=1.2,<2.0

# Phase 15.4: TLS fingerprint impersonation. Optional — falls back to httpx
# if not installed. Provides a drop-in `requests`-style API that mimics real
# Chrome's TLS handshake (JA3, ALPN, HTTP/2 SETTINGS) at the wire level —
# defeats fingerprint-based bot detection that classifies based on TLS
# alone, not just user-agent strings.
curl_cffi>=0.7,<1.0

# v3.43.14: Secure password storage.
#
# keyring        - OS keychain access (Windows Credential Manager,
#                  macOS Keychain, Linux Secret Service). The recommended
#                  storage for passwords on systems that have a keychain.
# cryptography   - AES-GCM for the master-password fallback backend (used
#                  when no keychain is available, e.g. headless Linux).
#
# Both have pure-wheel Windows installs. If they fail to install (rare),
# the app falls back to plaintext storage in sites_config.json — same
# behavior as v3.43.13 and earlier — but the migration UI won't appear.
keyring>=24.0,<26.0
cryptography>=42.0,<51.0

# v3.66.681 (B2/P6): OIDC / SSO login. authlib.jose verifies the provider's
# id_token (RS256) against its JWKS. Only needed when SSO is enabled;
# imported function-locally so the app runs without it otherwise.
authlib>=1.3,<2.0

# v3.43.64: MP4 metadata tagging via the mutagen library. Used by
# bulk_downloader/mp4_metadata.py to embed title/performer/album/date/
# comment/cover atoms into MP4 files after download. Pure Python, no
# compilation step, ~600KB installed. Fail-open: if missing, tagging
# is a silent no-op (the file downloads fine without tags).
mutagen>=1.47,<2.0

# v3.62.2: openpyxl powers the XLSX bulk-import template (a formatted
# downloadable workbook with a template-id dropdown) and parsing of
# uploaded .xlsx import files. Pure Python, no compilation step.
# bulk_downloader/csv_bulk.py: CSV import works without it; XLSX
# import/template degrade with a clear error if it is ever absent.
openpyxl>=3.1,<4.0

# v3.63.6: psutil powers the cpu_ram dashboard widget
# (bulk_downloader/app_widgets_api.py::_collect_data → cpu_pct/ram_pct/
# cpu_cores/ram_total_fmt). The widget code soft-imports psutil and
# fails open if it is missing — the response keys are then None, and
# the frontend renders "—% / —%" instead of misleading "0% / 0%".
# Pre-v3.63.6 psutil was documented as optional in doctor.py but not
# pinned anywhere, so fresh installs always missed it and the widget
# always displayed 0% / 0%. Stash diagnosis 2026-05-21 confirmed.
# Pure Python wheel; no compilation step on Linux or Windows.
psutil>=5.9,<8.0

# v3.66.794 (F0.4): production WSGI server. The werkzeug dev server that
# app.run() starts is single-process and crashes under concurrent load
# (Task-E fuzz recon). In production (debug off) downloader_ui serves under
# waitress -- a real threaded WSGI server. Soft/fail-open: imported only in
# _serve_wsgi, and if waitress is absent the app falls back to the werkzeug
# dev server rather than refusing to boot. Pure Python wheel, no build step.
# Size the pool with BD_WSGI_THREADS (default 30): each long-lived /api/stream
# SSE connection pins one worker thread for its life.
waitress>=3.0,<4.0

# Capture and validation must use the real pytest runner on every supported
# install, including production/test-box deployments that install only this
# core manifest.
pytest>=7.0,<10.0
pytest-xdist>=3.6,<4.0
# PEP 440 comparison for tools/check_requirements.py, which imports it
# directly. It arrives transitively via pytest today -- but that is in a
# DIFFERENT manifest than the one deploy.sh checks first, so a fresh venv would
# exit 2 on the first check.
packaging>=23.0,<27.0

# Pillow: project-knowledge/build_navigator.py and build_montage.py import it
# at MODULE SCOPE. It was waived as undeclared-by-design on the grounds that
# those builders were "documentation scratch, run by no lane" -- true until two
# rows made them CI-gated tooling. OPERATOR DECISION 2026-08-27: declare it in
# BOTH manifests, so a base install can actually run the builders rather than
# only a machine that happens to have test extras. The waiver entries in
# tests/test_v3_66_653_dep_freshness.py were retired in the same cut, following
# the psycopg precedent recorded there on 2026-08-10.
Pillow

# ---------------------------------------------------------------------------
# FORMER requirements-optional.txt (merged 2026-09-03, operator decision).
# These were a second manifest that scripts/deploy.sh step [5] never converged,
# so 6 of 12 fleet hosts ran with 5 to 25 of them silently absent (guarded
# imports degrade without a word: dedup's videohash, stealth, push, Postgres,
# every site *_api extractor). One manifest, converged on every deploy, is the
# fix by construction. The six pins that carried no bound are bounded here to
# the versions measured in the fleet venvs (test6, 2026-09-03). The 'graceful
# degradation' comments are kept: absence is still tolerated at import time,
# it is simply no longer the installed state.
# ---------------------------------------------------------------------------
plexapi>=4.18,<5.0

# Subtitle sidecars. subliminal searches providers and writes subtitles;
# babelfish converts operator language codes for those searches.
subliminal>=2.7,<3.0
babelfish>=0.6,<1.0

# Desktop tray icon. Pillow supplies the image support from requirements.txt;
# pystray supplies the platform tray/menu integration.
pystray>=0.19,<1.0

# Terminal dashboard. Enables the live Rich TUI; plain stdout remains the
# fallback when this optional package is not installed.
rich>=15.0,<16.0

# Plugin key/value Postgres fallback. The default remains SQLite; installing
# this distribution provides the legacy ``psycopg2`` import used for an
# explicitly supplied Postgres DSN when the modern psycopg driver is absent.
psycopg2-binary>=2.9,<3.0

# Phase 15.4: TLS fingerprint impersonation. Provides a drop-in `requests`-
# style API that mimics real Chrome's TLS handshake (JA3, ALPN, HTTP/2
# SETTINGS) at the wire level — defeats fingerprint-based bot detection
# that classifies based on TLS alone, not just user-agent strings. App
# falls back to httpx if not installed.
# (curl_cffi: declared above in the core list)

# Phase 16.43: Web push notifications. Both libraries needed together —
# cryptography for VAPID keypair gen, pywebpush for delivery. App hides
# the bell button when either is missing.
# (cryptography: declared above in the core list)
pywebpush>=2.0,<3.0

# Phase 37: QR code generation for cross-device pairing. The /api/pair
# endpoint returns SVG QR codes when this is installed; without it,
# the pairing modal falls back to a copyable URL.
qrcode>=7.0,<9.0

# v3.43.56: Cloudflare-evasion library. More comprehensive bot-
# detection patcher than the built-in STEALTH_JS — adds evasions
# for navigator.permissions, WebGL vendor/renderer leak, chrome.app
# absence, the iframe.contentWindow null check, plus several others
# that Cloudflare's heavily-obfuscated detection script checks. Use
# by enabling `use_stealth_library` in the site config. App falls
# back to STEALTH_JS if not installed (no error).
playwright-stealth>=1.0,<3.0

# v3.43.63: library-extractor fast path. Maintained PyPI extractor
# libraries (EchterAlsFake family) for adult-content sites. Each library
# is independent and may be installed à la carte; bulk_downloader's
# `extractors` module gracefully skips any not installed. Use by
# enabling `use_library_extractor: true` per site, OR the relevant
# templates ship with this on by default in v3.43.63.
#
# The shared base API (eaf_base_api) is pulled in transitively by most
# of these but listed explicitly so a minimal install still works.
#
# v3.47.1: any extractor pinned `>=N.M` that's been pulled from PyPI
# is commented out below. Uncomment + adjust pin when a fork/replacement
# emerges. The runner gracefully degrades — sites without their
# library extractor fall back to the generic teach-based path.
#
# v3.63.5: the `xnxx_api` and `beeg_api` pins were both removed
# entirely (not just commented out). `xnxx_api` was pulled from PyPI
# and unfindable; `beeg_api` installs hit intermittent 404s on a wheel
# that wasn't being maintained. The adapters in `extractors.py` for
# both sites still exist and still call `_try_import("xnxx_api" /
# "beeg_api")`, which now correctly returns None on every install
# (since neither package is in any requirements file) and the
# adapter returns `library_not_installed`, BD falls through to the
# generic teach-based path. To restore either, add the line back
# here AND verify the package still exists on PyPI first — the pins
# were removed because the failures were noisy on every fresh
# install, not because the underlying adapter logic was broken.
eaf_base_api>=2.5
phub>=4.0
xvideos_api>=1.7
xhamster_api>=1.6
spankbang_api>=1.4
eporner_api>=1.5
hqporner_api>=1.5
youporn_api>=1.4
porntrex_api>=1.3
missav_api>=1.2
xfreehd_api>=1.2
porngo_api>=1.2

# v3.43.63: m3u8 manifest parser (optional — used by hls_downloader for
# pre-flight variant inspection. Without it, ffmpeg still handles the
# manifest natively; this just gives us better diagnostics when an HLS
# download fails.)
# m3u8: NOT carried over. Declared since v3.4x for "hls_downloader pre-flight
# variant inspection", but no module imports it (AST + importlib census
# 2026-09-03); hls_downloader.py:30 records the pure-Python m3u8 path as the
# road not taken. A dead pin in a converged manifest is an install risk with no
# consumer, so it is dropped; doctor.py still REPORTS it as an optional extra.

# v3.43.70: apprise notifications. One library, 80+ services
# (Telegram, Discord, Pushover, ntfy, Gotify, Slack, Pushbullet,
# Pushsafer, Webex, Mattermost, Matrix, mailto://, sms gateways).
# URL-style syntax: `tgram://<bot_token>/<chat_id>`,
# `discord://<webhook_id>/<webhook_token>`, etc. See
# https://github.com/caronc/apprise/wiki for the full catalog.
# Fail-open in BD — the notify_apprise module reports
# `apprise_not_installed` and skips dispatch when missing.
apprise>=1.7,<2.0

# v3.43.72: perceptual video dedup. Computes a 64-bit pHash from
# evenly-spaced frames of a video; lets BD catch duplicate scenes
# across networks even when filenames + file sizes differ. Requires
# ffmpeg on PATH (which BD already needs for HLS/library extractors).
# Fail-open in BD — the dedup module reports `videohash_not_installed`
# and skips registration when missing.
videohash>=2.1,<3.0

# v3.43.73: Scrapling adaptive selectors + Turnstile bypass.
# Scrapling is an undetectable web-scraping library that adds two
# things to BD: (1) content-based selector recovery when sites
# tweak their HTML and BD's learned selectors break, (2) Cloudflare
# Turnstile bypass via StealthyFetcher. Both are opt-in per site;
# fail-open everywhere — missing library reports `not_available`
# and BD's existing teach path runs unchanged.
scrapling[fetchers]>=0.2,<1.0

# ---------------------------------------------------------------------------
# STAGED 2026-09-03 (cut w6-reqconsolidate, operator-directed): consolidate the
# TEST manifest into requirements.txt and declare the extractor/tool set the app
# imports or shells out to but never declared. requirements-cloak.txt,
# requirements-dev.txt and requirements-shuffle.txt STAY separate on purpose
# (cloak = skippable/heavy/deploy-waiver + own browser install; dev = build
# chain deploy must not install; shuffle = pytest-randomly must not auto-activate
# in the main venv). See the brief: bd-codex-briefs/w6-reqconsolidate.md.
# ---------------------------------------------------------------------------

# --- former requirements-test.txt uniques (pytest + pillow already declared above) ---
pyyaml>=6.0,<7.0            # shard-coverage + config gates import yaml
pyflakes>=3.0,<4.0         # bd-tool-smoke/lint fail CLOSED without it
pytest-timeout>=2.0,<3.0  # the canonical suite's --timeout plugin (F2: absent on a fresh install_linux box)
psycopg[binary]>=3.1,<4   # mod3 Postgres cutover tests
httpcore>=1.0,<2.0        # pinned transitive floor used directly by the http client tests

# --- extractor / downloader tools the app imports or subprocess-calls, previously UNDECLARED ---
# yt-dlp: imported directly (bulk_downloader/app.py, app_ytdlp_archive.py) AND subprocess-called;
# present only transitively today. Date-versioned; site extractors need to stay current, so a
# FLOOR only (no upper bound) -- reviewer: confirm the no-upper-bound exception vs the house style.
yt-dlp>=2025.1
# gallery-dl + youtube-dl: subprocess-called extractor fallbacks (fail-open; absent fleet-wide today).
gallery-dl>=1.26
youtube-dl>=2021.12.17
