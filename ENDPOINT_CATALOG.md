# ENDPOINT_CATALOG

Auto-generated by `tools/build_endpoint_catalog.py` (KB_PLAN_v2 B.2).
DO NOT EDIT BY HAND — `tests/test_endpoint_catalog_in_sync.py` will
fail the build. To regenerate after adding or removing a route:

    python tools/build_endpoint_catalog.py

`CSRF: yes` means a cookie-based browser session calling that
method+path must send a valid `X-CSRF-Token` header. `CSRF: no`
means the `_check_csrf` `@before_request` hook does not 403 the
request based on route (it may still reject on Bearer / session
absence, but those are auth-mode-dependent — see the script
docstring for the full rule).

The description column is the view function's docstring first line.
Routes with no docstring have an empty description — fixing that
is a documentation chore, not a catalog bug.

Schema version: 1


## /api/* routes (814)

```
POST    /api/a11y/audit                                        CSRF: yes  — Audit an HTML snippet for common ARIA issues. Body: {html}.
GET     /api/a11y/contrast                                     CSRF: no   — Compute WCAG contrast ratio. Query: ?fg=#fff&bg=#000.
POST    /api/a11y/plain_language                               CSRF: yes
GET     /api/account_pool/status_all                           CSRF: no   — Global view across every site's pool. Used by the dashboard
GET     /api/accounts/health                                   CSRF: no
GET     /api/activity/v2                                       CSRF: no   — SPA-shaped activity list with delta_vs_prev_period.
GET     /api/activity/v2/export.csv                            CSRF: no   — CSV export of the activity list. Same filters as
GET     /api/activity/v2/site_health                           CSRF: no   — Per-site daily completion counts. Query params:
POST    /api/ai/chat                                           CSRF: yes  — Phase 9.10: stateless "Ask the model" scratchpad. Body:
POST    /api/ai/classify                                       CSRF: yes  — Body: {element_desc}. Returns {ok, role, confidence}.
POST    /api/ai/diff_repair                                    CSRF: yes  — Phase 29: differential teach. Body: {broken_selectors,
GET     /api/ai/health                                         CSRF: no   — Rolling call stats (counts + recent latencies).
POST    /api/ai/models                                         CSRF: yes  — v3.62.3: list the models the selected provider currently
POST    /api/ai/normalize_resolution                           CSRF: yes  — Body: {filename}. Returns the normalized resolution shape.
GET     /api/ai/status                                         CSRF: no   — Reachability + model-installed check. Used by the global config
POST    /api/ai/suggest_selectors                              CSRF: yes  — Body: {dom_excerpt, screenshot_b64?, page_url?, context_hint?}.
GET     /api/alerts/active                                     CSRF: no   — Currently firing alerts (last N hours, default 24).
POST    /api/alerts/evaluate                                   CSRF: yes  — Force an immediate evaluation pass. Cron also runs this every
GET     /api/alerts/rules                                      CSRF: no   — List all configured rules (built-in + user-added). Used by
POST    /api/alerts/rules                                      CSRF: yes  — Cut 8: create/update an alert rule (incl. the job-lifecycle rule
POST    /api/alerts/rules/<rule_id>/remove                     CSRF: yes  — Delete a custom alert rule.
GET     /api/analyzer/captures                                 CSRF: no   — List capture artifacts available to the workbench. Read-only.
POST    /api/analyzer/load                                     CSRF: yes  — Body: {capture}. Returns {ok, has_dom, residual_count, residual_kinds,
POST    /api/analyzer/pin                                      CSRF: yes  — Body: {capture, selector, role, name?, host?}. Pins a REVIEW-ONLY
POST    /api/analyzer/test                                     CSRF: yes  — Body: {capture, selectors:[...]}. Evaluates selectors against the
POST    /api/analyzer/tree                                     CSRF: yes  — Body: {capture, max_depth?, max_children?}. Depth/breadth-limited tree
GET     /api/api_tokens                                        CSRF: no
POST    /api/api_tokens                                        CSRF: yes
DELETE  /api/api_tokens/<token_id>                             CSRF: yes
GET     /api/audit/for_target                                  CSRF: no   — Filter audit events by target prefix. Pass `prefix=sites_config:abc123`
GET     /api/audit/recent                                      CSRF: no
POST    /api/auth/login                                        CSRF: yes
POST    /api/auth/logout                                       CSRF: yes
GET     /api/auth/oidc/callback                                CSRF: no   — Handle the provider redirect: verify state, exchange code, verify the
GET     /api/auth/oidc/login                                   CSRF: no   — Begin the OIDC flow: stash state+nonce in the session, 302 to provider.
GET     /api/auth/oidc/status                                  CSRF: no   — Read-only: whether SSO is configured + enabled.
GET     /api/auth/users                                        CSRF: no
POST    /api/auth/users                                        CSRF: yes
DELETE  /api/auth/users/<username>                             CSRF: yes
POST    /api/auth/users/<username>/password                    CSRF: yes
POST    /api/auth/users/<username>/role                        CSRF: yes
GET     /api/auth/whoami                                       CSRF: no
POST    /api/auth_health/check/<sid>                           CSRF: yes  — Force a check of one site's cookies right now. Returns the
POST    /api/auth_health/check_all                             CSRF: yes  — Force a check of every site. Slow (one HTTP request per site).
GET     /api/auth_health/status                                CSRF: no   — Snapshot of every site's last-known auth health.
GET     /api/automation/drift_repair                           CSRF: no   — Status of the daily drift->AI-repair sweep: whether the toggle is on, the
POST    /api/automation/drift_repair/run                       CSRF: yes  — Run the drift-repair sweep ON DEMAND. Body: ``{force?: bool}`` -- force
POST    /api/automation/drift_repair/toggle                    CSRF: yes  — Set ``automation.drift_repair_enabled``. Body: ``{enabled: bool}``. The
GET     /api/automation/status                                 CSRF: no
POST    /api/backup/create                                     CSRF: yes  — Create a backup zip and return as download. Body params:
POST    /api/backup/drift                                      CSRF: yes
GET     /api/backup/history                                    CSRF: no
POST    /api/backup/preview                                    CSRF: yes  — Preview what a backup would contain WITHOUT actually creating it.
POST    /api/backup/restore                                    CSRF: yes  — Restore a backup. Accepts multipart/form-data with `file` (the
POST    /api/backup/smoke_restore                              CSRF: yes
POST    /api/backup/verify                                     CSRF: yes
POST    /api/batch/dedup_scan                                  CSRF: yes
POST    /api/batch/delete                                      CSRF: yes
POST    /api/batch/move                                        CSRF: yes
POST    /api/batch/retry                                       CSRF: yes
POST    /api/bg/enable/<name>                                  CSRF: yes
GET     /api/bg/status                                         CSRF: no
GET     /api/bitrot/issues                                     CSRF: no
POST    /api/bitrot/scan                                       CSRF: yes  — Trigger a bit-rot scan. Returns the summary {checked, intact,
GET     /api/bitrot/stats                                      CSRF: no
GET     /api/budget/<sid>                                      CSRF: no
POST    /api/bulk/enqueue                                      CSRF: yes  — Cut 8: enqueue a BATCH of URLs on a configured site, tracked via the
GET     /api/bw_chart/hourly                                   CSRF: no   — Per-hour bandwidth + jobs + failure-rate for the last N hours.
GET     /api/bw_chart/site_share                               CSRF: no   — Per-site bandwidth share (last N hours). Used to render a
GET     /api/bw_chart/summary                                  CSRF: no   — Headline numbers for last 24h (or N hours). Used by the
GET     /api/capacity                                          CSRF: no   — Return capacity forecasts: disk runway, queue ETA, bottleneck hint.
POST    /api/captcha/dismiss                                   CSRF: yes
GET     /api/captcha/pending                                   CSRF: no
GET     /api/captcha/pending/<path:url>                        CSRF: no
POST    /api/captcha/resolved                                  CSRF: yes
POST    /api/captcha/start_solve                               CSRF: yes
GET     /api/captures                                          CSRF: no   — Item 3: serve the cached recursive capture inventory -- paginated +
POST    /api/captures/allowlist_add                            CSRF: yes  — GCW guided-mode: add a root to the path allowlist -- the CONFIRM-GATED,
POST    /api/captures/build_draft                              CSRF: yes  — Item 3 follow-on: build a REVIEW-ONLY template draft from one stored
POST    /api/captures/live_learning                            CSRF: yes  — Arm/poll a learner action in the existing held-open Capture process.
POST    /api/captures/scan                                     CSRF: yes  — Item 3: explicit operator-triggered recursive capture scan. Walks the
POST    /api/captures/scrub                                    CSRF: yes  — Item 3 follow-on: scrub a raw ``.wacz`` capture (resolved by ``token``)
POST    /api/captures/setup_site                               CSRF: yes  — GCW-3 "Setup site" wizard step: create a site and (optionally) store
POST    /api/captures/stage_learning                           CSRF: yes  — Stage only the nonce-bound result proven by the Capture subprocess.
POST    /api/captures/suggest_rows                             CSRF: yes  — Auto-detect repeating row groups on a running interactive capture.
GET     /api/captures/validate_download_dir                    CSRF: no   — GCW guided-mode READ-ONLY download-root check. Query: ``?path=<abs>``.
GET     /api/changelog                                         CSRF: no   — Per-site changelog: success-rate deltas, new failure patterns,
GET     /api/changelog/<sid>                                   CSRF: no   — Detailed changelog for one site.
GET     /api/circuit/report                                    CSRF: no
POST    /api/circuit/reset                                     CSRF: yes
GET     /api/cleanup/summary                                   CSRF: no
POST    /api/cluster_rate/acquire                              CSRF: yes
GET     /api/cluster_rate/load                                 CSRF: no
POST    /api/cluster_rate/release                              CSRF: yes
GET     /api/cockpit/nav                                       CSRF: no
POST    /api/community_scrapers/bulk_install                   CSRF: yes  — Install all scrapers whose name matches any of the supplied
GET     /api/community_scrapers/index                          CSRF: no   — List available scrapers from GitHub. force=1 to bypass cache.
POST    /api/community_scrapers/install                        CSRF: yes  — Install one scraper to the configured Stash dir.
POST    /api/community_scrapers/remove                         CSRF: yes  — Remove an installed scraper.
GET     /api/community_scrapers/status                         CSRF: no   — Module availability + manifest summary.
POST    /api/concurrent/<sid>                                  CSRF: yes
GET     /api/config/export                                     CSRF: no   — Export full site configs as JSON. Passwords are stripped by default
POST    /api/config/import                                     CSRF: yes  — Import site configs. mode='merge' (default) keeps existing sites and
POST    /api/cookie_clipboard/parse                            CSRF: yes  — Parse pasted cookie text (Netscape / JSON / cURL / cookie header)
POST    /api/cookie_clipboard/save/<sid>                       CSRF: yes  — Parse text + save into the configured cookie_file path for site
GET     /api/cookie_quality                                    CSRF: no
GET     /api/cookie_quality/<sid>                              CSRF: no
POST    /api/cookie_relogin/check                              CSRF: yes
GET     /api/cookie_relogin/history                            CSRF: no
GET     /api/cost/report                                       CSRF: no
GET     /api/cost/sunset_candidates                            CSRF: no
POST    /api/crash_recovery/delete                             CSRF: yes  — Delete an orphan .part + sidecar. Body: {path}.
POST    /api/crash_recovery/ignore                             CSRF: yes  — Mark an orphan as 'leave alone' so future scans skip it.
POST    /api/crash_recovery/resume                             CSRF: yes  — Re-enqueue the URL associated with an orphan .part. The
GET     /api/crash_recovery/scan                               CSRF: no   — List orphan .part files across all sites. Files <24h old or
GET     /api/crash_recovery/stats                              CSRF: no
GET     /api/csrf                                              CSRF: no   — Return the CSRF token derived from the caller's session cookie.
GET     /api/daily_budget/history/<sid>                        CSRF: no   — Daily usage history for charting. Query: ?days=30 (max 365).
POST    /api/daily_budget/reset/<sid>                          CSRF: yes  — Zero today's counter for one site. Used in emergencies or for
GET     /api/daily_budget/status                               CSRF: no   — Snapshot of every site's today usage + budget status.
GET     /api/dashboard                                         CSRF: no
GET     /api/dashboard/v2                                      CSRF: no   — SPA-shaped dashboard snapshot. Composed of:
POST    /api/dashboard/v2/resolve                              CSRF: yes
GET     /api/dashboard/v2/sparkline                            CSRF: no   — High-cadence throughput sparkline. Returns the last 60 samples
GET     /api/dashboard/widgets                                 CSRF: no   — Return just the widget metrics + their internal state. Used
GET     /api/data/browser_status                               CSRF: no
GET     /api/data/capture_analytics                            CSRF: no
GET     /api/data/capture_diagnostics                          CSRF: no
GET     /api/data/deploy_health                                CSRF: no
GET     /api/data/dom_recorder_status                          CSRF: no
GET     /api/data/kb_analytics                                 CSRF: no
GET     /api/data/queue_analytics                              CSRF: no
GET     /api/data/release_analytics                            CSRF: no
GET     /api/data/replay_validation                            CSRF: no
GET     /api/data/secrets_status                               CSRF: no
GET     /api/data/site_health                                  CSRF: no
GET     /api/data/storage_tiers                                CSRF: no
GET     /api/data/template_health                              CSRF: no
GET     /api/data/vpn_status                                   CSRF: no
GET     /api/data/workflow_analytics                           CSRF: no
POST    /api/dedup/find                                        CSRF: yes  — Find duplicates for a specific file path (must already be in
GET     /api/dedup/groups                                      CSRF: no   — List all duplicate groups in the registry. For the review UI.
POST    /api/dedup/remove                                      CSRF: yes  — Remove a path from the registry (does NOT delete the file).
POST    /api/dedup/scan                                        CSRF: yes  — Kick off a background folder scan. Returns immediately.
POST    /api/dedup/scan/cancel                                 CSRF: yes  — Cancel an in-progress folder scan.
GET     /api/dedup/status                                      CSRF: no   — Module availability + registry stats + scan progress.
GET     /api/deploy/compose                                    CSRF: no
GET     /api/deploy/k8s                                        CSRF: no
GET     /api/deploy/systemd                                    CSRF: no
GET     /api/dev/account_pool                                  CSRF: no   — T5/D-17 — per-site account pool: count, active index, per-
GET     /api/dev/ai_fallback                                   CSRF: no   — T7/D-55 — evaluate the AI fallback chain against the current
GET     /api/dev/ai_health_history                             CSRF: no   — T8/D-57 — AI health snapshot as a history view: call/fail
GET     /api/dev/ai_latency                                    CSRF: no   — T8/D-53 — analysed view of aiassist's recent AI-call latencies:
GET     /api/dev/auth_cookie_test                              CSRF: no   — U17/D-29 — run login._looks_authenticated on each site's jar
GET     /api/dev/auth_map                                      CSRF: no   — Every route and the auth controls its view applies.
GET     /api/dev/backup_check                                  CSRF: no   — Verify a backup file, asserting the WAL sidecars. ?path= .
GET     /api/dev/bat_lint                                      CSRF: no   — Lint .bat files — ASCII, CRLF, for-loop redirection.
POST    /api/dev/cache_clear                                   CSRF: yes  — U28/D-123 (A) — drop BD's in-process caches. Body {targets?} —
GET     /api/dev/captcha_relay                                 CSRF: no   — T15/D-48 — captcha-relay queue grouped by type + status,
GET     /api/dev/changelog_lint                                CSRF: no   — Confirm CHANGELOG.md has a current '## vX.Y.Z' entry.
GET     /api/dev/config                                        CSRF: no   — Live app + per-site config, secrets redacted.
GET     /api/dev/config_audit                                  CSRF: no   — U15/D-86 — fleet-wide site-config schema audit (read-only).
GET     /api/dev/config_check                                  CSRF: no   — Validate per-site config — empty configs, dup URLs, cred refs.
POST    /api/dev/config_reload                                 CSRF: yes  — U28/D-119 (A) — re-read app_config.json and re-apply it without
POST    /api/dev/config_restore                                CSRF: yes  — U29/D-90 (A) — restore a named config snapshot. Body {name}.
POST    /api/dev/config_snapshot                               CSRF: yes  — U29/D-90 (A) — snapshot the live app config. Body {name?}.
GET     /api/dev/config_snapshot_diff                          CSRF: no   — D-90 — diff the live app config against a named snapshot (read-only;
GET     /api/dev/config_snapshots                              CSRF: no   — U29/D-90 — list available config snapshots (read-only).
GET     /api/dev/cookie_age                                    CSRF: no   — U17/D-22 — cookie expiry / age report (read-only).
GET     /api/dev/cookie_jar                                    CSRF: no   — U17/D-21 — per-cookie structure of each site's jar (read-only).
GET     /api/dev/coverage_map                                  CSRF: no   — U26/D-69 — which bulk_downloader modules have a matching test
GET     /api/dev/credential_resolver                           CSRF: no   — U18/D-26 — per-site credential-reference resolution report
GET     /api/dev/csrf_check                                    CSRF: no   — Confirm every state-changing /api route enforces CSRF.
GET     /api/dev/csrf_token                                    CSRF: no   — T6/D-30 — inspect the CSRF token mechanism: HMAC double-submit
GET     /api/dev/db_growth                                     CSRF: no   — T4/D-10 — DB size breakdown + a forward growth projection from
GET     /api/dev/db_stats                                      CSRF: no   — DB tables + row counts, index count, journal mode, file sizes.
GET     /api/dev/dead_css                                      CSRF: no   — T34/D-107 — find CSS selectors not referenced in templates or
GET     /api/dev/deadlock_check                                CSRF: no   — Heuristic deadlock check — two-snapshot blocked-acquire scan.
GET     /api/dev/dedup_hashes                                  CSRF: no   — T11/D-37 — read-only view of the perceptual-hash registry:
POST    /api/dev/deep_detect                                   CSRF: yes  — v3.66.3 — run the full deep-detect pipeline against an HTML
GET     /api/dev/dependency_audit                              CSRF: no   — U27/D-79 — requirements*.txt pin-discipline audit (read-only).
GET     /api/dev/discover                                      CSRF: no   — List all test files + their test classes/functions (AST parse,
GET     /api/dev/disk_usage                                    CSRF: no   — T17/D-112 — per-extension, per-age-bucket, per-site bytes
GET     /api/dev/dispatch_chain                                CSRF: no   — U5/D-11 — the ordered _process_one dispatch chain (read-only).
GET     /api/dev/dispatch_dryrun                               CSRF: no   — U5/D-20 — trace which _process_one branch a URL routes to,
GET     /api/dev/download_scan                                 CSRF: no   — T17/D-113 — scan download_dirs for anomalies (zero-byte
GET     /api/dev/duplicate_sites                               CSRF: no   — U16/D-88 — sites that duplicate each other (read-only).
GET     /api/dev/egress_ip                                     CSRF: no   — T14/D-49 — walk registered tunnels, report each tunnel's last
GET     /api/dev/enabled                                       CSRF: no   — Always available — tells the UI whether to show the Dev tab.
GET     /api/dev/env                                           CSRF: no   — Effective values of the BD_* env flags + behavioural markers.
GET     /api/dev/error_rate                                    CSRF: no   — 4xx/5xx counts per route.
GET     /api/dev/event_tap                                     CSRF: no   — U22/D-65 — recent SSE events captured from the broker's publish
GET     /api/dev/event_tap_ui                                  CSRF: no   — U22/D-105 — the standalone SSE-event-tap dev page. Self-contained
GET     /api/dev/exceptions                                    CSRF: no   — Recent unhandled request exceptions. Query param: limit.
GET     /api/dev/extractor_fastpath                            CSRF: no   — U19/D-39 — simulate the library-extractor fast path for a URL
GET     /api/dev/extractor_matrix                              CSRF: no   — U19/D-31 — the library-extractor capability matrix (read-only).
POST    /api/dev/feature_flag_delete                           CSRF: yes  — T40/D-121 mutating — remove a feature flag. Body: {name}.
POST    /api/dev/feature_flag_set                              CSRF: yes  — T40/D-121 mutating — set a feature flag. Body: {name, value}.
GET     /api/dev/feature_flags                                 CSRF: no   — T40/D-121 — read-only list of all feature flags + values.
GET     /api/dev/ffmpeg_preview                                CSRF: no   — U20/D-33 — preview the ffmpeg argv for a streaming URL without
GET     /api/dev/filename_template                             CSRF: no   — T12/D-40 — preview filename-template rendering. With ?template=,
POST    /api/dev/fixture_site/start                            CSRF: yes  — T2/D-72 (A) — start a fixture mock site in-process. Body
GET     /api/dev/fixture_site/status                           CSRF: no   — T2/D-72 — which fixture mock sites are running (read-only).
POST    /api/dev/fixture_site/stop                             CSRF: yes  — T2/D-72 (A) — stop a running fixture mock site. Body {name?}.
GET     /api/dev/flaky_tests                                   CSRF: no   — T1/D-70 — diff failures across several stored test_results.json
GET     /api/dev/flaresolverr_health                           CSRF: no   — T15/D-45 — FlareSolverr endpoint config, live ping
GET     /api/dev/fts_index                                     CSRF: no   — T3/D-5 — inspect the history_fts FTS5 index: availability,
POST    /api/dev/gc                                            CSRF: yes  — Run a full garbage collection; report objects freed.
GET     /api/dev/golden_files                                  CSRF: no   — T42/D-75 — inventory golden files + report drift between
GET     /api/dev/guard_status                                  CSRF: no   — U26/D-77 — guard/pin-test status from a test_results.json
GET     /api/dev/i18n_coverage                                 CSRF: no   — T38/D-108 — i18n catalog coverage vs strings extracted from
GET     /api/dev/import_preflight                              CSRF: no   — U15/D-89 — preflight a bulk-import CSV/XLSX file before
GET     /api/dev/index_advisor                                 CSRF: no   — U23/D-2 — list DB indexes and EXPLAIN QUERY PLAN each hot query,
GET     /api/dev/integrity                                     CSRF: no   — On-demand DB quick_check + integrity_check, each timed.
GET     /api/dev/invariants                                    CSRF: no   — Runtime audit of checkable DANGER_MAP invariants.
POST    /api/dev/job_replay                                    CSRF: yes  — U30/D-18 (A) — replay a single download by history-row id.
GET     /api/dev/keepers                                       CSRF: no   — Session-keeper heartbeat / state monitor.
GET     /api/dev/latency                                       CSRF: no   — p50/p95/p99 request latency per route.
GET     /api/dev/leak_scan                                     CSRF: no   — Scan for resource-leak signals — orphan processes, stale files.
GET     /api/dev/load                                          CSRF: no   — GET — current load-injection status. POST {action,...} where
POST    /api/dev/load                                          CSRF: yes  — GET — current load-injection status. POST {action,...} where
GET     /api/dev/lockfile_scan                                 CSRF: no   — U25/D-118 — report BD's lingering temp dirs and .lock files in
GET     /api/dev/log_level                                     CSRF: no   — GET — current log level. POST {level} — set it (reversible).
POST    /api/dev/log_level                                     CSRF: yes  — GET — current log level. POST {level} — set it (reversible).
GET     /api/dev/log_search                                    CSRF: no   — Search the app log by level / logger / text. Query params:
POST    /api/dev/login_flow_delete                             CSRF: yes  — T45/D-27 mutating — delete a saved login flow.
POST    /api/dev/login_flow_save                               CSRF: yes  — T45/D-27 mutating — save a captured login-flow action sequence.
GET     /api/dev/login_flows                                   CSRF: no   — T45/D-27 — read-only list of saved login flows. Optional
GET     /api/dev/login_template_dryrun                         CSRF: no   — U18/D-24 — dry-run applying a login template to a site, without
GET     /api/dev/logtail                                       CSRF: no   — Last ?n lines (default 200, max 5000) of the app log.
GET     /api/dev/magic_bytes                                   CSRF: no   — T10/D-35 — identify a file by its first bytes and flag any
POST    /api/dev/maintenance_disable                           CSRF: yes  — T36/D-122 mutating — end all active immediate-override windows.
POST    /api/dev/maintenance_enable                            CSRF: yes  — T36/D-122 mutating — start an immediate maintenance override.
GET     /api/dev/maintenance_mode                              CSRF: no   — T36/D-122 — read-only status: active maintenance windows +
GET     /api/dev/manifest_probe                                CSRF: no   — U21/D-32 — probe an HLS/DASH manifest's structure (read-only;
GET     /api/dev/manual_takeover_log                           CSRF: no   — T6/D-28 — classified timeline of manual-login / takeover events
GET     /api/dev/mem_audit                                     CSRF: no   — Memory snapshot. With ?settle=N, instead runs a settle-and-diff
POST    /api/dev/mem_audit/track                               CSRF: yes  — Body {action: "start"|"stop"} — toggle tracemalloc tracking so
GET     /api/dev/migration_status                              CSRF: no   — U24/D-4 — migration ledger snapshot: registered/applied/pending
GET     /api/dev/model_pull_check                              CSRF: no   — T39/D-58 — verify Ollama-installed models against the
GET     /api/dev/mp4_metadata                                  CSRF: no   — T10/D-36 — walk the top-level MP4 atoms; flag 'no moov' and
GET     /api/dev/ollama                                        CSRF: no   — AI / Ollama model inventory — configured vs installed.
GET     /api/dev/orphan_rows                                   CSRF: no   — U16/D-7 — DB rows whose site_id has no config (read-only).
GET     /api/dev/parametrize_fanout                            CSRF: no   — T1/D-73 — static scan of tests/*.py: how @parametrize fans test
GET     /api/dev/partials                                      CSRF: no   — T11/D-38 — find .part files (resumable-download tempfiles) in
GET     /api/dev/path_allowlist_test                           CSRF: no   — U27/D-82 — exercise the real _validate_path against probe paths
GET     /api/dev/pin_drift                                     CSRF: no   — U31/D-100 — cross-check requirements.txt against the installed
GET     /api/dev/proc                                          CSRF: no   — Process fingerprint — pid, uptime, open fds, RSS, threads.
GET     /api/dev/prompt_preview                                CSRF: no   — T7/D-52 — catalog the AI prompt templates and render filled
GET     /api/dev/queue_table                                   CSRF: no   — T3/D-6 — inspect the persisted queue table: per-site/per-status
GET     /api/dev/queue_throughput                              CSRF: no   — T4/D-15 — completed-job throughput series bucketed from the
GET     /api/dev/rate_limits                                   CSRF: no   — Per-site rate-limit cooldown state.
POST    /api/dev/request_replay/<request_id>                   CSRF: yes  — T44/D-46 mutating — re-issue a captured request. The replay
GET     /api/dev/request_replay_list                           CSRF: no   — T44/D-46 — read-only list of captured /api/* requests. Capture
GET     /api/dev/resolution_test                               CSRF: no   — U20/D-34 — exercise both INV-005 resolution code paths and show
GET     /api/dev/retry_schedule                                CSRF: no   — T5/D-13 — per-site auto-retry schedule + live scheduled retries
GET     /api/dev/route_timing                                  CSRF: no   — OBS-2 — per-route latency percentiles (count/p50/p95/max) over the recent
GET     /api/dev/routes                                        CSRF: no   — Every registered Flask route — path, methods, endpoint.
POST    /api/dev/run                                           CSRF: yes  — Start a test run. Body: {target, kind}.
GET     /api/dev/runner_console                                CSRF: no   — U30/D-19 — read-only fleet view of every live SiteRunner's
GET     /api/dev/runner_state                                  CSRF: no   — Per-site live runner state — queue depth, workers, paused, etc.
GET     /api/dev/runs                                          CSRF: no
GET     /api/dev/runs/<run_id>                                 CSRF: no   — Poll status of a run. The output field grows incrementally as
POST    /api/dev/runs/<run_id>/cancel                          CSRF: yes
GET     /api/dev/sast_summary                                  CSRF: no   — U27/D-78 — surface the tools/ SAST/DAST pipeline's latest
GET     /api/dev/secret_scan                                   CSRF: no   — U27/D-80 — grep the tree for hardcoded-secret shapes (read-only;
GET     /api/dev/sh_lint                                       CSRF: no   — Lint .sh files — LF endings, executable bit.
GET     /api/dev/slow_endpoints                                CSRF: no   — Requests over a latency threshold. Query param: threshold_ms.
GET     /api/dev/slow_queries                                  CSRF: no   — U23/D-1 — profile the representative hot DB queries and flag
POST    /api/dev/sql                                           CSRF: yes  — Run a single read-only SELECT. Body {query, limit?}. Anything
GET     /api/dev/sse_status                                    CSRF: no   — Live SSE broker state — connected client count and ages.
GET     /api/dev/stale_references                              CSRF: no   — U16/D-92 — config refs pointing at something gone (read-only).
GET     /api/dev/stealth_audit                                 CSRF: no   — T16/D-50 — per-site UA + stealth configuration audit; flags
GET     /api/dev/storage_tier_status                           CSRF: no   — T35/D-117 — StorageTierScheduler state + per-site candidate
GET     /api/dev/stuck_jobs                                    CSRF: no   — Queue rows stuck in 'running'. Query param: older_than (sec).
GET     /api/dev/systemd_check                                 CSRF: no   — U31/D-101 — validate a BulkDownloader systemd unit (read-only).
POST    /api/dev/tempdir_clean                                 CSRF: yes  — U25/D-114 (A) — remove BD's own stale temp dirs / .lock files.
GET     /api/dev/template_audit                                CSRF: no   — Structural validation of all registered login templates.
GET     /api/dev/test_run_diff                                 CSRF: no   — U26/D-74 — diff the in-GUI test runner's recent run history
GET     /api/dev/test_timing                                   CSRF: no   — T50/D-71 — slowest tests + per-file totals from the latest
GET     /api/dev/thread_dump                                   CSRF: no   — Full stack trace of every live thread.
GET     /api/dev/threads                                       CSRF: no   — Live thread inventory — name, daemon, alive.
POST    /api/dev/tls_check                                     CSRF: yes  — T43/D-47 — TLS cert inspection. POST + opt-in: caller must
POST    /api/dev/token_estimate                                CSRF: yes  — T37/D-56 — character-based token estimate for a prompt.
GET     /api/dev/version_check                                 CSRF: no   — Flag any banner/version string not matching __version__.
POST    /api/dev/vision_test                                   CSRF: yes  — T9/D-54 — exercise the vision pipeline end-to-end. ACTUALLY
GET     /api/dev/vpn_config                                    CSRF: no   — T13/D-41 — render every configured VPN tunnel with secrets
GET     /api/dev/vpn_probe                                     CSRF: no   — T14/D-42 — call vpn.check_health() per registered tunnel and
GET     /api/dev/vpn_rotation                                  CSRF: no   — T13/D-43 — group configured tunnels by provider and report
POST    /api/dev/wal_checkpoint                                CSRF: yes  — Flush the WAL sidecar into the main DB. Body {mode} optional —
POST    /api/dev/window_simulate                               CSRF: yes  — T41/D-16 — simulate site_in_window() against a spec + sample
GET     /api/dev/worker_profile                                CSRF: no   — T5/D-14 — per-runner worker-thread attribution: count, alive,
GET     /api/dev/zip_manifest                                  CSRF: no   — Verify a built release zip against the source tree. ?path= .
GET     /api/diagnostics/bundle                                CSRF: no   — Return the JSON diagnostics snapshot. Safe to share — sensitive
GET     /api/diagnostics/download                              CSRF: no   — Generate a zip of the diagnostics bundle for download.
GET     /api/diagnostics_bundle/download                       CSRF: no   — Stream a zipped bundle as a download.
GET     /api/diagnostics_bundle/preview                        CSRF: no   — Return the bundle as JSON for inline preview (no zip).
POST    /api/discovery/disco/run                               CSRF: yes  — Operator manual trigger for A-DISCO (cut 4b): a run-now. It FORCES a pass
GET     /api/discovery/disco/runs                              CSRF: no   — The persisted A-DISCO run history (disco_runner.recent_runs), so the operator
GET     /api/discovery/history                                 CSRF: no
POST    /api/discovery/run                                     CSRF: yes
POST    /api/discovery/scenes/start                            CSRF: yes  — Start one bounded authenticated library crawl from the existing GUI.
GET     /api/discovery/scenes/status                           CSRF: no
GET     /api/doctor                                            CSRF: no   — Full diagnostic pass: environment, optional deps, cookie age.
POST    /api/doctor/diagnose                                   CSRF: yes  — Pattern-match a failure error string. Body: {error: '...'} or
GET     /api/download_hold                                     CSRF: no   — Report HELD / CLEAR / UNKNOWN. Read-only; never mutates the store.
POST    /api/download_hold                                     CSRF: yes  — Record a durable hold, then stop any running pool.
POST    /api/download_hold/lift                                CSRF: yes  — Explicitly lift the hold. The lift is itself durable (held: false).
POST    /api/edge_deploy/all                                   CSRF: yes  — Return all deploy artifacts (compose + systemd + k8s).
POST    /api/edge_deploy/compose                               CSRF: yes  — Generate docker-compose.yml for current install.
POST    /api/eol/export                                        CSRF: yes
GET     /api/eol/migration_report                              CSRF: no
GET     /api/events_all                                        CSRF: no   — Phase 21.4: cross-site event feed. JSON cursor `?after={...}` maps
GET     /api/export/csv                                        CSRF: no
GET     /api/export/json                                       CSRF: no
GET     /api/export/m3u                                        CSRF: no
GET     /api/extension/lookup_url                              CSRF: no   — Given a URL, return what BD knows about it. Used by the
POST    /api/fed/announce                                      CSRF: yes  — A peer registers / refreshes. Requires HMAC signature in
POST    /api/fed/claim_url                                     CSRF: yes
POST    /api/fed/manual_register                               CSRF: yes  — Operator-initiated peer add. Bypasses the announce flow's HMAC
GET     /api/fed/peers                                         CSRF: no
POST    /api/fed/pending_review                                CSRF: yes  — Operator: approve or reject a pending peer template. Approve writes it
GET     /api/fed/pending_templates                             CSRF: no   — Read-only: peer templates awaiting operator review.
POST    /api/fed/set_trust                                     CSRF: yes  — Operator-set a peer's trust tier (C7 11.2). Body: {instance_id, tier}
GET     /api/fed/status                                        CSRF: no
GET     /api/fed/sync_pull                                     CSRF: no
GET     /api/fed/template_pull                                 CSRF: no   — Read-only: return the signed, redacted bundle for one template (by
POST    /api/fed/template_push                                 CSRF: yes  — Peer -> us: receive a template bundle and queue it for operator review.
GET     /api/fed/templates_available                           CSRF: no   — Read-only: descriptors of templates this instance can share.
POST    /api/file/reveal                                       CSRF: yes
GET     /api/fixtures/list                                     CSRF: no
POST    /api/fixtures/run_all                                  CSRF: yes
GET     /api/flaresolverr/sessions                             CSRF: no   — List active FlareSolverr sessions on the configured endpoint.
GET     /api/flaresolverr/status                               CSRF: no   — Return stats + most recent ping check.
POST    /api/flaresolverr/test                                 CSRF: yes  — Test a solve against a user-supplied URL. Used by the UI's
GET     /api/gallerydl_status                                  CSRF: no
POST    /api/gallerydl_update                                  CSRF: yes
GET     /api/gamification/report                               CSRF: no
GET     /api/global_config                                     CSRF: no   — Read/write global app settings. Currently:
POST    /api/global_config                                     CSRF: yes  — Read/write global app settings. Currently:
GET     /api/global_config/defaults                            CSRF: no   — P6-8: the SHIPPED global-config defaults (frozen at module load).
GET     /api/global_config/origins                             CSRF: no   — Per-field origin + apply-timing classification (read-only).
GET     /api/health                                            CSRF: no
GET     /api/health/checklist                                  CSRF: no
GET     /api/health/v2                                         CSRF: no   — SPA-shaped full health surface. Superset of /api/health — adds
GET     /api/history                                           CSRF: no
POST    /api/history/prune                                     CSRF: yes
POST    /api/history/vacuum                                    CSRF: yes
GET     /api/hourly_stats                                      CSRF: no   — Phase 74 (v3.43.16): time-of-day success rate analytics. Returns
GET     /api/i18n/load/<lang>                                  CSRF: no
GET     /api/i18n/locales                                      CSRF: no
POST    /api/i18n/save/<lang>                                  CSRF: yes
GET     /api/i18n/template                                     CSRF: no   — Generate a translation template from all the .html files
POST    /api/import/cancel/<job_id>                            CSRF: yes
GET     /api/import/recent                                     CSRF: no   — Recent imports across this run + persisted history.
POST    /api/import/start/<sid>                                CSRF: yes  — Start a background mass-import job. Body accepts either:
GET     /api/import/status/<job_id>                            CSRF: no   — Polling status endpoint. Returns the job state dict or 404.
GET     /api/import/stream/<job_id>                            CSRF: no   — SSE stream: pushes status updates every 500ms until the job
GET     /api/integrations/health                               CSRF: no   — Cut 7: read-only, FAIL-OPEN, SANITIZED integration health rollup.
POST    /api/interop/acknowledge                               CSRF: yes  — Set risk_acknowledged for a REGISTERED item. 400 if it was never registered
POST    /api/interop/enable                                    CSRF: yes  — Set the enabled flag for a REGISTERED item. 400 if unregistered.
POST    /api/interop/register                                  CSRF: yes  — Record (or update) an item's provenance. Off by default -- registering does
GET     /api/interop/registry                                  CSRF: no   — Read-only: every registered item (kind, item_id, provenance, flags) + the
GET     /api/jobs/stuck                                        CSRF: no   — List jobs that have made no progress for `?minutes=` (default 30).
POST    /api/jsonapi/probe                                     CSRF: yes  — v3.43.68: probe a site for HereSphere/DeoVR JSON API support.
GET     /api/knowledge/notes                                   CSRF: no
POST    /api/knowledge/notes                                   CSRF: yes
DELETE  /api/knowledge/notes/<int:nid>                         CSRF: yes
GET     /api/knowledge/recipes                                 CSRF: no   — Diagnostic recipes catalog. Filter by ?message=<text> to get
GET     /api/knowledge/runbook/<sid>                           CSRF: no
DELETE  /api/library/<int:lid>                                 CSRF: yes
GET     /api/library/<int:lid>                                 CSRF: no
POST    /api/library/<int:lid>/notes                           CSRF: yes
POST    /api/library/<int:lid>/rating                          CSRF: yes
POST    /api/library/<int:lid>/tags                            CSRF: yes  — Body: {tag: 'name'} → attach (creating tag if needed).
DELETE  /api/library/<int:lid>/tags/<path:tag_name>            CSRF: yes
POST    /api/library/<int:lid>/watched                         CSRF: yes
POST    /api/library/audit                                     CSRF: yes  — Run a full audit on a download directory.
GET     /api/library/browse                                    CSRF: no
GET     /api/library/integrity                                 CSRF: no   — #69 — file-system integrity / rename detection.
GET     /api/library/orphans                                   CSRF: no   — v3.50: Files under `?root=` that aren't in the library table.
POST    /api/library/orphans                                   CSRF: yes  — Find files on disk that aren't in history (likely manual
POST    /api/library/regen_nfos                                CSRF: yes  — Bulk-regenerate NFO sidecars from the history table. Body:
POST    /api/library/scan/cancel                               CSRF: yes
POST    /api/library/scan/start                                CSRF: yes  — Body: {roots: [...]} — start a scan. If roots is omitted, use
GET     /api/library/scan/status                               CSRF: no
GET     /api/library/stats                                     CSRF: no
GET     /api/library/tags                                      CSRF: no
DELETE  /api/library/tags/<int:tag_id>                         CSRF: yes
POST    /api/live/parse_url                                    CSRF: yes  — Helper: tell the UI whether a given URL is recognized as a
GET     /api/live/recordings                                   CSRF: no   — List all recordings (active + finished). Used by the panel.
GET     /api/live/recordings/<recording_id>                    CSRF: no   — Single-recording detail. 404 if not found.
GET     /api/live/status                                       CSRF: no   — Module-wide capability + current counts. Cheap, polled by the UI.
POST    /api/live/unwatch                                      CSRF: yes  — Cancel a watch. Body: {recording_id}.
POST    /api/live/watch                                        CSRF: yes  — Arm a watch on a live URL. Body: {url, output_dir}. Optional:
GET     /api/login_templates                                   CSRF: no   — List the available LOGIN templates for the config-form picker.
POST    /api/logs/clear                                        CSRF: yes  — v3.47.7: truncate the active log file and remove rotated archives.
GET     /api/logs/tail                                         CSRF: no   — Return the last N lines of the log file. Useful for remote debug
POST    /api/macros/delete/<sid>/<name>                        CSRF: yes  — Delete a macro.
GET     /api/macros/get/<sid>/<name>                           CSRF: no   — Get one macro by site_id + name.
GET     /api/macros/list                                       CSRF: no   — List all stored macros. ?site_id=X to filter.
POST    /api/macros/replay/<sid>/<name>                        CSRF: yes  — Execute a stored macro in a fresh standalone browser context.
GET     /api/macros/replay_status                              CSRF: no   — Cheap probe: is Playwright installed? What action kinds are
POST    /api/macros/save                                       CSRF: yes  — Record/overwrite a macro. Body: {site_id, name, actions,
POST    /api/macros/validate                                   CSRF: yes  — Static-validate a macro without saving. Body: {actions: [...]}
POST    /api/marketplace/export/<sid>                          CSRF: yes  — Export site `sid` as a portable bundle. Body may include:
POST    /api/marketplace/import                                CSRF: yes  — Import a bundle. Body: {bundle: <obj>, target_site_id: <str>,
POST    /api/marketplace/import/preview                        CSRF: yes  — Read-only preview mirror of /api/marketplace/import (Cut 3).
POST    /api/multi_conn/probe                                  CSRF: yes  — Probe whether a URL supports multi-connection. UI 'Test' button.
GET     /api/multi_conn/status                                 CSRF: no   — Multi-conn module availability + per-site enablement summary.
GET     /api/notify/apprise/schemes                            CSRF: no   — List recognized apprise schemes (for the UI to show in a hint).
GET     /api/notify/apprise/settings                           CSRF: no   — Return current global apprise settings.
POST    /api/notify/apprise/settings                           CSRF: yes  — Save global apprise settings + apply to running dispatcher.
POST    /api/notify/apprise/test                               CSRF: yes  — Send a test notification to all configured URLs. Returns per-URL
POST    /api/notify/apprise/validate                           CSRF: yes  — Validate a list of apprise URLs without sending. Returns per-URL
GET     /api/notify/presets                                    CSRF: no   — #91 — pre-built notification URL templates for common services.
GET     /api/openapi.json                                      CSRF: no   — Return the OpenAPI 3.1 spec for this BD instance. Reflects the
GET     /api/openapi/parity                                    CSRF: no   — REST/UI parity audit. Reports which API routes exist.
GET     /api/pair                                              CSRF: no   — Return data for the QR pairing modal: the LAN URL, an optional
POST    /api/pair/redeem                                       CSRF: no   — Phase 40: complete the QR pairing flow started in Phase 37.
GET     /api/palette/commands                                  CSRF: no
POST    /api/pause_all                                         CSRF: yes
GET     /api/phoenix/brands/<network>                          CSRF: no   — List all brands in a network.
POST    /api/phoenix/lookup                                    CSRF: yes  — Look up a URL in the catalog. Returns the best match + confidence.
GET     /api/phoenix/networks                                  CSRF: no   — List all known networks with entry counts.
POST    /api/phoenix/suggest                                   CSRF: yes  — Get a routing suggestion: route to existing site or create new.
GET     /api/playground/status                                 CSRF: no
POST    /api/playground/test                                   CSRF: yes  — Body: {url, selectors: [...], cookies: {...}, use_playwright: bool}
GET     /api/plex_advanced/library_stats/<sid>                 CSRF: no
POST    /api/plex_advanced/mark/<sid>/<int:rating_key>         CSRF: yes  — Body: {watched: True/False}.
GET     /api/plex_advanced/on_deck/<sid>                       CSRF: no
GET     /api/plex_advanced/recently_added/<sid>                CSRF: no
GET     /api/plex_advanced/search/<sid>                        CSRF: no
GET     /api/plex_advanced/server_info/<sid>                   CSRF: no
GET     /api/plex_advanced/status                              CSRF: no
GET     /api/plex_deep/backend_status                          CSRF: no   — Report which Plex backend(s) are available. The raw HTTP
GET     /api/plugins/config                                    CSRF: no   — GET: current plugin-load config (enabled/disabled/order/allow_full_access)
POST    /api/plugins/config                                    CSRF: yes  — GET: current plugin-load config (enabled/disabled/order/allow_full_access)
GET     /api/plugins/events                                    CSRF: no   — Documentation for plugin authors: known hook events + payloads.
POST    /api/plugins/install                                   CSRF: yes  — Install an uploaded plugin file via the managed install path -- the GUI
GET     /api/plugins/installed                                 CSRF: no   — GUI-facing managed-install registry + the no-sandbox disclaimer + the
POST    /api/plugins/reload                                    CSRF: yes  — Scan and (re)load all plugins from the plugin dir.
GET     /api/plugins/status                                    CSRF: no
POST    /api/plugins/uninstall                                 CSRF: yes  — Remove a managed-installed plugin (destructive; the GUI gates this behind
GET     /api/provenance/query                                  CSRF: no   — Query the ledger by url/filename/sha256/site/time range.
GET     /api/provenance/stats                                  CSRF: no
POST    /api/provenance/verify                                 CSRF: yes  — Run a full chain verification. Slow on large ledgers; intended
GET     /api/push/info                                         CSRF: no   — Returns the server's VAPID public key + whether push is available
POST    /api/push/subscribe                                    CSRF: yes  — Register a PushSubscription from the browser. Body is the
GET     /api/push/subscriptions                                CSRF: no
POST    /api/push/test                                         CSRF: yes  — Send a test push to all subscribers. Useful for verifying setup.
POST    /api/push/unsubscribe                                  CSRF: yes
GET     /api/queue/dead_letter                                 CSRF: no   — Phase 2 Cut 2.1: list dead-lettered jobs (terminal, retry-exhausted or
POST    /api/queue/dead_letter/requeue                         CSRF: yes  — Phase 2 Cut 2.1: requeue one dead-lettered job back to pending (retry
GET     /api/queue/preflight                                   CSRF: no   — Read-only go/no-go strip for the queue (Cut 4). Aggregates existing
GET     /api/queue/v2                                          CSRF: no   — SPA-shaped queue snapshot. Four buckets:
POST    /api/queue/v2/add_url                                  CSRF: yes  — v3.66.8 — enqueue a single URL on a configured site.
POST    /api/queue/v2/bulk_cancel                              CSRF: yes  — Cancel many URLs across one or more sites.
POST    /api/queue/v2/cancel                                   CSRF: yes  — Cancel one URL. Body: {site_id, url}. Marks the job as stopped
GET     /api/queue/v2/job_log                                  CSRF: no   — Last N events for one URL. Query: site_id, url, limit (default
GET     /api/queue/v2/job_log_diff                             CSRF: no   — Return two job logs + a precomputed unified diff.
GET     /api/queue_templates                                   CSRF: no
POST    /api/queue_templates                                   CSRF: yes
DELETE  /api/queue_templates/<int:tid>                         CSRF: yes
GET     /api/queue_templates/<int:tid>                         CSRF: no
PUT     /api/queue_templates/<int:tid>                         CSRF: yes
POST    /api/queue_templates/<int:tid>/apply/<sid>             CSRF: yes  — Import a template into a site's queue. Mode `append` (default)
GET     /api/quick_add                                         CSRF: no   — Add a single URL to whichever site matches its hostname.
POST    /api/quick_add                                         CSRF: yes  — Add a single URL to whichever site matches its hostname.
GET     /api/ramdisk/<sid>                                     CSRF: no
GET     /api/rate_limit/status                                 CSRF: no   — Live snapshot of the rate limiter: global caps, per-domain
POST    /api/rebalance/execute                                 CSRF: yes
GET     /api/rebalance/inventory                               CSRF: no
POST    /api/rebalance/plan                                    CSRF: yes
GET     /api/recommendations/recent                            CSRF: no
GET     /api/recommendations/similar                           CSRF: no
GET     /api/recommendations/top                               CSRF: no   — Combined top tags + top performers for dashboard.
POST    /api/replication/restore                               CSRF: yes  — Restore a CONFIGURED store from its file replica into a server-chosen
POST    /api/replication/start                                 CSRF: yes  — Start the Litestream replicate sidecar. Fail-closed: returns
GET     /api/replication/status                                CSRF: no   — Read-only durability snapshot. Wraps ``db_replication.replication_status``
POST    /api/replication/stop                                  CSRF: yes  — Stop the running sidecar. Idempotent: ``{ok: True, stopped: bool}``.
GET     /api/report_center/sections                            CSRF: no
POST    /api/resume_all                                        CSRF: yes
POST    /api/retention/apply                                   CSRF: yes  — Apply retention. Body: {dry_run: bool, confirm_ids?: [int],
GET     /api/retention/audit                                   CSRF: no   — Recent retention deletion audit log. ?dry_run=1 for dry-run rows
POST    /api/retention/mark_excluded/<int:hid>                 CSRF: yes  — Toggle 'do not delete' on a history row. Body: {excluded: bool}.
GET     /api/retention/preview/<sid>                           CSRF: no   — Show what retention would delete for one site (dry-run inspection).
GET     /api/retry_policy                                      CSRF: no   — Show the configured backoff curves per failure class. Used
POST    /api/retry_policy/classify                             CSRF: yes  — Test the classifier with a sample error message and/or HTTP
GET     /api/rights/audit                                      CSRF: no
POST    /api/rights/block_hash                                 CSRF: yes  — Block a perceptual hash.
POST    /api/rights/block_url                                  CSRF: yes  — Block a URL pattern. Body: {pattern, reason?, added_by?}.
GET     /api/rights/blocklist                                  CSRF: no   — List all blocklist entries.
POST    /api/rights/remove/<int:bid>                           CSRF: yes
GET     /api/route_preview                                     CSRF: no   — Dry-run routing: given URL(s), return which site each would
POST    /api/route_preview                                     CSRF: yes  — Dry-run routing: given URL(s), return which site each would
POST    /api/route_urls                                        CSRF: yes  — POST {"text": "url1\nurl2\n..."}  â†’  distributes to matching sites.
POST    /api/runners/pause_all                                 CSRF: yes  — Pause every site's runner (stops dequeueing new URLs; in-flight
POST    /api/runners/resume_all                                CSRF: yes  — Inverse of pause_all — restart all paused runners. Idempotent
GET     /api/runs                                              CSRF: no   — Most-recent-first list of tracked download runs (read-only).
GET     /api/runs/<int:run_id>/timeline                        CSRF: no   — A single run + its ordered event timeline (read-only). 404 with a JSON
GET     /api/saved_searches                                    CSRF: no
POST    /api/saved_searches                                    CSRF: yes
DELETE  /api/saved_searches/<int:search_id>                    CSRF: yes
PATCH   /api/saved_searches/<int:search_id>                    CSRF: yes  — F3.1: partial update of a saved search. Body carries any subset of
POST    /api/saved_searches/<int:search_id>/run                CSRF: yes
GET     /api/saved_searches/digest                             CSRF: no
GET     /api/scene_score/bottom                                CSRF: no   — Lowest-scored files — candidates for review or re-download.
GET     /api/scene_score/distribution                          CSRF: no   — Score distribution histogram. Used by dashboard summary card.
GET     /api/scene_score/top                                   CSRF: no   — Highest-scored files in the library (or one site).
POST    /api/scheduled_exports/add                             CSRF: yes  — Register a new scheduled export. Body: {label, format,
GET     /api/scheduled_exports/list                            CSRF: no   — List all scheduled exports + their last-run status.
POST    /api/scheduled_exports/remove/<int:sid>                CSRF: yes  — Delete a scheduled export.
POST    /api/scheduled_exports/run_now                         CSRF: yes  — Force-run all due exports immediately (bypasses scheduler).
GET     /api/schedules                                         CSRF: no   — List all recurring-capture schedules + their last/next-run state.
POST    /api/schedules                                         CSRF: yes  — Register a recurring-capture schedule. Body: {site_id,
POST    /api/schedules/<int:sid>/remove                        CSRF: yes  — Delete a recurring-capture schedule.
POST    /api/schedules/<int:sid>/run_now                       CSRF: yes  — Force-run one schedule immediately (bypasses the cadence guard).
GET     /api/schedules/export.ics                              CSRF: no   — F7: iCalendar (.ics) export of all recurring-capture schedules --
POST    /api/scrape_listing                                    CSRF: yes  — Phase 71 (v3.43.16): server-side listing-page scrape. Paste a
POST    /api/scrapling/fingerprint                             CSRF: yes  — Build a fingerprint from a snippet of HTML + selector. Used by
POST    /api/scrapling/recover                                 CSRF: yes  — Test selector recovery against a snippet of HTML + saved
GET     /api/scrapling/status                                  CSRF: no   — Module availability + runtime counters.
POST    /api/scrapling/turnstile_check                         CSRF: yes  — Pure detection — does the supplied HTML look like a Turnstile
GET     /api/search                                            CSRF: no   — GET /api/search?q=<query>&site_id=<sid>&status=<s>&limit=<n>
POST    /api/search/all                                        CSRF: yes  — Search every site that has use_search_extractor=True for a
GET     /api/search/facets                                     CSRF: no   — Read-only facet breakdown of history matches by site and status for the
POST    /api/search/site                                       CSRF: yes  — Search one configured site for a query string.
GET     /api/search/sites_available                            CSRF: no   — List sites that have use_search_extractor=True AND a valid
POST    /api/secrets/change_password                           CSRF: yes  — Re-encrypt all stored passwords with a new master password.
POST    /api/secrets/configure                                 CSRF: yes  — Switch the active backend. Body: {"backend": "windows_credential"|
POST    /api/secrets/delete                                    CSRF: yes  — Remove a stored password from the active backend. Body:
POST    /api/secrets/extension/fetch_one                       CSRF: yes  — Extension API: fetch ONE password for a chosen entry.
GET     /api/secrets/extension/list_for_origin                 CSRF: no   — Extension API: return entries matching a URL. Used by the
GET     /api/secrets/extension/list_paired                     CSRF: no   — Vault settings UI: list paired extensions with metadata.
POST    /api/secrets/extension/pair                            CSRF: yes  — Extension-side step: redeem a pairing token for a long-lived
POST    /api/secrets/extension/pair_issue                      CSRF: yes  — Server-side step: generate a pairing token. The vault settings
GET     /api/secrets/extension/ping                            CSRF: no   — Liveness check for the extension. Validates the token and
POST    /api/secrets/extension/revoke                          CSRF: yes  — Vault settings UI: revoke a paired extension by its short ID
POST    /api/secrets/import_apply                              CSRF: yes  — Save selected records into the active backend.
POST    /api/secrets/import_file                               CSRF: yes  — Parse an uploaded password-manager export. Returns the records
POST    /api/secrets/lock                                      CSRF: yes  — Lock the master-password backend (forget the derived key).
POST    /api/secrets/migrate                                   CSRF: yes  — Move all plaintext passwords from sites_config.json to the
GET     /api/secrets/status                                    CSRF: no   — Report the active backend, its lock status, and whether there
POST    /api/secrets/unlock                                    CSRF: yes  — Unlock the master-password backend. Body: {"password": "..."}.
GET     /api/secrets/usage                                     CSRF: no   — Cut 7: read-only secret USAGE map — which stored secret keys exist and
POST    /api/selector_drift/reset/<sid>                        CSRF: yes  — Reset drift state for one site — used after a successful
GET     /api/selector_drift/status                             CSRF: no   — Snapshot of every site's drift state. Stale-flagged first.
GET     /api/selector_drift/status/<sid>                       CSRF: no   — One site's drift state.
GET     /api/selftest                                          CSRF: no   — Return self-test results. Two modes:
POST    /api/semantic/reindex                                  CSRF: yes
POST    /api/semantic/search                                   CSRF: yes
GET     /api/semantic/status                                   CSRF: no
GET     /api/session_history                                   CSRF: no   — Recent session_history events for the UI's real-time log.
GET     /api/session_status                                    CSRF: no   — Returns the state of every keep-alive thread. UI polls this for
GET     /api/settings/env/effective                            CSRF: no
GET     /api/settings/envfile                                  CSRF: no
POST    /api/settings/envfile                                  CSRF: yes
GET     /api/settings/global/effective                         CSRF: no
GET     /api/settings/schema                                   CSRF: no
GET     /api/settings/secrets/health                           CSRF: no
GET     /api/settings/site/<sid>/editable                      CSRF: no   — gui-safe editable fields + current values (read-only; secrets excluded).
GET     /api/settings/site/<sid>/effective                     CSRF: no
POST    /api/settings/site/<sid>/validate                      CSRF: yes  — Dry-run validation/gate for a proposed gui-safe change set. NO write.
GET     /api/settings/store-raw                                CSRF: no
POST    /api/settings/store-raw                                CSRF: yes
POST    /api/settings/store-raw/rekey                          CSRF: yes
GET     /api/settings/vpn/summary                              CSRF: no
GET     /api/shares                                            CSRF: no
POST    /api/shares                                            CSRF: yes
DELETE  /api/shares/<token_id>                                 CSRF: yes
GET     /api/shares/access_log                                 CSRF: no
GET     /api/shortcuts                                         CSRF: no   — Catalog of all keyboard shortcuts. UI renders this as the
POST    /api/sites                                             CSRF: yes
DELETE  /api/sites/<sid>                                       CSRF: yes
PUT     /api/sites/<sid>                                       CSRF: yes
POST    /api/sites/<sid>/account_pool/reset/<int:account_idx>  CSRF: yes  — Reset one account to 'available' — clears dead state, cooldown,
GET     /api/sites/<sid>/account_pool/status                   CSRF: no   — Show pool state: per-account state (available / in_use /
GET     /api/sites/<sid>/accounts                              CSRF: no   — List accounts with their state. Passwords masked; cookie_file
POST    /api/sites/<sid>/accounts/reset_cooldown               CSRF: yes  — Force-clear the cooldown on one account (or all). Body:
POST    /api/sites/<sid>/accounts/rotate                       CSRF: yes  — Force a rotation to the next available account. Useful when the
POST    /api/sites/<sid>/ai/detect_login                       CSRF: yes  — Run AI login-form detection against a snapshot from the live
POST    /api/sites/<sid>/ai_reanalyze                          CSRF: yes
POST    /api/sites/<sid>/auto_submit_decision                  CSRF: yes  — Record the operator's approve/decline choice for a login form or
POST    /api/sites/<sid>/bulk_approve                          CSRF: yes
POST    /api/sites/<sid>/bulk_delete                           CSRF: yes
POST    /api/sites/<sid>/bulk_pause                            CSRF: yes
POST    /api/sites/<sid>/bulk_priority                         CSRF: yes
POST    /api/sites/<sid>/bulk_resume                           CSRF: yes
POST    /api/sites/<sid>/bulk_retry                            CSRF: yes
POST    /api/sites/<sid>/bulk_url_transform                    CSRF: yes  — Apply a regex find/replace across every URL in this site's queue.
POST    /api/sites/<sid>/candidates/inspect                    CSRF: yes  — #1 — Dry-run candidate inspector. Body: {"html": "...", "url"?: "..."}.
GET     /api/sites/<sid>/captcha/stats                         CSRF: no   — Per-site solver metrics — submissions, solves, failures, timeouts,
POST    /api/sites/<sid>/captcha/test                          CSRF: yes  — Verify the configured API key works + report balance. Hits the
POST    /api/sites/<sid>/clear                                 CSRF: yes
POST    /api/sites/<sid>/clone                                 CSRF: yes
GET     /api/sites/<sid>/cookies/export                        CSRF: no
POST    /api/sites/<sid>/cookies/import                        CSRF: yes
POST    /api/sites/<sid>/diff                                  CSRF: yes  — Preview what a proposed config change would alter. Body: the
GET     /api/sites/<sid>/events                                CSRF: no   — Live event log. Polled by the UI's event drawer.
GET     /api/sites/<sid>/export                                CSRF: no   — Export one site's config as a portable JSON envelope.
GET     /api/sites/<sid>/export_watchlist                      CSRF: no   — Phase 8.2: export URLs in failed and/or needs_review status as a
DELETE  /api/sites/<sid>/heuristic/fingerprint                 CSRF: yes  — Per-site URL pattern fingerprint accumulated from successful
GET     /api/sites/<sid>/heuristic/fingerprint                 CSRF: no   — Per-site URL pattern fingerprint accumulated from successful
GET     /api/sites/<sid>/history/export                        CSRF: no
GET     /api/sites/<sid>/hooks/spillover_check                 CSRF: no   — Show which download dir would be chosen RIGHT NOW based on the
POST    /api/sites/<sid>/hooks/test                            CSRF: yes  — Test one specific hook sink. Body: {"kind": "post_cmd"|"webhook"|
GET     /api/sites/<sid>/jd/coverage                           CSRF: no   — JD-3 (v3.66.694): report whether JDownloader covers this site's host
GET     /api/sites/<sid>/jd/diagnose                           CSRF: no   — Run JD bridge connectivity check for a site. Returns the full
GET     /api/sites/<sid>/jellyfin/diagnose                     CSRF: no   — Probe Jellyfin connectivity + auth for a site.
GET     /api/sites/<sid>/jellyfin/libraries                    CSRF: no   — List the Jellyfin libraries. UI uses this to confirm setup.
POST    /api/sites/<sid>/jobs/bulk_delete                      CSRF: yes  — Delete N URLs from the queue. Body: {urls: [...]}.
POST    /api/sites/<sid>/jobs/bulk_mark                        CSRF: yes  — Mark N URLs at once. Body: {urls: [...], status: str, message?: str}
POST    /api/sites/<sid>/jobs/bulk_priority                    CSRF: yes  — Set priority label on N URLs. Body: {urls: [...], priority: str}.
GET     /api/sites/<sid>/jobs/detail                           CSRF: no   — GET /api/sites/<sid>/jobs/detail?url=<url> → full job state.
POST    /api/sites/<sid>/jobs/mark                             CSRF: yes  — Phase 51 (v3.37.7): mark a single URL's job status. Used by the
POST    /api/sites/<sid>/jobs/reorder                          CSRF: yes  — Drag-to-reorder. Body: {ordering: {url: ord_int, ...}}.
POST    /api/sites/<sid>/keep_alive_toggle                     CSRF: yes  — Enable / disable keep-alive for a site. Body: {"enabled": bool}.
POST    /api/sites/<sid>/learned/apply_repairs                 CSRF: yes  — Phase 29 commit: apply operator-verified diff_repair proposals to a
GET     /api/sites/<sid>/learned/export                        CSRF: no   — Return the site's learned block as a downloadable JSON file.
POST    /api/sites/<sid>/learned/import                        CSRF: yes  — Accept a previously-exported (or hand-written) learned block.
POST    /api/sites/<sid>/load_cookies                          CSRF: yes
POST    /api/sites/<sid>/load_urls                             CSRF: yes
POST    /api/sites/<sid>/login                                 CSRF: yes
POST    /api/sites/<sid>/login_manual_cancel                   CSRF: yes  — User abandoned the manual login. Close the browser, no cookies.
POST    /api/sites/<sid>/login_manual_done                     CSRF: yes  — Phase 4.4: user finished a manual login. Capture cookies from the
POST    /api/sites/<sid>/login_template/apply                  CSRF: yes  — Apply a login template to a site. Body: {login_template: "id"}.
POST    /api/sites/<sid>/login_verify                          CSRF: yes  — v3.43.51: run the post-wizard headless verification. Replays
GET     /api/sites/<sid>/login_verify_status                   CSRF: no   — v3.43.51: read the last verify result. Used by the wizard's
POST    /api/sites/<sid>/manual_login                          CSRF: yes  — Phase 19: skip auto-login and open the browser straight to the
POST    /api/sites/<sid>/pause                                 CSRF: yes
GET     /api/sites/<sid>/pending_approvals                     CSRF: no   — T11 (v3.66.264): the per-site read surface the SPA approval gate
GET     /api/sites/<sid>/plex/diagnose                         CSRF: no   — Probe Plex connectivity + auth for a site. Same structured
GET     /api/sites/<sid>/plex/sections                         CSRF: no   — List the Plex libraries available on the configured server.
POST    /api/sites/<sid>/post_reveal_decision                  CSRF: yes  — F12: record the operator's approve/decline choice for a two-step
POST    /api/sites/<sid>/priority                              CSRF: yes
POST    /api/sites/<sid>/profile/seed                          CSRF: yes  — #7 — One-click LOCAL worker-profile seed. Copies the manual-login
GET     /api/sites/<sid>/profile/status                        CSRF: no   — #8 — Per-site browser-profile storage status. For the manual / main /
POST    /api/sites/<sid>/prune_selectors                       CSRF: yes  — Remove selectors with a miss/total ratio above the threshold.
GET     /api/sites/<sid>/qb/diagnose                           CSRF: no   — Run qB bridge connectivity check for a site. Mirrors the JD
GET     /api/sites/<sid>/queue                                 CSRF: no   — Phase 4.5/4.6: paginated server-side query of the queue table.
GET     /api/sites/<sid>/queue/counts                          CSRF: no   — Aggregate by-status counts for a single site's queue. Cheaper than
GET     /api/sites/<sid>/queue/export                          CSRF: no   — v3.49 (#62): dump the live queue to a JSON snapshot. Used by the
GET     /api/sites/<sid>/queue/grouped                         CSRF: no   — v3.49 (#57): bucket the queue by host/path/status/priority for the
POST    /api/sites/<sid>/queue/import                          CSRF: yes  — v3.49 (#63): import a queue snapshot. Body: {"rows": [...], "mode":
POST    /api/sites/<sid>/queue/save_template                   CSRF: yes  — Convenience: save THIS site's current queue as a template.
GET     /api/sites/<sid>/queue/search                          CSRF: no
POST    /api/sites/<sid>/queue_url                             CSRF: yes  — Force a URL into a specific site's queue, bypassing the
POST    /api/sites/<sid>/randomize_fingerprint                 CSRF: yes  — Phase 7.1: regenerate a random fingerprint for this site. Useful
GET     /api/sites/<sid>/readiness                             CSRF: no   — Read-only composite readiness for one site (Cut 4) -> green/amber/red +
POST    /api/sites/<sid>/reconnect                             CSRF: yes  — Force the keep-alive checker to run immediately for this site.
POST    /api/sites/<sid>/reorder                               CSRF: yes
POST    /api/sites/<sid>/reset_learned                         CSRF: yes  — Phase 5: clear any learned selectors for this site. Useful if the
POST    /api/sites/<sid>/resume                                CSRF: yes
POST    /api/sites/<sid>/retry                                 CSRF: yes
POST    /api/sites/<sid>/retry_one                             CSRF: yes
GET     /api/sites/<sid>/selector_stats                        CSRF: no   — Phase 13.3: hit-rate dashboard data. Returns each learned selector
POST    /api/sites/<sid>/session/reuse_onboarding              CSRF: yes  — Reuse the authenticated onboarding session for downloads (3e/C1).
POST    /api/sites/<sid>/start                                 CSRF: yes
GET     /api/sites/<sid>/stash/diagnose                        CSRF: no   — Probe Stash connectivity + auth for a site. Returns the same
POST    /api/sites/<sid>/stash/preview_url                     CSRF: yes  — Pre-download scrape preview. Surfaces what Stash would extract
POST    /api/sites/<sid>/stop                                  CSRF: yes
POST    /api/sites/<sid>/storage_tier/run_now                  CSRF: yes  — Trigger a migration pass immediately, out-of-cycle. Useful
GET     /api/sites/<sid>/storage_tier/status                   CSRF: no   — Per-site storage tier status — feature enabled, dest config,
POST    /api/sites/<sid>/take_over_cancel                      CSRF: yes
POST    /api/sites/<sid>/take_over_done                        CSRF: yes
POST    /api/sites/<sid>/take_over_url                         CSRF: yes  — Open a non-headless Chromium at the given URL with the site's
POST    /api/sites/<sid>/teach_cancel                          CSRF: yes
POST    /api/sites/<sid>/teach_commit                          CSRF: yes
POST    /api/sites/<sid>/teach_save_template                   CSRF: yes  — v3.43.16: save the current teach picks as a reusable user template.
POST    /api/sites/<sid>/teach_test_download                   CSRF: yes  — v3.43.16: dry-run the picks against the live page AND pull ~2 MB
POST    /api/sites/<sid>/teach_verify                          CSRF: yes
POST    /api/sites/<sid>/template/dry_run                      CSRF: yes  — #5 — Static template test runner. Body: {"html"?: "...", "url"?: "..."}.
POST    /api/sites/<sid>/template_capture_cancel               CSRF: yes  — Cancel an in-flight onboarding capture (CAP-CANCEL).
POST    /api/sites/<sid>/template_capture_finish               CSRF: yes  — Finish & SAVE an in-flight onboarding capture (CAP-FINISH).
POST    /api/sites/<sid>/template_onboard                      CSRF: yes  — Run template onboarding for the site (manual trigger from the UI).
GET     /api/sites/<sid>/template_status                       CSRF: no   — Read-only reviewed-template status for the site's page.
POST    /api/sites/<sid>/templates/apply                       CSRF: yes  — Merge a template's learned block into the site config. Existing
GET     /api/sites/<sid>/timeline                              CSRF: no   — Per-URL event timeline. Returns ALL events for one URL (not capped
POST    /api/sites/<sid>/watch/scan_now                        CSRF: yes  — Force an immediate scan of the configured watch folder for
GET     /api/sites/<sid>/watch/status                          CSRF: no   — Show watch-folder status: enabled, folder, last few processed
GET     /api/sites/<sid>/window/status                         CSRF: no   — Per-site download-window introspection: is this site
POST    /api/sites/bulk_csv                                    CSRF: yes  — Bulk-create sites from a CSV or XLSX. The file can arrive as an
GET     /api/sites/csv_template                                CSRF: no   — Download the CSV template for bulk site import. Data rows are at
POST    /api/sites/import                                      CSRF: yes  — Import a config exported by /export. Body: the export envelope
POST    /api/sites/preview_filename                            CSRF: yes  — #15 — filename templating live preview.
GET     /api/sites/schema                                      CSRF: no   — #58 — JSON Schema describing sites_config.json.
GET     /api/sites/selector_health                             CSRF: no   — #103 — per-site selector drift status.
GET     /api/sites/v2                                          CSRF: no   — SPA-shaped per-site state for the Sites tab. Each entry has
POST    /api/sites/v2/bulk                                     CSRF: yes  — Apply one action to a list of sites.
POST    /api/sites/validate                                    CSRF: yes  — Dry-run validate a config without saving. Body: the config dict.
GET     /api/sites/xlsx_template                               CSRF: no   — Download the XLSX template for bulk site import — a formatted
GET     /api/sites_list                                        CSRF: no   — Lightweight list of all configured sites, used by the browser
GET     /api/sse_status                                        CSRF: no   — Diagnostic snapshot: how many SSE subscribers are connected,
POST    /api/start_all                                         CSRF: yes
GET     /api/stats                                             CSRF: no
GET     /api/stats/bandwidth                                   CSRF: no   — Phase 8.3: 1-hour rolling bandwidth chart data. Returns up to 3600
GET     /api/stats/timeline                                    CSRF: no   — Per-day buckets of done/failed counts and bytes downloaded for the last
GET     /api/status                                            CSRF: no   — Phase 4.6: light=1 omits jobs/url_order. Frontend uses light mode
GET     /api/storage/validate                                  CSRF: no   — Read-only FS diagnosis of a path (Cut 4 operator intelligence).
POST    /api/storage_rebalance/execute                         CSRF: yes  — Execute a rebalance plan. Body: {plan, dry_run}.
POST    /api/storage_rebalance/inventory                       CSRF: yes  — Per-disk usage inventory. Body: {paths: [...]}.
POST    /api/storage_rebalance/plan                            CSRF: yes  — Generate a rebalance plan. Body: {paths, strategy, max_moves}.
GET     /api/stream                                            CSRF: no
POST    /api/stream/rotate_secret                              CSRF: yes  — Rotate the stream-token signing secret. Invalidates every
POST    /api/stream/token/<int:hid>                            CSRF: yes
POST    /api/subtitles/fetch/<int:hid>                         CSRF: yes  — Download subtitles for one history row's file. Body may include
GET     /api/subtitles/status                                  CSRF: no
POST    /api/supervisor/configure                              CSRF: yes  — Hot-reload supervisor rate limits.
GET     /api/supervisor/status                                 CSRF: no   — Token-bucket bandwidth supervisor stats.
GET     /api/synthetic_tests/list                              CSRF: no   — List all configured fixtures.
POST    /api/synthetic_tests/run_all                           CSRF: yes  — Run every fixture; report per-site pass/fail. Safe — no
POST    /api/tags/add                                          CSRF: yes  — Bulk add: body {history_ids: [...], tag: '...'}.
GET     /api/tags/all                                          CSRF: no   — List all distinct tags with use counts.
POST    /api/tags/delete                                       CSRF: yes  — Remove tag from every row. Body {tag}.
GET     /api/tags/for/<int:hid>                                CSRF: no   — List tags assigned to one history row.
POST    /api/tags/for_many                                     CSRF: yes  — Batch tag lookup. Body {history_ids: [...]}. Returns
POST    /api/tags/remove                                       CSRF: yes  — Bulk remove: body {history_ids: [...], tag: '...'}.
POST    /api/tags/rename                                       CSRF: yes  — Bulk rename: body {old, new}. Merges if new already exists
GET     /api/tags/rows/<tag>                                   CSRF: no   — Find all rows tagged X. ?site_id=Y to filter; ?limit=N.
GET     /api/tags/suggest/<int:hid>                            CSRF: no   — Run tag_inference on a row's filename — returns suggested tags
POST    /api/template/extract                                  CSRF: yes  — Take pasted HTML, return a draft site template + ranked
POST    /api/template/extract_login                            CSRF: yes  — Take a pasted login PAGE's HTML, return a learned.login block —
POST    /api/template/refine                                   CSRF: yes  — Send a rule-based draft template + the source HTML to the
POST    /api/template/sandbox                                  CSRF: yes  — Fetch a live URL + apply a draft template's selectors, return
POST    /api/template/test_extract                             CSRF: yes  — B2 (v3.66.240): set/clear a per-site DRAFT-TEST OVERRIDE and trigger one
GET     /api/template_manager                                  CSRF: no   — #10 — Template Manager: list reviewed + draft templates with status,
POST    /api/template_manager/disable                          CSRF: yes  — #10 — Disable a reviewed template (status -> disabled, so it is no longer
GET     /api/template_manager/drift                            CSRF: no   — Read-only drift of a draft/candidate vs its reviewed gold. ?file=<name>.
GET     /api/template_manager/inventory                        CSRF: no   — Rich, read-only inventory of all four template dirs.
POST    /api/template_manager/promote                          CSRF: yes  — #10 — Promote a draft to a reviewed template (explicit operator action).
POST    /api/template_manager/promote_check                    CSRF: yes  — GCW guided-mode READ-ONLY promote preflight.
GET     /api/templates                                         CSRF: no   — Return the template library, optionally filtered to those matching
GET     /api/tg/settings                                       CSRF: no   — Read TG bot settings (subset of notify_apprise.json).
POST    /api/tg/settings                                       CSRF: yes  — Save TG bot settings + apply to running bot.
GET     /api/tg/status                                         CSRF: no   — Bot health snapshot.
POST    /api/tg/test                                           CSRF: yes  — Send a manual test message to all allowlisted chat IDs.
POST    /api/thumbnail_sheets/contact_sheet/<int:hid>          CSRF: yes  — Generate a 4x4 contact sheet for one history row. Body may
POST    /api/thumbnail_sheets/single/<int:hid>                 CSRF: yes  — Generate one thumbnail at a given percentage of the video.
POST    /api/thumbnails/regenerate                             CSRF: yes  — Queue a thumbnail (re)generation for a specific file.
GET     /api/thumbnails/serve/<site_id>/<path:filename>        CSRF: no   — Serve a thumbnail file. Used by the UI to display thumbs inline.
GET     /api/thumbnails/status                                 CSRF: no   — ffmpeg availability + worker stats.
POST    /api/thumbs/contact_sheet                              CSRF: yes
POST    /api/thumbs/single                                     CSRF: yes
POST    /api/thumbs/sprite_sheet                               CSRF: yes
GET     /api/tools                                             CSRF: no   — List registered companion tools from tools/registry.json.
GET     /api/tools/available                                   CSRF: no
POST    /api/tools/run                                         CSRF: yes
POST    /api/tpdb/apply/<int:hid>                              CSRF: yes  — Apply TPDB metadata to the file on disk: write .nfo + atom-embed.
POST    /api/tpdb/lookup/<int:hid>                             CSRF: yes  — Look up TPDB metadata for one history row. Returns scene info
POST    /api/ui_events                                         CSRF: yes  — Ingest a batch of UI events from the frontend logger. Events
GET     /api/ui_events/download                                CSRF: no   — Stream the current ui_events.log file to the user as a download.
GET     /api/user_templates                                    CSRF: no   — List all user-saved templates with full data. The combined picker
POST    /api/user_templates                                    CSRF: yes  — Save a new user template OR update an existing one.
DELETE  /api/user_templates/<tid>                              CSRF: yes  — Remove a user template by id. 404 if not found.
GET     /api/user_templates/export                             CSRF: no   — Stream the user templates payload as a download. JSON file
POST    /api/user_templates/import                             CSRF: yes  — Accept a previously-exported templates payload.
POST    /api/user_templates/import/preview                     CSRF: yes  — Read-only preview of /api/user_templates/import (Cut 3).
POST    /api/vpn/auto_blacklist                                CSRF: yes
GET     /api/vpn/backends/availability                         CSRF: no
GET     /api/vpn/best_for/<sid>                                CSRF: no
GET     /api/vpn/blacklist                                     CSRF: no
POST    /api/vpn/kill_switch/<tunnel_id>/clear                 CSRF: yes
POST    /api/vpn/kill_switch/<tunnel_id>/trigger               CSRF: yes
PUT     /api/vpn/kill_switch/auto_recover                      CSRF: yes
GET     /api/vpn/kill_switch/state                             CSRF: no
GET     /api/vpn/providers                                     CSRF: no
POST    /api/vpn/providers/<provider_id>/locations             CSRF: yes  — POST not GET — credentials are required to list locations on some providers.
POST    /api/vpn/providers/<provider_id>/test_credentials      CSRF: yes
GET     /api/vpn/settings                                      CSRF: no
PUT     /api/vpn/settings                                      CSRF: yes
GET     /api/vpn/stats                                         CSRF: no
GET     /api/vpn/status                                        CSRF: no   — Overall VPN status: tunnels, kill switch state, registered providers.
POST    /api/vpn/system_killswitch/<tunnel_id>/apply           CSRF: yes
POST    /api/vpn/system_killswitch/<tunnel_id>/commit          CSRF: yes
POST    /api/vpn/system_killswitch/<tunnel_id>/plan            CSRF: yes
POST    /api/vpn/system_killswitch/<tunnel_id>/revert          CSRF: yes
GET     /api/vpn/system_killswitch/available                   CSRF: no
GET     /api/vpn/tunnels                                       CSRF: no
POST    /api/vpn/tunnels                                       CSRF: yes
DELETE  /api/vpn/tunnels/<tunnel_id>                           CSRF: yes
GET     /api/vpn/tunnels/<tunnel_id>                           CSRF: no
PATCH   /api/vpn/tunnels/<tunnel_id>                           CSRF: yes
PUT     /api/vpn/tunnels/<tunnel_id>                           CSRF: yes
POST    /api/vpn/tunnels/<tunnel_id>/cycle                     CSRF: yes
GET     /api/vpn/tunnels/<tunnel_id>/leak_test/history         CSRF: no
GET     /api/vpn/tunnels/<tunnel_id>/leak_test/latest          CSRF: no
POST    /api/vpn/tunnels/<tunnel_id>/leak_test/run             CSRF: yes
POST    /api/vpn/tunnels/<tunnel_id>/start                     CSRF: yes
POST    /api/vpn/tunnels/<tunnel_id>/stop                      CSRF: yes
POST    /api/vpn/tunnels/<tunnel_id>/webrtc_result             CSRF: yes  — Workers POST WebRTC probe results captured in browser contexts here.
GET     /api/vpn/webrtc_js                                     CSRF: no   — Return the JS snippet that workers inject into their Playwright contexts.
GET     /api/wakeup/history                                    CSRF: no
GET     /api/wakeup/should_wake                                CSRF: no
GET     /api/wayback/availability                              CSRF: no
GET     /api/wayback/snapshots                                 CSRF: no
GET     /api/weather                                           CSRF: no   — Current weather (green/yellow/red) for every configured site.
GET     /api/weather/<sid>                                     CSRF: no   — Current weather + recent probe history for one site.
POST    /api/weather/probe/<sid>                               CSRF: yes  — Force an immediate probe of one site. Useful for the operator
GET     /api/webhooks                                          CSRF: no
POST    /api/webhooks                                          CSRF: yes
DELETE  /api/webhooks/<int:wid>                                CSRF: yes
POST    /api/webhooks/drain                                    CSRF: yes  — Force a drain pass (mainly for tests).
GET     /api/webhooks/stats                                    CSRF: no
DELETE  /api/widgets/<scope>                                   CSRF: yes
GET     /api/widgets/<scope>                                   CSRF: no
PUT     /api/widgets/<scope>                                   CSRF: yes
GET     /api/widgets/all                                       CSRF: no
GET     /api/widgets/data                                      CSRF: no   — Aggregate current values for all 36 KPIs.
GET     /api/ytdlp/sites_affected                              CSRF: no   — Read-only blast-radius map: which configured sites depend on each extractor
POST    /api/ytdlp_archive/derive                              CSRF: yes  — Test URL → (extractor, id) derivation. Used by the UI to show
GET     /api/ytdlp_archive/stats                               CSRF: no   — Stats for the archive file configured on any site. The UI uses
GET     /api/ytdlp_status                                      CSRF: no   — Return {installed, version, age_days, stale} for the installed
POST    /api/ytdlp_update                                      CSRF: yes  — Manually trigger yt-dlp pip upgrade. Operator-initiated only —
```

## Non-API routes (206)

```
GET     /                                           CSRF: no   — Serve the D3 React SPA from frontend/dist/ at the site root.
GET     /<path:subpath>                             CSRF: no   — Serve the D3 React SPA from frontend/dist/ at the site root.
GET     /apple-touch-icon.png                       CSRF: no   — iOS demands PNG for apple-touch-icon. We embed a pre-rendered 180x180
GET     /cockpit                                    CSRF: no
GET     /cockpit/                                   CSRF: no
GET     /cockpit/api/activity                       CSRF: no
GET     /cockpit/api/allowlist                      CSRF: no
GET     /cockpit/api/assumptions                    CSRF: no
GET     /cockpit/api/authority/change               CSRF: no   — One Class-C change (?id=) of any kind: before/after, rollback-preview. Read-only.
GET     /cockpit/api/authority/grants               CSRF: no   — The (site x kind) grant matrix with current tier/trust and participation_eligible.
GET     /cockpit/api/authority/kinds                CSRF: no   — Registered Class-C apply kinds (action class, reverser/validator presence).
GET     /cockpit/api/authority/pending              CSRF: no   — Pending Class-C changes across all kinds (kind, site, changed keys, deadline).
GET     /cockpit/api/authority/status               CSRF: no   — Class-C apply summary across all kinds: pending, next deadline, grants active/
GET     /cockpit/api/autonomy/center                CSRF: no   — Class B autonomy overview: level, whether the cycle would apply + why, kill
GET     /cockpit/api/autonomy/governance-health     CSRF: no   — Governance monitoring: kill switch, policy, guardrails, throttle, anomalies.
GET     /cockpit/api/autonomy/metrics               CSRF: no   — Class B autonomy metrics: cycle history + action/reversal counts + throttle.
GET     /cockpit/api/autonomy/notifications         CSRF: no   — In-GUI notifications + what the next generation would add. Read-only.
GET     /cockpit/api/autonomy/queue                 CSRF: no   — Queue + proposed (dry-run) reordering + health. Read-only.
GET     /cockpit/api/autonomy/review-ops            CSRF: no   — Review-deadline tracking + pending Class C reviews. Read-only.
GET     /cockpit/api/blind-spots                    CSRF: no
GET     /cockpit/api/campaigns                      CSRF: no
POST    /cockpit/api/campaigns                      CSRF: yes
GET     /cockpit/api/capture-opportunity            CSRF: no
GET     /cockpit/api/capture-yield                  CSRF: no
POST    /cockpit/api/captures/build-multi-template  CSRF: yes  — Compare several APPROVED captures of one site into a review-required
POST    /cockpit/api/captures/build-template        CSRF: yes  — Build a recognition template from TWO recon captures of one action.
POST    /cockpit/api/captures/finish                CSRF: yes  — Stop a running interactive (noVNC) capture by writing the FINISH/CANCEL
POST    /cockpit/api/captures/goto                  CSRF: yes  — Re-navigate a running interactive capture back to its start URL by
POST    /cockpit/api/captures/normalize             CSRF: yes  — Onboarding step: build a RICH template draft from a captured .wacz and
POST    /cockpit/api/captures/pick                  CSRF: yes  — Arm / poll / clear a one-shot ACTIVE element-pick on a running
GET     /cockpit/api/collections                    CSRF: no
POST    /cockpit/api/collections                    CSRF: yes
POST    /cockpit/api/collections/add                CSRF: yes
GET     /cockpit/api/complexity                     CSRF: no
GET     /cockpit/api/compliance                     CSRF: no
GET     /cockpit/api/confidence                     CSRF: no
GET     /cockpit/api/corpus                         CSRF: no
GET     /cockpit/api/corpus/<entry_id>              CSRF: no
GET     /cockpit/api/coverage                       CSRF: no
GET     /cockpit/api/cross-site-drift               CSRF: no
GET     /cockpit/api/daily-mission                  CSRF: no
GET     /cockpit/api/debt                           CSRF: no
GET     /cockpit/api/debug-log                      CSRF: no
GET     /cockpit/api/decision-quality               CSRF: no
GET     /cockpit/api/diff                           CSRF: no
GET     /cockpit/api/drift                          CSRF: no
GET     /cockpit/api/eligibility/overview           CSRF: no   — Per-site participation eligibility + decay reasons + the capped considered set.
GET     /cockpit/api/eligibility/site               CSRF: no   — Per-site eligibility verdict (?site=): oracle tier, evidence freshness, decay
GET     /cockpit/api/eligibility/status             CSRF: no   — Eligibility-governance summary: Class C level, freshness/tier/cap thresholds,
GET     /cockpit/api/escalations                    CSRF: no
POST    /cockpit/api/escalations                    CSRF: yes
POST    /cockpit/api/escalations/clear              CSRF: yes
GET     /cockpit/api/evidence-scarcity              CSRF: no
GET     /cockpit/api/exec                           CSRF: no
GET     /cockpit/api/family-explorer                CSRF: no
GET     /cockpit/api/family-health                  CSRF: no
GET     /cockpit/api/forecasting                    CSRF: no   — Band F aggregate: every data-blocked forecasting/trend/sustainability
GET     /cockpit/api/graph                          CSRF: no
GET     /cockpit/api/guardrails/changes             CSRF: no   — The rollback ledger — recorded changes and their rolled-back state. Read-only.
GET     /cockpit/api/guardrails/pending             CSRF: no   — Outstanding unreviewed auto-changes and their fail-closed review deadlines
GET     /cockpit/api/guardrails/status              CSRF: no   — Guardrail registry, caps config, backlog/inflight, self-throttle metrics, kill
GET     /cockpit/api/health-checks                  CSRF: no
POST    /cockpit/api/health-checks/run              CSRF: yes
GET     /cockpit/api/housekeeping/log               CSRF: no   — The append-only Class B action log. Read-only.
GET     /cockpit/api/housekeeping/preview           CSRF: no   — What each Class B action WOULD do right now (suggest mode for all four) —
GET     /cockpit/api/housekeeping/status            CSRF: no   — Class B status: level, auto-eligibility, kill-switch, guardrails, counts.
GET     /cockpit/api/impact/<target>                CSRF: no
GET     /cockpit/api/impact/analyze                 CSRF: no   — Analyse one proposed change (?site=&target_kind=&action=). Read-only — never
GET     /cockpit/api/impact/overview                CSRF: no   — Per-site impact of a benign reversible probe change. Read-only.
GET     /cockpit/api/impact/status                  CSRF: no   — Impact summary across sites (benign probe). Read-only.
POST    /cockpit/api/import-plan/preview            CSRF: yes  — Preview a planning spreadsheet/CSV as DATA. Never executes anything.
GET     /cockpit/api/inbox                          CSRF: no
GET     /cockpit/api/investigate/<site>             CSRF: no
GET     /cockpit/api/lessons                        CSRF: no
GET     /cockpit/api/live/change                    CSRF: no   — One live change (?id=): before/after learned+scoring, rollback-preview. Read-only.
GET     /cockpit/api/live/grants                    CSRF: no   — Per-site grant table with current tier/trust and participation_eligible. Read-only.
GET     /cockpit/api/live/pending                   CSRF: no   — Pending live changes with the learned/scoring change flags and deadline. Read-only.
GET     /cockpit/api/live/status                    CSRF: no   — Live-apply summary: pending live changes, next deadline, grants active/suspended,
GET     /cockpit/api/maturity                       CSRF: no
GET     /cockpit/api/mission                        CSRF: no
GET     /cockpit/api/narrative                      CSRF: no
POST    /cockpit/api/notes                          CSRF: yes
GET     /cockpit/api/notes/<site>                   CSRF: no
GET     /cockpit/api/notifications                  CSRF: no
GET     /cockpit/api/novnc                          CSRF: no
GET     /cockpit/api/oracle/eligibility             CSRF: no   — The per-site eligibility matrix (tiers; automation-eligible: none). Read-only.
GET     /cockpit/api/oracle/held-out                CSRF: no   — Held-out vs training capture designation per site. Read-only.
GET     /cockpit/api/oracle/ineligible              CSRF: no   — Sites at Tier 0 / with hard failures + permanently-ineligible actions. Read-only.
GET     /cockpit/api/oracle/reports                 CSRF: no   — The assembled oracle report bundle (read-only data). Artifacts are written only
GET     /cockpit/api/oracle/status                  CSRF: no   — Oracle/eligibility summary: guardrails complete, tier distribution, 0
GET     /cockpit/api/oracle/verdict                 CSRF: no   — Per-site tiered oracle verdict (?site=). Read-only.
GET     /cockpit/api/org-health                     CSRF: no
GET     /cockpit/api/org-memory                     CSRF: no
GET     /cockpit/api/packet/<site>                  CSRF: no
GET     /cockpit/api/policy/audit                   CSRF: no   — The policy-change + kill-switch audit log (append-only). Read-only view.
GET     /cockpit/api/policy/matrix                  CSRF: no   — The 2-D autonomy policy matrix: per class — configured level, selectable
GET     /cockpit/api/policy/snapshots               CSRF: no   — Recorded immutable decision snapshots (empty in Phase A — nothing produces
GET     /cockpit/api/policy/status                  CSRF: no   — Compact governance status: policy version/hash, kill-switch state, guardrails
GET     /cockpit/api/portfolio-opportunity          CSRF: no
GET     /cockpit/api/portfolio-ranking              CSRF: no
GET     /cockpit/api/promotion/activity             CSRF: no   — Append-only governance-transition log (recent entries). Read-only.
GET     /cockpit/api/promotion/site                 CSRF: no   — Per-site transition history (?site=). Read-only.
GET     /cockpit/api/promotion/status               CSRF: no   — Transition totals + tracked fields. Read-only.
GET     /cockpit/api/queue                          CSRF: no
POST    /cockpit/api/queue                          CSRF: yes
POST    /cockpit/api/queue/launch                   CSRF: yes
POST    /cockpit/api/queue/reorder                  CSRF: yes
POST    /cockpit/api/queue/state                    CSRF: yes
GET     /cockpit/api/readiness                      CSRF: no
GET     /cockpit/api/report                         CSRF: no   — View one report (confined, redacted).
GET     /cockpit/api/reports                        CSRF: no
GET     /cockpit/api/resources                      CSRF: no
GET     /cockpit/api/review                         CSRF: no
GET     /cockpit/api/review-candidates              CSRF: no   — List normalized review candidates (templates/review_candidates/*.candidate.json):
GET     /cockpit/api/review-roi                     CSRF: no
GET     /cockpit/api/review/audit                   CSRF: no   — Unified decision audit across policy / housekeeping / guardrails. Read-only.
GET     /cockpit/api/review/dashboard               CSRF: no   — Outstanding reviews with evidence-chain pointers, soonest fail-closed deadline
POST    /cockpit/api/review/decide                  CSRF: yes
GET     /cockpit/api/review/diff                    CSRF: no   — Before/after + structured diff for a change (?change_id=). Read-only.
GET     /cockpit/api/review/evidence                CSRF: no   — Full evidence chain for a change (?change_id=): decision snapshot, diff, review
GET     /cockpit/api/review/rollback-preview        CSRF: no   — What rolling a change back would restore (?change_id=) — does not execute.
GET     /cockpit/api/risk                           CSRF: no
GET     /cockpit/api/rollback/center                CSRF: no   — Rollback Center dashboard: engine readiness, reverser registry, history counts,
GET     /cockpit/api/rollback/history               CSRF: no   — Recorded changes + which were reverted. Read-only.
GET     /cockpit/api/rollback/reversers             CSRF: no   — The registered-reverser registry — the change kinds that are reversible (and thus
GET     /cockpit/api/rollback/reversibility         CSRF: no   — Per-change reversibility (reverser registered AND not already rolled back).
POST    /cockpit/api/run-capture                    CSRF: yes
POST    /cockpit/api/run-report                     CSRF: yes
GET     /cockpit/api/saved-views                    CSRF: no
POST    /cockpit/api/saved-views                    CSRF: yes
POST    /cockpit/api/saved-views/delete             CSRF: yes
GET     /cockpit/api/search                         CSRF: no
POST    /cockpit/api/shell/close                    CSRF: yes
POST    /cockpit/api/shell/input                    CSRF: yes
POST    /cockpit/api/shell/open                     CSRF: yes
GET     /cockpit/api/shell/poll                     CSRF: no
POST    /cockpit/api/shell/signal                   CSRF: yes
GET     /cockpit/api/shell/status                   CSRF: no
GET     /cockpit/api/site/<site>                    CSRF: no
GET     /cockpit/api/staging/candidate              CSRF: no   — One site's staged candidate (?site=): evidence delta, behavioral-unchanged
GET     /cockpit/api/staging/candidates             CSRF: no   — Pending staged candidates + behavioral-unchanged check. Read-only.
GET     /cockpit/api/staging/status                 CSRF: no   — Staged-candidate summary: pending count, next deadline, eligible sites. Read-only.
GET     /cockpit/api/status                         CSRF: no   — Consolidated 'what is actually active' snapshot for the System Status
GET     /cockpit/api/structural-similarity          CSRF: no
POST    /cockpit/api/takeover/<sid>/input           CSRF: yes
GET     /cockpit/api/takeover/<sid>/screencast      CSRF: no
GET     /cockpit/api/task/<task_id>                 CSRF: no
GET     /cockpit/api/tasks                          CSRF: no
GET     /cockpit/api/template/autopilot             CSRF: no   — Operator-guided run for one URL/site (?target=): detect → templates →
GET     /cockpit/api/template/capture-intel         CSRF: no   — Per-capture quality/completeness/coverage + missing evidence. POSTURE-SAFE
GET     /cockpit/api/template/download-explain      CSRF: no   — Explain the download-candidate scoring decision (pure scorer, no live
GET     /cockpit/api/template/drift-intel           CSRF: no   — Drift intelligence aggregate: timeline + frequency + severity summary +
GET     /cockpit/api/template/family                CSRF: no   — One family's detail (?name=): shared selectors + cross-pollination
GET     /cockpit/api/template/family-intel          CSRF: no   — Overview of every inferred family: shared selectors/workflow/drift/failures.
GET     /cockpit/api/template/login-drift           CSRF: no   — Login drift signals (classified) + a safe login dry-run sample. Read-only;
GET     /cockpit/api/template/login-health          CSRF: no   — Per-site login-template health + session freshness + recent login history.
GET     /cockpit/api/template/login-review          CSRF: no   — Read-only login-template review queue (suggestions are data-only; the
GET     /cockpit/api/template/mission-control       CSRF: no   — The single operator screen: Needs Attention / Healthy / Active Work /
GET     /cockpit/api/template/playbook              CSRF: no   — Living dossier for one site (?site=). Read-only aggregation.
GET     /cockpit/api/template/playbook-index        CSRF: no   — Directory of every site's dossier-at-a-glance. Read-only.
GET     /cockpit/api/template/review-queue          CSRF: no   — Template review workbench feed: suggestions with before/after diffs,
GET     /cockpit/api/template/site-readiness        CSRF: no   — Per-site readiness ('can I trust this site today?') — DEFINED composite of
GET     /cockpit/api/template/unified-health        CSRF: no   — Unified video+login template health with stability + maturity + drift
GET     /cockpit/api/template/video-health          CSRF: no   — Per-site video/download template health + drift. Read-only.
GET     /cockpit/api/timeline                       CSRF: no
GET     /cockpit/api/trace/<entry_id>               CSRF: no
GET     /cockpit/api/trust/overview                 CSRF: no   — Per-site trust + current signal + eligibility + the below-minimum set. Read-only.
GET     /cockpit/api/trust/site                     CSRF: no   — Per-site trust detail (?site=): stored trust, current signal, what decay would
GET     /cockpit/api/trust/status                   CSRF: no   — Trust summary: min/baseline thresholds, below-min count, freeze state. Read-only.
GET     /cockpit/api/ui_prefs                       CSRF: no
POST    /cockpit/api/ui_prefs                       CSRF: yes
GET     /cockpit/api/validation/overview            CSRF: no   — Per-site re-validation schedule + status counts. Read-only.
GET     /cockpit/api/validation/site                CSRF: no   — Per-site re-validation detail (?site=): evidence age, status, recommended date.
GET     /cockpit/api/validation/status              CSRF: no   — Re-validation summary: interval/floor, due-soon/overdue/never counts. Read-only.
GET     /cockpit/api/warehouse                      CSRF: no
GET     /cockpit/reports                            CSRF: no   — Read-only Report Center. NEEDS OPERATOR CLICK-THROUGH VALIDATION.
GET     /cockpit/reports/capture_diagnostics        CSRF: no   — Read-only Capture Diagnostics panel (A4). Renders the three-axis verdict
GET     /cockpit/reports/dom_recorder_status        CSRF: no   — Read-only DOM Recorder Status panel (G4). Surfaces vendored asset
GET     /cockpit/reports/replay_validation          CSRF: no   — Read-only Replay Validation panel (A8). Renders headless rrweb
GET     /cockpit/reports/site_health                CSRF: no   — Read-only Site Health + Failure Clusters panel (F2-a / F2.1 + F2.2).
GET     /cockpit/reports/system_status              CSRF: no   — Read-only System Status panel (G7/G9). Renders browser/Cloak backend
GET     /cockpit/reports/vpn_secrets_status         CSRF: no   — Read-only VPN + Secrets status panel. Presence/state only — tunnel count,
GET     /cockpit/reports/workflow_analytics         CSRF: no   — Read-only Workflow Analytics panel (G5). Surfaces A6-1 derived_steps
GET     /cockpit/settings                           CSRF: no
GET     /cockpit/settings/secrets                   CSRF: no
GET     /cockpit/settings/site/<sid>                CSRF: no
GET     /cockpit/template-manager                   CSRF: no   — Read-only Template Manager page. NEEDS OPERATOR CLICK-THROUGH VALIDATION.
GET     /fleet/                                     CSRF: no
GET     /fleet/api/summary.json                     CSRF: no
GET     /framework/                                 CSRF: no
GET     /framework/api/cockpit.json                 CSRF: no   — Read-only JSON passthrough of the cockpit (for any custom frontend).
GET     /framework/report/<path:name>               CSRF: no
GET     /icon.svg                                   CSRF: no   — Same icon as the favicon — bundled inline so we don't need a static dir.
GET     /m                                          CSRF: no
GET     /m/                                         CSRF: no
GET     /m/ops                                      CSRF: no
GET     /m/ops/                                     CSRF: no
GET     /m2                                         CSRF: no   — Phase 1 root flip (v3.66.203): the D3 SPA moved from /m2 to `/`
GET     /m2/                                        CSRF: no   — Phase 1 root flip (v3.66.203): the D3 SPA moved from /m2 to `/`
GET     /m2/<path:subpath>                          CSRF: no   — Phase 1 root flip (v3.66.203): the D3 SPA moved from /m2 to `/`
GET     /manifest.json                              CSRF: no
GET     /metrics                                    CSRF: no   — Prometheus text-format exposition. Scrape-friendly; no auth.
GET     /screenshots/<path:filename>                CSRF: no
GET     /static/<path:filename>                     CSRF: no
GET     /stream/<token>                             CSRF: no   — Serve a video file with HTTP range support, gated by token.
GET     /sw.js                                      CSRF: no
```
